# Fourier Cosine Series

## Table of Contents

- [Introduction](#introduction)
- [Understanding the Even Extension of a Function](#understanding-the-even-extension-of-a-function)
- [Computing the Constant Term From the Half-Range Formula](#computing-the-constant-term-from-the-half-range-formula)
- [Computing the Cosine Coefficients From the Half-Range Formula](#computing-the-cosine-coefficients-from-the-half-range-formula)

## Prerequisites

- [Properties of Fourier Series](../6709/6709.md)

---

<a id="introduction"></a>
## Introduction

In the earlier topics, we saw that *symmetry* can force many Fourier coefficients to be zero automatically.
In particular, if a $2\pi$-periodic function is *even*, then its Fourier series has *cosine terms only*.

A **Fourier cosine series** uses this idea when a function is initially given only on the half-interval $[0,\pi)$, but we want a full Fourier series on $[-\pi,\pi)$ (and then a $2\pi$-periodic extension to all real $x$).

To do this, we start with a function $F(x)$ on $[0,\pi)$ and define its **even extension** $f(x)$ to $(-\pi,\pi)$ by
$f(x)=\begin{cases}F(x), & x \in [0,\pi),\\ F(-x), & x \in (-\pi,0).\end{cases}$

For example, a function $F(x)$, defined on $[0,1)$, and its even extension $f(x)$ to $x \in (-1,1)$ are shown below.

![](Source/Images/a60be008a552f0c8de9c07a97fc1b3be.png)

By construction, $f$ is an *even* function, meaning

$$
f(-x)=f(x)
$$

. Because $f$ is even, its Fourier series on $[-\pi,\pi)$ has *cosine terms only*:

$$
f(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty} a_n\cos(nx)
$$

Since $f(x)$ is even and $\cos(nx)$ is even for any

$$
n\ge 1
$$

the product $f(x)\cos(nx)$ is even too. Therefore, the cosine coefficients can be computed using the *half-range formulas*:

$$
\begin{aligned}
a_{0} &= \frac{1}{π}\int_{- π}^{π}f(x)dx \\
&= \frac{1}{π} \cdot 2\int_{0}^{π}f(x)dx \\
&= \frac{2}{π}\int_{0}^{π}f(x)dx
\end{aligned}
$$

and for

$$
n\ge 1
$$

$$
\begin{aligned}
a_{n} &= \frac{1}{π}\int_{- π}^{π}f(x)\cos (nx)dx \\
&= \frac{1}{π} \cdot 2\int_{0}^{π}f(x)\cos (nx)dx \\
&= \frac{2}{π}\int_{0}^{π}f(x)\cos (nx)dx
\end{aligned}
$$

So in practice, the workflow is the following:

1. Start with $F$ on $[0,\pi)$ and form the even extension $f$ on $(-\pi,\pi)$.
2. Compute $a_0$ and $a_n$ from the half-range integrals on $[0,\pi)$.
3. Write the Fourier cosine series of $f$.

Let's see some concrete examples.

---

<a id="understanding-the-even-extension-of-a-function"></a>
## Understanding the Even Extension of a Function

**Example:** Let $F(x)=x^3 - x + 5\cos x$ for $x\in[0,\pi)$, and let $f(x)$ be the **even** extension of $F(x)$ to $x \in (-\pi,\pi)$. What is $f(x)$ for $x\in(-\pi,0)$?

**Explanation**

If $F(x)$ is defined on $x\in[0,\pi)$, the even extension of $F$ is the function $f$ such that
$f(x)=\begin{cases}F(x), & x \in [0,\pi),\\ F(-x), & x \in (-\pi,0).\end{cases}$

In our cases, for $x\in(-\pi,0)$, we get

$$
\begin{aligned}
f(x) &= F(-x) \\
&= (-x)^{3} - (-x) + 5\cos (-x) \\
&=-x^{3} + x + 5\cos x
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-340794
content: |-
  Let $F(x) = x^{2} - 2x$ for $x \in [0,\pi)$, and let $f(x)$ be the **even** extension of $F(x)$ to $x \in (-\pi,\pi)$. What is $f(x)$ for $x \in (-\pi,0)$?
options:
- id: a
  content: |-
    $\mid - x^{2} + 2x \mid$
- id: b
  content: |-
    $-x^{2} - 2x$
- id: c
  content: |-
    $\left|x^{2}+2x\right|$
- id: d
  content: |-
    $-x^{2} + 2x$
- id: e
  content: |-
    $x^{2} + 2x$
  correct: true
```

---

**Question 2:**

```quiz
type: free
id: ma-340802
content: |-
  Let $F(x) = xe^{2x^{2}}$ for $x \in [0,\pi)$, and let $f(x)$ be the **even** extension of $F(x)$ to $x \in (-\pi,\pi)$. What is the value of $f(-1)$?$f(-1) =$
  $\underline{\hspace{1.5cm}}$
correct: >-
  $e^2$
```

---

<a id="computing-the-constant-term-from-the-half-range-formula"></a>
## Computing the Constant Term From the Half-Range Formula

**Example:** $f(x) \sim \dfrac{a_0}{2} + \sum_{n=1}^\infty a_n \cos(nx)$
Let $F(x)=\sin\left(\dfrac{3x}{4}\right)$ for $x\in[0,\pi)$. The Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of $F(x)$ is shown above. Find the expression for $a_0$.

**Explanation**

For our Fourier cosine series, the constant term is given by

$$
a_0=\frac{2}{\pi}\int_0^\pi f(x)\,\text{d}x
$$

In our case,

$$
f(x)=\sin\left(\dfrac{3x}{4}\right)
$$

for $x \in (0,\pi)$. So, substituting into the formula, we get

$$
\begin{aligned}
a_{0} &= \frac{2}{π}\int_{0}^{π}\sin (\frac{3x}{4})dx \\
&= \frac{2}{π}[- \frac{4}{3}\cos (\frac{3x}{4})]_{0}^{π} \\
&=-\frac{8}{3π}(\cos (\frac{3π}{4}) - \cos (0)) \\
&=-\frac{8}{3π}(-\frac{\sqrt{2}}{2} - 1) \\
&= \frac{4(2+\sqrt{2})}{3\pi}
\end{aligned}
$$

---

**Question 3:**

```quiz
type: free
id: ma-340814
content: |-
  $f(x)\sim\frac{a_{0}}{2} + \sum_{n=1}^{\infty}a_{n}\cos(nx)$
  Let $F(x) = 3x^{2} + 2$ for $x \in [0,\pi)$. The Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of $F(x)$ is shown above. Find the expression for $a_{0}$.$a_{0} =$ $\underline{\hspace{1.5cm}}$
correct: >-
  $2\pi^2+4$
```

---

**Question 4:**

```quiz
type: free
id: ma-340815
content: |-
  $f(x)\sim\frac{a_{0}}{2} + \sum_{n=1}^{\infty}a_{n}\cos(nx)$
  Let $F(x) =-\sin (\frac{x}{3})$ for $x \in [0,\pi)$. The Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of $F(x)$ is shown above. Find the expression for $a_{0}$.$a_{0} =$ $\underline{\hspace{1.5cm}}$
correct: >-
  $-\frac{3}{\pi}$
```

---

<a id="computing-the-cosine-coefficients-from-the-half-range-formula"></a>
## Computing the Cosine Coefficients From the Half-Range Formula

**Example:** Let $F(x)=-6x-2$ for $x\in[0,\pi)$. Find the missing parts in the Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of the function $F(x)$.

$XX_{X}^{X}$

**Explanation**

For our Fourier cosine series, the coefficients are given by

$$
a_n=\frac{2}{\pi}\int_0^\pi f(x)\cos(nx)\,\text{d}x
$$

In our case,

$$
f(x)=-6x-2
$$

for $x \in (0,\pi)$. So, substituting into the formula, we get

$$
a_n = \dfrac{2}{\pi}\int_0^\pi (-6x-2) \cos(nx)\,\text{d}x
$$

We use the integration by parts:

$$
\begin{bmatrix}u = -6x - 2 & \Rightarrow u' = -6 \\ v' = \cos(nx) & \Rightarrow v = \frac{1}{n}\sin(nx)\end{bmatrix}
$$

Thus, we obtain

$$
\begin{aligned}
a_{n} &= \frac{2}{\pi}\left(\left.uv\right|_{0}^{\pi} - \int_{0}^{\pi}vu'\,dx\right) \\
&= \frac{2}{\pi}\left(\frac{1}{n}[(-6x - 2)\sin(nx)]_{0}^{\pi} + \frac{6}{n}\int_{0}^{\pi}\sin(nx)\,dx\right) \\
&= \frac{2}{n\pi}\left(((-6\pi - 2)\sin(n\pi) + 2\sin(0)) - \frac{6}{n}[\cos(nx)]_{0}^{\pi}\right) \\
&= \frac{2}{n\pi}\left(0 - \frac{6}{n}(\cos(n\pi) - \cos(0))\right) \\
&= \frac{12(1 - \cos(n\pi))}{n^{2}\pi}
\end{aligned}
$$

Therefore, since

$$
\cos(n\pi)=(-1)^n
$$

we have

$$
a_n = \dfrac{12(1-(-1)^n)}{\pi n^2}
$$

Finally, the Fourier cosine series is

$$
f(x) \sim (-2-3\pi) + \sum_{n=1}^\infty \bbox[4pt, border: solid 1pt gainsboro]{\dfrac{12(1-(-1)^n)}{\pi n^2}} \cdot \bbox[4pt, border: solid 1pt gainsboro]{\cos(nx)}
$$

---

**Question 5:**

```quiz
type: select
id: ma-340810-select-1
content: |-
  $f(x)\sim\frac{a_{0}}{2} + \sum_{n=1}^{\infty}a_{n}\cos(nx)$
  Let $F(x) =-5x$ for $x \in [0,\pi)$. The Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of $F(x)$ is shown above. Find the expression for $a_{n}$.$a_{n} =$ $\underline{\hspace{2cm}}$
  for $n \ge 1$
options:
- id: select-1-option-1
  content: |-
    $(2(1 - (-1)^{n}))/(πn)$
- id: select-1-option-2
  content: |-
    $(5(1 - (-1)^{n}))/(4πn)$
- id: select-1-option-3
  content: |-
    $(10(1 + (-1)^{n}))/(πn)$
- id: select-1-option-4
  content: |-
    $(5(1 + (-1)^{n}))/(πn^{2})$
- id: select-1-option-5
  content: |-
    $(10(1 - (-1)^{n}))/(πn^{2})$
questions:
- id: select-1
  content: |-
    Select 1
  correct_option: select-1-option-5
```

---

**Question 6:**

```quiz
type: select
id: ma-340812-select-1
content: |-
  Let $F(x) = 2x + 3$ for $x \in [0,\pi)$. Select the correct missing parts in the Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of the function $F(x)$ below.$(4(-1)^{n})/(\pi n^{2})$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
options:
- id: select-1-option-1
  content: |-
    $(4(-1)^{n})/(πn^{2})$
- id: select-1-option-2
  content: |-
    $(2(-1)^{n})/(πn)$
- id: select-1-option-3
  content: |-
    $(2(1 - (-1)^{n}))/(πn)$
- id: select-1-option-4
  content: |-
    $(4((-1)^{n} - 1))/(πn^{2})$
questions:
- id: select-1
  content: |-
    Select 1
  correct_option: select-1-option-4
```

```quiz
type: select
id: ma-340812-select-2
content: |-
  Let $F(x) = 2x + 3$ for $x \in [0,\pi)$. Select the correct missing parts in the Fourier cosine series of the **even** $2\pi$-periodic extension $f(x)$ of the function $F(x)$ below.$(4(-1)^{n})/(\pi n^{2})$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
options:
- id: select-2-option-1
  content: |-
    $\cos (nx)$
- id: select-2-option-2
  content: |-
    $\cos (πx)$
- id: select-2-option-3
  content: |-
    $\sin (nx)$
- id: select-2-option-4
  content: |-
    $\sin (πx)$
questions:
- id: select-2
  content: |-
    Select 2
  correct_option: select-2-option-1
```
