# Fourier Sine Series

## Table of Contents

- [Introduction](#introduction)
- [Understanding the Odd Extension of a Function](#understanding-the-odd-extension-of-a-function)
- [Computing the Sine Coefficients From the Half-Range Formula](#computing-the-sine-coefficients-from-the-half-range-formula)

## Prerequisites

- [Applying the Integration By Parts Twice](../416/416.md)
- [Properties of Fourier Series](../6709/6709.md)

---

<a id="introduction"></a>
## Introduction

In the earlier topics, we saw that *symmetry* can force many Fourier coefficients to be zero automatically.
In particular, if a $2\pi$-periodic function is *odd*, then its Fourier series has *sine terms only*.

A **Fourier sine series** uses this idea when a function is initially given only on the half-interval, say, $[0,\pi)$, but we want a full Fourier series on $[-\pi,\pi)$ (and then a $2\pi$-periodic extension to all real $x$).

To do this, we start with a function $F(x)$ on $[0,\pi)$ and define its **odd extension** $f(x)$ to $(-\pi,\pi)$ by

$$
f(x)=
\begin{cases}
F(x), & x \in [0,\pi), \\
-F(-x), & x \in (-\pi,0).
\end{cases}
$$

For example, a function $F(x)$, defined on $x \in [0,1)$, and its odd extension $f(x)$ to $x \in (-1,1)$ are shown below.

![](Source/Images/ea7327f1733f126b31b7c13f8a7f7a13.png)

By construction, $f$ is an *odd* function, meaning

$$
f(-x)=-f(x)
$$

Because $f$ is odd, its Fourier series on $[-\pi,\pi)$ has *sine terms only*:

$$
f(x)\sim \sum_{n=1}^{\infty} b_n\sin(nx)
$$

Since $f(x)$ is odd and $\sin(nx)$ is odd for any

$$
n \geq 1
$$

then $f(x)\sin(nx)$ must be even.
So, the sine coefficients can be computed using the *half-range formula*:

$$
\begin{aligned}
b_{n} &= \frac{1}{π}\int_{- π}^{π}f(x)\sin (nx)dx \\
&= \frac{1}{π} \cdot 2\int_{0}^{π}f(x)\sin (nx)dx \\
&= \frac{2}{π}\int_{0}^{π}f(x)\sin (nx)dx
\end{aligned}
$$

So in practice, the workflow is the following:

1. Start with $F$ on $[0,\pi)$ and form the odd extension $f$ on $(-\pi,\pi)$.
2. Compute $b_n$ from the half-range integral on $[0,\pi)$.
3. Write the Fourier sine series of $f$.

Let's see some concrete examples.

---

<a id="understanding-the-odd-extension-of-a-function"></a>
## Understanding the Odd Extension of a Function

**Example:** Let $F(x)=x^2+10x$ for $x\in[0,6)$ and let $f(x)$ be the **odd** extension of $F(x)$ to $x \in (-6,6)$. What is the value of $f(-1)$?

**Explanation**

If $F(x)$ is defined on $x\in[0,6)$ the odd extension of $F$ is the function $f$ such that

$$
f(x)=
\begin{cases}
F(x), & x \in [0,6), \\
-F(-x), & x \in (-6,0).
\end{cases}
$$

In our case, for $x\in(-6,0)$, we get

$$
\begin{aligned}
f(x) &= -F(-x) \\
&= - ((-x)^{2} + 10(-x)) \\
&= - (x^{2} - 10x) \\
&=-x^{2} + 10x
\end{aligned}
$$

Therefore, we have

$$
\begin{aligned}
f(-1) &= - (-1)^{2} + 10(-1) =-11
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-338784
content: |-
  Let $F(x) = x^{2} - 2x$ for $x \in [0,\pi)$ and let $f(x)$ be the **odd** extension of $F(x)$ to $x \in (-\pi,\pi)$. What is $f(x)$ for $x \in (-\pi,0)$?
options:
- id: a
  content: |-
    $x^{2}+2x$
- id: b
  content: |-
    $\mid - x^{2} + 2x \mid$
- id: c
  content: |-
    $-x^{2} + 2x$
- id: d
  content: |-
    $x^{2} - 2x$
- id: e
  content: |-
    $-x^{2} - 2x$
  correct: true
```

---

**Question 2:**

```quiz
type: free
id: ma-338801
content: |-
  Let $F(x) = xe^{2x^{2}}$ for $x \in [0,\pi)$ and let $f(x)$ be the **odd** extension of $F(x)$ to $x \in (-\pi,\pi)$. What is the value of $f(-1)$? $f(-1) =$
  $\underline{\hspace{1.5cm}}$
correct: >-
  $-e^2$
```

---

<a id="computing-the-sine-coefficients-from-the-half-range-formula"></a>
## Computing the Sine Coefficients From the Half-Range Formula

**Example:** Let $F(x)=4x$ for $x\in[0,\pi)$. Find the missing parts in the Fourier sine series of the **odd** $2\pi$-periodic extension $f(x)$ of the function $F(x)$ below.

$AAAAA_{A}^{A}$

**Explanation**

For our Fourier sine series, the coefficients are given by

$$
b_n=\frac{2}{\pi}\int_0^\pi f(x)\sin(nx)\,\text{d}x
$$

In our case,

$$
f(x)=4x
$$

for $x \in (0,\pi)$. So, substituting into the formula, we get

$$
b_n = \dfrac{8}{\pi}\int_0^\pi x \sin(nx)\,\text{d}x
$$

We use the integration by parts:

$$
\begin{bmatrix}u = x & ⇒u^{′} = 1 \\ v^{′} = \sin (nx) & ⇒v =-\frac{1}{n}\cos (nx)\end{bmatrix}
$$

Thus, we obtain

$$
\begin{aligned}
b_{n} &= \left.\frac{8}{π}(uv\right|_{0}^{π} - \int_{0}^{π}vu^{′}dx) \\
&= \frac{8}{π}(-\frac{1}{n}[x\cos (nx)]_{0}^{π} + \frac{1}{n}\int_{0}^{π}\cos (nx)dx) \\
&=-\frac{8}{nπ}((π \cdot \cos (nπ) - 0 \cdot \cos (0)) - \frac{1}{n}[\sin (nx)]_{0}^{π}) \\
&=-\frac{8}{nπ}(π\cos (nπ) - \frac{1}{n}(\sin (nπ) - \sin (0))) \\
&= - (8\cos (nπ))/(n)
\end{aligned}
$$

Therefore, since

$$
\cos(n\pi)=(-1)^n
$$

we have

$$
b_n = -\dfrac{8(-1)^n}{n}=\dfrac{8(-1)^{n+1}}{n}
$$

Finally, the Fourier sine series is

$$
f(x) \sim \sum_{n=1}^\infty \dfrac{8(-1)^{n+1}}{n} \cdot \sin(nx)
$$

---

**Question 3:**

```quiz
type: select
id: ma-340708-select-1
content: |-
  $f(x)\sim\sum_{n=1}^{\infty} b_{n}\sin(nx)$
  Let $F(x) = 2$ for $x \in [0,\pi)$. The Fourier sine series of the **odd** $2\pi$-periodic extension $f(x)$ of $F(x)$ is shown above. Find the expression for $b_{n}$.
  $b_{n} =$ $\underline{\hspace{2cm}}$
options:
- id: select-1-option-1
  content: |-
    $(2(1 - (-1)^{n}))/(πn)$
- id: select-1-option-2
  content: |-
    $(4(1 - (-1)^{n}))/(πn)$
- id: select-1-option-3
  content: |-
    $(4(1 + (-1)^{n}))/(πn)$
- id: select-1-option-4
  content: |-
    $((1 - (-1)^{n}))/(4πn)$
- id: select-1-option-5
  content: |-
    $(2(1 + (-1)^{n}))/(πn)$
questions:
- id: select-1
  content: |-
    Select 1
  correct_option: select-1-option-2
```

---

**Question 4:**

```quiz
type: select
id: ma-340629-select-1
content: |-
  Let $F(x) =-5x$ for $x \in [0,\pi)$. Select the correct missing parts in the Fourier sine series of the **odd** $2\pi$-periodic extension $f(x)$ of the function $F(x)$ below. $(2(-1)^{n})/(n)$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
options:
- id: select-1-option-1
  content: |-
    $(2(-1)^{n})/(n)$
- id: select-1-option-2
  content: |-
    $(2(1 - (-1)^{n}))/(πn)$
- id: select-1-option-3
  content: |-
    $(10(-1)^{n})/(n)$
- id: select-1-option-4
  content: |-
    $(10(1 - (-1)^{n}))/(πn)$
questions:
- id: select-1
  content: |-
    Select 1
  correct_option: select-1-option-3
```

```quiz
type: select
id: ma-340629-select-2
content: |-
  Let $F(x) =-5x$ for $x \in [0,\pi)$. Select the correct missing parts in the Fourier sine series of the **odd** $2\pi$-periodic extension $f(x)$ of the function $F(x)$ below. $(2(-1)^{n})/(n)$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
options:
- id: select-2-option-1
  content: |-
    $\cos (nx)$
- id: select-2-option-2
  content: |-
    $\sin (πx)$
- id: select-2-option-3
  content: |-
    $\sin (nx)$
- id: select-2-option-4
  content: |-
    $\cos (πx)$
questions:
- id: select-2
  content: |-
    Select 2
  correct_option: select-2-option-3
```
