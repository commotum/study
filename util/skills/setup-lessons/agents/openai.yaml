interface:
  display_name: "Setup Lessons"
  short_description: "Populate MA lessons and refresh course indices"
  default_prompt: "Use $setup-lessons on this assignment markdown file to match each problem to Math Academy lessons, copy the needed lesson files and sources, normalize local names, and refresh the course progress indices."
