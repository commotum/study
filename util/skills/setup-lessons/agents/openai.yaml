interface:
  display_name: "Setup Lessons"
  short_description: "Populate MA lessons with core-move fallbacks"
  default_prompt: "Use $setup-lessons on this assignment markdown file to copy genuine Math Academy matches, generate targeted core-move fallbacks for unmatched problems, and refresh the course progress indices."
