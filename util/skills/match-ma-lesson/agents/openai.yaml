interface:
  display_name: "Match MA Lesson"
  short_description: "Verify MA matches and flag unmatched problems"
  default_prompt: "Use $match-ma-lesson to verify whether this problem has a genuine Math Academy equivalent and flag it for a core-move fallback when it does not."
