interface:
  display_name: "Skeleton"
  short_description: "Create assignment skeleton folders"
  default_prompt: "Use $skeleton to create a new assignment folder from a course folder, document name, and problem count."
