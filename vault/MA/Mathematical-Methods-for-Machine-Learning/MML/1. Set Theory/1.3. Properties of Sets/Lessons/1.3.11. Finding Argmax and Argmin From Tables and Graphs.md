# Finding Argmax and Argmin From Tables and Graphs

<!--
lesson-id: 6824
topic-code: MML.1.3.11
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding Argmax and Argmin From a Table](#finding-argmax-and-argmin-from-a-table)
- [Reading Argmax and Argmin From a Table](#reading-argmax-and-argmin-from-a-table)
- [Finding Argmax and Argmin From a Graph](#finding-argmax-and-argmin-from-a-graph)
- [Reading Argmax and Argmin From a Graph](#reading-argmax-and-argmin-from-a-graph)

## Prerequisites

- [Argmax and Argmin Notation](<1.3.10. Argmax and Argmin Notation.md>)

---

<a id="introduction"></a>
## Introduction

In a previous lesson, we learned that $\max$ and $\min$ return the largest and smallest *function values,* while $\arg\max$ and $\arg\min$ return the corresponding *input values.*

In this lesson, we practice finding $\max$, $\arg\max$, $\min$, and $\arg\min$ from common representations of a function:

- Tables of values
- Graphs

---

<a id="finding-argmax-and-argmin-from-a-table"></a>
## Finding Argmax and Argmin From a Table

When a table lists input-output pairs $(x,f(x))$ for $x$ in some set $S$, we can read:

- $\max_{x \in S} f(x)$ by finding the largest output value in the table.
- $\arg\max_{x \in S} f(x)$ by taking the input value where that largest output occurs.

The same idea applies to $\min$ and $\arg\min$, except we look for the smallest output value.

---

<a id="reading-argmax-and-argmin-from-a-table"></a>
## Reading Argmax and Argmin From a Table

**Example:** The table above gives values of $f$ on the set $S=\{-2,-1,0,1,2\}$. Use the table to compute
$\max_{x \in S} f(x),\qquad \arg\max_{x \in S} f(x),\qquad \min_{x \in S} f(x),\qquad \arg\min_{x \in S} f(x)$.

| $x$ | $f(x)$ |
| --- | ---: |
| $-2$ | $3$ |
| $-1$ | $0$ |
| $0$ | $5$ |
| $1$ | $-1$ |
| $2$ | $2$ |

**Explanation**

Recall that

- $\max\limits_{x \in S} f(x)$ is the maximum output value, while
- $\arg\max\limits_{x \in S} f(x)$ is an input in $S$ where that maximum output occurs.

On the other hand,

- $\min\limits_{x \in S} f(x)$ is the minimum output value, while
- $\arg\min\limits_{x \in S} f(x)$ is an input in $S$ where that minimum output occurs.

The output values in the table are $3,0,5,-1$, and $2$.

The largest output is $5$, and it occurs at $x=0$. Therefore,

$$
\max_{x \in S} f(x)=5 \qquad \textrm{and} \qquad \arg\max_{x \in S} f(x)=0
$$

The smallest output is $-1$, and it occurs at $x=1$. Therefore,

$$
\min_{x \in S} f(x)=-1 \qquad \textrm{and} \qquad \arg\min_{x \in S} f(x)=1
$$

---

**Question 1**

| $t$ | $g(t)$ |
| --- | ---: |
| $-2$ | $3$ |
| $-1$ | $-4$ |
| $1$ | $0$ |
| $2$ | $2$ |

The table above gives values of a function $g$ on the set $T = {-2,-1, 1, 2}$. Compute the following:

$max_(t ∈ T)g(t) =$
$\underline{\hspace{1.5cm}}$ $\arg max_(t ∈ T)g(t) =$
$\underline{\hspace{1.5cm}}$ $min_(t ∈ T)g(t) =$
$\underline{\hspace{1.5cm}}$ $\arg min_(t ∈ T)g(t) =$
$\underline{\hspace{1.5cm}}$

---

**Question 2**

| $t$ | $h(t)$ |
| --- | ---: |
| $-3$ | $2$ |
| $-1$ | $-1$ |
| $0$ | $-5$ |
| $2$ | $4$ |
| $5$ | $0$ |

The table above gives values of a function $h$ on the set $T = {-3,-1, 0, 2, 5}$. Compute the following:

$max_(t ∈ T)h(t) =$
$\underline{\hspace{1.5cm}}$ $\arg max_(t ∈ T)h(t) =$
$\underline{\hspace{1.5cm}}$ $min_(t ∈ T)h(t) =$
$\underline{\hspace{1.5cm}}$ $\arg min_(t ∈ T)h(t) =$
$\underline{\hspace{1.5cm}}$

---

<a id="finding-argmax-and-argmin-from-a-graph"></a>
## Finding Argmax and Argmin From a Graph

When a graph shows the points $(x,f(x))$ for $x$ in a set $S$, we can find:

- $\max_{x \in S} f(x)$ by locating the highest point on the graph and reading its $y$-coordinate.
- $\arg\max_{x \in S} f(x)$ by taking the corresponding $x$-coordinate.

The same idea applies to $\min$ and $\arg\min$, except we look for the lowest point.

---

<a id="reading-argmax-and-argmin-from-a-graph"></a>
## Reading Argmax and Argmin From a Graph

**Example:** Let $S=\{0,1,2,3,4\}$. The graph above shows the value of $f(x)$ for each $x \in S$. Use the graph to compute
$\max_{x \in S} f(x),\qquad \arg\max_{x \in S} f(x),\qquad \min_{x \in S} f(x),\qquad \arg\min_{x \in S} f(x)$.

![](<../Source/1.3.11. Finding Argmax and Argmin From Tables and Graphs/Images/26a1591c0494ecc2b22fd0a97160642a.png>)

**Explanation**

Recall that

- $\max\limits_{x \in S} f(x)$ is the maximum output value, while
- $\arg\max\limits_{x \in S} f(x)$ is an input in $S$ where that maximum output occurs.

On the other hand,

- $\min\limits_{x \in S} f(x)$ is the minimum output value, while
- $\arg\min\limits_{x \in S} f(x)$ is an input in $S$ where that minimum output occurs.

From the graph, the highest point has $y$-coordinate $5$, and it occurs at $x=1$. Therefore,

$$
\max_{x \in S} f(x)=\bbox[4pt,border: 1px solid lightgray]{5} \qquad \textrm{and} \qquad \arg\max_{x \in S} f(x)=\bbox[4pt,border: 1px solid lightgray]{1}
$$

The lowest point has $y$-coordinate $0$, and it occurs at $x=4$. Therefore,

$$
\min_{x \in S} f(x)=\bbox[4pt,border: 1px solid lightgray]{0} \qquad \textrm{and} \qquad \arg\min_{x \in S} f(x)=\bbox[4pt,border: 1px solid lightgray]{4}
$$

---

**Question 3**

![](<../Source/1.3.11. Finding Argmax and Argmin From Tables and Graphs/Images/q-355211.png>)

Let $T = {-1, 0, 1, 2, 3}$. The graph above shows the value of $h(x)$ for each $x ∈ T$. Assuming that all the dots have integer coordinates, compute the following:

$max_(x ∈ T)h(x) =$
$\underline{\hspace{1.5cm}}$ $\arg max_(x ∈ T)h(x) =$
$\underline{\hspace{1.5cm}}$ $min_(x ∈ T)h(x) =$
$\underline{\hspace{1.5cm}}$ $\arg min_(x ∈ T)h(x) =$
$\underline{\hspace{1.5cm}}$

---

**Question 4**

![](<../Source/1.3.11. Finding Argmax and Argmin From Tables and Graphs/Images/q-355232.png>)

Let $T = {-3,-2, 0, 1, 3}$. The graph above shows the value of $g(x)$ for each $x ∈ T$. Assuming that all the dots have integer coordinates, compute the following:

$max_(x ∈ T)g(x) =$
$\underline{\hspace{1.5cm}}$ $\arg max_(x ∈ T)g(x) =$
$\underline{\hspace{1.5cm}}$ $min_(x ∈ T)g(x) =$
$\underline{\hspace{1.5cm}}$ $\arg min_(x ∈ T)g(x) =$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
