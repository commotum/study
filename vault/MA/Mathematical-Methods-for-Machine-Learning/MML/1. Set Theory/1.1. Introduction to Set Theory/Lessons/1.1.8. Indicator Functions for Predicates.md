# Indicator Functions for Predicates

<!--
lesson-id: 3218
topic-code: MML.1.1.8
-->

## Table of Contents

- [Introduction](#introduction)
- [Using Indicator Functions to Check Predicates](#using-indicator-functions-to-check-predicates)
- [Set Notation and Bracket Notation](#set-notation-and-bracket-notation)
- [Converting Indicator Notations](#converting-indicator-notations)
- [Using Indicator Functions in Expressions: Sums](#using-indicator-functions-in-expressions-sums)
- [Using Indicator Functions in Expressions: Piecewise Functions](#using-indicator-functions-in-expressions-piecewise-functions)
- [Using Indicator Functions in Expressions](#using-indicator-functions-in-expressions)

## Prerequisites

- [Indicator Functions](<1.1.7. Indicator Functions.md>)

---

<a id="introduction"></a>
## Introduction

Recall that the indicator function $\mathbf{1}_A(x)$ of a set $A$ equals $1$ if $x \in A$ and $0$ if $x \notin A$.
${1,\begin{vmatrix}if [math]x \in A[/math], \\ 0,\end{vmatrix}if [math]x \notin A[/math]$.

In this lesson, we introduce a more general notation that allows us to write indicator functions for any condition, not just set membership.

The **Iverson bracket notation** $\mathbf{1}[P]$ takes a predicate $P$ and returns
${1,\begin{vmatrix}if [math]P[/math] is true, \\ 0,\end{vmatrix}if [math]P[/math] is false$.

For example:

- $\mathbf{1}[3 > 2] = 1$ because $3 > 2$ is true.
- $\mathbf{1}[5 \leq 4] = 0$ because $5 \leq 4$ is false.
- $\mathbf{1}[7 \in \{2, 5, 7\}] = 1$ because $7$ is an element of $\{2, 5, 7\}$.

---

<a id="using-indicator-functions-to-check-predicates"></a>
## Using Indicator Functions to Check Predicates

**Example:** Evaluate the indicator function
$\mathbf{1}[9 > 7]$.

**Explanation**

The indicator function $\mathbf{1}[P]$ equals $1$ if the predicate $P$ is true and $0$ if $P$ is false.
${1,\begin{vmatrix}if [math]P[/math] is true, \\ 0,\end{vmatrix}if [math]P[/math] is false$.

Here, the predicate is $9 > 7$. Since $9$ is greater than $7$, the predicate is true. Therefore,

$$
\mathbf{1}[9 > 7] = 1
$$

---

**Question 1:**

```quiz
type: radio
id: ma-345424
content: |-
  Evaluate the indicator function

  $1[4 > 7]$.
options:
- id: a
  content: |-
    $0$
  correct: true
- id: b
  content: |-
    $1$
- id: c
  content: |-
    $4$
- id: d
  content: |-
    $-3$
- id: e
  content: |-
    $7$
```

---

**Question 2:**

```quiz
type: radio
id: ma-345425
content: |-
  Evaluate the indicator function

  $1[5 ∈ {2, 4, 5, 9}]$.
options:
- id: a
  content: |-
    $5$
- id: b
  content: |-
    $4$
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $20$
- id: e
  content: |-
    $1$
  correct: true
```

---

<a id="set-notation-and-bracket-notation"></a>
## Set Notation and Bracket Notation

The set-based notation from the previous lesson is a special case of Iverson bracket notation. Specifically,

$$
\mathbf{1}_A(x) = \mathbf{1}[x \in A]
$$

Both expressions equal $1$ when $x \in A$ and $0$ when $x \notin A$.

The Iverson bracket is more flexible because it works with any predicate, such as inequalities. For example:

$$
{1\begin{vmatrix}ifx < 0 \\ 0\end{vmatrix}ifx \ge 0
$$

---

<a id="converting-indicator-notations"></a>
## Converting Indicator Notations

**Example:** Let $H = \{-5, -3, -1, 1\}$. Which of the following is equivalent to $\mathbf{1}_H(0)$?

1. $\mathbf{1}[0 \in \{-5, -3, -1, 1\}]$
2. $\mathbf{1}[0 \notin \{-5, -3, -1, 1\}]$
3. $\mathbf{1}[H \in \{-5, -3, -1, 1\}]$
4. $\mathbf{1}[\{-5, -3, -1, 1\} \in 0]$
5. $\mathbf{1}[0 = H]$

**Explanation**

The indicator function $\mathbf{1}[P]$ equals $1$ if the predicate $P$ is true and $0$ if $P$ is false.
${1,\begin{vmatrix}if [math]P[/math] is true, \\ 0,\end{vmatrix}if [math]P[/math] is false$.

Recall that the indicator function $\mathbf{1}_H(x)$ of a set $H$ equals $1$ if $x \in H$ and $0$ if $x \notin H$. This can be written using Iverson bracket notation as

$$
\mathbf{1}_H(x) = \mathbf{1}[x \in H]
$$

Therefore, we have

$$
\mathbf{1}_H(0) = \mathbf{1}[0 \in H] = \mathbf{1}[0 \in \{-5, -3, -1, 1\}]
$$

---

**Question 3:**

```quiz
type: radio
id: ma-345426
content: |-
  Let $A = {1, 3, 5, 7}$. Which of the following is equivalent to $1_{A}(3)$?
options:
- id: a
  content: |-
    $1[3 ∉ {1, 3, 5, 7}]$
- id: b
  content: |-
    $1[3 = A]$
- id: c
  content: |-
    $1[3 ∈ {1, 3, 5, 7}]$
  correct: true
- id: d
  content: |-
    $1[A ∈ {1, 3, 5, 7}]$
- id: e
  content: |-
    $1[{1, 3, 5, 7} ∈ 3]$
```

---

**Question 4:**

```quiz
type: radio
id: ma-349167
content: |-
  Let $B = {2, 4, 6, 8}$. Which of the following is equivalent to $1[5 ∈ B]$?
options:
- id: a
  content: |-
    $1_{B}(5)$
  correct: true
- id: b
  content: |-
    $1_{5}(B)$
- id: c
  content: |-
    $1_{B}(6)$
- id: d
  content: |-
    $1_{{2, 4, 6, 8}}(B)$
- id: e
  content: |-
    $1_{5}(2)$
```

---

<a id="using-indicator-functions-in-expressions-sums"></a>
## Using Indicator Functions in Expressions: Sums

Indicator functions are useful for writing expressions that behave differently depending on whether a condition is met.

**Sums with indicator functions.** When we multiply a term by an indicator function inside a sum, the indicator acts as a filter. For example, given values $x_1, x_2, \ldots, x_n$, the sum

$$
\sum_{i=1}^{n} \mathbf{1}[x_i > 0] \cdot x_i
$$

adds up only the positive values. If $x_i > 0$, the indicator equals $1$ and the term contributes $x_i$. If

$$
x_i \leq 0
$$

the indicator equals $0$ and the term contributes nothing.

Consider the values

$$
x_1 = 5, x_2 = -3, x_3 = 2
$$

Using the formula:

$$
\begin{aligned}
∑_(i = 1)^(3)1[x_{i} > 0] \cdot x_{i} &= 1[5 > 0] \cdot 5 + 1[- 3 > 0] \cdot (-3) + 1[2 > 0] \cdot 2 \\
&= 1 \cdot 5 + 0 \cdot (-3) + 1 \cdot 2 \\
&= 5 + 0 + 2 \\
&= 7
\end{aligned}
$$

Similarly,

$$
\sum_{i=1}^{n} \mathbf{1}[x_i > 0]
$$

counts the positive values. For this data, the count is

$$
1 + 0 + 1 = 2
$$

---

<a id="using-indicator-functions-in-expressions-piecewise-functions"></a>
## Using Indicator Functions in Expressions: Piecewise Functions

Indicator functions can also express piecewise functions compactly. Consider the absolute value function:

$$
{x,\begin{vmatrix}if [math]x \geq 0[/math], \\ -x,\end{vmatrix}if [math]x < 0[/math]
$$

We can write this as a single expression using indicator functions:

$$
\mid x \mid = \mathbf{1}[x \geq 0] \cdot x + \mathbf{1}[x < 0] \cdot (-x)
$$

Since the conditions

$$
x \geq 0
$$

and $x < 0$ are mutually exclusive, exactly one indicator equals $1$ and the other equals $0$. This "switch" mechanism selects the appropriate term automatically.

Let's verify this expression for a negative number, say $x = -4{:}$

$$
\begin{aligned}
\mid - 4 \mid &= 1[- 4 \ge 0] \cdot (-4) + 1[- 4 < 0] \cdot (- (-4)) \\
&= 0 \cdot (-4) + 1 \cdot 4 \\
&= 4
\end{aligned}
$$

---

<a id="using-indicator-functions-in-expressions"></a>
## Using Indicator Functions in Expressions

**Example:** Consider the function
$f(x) = \mathbf{1}[x \geq 1] \cdot x^2 + \mathbf{1}[x < 1] \cdot (-x)$.
What is $f(-2)$?

**Explanation**

The indicator function $\mathbf{1}[P]$ equals $1$ if the predicate $P$ is true and $0$ if $P$ is false.
${1,\begin{vmatrix}if [math]P[/math] is true, \\ 0,\end{vmatrix}if [math]P[/math] is false$.

For $x = -2$, we evaluate each indicator function:

- $\mathbf{1}[x \geq 1] = \mathbf{1}[-2 \geq 1] = 0$ (since $-2 \geq 1$ is false)
- $\mathbf{1}[x < 1] = \mathbf{1}[-2 < 1] = 1$ (since $-2 < 1$ is true)

Substituting into the expression for $f(x)$, we get

$$
\begin{aligned}
f(-2) &= 1[- 2 \ge 1] \cdot (-2)^{2} + 1[- 2 < 1] \cdot (- (-2)) \\
&= 0 \cdot 4 + 1 \cdot 2 \\
&= 2
\end{aligned}
$$

Note that the indicator functions act as a "switch": since $x = -2 < 1$, the first term vanishes and only the second term contributes to the result.

---

**Question 5**

$x_{1} = 4, x_{2} =-2, x_{3} = 7, x_{4} =-5, x_{5} = 1$
Given the values $x_{1}, x_{2}, x_{3}, x_{4}, x_{5}$ above, we have$∑_(i = 1)^(5)1[x_{i} > 0] \cdot x_{i} =$ $\underline{\hspace{1.5cm}}$.

---

**Question 6:**

```quiz
type: radio
id: ma-345428
content: |-
  Consider the function

  $f(x) = 1[x \ge 0] \cdot x^{2} + 1[x < 0] \cdot (-x)$.
  What is $f(-3)$?
options:
- id: a
  content: |-
    $3$
  correct: true
- id: b
  content: |-
    $-3$
- id: c
  content: |-
    $9$
- id: d
  content: |-
    $-9$
- id: e
  content: |-
    $0$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
