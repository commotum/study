# Likelihood Functions for Continuous Probability Distributions

<!--
lesson-id: 4123
topic-code: MML.12.2.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Likelihood Functions for Continuous Random Variables](#likelihood-functions-for-continuous-random-variables)
- [Likelihood Functions for Exponential Random Variables](#likelihood-functions-for-exponential-random-variables)
- [The Normal Distribution](#the-normal-distribution)
- [Likelihood Functions for Normal Random Variables](#likelihood-functions-for-normal-random-variables)

## Prerequisites

- [The Normal Distribution](<../../../../PCL/11. Probability & Combinatorics/11.5. The Normal Distribution/Lessons/11.5.3. The Normal Distribution.md>)
- [The Exponential Distribution](<../../../10. Probability & Random Variables/10.6. Continuous Probability Distributions/Lessons/10.6.7. The Exponential Distribution.md>)
- [Likelihood Functions for Discrete Probability Distributions](<12.2.3. Likelihood Functions for Discrete Probability Distributions.md>)

---

<a id="introduction"></a>
## Introduction

Likelihood functions of samples drawn from populations modeled using continuous random variables are defined similarly to discrete variables. The only difference is that we use probability density functions instead of probability mass functions.

If $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed *continuous* random variables with probability density function $f(x; \theta)$, then the likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is given by

$$
L(\theta) = P(X_1=x_1, X_2=x_2, \ldots, X_n = x_n) = \displaystyle\prod_{i=1}^n f(x_i;\,\theta)
$$

For example, let $X_1, X_2, X_3$ be continuous, I.I.D. random variables with the following probability density function:

$$
f(x; \theta) = \theta x^{-\theta-1}, \qquad x \geq 1
$$

Here, $\theta$ is an unknown parameter. Suppose we conduct our random sample and get the following results:

$$
x_1 = 2.5, \qquad x_2 = 3.2, \qquad x_3 = 1.2
$$

In this case, we compute the likelihood function as follows:

$$
\begin{aligned}
L(θ) &= ∏_(i = 1)^(n)(θx_{i}^{- θ - 1}) \\
&= (θx_{1}^{- θ - 1}) \cdot (θx_{2}^{- θ - 1})⋯(θx_{n}^{- θ - 1}) \\
&= θ^{n}(x_{1}^{- θ - 1}) \cdot (x_{2}^{- θ - 1})⋯(x_{n}^{- θ - 1}) \\
&= θ^{n}(x_{1} \cdot x_{2}⋯x_{n})^{- θ - 1} \\
&= θ^{n}(∏_(i = 1)^(n)x_{i})^{- θ - 1}
\end{aligned}
$$

For our sample, we have $n=3$, and

$$
\begin{aligned}
∏_(i = 1)^(n)x_{i} &= 2.5 \cdot 3.2 \cdot 1.2 = 9.6
\end{aligned}
$$

Therefore,

$$
L(\theta) = \theta^{3} (9.6)^{-\theta-1}
$$

---

<a id="likelihood-functions-for-continuous-random-variables"></a>
## Likelihood Functions for Continuous Random Variables

**Example:** Let $X_1, X_2, X_3$ be I.I.D. random variables with probability density function
$f(x; \theta) = (\theta+1) x^{-\theta-2}, \qquad x \geq 1$,
where $\theta$ is an unknown parameter. Find the likelihood function of the sample $2.0, \, 4.5, \,5.1$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
L(\theta) = P(X_1=x_1, X_2=x_2, \ldots, X_n = x_n) = \displaystyle\prod_{i=1}^n f(x_i;\,\theta)
$$

$$
f(x; \theta) = (\theta+1) x^{-\theta-2}, \qquad x \geq 1
$$

We can calculate the likelihood function as follows:

$$
\begin{aligned}
L(θ) &= ∏_(i = 1)^(n)((θ + 1)x_{i}^{- θ - 2}) \\
&= (θ + 1)^{n} \cdot ∏_(i = 1)^(n)x_{i}^{- θ - 2} \\
&= (θ + 1)^{n}(∏_(i = 1)^(n)x_{i})^{- θ - 2}
\end{aligned}
$$

For our sample, we have that

$$
\begin{aligned}
∏_(i = 1)^(n)x_{i} &= 2.0 \cdot 4.5 \cdot 5.1 = 45.9
\end{aligned}
$$

and $n=3$. Therefore,

$$
L(\theta) = (\theta+1)^{3} (45.9)^{-\theta-2}
$$

---

**Question 1**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, …, X_{15}$ be I.I.D. random variables with probability density function
$f(x; θ) = θx^{- θ - 1}, x \ge 1$,
where $θ$ is an unknown parameter. For a particular sample $x_{1}, x_{2}, …, x_{15}$, you're given that
$∏_(i = 1)^(15)x_{i} = 40.6$.
Find the likelihood function of the sample.

- [ ] A. $40.6(16)^{- θ - 1}$
- [ ] B. $θ^{40.6}(15)^{- θ - 1}$
- [ ] C. $θ^{16}(40.6)^{- θ}$
- [ ] D. $15(40.6)^{- θ}$
- [ ] E. $θ^{15}(40.6)^{- θ - 1}$

---

**Question 2**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, X_{3}$ be I.I.D. random variables with probability density function
$f(x; θ) = θx^{- θ - 1}, x \ge 1$,
where $θ$ is an unknown parameter. Find the likelihood function of the sample $2.1, 3.2, 5.0$.

- [ ] A. $θ^{33.6}(4)^{- θ - 1}$
- [ ] B. $θ^{33.6}(3)^{- θ - 1}$
- [ ] C. $θ^{3}(33.6)^{- θ - 1}$
- [ ] D. $θ^{33.6}(4)^{- θ}$
- [ ] E. $θ^{4}(33.6)^{- θ}$

---

<a id="likelihood-functions-for-exponential-random-variables"></a>
## Likelihood Functions for Exponential Random Variables

**Example:** Suppose $X_1,X_2,\ldots,X_6$ is an I.I.D random sample with $X_i \sim \text{Exp}(\theta)$ and unknown rate parameter $\theta$. Find the likelihood function of the sample $1.5, \, 2.0, \, 1.1, \, 0.4, \, 3.3, \, 2.7$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
L(\theta) = P(X_1=x_1, X_2=x_2, \ldots, X_n = x_n) = \displaystyle\prod_{i=1}^n f(x_i;\,\theta)
$$

In our case, we are given a sample of $n=6$ independent exponential random variables with unknown parameter $\theta$, for which the probability density function is

$$
f(x; \,\theta) = \theta e^{-\theta x}, \qquad x\geq 0
$$

We can calculate the likelihood function as follows:

$$
\begin{aligned}
L(θ) &= ∏_(i = 1)^(n)θe^{- θx_{i}} \\
&= (θe^{- θx_{1}}) \cdot (θe^{- θx_{2}})⋯(θe^{- θx_{n}}) \\
&= θ^{n}e^{- θ(x_{1} + x_{2} + ⋯ + x_{n})} \\
&= θ^{n}e^{- θ∑x_{i}} \\
&= θ^{n}e^{-Sθ}
\end{aligned}
$$

where

$$
\displaystyle S = \sum\limits_ {i=1}^n x_i
$$

For our sample, we have that

$$
\begin{aligned}
S &= ∑_(i = 1)^(n)x_{i} \\
&= 1.5 + 2.0 + 1.1 + 0.4 + 3.3 + 2.7 \\
&= 11
\end{aligned}
$$

and $n=6$. Therefore,

$$
\begin{aligned}
L(θ) &= θ^{6}e^{-11θ}
\end{aligned}
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, …, X_{7}$ is an I.I.D random sample with $X_{i}∼Exp(θ)$ and unknown rate parameter $θ$. For a particular sample $x_{1}, x_{2}, …, x_{7}$, you're given that
$∑_(i = 1)^(7)x_{i} = 18.2$.

Find the likelihood function of the sample.

*Hint: The probability density function for an exponential random variable is given by*
$f(x; θ) = θe^{- θx}, x \ge 0$.

- [ ] A. $θ^{-7}e^{18.2θ}$
- [ ] B. $θ^{18.2}e^{-7θ}$
- [ ] C. $θ^{11}e^{-18.2θ}$
- [ ] D. $θ^{7}e^{-18.2θ}$
- [ ] E. $θ^{-18.2}e^{7θ}$

---

**Question 4**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, X_{3}, X_{4}$ is an I.I.D random sample with $X_{i}∼Exp(θ)$ and unknown rate parameter $θ$. Find the likelihood function of the sample $2.1, 5.0, 4.2, 1.4$.

- [ ] A. $θ^{4}e^{-15.2θ}$
- [ ] B. $θ^{4}e^{-12.7θ}$
- [ ] C. $θ^{12.7}e^{-4θ}$
- [ ] D. $θ^{-4}e^{12.7θ}$
- [ ] E. $θ^{15.2}e^{-4θ}$

---

<a id="the-normal-distribution"></a>
## The Normal Distribution

The probability density function of a normally distributed random variable $X\sim N(\mu,\sigma^2)$ is given by

$$
f(x) = \dfrac{1}{\sigma\sqrt{2\pi}}e^{-\frac12\left(\frac{x-\mu}{\sigma}\right)^2}
$$

Some example normal distribution curves are shown below.

![](<../Source/12.2.5. Likelihood Functions for Continuous Probability Distributions/Images/ac45b21b7dec0d3705d173f2694b4c98.png>)

If we set $\sigma=1$, then $f(x)$ simplifies as follows:

$$
f(x) = \dfrac{1}{\sqrt{2\pi}}e^{-(x-\mu)^2/2}
$$

This is a standard normal random variable whose mean is shifted right by $\mu$ units.

Finally, setting the unknown parameter as $\theta$ when discussing likelihood functions is common. Following this convention, we set $\mu=\theta$, and we get

$$
f(x;\theta) = \dfrac{1}{\sqrt{2\pi}}e^{-(x-\theta)^2/2}
$$

Let's use this to compute some likelihood functions of some normally distributed sample data.

---

<a id="likelihood-functions-for-normal-random-variables"></a>
## Likelihood Functions for Normal Random Variables

**Example:** Suppose $X_1,X_2,X_3,X_4$ is an I.I.D random sample with $X_i \sim N(\theta, 1)$, where $\theta$ is an unknown mean. Find the likelihood function of the sample $0.4,\, -0.3,\, -2.2,\, -0.4$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
L(\theta) = P(X_1=x_1, X_2=x_2, \ldots, X_n = x_n) = \displaystyle\prod_{i=1}^n f(x_i;\,\theta)
$$

In our case, we are given a sample of $n=4$ independent normal random variables with unknown mean $\theta$ and variance

$$
\sigma^2 = 1
$$

for which the probability density function is

$$
f(x; \theta) = \dfrac{1}{\sigma \sqrt{2\pi}} e^{-\frac{1}{2} \left(\frac{x-\theta}{\sigma} \right)^2} = \dfrac{1}{\sqrt{2\pi}} e^{-(x-\theta)^2/2}, \qquad x \in (-\infty,\infty)
$$

We can calculate the likelihood function as follows:

$$
\begin{aligned}
L(θ) &= ∏_(i = 1)^(n)(\frac{1}{\sqrt{2π}}e^{- (x_{i} - θ)^{2}/2}) \\
&= (2π)^{-n/2}e^{- ∑(x_{i} - θ)^{2}/2} \\
&= (2π)^{-n/2}e^{- (∑x_{i}^{2} - 2θ∑x_{i} + nθ^{2})/2} \\
&= (2π)^{-n/2}e^{- (C - 2Sθ + nθ^{2})/2}
\end{aligned}
$$

where

$$
\displaystyle S = \sum\limits_ {i=1}^n x_i
$$

and

$$
\displaystyle C = \sum\limits_ {i=1}^n x_i^2
$$

For our sample, we have that $n=4$, and

$$
\begin{aligned}
S &= ∑_(i = 1)^(n)x_{i} \\
&= 0.4 + (-0.3) + (-2.2) + (-0.4) \\
&=-2.5 \\
C &= ∑_(i = 1)^(n)x_{i}^{2} \\
&= (0.4)^{2} + (-0.3)^{2} + (-2.2)^{2} + (-0.4)^{2} \\
&= 5.25
\end{aligned}
$$

Therefore,

$$
\begin{aligned}
L(θ) &= (2π)^{-4/2}e^{- (5.25 - 2(-2.5)θ + 4θ^{2})/2} \\
&= \frac{1}{4π^{2}}e^{-2.625 - 2.5θ - 2θ^{2}}
\end{aligned}
$$

---

**Question 5**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, …, X_{12}$ is an I.I.D random sample with $X_{i}∼N(θ, 1)$, where $θ$ is an unknown mean. For a particular sample $x_{1}, x_{2}, …, x_{12}$, you're given that
$∑_(i = 1)^(12)x_{i} = 7.4, ∑_(i = 1)^(12)x_{i}^{2} = 16.8$.
Given that the likelihood function of the sample is
$L(θ) = \frac{1}{64π^{6}}e^{g (θ)}$,
what is the expression for $g(θ)$?

*Hint: The probability density function for a normal random variable with mean $θ$ and variance $1$ is given by*
$f(x; θ) = \frac{1}{\sqrt{2π}}e^{- (x - θ)^{2}/2}, x ∈ (- ∞, ∞)$.

- [ ] A. $-8.4 - 7.4θ - 6θ^{2}$
- [ ] B. $8.4 - 7.4θ + 6θ^{2}$
- [ ] C. $-8.4 + 7.4θ - 6θ^{2}$
- [ ] D. $16.8 - 14.8θ + 12θ^{2}$
- [ ] E. $-16.8 + 14.8θ - 12θ^{2}$

---

**Question 6**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, X_{3}, X_{4}$ is an I.I.D random sample with $X_{i}∼N(θ, 1)$, where $θ$ is an unknown mean. Given that the likelihood function of the sample $-1.2, 0.4, 2.2, 0.8$ is
$L(θ) = \frac{1}{4π^{2}}e^{g (θ)}$,
what is the expression for $g(θ)$?

- [ ] A. $-3.54 + 2.2θ - 2θ^{2}$
- [ ] B. $-4.4 + 7.08θ - 2θ^{2}$
- [ ] C. $3.54 - 2.2θ + 2θ^{2}$
- [ ] D. $-7.08 + 4.4θ - 2θ^{2}$
- [ ] E. $2.2 - 3.54θ + 2θ^{2}$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
