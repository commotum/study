# Logarithmic Differentiation

<!--
lesson-id: 294
topic-code: MML.12.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Differentiating X Raised to the Power of a Function](#differentiating-x-raised-to-the-power-of-a-function)
- [Differentiating a Function Raised to the Power of X](#differentiating-a-function-raised-to-the-power-of-x)
- [Differentiating a Function Raised to the Power of a Function](#differentiating-a-function-raised-to-the-power-of-a-function)

## Prerequisites

- [Implicit Differentiation](<../../../../CAB/3. Differentiation- Composite, Implicit, and Inverse Functions/3.2. Differentiating Implicit and Inverse Functions/Lessons/3.2.1. Implicit Differentiation.md>)
- [Simplifying Expressions Using Basic Trigonometric Identities](<../../../../PCL/4. Trigonometry/4.2. Trigonometric Identities/Lessons/4.2.1. Simplifying Expressions Using Basic Trigonometric Identities.md>)
- [The Power Rule for Logarithms](<../../../../AG2/4. Exponentials & Logarithms/4.2. The Laws of Logarithms/Lessons/4.2.3. The Power Rule for Logarithms.md>)

---

<a id="introduction"></a>
## Introduction

**Logarithmic differentiation** is a technique to compute derivatives that are difficult to find using the usual rules of differentiation, such as the product, quotient, and chain rules.

Let's consider the following function.

$$
y = x^x
$$

We aim to compute

$$
\dfrac{\textrm d y}{\textrm d x}
$$

However, in this case, we note the following:

- Both the base and the exponent are functions of $x$.
- This means we cannot apply the power rule since the exponent is not a fixed number.
- We also cannot apply the rules for differentiating exponentials since the base is not a fixed number.

So instead, we'll use logarithmic differentiation. We proceed as follows:

**Step 1.** We take the natural logarithm of both sides and apply the power rule for logarithms:

$$
\begin{aligned}
y &= x^{x} \\
\ln y &= \ln x^{x} \\
\ln y &= x\ln x
\end{aligned}
$$

**Step 2.** We differentiate both sides of the above equation with respect to $x$ using implicit differentiation and the product rule:

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(x\ln x) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= \frac{d}{dx}(x) \cdot \ln x + x \cdot \frac{d}{dx}(\ln x) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= 1 \cdot \ln x + x \cdot (\frac{1}{x}) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= \ln x + 1
\end{aligned}
$$

**Step 3.** Now, we solve for

$$
\dfrac {\textrm{d}y}{\textrm{d}x}
$$

This gives

$$
\dfrac {\textrm{d}y}{\textrm{d}x} = y\left(\ln x + 1 \right)
$$

**Step 4.** Finally, we substitute back our original expression for $y=x^x{:}$

$$
\dfrac {\textrm{d}y}{\textrm{d}x} = x^x \left(\ln x + 1 \right)
$$

Let's see another example.

---

<a id="differentiating-x-raised-to-the-power-of-a-function"></a>
## Differentiating X Raised to the Power of a Function

**Example:** Find the derivative of the function $y = x^{x^3}$.

**Explanation**

We have the function

$$
y = x^{x^3}
$$

First, we take the natural logarithm of both sides:

$$
\begin{aligned}
\ln y &= \ln (x^{x^{3}}) \\
\ln y &= x^{3}\ln x
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(x^{3}\ln x) \\
\frac{1}{y} \cdot y^{′} &= (x^{3})^{′} \cdot (\ln x) + (x^{3}) \cdot (\ln x)^{′} \\
\frac{1}{y} \cdot y^{′} &= 3x^{2} \cdot \ln x + x^{3} \cdot \frac{1}{x} \\
\frac{y^{′}}{y} &= 3x^{2}\ln x + x^{2}
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(3x^{2}\ln x + x^{2})
\end{aligned}
$$

Finally, we substitute

$$
y=x^{x^3}{:}
$$

$$
\begin{aligned}
y^{′} &= x^{x^{3}}(3x^{2}\ln x + x^{2}) \\
&= x^{x^{3}}(x^{2}(3\ln x + 1)) \\
&= x^{x^{3} + 2}(3\ln x + 1)
\end{aligned}
$$

---

**Question 1**

```quiz
type: radio
id: ma-2296
content: |-
  Consider the function $y = x^{2x}$. Its derivative is given by

  $y^{′} = 2x^{2x} \cdot g(x)$.

  Find the function $g(x)$.
options:
- id: a
  content: |-
    $\ln x - 1$
- id: b
  content: |-
    $\ln x + 2$
- id: c
  content: |-
    $2\ln x + 1$
- id: d
  content: |-
    $2\ln x - 1$
- id: e
  content: |-
    $\ln x + 1$
  correct: true
```

---

**Question 2**

```quiz
type: radio
id: ma-2520
content: |-
  Consider the function $y = x^{\cos x}$. Its derivative is given by

  $\frac{dy}{dx} = x^{\cos x - 1} \cdot g(x)$.

  Find the function $g(x)$.
options:
- id: a
  content: |-
    $\cos x - x\ln x\sin x$
  correct: true
- id: b
  content: |-
    $x\sin x - \ln x\cos x$
- id: c
  content: |-
    $x\sin x - x^{3}\ln x\cos x$
- id: d
  content: |-
    $x\cos x - \ln x\sin x$
- id: e
  content: |-
    $2x\sin x + x\ln x\cos x$
```

---

<a id="differentiating-a-function-raised-to-the-power-of-x"></a>
## Differentiating a Function Raised to the Power of X

**Example:** Find the derivative of the function $y = (x^2-1)^x$.

**Explanation**

We have the function

$$
y = (x^2-1)^x
$$

First, we take the natural logarithm of both sides:

$$
\begin{aligned}
\ln y &= \ln ((x^{2} - 1)^{x}) \\
\ln y &= x\ln (x^{2} - 1)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(x\ln (x^{2} - 1)) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= \frac{d}{dx}(x) \cdot \ln (x^{2} - 1) + x \cdot \frac{d}{dx}(\ln (x^{2} - 1)) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= 1 \cdot \ln (x^{2} - 1) + x \cdot (\frac{1}{x^{2} - 1} \cdot 2x) \\
\frac{1}{y} \cdot \frac{dy}{dx} &= \ln (x^{2} - 1) + \frac{2x^{2}}{x^{2} - 1}
\end{aligned}
$$

Now, we solve for

$$
\dfrac {\textrm{d}y}{\textrm{d}x}{:}
$$

$$
\begin{aligned}
\frac{dy}{dx} &= y(\ln (x^{2} - 1) + \frac{2x^{2}}{x^{2} - 1})
\end{aligned}
$$

Finally, we substitute

$$
y = (x^2-1)^x{:}
$$

$$
\begin{aligned}
\frac{dy}{dx} &= (x^{2} - 1)^{x}(\ln (x^{2} - 1) + \frac{2x^{2}}{x^{2} - 1}) \\
&= (x^{2} - 1)^{x}(((x^{2} - 1)\ln (x^{2} - 1))/(x^{2} - 1) + \frac{2x^{2}}{x^{2} - 1}) \\
&= (x^{2} - 1)^{x - 1}((x^{2} - 1)\ln (x^{2} - 1) + 2x^{2})
\end{aligned}
$$

---

**Question 3**

```quiz
type: radio
id: ma-2519
content: |-
  Consider the function $y = (2x)^{x}$. Its derivative is given by

  $y^{′} = (2x)^{x} \cdot g(x)$.

  Find the function $g(x)$.
options:
- id: a
  content: |-
    $x\ln (2x) + 1$
- id: b
  content: |-
    $x\ln x$
- id: c
  content: |-
    $2 + \ln x$
- id: d
  content: |-
    $\ln (2x) + 1$
  correct: true
- id: e
  content: |-
    $\ln (2x)$
```

---

**Question 4**

```quiz
type: radio
id: ma-2522
content: |-
  Consider the function $y = (x + 1)^{x}$. Its derivative is given by

  $y^{′} = (x + 1)^{x - 1} \cdot g(x)$.

  Find the function $g(x)$.
options:
- id: a
  content: |-
    $x\ln (x + 1) + 1$
- id: b
  content: |-
    $\ln (x + 1) + x + 1$
- id: c
  content: |-
    $(x - 1)\ln (x + 1) - x$
- id: d
  content: |-
    $(x + 1)\ln (x + 1) + x$
  correct: true
- id: e
  content: |-
    $(x - 1)\ln (x + 1) + 2$
```

---

<a id="differentiating-a-function-raised-to-the-power-of-a-function"></a>
## Differentiating a Function Raised to the Power of a Function

**Example:** Find the derivative of the function $y = (\sin x)^{\cos x}$.

**Explanation**

We have the function

$$
y = (\sin x)^{\cos x}
$$

First, we take the natural logarithm of both sides:

$$
\begin{aligned}
\ln y &= \ln ((\sin x)^{\cos x}) \\
\ln y &= \cos x\ln (\sin x)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(\cos x\ln (\sin x)) \\
\frac{1}{y} \cdot y^{′} &= (\cos x)^{′} \cdot (\ln (\sin x)) + (\cos x) \cdot (\ln (\sin x))^{′} \\
\frac{1}{y} \cdot y^{′} &= (-\sin x) \cdot \ln (\sin x) + \cos x \cdot (\frac{1}{\sin x} \cdot \cos x) \\
\frac{y^{′}}{y} &= \cos x\cot x - \sin x\ln (\sin x)
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(\cos x\cot x - \sin x\ln (\sin x))
\end{aligned}
$$

Finally, we substitute

$$
y = (\sin x)^{\cos x}{:}
$$

$$
\begin{aligned}
y^{′} &= (\sin x)^{\cos x}(\cos x\cot x - \sin x\ln (\sin x))
\end{aligned}
$$

---

**Question 5**

```quiz
type: radio
id: ma-2298
content: |-
  Consider the function $y = (\cos x)^{\sin x}$. Its derivative is given by

  $y^{′} = (\cos x)^{\sin x} \cdot g(x)$.

  Find the function $g(x)$.
options:
- id: a
  content: |-
    $\cos x\ln (\sin x) + \sin x\cos x$
- id: b
  content: |-
    $\cos x\ln (\cos x) - \cos^{2} x\sin x$
- id: c
  content: |-
    $\cos x\ln (\cos x) - \sin x\tan x$
  correct: true
- id: d
  content: |-
    $\sin x\ln (\cos x) - \cos^{2} x\sin x$
- id: e
  content: |-
    $\sin x\ln (\sin x) - \sin x\cos x$
```

---

**Question 6:**

```quiz
type: radio
id: ma-138671
content: |-
  Find the slope of the tangent to the curve $y = (x + 1)^{x^{2}}$ at $x = 1$.
options:
- id: a
  content: |-
    $1$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $4\ln 2 + 1$
  correct: true
- id: d
  content: |-
    $\ln 2 + 2$
- id: e
  content: |-
    $2\ln 2$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
