# Confidence Intervals for Linear Regression Intercept Parameters

<!--
lesson-id: 4116
topic-code: MML.12.3.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Summary of the Key Result](#summary-of-the-key-result)
- [Finding Confidence Intervals for Intercept Parameters](#finding-confidence-intervals-for-intercept-parameters)
- [Finding Confidence Intervals in Context](#finding-confidence-intervals-in-context)
- [Deriving an Expression for the Confidence Interval](#deriving-an-expression-for-the-confidence-interval)
- [Expectation and Variance of Intercept Parameter Estimates](#expectation-and-variance-of-intercept-parameter-estimates)

## Prerequisites

- [Linear Regression](<../../../../PCL/12. Statistics/12.3. Regression/Lessons/12.3.2. Linear Regression.md>)
- [Residuals and Residual Plots](<../../../../IM2/6. Probability & Statistics/6.3. Correlation/Lessons/6.3.4. Residuals and Residual Plots.md>)
- [Confidence Intervals for One Mean: Unknown Population Variance](<12.3.2. Confidence Intervals for One Mean- Unknown Population Variance.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we conduct a random sample of $n$ of *paired* observations $(x_i,y_i)$ drawn from a population.

$$
(x_1,y_1), \quad (x_2,y_2), \quad \ldots, \quad (x_n,y_n)
$$

Using previously discussed techniques, we can calculate the linear regression line for this data, which we'll denote as

$$
y = \widehat{\beta_0} + \widehat{\beta_1} x
$$

Our *estimates* for the **slope parameter** $\widehat{\beta_1}$ and **intercept parameter** $\widehat{\beta_0}$ are given by

$$
\widehat{\beta_1} = \dfrac{S_{xy}}{S_{xx}}, \qquad \widehat{\beta_0} = \overline{y} - \widehat{\beta_1}\overline{x}
$$

where $\overline{x}$ and $\overline{y}$ are the mean values of $x_i$ and $y_i$, and the quantities $S_{xx}$ and $S_{xy}$ are given by

$$
S_{xx} = \sum_{i=1}^n (x_i-\overline{x})^2, \qquad S_{xy} = \sum_{i=1}^n (x_i-\overline{x})(y_i-\overline{y})
$$

Plotting our regression line alongside the sample data, we might get a diagram similar to the following:

![](<../Source/12.3.6. Confidence Intervals for Linear Regression Intercept Parameters/Images/35ec573e46819ebfa602cda12e840cf2.png>)

Notice that we referred to $\widehat{\beta_0}$ and $\widehat{\beta_1}$ as *estimates*. This is because if we were to conduct a census on the population, we'd be able to calculate the "true" regression equation for the data that considers every population member. We'll denote this "true" regression line as

$$
y = \beta_0 + \beta_1 x
$$

The two lines are shown below.

![](<../Source/12.3.6. Confidence Intervals for Linear Regression Intercept Parameters/Images/2b49c4297293f6aa1dbe9a324824ff43.png>)

To summarize,

- the regression equation $y = \widehat{\beta_0} + \widehat{\beta_1} x$ is an estimate of the true regression equation $y = \beta_0 + \beta_1 x$,
- $\widehat{\beta_1}$ is an estimate of the "true" slope parameter $\beta_1$, and
- $\widehat{\beta_0}$ is an estimate of the "true" intercept parameter $\beta_0$.

In this lesson, we will learn how to construct confidence intervals for the intercept parameter $\beta_0$.

---

<a id="summary-of-the-key-result"></a>
## Summary of the Key Result

Given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the intercept $\beta_0$ of our model can be written as

$$
\underbrace{\widehat{\,\beta}_0}_{\:\: \textrm{point} \ \textrm{estimate}} \: \pm \: \overbrace{t_{n-2,\alpha/2} \cdot \underbrace{\textrm{SE}[\widehat{\,\beta}_0\,]}_{\textrm{standard} \ \,\:\: \textrm{error}}}^{\textrm{margin of error}}
$$

Note the following:

- $\!\widehat{\,\beta}_0$ is our point estimate for the intercept, computed using the data points from our sample.
- $P(T > t_{n-2,\alpha/2}) = \dfrac{\alpha}{2}$, where $T$ has a student's $t$-distribution with $n-2$ degrees of freedom.
- $\textrm{SE}[\widehat{\,\beta}_0\,]$ is the standard error of $\widehat{\,\beta}_0$, given by
$\textrm{SE}[\widehat{\,\beta}_0\,] = \sqrt{\textrm{MSE}\cdot\left(\dfrac1n+\dfrac{\overline{x}^2}{S_{xx}}\right)}$
where we have:$\textrm{MSE}$ is the mean squared error of the model:
$\textrm{MSE} = \dfrac{1}{n-2}\sum\limits_{i=1}^n (y_i - \widehat{\,y}_i)^2$
where $\widehat{\,y}_i$ is the estimate of $y_i$ given by our regression model.$S_{xx}$ is the usual sum of squares formula:
$\displaystyle S_{xx} = \sum\limits_{i=1}^n (x_i - \overline{x})^2$

The derivation of this result is rather long, and we leave it until the end of the lesson. For now, note the following:

- Each data point in the sample has some *residual* (i.e., error) associated with it. The error of our regression model at the data point $(x_i,y_i)$ is given by
$(y_i - \widehat{\,y}_i)$.
- The expression $\displaystyle\sum\limits_{i=1}^n (y_i - \widehat{\,y}_i)^2$ gives the sum of the squared residuals of our regression model.
- Even considering the entire population, the data points will not fall on a perfectly straight line. Thus, errors are present even in the "true" regression model. These errors have some (unknown) variance, which we'll denote as $\sigma^2$.
- It can be shown that the $\textrm{MSE}$ is an *unbiased* estimator of $\sigma^2$.

---

<a id="finding-confidence-intervals-for-intercept-parameters"></a>
## Finding Confidence Intervals for Intercept Parameters

**Example:** Let $\widehat{\,y}=\widehat{\,\beta}_0+\widehat{\,\beta}_1x=1.6+0.7 x$ be the regression line fitted using a sample $(x_1,y_1), \: \ldots, \: (x_{8},y_{8})$ of $n=8$ paired observations. Assuming that all conditions for the simple regression model are satisfied, $\overline{x}=2.4$, the mean square error of the model is $1.25$, and $S_{xx} = \sum_{i=1}^n (x_i - \overline{x})^2 = 42$, find a $90\%$ confidence interval for the $y$-intercept of the regression line.

*Hint: Use the fact that $P(T > 1.943) = 0.05$, where $T$ has a student's $t$-distribution with $6$ degrees of freedom. Also, the standard error for $\widehat{\,\beta}_0$ is given by $\textrm{SE}[\widehat{\,\beta}_0\,] = \sqrt{\textrm{MSE} \cdot \bigg(\dfrac{1}{n} + \dfrac{\overline{x}^2}{S_{xx}} \bigg)}$*

**Explanation**

We use the following notations:

- $y=\beta_0 + \beta_1 x$ is the regression line that best fits the population, and
- $\widehat{\,y}=\widehat{\,\beta}_0+\widehat{\,\beta}_1x$ is the estimate of the "true" regression line given by the data in our sample.

In this case, we have that

$$
\widehat{\,y}=\widehat{\,\beta}_0+\widehat{\,\beta}_1x=1.6+0.7x
$$

We are told that all assumptions for a simple regression model are held. So, given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the $y$-intercept $\beta_0$ of our model can be written as

$$
\underbrace{\widehat{\,\beta}_0}_{\:\: \textrm{point} \ \textrm{estimate}} \: \pm \: \overbrace{t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,]}^{\textrm{margin of error}}
$$

Note the following:

- $\!\widehat{\,\beta}_0$ is our point estimate for the intercept, computed using the data points from our sample.
- $P(T > t_{n-2,\alpha/2}) = \dfrac{\alpha}{2}$, where $T$ has a student's $t$-distribution with $n-2$ degrees of freedom.
- $\textrm{SE}[\widehat{\,\beta}_0\,]$ is the standard error of $\widehat{\,\beta}_0$, given by
$\textrm{SE}[\widehat{\,\beta}_0\,] = \sqrt{\textrm{MSE}\cdot\left(\dfrac1n+\dfrac{\overline{x}^2}{S_{xx}}\right)}$
where we have:$\textrm{MSE}$ is the mean squared error of the model:
$\textrm{MSE} = \dfrac{1}{n-2}\sum\limits_{i=1}^n (y_i - \widehat{\,y}_i)^2$
where $\widehat{\,y}_i$ is the estimate of $y_i$ given by our regression model.$S_{xx}$ is the usual sum of squares formula:
$\displaystyle S_{xx} = \sum\limits_{i=1}^n (x_i - \overline{x})^2$

To construct the confidence interval, we proceed as follows:

- **Step 1.** Find the point estimate. From the regression equation $\widehat{\,y}=1.6+0.7x$, our point estimate is
$\widehat{\,\beta}_0 = 1.6$.
- **Step 2.** Find the margin of error. Since we are interested in finding a $90\%$ confidence interval, we have
$\alpha=1-0.90=0.1 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.05$.
We are given that $P(T > 1.943) = 0.05$. As a result,
$t_{n-2,\alpha/2} = t_{6,0.05} = 1.943$.
Now, we can compute the margin of error:
$t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,] = 1.943 \cdot \sqrt{1.25 \cdot \bigg(\dfrac{1}{8} + \dfrac{2.4^2}{42} \bigg)} \approx 1.1$
- **Step 3.** Determine the limits of the confidence interval:
$\widehat{\,\beta}_0 \: \pm \: t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,] = 1.6 \pm 1.1$

---

**Question 1**

```quiz
type: radio
id: ma-143256
content: |-
  > A scientific calculator is required to answer this question.

  Let $y^(^) = β^(^)_{0} + β^(^)_{1}x = 3.5 + 5x$ be the regression line fitted using a sample $(x_{1}, y_{1}), …, (x_{20}, y_{20})$ of $n = 20$ paired observations. Assuming that all conditions for the simple regression model are satisfied, $x^(―) = 2$, the mean square error of the model is $8$, and $S_{xx} = ∑_(i = 1)^(n)(x_{i} - x^(―))^{2} = 24$, find a $95%$ confidence interval for the $y$-intercept of the regression line.

  *Hint: Use the fact that $P(T > 2.101) = 0.025$, where $T$ has a student's $t$-distribution with $18$ degrees of freedom. Also, the standard error for $β^(^)_{0}$ is given by $SE[β^(^)_{0}] = \sqrt{MSE \cdot (\frac{1}{n} + (x^(―)^{2})/(S_{xx}))}$*
options:
- id: a
  content: |-
    $5 ± 1.2$
- id: b
  content: |-
    $3.5 ± 3.0$
- id: c
  content: |-
    $3.5 ± 1.2$
- id: d
  content: |-
    $5 ± 2.5$
- id: e
  content: |-
    $3.5 ± 2.8$
  correct: true
```

---

**Question 2**

```quiz
type: radio
id: ma-159567
content: |-
  > A scientific calculator is required to answer this question.

  Let $y^(^) = β^(^)_{0} + β^(^)_{1}x = 1.5 + 3.8x$ be the regression line fitted using a sample $(x_{1}, y_{1}), …, (x_{18}, y_{18})$ of $n = 18$ paired observations. Assuming that all conditions for the simple regression model are satisfied, $x^(―) = 2.4$, the mean square error of the model is $2.53$, and $S_{xx} = ∑_(i = 1)^(n)(x_{i} - x^(―))^{2} = 484$, find a $90%$ confidence interval for the $y$-intercept of the regression line.

  *Hint: Use the fact that $P(T > 1.746) = 0.05$, where $T$ has a student's $t$-distribution with $16$ degrees of freedom. Also, the standard error for $β^(^)_{0}$ is given by $SE[β^(^)_{0}] = \sqrt{MSE \cdot (\frac{1}{n} + (x^(―)^{2})/(S_{xx}))}$*
options:
- id: a
  content: |-
    $3.8 ± 0.3$
- id: b
  content: |-
    $1.5 ± 0.7$
  correct: true
- id: c
  content: |-
    $1.5 ± 0.1$
- id: d
  content: |-
    $1.5 ± 0.5$
- id: e
  content: |-
    $3.8 ± 1.4$
```

---

<a id="finding-confidence-intervals-in-context"></a>
## Finding Confidence Intervals in Context

**Example:** The table above shows the number of hours each of four students spent studying for a biology exam and the grades they obtained on the exam. The corresponding fitted linear regression line using this sample is $\widehat{\,y} = 21.3 + 10x$.
Assuming that all conditions for the simple regression model are satisfied, $\overline{x}=5$, and $\textrm{MSE}=1.375$, find a $90\%$ confidence interval for the $y$-intercept of the regression line.

| **Hours ($x$)** | **$4$** | **$4.5$** | **$5.5$** | **$6$** |
| --- | ---: | ---: | ---: | ---: |
| **Grades ($y$)** | $62$ | $65$ | $77$ | $81$ |

*Hint: Use the fact that $P(T > 2.920) = 0.05$, where $T$ has a student's $t$-distribution with $2$ degrees of freedom. Also, the standard error for the $y$-intecept is*
$\textrm{SE}[\widehat{\,\beta}_0\,] = \sqrt{\textrm{MSE} \cdot \bigg(\dfrac{1}{n} + \dfrac{\overline{x}^2}{S_{xx}} \bigg)}$.

**Explanation**

We use the following notations:

- $y=\beta_0 + \beta_1 x$ is the regression line that best fits the population, and
- $\widehat{\,y}=\widehat{\,\beta}_0+\widehat{\,\beta}_1x$ is the estimate of the "true" regression line given by the data in our sample.

In this case, we have that

$$
\widehat{\,y}=\widehat{\,\beta}_0+\widehat{\,\beta}_1x= 21.3 + 10x
$$

We are told that all assumptions for a simple regression model are held. So, given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the $y$-intercept $\beta_0$ of our model can be written as

$$
\beta_0 \: \pm \: \overbrace{t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,]}^{\textrm{margin of error}}
$$

Note the following:

- $\!\widehat{\,\beta}_0$ is our point estimate for the intercept, computed using the data points from our sample.
- $P(T > t_{n-2,\alpha/2}) = \dfrac{\alpha}{2}$, where $T$ has a student's $t$-distribution with $n-2$ degrees of freedom.
- $\textrm{SE}[\widehat{\,\beta}_0\,]$ is the standard error of $\widehat{\,\beta}_0$, given by
$\textrm{SE}[\widehat{\,\beta}_0\,] = \sqrt{\textrm{MSE}\cdot\left(\dfrac1n+\dfrac{\overline{x}^2}{S_{xx}}\right)}$
where we have:$\textrm{MSE}$ is the mean squared error of the model:
$\textrm{MSE} = \dfrac{1}{n-2}\sum\limits_{i=1}^n (y_i - \widehat{\,y}_i)^2$
where $\widehat{\,y}_i$ is the estimate of $y_i$ given by our regression model.$S_{xx}$ is the usual sum of squares formula:
$\displaystyle S_{xx} = \sum\limits_{i=1}^n (x_i - \overline{x})^2$
- **Step 1.** Find the point estimate. From the regression equation $\widehat{\,y}=21.3 + 10x$, our point estimate is
$\widehat{\,\beta}_0 = 21.3$.
- **Step 2.** Find the margin of error. Since we are interested in finding a $90\%$ confidence interval, we have
$\alpha=1-0.9=0.1 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.05$.
We are given that $P(T > 2.920) = 0.05$. As a result,
$t_{n-2,\alpha/2} = t_{2,0.005} = 2.920$.Computing $S_{xx}$, we get
$∑_(i = 1)^(n)(x_{i} - x^(―))^{2}|= (4 - 5)^{2} + (4.5 - 5)^{2} + (5.5 - 5)^{2} + (6 - 5)^{2}; = 1 + 0.25 + 0.25 + 1; = 2.5$. Now, we can compute the margin of error:
$t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,] = 2.920 \cdot \sqrt{1.375\cdot \bigg(\dfrac{1}{4} + \dfrac{5^2}{2.5} \bigg)} \approx 11.0$
- **Step 3.** Determine the limits of the confidence interval:
$\widehat{\,\beta}_0 \: \pm \: t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,] = 21.3 \pm 11.0$

---

**Question 3**

```quiz
type: radio
id: ma-143261
content: |-
  > A scientific calculator is required to answer this question.

  | **Age ($x$)** | **$3$** | **$5$** | **$7$** | **$9$** |
  | --- | ---: | ---: | ---: | ---: |
  | **Height ($y$)** | $95$ | $106$ | $110$ | $125$ |

  The table above gives the ages, in years, and the corresponding heights, in centimeters, of four children. The corresponding linear regression line fitted using this sample is $y^(^) = 80.8 + 4.7x$.
  Assuming that all conditions for the simple regression model are satisfied, $x^(―) = 6$, and $MSE = 10.1$, find a $95%$ confidence interval for the $y$-intercept of the regression line.

  *Hint: Use the fact that $P(T > 4.303) = 0.025$, where $T$ has a student's $t$-distribution with $2$ degrees of freedom. Also, the standard error for the $y$-intecept is*
  $SE[β^(^)_{0}] = \sqrt{MSE \cdot (\frac{1}{n} + (x^(―)^{2})/(S_{xx}))}$.
options:
- id: a
  content: |-
    $80.8 ± 14.3$
- id: b
  content: |-
    $4.7 ± 2.9$
- id: c
  content: |-
    $80.8 ± 22.2$
- id: d
  content: |-
    $80.8 ± 24.0$
- id: e
  content: |-
    $80.8 ± 19.6$
  correct: true
```

---

**Question 4**

```quiz
type: radio
id: ma-159516
content: |-
  > A scientific calculator is required to answer this question.

  | **Number of hours ($x$)** | **$2$** | **$3$** | **$4$** | **$5$** |
  | --- | ---: | ---: | ---: | ---: |
  | **Grade ($y$)** | $75$ | $82$ | $90$ | $91$ |

  The table above shows the hours four students spent studying for a math exam and their final grades. The corresponding fitted linear regression line using this sample is $y^(^) = 64.9 + 5.6x$.
  Assuming that all conditions for the simple regression model are satisfied, $x^(―) = 3.5$, and $MSE = 6.1$, find a $90%$ confidence interval for the $y$-intercept of the regression line.

  *Hint: Use the fact that $P(T > 2.920) = 0.05$, where $T$ has a student's $t$-distribution with $2$ degrees of freedom. Also, the standard error for the $y$-intecept is*
  $SE[β^(^)_{0}] = \sqrt{MSE \cdot (\frac{1}{n} + (x^(―)^{2})/(S_{xx}))}$.
options:
- id: a
  content: |-
    $64.9 ± 2.8$
- id: b
  content: |-
    $5.6 ± 10.2$
- id: c
  content: |-
    $64.9 ± 11.9$
  correct: true
- id: d
  content: |-
    $64.9 ± 10.2$
- id: e
  content: |-
    $64.9 ± 17.6$
```

---

<a id="deriving-an-expression-for-the-confidence-interval"></a>
## Deriving an Expression for the Confidence Interval

We'll now derive our expression for the confidence interval for our intercept parameter estimate $\widehat{\beta_0}$.

Suppose we conduct a random sample of $n$ of *paired* observations $(x_i,y_i)$.

$$
(x_1,y_1), \quad (x_2,y_2), \quad \ldots, \quad (x_n,y_n)
$$

Let's assume that our "global" regression line (i.e., the line that best fits the entire population) is given by

$$
y = \beta_0 + \beta_1 x
$$

For each

$$
i=1,2,\ldots,n
$$

let

$$
y_i = \beta_0 + \beta_1 x_i + \epsilon_i
$$

where we assume that the error terms $\epsilon_i$ are I.I.D, and

$$
\epsilon_i \sim N(0, \sigma^2)
$$

for some unknown variance $\sigma^2$. The error terms and their distribution can be visualized as shown below.

![](<../Source/12.3.6. Confidence Intervals for Linear Regression Intercept Parameters/Images/babd3d23836b98842c219ee2d4045e05.png>)

This means that each $y_i$ is a normally distributed random variable with

$$
y_i \sim N\left(\beta_0 + \beta_1 x_i, \sigma^2\right)
$$

It can be shown that

$$
\textrm E[\widehat{\beta}_0] = \beta_0, \qquad \textrm{Var}[\widehat{\beta_0}] = \sigma^2\left(\dfrac1n+ \dfrac{\overline{x}^2}{S_{xx}}\right)
$$

We'll derive these results separately in a while. But for now, we'll simply state them.

Using these results, we can express the distribution of $\widehat{\beta_0}$ as

$$
\widehat{\beta}_0 \sim N\left(\beta_0, \: \sigma^2\left(\dfrac1n+ \dfrac{\overline{x}^2}{S_{xx}}\right) \right)
$$

We do not know $\sigma^2$, the variance of the error terms. However, it can be shown that the mean squared error, given by

$$
\textrm{MSE} = \dfrac{1}{n-2}\sum\limits_{i=1}^n (y_i - \widehat{\,y}_i)^2
$$

is an unbiased estimator of $\sigma^2$.

Our estimate of the standard error of $\widehat{\beta}_0$ is now given by

$$
\textrm{SE}[\widehat{\beta}_0] = \sqrt{\textrm{MSE}\cdot\left(\dfrac1n+ \dfrac{\overline{x}^2}{S_{xx}}\right)}
$$

Since this is an *estimate* of the standard error, it can be shown that the random variable

$$
\dfrac{\widehat{\beta}_0 -\beta_0}{\textrm{SE}[\widehat{\beta_0}]}
$$

follows a student's $t$-distribution with $n-2$ degrees of freedom.

Finally, given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the intercept $\beta_0$ of our model can be written as

$$
\widehat{\,\beta}_0 \: \pm \: \overbrace{t_{n-2,\alpha/2} \cdot \textrm{SE}[\widehat{\,\beta}_0\,]}^{\textrm{margin of error}}
$$

---

<a id="expectation-and-variance-of-intercept-parameter-estimates"></a>
## Expectation and Variance of Intercept Parameter Estimates

Earlier, we made use of the following results:

$$
\textrm E[\widehat{\beta}_0] = \beta_0, \qquad \textrm{Var}[\widehat{\beta_0}] = \sigma^2\left(\dfrac{1}{n} + \dfrac{\overline{x}^2}{S_{xx}}\right)
$$

Let's now derive these. First, we will show that

$$
S_{xy} = \sum_{i=1}^n (x_i-\overline{x})y_i
$$

We can show this is true using some straightforward manipulations of the summation, as follows:

$$
\begin{aligned}
S_{xy} &= ∑_(i = 1)^(n)(x_{i} - x^(―))(y_{i} - y^(―)) \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i} - ∑_(i = 1)^(n)(x_{i} - x^(―))y^(―) \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i} - y^(―)∑_(i = 1)^(n)(x_{i} - x^(―)) \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i} - y^(―)∑_(i = 1)^(n)x_{i} + y^(―)∑_(i = 1)^(n)x^(―) \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i} - y^(―)∑_(i = 1)^(n)x_{i} + y^(―)nx^(―) \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i} - y^(―)∑_(i = 1)^(n)x_{i} + y^(―)∑_(i = 1)^(n)x_{i} \\
&= ∑_(i = 1)^(n)(x_{i} - x^(―))y_{i}
\end{aligned}
$$

Next, we define $k_i$ as

$$
k_i = \dfrac{x_i-\overline{x}}{S_{xx}}
$$

The following properties can be shown using straightforward algebra:

- $\displaystyle \sum_{i=1}^n k_i=0$
- $\displaystyle \sum_{i=1}^n k_i x_i=1$
- $\displaystyle \sum_{i=1}^n k_i^2=\dfrac{1}{S_{xx}}$

Now, recall that

$$
\widehat{\beta_0} = \overline{y} - \widehat{\beta_1}\overline{x}
$$

We can rewrite our expression for $\widehat{\beta_0}$ as follows:

$$
\begin{aligned}
β_{0}^(^) &= y^(―) - β_{1}x^(―) \\
&= \frac{1}{n}∑_(i = 1)^(n)y_{i} - \frac{S_{xy}}{S_{xx}}x^(―) \\
&= \frac{1}{n}∑_(i = 1)^(n)y_{i} - (∑_(i = 1)^(n)(x_{i} - x^(―))y_{i})/(S_{xx})x^(―) \\
&= \frac{1}{n}∑_(i = 1)^(n)y_{i} - ∑_(i = 1)^(n)k_{i}y_{i}x^(―) \\
&= \frac{1}{n}∑_(i = 1)^(n)y_{i} - x^(―)∑_(i = 1)^(n)k_{i}y_{i}
\end{aligned}
$$

We're now in a position to compute $\textrm{E}[{\widehat{\beta_0}}]$ and $\textrm{Var}[{\widehat{\beta_0}}]$.

- For the mean, we have
$E[β_{0}^(^)]|= \frac{1}{n}E[∑_(i)y_{i}] - x^(―)E[∑_(i)k_{i}y_{i}]; = \frac{1}{n}∑_(i)E[y_{i}] - x^(―)∑_(i)E[k_{i}y_{i}]; = \frac{1}{n}∑_(i)(β_{0} + β_{1}x_{i}) - x^(―)∑_(i)k_{i}(β_{0} + β_{1}x_{i}); = \frac{1}{n}∑_(i)(β_{0} + β_{1}x_{i}) - x^(―)[β_{0}∑_(i)k_{i} + β_{1}∑_(i)k_{i}x_{i}]; = \frac{1}{n}∑_(i)(β_{0} + β_{1}x_{i}) - x^(―)[β_{0}∑_(i)k_{i}^{0} + β_{1}∑_(i)k_{i}x_{i}^{1}]; = \frac{1}{n}∑_(i)(β_{0} + β_{1}x_{i}) - x^(―)β_{1}; = \frac{β_{0}}{n}∑_(i)1 + \frac{β_{1}}{n}∑_(i)x_{i} - x^(―)β_{1}; = \frac{β_{0}}{n} \cdot n + x^(―)β_{1} - x^(―)β_{1}; = β_{0}$.
- For the variance, we have
$Var[β_{0}^(^)]|= \frac{1}{n^{2}}Var[∑_(i)y_{i}] + x^(―)^{2}Var[∑_(i)k_{i}y_{i}]; = \frac{1}{n^{2}}∑_(i)Var[y_{i}] + x^(―)^{2}∑_(i)k_{i}^{2}Var[y_{i}]; = \frac{1}{n^{2}}nσ^{2} + x^(―)^{2}∑_(i)k_{i}^{2}σ^{2}; = \frac{1}{n^{2}}nσ^{2} + x^(―)^{2}σ^{2}∑_(i)k_{i}^{2}; = \frac{1}{n}σ^{2} + x^(―)^{2}σ^{2} \cdot \frac{1}{S_{xx}}; = σ^{2}(\frac{1}{n} + (x^(―)^{2})/(S_{xx}))$.

So finally, we have

$$
\widehat{\beta_0} \sim N\left(\beta_0,\sigma^2\left(\dfrac{1}{n} + \dfrac{\overline{x}^2}{S_{xx}}\right)\right)
$$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
