# The Second Partial Derivatives Test

<!--
lesson-id: 1944
topic-code: MML.9.8.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Critical Points of a Function](#finding-the-critical-points-of-a-function)
- [Classifying Stationary Points](#classifying-stationary-points)
- [The Second Partial Derivatives Test](#the-second-partial-derivatives-test)
- [Classifying a Critical Point Using the Second Partial Derivatives Test](#classifying-a-critical-point-using-the-second-partial-derivatives-test)
- [Finding and Classifying All Critical Points Using the Second Partial Derivatives Test](#finding-and-classifying-all-critical-points-using-the-second-partial-derivatives-test)
- [Relating the Second Partial Derivatives Test to the Taylor Polynomial Classification](#relating-the-second-partial-derivatives-test-to-the-taylor-polynomial-classification)

## Prerequisites

- [Global vs. Local Extrema and Critical Points of Multivariable Functions](<9.8.1. Global vs. Local Extrema and Critical Points of Multivariable Functions.md>)

---

<a id="introduction"></a>
## Introduction

The **critical points** of a function $f(\mathbf x)$ are the points

$$
\mathbf x = \mathbf a
$$

in the domain of $f$ such that

$$
\boldsymbol f'(\mathbf a) =\mathbf 0^T
$$

or $\boldsymbol f'(\mathbf a)$ does not exist. A critical point such that

$$
\boldsymbol f'(\mathbf a) = \mathbf 0^T
$$

is called a **stationary point**.

For example, consider the function

$$
f(x,y) = x^2 - 2x + y^2+6y
$$

The domain of $f$ is $\mathbb{R}^2$, and the first partial derivatives of $f$ are

$$
\dfrac {\partial f}{\partial x} = 2x-2, \qquad \dfrac {\partial f}{\partial y} = 2y+6
$$

So, the first derivative of $f$ is
$\begin{bmatrix}2x - 2 & 2y + 6\end{bmatrix}$

To find the critical points, we need to find the points $(x,y) \in \mathbb{R}^2$ such that $\begin{bmatrix}0 & 0\end{bmatrix}$ or $\boldsymbol f'(x,y)$ does not exist:

- Setting $\boldsymbol f'(x,y) = \mathbf{0}^T$, we obtain the following system:
${2x - 2 = 0; 2y + 6 = 0$
This system has the solution $x=1$ and $y=-3$. Consequently, $(1,-3)$ is a critical point of $f$.
- $\boldsymbol f'(x,y)$ exists everywhere, so there are no more critical points to be found.

Therefore, the only critical point of $f$ is $(1,-3)$.

---

<a id="finding-the-critical-points-of-a-function"></a>
## Finding the Critical Points of a Function

**Example:** Determine the critical points of $f(x,y) = x^3+y^3-3x^2y+9y$.

**Explanation**

The critical points of a multivariable function $f$ are the points $\mathbf a$ in the domain of $f$ such that

$$
\boldsymbol f'(\mathbf a) =\mathbf 0^T
$$

or $\boldsymbol f'(\mathbf a)$ does not exist, where

$$
\begin{bmatrix}\frac{∂f}{∂x_{1}} & \frac{∂f}{∂x_{2}} & ⋯ & \frac{∂f}{∂x_{n}}\end{bmatrix}
$$

In this case, the domain of $f$ is $\mathbb{R}^2$, and the first partial derivatives of $f$ are

$$
\begin{aligned}
\frac{∂f}{∂x} &= 3x^{2} - 6xy = 3x(x - 2y) \\
\frac{∂f}{∂y} &= 3y^{2} - 3x^{2} + 9
\end{aligned}
$$

So, the first derivative of $f$ is
$\begin{bmatrix}3x(x - 2y) & 3y^{2} - 3x^{2} + 9\end{bmatrix}$

To find the critical points, we need to find the points $(x,y) \in \mathbb{R}^2$ such that

$$
\boldsymbol f'(x,y) = \mathbf{0}^T
$$

or $\boldsymbol f'(x,y)$ does not exist. Note that, in our case, $\boldsymbol f'(x,y)$ exists everywhere, so we just need to find the points where

$$
\boldsymbol f'(x,y) = \mathbf{0}^T
$$

Setting

$$
\boldsymbol f'(x,y) = \mathbf{0}^T
$$

we have the following system:

$$
{3x(x - 2y) = 0; 3y^{2} - 3x^{2} + 9 = 0
$$

From the first equation, we obtain $x=0$ or $x=2y$. Now, we substitute these values into the second equation.

- For $x=0$, we get
$3y^2 - 3(0)^2 + 9 = 0 \qquad \Longrightarrow \qquad y^2 = -3$,
but this last equation has no real solution.
- For $x = 2y$, we obtain
$3y^2 - 3(2y)^2 + 9 = 0 \qquad \Longrightarrow \qquad y = \pm 1$.
Substituting these values into $x = 2y$, we get
$\begin{bmatrix}y =-1 & ⟹x = 2(-1) =-2 \\ y = 1 & ⟹x = 2(1) = 2\end{bmatrix}$.

Therefore, our system has two solutions, $(-2,-1)$ and $(2,1)$.

In conclusion, $f$ has two critical points, $(-2,-1)$ and $(2,1)$. Note that since

$$
\boldsymbol f'(\mathbf{x}) = \mathbf 0^T
$$

at these two points, they are also both stationary points.

---

**Question 1:**

```quiz
type: radio
id: ma-36622
content: |-
  Determine the stationary points of $f(x, y) = 2\ln x - 2xy + y^{2}$.
options:
- id: a
  content: |-
    $(1, 1)$ and $(-1,-1)$
- id: b
  content: |-
    $(0, 0)$
- id: c
  content: |-
    $(1, 1)$
  correct: true
- id: d
  content: |-
    $(-1,-1)$ and $(0, 0)$
- id: e
  content: |-
    $(-1, 1)$ and $(1,-1)$
```

---

**Question 2:**

```quiz
type: radio
id: ma-36635
content: |-
  Determine the critical points of $f(x, y) = x^{2} - 6xy + y^{3}$.
options:
- id: a
  content: |-
    $(0, 0)$ and $(18, 6)$
  correct: true
- id: b
  content: |-
    $(0, 0)$
- id: c
  content: |-
    $(6, 18)$
- id: d
  content: |-
    $(0, 0)$ and $(3,-3)$
- id: e
  content: |-
    $(0, 0)$ and $(6, 18)$
```

---

<a id="classifying-stationary-points"></a>
## Classifying Stationary Points

Recall that, for functions of two variables $f(x,y)$, there are typically three possible types of critical points that can occur:

- Local minimum points. For example, the elliptic paraboloid $z = f(x,y) = x^2+y^2$ has a minimum point at $(0,0)$, as shown below.![](<../Source/9.8.2. The Second Partial Derivatives Test/Images/2b19b0856ba42a6eded4e294ac6368c6.png>)

- Local maximum points. For example, the elliptic paraboloid $z = f(x,y) = -x^2-y^2$ has a maximum point at $(0,0)$, as shown below.![](<../Source/9.8.2. The Second Partial Derivatives Test/Images/168ed21c39494008c2c4749dd37502f0.png>)

- Saddle points. For example, the hyperbolic paraboloid $z = f(x,y) = y^2-x^2$ has a saddle point at $(0,0)$, as shown below.![](<../Source/9.8.2. The Second Partial Derivatives Test/Images/2a15cc17fbabea633126d6e8cd3eb219.png>)

Local maximum and local minimum points are analogous to what you would have seen in single-variable calculus.

A saddle point occurs when the slopes are zero in all directions, yet it is not a local maximum or minimum. The hyperbolic paraboloid shown above reaches a local maximum at $(0,0)$ along the plane $xz$-plane, yet this is a local minimum along the $yz$-plane.

---

<a id="the-second-partial-derivatives-test"></a>
## The Second Partial Derivatives Test

The **second partial derivatives test** helps to determine whether a stationary point of a function of two variables gives rise to a local maximum, a local minimum, or a saddle point.

Suppose that

$$
\mathbf x = \mathbf{a}
$$

is a critical point of $f(\mathbf x)$, and let $H(\mathbf{a})$ denote the determinant of $\boldsymbol f''(x,y)$ evaluated at $\mathbf{a}$ (recall that $H(\mathbf a)$ is called the *Hessian determinant* of $f$ at $\mathbf a$).

The second partial derivative test states the following:

- If $H(\mathbf{a}) > 0$, then:if $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) < 0$, then $f$ has a local maximum at $\mathbf{a}$, andif $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) > 0$, then $f$ has a local minimum at $\mathbf{a}$.
- If $H(\mathbf{a}) < 0$, then $f$ has a saddle point at $\mathbf{a}$.
- If $H(\mathbf{a}) = 0$, then the second partial derivatives test is inconclusive, and we need to classify the point using some other method.

As an example, consider the function

$$
f (x, y) = 3 + 2x^2 - x y + y^2
$$

Let's find the critical points of this function and then classify them using the second partial derivatives test.

We start by computing the partial derivatives:

$$
\dfrac{\partial f}{\partial x} = 4x -y, \qquad \dfrac{\partial f}{\partial y} = 2y -x
$$

The first derivative exists for any $(x,y)$ in the domain of the function, so we only need to find the stationary points. Setting

$$
\boldsymbol f'(x,y) = \mathbf{0}^T
$$

we obtain the following system:

$$
{4x - y = 0; 2y - x = 0
$$

The solution to this system is $x=0, y=0$. Hence, $(0,0)$ is the only stationary point.

To classify this stationary point, we use the second partial derivative test.

First, we compute the second partial derivatives:

$$
\begin{aligned}
\frac{∂^{2}f}{∂x^{2}} &= \frac{∂}{∂x}(4x - y) = 4 \\
\frac{∂^{2}f}{∂y^{2}} &= \frac{∂}{∂y}(2y - x) = 2 \\
\frac{∂^{2}f}{∂x∂y} &= \frac{∂^{2}f}{∂y∂x} = \frac{∂}{∂x}(2y - x) =-1
\end{aligned}
$$

So, the second derivative is

$$
\begin{aligned}
f^{″}(x, y) &= \begin{bmatrix}\frac{∂^{2}f}{∂x^{2}} & \frac{∂^{2}f}{∂x∂y} \\ \frac{∂^{2}f}{∂y∂x} & \frac{∂^{2}f}{∂y^{2}}\end{bmatrix} = \begin{bmatrix}4 & -1 \\ -1 & 2\end{bmatrix}
\end{aligned}
$$

Finally, we evaluate the second derivative at each of the stationary points and use it to categorize them:

- Evaluating the second derivative at $(0,0)$, we obtain
$f^{″}(0, 0) = \begin{bmatrix}4 & -1 \\ -1 & 2\end{bmatrix}$.
Computing the determinant $H(0,0)$, we get
$H(0,0) = \det(\boldsymbol f''(0,0)) = 4 \cdot 2 - (-1)^2 = 7 > 0$.
Finally, since $H(0,0) > 0$ and $\dfrac{\partial^2 f}{\partial x^2}(0,0) = (\boldsymbol f''(0,0))_{11}= 4 >0$, the function $f$ has a local minimum at at $(0,0)$.

In conclusion, $f(x,y)$ has a local minimum at $(0,0)$.

---

<a id="classifying-a-critical-point-using-the-second-partial-derivatives-test"></a>
## Classifying a Critical Point Using the Second Partial Derivatives Test

**Example:** The function
$f(x,y) %= 2x + 2y - x^2 + y^2 + 2$
has the first derivative
$\begin{bmatrix}2 - 2x & 2 + 2y\end{bmatrix}$
and has a stationary point at $(1,-1)$. If $H$ denotes the Hessian determinant of $f$, which of the following statements are true?

1. $H(1,-1) > 0$
2. $\dfrac{\partial^2 f}{\partial x^2}(1,-1) < 0$
3. The point $(1,-1)$ is a saddle point of $f$

**Explanation**

The second partial derivative test states that if $\mathbf{a}\in\mathbb{R}^2$ is a stationary point of $f(x,y)$ with Hessian determinant $H(\mathbf a)$, we have the following:

- If $H(\mathbf{a}) > 0$, then:if $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) < 0$, then $f$ has a local maximum at $\mathbf{a}$ if $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) >0$, then $f$ has a local minimum at $\mathbf{a}$
- If $H(\mathbf{a}) < 0$, then $f$ has a saddle point at $\mathbf{a}$
- If $H(\mathbf{a}) = 0$, then the second partial derivatives test is inconclusive.

First, we notice that since $\begin{bmatrix}2 - 2x & 2 + 2y\end{bmatrix}$ we must have

$$
\dfrac{\partial f}{\partial x} = 2-2x, \qquad \dfrac{\partial f}{\partial y} =2+2y
$$

Now, we compute the second partial derivatives:

$$
\begin{aligned}
\frac{∂^{2}f}{∂x^{2}} &= \frac{∂}{∂x}(2 - 2x) =-2 \\
\frac{∂^{2}f}{∂y^{2}} &= \frac{∂}{∂y}(2 + 2y) = 2 \\
\frac{∂^{2}f}{∂x∂y} &= \frac{∂^{2}f}{∂y∂x} = \frac{∂}{∂x}(2 + 2y) = 0
\end{aligned}
$$

So, the second derivative is

$$
\begin{aligned}
f^{″}(x, y) &= \begin{bmatrix}\frac{∂^{2}f}{∂x^{2}} & \frac{∂^{2}f}{∂x∂y} \\ \frac{∂^{2}f}{∂y∂x} & \frac{∂^{2}f}{∂y^{2}}\end{bmatrix} = \begin{bmatrix}-2 & 0 \\ 0 & 2\end{bmatrix}
\end{aligned}
$$

Evaluating the second derivative at $(1,-1)$, we obtain

$$
f^{″}(1,-1) = \begin{bmatrix}-2 & 0 \\ 0 & 2\end{bmatrix}
$$

With that in mind, let's examine our statements in turn.

- Statement I is false. Computing the determinant of $\boldsymbol f''(1,-1)$, we get
$H(1,-1) = \det(\boldsymbol f''(1,-1)) = -2 \cdot 2 - 0^2 = -4 < 0$.
- Statement II is true. Indeed, we have $\dfrac{\partial^2 f}{\partial x^2}(1,-1) = (\boldsymbol f''(1,-1))_{11} = -2 \lt 0$.
- Statement III is true. Indeed, since $H(1,-1) < 0$, the point $(1,-1)$ is a saddle point by the second partial derivative test.

Therefore, the correct answer is "II and III only."

---

**Question 3**

```quiz
type: radio
id: ma-37378
content: |-
  The function
  $f(x, y)$
  has the second derivative
  $f^{″}(x, y) = \begin{bmatrix}(2(1 + \ln x))/(x) & 2y \\ 2y & 2x\end{bmatrix}$
  and has a stationary point at $(1, 0)$. If $H$ denotes the Hessian determinant of $f$, which of the following statements are true?

  1. $H(1, 0) > 0$
  2. $\frac{∂^{2}f}{∂x^{2}}(1, 0) > 0$
  3. The point $(1, 0)$ is a local maximum of $f$
options:
- id: a
  content: |-
    I and II only
  correct: true
- id: b
  content: |-
    I only
- id: c
  content: |-
    I, II, and III
- id: d
  content: |-
    II only
- id: e
  content: |-
    II and III only
```

---

**Question 4**

```quiz
type: radio
id: ma-37588
content: |-
  The function
  $f(x, y)$
  has the first derivative
  $f^{′}(x, y) = \begin{bmatrix}2x & -2y\end{bmatrix}$,
  and has a stationary point at $(0, 0)$. If $H$ denotes the Hessian determinant of $f$, which of the following statements are true?

  1. $H(0, 0) > 0$
  2. $\frac{∂^{2}f}{∂x^{2}}(0, 0) < 0$
  3. The point $(0, 0)$ is a saddle point of $f$
options:
- id: a
  content: |-
    I and III only
- id: b
  content: |-
    I, II, and III
- id: c
  content: |-
    III only
  correct: true
- id: d
  content: |-
    None of the statements are true
- id: e
  content: |-
    I and II only
```

---

<a id="finding-and-classifying-all-critical-points-using-the-second-partial-derivatives-test"></a>
## Finding and Classifying All Critical Points Using the Second Partial Derivatives Test

**Example:** Find and categorize all the local extrema of the function
$f(x,y) % = x^3 -6xy + y^3$
given that
$\begin{bmatrix}3x^{2} - 3 & 2y\end{bmatrix}$

**Explanation**

The local extrema of $f$ occurs at its critical points. A point $\mathbf{a}$ in the domain of $f$ is a critical point of $f$ if

$$
\boldsymbol f'(\mathbf{a}) = \mathbf{0}^T
$$

(for stationary points) or $\boldsymbol f'(\mathbf{a})$ doesn't exist.

The second partial derivative test states that if $\mathbf{a}\in\mathbb{R}^2$ is a stationary point of $f(x,y)$ with Hessian determinant $H(\mathbf a)$, we have the following:

- If $H(\mathbf{a}) > 0$, then:if $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) < 0$, then $f$ has a local maximum at $\mathbf{a}$ if $\dfrac{\partial^2 f}{\partial x^2}(\mathbf{a}) >0$, then $f$ has a local minimum at $\mathbf{a}$
- If $H(\mathbf{a}) < 0$, then $f$ has a saddle point at $\mathbf{a}$
- If $H(\mathbf{a}) = 0$, then the second partial derivatives test is inconclusive.

First, notice that since $\begin{bmatrix}3x^{2} - 3 & 2y\end{bmatrix}$ we must have

$$
\dfrac{\partial f}{\partial x} = 3x^2 - 3, \qquad \dfrac{\partial f}{\partial y} =2y
$$

The first derivative exists for any $(x,y)$ in the domain of the function, so we only need to find the stationary points. Setting

$$
\boldsymbol f'(x,y) = \mathbf{0}^T
$$

we obtain the following system:

$$
{3x^{2} - 3 = 0; 2y = 0
$$

Solving the first equation for $x$, we get

$$
3x^2 -3= 0 \qquad\Longrightarrow\qquad x=\pm 1
$$

Solving the second equation for $y$, we get

$$
2y = 0 \qquad\Longrightarrow\qquad y=0
$$

Hence, $(1,0)$ and $(-1,0)$ are stationary points of $f(x,y)$.

To classify these stationary points, we use the second partial derivative test.

First, we compute the second partial derivatives:

$$
\begin{aligned}
\begin{bmatrix}\frac{∂^{2}f}{∂x^{2}} &= \frac{∂}{∂x}(3x^{2} - 3) = 6x & \frac{∂^{2}f}{∂x∂y}|= \frac{∂}{∂x}(2y) = 0 \\ \frac{∂^{2}f}{∂y∂x}|= \frac{∂}{∂y}(3x^{2} - 3) = 0 & \frac{∂^{2}f}{∂y^{2}}|= \frac{∂}{∂y}(2y) = 2\end{bmatrix}
\end{aligned}
$$

So, the second derivative is

$$
\begin{aligned}
f^{″}(x, y) &= \begin{bmatrix}\frac{∂^{2}f}{∂x^{2}} & \frac{∂^{2}f}{∂x∂y} \\ \frac{∂^{2}f}{∂y∂x} & \frac{∂^{2}f}{∂x∂y}\end{bmatrix} = \begin{bmatrix}6x & 0 \\ 0 & 2\end{bmatrix}
\end{aligned}
$$

Finally, we evaluate the second derivative at each of the stationary points and use it to categorize them:

- Evaluating the second derivative at $(1,0)$, we obtain
$f^{″}(1, 0) = \begin{bmatrix}6 & 0 \\ 0 & 2\end{bmatrix}$.
Computing the determinant $H(1,0)$, we get
$H(1,0) = \det(\boldsymbol f''(1,0)) = 6 \cdot 2 - 0^2 = 12 > 0$.
Now, since $H(1,0) > 0$ and $\dfrac{\partial^2 f}{\partial x^2}(1,0) = (\boldsymbol f''(1,0))_{11}= 6>0$, the function $f$ has a local minimum at $(1,0)$.
- Evaluating the second derivative at $(-1,0)$, we obtain
$f^{″}(-1, 0) = \begin{bmatrix}-6 & 0 \\ 0 & 2\end{bmatrix}$.
Computing the determinant $H(-1,0)$, we get
$H(-1,0) = \det(\boldsymbol f''(-1,0)) = -6\cdot 2 - 0^2 = -12 < 0$
Therefore, $f(-1,0)$ is a saddle point.

In conclusion, $f(x,y)$ has a local minimum at $(1,0)$ and a saddle point at $(-1,0)$.

---

**Question 5:**

```quiz
type: radio
id: ma-37592
content: |-
  Find and categorize all the local extrema of the function

  $f(x, y)$
  given that
  $f^{′}(x, y) = \begin{bmatrix}3x^{2} - 6y & 3y^{2} - 6x\end{bmatrix}$.
options:
- id: a
  content: |-
    $f(x, y)$ has a saddle point at $(0, 0)$ and a local maximum at $(2, 2)$
- id: b
  content: |-
    $f(x, y)$ has saddle points at $(0, 0)$ and $(2, 2)$
- id: c
  content: |-
    $f(x, y)$ has a saddle point at $(0, 0)$ and a local minimum at $(2, 2)$
  correct: true
- id: d
  content: |-
    $f(x, y)$ has a local minimum at $(0, 0)$ and a local maximum at $(-2,-2)$
- id: e
  content: |-
    $f(x, y)$ has a local minimum at $(0, 0)$ and a saddle point at $(-2,-2)$
```

---

**Question 6:**

```quiz
type: radio
id: ma-37590
content: |-
  Find and categorize all local the extrema of the function $f(x, y) = x^{2} + 2xy + 3y^{2} + 2x + 10y + 1$, and determine which of the following statements is true.
options:
- id: a
  content: |-
    $f(x, y)$ has a local minimum at $(1,-2)$
  correct: true
- id: b
  content: |-
    $f(x, y)$ has a local minimum at $(-3, 2)$
- id: c
  content: |-
    $f(x, y)$ has a local maximum at $(-3, 2)$
- id: d
  content: |-
    $f(x, y)$ has a saddle point at $(1,-2)$
- id: e
  content: |-
    $f(x, y)$ has a local maximum at $(1,-2)$
```

---

<a id="relating-the-second-partial-derivatives-test-to-the-taylor-polynomial-classification"></a>
## Relating the Second Partial Derivatives Test to the Taylor Polynomial Classification

Suppose $\mathbf{a} \in \mathbb{R}^2$ is a stationary point of a function $f(\mathbf x)$.

In a previous lesson, we saw that if

$$
H(\mathbf{a}) = \det(\boldsymbol{f}''(\mathbf{a})) \neq 0
$$

, then we can classify $\mathbf{a}$ as follows:

- if $\boldsymbol{f}''(\mathbf{a})$ is positive definite, then $\mathbf{a}$ is a local minimum
- if $\boldsymbol{f}''(\mathbf{a})$ is negative definite, then $\mathbf{a}$ is a local maximum
- if $\boldsymbol{f}''(\mathbf{a})$ is neither positive-definite nor negative-definite, then $\mathbf{a}$ is a saddle point

The second derivatives test follows from these facts. To see why, we first recall Sylvester's criterion:

> The quadratic form $\mathbf{x}^TA\mathbf{x}$ (as well as the $2\times 2$ matrix $A$) is
> - positive definite if and only if $\Delta_1 > 0$ and $\Delta_2 > 0$, and
> - negative definite if and only if $\Delta_1 < 0$ and $\Delta_2 > 0$,
> where $\Delta_1$ and $\Delta_2$ are the first and second principle minors of $A$, respectively.

First, notice the following:

- The first principal minor of $\boldsymbol f''(\mathbf a)$ is
$\Delta_1 = (\boldsymbol{f}''(\mathbf{a}))_{11} = \dfrac{\partial^2 f}{\partial x^2}$.
- The second principal minor of $\boldsymbol f''(\mathbf a)$ is the Hessian determinant
$\Delta_2 = \det(\boldsymbol{f}''(\mathbf{a})) = H(\mathbf{a})$.

Knowing this, we can go through each case:

- If $H(\mathbf a) < 0$, then $\Delta_2 < 0$. Hence, by Sylvester's criterion, the Hessian matrix $\boldsymbol f''(\mathbf{a})$ is neither positive definite nor negative definite, and we conclude that $f$ has a saddle point at $\mathbf{a}$.
- If $H(\mathbf a) > 0$, then $\Delta_2 > 0$. Then:If $\Delta_1 = \dfrac{\partial^2 f}{\partial x^2} < 0$, then $\boldsymbol f''(\mathbf{a})$ is negative definite by Sylvester's criterion, and we conclude that $f$ has a local maximum at $\mathbf{a}$.If $\Delta_1 = \dfrac{\partial^2 f}{\partial x^2} > 0$, then $\boldsymbol f''(\mathbf{a})$ is positive definite by Sylvester's criterion, and we conclude that $f$ has a local minimum at $\mathbf{a}$.

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
