# Lagrange Multipliers With Multiple Constraints

<!--
lesson-id: 3380
topic-code: MML.9.8.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Constructing a Lagrange System With Two Constraints](#constructing-a-lagrange-system-with-two-constraints)
- [Solving a Lagrange System With Two Constraints](#solving-a-lagrange-system-with-two-constraints)
- [Justification of the Lagrange Multiplier Method With Multiple Constraints](#justification-of-the-lagrange-multiplier-method-with-multiple-constraints)

## Prerequisites

- [The Multivariable Chain Rule in Vector Form](<../../9.3. Vector-Valued Functions/Lessons/9.3.6. The Multivariable Chain Rule in Vector Form.md>)
- [Lagrange Multipliers With One Constraint](<9.8.4. Lagrange Multipliers With One Constraint.md>)
- [Intersections of Lines and Planes With Surfaces](<../../9.1. Quadric Surfaces and Cylinders/Lessons/9.1.6. Intersections of Lines and Planes With Surfaces.md>)

---

<a id="introduction"></a>
## Introduction

We can find global extrema of multivariable functions subject to multiple constraints using the method of Lagrange multipliers.

For example, suppose that we want to find the minimum value of

$$
f(x,y,z) = 4xy+z^2
$$

subject to the following *two* constraints:

$$
g(x,y,z)=x^2+y^2=1, \qquad h(x,y,z)=2x^2 + 4y^2 - z = 0
$$

Geometrically, this means that we want to minimize $f(x,y,z)$ over the intersection of the cylinder

$$
x^2+y^2=1
$$

and the elliptic paraboloid

$$
2x^2 + 4y^2 - z = 0
$$

as shown below.

![](<../Source/9.8.5. Lagrange Multipliers With Multiple Constraints/Images/d2865657041d52ba80ab5056d09a7cfa.png>)

The method of Lagrange multipliers states that the extrema of the function $f(x,y,z)$ subject to constraints $g(x,y,z)=a$ and $h(x,y,z)=b$ can be found by solving the following system:

$$
{∇f(x, y, z) = λ∇g(x, y, z) + μ∇h(x, y, z); g(x, y, z) = a; h(x, y, z) = b
$$

Here, the constants $\lambda$ and $\mu$ are the Lagrange multipliers of this system.

As before, this method assumes that

$$
\nabla g \neq \mathbf{0}
$$

and

$$
\nabla h \neq \mathbf{0}
$$

on the curves $g(x,y,z)=a$ and $h(x,y,z)=b$, and that the extreme values exist.

**Note:** Geometrically, the condition

$$
\nabla f = \lambda \nabla g + \mu \nabla h
$$

states that

- $\nabla f$ lies in the plane spanned by $\nabla g$ and $\nabla h$, or, equivalently,
- $\nabla f$ is perpendicular to the level surfaces $g(x,y,z) = a$ and $h(x,y,z) = b$.

We'll explore this geometric intuition further at the end of the lesson.

---

<a id="constructing-a-lagrange-system-with-two-constraints"></a>
## Constructing a Lagrange System With Two Constraints

**Example:** Suppose we want to find the minimum of the function $f(x,y,z)=2xz+y^2$ on the intersection of the surfaces $x-z=2$ and $x^2+y^2+2z^2=3$ using the method of Lagrange multipliers. Then, the corresponding Lagrange multipliers $\lambda$ and $\mu$ satisfy the following system:

${⟨2z, A A, 2x⟩ = λ⟨1, 0, A A⟩ + μ⟨2x, 2y, A A⟩; x - z = 2; x^{2} + y^{2} + 2z^{2} = 3$

From left to right, what are the missing parts of the expressions?

**Explanation**

We need to find the minimum value of

$$
f(x,y,z) = 2xz+y^2
$$

subject to the constraints

$$
g(x,y,z)=x-z=2, \qquad h(x,y,z)=x^2+y^2+2z^2=3
$$

Therefore, by the method of Lagrange multipliers, we must solve the following system:

$$
{∇f(x, y, z) = λ∇g(x, y, z) + μ∇h(x, y, z); g(x, y, z) = 2; h(x, y, z) = 3
$$

We now expand the first equation of the system:

$$
\begin{aligned}
∇f(x, y, z) &= λ∇g(x, y, z) + μ∇h(x, y, z) \\
⟨f_{x}, f_{y}, f_{z}⟩ &= λ⟨g_{x}, g_{y}, g_{z}⟩ + μ⟨h_{x}, h_{y}, h_{z}⟩ \\
⟨2z, 2 y, 2x⟩ &= λ⟨1, 0,-1⟩ + μ⟨2x, 2y, 4 z⟩
\end{aligned}
$$

Therefore, the missing parts are $2y,\:$ $-1,\:$ and $4z$.

---

**Question 1**

```quiz
type: radio
id: ma-162671
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Suppose we would like to find the minimum value of the function $f(x, y, z) = x + y^{2} + z$ on the intersection of the surfaces $3y^{2} + z^{2} = 2$ and $x + z^{2} = 1$ using the method of Lagrange multipliers. Then, the corresponding Lagrange multipliers $λ$ and $μ$ satisfy the following system:

  ${⟨1, 2y, 1⟩ = λ⟨0, A A, 2z⟩ + μ⟨1, A A, 2z⟩; 3y^{2} + z^{2} = 2; x + z^{2} = 1$

  From left to right, what are the missing expressions?
options:
- id: a
  content: |-
    $3y^{2}$ and $z$
- id: b
  content: |-
    $3y^{2}$ and $y$
- id: c
  content: |-
    $6y$ and $y$
- id: d
  content: |-
    $6y$ and $0$
- id: e
  content: |-
    $3y$ and $1$
```

---

**Question 2**

```quiz
type: radio
id: ma-162670
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Suppose we would like to find the maximum of the function $f(x, y, z) = xy^{2}z$ on the intersection of the surfaces $x^{2} - 2z^{2} = 1$ and $y^{2} + 3z^{3} = 5$ using the method of Lagrange multipliers. Then, the corresponding Lagrange multipliers $λ$ and $μ$ satisfy the following system:

  ${⟨y^{2}z, A A, xy^{2}⟩ = λ⟨2x, 0, A A⟩ + μ⟨0, 2y, A A⟩; x^{2} - 2z^{2} = 1; y^{2} + 3z^{3} = 5$

  From left to right, what are the missing parts of the expressions?
options:
- id: a
  content: |-
    $-2y$, $-4z$, and $3z^{2}$
- id: b
  content: |-
    $-2yz$, $2x$, and $3z^{3}$
- id: c
  content: |-
    $2xyz$, $-2z^{2}$, and $3z^{2}$
- id: d
  content: |-
    $y^{2}z$, $-z^{2}$, and $3z^{2}$
- id: e
  content: |-
    $2xyz$, $-4z$, and $9z^{2}$
```

---

<a id="solving-a-lagrange-system-with-two-constraints"></a>
## Solving a Lagrange System With Two Constraints

**Example:** Use the method of Lagrange multipliers to find the minimum value of the function $f(x,y,z) = x-y-z$ on the intersection of the plane $x+y+z=2$ and the cylinder $x^2+z^2=4$.

**Explanation**

We need to find the minimum value of

$$
f(x,y,z) = x-y-z
$$

subject to the constraints

$$
g(x,y,z)=x+y+z=2, \qquad h(x,y,z)=x^2 + z^2 = 4
$$

Since $f(x,y,z)$ is continuous and the constraint is closed and bounded, $f(x,y,z)$ reaches a global maximum and a global minimum on the constraint.

Therefore, by the method of Lagrange multipliers, we must solve the following system:

$$
{∇f(x, y, z) = λ∇g(x, y, z) + μ∇h(x, y, z); g(x, y, z) = 2; h(x, y, z) = 4
$$

We start by expanding the first equation:

$$
\begin{aligned}
∇f(x, y, z) &= λ∇g(x, y, z) + μ∇h(x, y, z) \\
⟨f_{x}, f_{y}, f_{z}⟩ &= λ⟨g_{x}, g_{y}, g_{z}⟩ + μ⟨h_{x}, h_{y}, h_{z}⟩ \\
⟨1,-1,-1⟩ &= λ⟨1, 1, 1⟩ + μ⟨2x, 0, 2z⟩
\end{aligned}
$$

Therefore, we have the following system:

$$
{1 = λ + 2μx; - 1 = λ; - 1 = λ + 2μz⇒{x = \frac{1}{μ}; λ =-1; μz = 0
$$

Notice that $\mu$ can't be zero since this leads to a contradiction. So, from the third equation above, we must have $z=0$.

Next, we substitute $z=0$ into the second constraint:

$$
\begin{aligned}
x^{2} + z^{2} &= 4 \\
x^{2} &= 4 \\
x &= ± 2
\end{aligned}
$$

Finally, we substitute $x$ and $z$ into the first constraint:

- If $x=2$ and $z= 0$, we have
$x + y + z|= 2; 2 + y + 0|= 2; 2 + y|= 2; y|= 0$.
- If $x=-2$ and $z=0$, we have
$x + y + z|= 2; - 2 + y + 0|= 2; - 2 + y|= 2; y|= 4$.

Therefore, $f$ has possible extreme values at the points
$(2, 0, 0), \qquad (-2, 4, 0)$.

We evaluate $f$ at each of these points. These are given in the following table:

| $(x,y,z)$ | $(2, 0, 0)$ | $(-2, 4, 0)$ |
| --- | ---: | ---: |
| $f(x,y,z)$ | $2$ | $-6$ |

Therefore, the function reaches a minimum value at $(-2, 4, 0)$, and its minimum value is $-6$.

---

**Question 3:**

```quiz
type: radio
id: ma-162669
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Use the method of Lagrange multipliers to find the minimum value of the function $f(x, y, z) = 2x - 2y + z$ on the intersection of the plane $x - y + z = 1$ and the cylinder $x^{2} + y^{2} = 2$.
options:
- id: a
  content: |-
    $1$
- id: b
  content: |-
    $-1$
- id: c
  content: |-
    $-5$
- id: d
  content: |-
    $-3$
- id: e
  content: |-
    $2$
```

---

**Question 4:**

```quiz
type: radio
id: ma-162898
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Use the method of Lagrange multipliers to find the maximum value of the function $f(x, y, z) = x + 2y - 6z$ on the intersection of the plane $2x - 3y + 2z = 0$ and the cylinder $x^{2} + y^{2} = 2$.
options:
- id: a
  content: |-
    $10$
- id: b
  content: |-
    $15$
- id: c
  content: |-
    $14$
- id: d
  content: |-
    $-12$
- id: e
  content: |-
    $12$
```

---

<a id="justification-of-the-lagrange-multiplier-method-with-multiple-constraints"></a>
## Justification of the Lagrange Multiplier Method With Multiple Constraints

Let's now justify why the Lagrange multipliers method works with multiple constraints.

Suppose we wish to optimize the function $f(x,y,z)$ subject to the constraints $g(x,y,z)=a$ and $h(x,y,z)=b$.

Note the following:

- The natural domain of $f$ is some subset of $\mathbb R^3$.
- If we were to impose the first constraint only, we'd restrict the domain of $f$ to points on the level surface $g(x,y,z)=a$.
- Similarly, if we were to impose the second constraint only, we'd restrict the domain of $f$ to points on the level surface $h(x,y,z)=b$.
- Thus, by imposing *both* constraints, we restrict the domain of $f$ to the *intersection* of these two level surfaces. The intersection of two surfaces is a curve, which we'll denote as $\mathbf r(t)$ for some real parameter $t$.

The curve $\mathbf r(t)$ and the level surfaces are shown below.

![](<../Source/9.8.5. Lagrange Multipliers With Multiple Constraints/Images/31ff5053e94fed552e5347d2b9948602.png>)

Now, let $P(x_0,y_0,z_0)$ be a point that lies on $\mathbf r(t)$ for some $t = t_0$ that is an extrema (max or min) of $f$.

Since $g(x,y,z)=a$ and $h(x,y,z)=b$ are level surfaces of the functions $g(x,y,z)$ and $h(x,y,z)$, we have the following:

- $\nabla g$ is perpendicular to $g(x,y,z)=a$ at $P$, and is therefore perpendicular to $\mathbf r(t)$ at $P$.
- $\nabla h$ is perpendicular to $h(x,y,z)=b$ at $P$, and is therefore perpendicular to $\mathbf r(t)$ at $P$.

The situation is sketched below.

![](<../Source/9.8.5. Lagrange Multipliers With Multiple Constraints/Images/b7760c04c3a8e0c32042233da0996f73.png>)

Let's now compute $\big[f(\mathbf r(t))\big]'$, the derivative of $f$ over the curve $\mathbf r(t)$. By the chain rule, we get

$$
\begin{aligned}
[f(r(t))]^{′} &= [f(x(t), y(t), z(t))]^{′} \\
&= f_{x}(x, y, z) \cdot x^{′}(t) + f_{y}(x, y, z) \cdot y^{′}(t) + f_{z}(x, y, z) \cdot z^{′}(t) \\
&= ⟨f_{x}(x, y, z), f_{y}(x, y, z), f_{z}(x, y, z)⟩ \cdot ⟨x^{′}(t), y^{′}(t), z^{′}(t)⟩ \\
&= ∇f(r(t)) \cdot r^{′}(t)
\end{aligned}
$$

Since $P$ is an extremum, we must have

$$
\big[f(\mathbf{r}(t_0))\big]' = 0 \qquad\Longrightarrow\qquad \nabla f(\mathbf{r}(t_0)) \cdot \mathbf{r}'(t_0) = 0 \qquad\Longrightarrow\qquad \nabla f(\mathbf{r}(t_0)) \perp \mathbf{r}'(t_0)
$$

Since $\mathbf{r}'(t_0)$ is tangent to $\mathbf r(t)$ at $P$, we have that $\nabla f(\mathbf{r}(t_0))$ is perpendicular to $\mathbf r(t)$ at $P$.

So, $\nabla g$ and $\nabla h$ are both perpendicular to $\mathbf{r}(t)$ at $P$. Assuming they aren't parallel, they span the two-dimensional space perpendicular to $\mathbf{r}(t)$. Now $\nabla f$ is also perpendicular to $\mathbf{r}(t)$ at $P$. This means that $\nabla f$ lies in the plane spanned by $\nabla g$ and $\nabla h$. This condition can be represented by the following equation:

$$
\nabla f(x,y,z) = \lambda \nabla g(x,y,z) + \mu \nabla h(x,y,z)
$$

where the real numbers $\lambda$ and $\mu$ are the Lagrange multipliers.

![](<../Source/9.8.5. Lagrange Multipliers With Multiple Constraints/Images/e017039352f0d7c52d06be4b7e935d56.png>)

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
