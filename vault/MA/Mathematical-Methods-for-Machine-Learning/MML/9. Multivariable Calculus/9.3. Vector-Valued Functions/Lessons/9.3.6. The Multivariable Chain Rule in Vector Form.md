# The Multivariable Chain Rule in Vector Form

<!--
lesson-id: 1936
topic-code: MML.9.3.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Applying the Vector Form of the Chain Rule to a Function of Two Variables](#applying-the-vector-form-of-the-chain-rule-to-a-function-of-two-variables)
- [Applying the Vector Form of the Chain Rule to a Function of Three Variables](#applying-the-vector-form-of-the-chain-rule-to-a-function-of-three-variables)
- [Evaluating the Derivative at a Point Using the Chain Rule](#evaluating-the-derivative-at-a-point-using-the-chain-rule)
- [The Derivative as a Rate of Change](#the-derivative-as-a-rate-of-change)
- [Computing the Rate of Change of a Multivariable Function at a Point Using the Chain Rule](#computing-the-rate-of-change-of-a-multivariable-function-at-a-point-using-the-chain-rule)
- [Intuition Behind the Vector Form of the Multivariable Chain Rule](#intuition-behind-the-vector-form-of-the-multivariable-chain-rule)

## Prerequisites

- [The Gradient Vector](<9.3.3. The Gradient Vector.md>)
- [The Multivariable Chain Rule](<../../9.2. Partial Derivatives/Lessons/9.2.13. The Multivariable Chain Rule.md>)
- [Differentiating Vector-Valued Functions](<../../../../CBC/11. Parametric Equations/11.2. Vector-Valued Functions/Lessons/11.2.2. Differentiating Vector-Valued Functions.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we have the function

$$
f(\mathbf{r}) = f(x,y) = xy^2
$$

which takes a vector-valued function

$$
\mathbf{r}(t) = \left\langle x(t), y(t) \right\rangle = \left\langle \cos t, \sin t \right\rangle
$$

as input. How do we calculate

$$
\dfrac{\textrm{d}}{\textrm{d}t}\left[f(\mathbf{r}(t))\right]
$$

To do this, we use the **multivariable chain rule in vector form**, which states

$$
\dfrac{\textrm{d}}{\textrm{d}t}\left[f(\mathbf{r}(t))\right] = \nabla f(\mathbf{r}(t))\cdot \mathbf{r}'(t)
$$

Let's now apply this to our functions. First, we compute $\nabla f(x,y){:}$

$$
\begin{aligned}
∇f(x, y) &= ⟨\frac{∂}{∂x}(f(x, y)), \frac{∂}{∂y}(f(x, y))⟩ \\
&= ⟨\frac{∂}{∂x}(xy^{2}), \frac{∂}{∂y}(xy^{2})⟩ \\
&= ⟨y^{2}, 2xy⟩
\end{aligned}
$$

Next, we substitute $x=\cos t$ and $y=\sin t$ into the above to find $\nabla f(\mathbf{r}(t)){:}$

$$
\begin{aligned}
∇f(r(t)) &= ⟨(\sin t)^{2}, 2(\cos t)(\sin t)⟩ \\
&= ⟨\sin^{2} t, 2\sin t\cos t⟩
\end{aligned}
$$

Now, we calculate the derivative of $\mathbf{r}(t)$, which is

$$
\begin{aligned}
r^{′}(t) &= ⟨\frac{d}{dt}(\cos t), \frac{d}{dt}(\sin t)⟩ \\
&= ⟨ - \sin t, \cos t⟩
\end{aligned}
$$

Finally, applying the chain rule, we get

$$
\begin{aligned}
\frac{d}{dt}[f(r(t))] &= ∇f(r(t)) \cdot r^{′}(t) \\
&= ⟨\sin^{2} t, 2\sin t\cos t⟩ \cdot ⟨ - \sin t, \cos t⟩ \\
&=-\sin^{3} t + 2\sin t\cos^{2} t
\end{aligned}
$$

---

<a id="applying-the-vector-form-of-the-chain-rule-to-a-function-of-two-variables"></a>
## Applying the Vector Form of the Chain Rule to a Function of Two Variables

**Example:** If $f(x,y) = x^2 + 2 y^3$ and $\mathbf r (t) = \sin t \ \mathbf i + e^{t} \ \mathbf j$, compute $\dfrac {\textrm d}{\textrm d t} [f(\mathbf r(t))]$.

**Explanation**

The chain rule in vector form states that

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))] = \nabla f(\mathbf r(t)) \cdot \mathbf r'(t)
$$

First, we need to compute $\nabla f(x,y){:}$

$$
\begin{aligned}
∇f(x, y) &= \frac{∂}{∂x}(x^{2} + 2y^{3})i + \frac{∂}{∂y}(x^{2} + 2y^{3})j \\
&= 2xi + 6y^{2}j
\end{aligned}
$$

Next, we substitute $x = \sin t$ and $y=e^{t}$ into the above to find $\nabla f(\mathbf{r}(t)){:}$

$$
\begin{aligned}
∇f(r(t)) &= 2(\sin t)i + 6(e^{t})^{2}j \\
&= 2\sin ti + 6e^{2t}j
\end{aligned}
$$

Now, we calculate the derivative of $\mathbf{r}(t)$, which is

$$
\begin{aligned}
r^{′}(t) &= \frac{d}{dt}(\sin t)i + \frac{d}{dt}(e^{t})j \\
&= \cos ti + e^{t}j
\end{aligned}
$$

Finally, applying the chain rule, we get

$$
\begin{aligned}
\frac{d}{dt}[f(r(t))] &= ∇f(r(t)) \cdot r^{′}(t) \\
&= (2\sin ti + 6e^{2t}j) \cdot (\cos ti + e^{t}j) \\
&= 2\sin t\cos t + 6e^{2t} \cdot e^{t} \\
&= \sin 2t + 6e^{3t}
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-95056
content: |-
  If $f(x, y) = \ln xy$ and $r(t) = \cos ti + t^{2}j$, compute $\frac{d}{dt}[f(r(t))]$.
options:
- id: a
  content: |-
    $\frac{2t + 1}{t^{2}}$
- id: b
  content: |-
    $\frac{2 - t\tan t}{t}$
  correct: true
- id: c
  content: |-
    $\frac{t^{2}\sec t + 1}{t^{2}}$
- id: d
  content: |-
    $\frac{2 - t\sec t}{t}$
- id: e
  content: |-
    $\frac{t^{2}\tan t + 2}{t^{2}}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-36211
content: |-
  If $f(x, y) = x^{2} + y^{2}$ and $r(t) = e^{t}i + e^{-t}j$, compute $\frac{d}{dt}[f(r(t))]$.
options:
- id: a
  content: |-
    $4e^{2t}$
- id: b
  content: |-
    $2e^{2t} + 2e^{-2t}$
- id: c
  content: |-
    $2e^{t} - 2e^{-t}$
- id: d
  content: |-
    $2e^{2t} - 2e^{-2t}$
  correct: true
- id: e
  content: |-
    $e^{2t} + e^{-2t}$
```

---

<a id="applying-the-vector-form-of-the-chain-rule-to-a-function-of-three-variables"></a>
## Applying the Vector Form of the Chain Rule to a Function of Three Variables

**Example:** If $f(x,y,z) = xz\ln{y}$ and $\mathbf r (t) = t^2 \, \mathbf i + t \, \mathbf j +t^3 \, \mathbf k$, compute $\dfrac {\textrm d}{\textrm d t} [f(\mathbf r(t))]$.

**Explanation**

The chain rule in vector form states that

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))] = \nabla f(\mathbf r(t)) \cdot \mathbf r'(t)
$$

First, we need to compute $\nabla f(x,y,z){:}$

$$
\begin{aligned}
∇f(x, y, z) &= \frac{∂}{∂x}(xz\ln y)i + \frac{∂}{∂y}(xz\ln y)j + \frac{∂}{∂z}(xz\ln y)k \\
&= z\ln yi + \frac{xz}{y}j + x\ln yk
\end{aligned}
$$

Next, we substitute $x= t^2$, $y = t$ and $z= t^3$ into the above to find $\nabla f(\mathbf{r}(t)){:}$

$$
\begin{aligned}
∇f(r(t)) &= t^{3}\ln ti + t^{4}j + t^{2}\ln tk
\end{aligned}
$$

Now, we calculate the derivative of $\mathbf{r}(t)$, which is

$$
\begin{aligned}
r^{′}(t) &= \frac{d}{dt}(t^{2})i + \frac{d}{dt}(t)j + \frac{d}{dt}(t^{3})k \\
&= 2ti + j + 3t^{2}k
\end{aligned}
$$

Finally, applying the chain rule, we get

$$
\begin{aligned}
\frac{d}{dt}[f(r(t))] &= ∇f(r(t)) \cdot r^{′}(t) \\
&= (t^{3}\ln ti + t^{4}j + t^{2}\ln tk) \cdot (2ti + j + 3t^{2}k) \\
&= (t^{3}\ln t) \cdot 2t + (t^{4}) \cdot 1 + (t^{2}\ln t) \cdot 3t^{2} \\
&= 2t^{4}\ln t + t^{4} + 3t^{4}\ln t \\
&= t^{4}(5\ln t + 1)
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-95075
content: |-
  If $f(x, y, z) = z(x + y)$ and $r(t) = \sin ti + \cos tj + e^{t}k$, compute $\frac{d}{dt}[f(r(t))]$.
options:
- id: a
  content: |-
    $2e^{t}\sin t$
- id: b
  content: |-
    $2e^{t}\cos t$
  correct: true
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $2e^{t} + \sin t + \cos t$
- id: e
  content: |-
    $2e^{t}(\sin t + \cos t)$
```

---

**Question 4:**

```quiz
type: radio
id: ma-36238
content: |-
  If $f(x, y, z) = xy + yz + xz$ and $r(t) = ti + e^{t}j + ek$, compute $\frac{d}{dt}[f(r(t))]$.
options:
- id: a
  content: |-
    $e^{t + 1} + te^{t} + e$
- id: b
  content: |-
    $e^{t + 1} + (t + 1)e^{t} + 2$
- id: c
  content: |-
    $e^{t + 1} + (1 + 2t)e^{t} + e$
- id: d
  content: |-
    $e^{t + 1} + (1 + t)e^{t} + e$
  correct: true
- id: e
  content: |-
    $2e^{t} + 2e + t$
```

---

<a id="evaluating-the-derivative-at-a-point-using-the-chain-rule"></a>
## Evaluating the Derivative at a Point Using the Chain Rule

**Example:** Evaluate $\dfrac {\textrm d}{\textrm d t}[f(\mathbf r(t))]$ at $t = \pi$ given that
$\nabla f(x,y) = (2y - 1) \, \mathbf i + (2x-1) \, \mathbf j$
and
$\mathbf r (t) = \sin{t} \, \mathbf i + \cos{t} \, \mathbf j$.

**Explanation**

The chain rule in vector form states that

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))] = \nabla f(\mathbf r(t)) \cdot \mathbf r'(t)
$$

In addition, we've been given that

$$
\nabla f(x,y) = (2y - 1) \, \mathbf i + (2x - 1) \, \mathbf j
$$

First, we substitute $x = \sin{t}$ and $y = \cos{t}$ into $\nabla f(x,y)$ to find $\nabla f(\mathbf{r}(t)){:}$

$$
\begin{aligned}
∇f(r(t)) &= (2\cos t - 1)i + (2\sin t - 1)j
\end{aligned}
$$

Now, we calculate the derivative of $\mathbf{r}(t)$, which is

$$
\begin{aligned}
r^{′}(t) &= \frac{d}{dt}(\sin t)i + \frac{d}{dt}(\cos t)j \\
&= \cos ti + (-\sin t)j
\end{aligned}
$$

Applying the chain rule, we get

$$
\begin{aligned}
\frac{d}{dt}[f(r(t))] &= ∇f(r(t)) \cdot r^{′}(t) \\
&= (2\cos t - 1i + (2\sin t - 1)j) \cdot (\cos ti + (-\sin t)j) \\
&= (2\cos t - 1) \cdot \cos t + (2\sin t - 1) \cdot (-\sin t) \\
&= 2\cos^{2} t - \cos t - 2\sin^{2} t + \sin t \\
&= 2(\cos^{2} t - \sin^{2} t) + \sin t - \cos t \\
&= 2\cos 2t + \sin t - \cos t
\end{aligned}
$$

Finally, evaluating the above at $t = \pi$, we obtain

$$
\begin{aligned}
\frac{d}{dt}[f(r(π))] &= 2\cos 2π + \sin π - \cos π \\
&= 2(1) + 0 - (-1) \\
&= 3
\end{aligned}
$$

---

**Question 5:**

```quiz
type: radio
id: ma-94872
content: |-
  Evaluate $\frac{d}{dt}[f(r(t))]$ at $t = \frac{π}{2}$ given that $∇f(x, y) = \sin yi + x\cos yj$ and $r(t) = \frac{t}{2}i + tj$.
options:
- id: a
  content: |-
    $\frac{1}{2}$
  correct: true
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $2 - π$
- id: d
  content: |-
    $-1$
- id: e
  content: |-
    $\frac{π}{4}$
```

---

**Question 6:**

```quiz
type: radio
id: ma-36215
content: |-
  Evaluate $\frac{d}{dt}[f(r(t))]$ at $t = 1$ given that

  $∇f(x, y) = \frac{2x}{x^{2} + 3y}i + \frac{3}{x^{2} + 3y}j$
  and
  $r(t) = ti + t^{2}j$.
options:
- id: a
  content: |-
    $2$
  correct: true
- id: b
  content: |-
    $6$
- id: c
  content: |-
    $4$
- id: d
  content: |-
    $\frac{1}{2}$
- id: e
  content: |-
    $1$
```

---

<a id="the-derivative-as-a-rate-of-change"></a>
## The Derivative as a Rate of Change

We can consider $f(\mathbf{r}(t))$ as the values taken by the function $f$ along the curve $\mathbf{r}(t)$. So, the derivative

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))]
$$

can be interpreted as the **rate of change of $f$ with respect to $t$ along the curve $\mathbf{r}(t)$.**

Furthermore, if $\mathbf{r}(t)$ is a level curve

$$
f(x,y)=c
$$

then by the chain rule we have

$$
\begin{aligned}
f(r(t)) &= c \\
\frac{d}{dt}(f(r(t))) &= \frac{d}{dt}(c) \\
∇f(r(t)) \cdot r^{′}(t) &= 0
\end{aligned}
$$

From this, we can conclude that the gradient vector $\nabla f$ is orthogonal to every level curve of $f$.

---

<a id="computing-the-rate-of-change-of-a-multivariable-function-at-a-point-using-the-chain-rule"></a>
## Computing the Rate of Change of a Multivariable Function at a Point Using the Chain Rule

**Example:** Compute the rate of change of the function $f(x,y,z)=x^2-y^2-z$ with respect to $t$ along the curve $\mathbf r (t) = \left \langle \sin t, \cos t, t \right \rangle$ at the point $t=\dfrac \pi 4$.

**Explanation**

The rate of change of $f(x,y,z)$ with respect to $t$ along the curve $\mathbf r (t)$ is given by

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))]
$$

To calculate this, we can use the chain rule.

The chain rule in vector form states that

$$
\dfrac {\textrm d}{\textrm d t} [f(\mathbf r (t))] = \nabla f(\mathbf{r}(t)) \cdot \mathbf{r}'(t)
$$

First, we need to compute $\nabla f(x,y){:}$

$$
\begin{aligned}
∇f(x, y) &= ⟨\frac{∂}{∂x}(x^{2} - y^{2} - z), \frac{∂}{∂y}(x^{2} - y^{2} - z), \frac{∂}{∂z}(x^{2} - y^{2} - z)⟩ \\
&= ⟨2x,-2y,-1⟩
\end{aligned}
$$

Next, we substitute

$$
x = \sin t, y=\cos t
$$

and $z=t$ into the above to find $\nabla f(\mathbf r(t)){:}$

$$
\begin{aligned}
∇f(r(t)) &= ⟨2\sin t,-2\cos t,-1⟩
\end{aligned}
$$

Now, we calculate the derivative of $\mathbf{r}(t)$, which is

$$
\begin{aligned}
r^{′}(t) &= ⟨\frac{d}{dt}(\sin t), \frac{d}{dt}(\cos t), \frac{d}{dt}(t)⟩ \\
&= ⟨\cos t,-\sin t, 1⟩
\end{aligned}
$$

Applying the chain rule, we get

$$
\begin{aligned}
\frac{d}{dt}[f(r(t))] &= ∇f(r(t)) \cdot r^{′}(t) \\
&= ⟨2\sin t,-2\cos t,-1⟩ \cdot ⟨\cos t,-\sin t, 1⟩ \\
&= 2\sin t \cdot \cos t - 2\cos t \cdot (-\sin t) - 1 \cdot 1 \\
&= 2\sin t\cos t + 2\sin t\cos t - 1 \\
&= 4\sin t\cos t - 1
\end{aligned}
$$

Therefore, the rate of change of $f$ along the curve $\mathbf{r}(t)$ at

$$
t=\dfrac{\pi}{4}
$$

is

$$
\begin{aligned}
\frac{d}{dt}[f(r(\frac{π}{4}))] &= 4\sin (\frac{π}{4})\cos (\frac{π}{4}) - 1 \\
&= 4 \cdot \frac{\sqrt{2}}{2} \cdot \frac{\sqrt{2}}{2} - 1 \\
&= 1
\end{aligned}
$$

---

**Question 7:**

```quiz
type: radio
id: ma-36247
content: |-
  Compute the rate of change of the function $f(x, y, z) = x^{2} + 3xyz$ with respect to $t$ along the curve $r(t) = ⟨t^{2}, t + 1,-t⟩$ at the point $t = 1$.
options:
- id: a
  content: |-
    $9$
- id: b
  content: |-
    $1$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $-15$
- id: e
  content: |-
    $-17$
  correct: true
```

---

**Question 8:**

```quiz
type: radio
id: ma-36253
content: |-
  Compute the rate of change of the function $f(x, y) = \sin (x - y)$ with respect to $t$ along the curve $r(t) = ⟨2\sqrt{t}, 4t⟩$ at the point $t = 1$.
options:
- id: a
  content: |-
    $\cos 2$
- id: b
  content: |-
    $-3\cos 2$
  correct: true
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $5\cos 2$
- id: e
  content: |-
    $-3\cos 8$
```

---

<a id="intuition-behind-the-vector-form-of-the-multivariable-chain-rule"></a>
## Intuition Behind the Vector Form of the Multivariable Chain Rule

The chain rule for a function $f(\mathbf{r})$, where

$$
\mathbf{r}(t) = \left\langle x_1(t), x_2(t), \ldots, x_m(t) \right\rangle
$$

is a vector-valued function of $m$ variables, is the same as before:

$$
\dfrac{\textrm{d}f}{\textrm{d}t} = \nabla f(\mathbf{r}(t)) \cdot \mathbf{r}'(t)
$$

How does this chain rule in vector form relate to the chain rule we have seen before, which states that

$$
\dfrac {\textrm d f} {\textrm d t} = \dfrac {\partial f}{\partial x_1} \cdot \dfrac {\textrm d x_1}{\textrm d t} + \dfrac {\partial f}{\partial x_2} \cdot \dfrac {\textrm d x_2}{\textrm d t} + \dots + \dfrac {\partial f}{\partial x_m} \cdot \dfrac {\textrm d x_m}{\textrm d t}
$$

Let's expand $\nabla f(\mathbf{r}(t)) \cdot \mathbf{r}'(t)$ to see. Recall that

$$
∇f(r(t)) = ⟨\frac{∂f}{∂x_{1}}, \frac{∂f}{∂x_{2}}, …, \frac{∂f}{∂x_{m}}⟩
$$

and

$$
r^{′}(t) = ⟨\frac{dx_{1}}{dt}, \frac{dx_{2}}{dt}, …, \frac{dx_{m}}{dt}⟩
$$

Then

$$
\begin{aligned}
∇f(r(t)) \cdot r^{′}(t) &= ⟨\frac{∂f}{∂x_{1}}, \frac{∂f}{∂x_{2}}, …, \frac{∂f}{∂x_{m}}⟩ \cdot ⟨\frac{dx_{1}}{dt}, \frac{dx_{2}}{dt}, …, \frac{dx_{m}}{dt}⟩ \\
&= \frac{∂f}{∂x_{1}} \cdot \frac{dx_{1}}{dt} + \frac{∂f}{∂x_{2}} \cdot \frac{dx_{2}}{dt} + ⋯ + \frac{∂f}{∂x_{m}} \cdot \frac{dx_{m}}{dt}
\end{aligned}
$$

They are exactly the same, just different notations!

**Note**: If we replace $\mathbf{r}$ with a single-valued function $g(t)$, we get back the chain rule from single-variable calculus:

$$
\begin{aligned}
\frac{d}{dt}[f(g(t))] &= ∇f(g(t)) \cdot g^{′}(t) \\
&= \frac{df}{dg} \cdot \frac{dg}{dt}
\end{aligned}
$$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
