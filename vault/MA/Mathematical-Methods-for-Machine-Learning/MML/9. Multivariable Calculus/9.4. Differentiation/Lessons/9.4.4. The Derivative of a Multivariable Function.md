# The Derivative of a Multivariable Function

<!--
lesson-id: 4169
topic-code: MML.9.4.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Defining the Total Derivative](#defining-the-total-derivative)
- [Finding the Derivative of a Scalar Function](#finding-the-derivative-of-a-scalar-function)
- [Finding the Derivative of a Vector Function](#finding-the-derivative-of-a-vector-function)
- [The Tangent Plane to the Graph of a Scalar Function](#the-tangent-plane-to-the-graph-of-a-scalar-function)
- [Finding the Tangent Plane to the Graph of a Scalar Function in Matrix Form](#finding-the-tangent-plane-to-the-graph-of-a-scalar-function-in-matrix-form)
- [Affine Approximations of Vector Functions](#affine-approximations-of-vector-functions)
- [Finding an Affine Approximation of a Vector Function](#finding-an-affine-approximation-of-a-vector-function)

## Prerequisites

- [Linearization of Multivariable Functions](<../../9.2. Partial Derivatives/Lessons/9.2.12. Linearization of Multivariable Functions.md>)
- [The Jacobian of a Three-Dimensional Transformation](<9.4.3. The Jacobian of a Three-Dimensional Transformation.md>)

---

<a id="introduction"></a>
## Introduction

We've spent some time studying so-called **plane transformations** of the form

$$
\mathbf T: \mathbb R^2\to\mathbb R^2
$$

In this lesson, we'll generalize our understanding of derivatives to transformations (or functions) of the form

$$
\boldsymbol f:\mathbb R^n\to\mathbb R^m
$$

To motivate the discussion, we start by considering functions of one variable.

Recall that a function $f: \mathbb{R} \to \mathbb{R}$ is **differentiable** at a point $x = a$ if there exists a number $f'(a)$, called the **derivative** of $f$ at $a$, such that

$$
f'(a) = \lim_{x \to a} \frac{f(x) - f(a)}{x - a}
$$

We can restate this definition by applying basic algebraic properties of limits.

Since the left-hand side, $f'(a)$, is just a constant, we can rewrite the equation as

$$
\lim_{x \to a} f'(a) = \lim_{x \to a} \frac{f(x) - f(a)}{x - a}
$$

using the fact that the limit of a constant is the constant itself.

Bringing all terms to one side, we obtain

$$
\lim_{x \to a} \left(\frac{f(x) - f(a)}{x - a} - f'(a) \right) = 0
$$

which can be rearranged as

$$
\lim_{x \to a} \left(\frac{f(x) - f(a) - f'(a)(x - a)}{x - a} \right) = 0
$$

This final expression tells us that the difference between $f(x)$ and its *linear approximation* $f(a) + f'(a)(x - a)$ becomes negligible relative to $x - a$ as $x$ approaches $a$.

In other words, not only does $f(x)$ become close to $f(a) + f'(a)(x-a)$ near $a$, but the error between them shrinks *faster than* the distance $\mid x - a \mid$ itself.

Let's see how we can generalize this definition to vector-valued functions.

---

<a id="defining-the-total-derivative"></a>
## Defining the Total Derivative

Suppose that a transformation (or function) $\boldsymbol f:\mathbb R^n\to\mathbb R^m$ is defined by

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

where $f_1,f_2,\ldots,f_m$ are scalar functions of $n$ variables.
We say that $\boldsymbol{f}$ is **differentiable** at a point

$$
\mathbf{x} = \mathbf{a} \in \mathbb{R}^n
$$

if there exists an $m \times n$ matrix $\boldsymbol{f}'(\mathbf{a})$, called the **total derivative** of $\boldsymbol{f}$ at $\mathbf{a}$, such that

$$
\lim_{\mathbf{x} \to \mathbf{a}} \frac{\boldsymbol{f}(\mathbf{x}) - \boldsymbol{f}(\mathbf{a}) - \boldsymbol{f}'(\mathbf{a})(\mathbf{x} - \mathbf{a})}{\mid \mid \mathbf{x} - \mathbf{a} \mid \mid} = \mathbf{0}
$$

In other words, near $\mathbf{a}$, the function $\boldsymbol{f}$ is well-approximated by the linear map

$$
\boldsymbol{f}(\mathbf{a}) + \boldsymbol{f}'(\mathbf{a})(\mathbf{x} - \mathbf{a})
$$

and the error between $\boldsymbol{f}(\mathbf{x})$ and this linear approximation becomes negligible compared to the distance $\|\mathbf{x} - \mathbf{a}\|$ as $\mathbf{x}$ approaches $\mathbf{a}$.

If $\boldsymbol{f}$ is differentiable at $\mathbf{x}$, then the total derivative of $\boldsymbol f$ is given by the matrix

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} & ⋯ & \frac{∂f_{1}}{∂x_{n}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}} & ⋯ & \frac{∂f_{2}}{∂x_{n}} \\ ⋮ & ⋮ & ⋮ & ⋮ \\ \frac{∂f_{m}}{∂x_{1}} & \frac{∂f_{m}}{∂x_{2}} & ⋯ & \frac{∂f_{m}}{∂x_{n}}\end{bmatrix}
$$

Note the following:

- We sometimes refer to $\boldsymbol f'(\mathbf x)$ simply as **the derivative** of $\boldsymbol f(\mathbf x)$.
- For scalar-valued functions of the form $f: \mathbb R^n\to\mathbb R$, the total derivative is the *transpose* of the gradient vector:
$\boldsymbol{f}'(\mathbf a) = \left(\nabla f \right)^T(\mathbf a)$
- For vector-valued functions $\boldsymbol f: \mathbb R\to\mathbb R^n$ of a single parameter $t$, the total derivative at $t=a$ is the usual column vector containing the partial derivatives of the components of $f{:}$
$\begin{bmatrix}\frac{∂f_{1}}{∂t} \\ \frac{∂f_{2}}{∂t} \\ ⋮ \\ \frac{∂f_{n}}{∂t}\end{bmatrix}$

- For functions of the form $\boldsymbol f: \mathbb R^2\to\mathbb R^2$, the *determinant* of this matrix is the corresponding Jacobian:
$\det\left(\boldsymbol{f}'(\mathbf a)\right) = \dfrac{\partial (f_1,f_2)}{\partial (x_1,x_2)}$
Similarly, for functions of the form $\boldsymbol f: \mathbb R^3\to\mathbb R^3$, the *determinant* of this matrix is the corresponding Jacobian:
$\det\left(\boldsymbol{f}'(\mathbf a)\right) = \dfrac{\partial (f_1,f_2,f_3)}{\partial (x_1,x_2,x_3)}$

---

<a id="finding-the-derivative-of-a-scalar-function"></a>
## Finding the Derivative of a Scalar Function

**Example:** For the transformation $f:\mathbb R^2\to\mathbb R$, defined as

$\begin{bmatrix}x \\ y\end{bmatrix}$

calculate $\boldsymbol f'$ at $\begin{bmatrix}1 \\ -1\end{bmatrix}$

**Explanation**

Suppose a transformation $\boldsymbol{f}:\mathbb R^n\to\mathbb R^m$ is defined by

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

where $f_1,f_2,\ldots,f_m$ are scalar functions with $n$ variables. If $\boldsymbol f$ is differentiable at $\mathbf{x}$, then the total derivative of $\boldsymbol f$ at $\mathbf{x}$, denoted $\boldsymbol{f}'(\mathbf{x})$, is given by the $m \times n$ matrix

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} & ⋯ & \frac{∂f_{1}}{∂x_{n}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}} & ⋯ & \frac{∂f_{2}}{∂x_{n}} \\ ⋮ & ⋮ & ⋮ & ⋮ \\ \frac{∂f_{m}}{∂x_{1}} & \frac{∂f_{m}}{∂x_{2}} & ⋯ & \frac{∂f_{m}}{∂x_{n}}\end{bmatrix}
$$

In our case, we have the following:

$$
\begin{bmatrix}\frac{∂f}{∂x} & \frac{∂f}{∂y}\end{bmatrix}
$$

First, we compute the partial derivatives:

$$
\begin{aligned}
\frac{∂f}{∂x} &= \frac{∂}{∂x}(x^{2}y) = 2xy, \mid \frac{∂f}{∂y}|= \frac{∂}{∂y}(x^{2}y) = x^{2}
\end{aligned}
$$

Therefore, we have

$$
\begin{bmatrix}2xy & x^{2}\end{bmatrix}
$$

and $\boldsymbol{f}'$ at $\begin{bmatrix}1 \\ -1\end{bmatrix}$ is

$$
\begin{bmatrix}2(1)(-1) & 1^{2}\end{bmatrix}
$$

---

**Question 1**

```quiz
type: radio
id: ma-162943
content: |-
  For the function $f: R^{2} → R$, defined as

  $f(x) = x^{2} + y^{2}, x = \begin{bmatrix}x \\ y\end{bmatrix}$,

  calculate $f^{′}(x)$.
options:
- id: a
  content: |-
    $\begin{bmatrix}x^{2} & y^{2}\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}2x \\ 2y\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}2x & 2y \\ 2y & 2x\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}x^{2} \\ y^{2}\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}2x & 2y\end{bmatrix}$
  correct: true
```

---

**Question 2**

```quiz
type: radio
id: ma-162983
content: |-
  For the transformation $f: R^{2} → R$, defined as

  $f(x) = x^{2} - 2y, x = \begin{bmatrix}x \\ y\end{bmatrix}$,

  calculate $f^{′}$ at $a = \begin{bmatrix}0 \\ -1\end{bmatrix}$.
options:
- id: a
  content: |-
    $\begin{bmatrix}0 & -2\end{bmatrix}$
  correct: true
- id: b
  content: |-
    $\begin{bmatrix}0 \\ -2\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}2 & -2\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}2 & -2 \\ -2 & 2\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}2 \\ -2\end{bmatrix}$
```

---

<a id="finding-the-derivative-of-a-vector-function"></a>
## Finding the Derivative of a Vector Function

**Example:** For the transformation $\boldsymbol{f}:\mathbb R^2\to\mathbb R^3$, defined as

$\begin{bmatrix}xy \\ y^{2} \\ y\ln x\end{bmatrix}$

the total derivative $\boldsymbol f'$ is given by

$\begin{bmatrix}A A & x \\ A A & 2y \\ \frac{y}{x} & A A\end{bmatrix}$
From top to bottom, what are the missing entries?

**Explanation**

Suppose a transformation $\boldsymbol{f}:\mathbb R^n\to\mathbb R^m$ is defined by

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

where $f_1,f_2,\ldots,f_m$ are scalar functions with $n$ variables. If $\boldsymbol{f}$ is differentiable at $\mathbf{x}$, then the total derivative of $\boldsymbol{f}$ at $\mathbf{x}$, denoted $\boldsymbol{f}'(\mathbf{x})$, is given by the $m \times n$ matrix

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} & ⋯ & \frac{∂f_{1}}{∂x_{n}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}} & ⋯ & \frac{∂f_{2}}{∂x_{n}} \\ ⋮ & ⋮ & ⋮ & ⋮ \\ \frac{∂f_{m}}{∂x_{1}} & \frac{∂f_{m}}{∂x_{2}} & ⋯ & \frac{∂f_{m}}{∂x_{n}}\end{bmatrix}
$$

In our case, we have the following:

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x} & \frac{∂f_{1}}{∂y} \\ \frac{∂f_{2}}{∂x} & \frac{∂f_{2}}{∂y} \\ \frac{∂f_{3}}{∂x} & \frac{∂f_{3}}{∂y}\end{bmatrix}
$$

First, we compute the partial derivatives:

$$
\begin{aligned}
\begin{bmatrix}\frac{∂f_{1}}{∂x} &= \frac{∂}{∂x}(xy) = y, & \frac{∂f_{1}}{∂y}|= \frac{∂}{∂y}(xy) = x, \\ \frac{∂f_{2}}{∂x}|= \frac{∂}{∂x}(y^{2}) = 0, & \frac{∂f_{2}}{∂y}|= \frac{∂}{∂y}(y^{2}) = 2y, \\ \frac{∂f_{3}}{∂x}|= \frac{∂}{∂x}(y\ln x) = \frac{y}{x}, & \frac{∂f_{3}}{∂y}|= \frac{∂}{∂y}(y\ln x) = \ln x\end{bmatrix}
\end{aligned}
$$

Therefore, we have

$$
\begin{bmatrix}y & x \\ 0 & 2y \\ \frac{y}{x} & ln ⁡ x\end{bmatrix}
$$

Thus, the missing entries are $y,\:$ $0,\:$ and $\ln{x}$.

---

**Question 3**

```quiz
type: radio
id: ma-162941
content: |-
  For the transformation $f: R^{2} → R^{2}$, defined as

  $f(x) = \begin{bmatrix}2xy \\ e^{xy}\end{bmatrix}, x = \begin{bmatrix}x \\ y\end{bmatrix}$,

  calculate $f^{′}(x)$.
options:
- id: a
  content: |-
    $\begin{bmatrix}2x \\ xe^{xy}\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}ye^{xy} & 2x \\ 2y & xe^{xy}\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}2y & 2x \\ ye^{xy} & xe^{xy}\end{bmatrix}$
  correct: true
- id: d
  content: |-
    $\begin{bmatrix}2xy & 2xy \\ e^{xy} & e^{xy}\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}2y \\ ye^{xy}\end{bmatrix}$
```

---

**Question 4**

```quiz
type: radio
id: ma-162942
content: |-
  For the transformation $f: R^{3} → R^{2}$, defined as

  $f(x) = \begin{bmatrix}\ln (xyz) \\ x^{2}yz\end{bmatrix}, x = \begin{bmatrix}x \\ y \\ z\end{bmatrix}$,

  the total derivative $f^{′}$ is given by

  $f^{′}(x) = \begin{bmatrix}\frac{1}{x} & A A & \frac{1}{z} \\ A A & x^{2}z & A A\end{bmatrix}$.
  From left to right, what are the missing entries?
options:
- id: a
  content: |-
    $x^{2}y$, $\frac{1}{z}$, and $x^{2}y$
- id: b
  content: |-
    $2xyz$, $\frac{1}{y}$, and $yz$
- id: c
  content: |-
    $2xyz$, $\frac{1}{y}$, and $x^{2}y$
  correct: true
- id: d
  content: |-
    $x^{2}y$, $\frac{1}{x}$, and $x^{2}y$
- id: e
  content: |-
    $2xyz$, $\frac{1}{z}$, and $y^{2}z$
```

---

<a id="the-tangent-plane-to-the-graph-of-a-scalar-function"></a>
## The Tangent Plane to the Graph of a Scalar Function

The equation of the tangent plane to a surface $z=f(x,y)$ at the point $(x_0,y_0,z_0)$, where

$$
z_0=f(x_0,y_0)
$$

is given by

$$
z - z_0 = (x-x_0) \cdot \dfrac{\partial f}{\partial x}(x_0,y_0) + (y-y_0) \cdot \dfrac{\partial f}{\partial y}(x_0,y_0)
$$

Let's express this equation in terms of the total derivative.

Writing our multivariable function $f:\mathbb R^2 \to \mathbb R$ as

$$
f\left(\mathbf x \right)
$$

where $\begin{bmatrix}x \\ y\end{bmatrix}$ the total derivative of $f$ is given by

$$
\begin{bmatrix}\frac{∂f}{∂x} & \frac{∂f}{∂y}\end{bmatrix}
$$

Let $\begin{bmatrix}x_{0} \\ y_{0}\end{bmatrix}$ Then, we can re-write the equation of the tangent plane as a matrix equation as follows:

$$
\begin{aligned}
z - z_{0} &= (x - x_{0}) \cdot \frac{∂f}{∂x}(x_{0}, y_{0}) + (y - y_{0}) \cdot \frac{∂f}{∂y}(x_{0}, y_{0}) \\
z - z_{0} &= \begin{bmatrix}\frac{∂f}{∂x}(x_{0}, y_{0}) & \frac{∂f}{∂y}(x_{0}, y_{0})\end{bmatrix} \cdot \begin{bmatrix}x - x_{0} \\ y - y_{0}\end{bmatrix} \\
z - f(x_{0}, y_{0}) &= \begin{bmatrix}\frac{∂f}{∂x}(x_{0}, y_{0}) & \frac{∂f}{∂y}(x_{0}, y_{0})\end{bmatrix} \cdot (\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}x_{0} \\ y_{0}\end{bmatrix}) \\
z - f(a) &= \begin{bmatrix}\frac{∂f}{∂x}(a) & \frac{∂f}{∂y}(a)\end{bmatrix} \cdot (x - a) \\
z - f(a) &= f^{′}(a)(x - a)
\end{aligned}
$$

Therefore, the tangent plane to $f:\mathbb R^2\to\mathbb R$ at

$$
\mathbf x = \mathbf{a}
$$

in matrix form, is given by

$$
z - f(\mathbf a) = \boldsymbol{f}'(\mathbf a)(\mathbf x - \mathbf a)
$$

---

<a id="finding-the-tangent-plane-to-the-graph-of-a-scalar-function-in-matrix-form"></a>
## Finding the Tangent Plane to the Graph of a Scalar Function in Matrix Form

**Example:** Consider the function $f:\mathbb R^2\to\mathbb R$, defined as

$\begin{bmatrix}x \\ y\end{bmatrix}$

If the tangent to $f$ at $\begin{bmatrix}1 \\ 2\end{bmatrix}$ is given by

$\begin{bmatrix}∗ & ∗\end{bmatrix}$

find the missing values.

**Explanation**

Suppose a transformation $\boldsymbol{f}:\mathbb R^n\to\mathbb R^m$ is defined by

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

where $f_1,f_2,\ldots,f_m$ are scalar functions with $n$ variables. If $\boldsymbol{f}$ is differentiable at $\mathbf{x}$, then the total derivative of $\boldsymbol{f}$ at $\mathbf{x}$, denoted $\boldsymbol{f}'(\mathbf{x})$, is given by the $m \times n$ matrix

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} & ⋯ & \frac{∂f_{1}}{∂x_{n}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}} & ⋯ & \frac{∂f_{2}}{∂x_{n}} \\ ⋮ & ⋮ & ⋮ & ⋮ \\ \frac{∂f_{m}}{∂x_{1}} & \frac{∂f_{m}}{∂x_{2}} & ⋯ & \frac{∂f_{m}}{∂x_{n}}\end{bmatrix}
$$

In our case, we have the following:

$$
\begin{bmatrix}\frac{∂f}{∂x} & \frac{∂f}{∂y}\end{bmatrix}
$$

First, we compute the partial derivatives:

$$
\begin{aligned}
\frac{∂f}{∂x} &= \frac{∂}{∂x}(x^{3} + e^{y}) = 3x^{2}, \mid \frac{∂f}{∂y}|= \frac{∂}{∂y}(x^{3} + e^{y}) = e^{y}
\end{aligned}
$$

Therefore, we have

$$
\begin{bmatrix}3x^{2} & e^{y}\end{bmatrix}
$$

Evaluating $f$ and its derivative at $\begin{bmatrix}1 \\ 2\end{bmatrix}$ we have

$$
\begin{bmatrix}3 & e^{2}\end{bmatrix}
$$

The tangent to $f$ at

$$
\mathbf x = \mathbf a
$$

is given by

$$
z - f(\mathbf a) = \boldsymbol{f}'(\mathbf a)(\mathbf x - \mathbf a)
$$

Substituting $f(\mathbf a)$, $\boldsymbol{f}'(\mathbf a)$, and $\mathbf a$ in the equation above, we obtain

$$
\begin{bmatrix}3 & e 2\end{bmatrix}
$$

---

**Question 5**

```quiz
type: radio
id: ma-162945
content: |-
  Consider the function $f: R^{2} → R$, defined as

  $f(x) = x^{2} + y, x = \begin{bmatrix}x \\ y\end{bmatrix}$.

  If the tangent to $f$ at $a = \begin{bmatrix}1 \\ -1\end{bmatrix}$ is given by

  $z - ∗ = \begin{bmatrix}∗ & ∗\end{bmatrix}(\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}1 \\ -1\end{bmatrix})$

  what is the sum of the missing values?
options:
- id: a
  content: |-
    $3$
  correct: true
- id: b
  content: |-
    $1$
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $2$
- id: e
  content: |-
    $-2$
```

---

**Question 6**

```quiz
type: radio
id: ma-163057
content: |-
  Consider the function $f: R^{2} → R$, defined as

  $f(x) = e^{x} + y, x = \begin{bmatrix}x \\ y\end{bmatrix}$.

  If the tangent to $f$ at $a = \begin{bmatrix}0 \\ 1\end{bmatrix}$ is given by

  $z - ∗ = \begin{bmatrix}∗ & ∗\end{bmatrix}(\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}0 \\ 1\end{bmatrix})$

  what is the sum of the missing values?
options:
- id: a
  content: |-
    $4$
  correct: true
- id: b
  content: |-
    $2$
- id: c
  content: |-
    $1$
- id: d
  content: |-
    $6$
- id: e
  content: |-
    $3$
```

---

<a id="affine-approximations-of-vector-functions"></a>
## Affine Approximations of Vector Functions

If $f:\mathbb R \to \mathbb R$ is differentiable at $x=a$, then for points close to $x=a$, we have the linear approximation

$$
f(x) \approx f(a) + f'(a)(x-a)
$$

We have a similar result for transformations of the form $\boldsymbol{f}:\mathbb R^n\to\mathbb R^m$.

If $\boldsymbol f:\mathbb R^n \to \mathbb R^m$ is differentiable at

$$
\mathbf x=\mathbf a
$$

then for points close to

$$
\mathbf x=\mathbf a
$$

we have the linear approximation

$$
\boldsymbol{f}(\mathbf x) \approx \boldsymbol{f}(\mathbf a) + \boldsymbol{f}'(\mathbf a)(\mathbf x - \mathbf a)
$$

Notice that this approximation for $\boldsymbol f$ is affine. Thus, if a function is differentiable at a point

$$
\mathbf x = \mathbf a
$$

it is **locally affine** in a neighborhood of

$$
\mathbf x = \mathbf a
$$

Let's see some examples.

---

<a id="finding-an-affine-approximation-of-a-vector-function"></a>
## Finding an Affine Approximation of a Vector Function

**Example:** Consider the transformation $\boldsymbol{f}:\mathbb R^2\to\mathbb R^2$, defined as

$\begin{bmatrix}x_{2}\sin x_{1} \\ \sin x_{2}\end{bmatrix}$

If the linear approximation of $\boldsymbol f$ at $\begin{bmatrix}π \\ 0\end{bmatrix}$ is given by

$\begin{bmatrix}∗ \\ 0\end{bmatrix}$

what are the missing values?

**Explanation**

Suppose a transformation $\boldsymbol{f}:\mathbb R^n\to\mathbb R^m$ is defined by

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

where $f_1,f_2,\ldots,f_m$ are scalar functions with $n$ variables. If $\boldsymbol{f}$ is differentiable at $\mathbf{x}$, then the total derivative of $\boldsymbol{f}$ at $\mathbf{x}$, denoted $\boldsymbol{f}'(\mathbf{x})$, is given by the $m \times n$ matrix

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} & ⋯ & \frac{∂f_{1}}{∂x_{n}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}} & ⋯ & \frac{∂f_{2}}{∂x_{n}} \\ ⋮ & ⋮ & ⋮ & ⋮ \\ \frac{∂f_{m}}{∂x_{1}} & \frac{∂f_{m}}{∂x_{2}} & ⋯ & \frac{∂f_{m}}{∂x_{n}}\end{bmatrix}
$$

In our case, we have the following:

$$
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} & \frac{∂f_{1}}{∂x_{2}} \\ \frac{∂f_{2}}{∂x_{1}} & \frac{∂f_{2}}{∂x_{2}}\end{bmatrix}
$$

First, we compute the partial derivatives:

$$
\begin{aligned}
\begin{bmatrix}\frac{∂f_{1}}{∂x_{1}} &= \frac{∂}{∂x_{1}}(x_{2}\sin x_{1}) = x_{2}\cos x_{1}, & \frac{∂f_{1}}{∂x_{2}}|= \frac{∂}{∂x_{2}}(x_{2}\sin x_{1}) = \sin x_{1} \\ \frac{∂f_{2}}{∂x_{1}}|= \frac{∂}{∂x_{1}}(\sin x_{2}) = 0, & \frac{∂f_{2}}{∂x_{2}}|= \frac{∂}{∂x_{2}}(\sin x_{2}) = \cos x_{2}\end{bmatrix}
\end{aligned}
$$

Therefore, we have

$$
\begin{bmatrix}x_{2}\cos x_{1} & \sin x_{1} \\ 0 & \cos x_{2}\end{bmatrix}
$$

Evaluating $\boldsymbol{f}$ and its derivative at $\begin{bmatrix}π \\ 0\end{bmatrix}$ we have

$$
\begin{bmatrix}0 \\ 0\end{bmatrix}
$$

The linear approximation of $\boldsymbol{f}$ at

$$
\mathbf x = \mathbf a
$$

is given by

$$
\boldsymbol{f}\left(\mathbf x \right) \approx \boldsymbol{f}(\mathbf a) + \boldsymbol{f}'(\mathbf a)(\mathbf x - \mathbf a)
$$

Substituting $\boldsymbol{f}(\mathbf a)$, $\boldsymbol{f}'(\mathbf a)$, and $\mathbf a$ in the equation above, we obtain

$$
\begin{bmatrix}0 \\ 0\end{bmatrix}
$$

---

**Question 7**

```quiz
type: radio
id: ma-162944
content: |-
  Consider the transformation $f: R^{2} → R^{2}$, defined as

  $f(x) = \begin{bmatrix}2x_{1}x_{2} \\ e^{x_{1}x_{2}}\end{bmatrix}, x = \begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$.

  If the linear approximation of $f$ at $a = \begin{bmatrix}1 \\ \ln 2\end{bmatrix}$ is given by

  $f(x) \approx \begin{bmatrix}∗ \\ 2\end{bmatrix} + \begin{bmatrix}2\ln 2 & ∗ \\ ∗ & 2\end{bmatrix}(x - \begin{bmatrix}1 \\ \ln 2\end{bmatrix})$,

  what is the sum of the missing values?
options:
- id: a
  content: |-
    $2\ln 2 + 4$
- id: b
  content: |-
    $6$
- id: c
  content: |-
    $2\ln 2 + 2$
- id: d
  content: |-
    $4\ln 2 + 2$
  correct: true
- id: e
  content: |-
    $4$
```

---

**Question 8**

```quiz
type: radio
id: ma-163163
content: |-
  Consider the transformation $f: R^{2} → R^{2}$, defined as

  $f(x) = \begin{bmatrix}\sin (x_{1} - x_{2}) \\ 6x_{1} - 7x_{2}\end{bmatrix}, x = \begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$.

  If the linear approximation of $f$ at $a = \begin{bmatrix}3 \\ 2\end{bmatrix}$ is given by

  $f(x) \approx \begin{bmatrix}\sin 1 \\ ∗\end{bmatrix} + \begin{bmatrix}∗ & ∗ \\ 6 & -7\end{bmatrix}(x - \begin{bmatrix}3 \\ 2\end{bmatrix})$,

  what is the sum of the missing values?
options:
- id: a
  content: |-
    $4$
  correct: true
- id: b
  content: |-
    $4 + 2\cos 1$
- id: c
  content: |-
    $4 + \cos 1 + \sin 1$
- id: d
  content: |-
    $2 + 2\cos 1$
- id: e
  content: |-
    $2$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
