# Properties of Double Integrals

<!--
lesson-id: 2000
topic-code: MML.9.7.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Evaluating a Double Integral Using the Multiplicative Property](#evaluating-a-double-integral-using-the-multiplicative-property)
- [Applying the Multiplicative Property when the Integrand Contains Only One Variable](#applying-the-multiplicative-property-when-the-integrand-contains-only-one-variable)
- [Evaluating a Double Integral When the Integrand Contains Only One Variable](#evaluating-a-double-integral-when-the-integrand-contains-only-one-variable)
- [Additivity of Double Integrals](#additivity-of-double-integrals)
- [Using Additivity of Double Integrals](#using-additivity-of-double-integrals)
- [Open and Closed Regions of Integration](#open-and-closed-regions-of-integration)
- [Evaluating a Double Integral Using the Additivity Over a Rectangular Domain](#evaluating-a-double-integral-using-the-additivity-over-a-rectangular-domain)

## Prerequisites

- [Double Integrals Over Non-Rectangular Domains](<9.7.2. Double Integrals Over Non-Rectangular Domains.md>)

---

<a id="introduction"></a>
## Introduction

Double integrals have properties that are similar to the properties of definite integrals of single-variable functions:

- **Linearity of Double Integrals.** Suppose that $f(x,y)$ and $g(x,y)$ are continuous functions on a domain $D$ and that $\alpha, \beta$ are constants. Then, we have
$\iint\limits_{D} \left[\alpha f(x,y) + \beta g(x,y)\right] \ \textrm d A = \alpha\iint\limits_{D} f(x,y) \ \textrm d A + \beta\iint\limits_{D} g(x,y) \ \textrm d A$.
- **The Double Integral of Unity.** The integral of $f(x,y) = 1$ over a domain $D$ is simply the area of $D$:
$\displaystyle \iint\limits_{D} \ \textrm d A = \displaystyle \iint\limits_{D} 1 \cdot \textrm d A = \textrm{Area}\ \!(D)$.

Other properties are more specific to double integrals.

- **The Multiplicative Property of Double Integrals.** If $f(x)$ is continuous on $[a,b]$ and $g(y)$ is continuous on $[c,d]$ then
$\iint\limits_{R} f(x)g(y) \ \textrm d A = \left(\int_a^b f(x) \ \textrm d x\right)\left(\int_c^d g(y) \ \textrm d y\right)$,
where $R= [a,b] \times [c,d]$.

---

<a id="evaluating-a-double-integral-using-the-multiplicative-property"></a>
## Evaluating a Double Integral Using the Multiplicative Property

**Example:** Evaluate the double integral $\displaystyle \iint\limits_{R} xy\ \!e^{x+y}\ \textrm d A$ over the rectangle $R= [0,1] \times [1,2]$.

**Explanation**

We apply the multiplicative property of double integrals and then evaluate each of the integrals separately:

$$
\begin{aligned} \iint\limits_{R} xy \\ e^{x+y}\: \textrm d A &= \iint\limits_{R} \Big(x\ \! e^x \Big)\Big(y\ \! e^{y} \Big) \textrm d A \\[5pt] &= \left(\int_0^1x \\ e^{x} \ \textrm d x\right)\left(\int_1^2 y \ \!e^{y} \ \textrm d y\right) \\[5pt] &= \Big[x\ \!e^x-e^x\Big]_0^1 \cdot \Big[y\ \!e^y-e^y\Big]_1^2 \\[5pt] &= \Big(e^1-e^1 - \left(0-e^0\right)\Big) \cdot \Big(2e^2-e^2 - \left(e^1-e^1\right) \Big) \\[5pt] &= 1\cdot e^2 \\[5pt] &= e^2 \end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-71932
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Evaluate the double integral $∬_(R)\sin xe^{y}dA$ over the rectangle $R = [0, π] \cdot [0, 1]$.
options:
- id: a
  content: |-
    $-2e$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $2(1 - e)$
- id: d
  content: |-
    $2e$
- id: e
  content: |-
    $2(e - 1)$
```

---

**Question 2:**

```quiz
type: radio
id: ma-71933
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following gives the value of the double integral $∬_(R)y\sqrt{xy}dA$ over the rectangle $R = [1, 4] \cdot [0, 1]$?
options:
- id: a
  content: |-
    $(∫_{1}^{4}\sqrt{x}dx)(∫_{0}^{1}y\sqrt{y}dy)$
- id: b
  content: |-
    $(∫_{0}^{1}\sqrt{x}dx)(∫_{1}^{4}y\sqrt{y}dy)$
- id: c
  content: |-
    $(∫_{1}^{4}y\sqrt{xy}dx)(∫_{0}^{1}y\sqrt{xy}dy)$
- id: d
  content: |-
    $(∫_{0}^{1}ydx)(∫_{1}^{4}y\sqrt{x}dy)$
- id: e
  content: |-
    $(∫_{1}^{4}ydx)(∫_{0}^{1}y\sqrt{x}dy)$
```

---

<a id="applying-the-multiplicative-property-when-the-integrand-contains-only-one-variable"></a>
## Applying the Multiplicative Property when the Integrand Contains Only One Variable

We can also apply the multiplicative property when the integrand contains only one of the variables.

For example, suppose that

$$
R= [a,b] \times [c,d]
$$

Then, we have the general result

$$
\begin{aligned}
∬_(R)f(x)dA &= ∬_(R)(f(x))(1)dA \\
&= (∫_{a}^{b}f(x)dx)(∫_{c}^{d}1dy) \\
&= (∫_{a}^{b}f(x)dx)(d - c) \\
&= (d - c)∫_{a}^{b}f(x)dx
\end{aligned}
$$

By the same reasoning, we also have

$$
\iint\limits_{R} g(y) \, \textrm{d}A = (b-a) \int_c^d g(y) \, \textrm{d}y
$$

---

<a id="evaluating-a-double-integral-when-the-integrand-contains-only-one-variable"></a>
## Evaluating a Double Integral When the Integrand Contains Only One Variable

**Example:** Calculate $\displaystyle \iint\limits_{R} \left(x^2-y^2 +3\right) \textrm d A$, where $R= [-1,3] \times [2, 4]$.

**Explanation**

Using the linearity property of double integrals, we can write

$$
\begin{aligned}
∬_(R)(x^{2} - y^{2} + 3)dA &= ∬_(R)x^{2}dA - ∬_(R)y^{2}dA + ∬_(R)3dA
\end{aligned}
$$

Now, let's evaluate each of the three integrals.

- The function under the first integral is independent of $y$. So, we have
$∬_(R)x^{2}dA|= (4 - 2)∫_{-1}^{3}x^{2}dx; = 2 \cdot \frac{x^{3}}{3} \mid _{-1}^{3}; = 2(\frac{27}{3} + \frac{1}{3}); = \frac{56}{3}$.
- The function under the second integral is independent of $x$. So, we have
$∬_(R)y^{2}dA|= (3 - (-1))∫_{2}^{4}y^{2}dy; = 4∫_{2}^{4}y^{2}dy; = 4 \cdot \frac{y^{3}}{3} \mid _{2}^{4}; = \frac{4}{3}4^{3} - \frac{4}{3}2^{3}; = \frac{224}{3}$.
- The function under the third integral is a constant, so we have
$∬_(R)3dA = 3Area(R) = 3 \cdot (2 \cdot 4) = 24$.

Finally, combining our results, we obtain

$$
∬_(R)(x^{2} - y^{2} + 3)dA = \frac{56}{3} - \frac{224}{3} + 24 =-32
$$

---

**Question 3:**

```quiz
type: radio
id: ma-71994
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Evaluate the double integral $∬_(R)\frac{1 - 3xy^{2}}{x}dA$, where $R = [1, e] \cdot [2, 3]$.
options:
- id: a
  content: |-
    $20 - 19e$
- id: b
  content: |-
    $19 - 20e$
- id: c
  content: |-
    $16 + 20e$
- id: d
  content: |-
    $20e$
- id: e
  content: |-
    $19e - 2$
```

---

**Question 4:**

```quiz
type: radio
id: ma-71991
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Calculate $∬_(R)(1 - 3x^{2} + 2y)dA$, where $R = [- 1, 1] \cdot [0, 1]$.
options:
- id: a
  content: |-
    $\frac{4}{3}$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $1$
- id: d
  content: |-
    $\frac{5}{6}$
- id: e
  content: |-
    $2$
```

---

<a id="additivity-of-double-integrals"></a>
## Additivity of Double Integrals

Remember that the definite integral of a single-variable function can be split up over adjacent subintervals. That is to say,

$$
\int_a^c f(x) \, \textrm dx = \int_a^b f(x) \, \textrm dx + \int_b^c f(x) \, \textrm dx
$$

In the same way, the definite integral of a two-variable function can be split up over adjacent sub-domains.

Suppose that a domain $D$ is the union of two adjacent sub-domains $D_1$ and $D_2$ (meaning that $D_1$ and $D_2$ are non-overlapping regions that could share a common border). Then, we have

$$
\iint\limits_{D} f(x,y)\ \textrm{d}A = \iint\limits_{D_1} f(x,y)\ \textrm{d}A + \iint\limits_{D_2} f(x,y)\ \textrm{d}A
$$

This property can be generalized if $D$ is split into a finite number $n$ of sub-domains (where $n>2$):

$$
\iint\limits_{D} f(x,y)\ \textrm{d}A = \iint\limits_{D_1} f(x,y)\ \textrm{d}A + \iint\limits_{D_2} f(x,y)\ \textrm{d}A + \cdots + \iint\limits_{D_n} f(x,y)\ \textrm{d}A
$$

---

<a id="using-additivity-of-double-integrals"></a>
## Using Additivity of Double Integrals

**Example:** Given that $C$ and $D$ are finite, non-overlapping regions and that
$\iint\limits_{C} f(x,y)\ \textrm{d}A = 8, \qquad \iint\limits_{D} f(x,y)\ \textrm{d}A = -5$,
find the value of
$\iint\limits_{C \cup D} 3f(x,y)\ \textrm{d}A$.

**Explanation**

Using the linearity and additivity of double integrals, we get the following:

$$
\begin{aligned}
∬_(C∪D)3f(x, y)dA &= 3∬_(C∪D)f(x, y)dA \\
&= 3(∬_(C)f(x, y)dA + ∬_(D)f(x, y)dA) \\
&= 3(8 + (-5)) \\
&= 3 \cdot 3 \\
&= 9
\end{aligned}
$$

---

**Question 5**

```quiz
type: radio
id: ma-80452
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/9.7.3. Properties of Double Integrals/Images/q-80452.png>)

  Consider the region $R$ shown above. Let the function $f(x, y)$ be defined as
  $f(x, y) = {x,\begin{vmatrix}(x, y) ∈ S, \\ 0,\end{vmatrix}(x, y) ∈ T,; x^{2}y, \mid (x, y) ∈ U$.
  Then,
  $∬_(R)f(x, y)dA =$
options:
- id: a
  content: |-
    $∬_(S)xdA + ∬_(T)(x + x^{2}y)dA + ∬_(U)x^{2}ydA$
- id: b
  content: |-
    $∬_(S)xdA + ∬_(U)x^{2}ydA$
- id: c
  content: |-
    $∬_(T)xdA + ∬_(U)x^{2}ydA$
- id: d
  content: |-
    $∬_(S)xdA + ∬_(T)ydA + ∬_(U)x^{2}ydA$
- id: e
  content: |-
    $∬_(S)xdA + ∬_(T)x^{2}ydA$
```

---

**Question 6:**

```quiz
type: radio
id: ma-80482
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Given that $C$ and $D$ are finite, non-overlapping regions and that

  $∬_(C)f(x, y)dA = 7, ∬_(D)f(x, y)dA = 1$,
  find the value of
  $∬_(C∪D)5f(x, y)dA$.
options:
- id: a
  content: |-
    $35$
- id: b
  content: |-
    $40$
- id: c
  content: |-
    $50$
- id: d
  content: |-
    $30$
- id: e
  content: |-
    $45$
```

---

<a id="open-and-closed-regions-of-integration"></a>
## Open and Closed Regions of Integration

Suppose that we're integrating a single-variable function over a region $R$. The definite integral of $f(x)$ over $R$ is not affected by whether the region $R$ is open or closed.

For example, consider the function

$$
f(x) = 1
$$

and define the regions $R = [0,1]$ and $R' = (0,1)$. Integrating $f(x)$ over $R$ and $R'$ gives the same result:

$$
\int_R f(x) \,\textrm d x = \int_{R'} f(x) \,\textrm d x = \int_{0}^1 \,\textrm d x = 1
$$

Intuitively, this is true because the rectangles formed by $f(x)$, namely $[0, 1]\times [0,1]$ and $(0, 1)\times (0,1)$, have the same area.

We have a similar property for double integrals too. We can either include or exclude the boundaries of the integration regions without affecting the result.

For example, the double integral of

$$
f(x,y)=x^2+y^2
$$

over both

$$
R=[0,1] \times [0,1]
$$

and over

$$
R'=(0,1) \times (0,1)
$$

will give the same result:

$$
\iint\limits_{\large R} (x^2+y^2) \ \text{d}A = \iint\limits_{\large R'} (x^2+y^2) \ \text{d}A= \int_{0}^{1} \int_{0}^{1} (x^2+y^2) \ \text{d}A
$$

Intuitively, this is true because the *volumes* of the shapes formed by $f(x,y)$ over the regions $R$ and $R'$ are the same.

Moreover, we'd get the same result if we were to integrate $f(x,y)$ over any of the following regions:

$[MATH: (0,1] \times [0,1], \quad [0,1) \times [0,1], \quad (0,1) \times [0,1], \quad \ldots]$

The same idea applies to double-integrals over non-rectangular boundaries, too.

---

<a id="evaluating-a-double-integral-using-the-additivity-over-a-rectangular-domain"></a>
## Evaluating a Double Integral Using the Additivity Over a Rectangular Domain

**Example:** Evaluate the double integral $\displaystyle \iint\limits_{R} f(x,y)\ \textrm d A$, where

${2,\begin{vmatrix}if0 < x < 2,-1 < y < 0 \\ \frac{1}{xy + x + y + 1},\end{vmatrix}if0 < x < 2, 0 < y < 1$,
and $R$ is the domain of $f$.

**Explanation**

First, notice that we have

$$
R = R_1 \cup R_2
$$

where

$$
R_1= (0,2) \times (-1,0)
$$

and

$$
R_2= (0,2) \times (0,1)
$$

So, we can write

$$
\begin{aligned} \iint\limits_{R} f(x,y)\ \textrm d A &= \iint\limits_{R_1} 2 \ \textrm d A + \iint\limits_{R_2} \dfrac{1}{xy+x+y+1} \ \textrm d A \\[5pt] &= 2\iint\limits_{R_1} \textrm d A + \iint\limits_{R_2} \dfrac{1}{(x+1)(y+1)} \ \textrm d A \\[5pt] &= 2(2-0)(0-(-1)) + \Bigg(\int_0^2 \dfrac{1}{x+1} \ \textrm d x \Bigg) \Bigg(\int_0^1 \dfrac{1}{y+1}\ \textrm d y \Bigg) \\[5pt] &= 4 + \Big[\ln(x+1)\Big]_0^2 \cdot \Big[\ln(y+1)\Big]_0^1 \\[5pt] &= 4+ \ln 3\ln 2. \end{aligned}
$$

---

**Question 7:**

```quiz
type: radio
id: ma-71998
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Evaluate the double integral $∬_(R)f(x, y)dA$, where

  $f(x, y) = {6x,\begin{vmatrix}if0 < x < 1,-1 < y < 0, \\ e^{x + y},\end{vmatrix}if0 < x < 1, 0 < y < 1$,
  and $R$ is the domain of $f$.
options:
- id: a
  content: |-
    $e + 1$
- id: b
  content: |-
    $e^{2} - 2e + 4$
- id: c
  content: |-
    $e^{2} - 1$
- id: d
  content: |-
    $e^{2} - 4e + 4$
- id: e
  content: |-
    $e^{2} - 2e + 1$
```

---

**Question 8:**

```quiz
type: radio
id: ma-80449
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let the function $f(x, y)$ be defined as

  $f(x, y) = {x + y,\begin{vmatrix}if0 < x < 2,-1 < y < 0, \\ \sin (x + y),\end{vmatrix}if0 < x < 2, 0 < y < 1$.
  Then,
  $∬_((0, 2) \cdot (-1, 1))f(x, y)dA =$
options:
- id: a
  content: |-
    $∬_((0, 2) \cdot (-1, 0))(x + y)dA + ∬_((0, 2) \cdot (0, 1))\sin (x + y)dA$
- id: b
  content: |-
    $∬_((0, 2) \cdot (-1, 1))(x + y)dA + ∬_((0, 2) \cdot (-1, 1))\sin (x + y)dA$
- id: c
  content: |-
    $∬_((-1, 1))(x + y)dA + ∬_((0, 2))\sin (x + y)dA$
- id: d
  content: |-
    $∬_((0, 2))(x + y)dA + ∬_((-1, 1))\sin (x + y)dA$
- id: e
  content: |-
    $∬_((0, 1) \cdot (-1, 1))(x + y)dA + ∬_((1, 2) \cdot (-1, 1))\sin (x + y)dA$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
