# Matrix Gradients

<!--
lesson-id: 5869
topic-code: MML.9.5.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing the Gradient of a Function With Respect to a Matrix](#computing-the-gradient-of-a-function-with-respect-to-a-matrix)
- [Component-Wise Differentiation](#component-wise-differentiation)
- [Computing the Gradient of a Function Component-Wise](#computing-the-gradient-of-a-function-component-wise)
- [Differentiation Using the Multivariable Chain Rule](#differentiation-using-the-multivariable-chain-rule)
- [Applying the Chain Rule to Calculate Gradients](#applying-the-chain-rule-to-calculate-gradients)

## Prerequisites

- [Further Vector Gradients](<9.5.4. Further Vector Gradients.md>)

---

<a id="introduction"></a>
## Introduction

In previous lessons, we defined the gradient of a scalar-valued function with respect to a vector. In this lesson, we extend this idea to functions where the input is a *matrix* and the output is a scalar.

Suppose we have a function $f: \mathbb{R}^{m \times n} \to \mathbb{R}$ that maps a matrix $A \in \mathbb{R}^{m \times n}$ to a scalar value $f(A)$. Just like with vector inputs, we define the **gradient of $f$ with respect to $A$** as the matrix of partial derivatives of $f$ with respect to each individual entry of $A{:}$

$$
\begin{bmatrix}\frac{∂f}{∂a_{11}} & \frac{∂f}{∂a_{12}} & ⋯ & \frac{∂f}{∂a_{1n}} \\ \frac{∂f}{∂a_{21}} & \frac{∂f}{∂a_{22}} & ⋯ & \frac{∂f}{∂a_{2n}} \\ ⋮ & ⋮ & ⋱ & ⋮ \\ \frac{∂f}{∂a_{m1}} & \frac{∂f}{∂a_{m2}} & ⋯ & \frac{∂f}{∂a_{mn}}\end{bmatrix}
$$

This gradient has the same shape as the matrix $A$. It tells us how sensitive the output $f(A)$ is to changes in each element $a_{ij}$.

Let’s apply this to a concrete example. Suppose we define the function

$$
f(X) = \ln(\det(X))
$$

for a square matrix $X \in \mathbb{R}^{2 \times 2}$. Let's find the gradient of $f$ with respect to $X$ and evaluate it at a specific point.

First, we write the matrix $X$ as
$\begin{bmatrix}x_{11} & x_{12} \\ x_{21} & x_{22}\end{bmatrix}$
Then, the determinant of $X$ is

$$
\det(X) = x_{11}x_{22} - x_{12}x_{21}
$$

So, the function becomes

$$
f(X) = \ln(x_{11}x_{22} - x_{12}x_{21})
$$

To find the gradient, we differentiate $f$ with respect to each element of $X$. This gives

$$
\begin{aligned}
∇_{X}f(X) &= \begin{bmatrix}\frac{∂f}{∂x_{11}} & \frac{∂f}{∂x_{12}} \\ \frac{∂f}{∂x_{21}} & \frac{∂f}{∂x_{22}}\end{bmatrix} \\
&= \begin{bmatrix}\frac{∂}{∂x_{11}}(\ln (x_{11}x_{22} - x_{12}x_{21})) & \frac{∂}{∂x_{12}}(\ln (x_{11}x_{22} - x_{12}x_{21})) \\ \frac{∂}{∂x_{21}}(\ln (x_{11}x_{22} - x_{12}x_{21})) & \frac{∂}{∂x_{22}}(\ln (x_{11}x_{22} - x_{12}x_{21}))\end{bmatrix} \\
&= \begin{bmatrix}\frac{x_{22}}{x_{11}x_{22} - x_{12}x_{21}} & -\frac{x_{21}}{x_{11}x_{22} - x_{12}x_{21}} \\ -\frac{x_{12}}{x_{11}x_{22} - x_{12}x_{21}} & \frac{x_{11}}{x_{11}x_{22} - x_{12}x_{21}}\end{bmatrix} \\
&= \frac{1}{x_{11}x_{22} - x_{12}x_{21}}\begin{bmatrix}x_{22} & -x_{21} \\ -x_{12} & x_{11}\end{bmatrix} \\
&= (1)/(det(X))\begin{bmatrix}x_{22} & -x_{21} \\ -x_{12} & x_{11}\end{bmatrix}
\end{aligned}
$$

Now, suppose we wish to evaluate this gradient at
$\begin{bmatrix}3 & 1 \\ 5 & -2\end{bmatrix}$
First, we compute the determinant:

$$
\begin{aligned}
det(X) &= 3 \cdot (-2) - 1 \cdot 5 \\
&=-6 - 5 \\
&=-11
\end{aligned}
$$

Then, we substitute our values into our gradient. This gives

$$
\begin{aligned}
∇_{X}f(X) &= -\frac{1}{11}\begin{bmatrix}-2 & -5 \\ -1 & 3\end{bmatrix} \\
&= \frac{1}{11}\begin{bmatrix}2 & 5 \\ 1 & -3\end{bmatrix}
\end{aligned}
$$

This result tells us how the scalar output

$$
f(X) = \ln(\det(X))
$$

would change with respect to small changes in each entry of the matrix $X$. Each element in the gradient matrix captures that local rate of change.

---

<a id="computing-the-gradient-of-a-function-with-respect-to-a-matrix"></a>
## Computing the Gradient of a Function With Respect to a Matrix

**Example:** Consider the function $f: \mathbb{R}^{2 \times 2} \rightarrow \mathbb{R}$ defined by $f(X) = (\det(X))^2$.

What is the gradient of $f$ with respect to $X$ at $\begin{bmatrix}-\frac{1}{2} & \frac{3}{2} \\ -4 & 10\end{bmatrix}$

**Explanation**

The gradient of a function $f: \mathbb{R}^{m \times n} \rightarrow \mathbb{R}$ with respect to a matrix $X \in \mathbb{R}^{m \times n}$ is

$$
\begin{bmatrix}\frac{∂f}{∂x_{11}} & \frac{∂f}{∂x_{12}} & ⋯ & \frac{∂f}{∂x_{1n}} \\ \frac{∂f}{∂x_{21}} & \frac{∂f}{∂x_{22}} & ⋯ & \frac{∂f}{∂x_{2n}} \\ ⋮ & ⋮ & ⋱ & ⋮ \\ \frac{∂f}{∂x_{m1}} & \frac{∂f}{∂x_{m2}} & ⋯ & \frac{∂f}{∂x_{mn}}\end{bmatrix}
$$

In our case, writing out the function $f(X)$ where $\begin{bmatrix}x_{11} & x_{12} \\ x_{21} & x_{22}\end{bmatrix}$, we get

$$
f(X) = (\det(X))^2 = (x_{11}x_{22} - x_{12}x_{21})^2
$$

So, the gradient is

$$
\begin{aligned}
∇_{X}f(X) &= \begin{bmatrix}\frac{∂f}{∂x_{11}} & \frac{∂f}{∂x_{12}} \\ \frac{∂f}{∂x_{21}} & \frac{∂f}{∂x_{22}}\end{bmatrix} \\
&= \begin{bmatrix}\frac{∂}{∂x_{11}}(x_{11}x_{22} - x_{12}x_{21})^{2} & \frac{∂}{∂x_{12}}(x_{11}x_{22} - x_{12}x_{21})^{2} \\ \frac{∂}{∂x_{21}}(x_{11}x_{22} - x_{12}x_{21})^{2} & \frac{∂}{∂x_{22}}(x_{11}x_{22} - x_{12}x_{21})^{2}\end{bmatrix} \\
&= \begin{bmatrix}2(x_{11}x_{22} - x_{12}x_{21}) \cdot x_{22} & 2(x_{11}x_{22} - x_{12}x_{21}) \cdot (-x_{21}) \\ 2(x_{11}x_{22} - x_{12}x_{21}) \cdot (-x_{12}) & 2(x_{11}x_{22} - x_{12}x_{21}) \cdot x_{11}\end{bmatrix} \\
&= 2(x_{11}x_{22} - x_{12}x_{21})\begin{bmatrix}x_{22} & -x_{21} \\ -x_{12} & x_{11}\end{bmatrix} \\
&= 2det(X)\begin{bmatrix}x_{22} & -x_{21} \\ -x_{12} & x_{11}\end{bmatrix}
\end{aligned}
$$

Evaluating at

$$
\begin{bmatrix}-\frac{1}{2} & \frac{3}{2} \\ -4 & 10\end{bmatrix}
$$

we get

$$
\begin{aligned}
∇_{X}f(X) &= 2(-\frac{1}{2} \cdot 10 - (-4) \cdot \frac{3}{2})\begin{bmatrix}10 & 4 \\ -\frac{3}{2} & -\frac{1}{2}\end{bmatrix} \\
&= 2(-5 - (-6))\begin{bmatrix}10 & 4 \\ -\frac{3}{2} & -\frac{1}{2}\end{bmatrix} \\
&= \begin{bmatrix}20 & 8 \\ -3 & -1\end{bmatrix}
\end{aligned}
$$

---

**Question 1:** Consider the function $f: R^{2 \cdot 2} → R$ defined by
$f(X) =-\ln (det(X))$.
What is the gradient of $f$ with respect to $X$ at $X = \begin{bmatrix}2 & 1 \\ 1 & 2\end{bmatrix}$?

- [ ] A. $\begin{bmatrix}3 \\ 3\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-\frac{2}{3} & \frac{1}{3} \\ \frac{1}{3} & -\frac{2}{3}\end{bmatrix}$
- [ ] C. $\begin{bmatrix}2 & 1 \\ 1 & 2\end{bmatrix}$
- [ ] D. $\begin{bmatrix}2 & -1 \\ -1 & 2\end{bmatrix}$
- [ ] E. $1$

---

**Question 2:** Consider the function $f: R^{2 \cdot 2} → R$ defined by
$f(X) = ∑_(i = 1)^(2)∑_(j = 1)^(2)x_{ij}^{2}$
where $x_{ij}$ are the corresponding matrix's entries. What is the gradient of $f$ with respect to $X$ at $X = \begin{bmatrix}-3 & 4 \\ 1 & -2\end{bmatrix}$?

- [ ] A. $\begin{bmatrix}6 & -8 \\ -2 & 4\end{bmatrix}$
- [ ] B. $\begin{bmatrix}6 & 8 \\ 2 & 4\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-6 & 8 \\ 2 & -4\end{bmatrix}$
- [ ] D. $\begin{bmatrix}9 & 16 \\ 1 & 4\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-6 & 2 \\ 8 & -4\end{bmatrix}$

---

<a id="component-wise-differentiation"></a>
## Component-Wise Differentiation

Suppose we have a scalar-valued function defined by

$$
f = \mathbf{y}^T A \mathbf{x}
$$

where $\mathbf{y} \in \mathbb{R}^m$, $\mathbf{x} \in \mathbb{R}^n$, and $A \in \mathbb{R}^{m \times n}$.

We want to compute the gradient of $f$ with respect to the matrix $A$. The result turns out to be

$$
\nabla_A f = \mathbf{y} \cdot \mathbf{x}^T
$$

Let’s verify this by working component-wise.

First, note that we can write the product $\mathbf y^T A \mathbf x$ as a double summation as follows:

$$
f = \mathbf y^T A \mathbf x = \sum_{k=1}^m \sum_{l=1}^n y_k A_{kl} x_l
$$

In a previous lesson, we saw how to do this for $\mathbf x^T A \mathbf x$. The idea here is similar.

Now, we differentiate this summation with respect to $A_{ij}{:}$

$$
\frac{\partial f}{\partial A_{ij}} = \frac{\partial}{\partial A_{ij}} \left(\sum_{k=1}^m \sum_{l=1}^n y_k A_{kl} x_l \right)
$$

Since only the term where $k = i$ and $l = j$ depends on $A_{ij}$, all other terms vanish. So, we get

$$
\frac{\partial f}{\partial A_{ij}} = y_i x_j
$$

This tells us that the entry in the $i$th row and $j$th column of the gradient matrix is $y_i x_j$. Hence, the full gradient matrix is

$$
\nabla_A f = \mathbf{y} \cdot \mathbf{x}^T
$$

This result is an example of an outer product. The gradient of $\mathbf{y}^T A \mathbf{x}$ with respect to the matrix $A$ is given by multiplying the column vector $\mathbf{y}$ by the row vector $\mathbf{x}^T$, resulting in a matrix of the same shape as $A$.

Let’s look at a few examples to see this in action.

---

<a id="computing-the-gradient-of-a-function-component-wise"></a>
## Computing the Gradient of a Function Component-Wise

**Example:** Consider the function $L: \mathbb{R}^{2 \times 3} \rightarrow \mathbb{R}$ defined by $L(\mathbf{A}) = \mathbf{y}^T\mathbf{A}\mathbf{x}$ where
$\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix}$
What is $\nabla_{\mathbf{A}} L$ at
$\begin{bmatrix}2 & 1 & 0 \\ -1 & 4 & 3\end{bmatrix}$

**Explanation**

The gradient of $L$ with respect to $\mathbf{A}$ is a matrix of the same shape as $\mathbf{A}$, with $(i,j)$th entry given by

$$
\dfrac{\partial L}{\partial a_{ij}}
$$

the partial derivative of $L$ with respect to the $(i,j)$th entry of $\mathbf{A}$.

Writing out $L$ in component-wise notation as

$$
L = \sum\limits_{i=1}^2 \sum\limits_{j=1}^3 a_{ij} y_i x_j
$$

we have

$$
\dfrac{\partial L}{\partial a_{ij}} = y_i x_j
$$

Thus,

$$
\nabla_{\mathbf{A}} L = \mathbf{y} \cdot \mathbf{x}^T
$$

Evaluating at the given values $\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix}$ and $\begin{bmatrix}4 \\ 5\end{bmatrix}$ we get

$$
\begin{bmatrix}4 \\ 5\end{bmatrix}
$$

---

**Question 3:** Consider the function $L: R^{3 \cdot 2} → R$ defined by $L(A) = 3(y^{T}Ax)$ where
$x = \begin{bmatrix}3 \\ 3\end{bmatrix}, y = \begin{bmatrix}0 \\ -1 \\ 4\end{bmatrix}$.
What is $∇_{A}L$ at
$A = \begin{bmatrix}-2 & 3 \\ 5 & 1 \\ -1 & 0\end{bmatrix}$?

- [ ] A. $\begin{bmatrix}0 & 0 \\ -9 & -9 \\ 36 & 36\end{bmatrix}$
- [ ] B. $\begin{bmatrix}0 \\ -18 \\ 72\end{bmatrix}$
- [ ] C. $\begin{bmatrix}0 & 0 \\ -3 & -3 \\ 12 & 12\end{bmatrix}$
- [ ] D. $\begin{bmatrix}0 & -9 & 36 \\ 0 & -9 & 36\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-2 & 3 \\ 5 & 1 \\ -1 & 0\end{bmatrix}$

---

**Question 4:** Consider the function $L: R^{2 \cdot 3} → R$ defined by $L(A) = y^{T}Ax$ where
$x = \begin{bmatrix}0 \\ 10 \\ 20\end{bmatrix}, y = \begin{bmatrix}7 \\ 8\end{bmatrix}$.
What is $∇_{A}L$ at
$A = \begin{bmatrix}1 & 2 & 0 \\ 4 & 0 & 0\end{bmatrix}$?

- [ ] A. $\begin{bmatrix}0 & 70 & 140 \\ 0 & 80 & 160\end{bmatrix}$
- [ ] B. $\begin{bmatrix}0 & 0 \\ 70 & 80 \\ 140 & 160\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 4 \\ 2 & 0 \\ 0 & 0\end{bmatrix}$
- [ ] D. $\begin{bmatrix}0 & 0 \\ 10 & 70 \\ 20 & 80\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 2 & 0 \\ 4 & 0 & 0\end{bmatrix}$

---

<a id="differentiation-using-the-multivariable-chain-rule"></a>
## Differentiation Using the Multivariable Chain Rule

When working with scalar-valued functions defined on matrices, it's often helpful to compute gradients using component-wise differentiation and the chain rule. This approach breaks down the computation into simpler intermediate steps and allows us to clearly see the dependencies.

Consider a *scalar* function $L(\mathbf A)$, defined as

$$
L(\mathbf{A}) = \frac{1}{2} \left(\mathbf{A}\mathbf{x} - \mathbf{b} \right)^T \left(\mathbf{A}\mathbf{x} - \mathbf{b} \right)
$$

where $\mathbf{A} \in \mathbb{R}^{2 \times 3}$, $\mathbf{x} \in \mathbb{R}^3$, and $\mathbf{b} \in \mathbb{R}^2$.

Suppose we want to compute the gradient $\nabla_{\mathbf{A}} L$, which is a matrix of the same shape as $\mathbf{A}$. The entry in the $i$th row and $j$th column of the gradient matrix is given by

$$
\dfrac{\partial L}{\partial a_{ij}}
$$

To simplify the differentiation, we define $\mathbf y \in\mathbb R^{2\times 1}$ as
$\begin{bmatrix}y_{1} \\ y_{2}\end{bmatrix}$

Then, we can write $L$ in terms of $\mathbf y$ as

$$
L = \frac{1}{2} \mathbf{y}^T \mathbf{y}
$$

To compute

$$
\dfrac{\partial L}{\partial a_{ij}}
$$

, we apply the multivariable chain rule:

$$
\frac{\partial L}{\partial a_{ij}} = \sum_{k=1}^2 \frac{\partial L}{\partial y_k} \cdot \frac{\partial y_k}{\partial a_{ij}}
$$

Note that the summation is from $k=1$ to $k=2$ because $\mathbf y$ has two components, and we're differentiating with respect to each of them.

Now, let's analyze the two factors of the product that appear in this summation.

- We know that
$∇_{y}L|= ∇_{y}(\frac{1}{2}y^{T}y); = \frac{1}{2} \cdot ∇_{y}(y^{T}y); = \frac{1}{2} \cdot 2y; = y$.
Therefore, we have
$\frac{\partial L}{\partial y_k} = y_k$.
- Writing $\mathbf{y} = \mathbf{A} \mathbf{x} - \mathbf{b}$ in component form
$y_i = \sum_{j=1}^3 a_{ij} x_j - b_i$,
we have
${x_{j},\begin{vmatrix}i = k, \\ 0,\end{vmatrix}otherwise$.
Only the $k$th row of $\mathbf{A}$ affects $y_k$.

Putting our two results together gives

$$
\begin{aligned}
\frac{∂L}{∂a_{ij}} &= ∑_(k = 1)^(2)\frac{∂L}{∂y_{k}} \cdot \frac{∂y_{k}}{∂a_{ij}} \\
&= \frac{∂L}{∂y_{i}} \cdot \frac{∂y_{i}}{∂a_{ij}} \\
&= y_{i}x_{j}
\end{aligned}
$$

because all terms vanish except the one where $k=i$.

Therefore, we conclude that

$$
\nabla_{\mathbf{A}} L = \mathbf{y} \cdot \mathbf{x}^T
$$

In the next example, we will use this formula to compute the gradient for specific vectors.

---

<a id="applying-the-chain-rule-to-calculate-gradients"></a>
## Applying the Chain Rule to Calculate Gradients

**Example:** Consider the function $L: \mathbb{R}^{2 \times 3} \rightarrow \mathbb{R}$ defined by $L(\mathbf{A}) = \dfrac{1}{2}(\mathbf{A}\mathbf{x} - \mathbf{b})^T(\mathbf{A}\mathbf{x} - \mathbf{b})$, where
$\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix}$
What is $\nabla_{\mathbf{A}} L$ at
$\begin{bmatrix}1 & 0 & 2 \\ 0 & 1 & 1\end{bmatrix}$

**Explanation**

The gradient of $L$ with respect to $\mathbf{A}$ is a matrix of the same shape as $\mathbf{A}$, with $(i,j)$th entry given by

$$
\dfrac{\partial L}{\partial a_{ij}}
$$

the partial derivative of $L$ with respect to the $(i,j)$th entry of $\mathbf{A}$.

We can compute

$$
\dfrac{\partial L}{\partial a_{ij}}
$$

by applying the chain rule.

Let

$$
\mathbf{y} = \mathbf{A}\mathbf{x} - \mathbf{b} \in \mathbb{R}^2
$$

Then,

$$
L(\mathbf{A}) = \dfrac{1}{2}\mathbf{y}^T\mathbf{y}
$$

and

$$
\dfrac{\partial L}{\partial a_{ij}} = \sum_{k=1}^2 \dfrac{\partial L}{\partial y_k} \cdot \dfrac{\partial y_k}{\partial a_{ij}}
$$

Now, since

$$
\nabla_{\mathbf{y}} L = \nabla_{\mathbf{y}} \bigg(\dfrac{1}{2}\mathbf{y}^T\mathbf{y} \bigg) = \mathbf{y}
$$

we have that

$$
\dfrac{\partial L}{\partial y_k} = y_k
$$

Writing out

$$
\mathbf{y} = \mathbf{A}\mathbf{x} - \mathbf{b}
$$

in component-wise notation as

$$
y_i = \sum\limits_{j=1}^3 (a_{ij} x_j - b_i)
$$

we have

- $\dfrac{\partial y_k}{\partial a_{ij}} = x_j$ if $i = k$, and
- $\dfrac{\partial y_k}{\partial a_{ij}} = 0$ if $i \neq k$.

This means that

$$
\begin{aligned}
\frac{∂L}{∂a_{ij}} &= ∑_(k = 1)^(2)\frac{∂L}{∂y_{k}} \cdot \frac{∂y_{k}}{∂a_{ij}} \\
&= \frac{∂L}{∂y_{i}} \cdot \frac{∂y_{i}}{∂a_{ij}} \\
&= y_{i} \cdot \frac{∂y_{i}}{∂a_{ij}} \\
&= y_{i}x_{j}
\end{aligned}
$$

The $(i,j)$th entry of $\nabla_{\mathbf{A}} L$ is the $i$th entry of $\mathbf{y}$ multiplied by the $j$th entry of $\mathbf{x}$, so

$$
\nabla_{\mathbf{A}} L = \mathbf{y} \cdot \mathbf{x}^T
$$

Finally, evaluating $\nabla_{\mathbf{A}} L$ at $\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix}$ we get

$$
\begin{aligned}
∇_{A}L &= y \cdot x^{T} \\
&= (Ax - b) \cdot x^{T} \\
&= (\begin{bmatrix}1 & 0 & 2 \\ 0 & 1 & 1\end{bmatrix}\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix} - \begin{bmatrix}4 \\ 5\end{bmatrix}) \cdot \begin{bmatrix}1 & 2 & 3\end{bmatrix} \\
&= (\begin{bmatrix}7 \\ 5\end{bmatrix} - \begin{bmatrix}4 \\ 5\end{bmatrix}) \cdot \begin{bmatrix}1 & 2 & 3\end{bmatrix} \\
&= \begin{bmatrix}3 \\ 0\end{bmatrix} \cdot \begin{bmatrix}1 & 2 & 3\end{bmatrix} \\
&= \begin{bmatrix}3 & 6 & 9 \\ 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

---

**Question 5**

Consider the function $L: R^{3 \cdot 2} → R$ defined by $L(A) = (3Ax - 2b)^{T}(3Ax - 2b)$, where
$x = \begin{bmatrix}0 \\ 4\end{bmatrix}, b = \begin{bmatrix}2 \\ 1 \\ 0\end{bmatrix}$.
What is $∇_{A}L$ at
$A = \begin{bmatrix}1 & 0 \\ 1 & 2 \\ 0 & 3\end{bmatrix}$?

| $∇_{A} =$ |  | $\underline{\hspace{1.5cm}}$ | $-96$ |  |
| --- | ---: | ---: | ---: | ---: |
|  | $0$ | $\underline{\hspace{1.5cm}}$ |  |  |
|  | $\underline{\hspace{1.5cm}}$ | $864$ |  |  |

---

**Question 6**

Consider the function $L: R^{2 \cdot 3} → R$ defined by $L(A) = \frac{1}{2}(Ax - b)^{T}(Ax - b)$, where
$x = \begin{bmatrix}0 \\ 10 \\ 20\end{bmatrix}, b = \begin{bmatrix}7 \\ 8\end{bmatrix}$.
What is $∇_{A}L$ at
$A = \begin{bmatrix}1 & 2 & 0 \\ 4 & 0 & 0\end{bmatrix}$?

| $∇_{A} =$ |  | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |  |
| --- | ---: | ---: | ---: | ---: | ---: |
|  | $0$ | $\underline{\hspace{1.5cm}}$ | $-160$ |  |  |

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
