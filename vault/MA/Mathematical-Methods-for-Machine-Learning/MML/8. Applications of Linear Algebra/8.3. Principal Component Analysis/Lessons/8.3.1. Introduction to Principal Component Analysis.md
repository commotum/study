# Introduction to Principal Component Analysis

<!--
lesson-id: 3775
topic-code: MML.8.3.1
-->

## Table of Contents

- [Introduction](#introduction)
- [PCA: Data Setup](#pca-data-setup)
- [PCA: Constructing the Covariance Matrix](#pca-constructing-the-covariance-matrix)
- [PCA: Diagonalization](#pca-diagonalization)
- [The Principal Components](#the-principal-components)
- [Interpretation of Principal Components](#interpretation-of-principal-components)
- [Identifying the First Principal Component From a Scatter Plot](#identifying-the-first-principal-component-from-a-scatter-plot)
- [Identifying the Second Principal Component From a Scatter Plot](#identifying-the-second-principal-component-from-a-scatter-plot)
- [Applications of Principal Component Analysis](#applications-of-principal-component-analysis)
- [Representing Principal Components as Linear Combinations](#representing-principal-components-as-linear-combinations)

## Prerequisites

- [The Z-Score](<../../../../PCL/12. Statistics/12.1. Analyzing Data/Lessons/12.1.7. The Z-Score.md>)
- [The Sample Covariance Matrix](<../../../12. Parametric Inference/12.1. Point Estimation/Lessons/12.1.9. The Sample Covariance Matrix.md>)
- [Diagonalization of 2x2 Symmetric Matrices](<../../../5. Diagonalization of Matrices/5.2. Diagonalization/Lessons/5.2.6. Diagonalization of 2x2 Symmetric Matrices.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we are analyzing house prices in a specific neighborhood, gathering data on quantitative features such as the total floor space, number of bedrooms and bathrooms, and distance to the town center. Our goal is to understand the relationship between these features and property values.

Problems involving multi-dimensional data analysis, such as this one, pose several challenges:

- Multi-dimensional data is often difficult to analyze.
- Visualization can be virtually impossible.
- Storing multi-dimensional data on a computer can require substantial hardware resources.

We can overcome these challenges using **dimensionality reduction.** Dimensionality reduction works by compressing data into a smaller number of variables while (ideally) preserving as much information as possible.

To demonstrate the basic idea behind dimensionality reduction, consider the following data set for the variables $x$ and $y{:}$

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/be7a44a4b7e9cf74c39037a1b5f59789.png>)

Notice that the data does not vary much in the $y$-direction. Therefore, we can transform the data set by projecting all points onto the $x$-axis. This process removes the variation in $y$ with almost no loss of overall information, as shown below:

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/651afef7701aecac9363ddad7cb95f8d.png>)

Multi-dimensional data typically exhibit characteristics suitable for dimensionality reduction:

- Many variables are often correlated. In our house prices example, the number of bedrooms and bathrooms likely correlate. This means that one variable can often be expressed in terms of another. Or, we can find a new variable that describes the variation in both original variables.
- Ultimately, we can aim to reduce the higher-dimensional structure of the data to a more fundamental, lower-dimensional structure containing only uncorrelated variables.

By reducing the dimensionality of the data, we can work with a compact representation that retains only the most critical variables and discards the rest. This simplifies analysis and visualization.

---

<a id="pca-data-setup"></a>
## PCA: Data Setup

In the previous section, we saw that dimensionality reduction compresses data into fewer, uncorrelated variables. **Principal Component Analysis (PCA)** is a widely used technique for achieving this.

Suppose we have a sample of $n$ observations, each with $m$ features. We organize this data into an $n \times m$ **observation matrix** $X$, where each row is an observation and each column is a feature.

For example, if we had $n=3$ observations of $m=2$ features, our observation matrix might look like this:
$\begin{bmatrix}1 & 10 \\ 2 & 20 \\ 3 & 30\end{bmatrix}$
Before applying PCA, we must standardize the data. This is because PCA is sensitive to the scale of features. For instance, measuring a feature in meters versus centimeters would lead to different results. We standardize by giving each column a mean of $0$ and a standard deviation of $1$.

For our example, the first column has mean $2$ and standard deviation $1$, while the second column has mean $20$ and standard deviation $10$. Our standardized observation matrix $X$ is:
$\begin{bmatrix}(1 - 2)/1 & (10 - 20)/10 \\ (2 - 2)/1 & (20 - 20)/10 \\ (3 - 2)/1 & (30 - 20)/10\end{bmatrix}$
Next, we'll use this standardized data to analyze the relationships between the features.

---

<a id="pca-constructing-the-covariance-matrix"></a>
## PCA: Constructing the Covariance Matrix

Now that our data matrix $X$ is standardized, we examine the relationships between its features using the **sample covariance matrix**, $C$. For standardized data, this is calculated as:

$$
C=\dfrac{1}{n-1} X^T X
$$

Using our standardized matrix from the previous slide, where $n=3$:

$$
\begin{aligned}
C &= \frac{1}{3 - 1}\begin{bmatrix}-1 & 0 & 1 \\ -1 & 0 & 1\end{bmatrix}\begin{bmatrix}-1 & -1 \\ 0 & 0 \\ 1 & 1\end{bmatrix} \\
&= \frac{1}{2}\begin{bmatrix}2 & 2 \\ 2 & 2\end{bmatrix} \\
&= \begin{bmatrix}1 & 1 \\ 1 & 1\end{bmatrix}
\end{aligned}
$$

The diagonal entries of $C$ are the variances of the features, and the off-diagonal entries are the covariances between pairs of features.

---

<a id="pca-diagonalization"></a>
## PCA: Diagonalization

Let's look again at the covariance matrix we just calculated:
$\begin{bmatrix}1 & 1 \\ 1 & 1\end{bmatrix}$
Notice that the matrix $C$ is symmetric. In fact, the sample covariance matrix is *always* a real symmetric matrix.

Recall from linear algebra that any real symmetric matrix is **orthogonally diagonalizable**. This means we can find an orthogonal matrix $V$ and a diagonal matrix $D$ such that
$V^T C V = D$
The entries of the diagonal matrix $D$ are the eigenvalues of $C$, and the columns of the matrix $V$ are the corresponding orthonormal eigenvectors.

Next, we shall see how to use the matrix $V$ to find uncorrelated features for our data.

---

<a id="the-principal-components"></a>
## The Principal Components

Recall that our goal is to find new features that are uncorrelated — equivalently, features whose covariance matrix is diagonal. In the previous section, we found an orthogonal matrix $V$ such that $V^T\!CV$ is diagonal. This motivates defining a **transformed observation matrix** $Y = XV$. The columns of this new matrix $Y$ are our new features.

Substituting

$$
C = \dfrac{1}{n-1} X^T X
$$

into the expression $V^T\!CV$ and noting that

$$
(XV)^T = V^T X^T{:}
$$

$$
\begin{aligned}
V^{T}CV_(⏟)_(diagonal) &= V^{T}(\frac{1}{n - 1}X^{T}X)V \\
&= \frac{1}{n - 1}(XV)^{T}(XV) \\
&= \frac{1}{n - 1}Y^{T}Y_(⏟)_(covariance of [math]Y[/math])
\end{aligned}
$$

The covariance matrix of $Y$ is diagonal, so the new features $y_1, y_2, \ldots, y_m$ are uncorrelated. The diagonal entries are the eigenvalues of $C$, so

$$
\text{Var}(y_i) = \lambda_i
$$

By arranging the columns of $V$ in order of decreasing eigenvalue, the new features are also arranged in order of decreasing variance.

The columns of $V$ (the unit eigenvectors of $C$) are called the **principal components**. The $1$st *principal component* is the eigenvector corresponding to the largest eigenvalue, the $2$nd corresponds to the second-largest, and so on.

This procedure is the core of **Principal Component Analysis (PCA)**. To summarize:

> *We define the change-of-variables $Y = XV$, where the columns of $V$ are the **principal components** of the data, arranged in order of decreasing eigenvalue. This ensures that:*
> - *The new features $y_1,y_2,\ldots,y_m$ are uncorrelated (diagonal covariance).*
> - *The new features are arranged in order of decreasing variance ($\text{Var}(y_i) = \lambda_i$).*

Now let's understand what these vectors mean in terms of the original data.

---

<a id="interpretation-of-principal-components"></a>
## Interpretation of Principal Components

The statistical interpretation of the principal components is as follows:

- The $1$st principal component defines a direction along which the original data has the maximum variance.
- The $2$nd principal component is perpendicular to the $1$st component and defines a direction along which the remaining data has the maximum variance.
- The $3$rd principal component is perpendicular to both the $1$st and the $2$nd components and defines a direction along which the remaining data has the maximum variance.$\vdots$ and so on.

To illustrate, let's consider the following scatter plot of a sample of two-dimensional (paired) observations.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/368882f67158c570527bd593cbb15ea0.png>)

In our diagram, the direction along which the data varies the most corresponds (approximately) to the line shown below.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/ba1a44157e355104151c9ae1780be494.png>)

Therefore, the vectors corresponding to the first and second principal components of the data could be the unit vectors shown below.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/91b24aa4e6cf140ecb1131b6366d2ffc.png>)

---

<a id="identifying-the-first-principal-component-from-a-scatter-plot"></a>
## Identifying the First Principal Component From a Scatter Plot

**Example:** The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the first principal component of the data?

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/92fc0b08a3b713d235ef9b2a2ab0daf8.png>)

**Explanation**

Recall that the *first principal component* of the data corresponds to the direction in which the data varies the most (the direction where we have the maximum variance).

In our diagram, the direction where the variance changes the most corresponds (approximately) to the vector $\mathbf{a}$, as shown below.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/3d4c15dc774d411a46923bd50d98ca35.png>)

Therefore, the correct answer is "$\mathbf{a}$ only."

---

**Question 1**

```quiz
type: radio
id: ma-148440
content: |-
  ![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/q-148440.png>)

  The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the first principal component of the data?
options:
- id: a
  content: |-
    $c$ only
- id: b
  content: |-
    $b$ and $c$ only
- id: c
  content: |-
    $a$ only
- id: d
  content: |-
    $b$ only
- id: e
  content: |-
    $a$ and $b$ only
  correct: true
```

---

**Question 2**

```quiz
type: radio
id: ma-148157
content: |-
  ![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/q-148157.png>)

  The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the first principal component of the data?
options:
- id: a
  content: |-
    $c$ only
- id: b
  content: |-
    $a$ and $b$ only
- id: c
  content: |-
    $b$ and $c$ only
- id: d
  content: |-
    $a$ only
- id: e
  content: |-
    $b$ only
  correct: true
```

---

<a id="identifying-the-second-principal-component-from-a-scatter-plot"></a>
## Identifying the Second Principal Component From a Scatter Plot

**Example:** The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the second principal component of the data?

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/c15e6a9a2161adcf61a9f6b35330e0d6.png>)

**Explanation**

Recall that the *first principal component* of the data corresponds to the direction in which the data varies the most (the direction where we have the maximum variance).

The *second principal component* of the data corresponds to the direction perpendicular to the first principal component in which the data varies the most.

In our diagram, the direction where the variance changes the most corresponds (approximately) to the vector $\mathbf{b}$, as shown below. So, $\mathbf{b}$ defines the direction of the first principal component.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/afcf62fd44044d12a87e7c669aa20e84.png>)

Next, notice that in the 2D case, we have only one direction perpendicular to the first principal component. And this direction corresponds to the vector $\mathbf{c}$, as shown below.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/50a19828e4365d90a8dbabb12596bdc4.png>)

Therefore, the correct answer is "$\mathbf{c}$ only."

---

**Question 3**

```quiz
type: radio
id: ma-148491
content: |-
  ![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/q-148491.png>)

  The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the second principal component of the data?
options:
- id: a
  content: |-
    $b$ only
- id: b
  content: |-
    $a$ only
- id: c
  content: |-
    $b$ and $c$ only
  correct: true
- id: d
  content: |-
    $a$ and $b$ only
- id: e
  content: |-
    $c$ only
```

---

**Question 4**

```quiz
type: radio
id: ma-148159
content: |-
  ![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/q-148159.png>)

  The scatter plot for a sample of paired observations is shown above. What vectors correspond to the direction of the second principal component of the data?
options:
- id: a
  content: |-
    $c$ only
  correct: true
- id: b
  content: |-
    $b$ only
- id: c
  content: |-
    $a$ only
- id: d
  content: |-
    $b$ and $c$ only
- id: e
  content: |-
    $a$ and $c$ only
```

---

<a id="applications-of-principal-component-analysis"></a>
## Applications of Principal Component Analysis

Suppose a physician measured two features, $x_1$ and $x_2$, corresponding to height and weight for each athlete among a group who attended a particular competition. The collected data were stored in a standardized observation matrix, with each row containing both features for each athlete.

A scatter plot of the physician's results is shown below.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/20a8b3daec066a922821a2c36630b30f.png>)

Remember that the first and second principal components, denoted $\mathbf v_1$ and $\mathbf v_2$, are the unit eigenvectors of the sample covariance matrix for the data set. The physician calculates that the first and second principal components of this data are approximately
$\begin{bmatrix}0.316 \\ 0.947\end{bmatrix}$

The first and second principal components are shown below:

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/bfc97a6bbe195c83294108242e04a373.png>)

The physician wants to combine the two features linearly into a single feature that preserves the maximum variance. What is the best candidate for the new feature?

The matrix of principal components, denoted $V$, is given by

$$
[ \mid \mid \mid; v_{1} \mid v_{2}; \mid \mid \mid ]
$$

Applying the change of variables $Y = XV,\,$ we can convert our observation matrix $X$ into a new matrix $Y$ of (linearly) transformed observations such that

- the covariance matrix of $Y$ is diagonal (meaning that the new features $y_1$ and $y_2$ are uncorrelated), and
- the features $y_1$ and $y_2$ are arranged in order of decreasing variance (meaning that most of the variance is concentrated in $y_1$).

Applying the change of variables to a single observation $\begin{bmatrix}x_{1} & x_{2}\end{bmatrix}$ we have

$$
\begin{aligned}
\begin{bmatrix}y_{1} & y_{2}\end{bmatrix} &= \begin{bmatrix}x_{1} & x_{2}\end{bmatrix}V \\
&= \begin{bmatrix}x_{1} & x_{2}\end{bmatrix}\begin{bmatrix}0.316 & -0.947 \\ 0.947 & 0.316\end{bmatrix} \\
&= \begin{bmatrix}0.316x_{1} + 0.947x_{2} & -0.947x_{1} + 0.316x_{2}\end{bmatrix}
\end{aligned}
$$

As a result,

$$
y_1 = 0.316 x_1 + 0.947 x_2
$$

will contain most of the variance of the data. Therefore, the best candidate for the new single feature is

$$
y_1 = 0.316 x_1 + 0.947 x_2
$$

---

<a id="representing-principal-components-as-linear-combinations"></a>
## Representing Principal Components as Linear Combinations

**Example:** A geologist has measured two specific features, $x_1$ and $x_2$, for each rock among a sample of rocks she is studying. The collected data was stored in a standardized observation matrix, where each row contains both features for a particular rock.
The first and second principal components of the data are approximately
$\begin{bmatrix}-0.447 \\ 0.894\end{bmatrix}$
By combining the two measured features linearly, the geologist wants to construct a single feature that preserves the maximum amount of variance of the collected data. Which of the following could be the best candidate for the new feature?

**Explanation**

Recall the direction in which the data varies the most (the direction where we have the maximum variance) corresponds to the *first principal component* of the data.

![](<../Source/8.3.1. Introduction to Principal Component Analysis/Images/5564f2a421051c9f4b4561c46df46326.png>)

Let

$$
[ \mid \mid \mid; v_{1} \mid v_{2}; \mid \mid \mid ]
$$

be the matrix of principal components. Then, applying the change of variables $Y = XV$, we can convert our observation matrix $X$ into a new matrix $Y$ of (linearly) transformed observations such that

- the covariance matrix of $Y$ is diagonal (meaning that the new features $y_1$ and $y_2$ are uncorrelated), and
- the features $y_1$ and $y_2$ are arranged in order of decreasing variance (meaning that most of the variance is concentrated in $y_1$).

Applying our change of variables to a single observation $\begin{bmatrix}x_{1} & x_{2}\end{bmatrix}$ we get

$$
\begin{aligned}
\begin{bmatrix}y_{1} & y_{2}\end{bmatrix} &= \begin{bmatrix}x_{1} & x_{2}\end{bmatrix}V \\
&= \begin{bmatrix}x_{1} & x_{2}\end{bmatrix}\begin{bmatrix}-0.447 & -0.894 \\ 0.894 & -0.447\end{bmatrix} \\
&= \begin{bmatrix}-0.447x_{1} + 0.894x_{2} & -0.894x_{1} - 0.447x_{2}\end{bmatrix}
\end{aligned}
$$

As a result, we have that

$$
y_1 = -0.447 x_1 + 0.894 x_2
$$

will contain most of the variance of the data.

Therefore, the best candidate for the new single feature is

$$
y_1 = -0.447 x_1 + 0.894 x_2
$$

---

**Question 5**

```quiz
type: radio
id: ma-343703
content: |-
  An engineer measured two particular features, $x_{1}$ and $x_{2}$, for each road bridge in a city. The collected data was stored in a standardized observation matrix, where each row contains both features for a particular bridge.
  The first and second principal components of the data are approximately
  $v_{1} \approx \begin{bmatrix}0.669 \\ 0.743\end{bmatrix}, v_{2} \approx \begin{bmatrix}0.743 \\ -0.669\end{bmatrix}$.

  By combining the two measured features linearly, the engineer wants to construct a single feature that preserves the maximum amount of variance of the collected data. Which of the following could be the best candidate for the new feature?
options:
- id: a
  content: |-
    $0.669x_{1} - 0.743x_{2}$
- id: b
  content: |-
    $0.743x_{2}$
- id: c
  content: |-
    $0.669x_{1} + 0.743x_{2}$
  correct: true
- id: d
  content: |-
    $0.743x_{1} - 0.669x_{2}$
- id: e
  content: |-
    $0.669(x_{1} + x_{2})$
```

---

**Question 6**

```quiz
type: radio
id: ma-344071
content: |-
  A scientist measured two particular features, $x_{1}$ and $x_{2}$, for each athlete in a team. The collected data was stored in a standardized observation matrix, where each row contains both features for a particular athlete.
  The first and second principal components of the data are approximately
  $v_{1} \approx \begin{bmatrix}0.894 \\ 0.447\end{bmatrix}, v_{2} \approx \begin{bmatrix}-0.447 \\ 0.894\end{bmatrix}$.

  By combining the two measured features linearly, the scientist wants to construct a single feature that preserves the maximum amount of variance of the collected data. Which of the following could be the best candidate for the new feature?
options:
- id: a
  content: |-
    $0.447x_{2}$
- id: b
  content: |-
    $0.894(x_{1} + x_{2})$
- id: c
  content: |-
    $0.894x_{1} + 0.447x_{2}$
  correct: true
- id: d
  content: |-
    $0.894x_{1} - 0.447x_{2}$
- id: e
  content: |-
    $-0.447x_{1} + 0.894x_{2}$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
