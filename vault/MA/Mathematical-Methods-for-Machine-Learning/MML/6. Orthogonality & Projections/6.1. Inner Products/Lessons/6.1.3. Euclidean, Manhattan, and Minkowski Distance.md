# Euclidean, Manhattan, and Minkowski Distance

<!--
lesson-id: 2915
topic-code: MML.6.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Euclidean Metric](#euclidean-metric)
- [Manhattan Metric](#manhattan-metric)
- [Connection Between Metrics and Norms](#connection-between-metrics-and-norms)
- [Computing the Euclidean Distance](#computing-the-euclidean-distance)
- [Computing the Manhattan Distance](#computing-the-manhattan-distance)
- [The Minkowski Distance](#the-minkowski-distance)
- [Computing the Minkowski Distance](#computing-the-minkowski-distance)
- [The Chebyshev Distance](#the-chebyshev-distance)
- [Computing the Chebyshev Distance](#computing-the-chebyshev-distance)

## Prerequisites

- [The Norm of a Vector in N-Dimensional Euclidean Space](<6.1.2. The Norm of a Vector in N-Dimensional Euclidean Space.md>)

---

<a id="introduction"></a>
## Introduction

In this lesson, we’ll explore different ways to measure how far apart two points are in space.

To formally define a distance between two points $x$ and $y$, we use a **metric function** or **distance function**. This is a rule $d(x, y)$ that assigns a non-negative number to each pair of points, representing how far apart they are.

A valid distance function must satisfy these four properties:

- *Identity*:
$d(x, x) = 0$
- *Positivity*:
$d(x, y) > 0 \quad \text{if} \quad x \ne y$
- *Symmetry*:
$d(x, y) = d(y, x)$
- *Triangle inequality*:
$d(x, z) \leq d(x, y) + d(y, z)$

These conditions ensure the function behaves like a meaningful measure of distance.

Let’s consider some widely used concrete examples of metrics.

---

<a id="euclidean-metric"></a>
## Euclidean Metric

The most familiar example of a metric is the **Euclidean distance**, which represents the straight-line distance between two points.

For two points $\mathbf{x}$ and $\mathbf{y}$, it is defined as

$$
d_2(\mathbf{x}, \mathbf{y}) = \sqrt{\sum_{i=1}^N (x_i - y_i)^2}
$$

Euclidean distance measures the shortest path directly connecting the points, as shown in the figure below.

![](<../Source/6.1.3. Euclidean, Manhattan, and Minkowski Distance/Images/f1cf11fa0179442b5bf2904628016477.png>)

Next, we will look at an alternative metric based on grid-like movement.

---

<a id="manhattan-metric"></a>
## Manhattan Metric

Another common metric is the **Manhattan distance**, also known as **taxicab distance**.

It is defined as

$$
d_1(\mathbf{x}, \mathbf{y}) = \sum_{i=1}^N \mid x_i - y_i \mid
$$

Manhattan distance measures the total movement along a grid, adding the distance traveled in each coordinate direction rather than cutting across the diagonal. This is shown in the figure below.

![](<../Source/6.1.3. Euclidean, Manhattan, and Minkowski Distance/Images/31c0370b64f2e2b3015aec4b92f2a788.png>)

---

<a id="connection-between-metrics-and-norms"></a>
## Connection Between Metrics and Norms

Although Manhattan and Euclidean distances are defined in different ways, they have something important in common. What matters for both is the difference between the two points, not their absolute positions.

Each one measures the length of the vector connecting the points, just with a different notion of distance. This makes it possible to express both using the idea of a norm.

A **norm** is a function that measures the size or length of a vector.

There are many types of norms, but two of the most commonly used, and closely related to the distance metrics we just defined, are the **Euclidean norm** and the **Manhattan norm**.

- The **Euclidean norm** (also called the $L^2$ norm) is given by
$\| \mathbf{v} \|_2 = \sqrt{\sum_{i=1}^N v_i^2}$.
This is the usual length of a vector in geometric terms.
- The **Manhattan norm** (also called the $L^1$ norm) is given by
$\\begin{vmatrix}\mathbf{v} \|_1 = \sum_{i=1}^N & v_i\end{vmatrix}$.
This gives the total amount of movement in all coordinate directions.

Now we can rewrite our earlier distance formulas using norms. The Euclidean distance becomes

$$
d_2(\mathbf{x}, \mathbf{y}) = \| \mathbf{x} - \mathbf{y} \|_2
$$

and the Manhattan distance becomes

$$
d_1(\mathbf{x}, \mathbf{y}) = \| \mathbf{x} - \mathbf{y} \|_1
$$

This makes the connection between distances and vector norms clear; distances are just norms of differences.

Now, let's work through a few examples to understand how to compute distances step by step.

---

<a id="computing-the-euclidean-distance"></a>
## Computing the Euclidean Distance

**Example:** Suppose $\mathbf a=[7,\, 2,\, 4]^T$ and $\mathbf b=[2,\, 3,\, 1]^T$. Find the Euclidean distance $d_2$ between $\mathbf a$ and $\mathbf b$.

**Explanation**

Recall that the Euclidean distance is defined as

$$
d_2(\mathbf a,\mathbf b) =\|\mathbf a-\mathbf b\|_2
$$

where for an $N$-dimensional vector $\mathbf v$,

$$
\|\mathbf v\|_2 = \sqrt{\sum_{i=1}^N \mid v_i \mid ^2}
$$

Therefore, we have

$$
\begin{aligned}
d_{2}(a, b) &= ‖a - b‖_{2} \\
&= ‖\begin{bmatrix}7 \\ 2 \\ 4\end{bmatrix} - \begin{bmatrix}2 \\ 3 \\ 1\end{bmatrix}‖_{2} \\
&= \sqrt{\begin{vmatrix}7 - 2 & ^{2} + & 2 - 3 & ^{2} + & 4 - 1\end{vmatrix}^{2}} \\
&= \sqrt{35} \\
&\approx 5.916
\end{aligned}
$$

rounded to $3$ decimal places.

---

**Question 1**

Suppose $a = [5, 1]^{T}$ and $b = [3,-2]^{T}$. Find the Euclidean distance between $a$ and $b$.Rounded to three decimal places, $d_{2}(a, b) \approx$
$\underline{\hspace{1.5cm}}$.

---

**Question 2**

Suppose $a = [- 2, 2, 0]^{T}$ and $b = [1, 6, 1]^{T}$. Find the Euclidean distance between $a$ and $b$.Rounded to three decimal places, $d_{2}(a, b) \approx$ $\underline{\hspace{1.5cm}}$.

---

<a id="computing-the-manhattan-distance"></a>
## Computing the Manhattan Distance

**Example:** Suppose $\mathbf a=[6,\, -1,\,2]^T$ and $\mathbf b=[4,\, -4,\,0]^T$. Find the Manhattan distance $d_1$ between $\mathbf a$ and $\mathbf b$.

**Explanation**

Recall that the Manhattan distance is defined as

$$
d_1(\mathbf a,\mathbf b) =\|\mathbf a-\mathbf b\|_1
$$

where for an $N$-dimensional vector $\mathbf v$,

$$
\\begin{vmatrix}\mathbf v\|_1 = \sum_{i=1}^N & v_i\end{vmatrix}
$$

Therefore, we have

$$
\begin{aligned}
d_{1}(a, b) &= ‖a - b‖_{1} \\
&= ‖\begin{bmatrix}6 \\ -1 \\ 2\end{bmatrix} - \begin{bmatrix}4 \\ -4 \\ 0\end{bmatrix}‖_{1} \\
&=\begin{vmatrix}6 - 4 & + & -1 - (-4) & + & 2 - 0\end{vmatrix} \\
&= 7
\end{aligned}
$$

---

**Question 3**

Suppose $a = [1, 2]^{T}$ and $b = [- 1,-1]^{T}$. Find the Manhattan distance between $a$ and $b$.$d_{1}(a, b) =$ $\underline{\hspace{1.5cm}}$

---

**Question 4**

Suppose $a = [6, 3, 2]^{T}$ and $b = [3, 6, 4]^{T}$. Find the Manhattan distance between $a$ and $b$.$d_{1}(a, b) =$
$\underline{\hspace{1.5cm}}$

---

<a id="the-minkowski-distance"></a>
## The Minkowski Distance

We’ve seen how both Euclidean and Manhattan distances can be written in terms of vector norms. These are actually special cases of a more general formula known as the **Minkowski distance**.

Given a fixed value

$$
p \ge 1
$$

, the Minkowski distance between two points $\mathbf{a}$ and $\mathbf{b}$ in $N$-dimensional space is defined as

$$
d_p(\mathbf{a}, \mathbf{b}) = \left(\sum_{i=1}^N \mid a_i - b_i \mid ^p \right)^{\frac{1}{p}}
$$

The corresponding **Minkowski norm** is given by

$$
\| \mathbf{v} \|_p = \left(\sum_{i=1}^N \mid v_i \mid ^p \right)^{\frac{1}{p}}
$$

The parameter $p$ determines how differences in individual coordinates contribute to the total distance:

- **$p=1$** (Manhattan): All differences are summed equally.
- **$p=2$** (Euclidean): Larger differences have more weight due to squaring.
- **$p \to \infty$**: The distance is determined entirely by the single largest coordinate difference.

Let’s compute an example to see how this works in practice.

Suppose

$$
\mathbf{a} = [1,\ 2,\ 3]^T
$$

and

$$
\mathbf{b} = [4,\ 0,\ 6]^T
$$

. Let's compute their Minkowski distance using $p = 5$.

Using the formula, we have

$$
\begin{aligned}
d_{5}(a, b) &= ‖a - b‖_{5} \\
&= ‖\begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix} - \begin{bmatrix}4 \\ 0 \\ 6\end{bmatrix}‖_{5} \\
&= (\begin{vmatrix}1 - 4 & ^{5} + & 2 - 0 & ^{5} + & 3 - 6\end{vmatrix}^{5})^{1/5} \\
&= (\begin{vmatrix}-3 & ^{5} + 2^{5} + & -3\end{vmatrix}^{5})^{1/5} \\
&= (243 + 32 + 243)^{1/5} \\
&= 518^{1/5} \\
&\approx 3.490
\end{aligned}
$$

Now, let's try a few examples to build intuition.

---

<a id="computing-the-minkowski-distance"></a>
## Computing the Minkowski Distance

**Example:** Suppose $\mathbf a=[6,\, -3,\, 2]^T$, and $\mathbf b=[7,\, -1,\, 1]^T$, and $p=4$. Find the Minkowski distance $d_p$ between $\mathbf a$ and $\mathbf b$.

**Explanation**

Recall that the Minkowski distance is defined as

$$
d_p(\mathbf a,\mathbf b) =\|\mathbf a-\mathbf b\|_p
$$

where for an $N$-dimensional vector $\mathbf v$,

$$
\|\mathbf v\|_p = \left(\sum_{i=1}^N \mid v_i \mid ^p \right)^{1/p}
$$

Therefore, we have

$$
\begin{aligned}
d_{4}(a, b) &= ‖a - b‖_{4} \\
&= ‖\begin{bmatrix}6 \\ -3 \\ 2\end{bmatrix} - \begin{bmatrix}7 \\ -1 \\ 1\end{bmatrix}‖_{4} \\
&= (\begin{vmatrix}6 - 7 & ^{4} + & -3 - (-1) & ^{4} + & 2 - 1\end{vmatrix}^{4})^{1/4} \\
&= 18^{1/4} \\
&\approx 2.060
\end{aligned}
$$

rounded to $3$ decimal places.

---

**Question 5**

Suppose $a = [1,-1]^{T}$, and $b = [- 2, 1]^{T}$, and $p = 3$. Find the Minkowski distance between $a$ and $b$.Rounded to three decimal places, $d_{p}(a, b) \approx$
$\underline{\hspace{1.5cm}}$.

---

**Question 6**

Suppose $a = [1, 2, 3]^{T}$, and $b = [0, 1, 2]^{T}$, and $p = 3$. Find the Minkowski distance between $a$ and $b$.Rounded to three decimal places, $d_{p}(a, b) \approx$
$\underline{\hspace{1.5cm}}$.

---

<a id="the-chebyshev-distance"></a>
## The Chebyshev Distance

Now that we’ve learned how the Minkowski norm depends on the parameter $p$, let’s see what happens when we keep increasing $p$.

Recall that the Minkowski norm of a vector $\mathbf{v} \in \mathbb{R}^N$ is defined as

$$
\| \mathbf{v} \|_p = \left(\sum_{i=1}^N \mid v_i \mid ^p \right)^{1/p}
$$

Let’s explore how this behaves using a concrete example. Consider the vector

$$
\mathbf{v} = [1,\ 2,\ 3]^T
$$

.

Now compute its Minkowski norms for increasing values of $p$:

$$
\begin{aligned}
‖v‖_{2} &= (1^{2} + 2^{2} + 3^{2})^{1/2} = (14)^{1/2} \approx 3.742 \\
‖v‖_{5} &= (1^{5} + 2^{5} + 3^{5})^{1/5} = 276^{1/5} \approx 3.077 \\
‖v‖_{10} &= (1^{10} + 2^{10} + 3^{10})^{1/10} = 60074^{1/10} \approx 3.005 \\
‖v‖_{100} &= (1^{100} + 2^{100} + 3^{100})^{1/100} \approx 3
\end{aligned}
$$

As $p$ increases, the smaller components contribute less and less to the overall value. The norm becomes increasingly influenced by the largest coordinate, in this case, $3$.

Let’s now understand why this happens more formally. Suppose $\mathbf{v}$ is a nonzero vector, and that one of its entries is strictly larger than all the others. Let $j$ be the index where $\begin{vmatrix}v_j & > & v_i\end{vmatrix}$ for every

$$
i \ne j
$$

.

Our goal is to understand the behavior of the norm as $p \to \infty{:}$

$$
\lim_{p \to \infty} \|\mathbf{v}\|_p = \lim_{p \to \infty} \left(\sum_{i=1}^N \mid v_i \mid ^p \right)^{1/p}
$$

To simplify this expression, we factor out $\mid v_j \mid ^p$ from the sum inside

$$
\lim_{p \to \infty} \\begin{vmatrix}\mathbf{v}\|_p = \lim_{p \to \infty} & v_j & \left(1 + \sum_{i \ne j} \left(\frac{\begin{vmatrix}v_i &}{\end{vmatrix}v_j\end{vmatrix}} \right)^p \right)^{1/p}
$$

Since $\mid v_j \mid$ is the largest component, each ratio

$$
\dfrac{\begin{vmatrix}v_i &}{\mid v_j\end{vmatrix}}
$$

is a number less than $1$. When we raise numbers less than $1$ to larger and larger powers, they shrink toward zero.

That leaves us with

$$
\lim_{p \to \infty} \\begin{vmatrix}\mathbf{v}\|_p = & v_j & \cdot 1 = & v_j\end{vmatrix}
$$

This motivates the definition of the **Chebyshev norm** (also called the **max norm** or $L^\infty$ norm) as

$$
\|\mathbf{v}\|_\infty = \max\left(\begin{vmatrix}v_1 &,\ & v_2 &,\ \ldots,\ & v_N\end{vmatrix} \right)
$$

It simply picks out the largest absolute value among the components of $\mathbf{v}$.

Using this norm, we define the **Chebyshev distance** between two points $\mathbf{a}$ and $\mathbf{b}$ as

$$
d_\infty(\mathbf{a}, \mathbf{b}) = \max\left(\begin{vmatrix}a_1 - b_1 &,\ & a_2 - b_2 &,\ \ldots,\ & a_N - b_N\end{vmatrix} \right)
$$

This distance reflects the largest difference across coordinates. It’s especially useful in situations where we care about the worst-case difference in any direction.

Let’s work through a few examples to see how this plays out in practice.

---

<a id="computing-the-chebyshev-distance"></a>
## Computing the Chebyshev Distance

**Example:** Suppose $\mathbf a=[-2,\, 6,\, -7]^T$ and $\mathbf b=[8,\, -9,\, -9]^T$. Find the Chebyshev distance $d_\infty$ between $\mathbf a$ and $\mathbf b$.

**Explanation**

Recall that the Chebyshev distance is defined as

$$
d_\infty(\mathbf a,\mathbf b) =\|\mathbf a-\mathbf b\|_\infty
$$

where for an $N$-dimensional vector $\mathbf v$,

$$
\|\mathbf v\|_\infty = \operatorname{max}(\begin{vmatrix}v_1 &, & v_2 &,\ldots, & v_N\end{vmatrix})
$$

Therefore, we have

$$
\begin{aligned}
d_{∞}(a, b) &= ‖a - b‖_{∞} \\
&= ‖\begin{bmatrix}-2 \\ 6 \\ -7\end{bmatrix} - \begin{bmatrix}8 \\ -9 \\ -9\end{bmatrix}‖_{∞} \\
&= \max(\begin{vmatrix}-2 - 8 &, & 6 - (-9) &, & -7 - (-9)\end{vmatrix}) \\
&= \max(10, 15, 2) \\
&= 15
\end{aligned}
$$

---

**Question 7**

Suppose $a = [1,-2]^{T}$ and $b = [- 7, 8]^{T}$. Find the Chebyshev distance between $a$ and $b$.$d_{∞}(a, b) =$
$\underline{\hspace{1.5cm}}$

---

**Question 8**

Suppose $a = [8, 4,-3]^{T}$ and $b = [- 4, 4,-1]^{T}$. Find the Chebyshev distance between $a$ and $b$.$d_{∞}(a, b) =$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
