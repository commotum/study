# Orthogonal Linear Transformations

<!--
lesson-id: 4319
topic-code: MML.6.2.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Applying the Angle Preserving Property of Orthogonal Linear Transformations](#applying-the-angle-preserving-property-of-orthogonal-linear-transformations)
- [Identifying the Orthogonality of a Two-Vector Set](#identifying-the-orthogonality-of-a-two-vector-set)

## Prerequisites

- [Orthogonal Matrices](<6.2.5. Orthogonal Matrices.md>)

---

<a id="introduction"></a>
## Introduction

A linear transformation $\mathbf{Q}:\mathbb{R}^n \to \mathbb{R}^n$ is an **orthogonal linear transformation** if for any $\mathbf{x}, \mathbf{y} \in \mathbb{R}^n$, we have that

$$
\mathbf{Q}(\mathbf{x}) \cdot \mathbf{Q}(\mathbf{y}) = \mathbf{x} \cdot \mathbf{y}
$$

In this case, we say that $\mathbf{Q}$ *preserves the dot product*.

The linear transformation $\mathbf{Q}$ being orthogonal is equivalent to the following:

- The matrix $Q$ of transformation $\mathbf{Q}$ relative to any orthonormal basis (including the standard basis) of $\mathbb{R}^n$ is orthogonal.
- $\| \mathbf{Q}(\mathbf{x}) \| = \| \mathbf{x} \|$. In other words, $\mathbf{Q}$ preserves vector norms.
- If $\mathbf{x} \perp \mathbf{y}$ then $\mathbf{Q}(\mathbf{x}) \perp \mathbf{Q}(\mathbf{y})$ for any $\mathbf{x}, \mathbf{y} \in \mathbb{R}^n$. In general, $\mathbf{Q}$ preserves the angle between any two vectors.
- $\mathbf{Q}$ maps any orthogonal (orthonormal) set into an orthogonal (orthonormal) set.

---

<a id="applying-the-angle-preserving-property-of-orthogonal-linear-transformations"></a>
## Applying the Angle Preserving Property of Orthogonal Linear Transformations

**Example:** Let $\mathbf{Q}$ be an orthogonal linear transformation. If $\begin{bmatrix}-1 \\ -2 \\ 2\end{bmatrix}$ and $\begin{bmatrix}3 \\ 0 \\ 5\end{bmatrix}$, find

1. $\Vert \mathbf{Q}(\mathbf{x}) \Vert$.
2. $\mathbf{Q}(\mathbf{x}) \cdot \mathbf{Q}(\mathbf{y})$.

**Explanation**

Since $\mathbf{Q}$ is orthogonal, we have that

$$
\mathbf{Q}(\mathbf{x}) \cdot \mathbf{Q}(\mathbf{y}) = \mathbf{x} \cdot \mathbf{y}
$$

for any vectors $\mathbf{x}$ and $\mathbf{y}$.

The linear transformation $\mathbf{Q}$ being orthogonal is equivalent to the following:

- The matrix $Q$ of transformation $\mathbf{Q}$ relative to any orthonormal basis (including the standard basis) of $\mathbb{R}^n$ is orthogonal.
- $\mid \mid \mathbf{Q}(\mathbf{x}) \mid \mid = \mid \mid \mathbf{x} \mid \mid$.
- If $\mathbf{x} \perp \mathbf{y}$ then $\mathbf{Q}(\mathbf{x}) \perp \mathbf{Q}(\mathbf{y})$ for any $\mathbf{x}, \mathbf{y} \in \mathbb{R}^n$. In general, $\mathbf{Q}$ preserves the angle between any two vectors.
- $\mathbf{Q}$ maps any orthogonal (orthonormal) set into an orthogonal (orthonormal) set.

In this case, since $\mathbf{Q}$ is orthogonal, we have

$$
\begin{aligned}
‖Q(x)‖ &= ‖x‖ \\
&= \sqrt{x_{1}^{2} + x_{2}^{2} + x_{3}^{2}} \\
&= \sqrt{(-1)^{2} + (-2)^{2} + 2^{2}} \\
&= 3
\end{aligned}
$$

Also,

$$
\mathbf{Q}(\mathbf{x}) \cdot \mathbf{Q}(\mathbf{y}) = \mathbf{x} \cdot \mathbf{y}
$$

As a result,

$$
\begin{aligned}
Q(x) \cdot Q(y) &= x \cdot y \\
&= (-1) \cdot 3 + (-2) \cdot 0 + 2 \cdot 5 \\
&= 7
\end{aligned}
$$

---

**Question 1:** Let $Q$ be an orthogonal linear transformation. Find $‖Q(x)‖$, where $x = \begin{bmatrix}2 \\ 4 \\ \sqrt{5}\end{bmatrix}$.

- [ ] A. $4$
- [ ] B. $5$
- [ ] C. $3$
- [ ] D. $5\sqrt{2}$
- [ ] E. $2\sqrt{5}$

---

**Question 2:** Let $Q$ be an orthogonal linear transformation. Find $Q(x) \cdot Q(y)$, where $x = \begin{bmatrix}1 \\ -1 \\ 1\end{bmatrix}$ and $y = \begin{bmatrix}-2 \\ 1 \\ -2\end{bmatrix}$.

- [ ] A. $-5$
- [ ] B. $-3$
- [ ] C. $6$
- [ ] D. $5$
- [ ] E. $-4$

---

<a id="identifying-the-orthogonality-of-a-two-vector-set"></a>
## Identifying the Orthogonality of a Two-Vector Set

**Example:** $\begin{bmatrix}\frac{2\sqrt{2}}{3} \\ \frac{1}{3}\end{bmatrix}$

Consider the vectors above, and let $\mathbf{Q}$ be an orthogonal linear transformation. Which of the following statements are true?

1. $\mathbf{u}_1 \cdot \mathbf{u}_2 = 0$
2. $\{\mathbf{Q}(\mathbf{u}_1),\mathbf{Q}(\mathbf{u}_2)\}$ is orthogonal
3. $\{\mathbf{Q}(\mathbf{u}_1),\mathbf{Q}(\mathbf{u}_2)\}$ is orthonormal

**Explanation**

Since $\mathbf{Q}$ is orthogonal, we have that

$$
\mathbf{Q}(\mathbf{x}) \cdot \mathbf{Q}(\mathbf{y}) = \mathbf{x} \cdot \mathbf{y}
$$

for any vectors $\mathbf{x}$ and $\mathbf{y}$.

The linear transformation $\mathbf{Q}$ being orthogonal is equivalent to the following:

- The matrix $Q$ of transformation $\mathbf{Q}$ relative to any orthonormal basis (including the standard basis) of $\mathbb{R}^n$ is orthogonal.
- $\mid \mid \mathbf{Q}(\mathbf{x}) \mid \mid = \mid \mid \mathbf{x} \mid \mid$.
- If $\mathbf{x} \perp \mathbf{y}$ then $\mathbf{Q}(\mathbf{x}) \perp \mathbf{Q}(\mathbf{y})$ for any $\mathbf{x}, \mathbf{y} \in \mathbb{R}^n$. In general, $\mathbf{Q}$ preserves the angle between any two vectors.
- $\mathbf{Q}$ maps any orthogonal (orthonormal) set into an orthogonal (orthonormal) set.

With that in mind, let's examine our statements.

- Statement I is true. Indeed,
$\begin{bmatrix}\frac{2\sqrt{2}}{3} \\ \frac{1}{3}\end{bmatrix}$
- Statement II is true. Indeed, $\mathbf{Q}(\mathbf{u}_1) \perp \mathbf{Q}(\mathbf{u}_2)$ since
$\mathbf{Q}(\mathbf{u}_1)\cdot\mathbf{Q}(\mathbf{u}_2) = \mathbf{u}_1\cdot\mathbf{u}_2 = 0$.
- Statement III is true. Indeed, both $\mathbf{Q}(\mathbf{u}_1)$ and $\mathbf{Q}(\mathbf{u}_2)$ are unit vectors:
$‖Q(u_{1})‖|= ‖u_{1}‖ = \sqrt{(\frac{2\sqrt{2}}{3})^{2} + (\frac{1}{3})^{2}} = 1; ‖Q(u_{2})‖|= ‖u_{2}‖ = \sqrt{(-\frac{1}{3})^{2} + (\frac{2\sqrt{2}}{3})^{2}} = 1$

Therefore, the correct answer is "I, II, and III."

---

**Question 3**

$u_{1} = \begin{bmatrix}9 \\ 6\end{bmatrix}, u_{2} = \begin{bmatrix}2 \\ -3\end{bmatrix}$

Consider the vectors above, and let $Q$ be an orthogonal linear transformation. Which of the following statements are true?

1. $u_{1} \cdot u_{2} = 0$
2. ${Q(u_{1}), Q(u_{2})}$ is orthogonal
3. ${Q(u_{1}), Q(u_{2})}$ is orthonormal

- [ ] A. None of the statements are true.
- [ ] B. I only
- [ ] C. I and III only
- [ ] D. I, II, and III
- [ ] E. I and II only

---

**Question 4**

$u_{1} = \begin{bmatrix}-\frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}}\end{bmatrix}, u_{2} = \begin{bmatrix}\frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}}\end{bmatrix}$

Consider the vectors above, and let $Q$ be an orthogonal linear transformation. Which of the following statements are true?

1. $u_{1} \cdot u_{2} = 0$
2. ${Q(u_{1}), Q(u_{2})}$ is orthogonal
3. ${Q(u_{1}), Q(u_{2})}$ is orthonormal

- [ ] A. I only
- [ ] B. None of the statements are true.
- [ ] C. II and III only
- [ ] D. I, II, and III
- [ ] E. I and II only

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
