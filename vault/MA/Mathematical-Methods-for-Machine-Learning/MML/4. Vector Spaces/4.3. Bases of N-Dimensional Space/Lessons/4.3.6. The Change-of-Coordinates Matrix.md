# The Change-of-Coordinates Matrix

<!--
lesson-id: 1928
topic-code: MML.4.3.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Change-of-Coordinates Matrix Given a Linear Connection Between Two Bases](#finding-the-change-of-coordinates-matrix-given-a-linear-connection-between-two-bases)
- [Finding the Change-of-Coordinates Matrix When One of the Bases is the Standard Basis](#finding-the-change-of-coordinates-matrix-when-one-of-the-bases-is-the-standard-basis)
- [The Inverse of a Change-of-Coordinates Matrix](#the-inverse-of-a-change-of-coordinates-matrix)
- [Using the Invertibility Property of the Change-of-Coordinates Matrix](#using-the-invertibility-property-of-the-change-of-coordinates-matrix)
- [Computing the Change-of-Coordinates Matrix Using Row-Reduction](#computing-the-change-of-coordinates-matrix-using-row-reduction)
- [Finding the Change-of-Coordinates Matrix Between Two Bases](#finding-the-change-of-coordinates-matrix-between-two-bases)

## Prerequisites

- [Inverses of 2x2 Matrices](<../../../../PCL/6. Matrices/6.4. The Inverse of a Matrix/Lessons/6.4.2. Inverses of 2x2 Matrices.md>)
- [Multiplying a Matrix by a Column Vector](<../../../../PCL/6. Matrices/6.2. Matrix Multiplication/Lessons/6.2.1. Multiplying a Matrix by a Column Vector.md>)
- [Finding the Inverse of a 2x2 Matrix Using Row Operations](<../../../3. Vectors and Matrices/3.4. The Inverse of a Matrix/Lessons/3.4.1. Finding the Inverse of a 2x2 Matrix Using Row Operations.md>)
- [Writing Vectors in Different Bases](<4.3.5. Writing Vectors in Different Bases.md>)

---

<a id="introduction"></a>
## Introduction

Given two bases $\color{blue}\mathcal{B}$ and $\color{red}\mathcal{C}$ of a vector space $V$, the **change-of-coordinates matrix from $\color{blue}\mathcal{B}$ to $\color{red}\mathcal{C}$** is the matrix $P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}}$, whose columns are the coordinates of the vectors from $\color{blue}\mathcal{B}$ relative to the basis $\color{red}\mathcal{C}$.

In expanded form, the matrix looks as follows:
$\mid \mid \mid \mid \mid; [b_{1}]_{C}\begin{vmatrix}[b_{2}]_{C} & …\end{vmatrix}[b_{n}]_{C}; \mid \mid \mid \mid \mid$

For example, let's consider two bases $\mathcal B$ and $\mathcal C$ of $\mathbb{R}^2$ given by

$$
\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2\}, \qquad\mathcal{C}=\{\mathbf{c}_1,\mathbf{c}_2\}
$$

If we happen to know that

$$
\begin{bmatrix}-12 \\ 11\end{bmatrix}
$$

then the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$ is given by

$$
P_{B → C} = \mid \begin{bmatrix}-12 & 3 \\ 11 & 6\end{bmatrix}.; ↑_([b_{1}]_{C})↑_([b_{2}]_{C})
$$

The beauty of the change-of-coordinates matrix is that it allows us to express the vector $[\mathbf x]_{\mathcal B}$ in the basis $\mathcal C$ via the transformation

$$
[\mathbf x]_{\mathcal C} = P_{\small\mathcal{B}\to\mathcal{C}} \cdot [\mathbf x]_{\mathcal B}
$$

This can be visualized as shown below.

![](<../Source/4.3.6. The Change-of-Coordinates Matrix/Images/dd5edf0aea2275637a6fcc193284cb56.png>)

---

<a id="finding-the-change-of-coordinates-matrix-given-a-linear-connection-between-two-bases"></a>
## Finding the Change-of-Coordinates Matrix Given a Linear Connection Between Two Bases

**Example:** Let $\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2\}$ and $\mathcal{C}=\{\mathbf{c}_1,\mathbf{c}_2\}$ be bases of $\mathbb{R}^2$. If $\mathbf{c}_1=\mathbf{b}_2-4\mathbf{b}_1$ and $\mathbf{c}_2=5\mathbf{b}_1+7\mathbf{b}_2$, find the change-of-coordinates matrix $P_{\small\mathcal{C}\to\mathcal{B}}$ from $\mathcal{C}$ to $\mathcal{B}$.

**Explanation**

The coordinates of the vectors in $\mathcal{C}$ relative to the basis $\mathcal{B}$ are
$\begin{bmatrix}-4 \\ 1\end{bmatrix}$

The change-of-coordinates matrix from $\mathcal{C}$ to $\mathcal{B}$ is a matrix whose respective columns are the coordinates of the vectors of $\mathcal{C}$ relative to the basis $\mathcal{B}$.

Therefore, we obtain
$\begin{bmatrix}-4 & 5 \\ 1 & 7\end{bmatrix}$

---

**Question 1**

Let $B = {b_{1}, b_{2}, b_{3}}$ and $C = {c_{1}, c_{2}, c_{3}}$ be bases of $R^{3}$.
What are the values of $a, b$, and $c$ if

$b_{1}|= c_{1} + 2c_{2} - c_{3},; b_{2}|= 5c_{3} + 2c_{2},; b_{3}|= c_{2} - c_{1}$,

and the change-of-coordinates matrix from $B$ to $C$ is

$P_{B → C} = \begin{bmatrix}1 & 0 & -1 \\ a & b & 1 \\ -1 & c & 0\end{bmatrix}$?

- [ ] A. $a = 1$, $b = 2$, and $c = 0$
- [ ] B. $a = 2$, $b = 2$, and $c = 5$
- [ ] C. $a = 2$, $b = 0$, and $c =-1$
- [ ] D. $a = 0$, $b = 2$, and $c =-1$
- [ ] E. $a = 1$, $b =-2$, and $c = 5$

---

**Question 2**

Let $B = {b_{1}, b_{2}}$ and $C = {c_{1}, c_{2}}$ be bases of $R^{2}$.
Find the change-of-coordinates matrix $P_{B → C}$ from $B$ to $C$ given that

$[b_{1}]_{C} = \begin{bmatrix}1 \\ -3\end{bmatrix}, [b_{2}]_{C} = \begin{bmatrix}0 \\ -2\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}-3 & -2 \\ 1 & 0\end{bmatrix}$
- [ ] B. $\begin{bmatrix}0 & -2 \\ 1 & -3\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & -2 \\ -3 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & 0 \\ -3 & -2\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & -3 \\ 0 & -2\end{bmatrix}$

---

<a id="finding-the-change-of-coordinates-matrix-when-one-of-the-bases-is-the-standard-basis"></a>
## Finding the Change-of-Coordinates Matrix When One of the Bases is the Standard Basis

**Example:** Let $\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2,\mathbf{b}_3\}$ be a basis of $\mathbb{R}^3$ and let $\mathcal{S}$ be the standard basis of $\mathbb{R}^3$. Find the values of $a,b$, and $c$ if

$\begin{bmatrix}2 \\ -3 \\ 10\end{bmatrix}$

and the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{S}$ is

$\begin{bmatrix}2 & a & 2 \\ -3 & 7 & b \\ c & -4 & -1\end{bmatrix}$

**Explanation**

Recall that the standard basis of $\mathbb{R}^3$ consists of the vectors $\mathbf e_1, \mathbf e_2$, and $\mathbf e_3$, given by
$\begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix}$

The components of the vectors in $\mathcal{B}$ are just the coordinates relative to the standard basis:

$$
\begin{bmatrix}2 \\ -3 \\ 10\end{bmatrix}
$$

The change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{S}$ is a matrix whose respective columns are the coordinates of the vectors of $\mathcal{B}$ relative to the basis $\mathcal{S}$.

Therefore, we obtain
$\begin{bmatrix}2 & 0 & 2 \\ -3 & 7 & 0 \\ 10 & -4 & -1\end{bmatrix}$

Finally, $a=0$, $b=0$, and $c=10$.

---

**Question 3**

Let $B = {b_{1}, b_{2}, b_{3}}$ be a basis of $R^{3}$ and let $S$ be the standard basis of $R^{3}$. What are the values of $a, b$, and $c$ if

$b_{1} = \begin{bmatrix}-3 \\ 4 \\ 0\end{bmatrix}, b_{2} = \begin{bmatrix}-5 \\ -1 \\ 0\end{bmatrix}, b_{3} = \begin{bmatrix}0 \\ 0 \\ 1\end{bmatrix}$,

and the change-of-coordinates matrix from $B$ to $S$ is

$P_{B → S} = \begin{bmatrix}a & -5 & 0 \\ 4 & b & 0 \\ 0 & 0 & c\end{bmatrix}$.

- [ ] A. $a =-3$, $b =-1$, and $c = 1$
- [ ] B. $a =-5$, $b = 4$, and $c = 1$
- [ ] C. $a = 0$, $b = 0$, and $c = 1$
- [ ] D. $a =-3$, $b = 0$, and $c = 4$
- [ ] E. $a =-3$, $b = 0$, and $c = 0$

---

**Question 4:** Let $B = {b_{1}, b_{2}}$ be a basis of $R^{2}$ and let $S$ be the standard basis of $R^{2}$.
Find the change-of-coordinates matrix $P_{B → S}$ from $B$ to $S$ if
$b_{1} = \begin{bmatrix}1 \\ -2\end{bmatrix}, b_{2} = \begin{bmatrix}1 \\ -3\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}1 & 1 \\ -3 & -2\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-2 & 1 \\ 1 & -3\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 1 \\ -2 & -3\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-2 & -3 \\ 1 & 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-3 & 1 \\ 2 & 1\end{bmatrix}$

---

<a id="the-inverse-of-a-change-of-coordinates-matrix"></a>
## The Inverse of a Change-of-Coordinates Matrix

Since the columns of a change-of-coordinates matrix are linearly independent, the matrix must be invertible.

In fact, the change-of-coordinates matrix from $\color{blue}\mathcal{B}$ to $\color{red}\mathcal{C}$ and the change-of-coordinates matrix from $\color{red}\mathcal{C}$ to $\color{blue}\mathcal{B}$ are the inverses of each other:

$$
P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}}^{-1} = P_{\small{\color{red}\mathcal{C}}\to{\color{blue}\mathcal{B}}} \qquad\textrm{and}\qquad P_{\small{\color{red}\mathcal{C}}\to{\color{blue}\mathcal{B}}}^{-1} = P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}}
$$

---

<a id="using-the-invertibility-property-of-the-change-of-coordinates-matrix"></a>
## Using the Invertibility Property of the Change-of-Coordinates Matrix

**Example:** The change-of-coordinates matrix from the basis $\mathcal{B}$ to the basis $\mathcal{C}$ is given by
$\begin{bmatrix}-4 & -6 \\ 3 & 5\end{bmatrix}$
Find the change-of-coordinates matrix from $\mathcal{C}$ to $\mathcal{B}$.

**Explanation**

The change-of-coordinates matrix from $\mathcal{C}$ to $\mathcal{B}$ is the inverse of the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$. Therefore, we simply need to find the inverse of the given matrix.

Computing the inverse of $P_{\small\mathcal{B}\to\mathcal{C}}$, we get

$$
\begin{aligned}
P_{C → B} &= P_{B → C}^{-1} \\
&= \begin{bmatrix}-4 & -6 \\ 3 & 5\end{bmatrix}^{-1} \\
&= (1)/((-4) \cdot 5 - (-6) \cdot 3)\begin{bmatrix}5 & 6 \\ -3 & -4\end{bmatrix} \\
&= (-\frac{1}{2}) \cdot \begin{bmatrix}5 & 6 \\ -3 & -4\end{bmatrix} \\
&= \begin{bmatrix}-\frac{5}{2} & -3 \\ \frac{3}{2} & 2\end{bmatrix}
\end{aligned}
$$

---

**Question 5:** The change-of-coordinates matrix from the basis $B$ to the basis $C$ is given by
$P_{B → C} = \begin{bmatrix}3 & -5 \\ -6 & 9\end{bmatrix}$.
Find the change-of-coordinates matrix from $C$ to $B$.

- [ ] A. $\begin{bmatrix}-3 & -\frac{5}{3} \\ 5 & 2\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-3 & -\frac{5}{3} \\ -2 & -1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}3 & -1 \\ -2 & \frac{5}{3}\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-3 & 1 \\ 2 & -\frac{5}{3}\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-2 & 1 \\ 3 & -\frac{5}{3}\end{bmatrix}$

---

**Question 6:** The change-of-coordinates matrix from the basis $C$ to the basis $B$ is given by
$P_{C → B} = \begin{bmatrix}5 & 7 \\ -3 & -4\end{bmatrix}$. Find the change-of-coordinates matrix from $B$ to $C$.

- [ ] A. $\begin{bmatrix}-4 & 7 \\ 3 & -5\end{bmatrix}$
- [ ] B. $\begin{bmatrix}5 & 3 \\ -7 & -4\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-4 & 3 \\ -7 & 5\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-4 & -7 \\ 3 & 5\end{bmatrix}$
- [ ] E. $\begin{bmatrix}3 & -5 \\ 4 & 7\end{bmatrix}$

---

<a id="computing-the-change-of-coordinates-matrix-using-row-reduction"></a>
## Computing the Change-of-Coordinates Matrix Using Row-Reduction

Suppose that we have bases

$$
\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2 \}
$$

and

$$
\mathcal{C}=\{\mathbf{c}_1,\mathbf{c}_2 \}
$$

and we want to find the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$, denoted $P_{\small\mathcal{B}\to\mathcal{C}}$.

Recall that the columns of $P_{\small\mathcal{B}\to\mathcal{C}}$ are the coordinates of the vectors of $\mathcal{B}$ relative to the basis $\mathcal{C}$, that is
$\mid \mid \mid; [b_{1}]_{C} \mid [b_{2}]_{C}; \mid \mid \mid$

To compute $P_{\small{\color{black}\mathcal{B}}\to{\color{black}\mathcal{C}}}$, we need to find the coordinates of $\mathbf{b}_1$ and $\mathbf{b}_2$ relative to the basis $\mathcal{C}$. We can do this simultaneously by considering the matrix
$\mid \mid \mid \mid \mid \mid \mid; c_{1}\begin{vmatrix}c_{2} & b_{1}\end{vmatrix}b_{2}; \mid \mid \mid \mid \mid \mid \mid$

If we reduce the left-hand side of $M$ to reduced row-echelon form using Gaussian elimination, $P_{\small\mathcal{B}\to\mathcal{C}}$ will emerge on the right-hand side:

$$
I \mid P_{B → C}
$$

---

<a id="finding-the-change-of-coordinates-matrix-between-two-bases"></a>
## Finding the Change-of-Coordinates Matrix Between Two Bases

**Example:** Let $\begin{bmatrix}2 \\ 7\end{bmatrix}$ and $\begin{bmatrix}1 \\ 3\end{bmatrix}$ be bases of $\mathbb{R}^2$. Find the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$.

**Explanation**

Let's denote the vectors of $\mathcal{B}$ and $\mathcal{C}$ as $\{\mathbf{b}_1,\mathbf{b}_2 \}$ and $\{\mathbf{c}_1,\mathbf{c}_2 \}$, respectively.

The change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$ is a matrix whose columns are the coordinates of the vectors of $\mathcal{B}$ relative to the basis $\mathcal{C}$.

So, we need to find the coordinates of $\mathbf{b}_1$ and $\mathbf{b}_2$ relative to the basis $\mathcal{C}$. We can do this simultaneously by considering the matrix
$\mid \mid \mid \mid \mid \mid \mid; c_{1}\begin{vmatrix}c_{2} & b_{1}\end{vmatrix}b_{2}; \mid \mid \mid \mid \mid \mid \mid$

Now, we simply reduce the left-hand side to reduced row-echelon form using Gaussian elimination:

$$
\begin{aligned}
M &= \begin{bmatrix}1 & -2 & 2 & -4 \\ 3 & -5 & 7 & 1\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + (-3)R_{1} \\ ∼\begin{bmatrix}1 & -2 & 2 & -4 \\ 0 & 1 & 1 & 13\end{bmatrix} & R_{1}\end{vmatrix}:=R_{1} + 2R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 4 & 22 \\ 0 & 1 & 1 & 13\end{bmatrix}
\end{aligned}
$$

On the right-hand side, we obtain the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$. Therefore, we have

$$
P_{B → C} = \mid \begin{bmatrix}4 & 22 \\ 1 & 13\end{bmatrix}
$$

---

**Question 7:** Let $B = {\begin{bmatrix}1 \\ 3\end{bmatrix}, \begin{bmatrix}-3 \\ -6\end{bmatrix}}$ be a basis of $R^{2}$ and let $S$ be the standard basis of $R^{2}$. Find the change-of-coordinates matrix from $S$ to $B$.

- [ ] A. $\begin{bmatrix}-2 & 1 \\ -1 & \frac{1}{3}\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-2 & 1 \\ -1 & 3\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-1 & 1 \\ -2 & \frac{1}{3}\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & -2 \\ \frac{1}{3} & -1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & -2 \\ -1 & \frac{1}{3}\end{bmatrix}$

---

**Question 8:** Let $B = {\begin{bmatrix}2 \\ -7\end{bmatrix}, \begin{bmatrix}-3 \\ 2\end{bmatrix}}$ and $C = {\begin{bmatrix}1 \\ -4\end{bmatrix}, \begin{bmatrix}-2 \\ 6\end{bmatrix}}$ be bases of $R^{2}$. Find the change-of-coordinates matrix from $B$ to $C$.

- [ ] A. $\begin{bmatrix}1 & -7 \\ 5 & -\frac{1}{2}\end{bmatrix}$
- [ ] B. $\begin{bmatrix}7 & -1 \\ \frac{1}{2} & -5\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 7 \\ \frac{1}{2} & -5\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & 7 \\ -\frac{1}{2} & 5\end{bmatrix}$
- [ ] E. $\begin{bmatrix}7 & 1 \\ -\frac{1}{2} & 5\end{bmatrix}$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
