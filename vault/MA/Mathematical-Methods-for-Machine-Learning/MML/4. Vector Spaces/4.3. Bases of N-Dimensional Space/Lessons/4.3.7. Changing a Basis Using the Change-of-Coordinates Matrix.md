# Changing a Basis Using the Change-of-Coordinates Matrix

<!--
lesson-id: 1910
topic-code: MML.4.3.7
-->

## Table of Contents

- [Introduction](#introduction)
- [The Change-of-Basis Formula in Expanded Form](#the-change-of-basis-formula-in-expanded-form)
- [Finding the Coordinates of a Vector in a New Basis Given the Change-of-Coordinates Matrix](#finding-the-coordinates-of-a-vector-in-a-new-basis-given-the-change-of-coordinates-matrix)
- [Finding the Coordinates of a Vector in a New Basis Given the Inverse Change-of-Coordinates Matrix](#finding-the-coordinates-of-a-vector-in-a-new-basis-given-the-inverse-change-of-coordinates-matrix)
- [Finding the Coordinates of a Vector in a New Basis Given Its Coordinates in the Standard Basis](#finding-the-coordinates-of-a-vector-in-a-new-basis-given-its-coordinates-in-the-standard-basis)
- [Using Gaussian Elimination to Find the Coordinates of a Vector in a New Basis](#using-gaussian-elimination-to-find-the-coordinates-of-a-vector-in-a-new-basis)
- [Finding the Coordinates of a Vector in a New Basis Given Its Coordinates in Another Basis](#finding-the-coordinates-of-a-vector-in-a-new-basis-given-its-coordinates-in-another-basis)

## Prerequisites

- [The Change-of-Coordinates Matrix](<4.3.6. The Change-of-Coordinates Matrix.md>)

---

<a id="introduction"></a>
## Introduction

Suppose that $\mathbf x \in V$ for some vector space $V$, and that $\mathcal B$ and $\mathcal C$ are two bases of $V$.

To find the coordinates of $\mathbf{x}$ relative to $\mathcal{C}$ given its coordinates relative to $\mathcal B$, we can use the **change-of-basis formula**

$$
[x]_{C} = P_{B → C} \cdot [x]_{B}
$$

where we recall that

- $[\,\mathbf x\,]_{\color{blue}\mathcal{B}}$ and $[\,\mathbf x\,]_{\color{red}\mathcal{C}}$ are the coordinates of $\mathbf{x}$ relative to the bases $\color{blue}\mathcal{B}$ and ${\color{red}\mathcal{C}}$, respectively, and
- $P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}}$ is the change-of-coordinates matrix from $\color{blue}\mathcal{B}$ to ${\color{red}\mathcal{C}}$, whose columns are the coordinates of vectors of the basis $\color{blue}\mathcal{B}$ relative to the basis ${\color{red}\mathcal{C}}$.

As an example, suppose we are given two bases

$$
\mathcal{B}=\left\{\mathbf{b}_1, \mathbf{b}_2 \right\}
$$

and

$$
\mathcal{C}=\left\{\mathbf{c}_1, \mathbf{c}_2 \right\}
$$

of a vector space $V$, and we already know that the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$ is given by
$\begin{bmatrix}-1 & 2 \\ 2 & 1\end{bmatrix}$

Let $\mathbf{v} \in V$ and $\begin{bmatrix}2 \\ 2\end{bmatrix}$ Then we can easily find $[\,\mathbf{v}\,]_{\mathcal{C}}$, as follows:

$$
\begin{aligned}
[v]_{C} &= \begin{bmatrix}-1 & 2 \\ 2 & 1\end{bmatrix}^(⏞)^(P_{B → C}) \cdot \begin{bmatrix}2 \\ 2\end{bmatrix}^(⏞)^([v]_{B}) \\
&= \begin{bmatrix}2 \\ 6\end{bmatrix}
\end{aligned}
$$

---

<a id="the-change-of-basis-formula-in-expanded-form"></a>
## The Change-of-Basis Formula in Expanded Form

We've seen that to find the coordinates of a vector $\mathbf{x}$ relative to a basis $\mathcal{C}$ given its coordinates relative to a basis $\mathcal B$, we can use the formula

$$
[x]_{C} = P_{B → C} \cdot [x]_{B}
$$

In expanded form, the formula looks as follows:
$\mid; [x]_{C}; \mid$

Also, remember that the change-of-coordinates matrix from $\color{blue}\mathcal{B}$ to $\color{red}\mathcal{C}$ and from $\color{red}\mathcal{C}$ to $\color{blue}\mathcal{B}$ are the inverses of each other:

$$
P_{\small{\color{red}\mathcal{C}}\to{\color{blue}\mathcal{B}}} = P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}}^{-1} \quad\textrm{and}\quad P_{\small{\color{blue}\mathcal{B}}\to{\color{red}\mathcal{C}}} = P_{\small{\color{red}\mathcal{C}}\to{\color{blue}\mathcal{B}}}^{-1}
$$

---

<a id="finding-the-coordinates-of-a-vector-in-a-new-basis-given-the-change-of-coordinates-matrix"></a>
## Finding the Coordinates of a Vector in a New Basis Given the Change-of-Coordinates Matrix

**Example:** The change-of-coordinates matrix from the basis $\mathcal{B}$ to the basis $\mathcal{C}$ and the coordinates of the vector $\mathbf{x}$ relative to the basis $\mathcal{B}$ are given below. What is $[\mathbf{x}]_{\mathcal{C}}$?

$\begin{bmatrix}-3 & 6 \\ 5 & -8\end{bmatrix}$

**Explanation**

Given the coordinates of $\mathbf{x}$ relative to $\mathcal{B}$ and the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$, we find the coordinates of $\mathbf{x}$ relative to $\mathcal{C}$ as follows:

$$
\begin{aligned}
[x]_{C} &= P_{B → C}[x]_{B} \\
&= \begin{bmatrix}-3 & 6 \\ 5 & -8\end{bmatrix}\begin{bmatrix}5 \\ 3\end{bmatrix} \\
&= \begin{bmatrix}3 \\ 1\end{bmatrix}
\end{aligned}
$$

---

**Question 1**

$P_{B → C} = \begin{bmatrix}3 & -5 & 1 \\ -6 & 9 & 0 \\ 1 & -1 & 1\end{bmatrix}$

The change-of-coordinates matrix from the basis $B = {b_{1}, b_{2}, b_{3}}$ to the basis $C = {c_{1}, c_{2}, c_{3}}$ is given above. Find $[x]_{C}$ given that $x = 5b_{1} - b_{2}$.

- [ ] A. $\begin{bmatrix}21 \\ -34 \\ -1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}30 \\ -6 \\ 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}20 \\ -39 \\ 6\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-1 \\ 5 \\ 30\end{bmatrix}$
- [ ] E. $\begin{bmatrix}5 \\ -1 \\ 0\end{bmatrix}$

---

**Question 2**

The change-of-coordinates matrix from the basis $B$ to the basis $C$ and the coordinates of the vector $x$ relative to the basis $B$ are given below. What is $[x]_{C}$?

$P_{B → C} = \begin{bmatrix}1 & -2 \\ -4 & 3\end{bmatrix}, [x]_{B} = \begin{bmatrix}2 \\ 4\end{bmatrix}$

- [ ] A. $\begin{bmatrix}8 \\ 5\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-3 \\ -1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-6 \\ 4\end{bmatrix}$
- [ ] D. $\begin{bmatrix}4 \\ -6\end{bmatrix}$
- [ ] E. $\begin{bmatrix}5 \\ -8\end{bmatrix}$

---

<a id="finding-the-coordinates-of-a-vector-in-a-new-basis-given-the-inverse-change-of-coordinates-matrix"></a>
## Finding the Coordinates of a Vector in a New Basis Given the Inverse Change-of-Coordinates Matrix

**Example:** The change-of-coordinates matrix from $\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2 \}$ to $\mathcal{C} = \{\mathbf{c}_1,\mathbf{c}_2 \}$ is given below. Find $[\mathbf{x}]_{\mathcal{B}}$ given that $\mathbf{x} =2\mathbf{c}_1-6\mathbf{c}_2$.

$\begin{bmatrix}-4 & 3 \\ 6 & -4\end{bmatrix}$

**Explanation**

We will use the change-of-coordinates formula in the form

$$
[\,\mathbf{x}\,]_{\mathcal{B}} = P_{\small\mathcal{C}\to\mathcal{B}} [\,\mathbf{x}\,]_{\mathcal{C}}
$$

First, we need to find the change-of-coordinates matrix $P_{\small\mathcal{C}\to\mathcal{B}}$ from $\mathcal{C}$ to $\mathcal{B}$. To do that, we compute the inverse of $P_{\small\mathcal{B}\to\mathcal{C}}$:

$$
\begin{aligned}
P_{C → B} &= P_{B → C}^{-1} \\
&= \begin{bmatrix}-4 & 3 \\ 6 & -4\end{bmatrix}^{-1} \\
&= (1)/((-4)(-4) - 6 \cdot 3)\begin{bmatrix}-4 & -3 \\ -6 & -4\end{bmatrix} \\
&= \begin{bmatrix}2 & \frac{3}{2} \\ 3 & 2\end{bmatrix}
\end{aligned}
$$

Notice that since

$$
\mathbf{x} = 2 \mathbf{c}_1-6\mathbf{c}_2
$$

we obtain
$\begin{bmatrix}2 \\ -6\end{bmatrix}$

Now, we compute $[\,\mathbf{x}\,]_{\mathcal{B}}$:

$$
\begin{aligned}
[x]_{B} &= P_{C → B}[x]_{C} \\
&= \begin{bmatrix}2 & \frac{3}{2} \\ 3 & 2\end{bmatrix}\begin{bmatrix}2 \\ -6\end{bmatrix} \\
&= \begin{bmatrix}-5 \\ -6\end{bmatrix}
\end{aligned}
$$

---

**Question 3**

The change-of-coordinates matrix from $B$ to $C$ and the coordinates of the vector $x$ relative to the basis $C$ are given below. What is $[x]_{B}$?

$P_{B → C} = \begin{bmatrix}5 & -3 \\ -3 & 2\end{bmatrix}, [x]_{C} = \begin{bmatrix}5 \\ 4\end{bmatrix}$

- [ ] A. $\begin{bmatrix}22 \\ 35\end{bmatrix}$
- [ ] B. $\begin{bmatrix}12 \\ 27\end{bmatrix}$
- [ ] C. $\begin{bmatrix}7 \\ 22\end{bmatrix}$
- [ ] D. $\begin{bmatrix}24 \\ 30\end{bmatrix}$
- [ ] E. $\begin{bmatrix}2 \\ 7\end{bmatrix}$

---

**Question 4**

The change-of-coordinates matrix from $B = {b_{1}, b_{2}}$ to $C = {c_{1}, c_{2}}$ is given below. Find $[x]_{B}$ given that $x =-3c_{1} - 2c_{2}$.

$P_{B → C} = \begin{bmatrix}1 & 3 \\ -2 & -4\end{bmatrix}$

- [ ] A. $\begin{bmatrix}1 \\ -2\end{bmatrix}$
- [ ] B. $\begin{bmatrix}8 \\ -\frac{7}{2}\end{bmatrix}$
- [ ] C. $\begin{bmatrix}8 \\ -4\end{bmatrix}$
- [ ] D. $\begin{bmatrix}2 \\ -\frac{7}{2}\end{bmatrix}$
- [ ] E. $\begin{bmatrix}9 \\ -4\end{bmatrix}$

---

<a id="finding-the-coordinates-of-a-vector-in-a-new-basis-given-its-coordinates-in-the-standard-basis"></a>
## Finding the Coordinates of a Vector in a New Basis Given Its Coordinates in the Standard Basis

**Example:** The basis $\mathcal{B}$ of $\mathbb{R}^2$ and the coordinates of the vector $\mathbf{x}$ relative to the standard basis $\mathcal{S}$ are given below. Find $[\mathbf{x}]_{\mathcal{B}}$.

$\begin{bmatrix}1 \\ 3\end{bmatrix}$

**Explanation**

We will use the change-of-coordinates formula in the form

$$
[\,\mathbf{x}\,]_{\mathcal{B}} = P_{\small\mathcal{S}\to\mathcal{B}} [\,\mathbf{x}\,]_{\mathcal{S}}
$$

First, recall that the change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{S}$ is the matrix whose columns are the vectors of $\mathcal{B}$ relative to the standard basis $\mathcal{S}$. As a result, we have

$$
\begin{bmatrix}1 & 3 \\ 3 & 10\end{bmatrix}
$$

Now, we need to find the change-of-coordinates matrix $P_{\small\mathcal{S}\to\mathcal{B}}$ from $\mathcal{S}$ to $\mathcal{B}$. To do that, we compute the inverse of $P_{\small\mathcal{B}\to\mathcal{S}}$:

$$
\begin{aligned}
P_{S → B} &= P_{B → S}^{-1} \\
&= \begin{bmatrix}1 & 3 \\ 3 & 10\end{bmatrix}^{-1} \\
&= \frac{1}{1 \cdot 10 - 3 \cdot 3}\begin{bmatrix}10 & -3 \\ -3 & 1\end{bmatrix} \\
&= \begin{bmatrix}10 & -3 \\ -3 & 1\end{bmatrix}
\end{aligned}
$$

Finally, we compute $[\,\mathbf{x}\,]_{\mathcal{B}}$:

$$
\begin{aligned}
[x]_{B} &= P_{S → B}[x]_{S} \\
&= \begin{bmatrix}10 & -3 \\ -3 & 1\end{bmatrix}\begin{bmatrix}1 \\ 5\end{bmatrix} \\
&= \begin{bmatrix}-5 \\ 2\end{bmatrix}
\end{aligned}
$$

---

**Question 5**

$B = {\begin{bmatrix}3 \\ -2\end{bmatrix}, \begin{bmatrix}9 \\ -8\end{bmatrix}}, [x]_{S} = \begin{bmatrix}-6 \\ 4\end{bmatrix}$

The basis $B$ of $R^{2}$ and the coordinates of the vector $x$ relative to the standard basis $S$ are given above. Find $[x]_{B}$.

- [ ] A. $\begin{bmatrix}1 \\ -4\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-2 \\ 0\end{bmatrix}$
- [ ] C. $\begin{bmatrix}3 \\ -1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-2 \\ -4\end{bmatrix}$
- [ ] E. $\begin{bmatrix}0 \\ 4\end{bmatrix}$

---

**Question 6**

$B = {\begin{bmatrix}1 \\ 3\end{bmatrix}, \begin{bmatrix}5 \\ 16\end{bmatrix}}, [x]_{S} = \begin{bmatrix}2 \\ 7\end{bmatrix}$

The basis $B$ of $R^{2}$ and the coordinates of the vector $x$ relative to the standard basis $S$ are given above. Find $[x]_{B}$.

- [ ] A. $\begin{bmatrix}-3 \\ 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}3 \\ 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-5 \\ 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}3 \\ 0\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-3 \\ 0\end{bmatrix}$

---

<a id="using-gaussian-elimination-to-find-the-coordinates-of-a-vector-in-a-new-basis"></a>
## Using Gaussian Elimination to Find the Coordinates of a Vector in a New Basis

Let

$$
\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2 \}
$$

and

$$
\mathcal{C}=\{\mathbf{c}_1,\mathbf{c}_2 \}
$$

be bases of $\mathbb{R}^2$. Now, suppose we would like to find the coordinates of $\mathbf{b}_1$ and $\mathbf{b}_2$ relative to the basis $\mathcal{C}$.

- We first need to find $x_1$ and $x_2$ such that
$x_1 \mathbf{c}_1+x_2 \mathbf{c}_2 = \mathbf{b}_1$,
which is equivalent to solving the system with the augmented matrix
$\mid \mid \mid \mid \mid; c_{1} \mid c_{2} \mid b_{1}; \mid \mid \mid \mid \mid$
- Similarly, we need to find $y_1$ and $y_2$ such that
$y_1 \mathbf{c}_1+y_2 \mathbf{c}_2 = \mathbf{b}_2$,
which is equivalent to solving the system with the augmented matrix
$\mid \mid \mid \mid \mid; c_{1} \mid c_{2} \mid b_{2}; \mid \mid \mid \mid \mid$

We can solve both systems using Gaussian elimination. However, we can do that simultaneously by considering the matrix
$\mid \mid \mid \mid \mid \mid \mid; c_{1}\begin{vmatrix}c_{2} & b_{1}\end{vmatrix}b_{2}; \mid \mid \mid \mid \mid \mid \mid$

Reducing the left-hand side to reduced row-echelon form using Gaussian elimination, we get a matrix of the form

$$
M \mid ∼\begin{bmatrix}1 & 0 & x_{1} & y_{1} \\ 0 & 1 & x_{2} & y_{2}\end{bmatrix}
$$

where the required coefficients will be stored in the columns on the right-hand side.

---

<a id="finding-the-coordinates-of-a-vector-in-a-new-basis-given-its-coordinates-in-another-basis"></a>
## Finding the Coordinates of a Vector in a New Basis Given Its Coordinates in Another Basis

**Example:** The bases $\mathcal{B}$ and $\mathcal{C}$ of $\mathbb{R}^2$ and the coordinates of the vector $\mathbf{x}$ relative to the basis $\mathcal{B}$ are given below. Find $[\mathbf{x}]_{\mathcal{C}}$.

$\begin{bmatrix}1 \\ 1\end{bmatrix}$

**Explanation**

Let's denote the vectors of $\mathcal{B}$ and $\mathcal{C}$ as $\{\mathbf{b}_1,\mathbf{b}_2 \}$ and $\{\mathbf{c}_1,\mathbf{c}_2 \}$, respectively.

The change-of-coordinates matrix from $\mathcal{B}$ to $\mathcal{C}$ is a matrix whose columns are the vectors of $\mathcal{B}$ relative to the basis $\mathcal{C}$.

So, we need to find the coordinates of $\mathbf{b}_1$ and $\mathbf{b}_2$ relative to the basis $\mathcal{C}$. We can do this simultaneously by considering the matrix
$\mid \mid \mid \mid \mid \mid \mid; c_{1}\begin{vmatrix}c_{2} & b_{1}\end{vmatrix}b_{2}; \mid \mid \mid \mid \mid \mid \mid$

Reducing the left-hand side to reduced row-echelon form using Gaussian elimination, we get

$$
\begin{aligned}
M &= \begin{bmatrix}1 & 2 & 1 & 1 \\ 6 & 13 & 1 & -1\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + (-6)R_{1} \\ ∼\begin{bmatrix}1 & 2 & 1 & 1 \\ 0 & 1 & -5 & -7\end{bmatrix} & R_{1}\end{vmatrix}:=R_{1} + (-2)R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 11 & 15 \\ 0 & 1 & -5 & -7\end{bmatrix}
\end{aligned}
$$

Therefore, the change-of-coordinates matrix $P_{\small\mathcal{B}\to\mathcal{C}}$ is
$\begin{bmatrix}11 & 15 \\ -5 & -7\end{bmatrix}$

Finally, we compute $[\,\mathbf{x}\,]_{\mathcal{C}}$:

$$
\begin{aligned}
[x]_{C} &= P_{B → C}[x]_{B} \\
&= \begin{bmatrix}11 & 15 \\ -5 & -7\end{bmatrix}\begin{bmatrix}-2 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}-7 \\ 3\end{bmatrix}
\end{aligned}
$$

---

**Question 7**

$B = {\begin{bmatrix}1 \\ 2\end{bmatrix}, \begin{bmatrix}3 \\ 7\end{bmatrix}}, C = {\begin{bmatrix}1 \\ 2\end{bmatrix}, \begin{bmatrix}2 \\ 0\end{bmatrix}}, [x]_{C} = \begin{bmatrix}3 \\ -1\end{bmatrix}$

The bases $B$ and $C$ of $R^{2}$ and the coordinates of the vector $x$ relative to the basis $C$ are given above. Find $[x]_{B}$.

- [ ] A. $\begin{bmatrix}14 \\ -8\end{bmatrix}$
- [ ] B. $\begin{bmatrix}12 \\ 6\end{bmatrix}$
- [ ] C. $\begin{bmatrix}15 \\ -4\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-11 \\ 4\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 \\ 0\end{bmatrix}$

---

**Question 8**

$B = {\begin{bmatrix}0 \\ -2\end{bmatrix}, \begin{bmatrix}1 \\ 0\end{bmatrix}}, C = {\begin{bmatrix}1 \\ 3\end{bmatrix}, \begin{bmatrix}1 \\ 4\end{bmatrix}}, [x]_{B} = \begin{bmatrix}6 \\ -9\end{bmatrix}$.

The bases $B$ and $C$ of $R^{2}$ and the coordinates of the vector $x$ relative to the basis $B$ are given above. Find $[x]_{C}$.

- [ ] A. $\begin{bmatrix}24 \\ 15\end{bmatrix}$
- [ ] B. $\begin{bmatrix}14 \\ -10\end{bmatrix}$
- [ ] C. $\begin{bmatrix}2 \\ 0\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-\frac{2}{3} \\ \frac{8}{3}\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-24 \\ 15\end{bmatrix}$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
