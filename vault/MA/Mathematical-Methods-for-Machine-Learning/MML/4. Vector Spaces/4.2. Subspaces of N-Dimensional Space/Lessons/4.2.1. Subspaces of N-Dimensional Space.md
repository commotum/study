# Subspaces of N-Dimensional Space

<!--
lesson-id: 1853
topic-code: MML.4.2.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining Whether a Line Is a Subspace of Two-Dimensional Space](#determining-whether-a-line-is-a-subspace-of-two-dimensional-space)
- [Lines as Subspaces of Three-Dimensional Space](#lines-as-subspaces-of-three-dimensional-space)
- [Determining Whether a Line Is a Subspace of Three-Dimensional Space](#determining-whether-a-line-is-a-subspace-of-three-dimensional-space)
- [Planes as Subspace of Three-Dimensional Space](#planes-as-subspace-of-three-dimensional-space)
- [Determining Whether a Plane Is a Subspace of Three-Dimensional Space](#determining-whether-a-plane-is-a-subspace-of-three-dimensional-space)

## Prerequisites

- [The Cartesian Equation of a Plane](<../../../3. Vectors and Matrices/3.1. Vector Geometry/Lessons/3.1.5. The Cartesian Equation of a Plane.md>)
- [Linear Combinations of Vectors in N-Dimensional Euclidean Space](<../../4.1. Vectors in N-Dimensional Space/Lessons/4.1.2. Linear Combinations of Vectors in N-Dimensional Euclidean Space.md>)
- [Describing Planar Regions Using Set-Builder Notation](<../../../1. Set Theory/1.1. Introduction to Set Theory/Lessons/1.1.6. Describing Planar Regions Using Set-Builder Notation.md>)

---

<a id="introduction"></a>
## Introduction

A **subspace** of $\mathbb{R}^n$ is a subset of $\mathbb R^n$ that is closed under vector addition and scalar multiplication. In other words:

> $H\subseteq \mathbb R^n$ is a subspace of $\mathbb{R}^n$ if and only if it satisfies the following properties:
> - *Closure under vector addition:* $\qquad$ If $\mathbf{u}$ and $\mathbf{v}$ are in $H$, then their sum $\mathbf{u}+\mathbf{v}$ is also in $H$.
> - *Closure under scalar multiplication:* $\qquad$ If $\mathbf{u}$ is in $H$, then $c\cdot \mathbf{u}$ is also in $H$ for any scalar number $c$.

For example, consider the line $L$ with equation $y=3x$ in $\mathbb{R}^2$. Is $L$ a subspace of $\mathbb{R}^2$?

First, notice that we can express the points on $L$ as the following set:

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

In other words, $L$ contains all vectors of the form $\begin{bmatrix}x \\ 3x\end{bmatrix}$ where $x$ is a real number.

To determine whether $L$ is a subspace of $\mathbb R^2$, we need to check the two conditions above.

- Let's pick two arbitrary vectors in $L$, namely, $\begin{bmatrix}u_{1} \\ 3u_{1}\end{bmatrix}$ and $\begin{bmatrix}v_{1} \\ 3v_{1}\end{bmatrix}$ If we compute their sum, we get
$u + v|= \begin{bmatrix}u_{1} \\ 3u_{1}\end{bmatrix} + \begin{bmatrix}v_{1} \\ 3v_{1}\end{bmatrix}; = \begin{bmatrix}u_{1} + v_{1} \\ 3u_{1} + 3v_{1}\end{bmatrix}; = \begin{bmatrix}u_{1} + v_{1} \\ 3 \cdot (u_{1} + v_{1})\end{bmatrix}$,
which satisfies the condition $y=3x$. Therefore, $\mathbf{u}+\mathbf{v} \in L. \quad {\color{green}\checkmark}$
- Let's pick an arbitrary vector in $L$, namely, $\begin{bmatrix}u_{1} \\ 3u_{1}\end{bmatrix}$ Then, for any scalar $c$ we have
$c \cdot u|= c \cdot \begin{bmatrix}u_{1} \\ 3u_{1}\end{bmatrix}; = \begin{bmatrix}cu_{1} \\ 3 \cdot cu_{1}\end{bmatrix}$,
which satisfies the condition $y=3x$. Therefore, $c\cdot \mathbf{u}\in L. \quad {\color{green}\checkmark}$

The set $L$ satisfies both conditions. So, we can conclude that it is a subspace of $\mathbb{R}^2$.

**Note:** Since the second condition must be satisfied for any $c$, including $c=0$, we can conclude that every subspace must contain the zero vector $\mathbf{0}$. So, if a set does not contain $\mathbf{0}$ then it is *not* a subspace of $\mathbb{R}^n$.

For instance, the set

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

does *not* contain the zero vector (since

$$
0 \neq 0 + 1
$$

), and consequently it is *not* a subspace of $\mathbb{R}^2$.

---

<a id="determining-whether-a-line-is-a-subspace-of-two-dimensional-space"></a>
## Determining Whether a Line Is a Subspace of Two-Dimensional Space

**Example:** Let $L$ be the line with equation $y=5+3x$ in $\mathbb{R}^2$. Which of the following statements are true?

1. $L$ contains the zero-vector
2. $L$ is closed under vector addition
3. $L$ is closed under scalar multiplication
4. $L$ is **not** a subspace of $\mathbb{R}^2$

**Explanation**

Recall the following two facts about subspaces:

- A set of vectors in a vector space is a *subspace* if it is closed under vector addition and scalar multiplication.
- If a non-empty set is closed under scalar multiplication, then this set must contain the zero-vector (because $0 \cdot \mathbf{v} = \mathbf{0}$ for any vector $\mathbf{v}$). So, if a set does not contain the zero vector, then this set is not closed under scalar multiplication.

Notice that $L$ contains vectors of the form

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

With that in mind, let's examine our statements.

- Statement I is false. The coordinates of the zero-vector do not satisfy the equation $y=5+3x{:}$
$y|= 5 + 3x; (0)|= 5 + 3(0); 0|= 5\cdot$
- Statement II is false. Let, for example, $\begin{bmatrix}0 \\ 5\end{bmatrix}$ and $\begin{bmatrix}-1 \\ 2\end{bmatrix}$ from $L$. Then,
$a + b|= \begin{bmatrix}0 \\ 5\end{bmatrix} + \begin{bmatrix}-1 \\ 2\end{bmatrix} = \begin{bmatrix}0 + (-1) \\ 5 + 2\end{bmatrix} = \begin{bmatrix}-1 \\ 7\end{bmatrix}$.
However, $\begin{bmatrix}-1 \\ 7\end{bmatrix}$ since substituting the coordinates into the equation $y=5+3x$ gives the following:
$y|= 5 + 3x; 7|= 5 + 3(-1); 7|= 2\cdot$
- Statement III is false. Since $\mathbf{0} \not\in L$, this set is not closed under scalar multiplication.
- Statement IV is true. Since statements II and III are false, $L$ is *not* a subspace of $\mathbb{R}^2$.

Therefore, the correct answer is "IV only."

---

**Question 1**

```quiz
type: radio
id: ma-155956
content: |-
  Let $L$ be the line with equation $x = 2y$ in $R^{2}$. Which of the following statements are true?

  1. $L$ contains the zero-vector
  2. $L$ is closed under vector addition
  3. $L$ is closed under scalar multiplication
  4. $L$ is a subspace of $R^{2}$
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    II and III only
- id: c
  content: |-
    All the statements are true
  correct: true
- id: d
  content: |-
    II only
- id: e
  content: |-
    I only
```

---

**Question 2**

```quiz
type: radio
id: ma-156607
content: |-
  Let $L$ be the line with equation $y = 1 - 2x$ in $R^{2}$. Which of the following statements are true?

  1. $L$ contains the zero-vector
  2. $L$ is closed under vector addition
  3. $L$ is closed under scalar multiplication
  4. $L$ is **not** a subspace of $R^{2}$
options:
- id: a
  content: |-
    II only
- id: b
  content: |-
    III only
- id: c
  content: |-
    I and II only
- id: d
  content: |-
    II and III only
- id: e
  content: |-
    IV only
  correct: true
```

---

<a id="lines-as-subspaces-of-three-dimensional-space"></a>
## Lines as Subspaces of Three-Dimensional Space

Recall that $H$ is a subspace of $\mathbb{R}^n$ if and only if it satisfies the following properties:

> *Closure under vector addition:*
> $\qquad$ If $\mathbf{u}$ and $\mathbf{v}$ are in $H$, then their sum $\mathbf{u}+\mathbf{v}$ is also in $H$.
> *Closure under scalar multiplication:*
> $\qquad$ If $\mathbf{u}$ is in $H$, then $c\cdot \mathbf{u}$ is also in $H$ for any scalar number $c$.

Let $L$ be the line in $\mathbb{R}^3$ that passes through the points $(0,0,0)$ and $(4,1,-2)$. Is $L$ a subspace of $\mathbb{R}^3$?

First, note that the direction vector of the line $L$ is
$\begin{bmatrix}4 \\ 1 \\ -2\end{bmatrix}$
This means that the vector equation of the line is

$$
\begin{aligned}
r &= \begin{bmatrix}0 \\ 0 \\ 0\end{bmatrix} + t\begin{bmatrix}4 \\ 1 \\ -2\end{bmatrix} = \begin{bmatrix}4t \\ t \\ -2t\end{bmatrix}
\end{aligned}
$$

for $t\in\mathbb R$, and the corresponding Cartesian equation is

$$
\dfrac x4=y=\dfrac z{-2}
$$

To determine whether $L$ is a subspace, we need to check the two conditions above:

- Let's pick two arbitrary vectors in $L$, namely, $\begin{bmatrix}4u_{1} \\ u_{1} \\ -2u_{1}\end{bmatrix}$ and $\begin{bmatrix}4v_{1} \\ v_{1} \\ -2v_{1}\end{bmatrix}$ If we compute their sum, we get
$u + v|= \begin{bmatrix}4u_{1} \\ u_{1} \\ -2u_{1}\end{bmatrix} + \begin{bmatrix}4v_{1} \\ v_{1} \\ -2v_{1}\end{bmatrix}; = \begin{bmatrix}4u_{1} + 4v_{1} \\ u_{1} + v_{1} \\ (-2u_{1}) + (-2v_{1})\end{bmatrix}; = \begin{bmatrix}4 \cdot (u_{1} + v_{1}) \\ u_{1} + v_{1} \\ (-2) \cdot (u_{1} + v_{1})\end{bmatrix}$,
which satisfies the condition $\dfrac x4=y=\dfrac z{-2}$. Therefore, $\mathbf{u}+\mathbf{v}\in L. \quad {\color{green}\checkmark}$
- Let's pick an arbitrary vector in $L$, namely, $\begin{bmatrix}4u_{1} \\ u_{1} \\ -2u_{1}\end{bmatrix}$ Then, for any scalar $c$, we have
$c \cdot u|= c \cdot \begin{bmatrix}4u_{1} \\ u_{1} \\ -2u_{1}\end{bmatrix}; = \begin{bmatrix}4 \cdot cu_{1} \\ cu_{1} \\ (-2) \cdot cu_{1}\end{bmatrix}$,
which satisfies the condition $\dfrac x4=y=\dfrac z{-2}$. Therefore, $c\cdot \mathbf{u}\in L. \quad {\color{green}\checkmark}$

The set $L$ satisfies both conditions. So, we can conclude that it is a subspace of $\mathbb{R}^3$.

---

<a id="determining-whether-a-line-is-a-subspace-of-three-dimensional-space"></a>
## Determining Whether a Line Is a Subspace of Three-Dimensional Space

**Example:** Let $L$ be the line in $\mathbb{R}^3$ that passes through the points $(1,2,2)$ and $(2,1,1)$. Which of the following statements are true?

1. $L$ contains the zero-vector
2. $L$ is closed under vector addition
3. $L$ is closed under scalar multiplication
4. $L$ is **not** a subspace of $\mathbb{R}^3$

**Explanation**

Recall the following two facts about subspaces:

- A set of vectors in a vector space is a *subspace* if it is closed under vector addition and scalar multiplication.
- If a non-empty set is closed under scalar multiplication, then this set must contain the zero-vector (because $0 \cdot \mathbf{v} = \mathbf{0}$ for any vector $\mathbf{v}$). So, if a set does not contain the zero vector, then this set is not closed under scalar multiplication.

With that in mind, let's examine our statements.

- Statement I is false. The direction vector of the line $L$ is
$\begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix}$
This means that the vector equation of the line is
$r|= \begin{bmatrix}1 \\ 2 \\ 2\end{bmatrix} + t\begin{bmatrix}1 \\ -1 \\ -1\end{bmatrix}$
for $t\in\mathbb R$, and the corresponding Cartesian equation is
${x-1}=\dfrac{y-2}{-1}=\dfrac{z-2}{-1}$.
But this line does not pass through the origin since
${(0)-1}\neq\dfrac{(0)-2}{-1}=\dfrac{(0)-2}{-1}. \quad {\color{red}\times}$
- Statement II is false. Let's consider the vectors $\begin{bmatrix}1 \\ 2 \\ 2\end{bmatrix}$ and $\begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix}$ from $L$ (the position vectors of the given points). Then,
$\begin{bmatrix}1 \\ 2 \\ 2\end{bmatrix}$
However, the point $(3,3,3)$ does not belong to $L$ since
${(3)-1}\neq\dfrac{(3)-2}{-1}=\dfrac{(3)-2}{-1}. \quad {\color{red}\times}$
- Statement III is false. Since $\mathbf{0} \not\in L$, this set is not closed under scalar multiplication.
- Statement IV is true. Since statements II and III are false, $L$ is *not* a subspace of $\mathbb{R}^3$.

Therefore, the correct answer is "IV only."

---

**Question 3**

```quiz
type: radio
id: ma-156679
content: |-
  Let $L$ be the line in $R^{3}$ that passes through the points $(0, 0, 0)$ and $(1,-1, 1)$. Which of the following statements are true?

  1. $L$ contains the zero-vector
  2. $L$ is closed under vector addition
  3. $L$ is closed under scalar multiplication
  4. $L$ is **not** a subspace of $R^{3}$
options:
- id: a
  content: |-
    II and IV only
- id: b
  content: |-
    III only
- id: c
  content: |-
    I and III only
- id: d
  content: |-
    IV only
- id: e
  content: |-
    I, II, and III only
  correct: true
```

---

**Question 4**

```quiz
type: radio
id: ma-155957
content: |-
  Let $L$ be the line in $R^{3}$ that passes through the points $(-2, 2,-2)$ and $(3,-4, 2)$. Which of the following statements are true?

  1. $L$ contains the zero-vector
  2. $L$ is closed under vector addition
  3. $L$ is closed under scalar multiplication
  4. $L$ is **not** a subspace of $R^{3}$
options:
- id: a
  content: |-
    I only
- id: b
  content: |-
    I, II, and III only
- id: c
  content: |-
    IV only
  correct: true
- id: d
  content: |-
    I and III only
- id: e
  content: |-
    All the statements are true
```

---

<a id="planes-as-subspace-of-three-dimensional-space"></a>
## Planes as Subspace of Three-Dimensional Space

Again, recall that $H$ is a subspace of $\mathbb{R}^n$ if and only if it satisfies the following properties:

> - *Closure under vector addition:* $\qquad$ If $\mathbf{u}$ and $\mathbf{v}$ are in $H$, then their sum $\mathbf{u}+\mathbf{v}$ is also in $H$.
> - *Closure under scalar multiplication:* $\qquad$ If $\mathbf{u}$ is in $H$, then $c\cdot \mathbf{u}$ is also in $H$ for any scalar number $c$.

Consider the plane $\Pi$ with equation $z=4x-2y$ in $\mathbb{R}^3$.

Notice that $\Pi$ contains vectors of the form

$$
\begin{bmatrix}x \\ y \\ z\end{bmatrix}
$$

Is $\Pi$ a subspace of $\mathbb{R}^3$? To find out, we need to check the two conditions above.

- Let's pick two arbitrary vectors in $\Pi$, namely, $\begin{bmatrix}u_{1} \\ u_{2} \\ 4u_{1} - 2u_{2}\end{bmatrix}$ and $\begin{bmatrix}v_{1} \\ v_{2} \\ 4v_{1} - 2v_{2}\end{bmatrix}$ If we compute their sum, we get
$u + v|= \begin{bmatrix}u_{1} \\ u_{2} \\ 4u_{1} - 2u_{2}\end{bmatrix} + \begin{bmatrix}v_{1} \\ v_{2} \\ 4v_{1} - 2v_{2}\end{bmatrix}; = \begin{bmatrix}u_{1} + v_{1} \\ u_{2} + v_{2} \\ 4u_{1} - 2u_{2} + 4v_{1} - 2v_{2}\end{bmatrix}; = \begin{bmatrix}u_{1} + v_{1} \\ u_{2} + v_{2} \\ 4 \cdot (u_{1} + v_{1}) - 2 \cdot (u_{2} + v_{2})\end{bmatrix}$,
which satisfies the condition $z=4x-2y$. Therefore, $\mathbf{u}+\mathbf{v}\in \Pi. \quad {\color{green}\checkmark}$
- Let's pick an arbitrary vector in $\Pi$, say, $\begin{bmatrix}u_{1} \\ u_{2} \\ 4u_{1} - 2u_{2}\end{bmatrix}$ Then, for any scalar $c$ we have
$c \cdot u|= c \cdot \begin{bmatrix}u_{1} \\ u_{2} \\ 4u_{1} - 2u_{2}\end{bmatrix}; = \begin{bmatrix}cu_{1} \\ cu_{2} \\ 4 \cdot cu_{1} - 2 \cdot cu_{2}\end{bmatrix}$,
which satisfies the condition $z=4x-2y$. Therefore, $c\cdot \mathbf{u}\in \Pi. \quad {\color{green}\checkmark}$

The set $\Pi$ satisfies both conditions. So, we can conclude that it is a subspace of $\mathbb{R}^3$.

---

<a id="determining-whether-a-plane-is-a-subspace-of-three-dimensional-space"></a>
## Determining Whether a Plane Is a Subspace of Three-Dimensional Space

**Example:** Let $\Pi$ be the plane with equation $x+4y-3z=2$ in $\mathbb{R}^3$. Which of the following statements are true?

1. $\Pi$ contains the zero-vector
2. $\Pi$ is closed under vector addition
3. $\Pi$ is closed under scalar multiplication
4. $\Pi$ is **not** a subspace of $\mathbb{R}^3$

**Explanation**

Recall the following two facts about subspaces:

- A set of vectors in a vector space is a *subspace* if it is closed under vector addition and scalar multiplication.
- If a non-empty set is closed under scalar multiplication, then this set must contain the zero-vector (because $0 \cdot \mathbf{v} = \mathbf{0}$ for any vector $\mathbf{v}$). So, if a set does not contain the zero vector, then this set is not closed under scalar multiplication.

Notice that $\Pi$ contains vectors of the form

$$
\begin{bmatrix}x \\ y \\ z\end{bmatrix}
$$

With that in mind, let's examine our statements.

- Statement I is false. The plane does not pass through the origin since
$x + 4y - 3z|= 2; (0) + 4(0) - 3(0)|= 2; 0|= 2\cdot$
- Statement II is false. Let's consider $\begin{bmatrix}2 \\ 0 \\ 0\end{bmatrix}$ and $\begin{bmatrix}1 \\ 1 \\ 1\end{bmatrix}$ Then
$a + b|= \begin{bmatrix}2 \\ 0 \\ 0\end{bmatrix} + \begin{bmatrix}1 \\ 1 \\ 1\end{bmatrix} = \begin{bmatrix}2 + 1 \\ 0 + 1 \\ 0 + 1\end{bmatrix} = \begin{bmatrix}3 \\ 1 \\ 1\end{bmatrix}$,
However, $\begin{bmatrix}3 \\ 1 \\ 1\end{bmatrix}$ does not belong to $\Pi$ since
$x + 4y - 3z|= 2; (3) + 4(1) - 3(1)|= 2; 4|= 2\cdot$

- Statement III is false. Since $\mathbf{0} \not\in \Pi$, this set is not closed under scalar multiplication.
- Statement IV is true. Since statements II and III are false, $\Pi$ is *not* a subspace of $\mathbb{R}^3$.

Therefore, the correct answer is "IV only."

---

**Question 5**

```quiz
type: radio
id: ma-156719
content: |-
  Let $Π$ be the plane with equation $z = x - y$ in $R^{3}$. Which of the following statements are true?

  1. $Π$ contains the zero-vector
  2. $Π$ is closed under vector addition
  3. $Π$ is closed under scalar multiplication
  4. $Π$ is **not** a subspace of $R^{3}$
options:
- id: a
  content: |-
    I only
- id: b
  content: |-
    III only
- id: c
  content: |-
    II only
- id: d
  content: |-
    I, II, and III only
  correct: true
- id: e
  content: |-
    I, and IV only
```

---

**Question 6**

```quiz
type: radio
id: ma-156685
content: |-
  Let $Π$ be the plane with equation $x - y - z = 1$ in $R^{3}$. Which of the following statements are true?

  1. $Π$ contains the zero-vector
  2. $Π$ is closed under vector addition
  3. $Π$ is closed under scalar multiplication
  4. $Π$ is **not** a subspace of $R^{3}$
options:
- id: a
  content: |-
    I and III only
- id: b
  content: |-
    II only
- id: c
  content: |-
    I and II only
- id: d
  content: |-
    IV only
  correct: true
- id: e
  content: |-
    III only
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
