# The Dimension of a Span

<!--
lesson-id: 1866
topic-code: MML.4.4.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Finite and Infinite-Dimensional Vector Spaces](#finite-and-infinite-dimensional-vector-spaces)
- [Finding the Dimension of a Span by Inspection](#finding-the-dimension-of-a-span-by-inspection)
- [Finding the Dimension of a Span Using Row Reduction](#finding-the-dimension-of-a-span-using-row-reduction)
- [Geometric Intuition for Finite-Dimensional Vector Spaces](#geometric-intuition-for-finite-dimensional-vector-spaces)

## Prerequisites

- [Finding a Basis of a Span](<../../4.3. Bases of N-Dimensional Space/Lessons/4.3.1. Finding a Basis of a Span.md>)

---

<a id="introduction"></a>
## Introduction

There is something interesting about the bases of a subspace, summarized in the theorem below:

> If the vector space $V$ has a basis with $n$ vectors, then any basis of $V$ has exactly $n$ vectors.

The number $n$ of vectors in any basis of $V$ is called the **dimension of the space** $V$ and is denoted $\textrm{dim}(V)$.

For example, consider the linear span below:
$\begin{bmatrix}3 \\ -1\end{bmatrix}$

We know that, in order to find a basis of the span, we start by creating a matrix whose columns are made up of our vectors, and then we reduce the matrix to row echelon form:

$$
\begin{aligned}
M &= \begin{bmatrix}3 & -3 & -6 \\ -1 & 1 & 5\end{bmatrix} \mid R_{2} \mid:=R_{2} + \frac{1}{3}R_{1} \\
&= ∼\begin{bmatrix}3 & -3 & -6 \\ 0 & 0 & 3\end{bmatrix}
\end{aligned}
$$

We see that the matrix above has two pivot columns: the $1$st and the $3$rd columns. This tells us that

$$
\mathcal{B}=\{\mathbf{v}_1,\mathbf{v}_3 \}
$$

is a basis of $H$.

Since $\mathcal{B}$ consists of two vectors, *any basis* of $H$ will also have *exactly* two vectors. So, we have

$$
\textrm{dim}(H)=2
$$

---

<a id="finite-and-infinite-dimensional-vector-spaces"></a>
## Finite and Infinite-Dimensional Vector Spaces

As defined earlier, the dimension of a vector space $V$ is the number of vectors in any basis of $V$.

However, note that the definition above is only applicable if a vector space has at least one basis with a finite number of elements.

- If a vector space has a basis with a finite number of vectors, then it is called **finite-dimensional**.
- Otherwise, if there is no finite basis, the vector space is called **infinite-dimensional**.

In linear algebra, we mostly consider finite-dimensional vector spaces.

---

<a id="finding-the-dimension-of-a-span-by-inspection"></a>
## Finding the Dimension of a Span by Inspection

**Example:** Determine the dimension of
$\begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix}$

**Explanation**

We start by creating a matrix whose columns are made up of our vectors, and then we reduce the matrix to row echelon form:

$$
\begin{bmatrix}1 & 1 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0\end{bmatrix}
$$

In this case, our matrix is already in a row echelon form. We see that the matrix above has two pivot columns: the $1$st and the $2$nd columns. This tells us that

$$
\mathcal{B}=\{\mathbf{v}_1,\mathbf{v}_2 \}
$$

forms a basis for our span.

Since $\mathcal{B}$ consists of two vectors, any basis of our span will also have *exactly* two vectors. Therefore, the dimension of the span is $2$:

$$
\begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix}
$$

---

**Question 1**

```quiz
type: radio
id: ma-79149
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $v_{1}, v_{2}, v_{3}$ be linearly independent vectors, and suppose that the vector $w$ can be written as a linear combination of $v_{1}, v_{2}, v_{3}$. Which of the following statements are true?

  1. $v_{1}, v_{2}, v_{3}$ form a basis of $Span{v_{1}, v_{2}, v_{3}}$
  2. $dim(Span{v_{1}, v_{2}, v_{3}}) = 3$
  3. $dim(Span{v_{1}, v_{2}, v_{3}, w}) = 4$
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    I and II only
- id: c
  content: |-
    I only
- id: d
  content: |-
    I, II, and III
- id: e
  content: |-
    I and III only
```

---

**Question 2:**

```quiz
type: radio
id: ma-79143
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $dim(Span{\begin{bmatrix}2 \\ 0 \\ 0\end{bmatrix}, \begin{bmatrix}0 \\ 1 \\ 0\end{bmatrix}, \begin{bmatrix}0 \\ 0 \\ 0\end{bmatrix}})$?
options:
- id: a
  content: |-
    $1$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $9$
- id: d
  content: |-
    $3$
- id: e
  content: |-
    $2$
```

---

<a id="finding-the-dimension-of-a-span-using-row-reduction"></a>
## Finding the Dimension of a Span Using Row Reduction

**Example:** Determine the dimension of
$\begin{bmatrix}2 \\ -6 \\ 4\end{bmatrix}$

**Explanation**

We start by creating a matrix whose columns are made up of our vectors, and then we reduce the matrix to row echelon form:

$$
\begin{aligned}
M &= \begin{bmatrix}2 & -1 & 5 \\ -6 & 3 & -15 \\ 4 & -2 & 10\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + 3R_{1} \\ ∼\begin{bmatrix}2 & -1 & 5 \\ 0 & 0 & 0 \\ 4 & -2 & 10\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + (-2)R_{1} \\
&= ∼\begin{bmatrix}2 & -1 & 5 \\ 0 & 0 & 0 \\ 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

We see that the matrix above has one pivot columns: the $1$st column. This tells us that

$$
\mathcal{B}=\{\mathbf{v}_1 \}
$$

forms a basis for our span.

Since $\mathcal{B}$ consists of one vector, any basis of our span will also have *exactly* one vector. Therefore, the dimension of the span is $1$:

$$
\begin{bmatrix}2 \\ -6 \\ 4\end{bmatrix}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-34811
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Determine the dimension of $Span{\begin{bmatrix}2 \\ -2 \\ 1\end{bmatrix}, \begin{bmatrix}-2 \\ 1 \\ -1\end{bmatrix}, \begin{bmatrix}2 \\ -2 \\ 3\end{bmatrix}}$.
options:
- id: a
  content: |-
    $1$
- id: b
  content: |-
    $3$
- id: c
  content: |-
    $6$
- id: d
  content: |-
    $2$
- id: e
  content: |-
    $0$
```

---

**Question 4:**

```quiz
type: radio
id: ma-34854
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $dim(Span{\begin{bmatrix}3 \\ -6\end{bmatrix}, \begin{bmatrix}5 \\ -10\end{bmatrix}})$?
options:
- id: a
  content: |-
    $10$
- id: b
  content: |-
    $2$
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $1$
- id: e
  content: |-
    $3$
```

---

<a id="geometric-intuition-for-finite-dimensional-vector-spaces"></a>
## Geometric Intuition for Finite-Dimensional Vector Spaces

We can build some geometric intuition for finite-dimensional vector spaces. To illustrate, let's find the dimensions of $\{\mathbf{0}\}$, $\mathbb{R}$, $\mathbb{R}^2$, and $\mathbb{R}^3$.

First, note that $\{\mathbf{0}\}$ can be written as $\mathbb{R}^0$, and $\mathbb{R}$ can be written as $\mathbb{R}^1$. We have the following standard bases:

$$
[1]
$$

Consequently, we have the following dimensions:

$$
dim(R^{0}) = 0, dim(R^{1}) = 1, dim(R^{2}) = 2, dim(R^{3}) = 3
$$

Now, let's build some geometric intuition. We can represent the standard bases in the following diagram:

![](<../Source/4.4.1. The Dimension of a Span/Images/613df4d645a1e9e595cce64297abf750.png>)

So, the sequence $\mathbb{R}^0$, $\mathbb{R}^1$, $\mathbb{R}^2$, $\mathbb{R}^3$, $\ldots$ can be visualized as follows:

![](<../Source/4.4.1. The Dimension of a Span/Images/69f6d9246c5b72a4bcd3facae33ed0cc.png>)

Notice the following:

- The basis for $\mathbb{R}^0$ is represented geometrically as a $0$-dimensional point.
- The basis for $\mathbb{R}^1$ is represented geometrically as a $1$-dimensional line segment.
- The basis for $\mathbb{R}^2$ is represented geometrically as a $2$-dimensional square.
- The basis for $\mathbb{R}^3$ is represented geometrically as a $3$-dimensional cube.

Unfortunately, we can't visualize dimensions greater than $3$, but we know that $\mathbb{R}^n$ has the following standard basis consisting of $n$ vectors:

$$
\begin{bmatrix}1 \\ 0 \\ 0 \\ ⋮ \\ 0\end{bmatrix}
$$

Therefore,

$$
\textrm{dim}(\mathbb{R}^n) = n
$$

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
