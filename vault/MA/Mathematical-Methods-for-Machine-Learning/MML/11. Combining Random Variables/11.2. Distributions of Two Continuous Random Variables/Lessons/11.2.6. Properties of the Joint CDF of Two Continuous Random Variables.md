# Properties of the Joint CDF of Two Continuous Random Variables

<!--
lesson-id: 3866
topic-code: MML.11.2.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Recovering a Marginal CDF](#recovering-a-marginal-cdf)
- [Finding a Marginal CDF Using a Joint CDF](#finding-a-marginal-cdf-using-a-joint-cdf)
- [Finding Part of a Joint CDF from Marginal CDFs for Continuous Random Variables](#finding-part-of-a-joint-cdf-from-marginal-cdfs-for-continuous-random-variables)
- [Recovering the Joint PDF from a Joint CDF](#recovering-the-joint-pdf-from-a-joint-cdf)

## Prerequisites

- [Higher-Order Partial Derivatives](<../../../9. Multivariable Calculus/9.2. Partial Derivatives/Lessons/9.2.9. Higher-Order Partial Derivatives.md>)
- [The Joint CDF of Two Continuous Random Variables](<11.2.5. The Joint CDF of Two Continuous Random Variables.md>)

---

<a id="introduction"></a>
## Introduction

Recall that the **joint cumulative distribution function** (or joint CDF) of two random variables $X$ and $Y$ is defined as

$$
F(x,y)=P(X \leq x,Y \leq y)
$$

If $X$ and $Y$ are continuous random variables with the joint probability density function $f(x, y)$, we have the following relationship between the joint PDF and joint CDF:

$$
F(x,y)=\int^y_{-\infty}\int^x_{-\infty} f(u,v)\; \textrm{d}u\textrm{d}v
$$

Geometrically, the joint CDF gives the probability that the random pair $(X,Y)$ will lie inside the infinite rectangular region $\Delta$ that has its top-right corner at $(x,y)$ and whose sides are parallel to the coordinate axes.

![](<../Source/11.2.6. Properties of the Joint CDF of Two Continuous Random Variables/Images/a157904e04094b85ef5f852de41c8966.png>)

The joint CDF satisfies the following properties:

- $0\leq F(x,y)\leq 1$
- $F(\infty, \infty)=1$
- $F(-\infty,y)=F(x,-\infty)=0$
- The **marginal CDF** of $X$ is given by $F_X(x)=F(x,\infty)$.
- The **marginal CDF** of $Y$ is given by $F_Y(y)=F(\infty,y)$.
- If $X$ and $Y$ are independent random variables, then
$F(x,y)=F_X(x)\cdot F_Y(y)$.
- The joint probability density function $f(x, y)$ is given by
$f(x,y) = \dfrac{\partial^2}{\partial x \partial y} F(x,y)$.

In this lesson, we'll explore the last four of these properties in more detail.

---

<a id="recovering-a-marginal-cdf"></a>
## Recovering a Marginal CDF

The first property to consider is how to recover the marginal CDFs of $X$ and $Y$ from the joint CDF.

Suppose the joint CDF of two continuous random variables $X$ and $Y$ is given by

$$
{(1 - e^{-x})(1 - e^{-2y}),\begin{vmatrix}x \ge 0, y \ge 0 \\ 0,\end{vmatrix}otherwise
$$

Let's calculate $F_Y(y)$, the marginal CDF of $Y$ using the following property:

$$
F_Y(y) = F(\infty,y) = \lim\limits_{x \to \infty} F(x,y)
$$

Therefore, for

$$
y \geq 0
$$

we have

$$
\begin{aligned}
F_{Y}(y) &= lim_(x → ∞)[(1 - e^{-x})(1 - e^{-2y})] \\
&= (1 - e^{-2y})lim_(x → ∞)(1 - \frac{1}{e^{x}}) \\
&= (1 - e^{-2y})lim_(x → ∞)(1 - 0) \\
&= 1 - e^{-2y}
\end{aligned}
$$

So, the full expression for the marginal CDF is as follows:

$$
{1 - e^{-2y},\begin{vmatrix}y \ge 0 \\ 0,\end{vmatrix}otherwise
$$

Let's see another example.

---

<a id="finding-a-marginal-cdf-using-a-joint-cdf"></a>
## Finding a Marginal CDF Using a Joint CDF

**Example:** The joint CDF of two continuous random variables $X$ and $Y$, for $(x,y) \in [0,1] \times [0,2]$, is given by

$F(x,y)= \dfrac{xy}{8}(2x^2+y)$.

Find $F_X(x)$, the marginal CDF of $X$, for $0 \leq x \leq 1$.

**Explanation**

The marginal CDF of $X$ is given by

$$
F_X(x) = F(x,\infty) = \lim\limits_{y \to \infty} F(x,y)
$$

Therefore, for

$$
0\leq x\leq 1
$$

we have

$$
\begin{aligned}
F_{X}(x) &= F(x, ∞) \\
&= F(x, 2) \\
&= (x(2))/(8)(2x^{2} + (2)) \\
&= \frac{4x}{8}(x^{2} + 1) \\
&= \frac{1}{2}(x^{3} + x)
\end{aligned}
$$

---

**Question 1**

```quiz
type: radio
id: ma-148023
content: |-
  The joint CDF of two continuous random variables $X$ and $Y$ is given by

  $F(x, y) = {0,\begin{vmatrix}x < 0ory < 0 \\ (1 - e^{-3x})y^{2},\end{vmatrix}x \ge 0, 0 \le y \le 1; F_{X}(x), \mid x \ge 0, y > 1$.

  Find $F_{Y}(y)$, the marginal CDF of $Y$, for $y \ge 0$.
options:
- id: a
  content: |-
    $y$
- id: b
  content: |-
    $y^{2}$
  correct: true
- id: c
  content: |-
    $y^{3}$
- id: d
  content: |-
    $2y$
- id: e
  content: |-
    $\frac{1}{3}y^{3}$
```

---

**Question 2**

```quiz
type: radio
id: ma-148034
content: |-
  The joint CDF of two continuous random variables $X$ and $Y$, for $(x, y) ∈ [0, 1] \cdot [0, 2]$, is given by

  $F(x, y) = \frac{1}{20}x^{2}y(4x + 3y)$.

  Find $F_{X}(x)$, the marginal CDF of $X$, for $0 \le x \le 1$.
options:
- id: a
  content: |-
    $\frac{1}{5}(2x^{3} + 3x^{2})$
  correct: true
- id: b
  content: |-
    $\frac{1}{5}(3x + 2)$
- id: c
  content: |-
    $\frac{1}{10}(2x + 3)$
- id: d
  content: |-
    $\frac{1}{4}(2x^{2} + 3x)$
- id: e
  content: |-
    $\frac{1}{4}(3x^{3} + 2x^{2})$
```

---

<a id="finding-part-of-a-joint-cdf-from-marginal-cdfs-for-continuous-random-variables"></a>
## Finding Part of a Joint CDF from Marginal CDFs for Continuous Random Variables

**Example:** The marginal CDFs of two continuous independent random variables $X$ and $Y$ are given below.

${1 - \frac{1}{x},\begin{vmatrix}x \ge 1 \\ 0,\end{vmatrix}otherwise$

Find the expression for the joint CDF when $x \geq 1$ and $y\geq 1$.

**Explanation**

If $X$ and $Y$ are independent continuous random variables with marginal CDFs $F_X(x)$ and $F_Y(y)$, respectively, then the corresponding joint CDF of $F(x,y)$ is given by

$$
F(x,y) = F_X(x) \cdot F_Y (y)
$$

Therefore, for

$$
x \geq 1
$$

and

$$
y \geq 1
$$

the joint CDF for our random variables is

$$
\begin{aligned}
F(x, y) &= F_{X}(x) \cdot F_{Y}(y) \\
&= (1 - \frac{1}{x})(1 - \frac{1}{\sqrt{y^{3}}}) \\
&= 1 - \frac{1}{x} - \frac{1}{\sqrt{y^{3}}} + \frac{1}{x\sqrt{y^{3}}}
\end{aligned}
$$

Therefore, the full expression for the joint CDF is

$$
{1 - \frac{1}{x} - \frac{1}{\sqrt{y^{3}}} + \frac{1}{x\sqrt{y^{3}}},\begin{vmatrix}x \ge 1, y \ge 1 \\ 0,\end{vmatrix}otherwise
$$

---

**Question 3**

```quiz
type: radio
id: ma-111912
content: |-
  The marginal CDFs of two continuous independent random variables $X$ and $Y$ are given below.

  $F_{X}(x) = {1 - e^{-2x},\begin{vmatrix}x \ge 0 \\ 0,\end{vmatrix}otherwiseF_{Y}(y) = {1 - e^{-3y},\begin{vmatrix}y \ge 0 \\ 0,\end{vmatrix}otherwise$

  Find the expression for the joint CDF when $x \ge 0$ and $y \ge 0$.
options:
- id: a
  content: |-
    $1 - e^{-3y}$
- id: b
  content: |-
    $1 - e^{-2x} - e^{-3y} + e^{-6xy}$
- id: c
  content: |-
    $1 - e^{-2x}$
- id: d
  content: |-
    $1 - e^{-2x} - e^{-3y}$
- id: e
  content: |-
    $1 - e^{-2x} - e^{-3y} + e^{-2x - 3y}$
  correct: true
```

---

**Question 4**

```quiz
type: radio
id: ma-159316
content: |-
  The marginal CDFs of two continuous independent random variables $X$ and $Y$ are given below.

  $F_{X}(x) = {1 - \frac{1}{x^{2}},\begin{vmatrix}x \ge 1 \\ 0,\end{vmatrix}otherwiseF_{Y}(y) = {1 - \frac{1}{y},\begin{vmatrix}y \ge 1 \\ 0,\end{vmatrix}otherwise$

  Find the expression for the joint CDF when $x \ge 1$ and $y \ge 1$.
options:
- id: a
  content: |-
    $1 - \frac{2}{x^{2}} - \frac{2}{y} + \frac{1}{x^{2}y}$
- id: b
  content: |-
    $1 + \frac{1}{x^{2}y^{2}}$
- id: c
  content: |-
    $1 - \frac{1}{x^{2}} - \frac{1}{y} + \frac{1}{x^{2}y}$
  correct: true
- id: d
  content: |-
    $1 - \frac{1}{x^{2}y}$
- id: e
  content: |-
    $1 - \frac{1}{x^{2}} - \frac{1}{y}$
```

---

<a id="recovering-the-joint-pdf-from-a-joint-cdf"></a>
## Recovering the Joint PDF from a Joint CDF

**Example:** The joint CDF of two continuous random variables $X$ and $Y$ is given by

${0,\begin{vmatrix}x < 0ory < 0 \\ x^{2}(1 - e^{-y}),\end{vmatrix}0 \le x \le 1, y \ge 0; F_{Y}(y), \mid x > 1, y \ge 0$.

Find the joint PDF $f(x,y)$ for $0 \leq x \leq 1, \; y \geq 0$.

**Explanation**

Since the partial derivatives of $F(x,y)$ are defined, the joint probability density function $f(x, y)$ is given by

$$
f(x,y) = \dfrac{\partial^2}{\partial x \partial y} F(x,y)
$$

For

$$
0 \leq x \leq 1
$$

and

$$
y \geq 0
$$

we have

$$
\begin{aligned}
f(x, y) &= \frac{∂^{2}}{∂x∂y}F(x, y) \\
&= \frac{∂}{∂x}(\frac{∂}{∂y}(x^{2}(1 - e^{-y}))) \\
&= \frac{∂}{∂x}(x^{2}\frac{∂}{∂y}(1 - e^{-y})) \\
&= \frac{∂}{∂x}(x^{2}e^{-y}) \\
&= e^{-y} \cdot \frac{∂}{∂x}(x^{2}) \\
&= e^{-y} \cdot 2x \\
&= 2xe^{-y}
\end{aligned}
$$

Otherwise,

$$
f(x,y) = 0
$$

Therefore, we have

$$
{2xe^{-y},\begin{vmatrix}0 \le x \le 1, y \ge 0 \\ 0,\end{vmatrix}otherwise
$$

---

**Question 5**

```quiz
type: radio
id: ma-110423
content: |-
  The joint CDF of two continuous random variables $X$ and $Y$ is given by

  $F(x, y) = {(1 - e^{-x})(1 - e^{-y}),\begin{vmatrix}x \ge 0, y \ge 0 \\ 0,\end{vmatrix}otherwise$.

  Find the joint PDF $f(x, y)$ for $x \ge 0, y \ge 0$.
options:
- id: a
  content: |-
    $1 - e^{-x} - e^{-y}$
- id: b
  content: |-
    $e^{-x - y}$
  correct: true
- id: c
  content: |-
    $e^{-x} - e^{-y}$
- id: d
  content: |-
    $1 - e^{-x - y}$
- id: e
  content: |-
    $1 - e^{x - y}$
```

---

**Question 6**

```quiz
type: radio
id: ma-148037
content: |-
  The joint CDF of two continuous random variables $X$ and $Y$ is given by

  $F(x, y) = {0,\begin{vmatrix}x < 1ory < 1 \\ (4(x^{2} - 1)(y - 1))/(9y),\end{vmatrix}1 \le x \le 2, 1 \le y \le 4; F_{X}(x),\begin{vmatrix}1 \le x \le 2, y > 4 \\ F_{Y}(y),\end{vmatrix}x > 2, 1 \le y \le 4; 1, \mid x > 2, y > 4$.

  where $F_{X}$ and $F_{Y}$ are the marginal CDFs of $X$ and $Y$, respectively. Find the joint PDF $f(x, y)$ for $1 \le x \le 2$ and $1 \le y \le 4$.
options:
- id: a
  content: |-
    $\frac{4x^{2}}{9y}$
- id: b
  content: |-
    $\frac{8x}{9y^{2}}$
  correct: true
- id: c
  content: |-
    $\frac{4x^{2}}{9y^{2}}$
- id: d
  content: |-
    $\frac{8x}{9y}$
- id: e
  content: |-
    $\frac{8x^{2}}{9y}$
```

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
