# The Change-of-Variables Method for Continuous Random Variables

<!--
lesson-id: 3056
topic-code: MML.10.3.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Transformations Where the Change-of-Variables Method Is Applicable](#identifying-transformations-where-the-change-of-variables-method-is-applicable)
- [Computing the PDF of a Random Variable Under an Affine Transformation](#computing-the-pdf-of-a-random-variable-under-an-affine-transformation)
- [Computing the PDF of a Random Variable Under a Nonlinear Transformation](#computing-the-pdf-of-a-random-variable-under-a-nonlinear-transformation)
- [Proof of the Change-of-Variables Formula](#proof-of-the-change-of-variables-formula)

## Prerequisites

- [Inverses of Exponential and Logarithmic Functions](<../../../../AG2/4. Exponentials & Logarithms/4.6. Graphs of Logarithmic Functions/Lessons/4.6.4. Inverses of Exponential and Logarithmic Functions.md>)
- [The Distribution Function Method](<10.3.3. The Distribution Function Method.md>)
- [Inverses of Quadratic Functions](<../../../../AG2/3. Functions/3.1. Functions/Lessons/3.1.7. Inverses of Quadratic Functions.md>)
- [Inverses of Radical Functions](<../../../../AG2/6. Radical Expressions & Functions/6.2. Graphs of Radical Functions/Lessons/6.2.8. Inverses of Radical Functions.md>)
- [Inverses of Reciprocal Functions](<../../../../AG2/5. Rational Expressions & Functions/5.2. Reciprocal Functions/Lessons/5.2.5. Inverses of Reciprocal Functions.md>)

---

<a id="introduction"></a>
## Introduction

Let $X$ be a continuous random variable whose probability density function is known. We know that if $Y = u(X)$, then we can apply the distribution function method to compute the PDF of $Y$.

It's possible to generalize the distribution function method when $u(X)$ is strictly monotonic. This generalization is known as the **change-of-variables method**, and is stated below:

> *If $X\in (a,b)$ is a continuous random variable with probability density function $f_X$ and $Y = u(X)$, where the function $u(x)$ is strictly monotonic, then according to the change-of-variables method, the probability density function of $Y$ is given by*
> ${f_{X}(u^{-1}(y)) \cdot\begin{vmatrix}(u^{-1})^{′}(y) &,\end{vmatrix}yis betweenu(a)andu(b); 0, \mid otherwise$.

Proof of this result will be provided at the end of the lesson.

The advantage of the change-of-variables method is that it's often easier and faster to implement than the distribution function method. The disadvantage is that it only works when $u$ is monotonic, whereas the distribution function method can be extended to deal with non-monotonic functions.

For example, suppose that $X$ has the probability density function

$$
{\frac{1}{2}x,\begin{vmatrix}0 < x < 2 \\ 0,\end{vmatrix}otherwise
$$

Let the random variable $Y = 3X$. Then $u(x) = 3x$, which is strictly monotonically increasing. Therefore, the change-of-variables method can be used to compute the PDF of $Y$.

First, we find $u^{-1}(y)$ as follows:

$$
\begin{aligned}
u(y) &= 3y \\
y &= 3u^{-1}(y) \\
u^{-1}(y) &= \frac{1}{3}y
\end{aligned}
$$

Then, we find $(u^{-1})'(y){:}$

$$
u^{-1}(y) = \dfrac{1}{3}y \quad \Longrightarrow \quad (u^{-1})'(y) = \dfrac{1}{3}
$$

According to the change-of-variables method, for nonzero $f_Y$, we have

$$
\begin{aligned}
f_{Y}(y) &= f_{X}(u^{-1}(y)) \cdot \mid (u^{-1})^{′}(y) \mid \\
&= f_{X}(\frac{1}{3}y) \cdot \mid \frac{1}{3} \mid \\
&= \frac{1}{2}(\frac{1}{3}y) \cdot \frac{1}{3} \\
&= \frac{1}{18}y
\end{aligned}
$$

Finally, since $f_X$ is nonzero on $0 < x < 2$, the bounds are $a=0$ and $b=2$, and we have

$$
u(0) = 0, \qquad u(2) = 6
$$

Therefore, the probability distribution of $Y$ is

$$
{\frac{1}{18}y,\begin{vmatrix}0 < y < 6 \\ 0,\end{vmatrix}otherwise
$$

---

<a id="identifying-transformations-where-the-change-of-variables-method-is-applicable"></a>
## Identifying Transformations Where the Change-of-Variables Method Is Applicable

**Example:** Suppose that $X$ is a continuous random variable. For which of the following transformations can the change-of-variables method be applied to find the probability density function of $Y$?

1. $Y = X^4$, where $X\in (0,2)$
2. $Y = \cos X$, where $X\in (-\infty, \infty)$
3. $Y = e^{-X}$, where $X\in (0, \infty)$

**Explanation**

The change-of-variables method can be applied when a transformation $Y = u(X)$ is strictly increasing or strictly decreasing (i.e., strictly monotonic) over the entire support of $X$.

With that in mind, let's inspect each transformation in turn.

- Transformation I is strictly monotonic. The function $u(x) = x^4$ is strictly increasing for $x\in (0,2)$.
- Transformation II is *not* strictly monotonic. For example, $u(x) = \cos x$ is decreasing on $x \in \left(0,\pi \right)$ and increasing on $x \in \left(\pi,2\pi\right)$.
- Transformation III is strictly monotonic. The function $u(x) =e^{-x}$ is strictly decreasing for $x\in (0,\infty)$.

Therefore, the correct answer is "I and III only."

---

**Question 1**

Suppose that $X$ is a continuous random variable. For which of the following transformations can the change-of-variables method be applied to find the probability density function of $Y$?

1. $Y = X^{2}$, where $X ∈ (-1, 1)$
2. $Y = 3 - 2X$, where $X ∈ [2, 5]$
3. $Y = e^{X}$, where $X ∈ (- ∞, ∞)$

- [ ] A. I, II, and III
- [ ] B. I and II only
- [ ] C. II and III only
- [ ] D. I and III only
- [ ] E. II only

---

**Question 2**

Suppose that $X$ is a continuous random variable. For which of the following transformations can the change-of-variables method be applied to find the probability density function of $Y$?

1. $Y = X^{2}$, where $X ∈ (0, 5)$
2. $Y = \sin X$, where $X ∈ (- ∞, ∞)$
3. $Y = \ln X$, where $X ∈ (0, ∞)$

- [ ] A. I only
- [ ] B. I and III only
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. III only

---

<a id="computing-the-pdf-of-a-random-variable-under-an-affine-transformation"></a>
## Computing the PDF of a Random Variable Under an Affine Transformation

**Example:** Let $X$ be a continuous random variable with the following probability density function:
${\frac{1}{156}x^{3},\begin{vmatrix}1 < x < 5 \\ 0,\end{vmatrix}otherwise$

If $Y = \dfrac{1-X}{4}$, use the change-of-variables method to find the probability density function of $Y$.

**Explanation**

If $X\in (a,b)$ is a continuous random variable with probability density function $f_X$ and $Y = u(X)$, where the function $u(x)$ is strictly monotonic, then according to the change-of-variables method, the probability density function of $Y$ is given by
${f_{X}(u^{-1}(y)) \cdot\begin{vmatrix}(u^{-1})^{′}(y) &,\end{vmatrix}yis betweenu(a)andu(b); 0, \mid otherwise$.

The transformation

$$
u(x) = \dfrac{1-x}{4}
$$

is strictly monotonically *decreasing*, so the change-of-variables method can be used.

First, we find $u^{-1}(y)$, as follows:

$$
\begin{aligned}
u(y) &= \frac{1 - y}{4} \\
y &= (1 - u^{-1}(y))/(4) \\
u^{-1}(y) &= 1 - 4y
\end{aligned}
$$

Then, we find $(u^{-1})'(y){:}$

$$
(u^{-1})'(y) = -4
$$

According to the change-of-variables method, for nonzero $f_Y$, we have

$$
\begin{aligned}
f_{Y}(y) &= f_{X}(u^{-1}(y)) \cdot \mid (u^{-1})^{′}(y) \mid \\
&= f_{X}(1 - 4y) \cdot \mid - 4 \mid \\
&= \frac{1}{156}(1 - 4y)^{3} \cdot 4 \\
&= \frac{1}{39}(1 - 4y)^{3}
\end{aligned}
$$

Finally, since $f_X$ is nonzero on $1 < x < 5$, the bounds are $a=1$ and $b=5$, we have

$$
u(1) = 0, \quad u(5) = -1
$$

Therefore, the probability distribution of $Y$ is

$$
{\frac{1}{39}(1 - 4y)^{3},\begin{vmatrix}-1 < y < 0 \\ 0,\end{vmatrix}otherwise
$$

---

**Question 3**

Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{1}{2}x,\begin{vmatrix}0 < x < 2 \\ 0,\end{vmatrix}otherwise$

If $Y = 1 - 2X$, use the change-of-variables method to find the probability density function of $Y$.

- [ ] A. $f_{Y}(y) = {\frac{1}{2}(1 - 2y),\begin{vmatrix}-3 < y < 1 \\ 0,\end{vmatrix}otherwise$
- [ ] B. $f_{Y}(y) = {\frac{1}{4}(1 - y),\begin{vmatrix}-3 < y < 1 \\ 0,\end{vmatrix}otherwise$
- [ ] C. $f_{Y}(y) = {\frac{1}{2}(1 - 2y),\begin{vmatrix}0 < y < 2 \\ 0,\end{vmatrix}otherwise$
- [ ] D. $f_{Y}(y) = {\frac{1}{8}(1 - y),\begin{vmatrix}-3 < y < 1 \\ 0,\end{vmatrix}otherwise$
- [ ] E. $f_{Y}(y) = {\frac{1}{8}(1 - y),\begin{vmatrix}0 < y < 2 \\ 0,\end{vmatrix}otherwise$

---

**Question 4**

Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {2e^{-2x},\begin{vmatrix}x \ge 0 \\ 0,\end{vmatrix}otherwise$

If $Y =-5X$, use the change-of-variables method to find the probability density function of $Y$.

- [ ] A. $f_{Y}(y) = {\frac{2}{5}e^{2y/5},\begin{vmatrix}y \le 0 \\ 0,\end{vmatrix}otherwise$.
- [ ] B. $f_{Y}(y) = {5e^{-y/5},\begin{vmatrix}y \ge 0 \\ 0,\end{vmatrix}otherwise$.
- [ ] C. $f_{Y}(y) = {\frac{4}{5}e^{2y/5},\begin{vmatrix}y \le 0 \\ 0,\end{vmatrix}otherwise$.
- [ ] D. $f_{Y}(y) = {2e^{-y/5},\begin{vmatrix}y \ge 0 \\ 0,\end{vmatrix}otherwise$.
- [ ] E. $f_{Y}(y) = {2e^{2y/5},\begin{vmatrix}y \le 0 \\ 0,\end{vmatrix}otherwise$.

---

<a id="computing-the-pdf-of-a-random-variable-under-a-nonlinear-transformation"></a>
## Computing the PDF of a Random Variable Under a Nonlinear Transformation

**Example:** Let $X$ be a continuous random variable with the following probability density function:
${3x^{2},\begin{vmatrix}0 < x < 1 \\ 0,\end{vmatrix}otherwise$

If $Y = \sqrt{X}$, use the change-of-variables method to find the probability density function of $Y$.

**Explanation**

If $X \in (a,b)$ is a continuous random variable with probability density function $f_X$ and $Y = u(X)$, where the function $u(x)$ is strictly monotonic, then according to the change-of-variables method, the probability density function of $Y$ is given by
${f_{X}(u^{-1}(y)) \cdot\begin{vmatrix}(u^{-1})^{′}(y) &,\end{vmatrix}yis betweenu(a)andu(b); 0, \mid otherwise$.

Here, we have

$$
Y = \sqrt{X}
$$

The transformation

$$
u(x) = \sqrt{x}
$$

is strictly monotonically *increasing*, so the change-of-variables method can be used.

First, we find $u^{-1}(y)$ as follows:

$$
\begin{aligned}
u(y) &= \sqrt{y} \\
y &= \sqrt{u^{-1}(y)} \\
u^{-1}(y) &= y^{2}
\end{aligned}
$$

Then, we find $(u^{-1})'(y){:}$

$$
(u^{-1})'(y) = 2y
$$

Therefore, according to the change-of-variable method, we have

$$
\begin{aligned}
f_{Y}(y) &= f_{X}(u^{-1}(y)) \cdot \mid (u^{-1})^{′}(y) \mid \\
&= f_{X}(y^{2}) \cdot \mid 2y \mid \\
&= 3(y^{2})^{2} \cdot 2y \\
&= 3y^{4} \cdot 2y \\
&= 6y^{5}
\end{aligned}
$$

Finally, since $f_X$ is nonzero on $0 < x < 1$, the bounds are $a = 0$ and $b = 1$, we have

$$
u(0) = 0, \quad u(1) = 1
$$

Therefore, the probability distribution of $Y$ is
${6y^{5},\begin{vmatrix}0 < y < 1 \\ 0,\end{vmatrix}otherwise$.

---

**Question 5**

Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{3}{14}\sqrt{x},\begin{vmatrix}1 < x < 4 \\ 0,\end{vmatrix}otherwise$

If $Y = \ln X$, use the change-of-variables method to find the probability density function of $Y$.

- [ ] A. $f_{Y}(y) = {\frac{3}{28}\ln y,\begin{vmatrix}0 < y < \ln 4 \\ 0,\end{vmatrix}otherwise$
- [ ] B. $f_{Y}(y) = {\frac{3}{28}\ln y,\begin{vmatrix}1 < y < 4 \\ 0,\end{vmatrix}otherwise$
- [ ] C. $f_{Y}(y) = {\frac{3}{14}e^{3y/2},\begin{vmatrix}0 < y < \ln 4 \\ 0,\end{vmatrix}otherwise$
- [ ] D. $f_{Y}(y) = {\frac{3}{14}e^{y},\begin{vmatrix}e < y < e^{4} \\ 0,\end{vmatrix}otherwise$
- [ ] E. $f_{Y}(y) = {\frac{3}{14}\sqrt{\ln y},\begin{vmatrix}1 < y < 4 \\ 0,\end{vmatrix}otherwise$

---

**Question 6**

Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{2\ln x}{x},\begin{vmatrix}1 < x < e \\ 0,\end{vmatrix}otherwise$

If $Y = \frac{1}{X}$, use the change-of-variables method to find the probability density function of $Y$.

- [ ] A. $f_{Y}(y) = {\frac{1}{e - 1},\begin{vmatrix}1 < y < e \\ 0,\end{vmatrix}otherwise$
- [ ] B. $f_{Y}(y) = {2\ln y,\begin{vmatrix}1 < y < e \\ 0,\end{vmatrix}otherwise$
- [ ] C. $f_{Y}(y) = {- (2\ln (y))/(y),\begin{vmatrix}e^{-1} < y < 1 \\ 0,\end{vmatrix}otherwise$
- [ ] D. $f_{Y}(y) = {ye^{y - 2},\begin{vmatrix}1 < y < 2 \\ 0,\end{vmatrix}otherwise$
- [ ] E. $f_{Y}(y) = {- (y\ln (y))/(2),\begin{vmatrix}e^{-1} < y < 1 \\ 0,\end{vmatrix}otherwise$

---

<a id="proof-of-the-change-of-variables-formula"></a>
## Proof of the Change-of-Variables Formula

Let's restate the change-of-variables method:

> *If $X\in (a,b)$ is a continuous random variable with probability density function $f_X$ and $Y = u(X)$, where the function $u(x)$ is strictly monotonic, then according to the change-of-variables method, the probability density function of $Y$ is given by*
> ${f_{X}(u^{-1}(y)) \cdot\begin{vmatrix}(u^{-1})^{′}(y) &,\end{vmatrix}yis betweenu(a)andu(b); 0, \mid otherwise$.

Now, let's prove this result.

The first part is simple. Because $X$ is between $a$ and $b$, and $u$ is monotonic, $Y=u(X)$ must be between $u(a)$ and $u(b)$. So

$$
f_Y(y) = 0
$$

when $y$ is not between $u(a)$ and $u(b)$.

To carry out the rest of the proof, we will need to apply the distribution function method in two cases:

- **Case 1**: $u$ is monotonically increasing
- **Case 2**: $u$ is monotonically decreasing

We'll consider the case where $u$ is monotonically increasing only. The proof when $u$ is monotonically decreasing is similar.

First, we find the cumulative distribution of $Y$. Assuming $y$ is between $u(a)$ and $u(b)$, we have

$$
\begin{aligned}
F_{Y}(y) &= P(Y \le y) \\
&= P(u(X) \le y) \\
&= P(X \le u^{-1}(y)) \\
&= ∫_{a}^{u^{-1}(y)}f_{X}(t)dt \\
&= F_{X}(t) \mid _{a}^{u^{-1}(y)} \\
&= F_{X}(u^{-1}(y)) - F_{X}(a)
\end{aligned}
$$

Then, we differentiate to get the probability density function:

$$
\begin{aligned}
f_{Y}(y) &= \frac{d}{dy}[F_{Y}(y)] \\
&= \frac{d}{dy}[F_{X}(u^{-1}(y)) - F_{X}(a)] \\
&= f_{X}(u^{-1}(y))(u^{-1})^{′}(y)
\end{aligned}
$$

Because $u$ is monotonically increasing, we have that $u^{-1}$ is monotonically increasing as well. To see this, it's helpful to remember that we reflect a function over the line $y=x$ to get its inverse.

Since the slope of a monotonically increasing function is always zero or positive, we have

$$
(u^{-1})'(y) \geq 0
$$

and consequently

$$
(u^{-1})'(y) = \left| (u^{-1})'(y) \right|
$$

Therefore,

$$
\begin{aligned}
f_{Y}(y) &= f_{X}(u^{-1}(y)) \mid (u^{-1})^{′}(y) \mid
\end{aligned}
$$

as required.

```update-progress
```

[[MA/Mathematical-Methods-for-Machine-Learning/MML/Home|Home]]
[[MA/Mathematical-Methods-for-Machine-Learning/MML/0. Table of Contents/TOC|Table of Contents]]
