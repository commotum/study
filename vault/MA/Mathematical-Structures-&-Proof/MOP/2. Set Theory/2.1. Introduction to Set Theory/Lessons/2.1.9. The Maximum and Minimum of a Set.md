# The Maximum and Minimum of a Set

<!--
lesson-id: 4396
topic-code: MOP.2.1.9
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Maximum and Minimum of a Set](#finding-the-maximum-and-minimum-of-a-set)
- [Explaining the Existence of a Maximum of Minimum](#explaining-the-existence-of-a-maximum-of-minimum)
- [Maximums and Minimums in More Complex Settings](#maximums-and-minimums-in-more-complex-settings)

## Prerequisites

- [The Conditional Definition of a Set](<2.1.4. The Conditional Definition of a Set.md>)

---

<a id="introduction"></a>
## Introduction

The **minimum** of a set $S$ is the element $x_{\min} \in S$ such that

$$
x_{\min} \leq x
$$

for all $x \in S$. In other words, the minimum is the element of $S$ that is *smaller than or equal to* any other element.

Similarly, the **maximum** of a set $S$ is the element $x_{\max} \in S$ such that

$$
x \leq x_{\max}
$$

for all $x \in S$. In other words, the maximum is the element of $S$ that is *larger than or equal to* any other element.

For example, what are the minimum and maximum of the following set?

$$
A = \big\{2, \: -\sqrt{5}, \: 3, \: -1 \big\}
$$

Note that

$$
-\sqrt{5} \approx -2.236
$$

Therefore:

$$
\begin{aligned}
min(A) &= min{2,-\sqrt{5}, 3,-1} =-\sqrt{5} \\
max(A) &= max{2,-\sqrt{5}, 3,-1} = 3
\end{aligned}
$$

Some sets may not have a maximum or minimum.

For example, consider the semi-open interval of real numbers $[MATH: B=[1, 5).]$ In this case, we have

$[MATH: \min(B) = \min[1, 5) = 1.]$

However, since $5 \notin B$, the set has no maximum!

Finally, note the following:

- The maximum and minimum elements *must* belong to the underlying set.
- If the maximum or minimum elements exist, they are unique. In other words, there can be, at most, only one maximum and only one minimum.
- If the "less than or equal to" operation is not defined *for all* elements of a set $S$, then the set has no maximum or minimum. For example, the following set has no minimum and maximum:
$S = \big\{\mathbb R, \clubsuit, 2, 3, \langle 1, 2, 3\rangle \big\}$

---

<a id="finding-the-maximum-and-minimum-of-a-set"></a>
## Finding the Maximum and Minimum of a Set

**Example:** What are the minimum and maximum of the set $A = \left[0,3\right]$?

**Explanation**

Recall that

- $x_{\max}$ is the maximum of a set $S$ if $x_{\max} \in S$ and $x \leq x_{\max}$ for all $x \in S$, and
- $x_{\min}$ is the minimum of a set $S$ if $x_{\min} \in S$ and $x_{\min} \leq x$ for all $x \in S$.

Therefore, we have the following:

$$
\begin{aligned}
min(A) &= min[0, 3] = 0 \\
max(A) &= max[0, 3] = 3
\end{aligned}
$$

---

**Question 1**

What are the minimum and maximum of the set $A = {1, π, 3,-2}$?$min(A) =$
$\underline{\hspace{1.5cm}}$
$max(A) =$
$\underline{\hspace{1.5cm}}$

---

**Question 2**

What are the minimum and maximum of the set $B = [- 1, \sqrt{5}]$?$min(A) =$
$\underline{\hspace{1.5cm}}$
$max(A) =$
$\underline{\hspace{1.5cm}}$

---

<a id="explaining-the-existence-of-a-maximum-of-minimum"></a>
## Explaining the Existence of a Maximum of Minimum

**Example:** Given the set $[MATH: S = (-\infty,7],]$ explain why $\max(S) = 7$.

**Explanation**

Recall that $x_{\max}$ is the maximum of a set $S$ if

- $x_{\max} \in S$, and
- $x\leq x_{\max}$ for all $x \in S$.

In our case, we have that $7$ is the maximum of the set $S$ because

- $7 \in S$, and
- $x \leq 7$ for all $x \in S$.

---

**Question 3**

Complete the following reasoning about the set $[MATH: S = (- ∞, 1]:]$

The value $1$ is the $\underline{\hspace{2cm}}$ of the set $S$ because $\underline{\hspace{2cm}}$ and $\underline{\hspace{2cm}}$ for all $x ∈ S$.

#### Select 1

- [ ] minimum
- [ ] maximum

#### Select 2

- [ ] [MATH: 1 ∉ S]
- [ ] [MATH: 1 ∈ S]

#### Select 3

- [ ] [MATH: x ≤ 1]
- [ ] [MATH: x ≥ 1]

---

**Question 4**

Complete the following reasoning about the set $S = (- ∞, 1)$:

The value $-99$ is not the minimum of the set $S$ because $\underline{\hspace{2cm}}$ and yet $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] [MATH: − 98 ∉ S]
- [ ] [MATH: − 100 ∉ S]
- [ ] [MATH: − 100 ∈ S]
- [ ] [MATH: − 98 ∈ S]

#### Select 2

- [ ] [MATH: − 100 < − 99]
- [ ] [MATH: − 98 > − 99]

---

<a id="maximums-and-minimums-in-more-complex-settings"></a>
## Maximums and Minimums in More Complex Settings

**Example:** What are the maximum and minimum values of the set $A = \{n \in \mathbb{Z} \mid \: 3 \lt n \leq 8 \}$?

**Explanation**

Recall that

- $x_{\max}$ is the maximum of a set $S$ if $x_{\max} \in S$ and $x \leq x_{\max}$ for all $x \in S$, and
- $x_{\min}$ is the minimum of a set $S$ if $x_{\min} \in S$ and $x_{\min} \leq x$ for all $x \in S$.

Notice that the elements of the set can be listed as

$$
A = \{4, \: 5, \: 6, \: 7, 8\}
$$

Therefore, the minimum of the set equals $4$, while the maximum of the set equals $8$.

---

**Question 5**

What are the minimum and maximum of the set $R_{0}^{-}$?

The set has no $\underline{\hspace{2cm}}$, while the $\underline{\hspace{2cm}}$ is $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] minimum
- [ ] maximum

#### Select 2

- [ ] maximum
- [ ] minimum

#### Select 3

- [ ] [MATH: 1]
- [ ] [MATH: − 1]
- [ ] [MATH: 0]
- [ ] [MATH: − ∞]
- [ ] [MATH: ∞]

---

**Question 6**

What are the minimum and maximum of the set $A = {n ∈ Z∣5 < n \le 9}$?

The minimum of the set $\underline{\hspace{2cm}}$, while the maximum of the set $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] does not exist
- [ ] equals [MATH: 5]
- [ ] equals [MATH: 9]
- [ ] equals [MATH: 6]

#### Select 2

- [ ] equals [MATH: 6]
- [ ] equals [MATH: 5]
- [ ] does not exist
- [ ] equals [MATH: 9]

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/MOP/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/MOP/0. Table of Contents/TOC|Table of Contents]]
