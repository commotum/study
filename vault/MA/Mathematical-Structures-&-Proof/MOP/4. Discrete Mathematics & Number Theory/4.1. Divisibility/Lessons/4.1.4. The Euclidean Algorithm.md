# The Euclidean Algorithm

<!--
lesson-id: 2676
topic-code: MOP.4.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing the Greatest Common Divisor of Two Three-Digit Numbers](#computing-the-greatest-common-divisor-of-two-three-digit-numbers)
- [Why Does the Euclidean Algorithm Work?](#why-does-the-euclidean-algorithm-work)
- [Computing the Greatest Common Divisor of Larger Numbers](#computing-the-greatest-common-divisor-of-larger-numbers)
- [Determining Whether Two Integers are Coprime](#determining-whether-two-integers-are-coprime)

## Prerequisites

- [The Division Algorithm](<4.1.3. The Division Algorithm.md>)

---

<a id="introduction"></a>
## Introduction

The **greatest common divisor** (or $\textrm{gcd}$) of two integers is the largest positive integer that divides both of the integers. For example,

$$
\textrm{gcd}(48,18) = 6
$$

because $6$ is the largest integer that divides both $48$ and $18$.

We often wish to compute the greatest common divisor of two large numbers. Doing this by inspection is hard. Another method is to use prime factorization, but this is also difficult and inefficient for large numbers.

Another method, known as the **Euclidian algorithm** is very efficient, despite being over $2\,000$ years old.

Let's demonstrate how to compute $\textrm{gcd}(48,18)$ using the Euclidian algorithm. The first step is to apply the division algorithm and write down

$$
48 = 18q + r
$$

When we do, we get

$$
48 = {\color{blue}{18}}(2) + {\color{red}12}
$$

We ignore the quotient $2$ for now and focus on the ${\color{blue}{18}}$ (the smaller of our two numbers) and the remainder ${\color{red}12}$. We apply the division algorithm again to these two numbers, and this gives

$$
18 = {\color{blue}{12}}(1) + {\color{red}6}
$$

Applying the division algorithm to the number ${\color{blue}{12}}$ and the remainder ${\color{red}6}$ gives

$$
12 = {\color{blue}{6}}(2) + {\color{red}0}
$$

We have now reached a remainder of ${\color{red}0}$, which means that we are done.

In practice, we normally write the steps underneath one another, like so.

$$
\begin{aligned}
48 &= \begin{vmatrix}18(2) & +\end{vmatrix}12 \\
↙\begin{vmatrix}↙ \\ 18 &= \end{vmatrix}12(1)\begin{vmatrix}+ & 6 \\ ↙ & ↙ \\ 12|= & 6(2)\end{vmatrix}+ \mid 0
\end{aligned}
$$

The last positive remainder is ${\color{red}6}$. The Euclidean algorithm states that this is the greatest common divisor that we seek. Therefore, we conclude that

$$
\textrm{gcd}(48,18) = {\color{red}6}
$$

Finally, we note that two integers $a$ and $b$ are **coprime** if

$$
\gcd(a,b) = 1
$$

---

<a id="computing-the-greatest-common-divisor-of-two-three-digit-numbers"></a>
## Computing the Greatest Common Divisor of Two Three-Digit Numbers

**Example:** Find the greatest common divisor of $713$ and $322$.

**Explanation**

We use the Euclidean algorithm, as follows:

$$
\begin{aligned}
713 &= \begin{vmatrix}322(2) & +\end{vmatrix}69 \\
↙\begin{vmatrix}↙ \\ 322 &= \end{vmatrix}69(4)\begin{vmatrix}+ & 46 \\ ↙ & ↙ \\ 69|= & 46(1)\end{vmatrix}+\begin{vmatrix}23 \\ ↙\end{vmatrix}↙ \\
46 &= \begin{vmatrix}23(2) & +\end{vmatrix}0
\end{aligned}
$$

The last positive remainder is ${\color{red}23}$. Therefore, we conclude that the greatest common divisor of $713$ and $322$ is $23$. Symbolically, we write

$$
\textrm{gcd}(713, 322) = 23
$$

---

**Question 1:**

```quiz
type: radio
id: ma-77116
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the greatest common divisor of $296$ and $185$.
options:
- id: a
  content: |-
    $33$
- id: b
  content: |-
    $37$
- id: c
  content: |-
    $43$
- id: d
  content: |-
    $31$
- id: e
  content: |-
    $27$
```

---

**Question 2:**

```quiz
type: radio
id: ma-77108
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the greatest common divisor of $464$ and $208$.
options:
- id: a
  content: |-
    $22$
- id: b
  content: |-
    $8$
- id: c
  content: |-
    $14$
- id: d
  content: |-
    $18$
- id: e
  content: |-
    $16$
```

---

<a id="why-does-the-euclidean-algorithm-work"></a>
## Why Does the Euclidean Algorithm Work?

We now return to the question of why the Euclidean algorithm works. To help us to understand, let's consider once more how to compute $\textrm{gcd}(48,18)$. Using the algorithm, we get the following:

$$
\begin{aligned}
48 &= \begin{vmatrix}18(2) & +\end{vmatrix}12 \\
↙\begin{vmatrix}↙ \\ 18 &= \end{vmatrix}12(1)\begin{vmatrix}+ & 6 \\ ↙ & ↙ \\ 12|= & 6(2)\end{vmatrix}+ \mid 0
\end{aligned}
$$

Carrying out the process gives the following pairs of integers:

$$
48,18\quad \longrightarrow\quad 18,12 \quad \longrightarrow \quad 12,6
$$

We claim that $\textrm{gcd}(48,18)$ must be the same as $\textrm{gcd}(18,12)$. To see why, notice that we used the division algorithm to write

$$
48 = 18(2) + 12
$$

If $48$ and $18$ are divisible by some integer $n$, then $n$ must also divide $18(2)$. This means that $n$ must divide $48-18(2)$, i.e., $n$ must divide $12$. So any number $n$ that divides both $48$ and $18$ must also divide both $18$ and $12$.

Repeating the same argument, we are able to conclude that

$$
\textrm{gcd}(18,12) = \textrm{gcd}(12,6)
$$

Since

$$
\textrm{gcd}(12,6) = 6
$$

we conclude that

$$
\textrm{gcd}(48,18) = \textrm{gcd}(18,12) = \textrm{gcd}(12,6) = 6
$$

In this example, the algorithm concluded in three steps, but the argument can be extended to many steps.

---

<a id="computing-the-greatest-common-divisor-of-larger-numbers"></a>
## Computing the Greatest Common Divisor of Larger Numbers

**Example:** Find the greatest common divisor of $1\,036$ and $555$.

**Explanation**

We use the Euclidean algorithm, as follows:

$$
\begin{aligned}
1036 &= \begin{vmatrix}555(1) & +\end{vmatrix}481 \\
↙\begin{vmatrix}↙ \\ 555 &= \end{vmatrix}481(1)\begin{vmatrix}+ & 74 \\ ↙ & ↙ \\ 481|= & 74(6)\end{vmatrix}+\begin{vmatrix}37 \\ ↙\end{vmatrix}↙ \\
74 &= \begin{vmatrix}37(2) & +\end{vmatrix}0
\end{aligned}
$$

The last positive remainder is ${\color{red}37}$. Therefore, we conclude that the greatest common divisor of $1\,036$ and $555$ is $37$. Symbolically, we write

$$
\textrm{gcd}(1\,036, 555) = 37
$$

---

**Question 3:**

```quiz
type: radio
id: ma-79642
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the greatest common divisor of $3523$ and $1521$.
options:
- id: a
  content: |-
    $26$
- id: b
  content: |-
    $7$
- id: c
  content: |-
    $13$
- id: d
  content: |-
    $3$
- id: e
  content: |-
    $15$
```

---

**Question 4:**

```quiz
type: radio
id: ma-79627
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the greatest common divisor of $1248$ and $816$.
options:
- id: a
  content: |-
    $36$
- id: b
  content: |-
    $34$
- id: c
  content: |-
    $24$
- id: d
  content: |-
    $48$
- id: e
  content: |-
    $52$
```

---

<a id="determining-whether-two-integers-are-coprime"></a>
## Determining Whether Two Integers are Coprime

**Example:** Which of the following statements are true?

1. $\textrm{gcd}(651, 574) = 7$
2. $\textrm{gcd}(651, 574) = 1$
3. $651$ and $574$ are coprime

**Explanation**

Recall that two integers are coprime if their greatest common divisor is $1$.

With that in mind, let's examine our statements in turn.

- Statement I is true, while statement II is false. Indeed, let's use the Euclidean algorithm, as follows:
$651|=\begin{vmatrix}574 \cdot 1 & +\end{vmatrix}77; ↙\begin{vmatrix}↙ \\ 574|=\end{vmatrix}77 \cdot 7\begin{vmatrix}+ & 35 \\ ↙ & ↙ \\ 77|= & 35 \cdot 2\end{vmatrix}+\begin{vmatrix}7 \\ ↙\end{vmatrix}↙; 35|=\begin{vmatrix}7 \cdot 5 & +\end{vmatrix}0$
The last positive remainder is ${\color{blue}7}$. So, the greatest common divisor of $651$ and $574$ is $7$:
$\textrm{gcd}(651, 574) = 7$.
- Statement III is false. Since $\textrm{gcd}(651, 574) > 1$, the numbers are not coprime.

Therefore, the correct answer is "I only."

---

**Question 5**

```quiz
type: radio
id: ma-98404
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following statements are true?

  1. $gcd(888, 205) = 7$
  2. $gcd(888, 205) = 1$
  3. $888$ and $205$ are coprime
options:
- id: a
  content: |-
    II only
- id: b
  content: |-
    I and III only
- id: c
  content: |-
    I only
- id: d
  content: |-
    II and III only
- id: e
  content: |-
    III only
```

---

**Question 6**

```quiz
type: radio
id: ma-98471
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following statements are true?

  1. $gcd(6021, 4023) = 1$
  2. $gcd(6021, 4023) = 27$
  3. $6021$ and $4023$ are not coprime
options:
- id: a
  content: |-
    I only
- id: b
  content: |-
    I and III only
- id: c
  content: |-
    II and III only
- id: d
  content: |-
    III only
- id: e
  content: |-
    II only
```

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/MOP/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/MOP/0. Table of Contents/TOC|Table of Contents]]
