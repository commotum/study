# Additive Inverses Modulo N

<!--
lesson-id: 2735
topic-code: MOP.6.2.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing Additive Inverses Modulo N](#computing-additive-inverses-modulo-n)
- [Finding the Additive Inverse of a Number](#finding-the-additive-inverse-of-a-number)
- [Finding the Additive Inverse of a Larger Number](#finding-the-additive-inverse-of-a-larger-number)
- [Finding the Additive Inverse of a Negative Number](#finding-the-additive-inverse-of-a-negative-number)

## Prerequisites

- [Modular Residues](<../../6.1. Modular Congruence/Lessons/6.1.3. Modular Residues.md>)

---

<a id="introduction"></a>
## Introduction

The **additive inverse** of $a$ modulo $n$ is the integer

$$
0 \leq x \lt n
$$

such that

$$
x + a \equiv 0 \quad (\textrm{mod} \: n)
$$

For example, the integer $7$ is the additive inverse of $5$ modulo $12$ since
$7 + 5≡12≡0(mod12)$.

We can visualize this using a clock diagram.

![](<../Source/6.2.1. Additive Inverses Modulo N/Images/23cae19e874713ddc413f2f777a4997c.png>)

On the other hand, the integer $6$ is *not* the additive inverse of $5$ modulo $12$ since $6 + 5 \equiv 11\not\equiv 0 \, (\textrm{mod} \: 12)$.

In general, we have the following theorem:

> *For every integer, there exists a unique additive inverse for a given modulus $n$.*

Note the following:

- The word "unique" in the statement above means with respect to the inequality $0 \leq x \lt n$.
- It's important to know that any integer summed with an integer congruent to its inverse will also give zero. For example, $x=7$ is the additive inverse of $5$ modulo $12$. But since $19\equiv 7\:(\textrm{mod}\: 12)$, we have that the sum of $5$ and $19$ is also congruent to $0{:}$
$19 + 5≡24≡2 \cdot 12≡0(mod12)$

Let's see some more examples.

---

<a id="computing-additive-inverses-modulo-n"></a>
## Computing Additive Inverses Modulo N

**Example:** Is the following statement true or false?

> The integer $21$ is the additive inverse of $14$ modulo $35$.

**Explanation**

The additive inverse of $14$ modulo $35$ is the integer

$$
0 \leq x \lt 35
$$

such that

$$
x + 14 \equiv 0 \quad (\textrm{mod}\:35)
$$

Now, notice that when we add our two numbers, we get a number that is congruent to zero modulo $35{:}$

$$
21 + 14 \mid ≡35\begin{vmatrix}(mod35) \\ ≡0\end{vmatrix}(mod35)
$$

Therefore, the statement is true.

---

**Question 1**

Select the correct options in the reasoning below.

The integer $17$
$\underline{\hspace{2cm}}$
the additive inverse of $12$ modulo $29$ since $17 + 12$ $\underline{\hspace{2cm}}$ $(mod29)$.

#### Select 1

- [ ] is not
- [ ] is

#### Select 2

- [ ] [MATH: ≢1]
- [ ] [MATH: ≢0]
- [ ] [MATH: ≡1]
- [ ] [MATH: ≡0]

---

**Question 2**

Select the correct options in the reasoning below.

The integer $21$
$\underline{\hspace{2cm}}$
the additive inverse of $15$ modulo $34$ since $21 + 15$ $\underline{\hspace{2cm}}$ $(mod34)$.

#### Select 1

- [ ] is
- [ ] is not

#### Select 2

- [ ] [MATH: ≢0]
- [ ] [MATH: ≡0]
- [ ] [MATH: ≢1]
- [ ] [MATH: ≡1]

---

<a id="finding-the-additive-inverse-of-a-number"></a>
## Finding the Additive Inverse of a Number

**Example:** Find the additive inverse of $17$ modulo $26$.

**Explanation**

The additive inverse of $17$ modulo $26$ is the integer

$$
0 \leq x \lt 26
$$

such that

$$
x + 17 \equiv 0 \quad (\textrm{mod}\:26)
$$

Notice that $-17 + 17 \equiv 0$, so $x\equiv -17\:(\textrm{mod}\:26)$. To write this in the required form, we use the following sequence of congruences:

$$
x \mid ≡ - 17\begin{vmatrix}(mod26) \\ ≡0 - 17\end{vmatrix}(mod26); ≡26 - 17\begin{vmatrix}(mod26) \\ ≡9\end{vmatrix}(mod26)
$$

Therefore, the additive inverse of the element $17$ modulo $26$ is $\bbox[3pt,Gainsboro]{\color{blue}9}$.

We can verify this result as follows:
$9 + 17 \equiv 26 \equiv 0\:\:{\color{green}{\checkmark}} \quad (\textrm{mod}\:26)$

**Note:** The additive inverse of $a$ modulo $n$ is simply the residue of $-a$ modulo $n$, i.e. $-a \: \textrm{mod} \: n$.

---

**Question 3**

Expressed as a nonnegative integer smaller than $24$, the additive inverse of $15$ modulo $24$ is $\underline{\hspace{1.5cm}}$.

---

**Question 4**

Expressed as a nonnegative integer smaller than $18$, the additive inverse of $11$ modulo $18$ is $\underline{\hspace{1.5cm}}$.

---

<a id="finding-the-additive-inverse-of-a-larger-number"></a>
## Finding the Additive Inverse of a Larger Number

**Example:** Find the additive inverse of $23$ modulo $15$.

**Explanation**

The additive inverse of $23$ modulo $15$ is the integer

$$
0 \leq x \lt 15
$$

such that

$$
x + 23 \equiv 0 \quad (\textrm{mod}\:15)
$$

First, we note that $23\equiv 8\:(\textrm{mod}\:15){:}$
$23 \mid ≡1 \cdot 15 + 8\begin{vmatrix}(mod15) \\ ≡1 \cdot 0 + 8\end{vmatrix}(mod15); ≡8 \mid (mod15)$

Also, note that $-8 + 8 \equiv 0$, so $x\equiv -8\:(\textrm{mod}\:15)$. To write this in the required form, we use the following sequence of congruences:

$$
x \mid ≡ - 8\begin{vmatrix}(mod15) \\ ≡0 - 8\end{vmatrix}(mod15); ≡15 - 8\begin{vmatrix}(mod15) \\ ≡7\end{vmatrix}(mod15)
$$

Therefore, the additive inverse of the element $23$ modulo $15$ is $\bbox[3pt,Gainsboro]{\color{blue}7}$.

We can verify this result as follows:
$23 + 7≡30≡2 \cdot 15≡0✓(mod15)$

**Note:** In general, the additive inverse of $a$ modulo $n$ is simply the residue of $-a$ modulo $n$, i.e. $-a \: \textrm{mod} \: n$.

---

**Question 5**

Expressed as a nonnegative integer smaller than $32$, the additive inverse of $32$ modulo $32$ is $\underline{\hspace{1.5cm}}$.

---

**Question 6**

Expressed as a nonnegative integer smaller than $16$, the additive inverse of $45$ modulo $16$ is $\underline{\hspace{1.5cm}}$.

---

<a id="finding-the-additive-inverse-of-a-negative-number"></a>
## Finding the Additive Inverse of a Negative Number

**Example:** Find the additive inverse of $-43$ modulo $15$.

**Explanation**

The additive inverse of $-43$ modulo $15$ is the integer

$$
0 \leq x \lt 15
$$

such that

$$
x + (-43) \equiv 0 \quad (\textrm{mod}\:15)
$$

First, we note that $-43\equiv 2\:(\textrm{mod}\:15){:}$
$-43 \mid ≡(-3) \cdot 15 + 2\begin{vmatrix}(mod15) \\ ≡(-3) \cdot 0 + 2\end{vmatrix}(mod15); ≡2 \mid (mod15)$

Also, note that $-2 + 2 \equiv 0$, so $x\equiv -2\:(\textrm{mod}\:15)$. To write this in the required form, we use the following sequence of congruences:

$$
x \mid ≡ - 2\begin{vmatrix}(mod15) \\ ≡0 - 2\end{vmatrix}(mod15); ≡15 - 2\begin{vmatrix}(mod15) \\ ≡13\end{vmatrix}(mod15)
$$

Therefore, the additive inverse of the element $-43$ modulo $15$ is $\bbox[3pt,Gainsboro]{\color{blue}13}$.

We can verify this result as follows:
$-43 + 13≡ - 30≡(-2) \cdot 15≡0✓(mod15)$

**Note:** In general, the additive inverse of $a$ modulo $n$ is simply the residue of $-a$ modulo $n$, i.e. $-a \: \textrm{mod} \: n$.

---

**Question 7**

Expressed as a nonnegative integer smaller than $12$, the additive inverse of $-9$ modulo $12$ is $\underline{\hspace{1.5cm}}$.

---

**Question 8**

Expressed as a nonnegative integer smaller than $13$, the additive inverse of $-50$ modulo $13$ is $\underline{\hspace{1.5cm}}$.

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/MOP/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/MOP/0. Table of Contents/TOC|Table of Contents]]
