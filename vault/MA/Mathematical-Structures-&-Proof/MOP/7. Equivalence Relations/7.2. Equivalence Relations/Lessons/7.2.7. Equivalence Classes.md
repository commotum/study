# Equivalence Classes

<!--
lesson-id: 2642
topic-code: MOP.7.2.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Equivalence Classes](#equivalence-classes)
- [Identifying Elements Belonging to an Equivalence Class](#identifying-elements-belonging-to-an-equivalence-class)
- [Identifying Equivalence Classes Containing a Given Element](#identifying-equivalence-classes-containing-a-given-element)
- [Properties of Equivalence Classes](#properties-of-equivalence-classes)
- [Counting Equivalence Classes](#counting-equivalence-classes)

## Prerequisites

- [The Cyclic Property of the Imaginary Unit](<../../../../AG2/1. Number Systems/1.2. Introduction to Complex Numbers/Lessons/1.2.7. The Cyclic Property of the Imaginary Unit.md>)
- [Equivalence Relations](<7.2.4. Equivalence Relations.md>)
- [Residue Classes](<7.2.6. Residue Classes.md>)

---

<a id="introduction"></a>
## Introduction

Recall that the residue class of $a$ modulo $n$, denoted $[a]_n$, is the set of integers that are congruent to $a$ modulo $n$.

$$
[a]_n = \big\{x \in \mathbb{Z} \: \mid \: x \equiv a \, \: (\text{mod}\:n) \big\}
$$

For example:

- The residue class of $0$ modulo $2$ corresponds to the even integers because all even integers have a remainder of $0$ when divided by $2$.
$[0]_{2} = {0, ± 2, ± 4, …}$
- On the other hand, the residue class of $1$ modulo $2$ corresponds to the odd integers because all odd integers have a remainder of $1$ when divided by $2$.
$[1]_{2} = {± 1, ± 3, ± 5, …}$

Notice that the residue classes $[0]_2$ and $[1]_2$ *partition* the integers, meaning that any integer belongs either to $[0]_2$ or $[1]_2$, but never both.

![](<../Source/7.2.7. Equivalence Classes/Images/2f64e034833908f141d8fff951fdae86.png>)

---

<a id="equivalence-classes"></a>
## Equivalence Classes

A residue class is a particular type of a more general object called an **equivalence class.**

Given an equivalence relation $\sim$ on a set $A$, the equivalence class of an element $a \in A$, denoted $[a]$, consists of all the elements of $A$ that are equivalent to $a$.

$$
[a] = \{x \in A \, \mid \, x \sim a \}
$$

Since any equivalence relation is symmetric, we can also write

$$
[a] = \{x \in A \, \mid \, a \sim x \}
$$

For example, consider the equivalence relation $\sim$ on $\mathbb{Z}$ defined by $a \sim b$ if and only if

$$
\begin{aligned}
\begin{vmatrix}a &= & b\end{vmatrix}
\end{aligned}
$$

Under this relation, two integers are equivalent if they have the same absolute value. The first few equivalence classes are as follows:

$$
\begin{aligned}
[0] &= {a ∈ Z∣a∼0} \\
= {a ∈ Z∣\begin{vmatrix}a &= & 0\end{vmatrix}} \\
= {a ∈ Z∣\begin{vmatrix}a &= 0} \\ = {0} \\ [1]|= {a ∈ Z∣a∼1} \\ = {a ∈ Z∣\begin{vmatrix}a|= & 1\end{vmatrix}} \\ = {a ∈ Z∣ \mid a|= 1} \\ = {± 1} \\ [2]|= {a ∈ Z∣a∼2} \\ = {a ∈ Z∣\begin{vmatrix}a|= & 2\end{vmatrix}} \\ = {a ∈ Z∣ \mid a|= 2} \\ = {± 2} \\ [3]|= {a ∈ Z∣a∼3} \\ = {a ∈ Z∣\begin{vmatrix}a|= & 3\end{vmatrix}} \\ = {a ∈ Z∣\end{vmatrix}a|= 3} \\
&= {± 3} \\
&= ⋮
\end{aligned}
$$

Note the following:

- Equivalence relations can have a finite or infinite number of equivalence classes. This particular equivalence relation has an infinite number of equivalence classes.
- For equivalence classes containing more than one element, we can use any element to represent the class. For example, $[1]$ and $[-1]$ represent the same equivalence class.

---

<a id="identifying-elements-belonging-to-an-equivalence-class"></a>
## Identifying Elements Belonging to an Equivalence Class

**Example:** Consider the equivalence relation $\sim$ on $\mathbb{Z}$, defined by $x \sim y$ if $12 \, \mid \, (x-y)$. Which of the following are in the equivalence class $[9]$?

1. $9$
2. $21$
3. $22$

**Explanation**

The equivalence class $[9]$ consists of the elements that are equivalent to $9$ under the given equivalence relation. So, we have

$$
\begin{aligned}
[9] &= {x ∈ Z: x∼9} \\
&= {x ∈ Z: 12 \mid (x - 9)} \\
&= {x ∈ Z: x = 9 + 12k, k ∈ Z} \\
&= {…,-15,-3, 9, 21, 33, …}
\end{aligned}
$$

We see that $9$ and $21$ are in $[9]$, while $22$ is not.

Therefore, the correct answer is "I and II only."

---

**Question 1**

Consider the equivalence relation $∼$ on $C$, defined by $x∼y$ if $x^{2} = y^{2}$. Which of the following are in the equivalence class $[- 1]$?

1. $\textrm{i}$
2. $1$
3. $-1$

- [ ] A. II and III only
- [ ] B. I and II only
- [ ] C. II only
- [ ] D. III only
- [ ] E. I and III only

---

**Question 2**

Consider the equivalence relation $∼$ on $R$, defined by $x∼y$ if $\sin x = \sin y$. Which of the following are in the equivalence class $[- π]$?

1. $0$
2. $-\frac{π}{2}$
3. $π$

- [ ] A. I only
- [ ] B. I and III only
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. II only

---

<a id="identifying-equivalence-classes-containing-a-given-element"></a>
## Identifying Equivalence Classes Containing a Given Element

**Example:** Consider the equivalence relation $\sim$ on $\mathbb{R}$, defined by $x \sim y$ if $\sin x = \sin y$. Which of the following equivalence classes contain the element $\dfrac{\pi}{2}$?

1. $\left[\dfrac{3\pi}{2}\right]$
2. $\left[-\dfrac{\pi}{2}\right]$
3. $\left[-\dfrac{3\pi}{2}\right]$

**Explanation**

We have

$$
\dfrac{\pi}{2} \in [x]
$$

if

$$
\dfrac{\pi}{2} \sim x
$$

Using the definition of the given equivalence relation, we require

$$
\begin{aligned}
\sin (\frac{π}{2}) &= \sin x \\
1 &= \sin x \\
\sin x &= 1
\end{aligned}
$$

Let's now consider each of the given equivalence classes.

- $\dfrac{\pi}{2} \not\in \left[\dfrac{3\pi}{2}\right]$ because $\sin\left(\dfrac{3\pi}{2}\right) =-1\neq 1 \quad \color{red}\times$
- $\dfrac{\pi}{2} \not\in \left[-\dfrac{\pi}{2}\right]$ because $\sin\left(-\dfrac{\pi}{2}\right) =-1\neq 1 \quad \color{red}\times$
- $\dfrac{\pi}{2} \in \left[-\dfrac{3\pi}{2}\right]$ because $\sin \left(-\dfrac{3\pi}{2}\right) = 1 \quad \color{green}\checkmark$

Therefore, the correct answer is "III only".

---

**Question 3**

Consider the equivalence relation $∼$ on $C$, defined by $x∼y$ if $x^{4} = y^{4}$. Which of the following equivalence classes contain the element $\textrm{i}$?

1. $[- 1]$
2. $[- \textrm{i}]$
3. $[2\textrm{i}]$

- [ ] A. II only
- [ ] B. I and II only
- [ ] C. II and III only
- [ ] D. I, II, and III
- [ ] E. I and III only

---

**Question 4**

Consider the equivalence relation $∼$ on $Z$, defined by $x∼y$ if $5 \mid (x - y)$. Which of the following equivalence classes contain the element $-4$?

1. $[- 14]$
2. $[11]$
3. $[14]$

- [ ] A. I only
- [ ] B. I and III only
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. I, II, and III

---

<a id="properties-of-equivalence-classes"></a>
## Properties of Equivalence Classes

The properties of residue classes generalize to equivalence classes. Let's list some of them:

- If $a \sim b$ then $[a]=[b]$. This allows us to pick any element of a class as its representative.
- On the other hand, two distinct equivalence classes have an empty intersection:
$a \not\sim b \qquad\Longrightarrow\qquad [a] \cap [b] = \emptyset$
- The residue classes form a partition of $A$. Moreover, $A$ can be constructed from the (disjoint) union of all distinct equivalence classes:
$A = \bigcup_{i\in I}\,[a]_i$
where $[a]_i$ denotes the $i$'th residue class and $I$ is an index set spanning all possible classes.

Consider once again the equivalence relation $\sim$ on $\mathbb{Z}$ defined by $a \sim b$ if and only if

$$
\begin{aligned}
\begin{vmatrix}a &= & b\end{vmatrix}
\end{aligned}
$$

We found that the equivalence classes were as follows:

$$
\begin{aligned}
[0] &= {0} \\
[1] &= {± 1} \\
[2] &= {± 2} \\
[3] &= {± 3} \\
&= ⋮
\end{aligned}
$$

These equivalence classes form a partition of the set of integers:
$\big\{[0], [1], [2],\ldots\big\}$

Moreover, the integers can be constructed from the disjoint union of these classes:

$$
\begin{aligned}
Z &= [0]∪[1]∪[2]∪⋯ \\
&= {0}∪{± 1}∪{± 2}∪⋯
\end{aligned}
$$

---

<a id="counting-equivalence-classes"></a>
## Counting Equivalence Classes

**Example:** Consider the equivalence relation $\sim$ on the set $A = \{0, 1, 2, \ldots, 9\}$, defined by $a \sim b$ if $\textrm{i}^a = \textrm{i}^b$, where $\textrm{i}$ is the imaginary unit in $\mathbb{C}$. How many distinct equivalence classes does $\sim$ partition $A$ into?

**Explanation**

The equivalence classes of $\sim$ partition $A$, meaning that any element $a \in A$ belongs to exactly one distinct equivalence class.

So, we compute equivalence classes until all the elements of $A$ have been placed into an equivalence class.

$$
\begin{aligned}
[0] &= {a ∈ A∣a∼0} \\
&= {a ∈ A∣\textrm{i}^{a} = \textrm{i}^{0}} \\
&= {a ∈ A∣\textrm{i}^{a} = 1} \\
&= {0, 4, 8} \\
[1] &= {a ∈ A∣a∼1} \\
&= {a ∈ A∣\textrm{i}^{a} = \textrm{i}^{1}} \\
&= {a ∈ A∣\textrm{i}^{a} = \textrm{i}} \\
&= {1, 5, 9} \\
[2] &= {a ∈ A∣a∼2} \\
&= {a ∈ A∣\textrm{i}^{a} = \textrm{i}^{2}} \\
&= {a ∈ A∣\textrm{i}^{a} =-1} \\
&= {2, 6} \\
[3] &= {a ∈ A∣a∼3} \\
&= {a ∈ A∣\textrm{i}^{a} = \textrm{i}^{3}} \\
&= {a ∈ A∣\textrm{i}^{a} =-\textrm{i}} \\
&= {3, 7}
\end{aligned}
$$

Each element of $A$ has been placed into an equivalence class, so we are done.

We have $4$ distinct equivalence classes:

$$
[0] = \{0, 4, 8 \}, \quad [1] = \{1, 5, 9 \}, \quad [2] = \{2, 6 \}, \quad [3] = \{3, 7 \}
$$

---

**Question 5:** Consider the equivalence relation $∼$ on the set $A = {0, 1, 2, …, 9}$, defined by $a∼b$ if $(-1)^{a} = (-1)^{b}$. How many distinct equivalence classes does $∼$ partition $A$ into?

- [ ] A. $1$
- [ ] B. $5$
- [ ] C. $2$
- [ ] D. $4$
- [ ] E. $3$

---

**Question 6:** Consider the equivalence relation $∼$ on the set $A = {0, 1, 2, …, 9}$, defined by $a∼b$ if $\cos (\frac{πa}{2}) = \cos (\frac{πb}{2})$. How many distinct equivalence classes does $∼$ partition $A$ into?

- [ ] A. $3$
- [ ] B. $5$
- [ ] C. $2$
- [ ] D. $1$
- [ ] E. $4$

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/MOP/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/MOP/0. Table of Contents/TOC|Table of Contents]]
