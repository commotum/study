# Nondeterministic Finite Automata

<!--
lesson-id: 3798
topic-code: DSM.8.2.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Constructing a Transition Table Given a Diagram](#constructing-a-transition-table-given-a-diagram)
- [Drawing a Diagram Given a Transition Table](#drawing-a-diagram-given-a-transition-table)
- [Extending the Transition Function to Strings](#extending-the-transition-function-to-strings)
- [Applying a Transition Function to a String](#applying-a-transition-function-to-a-string)
- [The Language of a NFA](#the-language-of-a-nfa)
- [Identifying Strings Accepted by an NFA](#identifying-strings-accepted-by-an-nfa)

## Prerequisites

- [Power Sets](<../../../../MOP/2. Set Theory/2.2. Subsets/Lessons/2.2.2. Power Sets.md>)
- [Processing Strings Using Deterministic Finite Automata](<8.2.2. Processing Strings Using Deterministic Finite Automata.md>)

---

<a id="introduction"></a>
## Introduction

Recall that a deterministic finite automaton (DFA) is a specific type of acceptor transitioning between states based on predefined rules determined by the input symbols.

Another type of acceptor, similar to a DFA, is a **nondeterministic finite automaton** (NFA).
The difference is that unlike a DFA, where each state has exactly one transition for every input symbol, an NFA can have multiple or no transitions.

More formally, an NFA is a five-tuple of the form

$$
A = (Q, \Sigma, \delta, q_0, F)
$$

where the components are as follows:

- $Q$ is nonempty finite set of **states**. These are all the possible configurations the NFA can be in.
- $\Sigma$ is a nonempty finite set of **input symbols**. The NFA can process this "alphabet" of symbols as inputs.
- $\delta$ is a **transition function** of the form $\delta: Q \times \Sigma \to 2^Q$, where $2^Q$ denotes the power set of $Q$. It takes a state and an input symbol and returns a *set of states*. This function determines how the NFA moves from one state to another based on a given input symbol.
- $q_0 \in Q$ is a **start state** (or **initial state**). This is the state in which the NFA begins.
- $F \subseteq Q$ is a set of **final** (or **accepting**) **states**.

We can visualize an NFA as a directed graph and write the transition function in a table, just like for a DFA.
An example of the transition table of an NFA is given below.

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! q$ | $\{r\}$ | $\emptyset$ |
| $\ast \: r$ | $\{q,r\}$ | $\{r\}$ |

Notice that each state can have no (empty set $\emptyset$), one, or multiple transitions for every input symbol. Now, let's construct a diagram representing the automaton given by this transition table.

From the table, we determine that the automaton has the following components:

- the set of states $Q=\{q,r\}$, (given by the rows)
- the set of input symbols $\Sigma=\{0,1\}$, (given by the columns)
- the start state $q$ (labeled by $\to$), and
- the set of final states $F=\{r\}$ (labeled by $\ast$).

First, let's draw the states of our automaton.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/0b0b31de1c9e3024a2138b3d506bb044.png>)

According to the transition table, $q$ is the start state, and $r$ is a final state. So, we labeled $q$ with an input arrow and circled $r$ with a double line.

From the transition table, we also get the following:

- Consider row $q$ of the table. Its intersection with column $0$ is $\{r\}$, indicating that $\delta(q,0)=\{r\}$. So, we draw an arrow from $q$ to $r$ labeled $0$.Its intersection with column $1$ is $\emptyset$, indicating that $\delta(q,1)=\emptyset$. So, there are no arrows from $q$ that are labeled with $1$.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/52adb92ce5c174bee03a6c59c316bc05.png>)

- Consider row $r$ of the table. Its intersection with column $0$ is $\{q,r\}$, indicating that $\delta(r,0)=\{q,r\}$. So, we draw an arrow from $q$ to $r$ and an arrow (loop) from $r$ to $r$, both labeled $0$.Its intersection with column $1$ is $\{r\}$, indicating that $\delta(r,1)=\{r\}$. So, we draw an arrow (loop) from $r$ to $r$ labeled $1$.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/fca211810491f35e18a5c496b0d25ec6.png>)

We have gone through every cell in the table and added all the arcs, so our diagram is complete.

**Watch out!** As with DFAs, we draw only one loop from $r$ to $r$ labeled with both $0,1$ instead of two separate loops for each of the symbols $0$ and $1$.

---

<a id="constructing-a-transition-table-given-a-diagram"></a>
## Constructing a Transition Table Given a Diagram

**Example:** The diagram above shows a nondeterministic finite automaton. Construct the transition table corresponding to the given diagram.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/965a85a153779529ea4d3d38e0062d41.png>)

**Explanation**

In our case, the automaton has the following components:

- the set of states $Q=\{p,q,r\}$,
- the set of input symbols $\Sigma=\{0,1\}$,
- the start state $p$, and
- the set of final states $F=\{q,r\}$.

With that in mind, let's determine the transition function.

First, we draw an empty table with a different state written in each row and a different input symbol written in each column:

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ |  |  |
| $\ast \: q$ |  |  |
| $\ast \:r$ |  |  |

Notice that we write $\to$ next to $p$ since it's the start state, and we write $\ast$ next to $q$ and $r$ since they are final states.

Next, we fill in the remaining cells:

- There is an arrow (loop) from $p$ to $p$, labeled $0$. This means that $\delta(p,0)=\{p\}$. So, we write down $\{p\}$ at the intersection of row $p$ and column $0$.
- There is an arrow (loop) from $p$ to $p$, labeled $1$, and there is an arrow from $p$ to $q$, also labeled $1$. This means that $\delta(p,1)=\{p,q\}$. So, we write down $\{p,q\}$ at the intersection of row $p$ and column $1$.
- There are no arrows from $q$ that are labeled with $0$. This means that $\delta(q,0)=\emptyset$. So, we write down $\emptyset$ at the intersection of row $q$ and column $0$.
- There is an arrow (loop) from $q$ to $q$, labeled $1$. This means that $\delta(q)=\{q\}$. So, we write down $\{q\}$ at the intersection of row $q$ and column $1$.
- There is an arrow (loop) from $r$ to $r$, labeled $0$, and there is an arrow from $r$ to $p$, also labeled $0$. This means that $\delta(r,0)=\{r,p\}$. So, we write down $\{r,p\}$ at the intersection of row $r$ and column $0$.
- There are no arrows from $r$ that are labeled with $1$. This means that $\delta(r,1)=\emptyset$. So, we write down $\emptyset$ at the intersection of row $r$ and column $1$.

The transition table corresponding to the given diagram is shown below.

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ | $\bbox[3pt,Gainsboro]{\color{blue}\{p\}}$ | $\bbox[3pt,Gainsboro]{\color{blue}\{p,q\}}$ |
| $\ast \: q$ | $\bbox[3pt,Gainsboro]{\color{blue}\emptyset}$ | $\bbox[3pt,Gainsboro]{\color{blue}\{q\}}$ |
| $\ast\: r$ | $\bbox[3pt,Gainsboro]{\color{blue}\{r,p\}}$ | $\bbox[3pt,Gainsboro]{\color{blue}\emptyset}$ |

---

**Question 1**

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236003.png>)

The diagram above shows a nondeterministic finite automaton. Select the correct options in the corresponding transition table below.

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→ p$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |
| $∗q$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: {q}]
- [ ] [MATH: {p}]
- [ ] [MATH: {p, q}]
- [ ] [MATH: ∅]

#### Select 2

- [ ] [MATH: {q}]
- [ ] [MATH: {p, q}]
- [ ] [MATH: {p}]
- [ ] [MATH: ∅]

#### Select 3

- [ ] [MATH: {p}]
- [ ] [MATH: {q}]
- [ ] [MATH: {p, q}]
- [ ] [MATH: ∅]

#### Select 4

- [ ] [MATH: {q}]
- [ ] [MATH: {p}]
- [ ] [MATH: {p, q}]
- [ ] [MATH: ∅]

---

**Question 2**

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-257902.png>)

The diagram above shows a nondeterministic finite automaton. Select the correct options in the corresponding transition table below.

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→; ∗p$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |
| $∗q$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |
| $r$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: {r, q}]
- [ ] [MATH: {p}]
- [ ] [MATH: {q}]
- [ ] [MATH: ∅]

#### Select 2

- [ ] [MATH: {p}]
- [ ] [MATH: {q}]
- [ ] [MATH: {p, r}]
- [ ] [MATH: ∅]

#### Select 3

- [ ] [MATH: {q}]
- [ ] [MATH: {p}]
- [ ] [MATH: ∅]
- [ ] [MATH: {q, r}]

#### Select 4

- [ ] [MATH: {p}]
- [ ] [MATH: {r}]
- [ ] [MATH: {p, r}]
- [ ] [MATH: ∅]

#### Select 5

- [ ] [MATH: ∅]
- [ ] [MATH: {q}]
- [ ] [MATH: {q, r}]
- [ ] [MATH: {p}]

#### Select 6

- [ ] [MATH: ∅]
- [ ] [MATH: {q}]
- [ ] [MATH: {p, q}]
- [ ] [MATH: {p}]

---

<a id="drawing-a-diagram-given-a-transition-table"></a>
## Drawing a Diagram Given a Transition Table

**Example:** Draw a diagram representing the nondeterministic finite automaton given by the transition table above.

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ | $\{p,q\}$ | $\emptyset$ |
| $q$ | $\{q,r\}$ | $\emptyset$ |
| $\ast \: r$ | $\{r,p\}$ | $\{r\}$ |

**Explanation**

In our case, the automaton has the following components:

- the set of states $Q=\{p,q,r\}$,
- the set of input symbols $\Sigma=\{0,1\}$,
- the start state $p$, and
- the set of final states $F=\{r\}$.

First, let's draw the states of our automaton.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/42033dc2f68be96d8972a25ba5f56b87.png>)

According to the transition table, $p$ is the start state, and $r$ is a final state. So, we labeled $p$ with an input arrow and circled $r$ with a double line.

From the transition table, we also get the following:

- Consider row $p$ of the table. Its intersection with column $0$ is $\{p,q\}$, indicating that $\delta(p,0)=\{p,q\}$. So, we draw an arrow from $p$ to $q$ and an arrow (loop) from $p$ to $p$, both labeled $0$.Its intersection with column $1$ is $\emptyset$, indicating that $\delta(p,1)=\emptyset$. So, there are no arrows from $p$ that are labeled with $1$.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/6942c18e909940c46f82f06cbffe7fff.png>)

- Consider row $q$ of the table. Its intersection with column $0$ is $\{q,r\}$, indicating that $\delta(q,0)=\{q,r\}$. So, we draw an arrow from $q$ to $r$ and an arrow (loop) from $q$ to $q$, both labeled $0$.Its intersection with column $1$ is $\emptyset$, indicating that $\delta(q,1)=\emptyset$. So, there are no arrows from $q$ that are labeled with $1$.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/a31c6db0d135c17f1e686666e237e59c.png>)

- Consider row $r$ of the table. Its intersection with column $0$ is $\{r,p\}$, indicating that $\delta(r,0)=\{r,p\}$. So, we draw an arrow from $r$ to $p$ and an arrow (loop) from $r$ to $r$, both labeled $0$.Its intersection with column $1$ is $\{r\}$, indicating that $\delta(r,1)=\{r\}$. So, we draw an arrow (loop) from $r$ to $r$, labeled $1$.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/0f738ff0524c172ff5fe4e572af97e2b.png>)

We have been through every cell in the table, so our diagram is complete.

---

**Question 3**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→; ∗r$ | ${r, q}$ | ${r}$ |
| $q$ | $∅$ | ${q}$ |

Which of the following diagrams represents the nondeterministic finite automaton given by the transition table above?

- [ ] A. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-258044-a-4.png>)
- [ ] B. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-258044-a-1.png>)
- [ ] C. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-258044-a-5.png>)
- [ ] D. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-258044-a-2.png>)
- [ ] E. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-258044-a-3.png>)

---

**Question 4**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→ p$ | ${p}$ | ${q}$ |
| $q$ | $∅$ | ${r}$ |
| $∗r$ | ${q, r}$ | $∅$ |

Which of the following diagrams represents the nondeterministic finite automaton given by the transition table above?

- [ ] A. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236065-a-5.png>)
- [ ] B. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236065-a-1.png>)
- [ ] C. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236065-a-3.png>)
- [ ] D. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236065-a-2.png>)
- [ ] E. ![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236065-a-4.png>)

---

<a id="extending-the-transition-function-to-strings"></a>
## Extending the Transition Function to Strings

We know that a nondeterministic finite automaton (NFA) is a subtype of acceptors - automata that process a sequence of symbols (a string) and decide whether to "accept" or "reject" it. We'll now explore how an NFA performs this task.

Let

$$
\omega = a \beta
$$

be a string, where $a$ is the first symbol of $\omega$ and $\beta$ is the remainder of $\omega$ after removing the first symbol.
Then, we can extend the transition function $\delta$ of a nondeterministic finite automaton to strings recursively as follows:

$$
\begin{aligned}
δ(q, ε) &= ∅ \\
δ(q, ω) &= ⋃_(s ∈ δ(q, a))δ(s, β)
\end{aligned}
$$

where $\varepsilon$ denotes the empty string (the string containing no symbols) and $q$ is a state of the automaton.

To demonstrate, let's find $\delta(q, 001)$, where $\delta: Q \times \Sigma \to 2^Q$ is the transition function of our previous DFA represented by the diagram below.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/d81ba168a24cd98afa1f7bc16b56624e.png>)

According to the definition of the transition function $\delta$, for

$$
\omega = 001
$$

we obtain the following:

$$
\begin{aligned}
δ(q, 001) &= ⋃_(s ∈ δ(q, 0))δ(s, 01) \\
&= δ(r, 01) \\
&= ⋃_(s ∈ δ(r, 0))δ(s, 1) \\
&= δ(q, 1)∪δ(r, 1) \\
&= ∅∪{r} \\
&= {r}
\end{aligned}
$$

Let's describe what is going on in the computations from a slightly different point of view.

- **Step 1.** The automaton is at the start state $q$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{0} \, 0 \, 1$
From the diagram, $\delta(q,0)= \{r\}$. So, the automaton switches to state $r$ and moves to the next letter of the string.
- **Step 2.** The automaton is at the state $r$ and observes the second letter of the string:
${\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the diagram, $\delta(r,1)= \{q,r\}$. So, the automaton goes to states $q$ and $r$ simultaneously and moves to the next letter of the string.**Step 3.1.** The automaton is at the state $q$ and observes the third letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(q,1)= \emptyset$. So, the automaton is stuck.**Step 3.2.** The automaton is at the state $r$ and observes the third letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(r,1)= \{r\}$. So, the automaton switches to state $r$ and stops since the entire string has been processed.

The diagram below summarizes the process. It notes the state of the DFA and the part of the string left to process at each step.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/5285e35590248c02db681f38cb958965.png>)

The process properly terminates at the state $r$. Therefore,

$$
\delta(q, 001) = \{r\}
$$

---

<a id="applying-a-transition-function-to-a-string"></a>
## Applying a Transition Function to a String

**Example:** Consider the nondeterministic finite automaton represented by the transition table above. Given that $\delta: Q \times \Sigma \to 2^Q$ is the transition function of the automaton, find $\delta(b, 001)$.

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! b$ | $\{a\}$ | $\emptyset$ |
| $\ast \: a$ | $\{a\}$ | $\{a,b\}$ |

**Explanation**

The diagram corresponding to our automaton is shown below.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/9297746d58d2d1ed72ea6589165feb17.png>)

Let

$$
\omega = a \beta
$$

be a finite sequence of symbols (a string), where $a$ is the first symbol of $\omega$ and $\beta$ is the remainder of $\omega$ after removing the first symbol.

Then, we can extend the transition function $\delta$
of the automaton to strings recursively as

$$
\begin{aligned}
δ(q, ε) &= ∅, \\
δ(q, ω) &= ⋃_(s ∈ δ(q, a))δ(s, β)
\end{aligned}
$$

where $\varepsilon$ denotes the empty string (the string containing no symbols) and $q$ is a state of an automaton.

According to the definition of the transition function $\delta$, for

$$
\omega = 001
$$

we obtain the following:

$$
\begin{aligned}
δ(b, 001) &= ⋃_(s ∈ δ(b, 0))δ(s, 01) \\
&= δ(a, 01) \\
&= ⋃_(s ∈ δ(a, 0))δ(s, 1) \\
&= δ(a, 1) \\
&= {a, b}
\end{aligned}
$$

Let's describe what is going on in the computations from a slightly different point of view.

- Step 1. The automaton is at the start state $b$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{0} \, 0 \, 1$
From the diagram, $\delta(b,0)= \{a\}$. So, the automaton switches to state $a$ and moves to the next letter of the string.
- Step 2. The automaton is at the state $a$ and observes the second letter of the string:
${\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the diagram, $\delta(a,0)= \{a\}$. So, the automaton goes to states $a$ and moves to the next letter of the string.
- Step 3. The automaton is at the state $a$ and observes the third letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(a,1)= \{a,b\}$. So the automaton goes to states $a$ and $b$ simultaneously and stops since the entire string has been processed.

We can summarize the process in the diagram below.

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/6047d0dff763819551781ef50478fbd8.png>)

The process properly terminates at the states $a$ and $b$. Therefore,

$$
\delta(b, 001) = \{a,b\}
$$

---

**Question 5**

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236067.png>)

Consider the nondeterministic finite automaton represented by the above diagram. Given that $δ: Q \cdot Σ → 2^{Q}$ is the transition function of the automaton, $δ(p, 011) =$

- [ ] A. $∅$
- [ ] B. ${p, q}$
- [ ] C. ${q}$
- [ ] D. ${q, r}$
- [ ] E. ${p}$

---

**Question 6**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→ p$ | ${p}$ | ${q}$ |
| $∗q$ | ${p, q}$ | $∅$ |

Consider the nondeterministic finite automaton represented by the transition table above. Given that $δ: Q \cdot Σ → 2^{Q}$ is the transition function of the automaton, $δ(p, 101) =$

- [ ] A. ${0}$
- [ ] B. ${q}$
- [ ] C. $∅$
- [ ] D. ${p}$
- [ ] E. ${p, q}$

---

<a id="the-language-of-a-nfa"></a>
## The Language of a NFA

A nondeterministic finite automaton accepts a finite string if, after processing the entire string, the automation ends in one of its final states. Otherwise, the string is rejected. The set of all accepted finite strings is the **language** of the NFA.

For example, consider the three strings $100$, $0101$, and $0110$. Are any of these accepted by the nondeterministic automaton represented by the diagram below?

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/be8827a0b5a5a9a14de81f09e60aaa8a.png>)

To find out, let's apply the extended transition function to our strings.

- Processing the string $100$, we get:

$δ(q, 100)|= ⋃_(s ∈ δ(q, 1))δ(s, 00); = ∅$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/ae9520f83c606b371b8cc1cf6c70e7ac.png>)
The process is "stuck". So, the string $100$ is rejected.

- Processing the string $0101$, we get:

$δ(q, 0101)|= ⋃_(s ∈ δ(q, 0))δ(s, 101); = δ(r, 101); = ⋃_(s ∈ δ(r, 1))δ(s, 01); = δ(r, 01); = ⋃_(s ∈ δ(r, 0))δ(s, 1); = δ(q, 1)∪δ(r, 1); = ∅∪{r}; = {r}$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/d7d3a29f98fe57385463783a51bddb19.png>)
The process terminates at the state $r$. Since $r$ is a final (accepting) state, the string $0101$ is accepted.

- Processing the string $0110$, we get:

$δ(q, 0110)|= ⋃_(s ∈ δ(q, 0))δ(s, 110); = δ(r, 110); = ⋃_(s ∈ δ(r, 1))δ(s, 10); = δ(r, 10); = ⋃_(s ∈ δ(r, 1))δ(s, 0); = δ(r, 0); = {q, r}$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/6a3c1daccafc543fd15f11dd5cf4cc83.png>)
The process is terminated at the states $q$ and $r$. Since $r$ is a final (accepting) state, the string $0110$ is accepted.

The language of this NFA consists of all finite strings of $0$'s and $1$'s that start with $0$.

We begin processing a string in state $q$. Notice that when the automaton initially reads a $1$, the automaton gets stuck. However, when it initially encounters a $0$, it switches to the accepting state $r$. If the automaton is in $r$ and processes another symbol, it remains in the accepting state.

Therefore, the automaton will end in the accepting state $r$ if and only if the input starts with $0$.

---

<a id="identifying-strings-accepted-by-an-nfa"></a>
## Identifying Strings Accepted by an NFA

**Example:** Which of the following strings are accepted by the finite nondeterministic automaton represented by the above diagram?

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ | $\{p\}$ | $\{q\}$ |
| $q$ | $\emptyset$ | $\{p,r\}$ |
| $\ast \: r$ | $\{r\}$ | $\emptyset$ |

- $011$
- $0100$
- $1101$

**Explanation**

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/b4f6dee7c05a959bed316a0baf3a2b9a.png>)

Let's apply the extended transition function to our strings.

- Consider the string $011{:}$

$δ(p, 011)|= ⋃_(s ∈ δ(p, 0))δ(s, 11); = δ(p, 11); = ⋃_(s ∈ δ(p, 1))δ(s, 1); = δ(q, 1); = δ(r)∪δ(p); = {r, p}$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/27344038cc473d325e402055dde0dc24.png>)
The process is terminated at the states $r$ and $p$. Since $r$ is a final (accepting) state, the string $011$ is $\bbox[3pt,Gainsboro]{\color{blue}\textrm{accepted}}$.
- Consider the string $0100{:}$

$δ(p, 0100)|= ⋃_(s ∈ δ(p, 0))δ(s, 100); = δ(p, 100); = ⋃_(s ∈ δ(p, 1))δ(s, 00); = δ(q, 00); = ∅$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/ca93dc419403494b6c24774854983661.png>)
The process is "stuck". So, the string $0100$ is $\bbox[3pt,Gainsboro]{\color{blue}\textrm{rejected}}$.
- Consider the string $1101{:}$

$δ(p, 1101)|= ⋃_(s ∈ δ(p, 1))δ(s, 101); = δ(q, 101); = δ(r, 01)∪δ(p, 01); = (⋃_(s ∈ δ(r, 0))δ(s, 1))∪(⋃_(s ∈ δ(p, 0))δ(s, 0)); = δ(r, 1)∪δ(p, 1); = ∅∪{q}; = {q}$![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/ca9c5d53b3890caedeb28f81c6e26c11.png>)
The process is terminated at the state $q$. Since $q$ is not a final (accepting) state, the string $1101$ is $\bbox[3pt,Gainsboro]{\color{blue}\textrm{rejected}}$.

---

**Question 7**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→ p$ | ${p}$ | ${q}$ |
| $∗q$ | ${q}$ | $∅$ |

Which of the following strings are accepted by the finite nondeterministic automaton represented by the above diagram?

| $110$ | $$: | $\underline{\hspace{2cm}}$ |
| --- | ---: | ---: |
| $0100$ | $$: | $\underline{\hspace{2cm}}$ |
| $0111$ | $$: | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] Rejected
- [ ] Accepted

#### Select 2

- [ ] Rejected
- [ ] Accepted

#### Select 3

- [ ] Rejected
- [ ] Accepted

---

**Question 8**

![](<../Source/8.2.3. Nondeterministic Finite Automata/Images/q-236129.png>)

Which of the following strings are accepted by the finite nondeterministic automaton represented by the above diagram?

| $000$ | $$: | $\underline{\hspace{2cm}}$ |
| --- | ---: | ---: |
| $0101$ | $$: | $\underline{\hspace{2cm}}$ |
| $1101$ | $$: | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] Rejected
- [ ] Accepted

#### Select 2

- [ ] Accepted
- [ ] Rejected

#### Select 3

- [ ] Rejected
- [ ] Accepted

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
