# Processing Strings Using Deterministic Finite Automata

<!--
lesson-id: 5414
topic-code: DSM.8.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Applying a Transition Function to a String Given a Diagram](#applying-a-transition-function-to-a-string-given-a-diagram)
- [Applying a Transition Function to a String Given a Transition Table](#applying-a-transition-function-to-a-string-given-a-transition-table)
- [The Language of a DFA](#the-language-of-a-dfa)
- [Identifying Strings Accepted by a DFA](#identifying-strings-accepted-by-a-dfa)

## Prerequisites

- [Deterministic Finite Automata](<8.2.1. Deterministic Finite Automata.md>)

---

<a id="introduction"></a>
## Introduction

In a previous lesson, we claimed that a deterministic finite automaton (DFA) is an **acceptor**—an automaton that processes a sequence of symbols and decides whether to "accept" or "reject" them. In this lesson, we'll explore how a DFA performs this task.

A **string** is a finite sequence of symbols from an **alphabet** $\Sigma$, which is a nonempty finite set. For example, $\textrm{aab}$ is a string on the alphabet $\{\textrm{a}, \textrm{b} \}$, and $\textrm{100a11b}$ is a string on the alphabet $\{\textrm{0}, \textrm{1}, \textrm{a}, \textrm{b} \}$.

Let

$$
\omega = a \beta
$$

be a string, where $a$ is the first symbol of $\omega$ and $\beta$ is the remainder of $\omega$ after removing the first symbol.
Then, starting from the transition function $\delta: Q \times \Sigma \to Q$ of an automaton

$$
A = (Q, \Sigma, \delta, q_0, F)
$$

whose set of input symbols is the alphabet $\Sigma$, we can extend $\delta$ to a function on strings on the alphabet $\Sigma$ recursively as follows:

$$
\begin{aligned}
δ(q, ε) &= q \\
δ(q, ω) &= δ(δ(q, a), β)
\end{aligned}
$$

where $\varepsilon$ denotes the empty string (the string containing no symbols) and $q$ is a state of an automaton.

To demonstrate, let's find $\delta(p, 1201)$, where $\delta: Q \times \Sigma \to Q$ is the transition function of the DFA represented by the diagram below.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/7023de0c3ef0b466b96cc95030ea82c5.png>)

According to the definition of the transition function $\delta$, for

$$
\omega = 1201
$$

we obtain the following:

$$
\begin{aligned}
δ(p, 1201) &= δ(δ(p, 1), 201) \\
&= δ(p, 201) \\
&= δ(δ(p, 2), 01) \\
&= δ(p, 01) \\
&= δ(δ(p, 0), 1) \\
&= δ(q, 1) \\
&= p
\end{aligned}
$$

Let's describe what is going on in the computations from a slightly different point of view.

- **Step 1.** The automaton is at the start state $p$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{1} \, 2 \, 0 \, 1$
From the diagram, $\delta(p,1)=p$. So, the automaton switches to state $p$ (in this case, it does not change the state) and moves to the next letter of the string.
- **Step 2.** The automaton is at the state $p$ and observes the second letter of the string:
${\color{lightgrey}1} \, \bbox[2pt,Gainsboro]{2} \, 0 \, 1$
From the diagram, $\delta(p,2)=p$. So, the automaton switches to state $p$ (in this case, it does not change the state) and moves to the next letter of the string.
- **Step 3.** The automaton is at the state $p$ and observes the third letter of the string:
${\color{lightgrey}1} \, {\color{lightgrey}2} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the diagram, $\delta(p,0)=q$. So, the automaton switches to state $q$ and moves to the next letter of the string.
- **Step 4.** The automaton is at the state $q$ and observes the fourth letter of the string:
${\color{lightgrey}1} \, {\color{lightgrey}2} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(q,1)=p$. So, the automaton switches to state $p$ and stops since the entire string has been processed.

The diagram below summarizes the process. It notes the state of the DFA and the part of the string left to process at each step.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/54a2b3788d2590ff8601845842d9ebcd.png>)

---

<a id="applying-a-transition-function-to-a-string-given-a-diagram"></a>
## Applying a Transition Function to a String Given a Diagram

**Example:** Consider the deterministic finite automaton represented by the above diagram. Given that $\delta: Q \times \Sigma \to Q$ is the transition function of the automaton, find $\delta(p, 0101)$.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/9f829fc8966016ab8b2e19403ce1188f.png>)

**Explanation**

Let

$$
\omega = a \beta
$$

be a finite sequence of symbols (a string), where $a$ is the first symbol of $\omega$ and $\beta$ is the remainder of $\omega$ after removing the first symbol.

Then, we can extend the transition function $\delta$ of the automaton to strings recursively as

$$
\begin{aligned}
δ(q, ε) &= q, \\
δ(q, ω) &= δ(δ(q, a), β)
\end{aligned}
$$

where $\varepsilon$ denotes the empty string (the string containing no symbols) and $q$ is a state of an automaton.

According to the definition of the transition function $\delta$, for

$$
\omega = 0101
$$

we obtain the following:

$$
\begin{aligned}
δ(p, 0101) &= δ(δ(p, 0), 101) \\
&= δ(r, 101) \\
&= δ(δ(r, 1), 01) \\
&= δ(r, 01) \\
&= δ(δ(r, 0), 1) \\
&= δ(q, 1) \\
&= r
\end{aligned}
$$

Let's describe what is going on in the computations from a slightly different point of view.

- *Step 1.* The automaton is at the start state $p$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{0} \, 1 \, 0 \, 1$
From the diagram, $\delta(p,0)=r$. So, the automaton switches to state $r$ and moves to the next letter of the string.
- *Step 2.* The automaton is at the state $r$ and observes the second letter of the string:
${\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1} \, 0 \, 1$
From the diagram, $\delta(r,1)=r$. So, the automaton switches to state $r$ (in this case, does not change the state) and moves to the next letter of the string.
- *Step 3.* The automaton is at the state $r$ and observes the third letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}1} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the diagram, $\delta(r,0)=q$. So, the automaton switches to state $q$ and moves to the next letter of the string.
- *Step 4.* The automaton is at the state $q$ and observes the fourth letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}1} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(q,1)=r$. So, the automaton switches to state $r$ and stops since the entire string has been processed.

We can summarize the process in the diagram below.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/04b030ddae9ec20c2b1cb342f521e713.png>)

---

**Question 1**

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/q-235950.png>)

Consider the deterministic finite automaton represented by the above diagram. Given that $δ: Q \cdot Σ → Q$ is the transition function of the automaton, $δ(p, 0010) =$

- [ ] A. $r$
- [ ] B. $q$
- [ ] C. $p$
- [ ] D. $0$
- [ ] E. $1$

---

**Question 2**

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/q-257745.png>)

Consider the deterministic finite automaton represented by the above diagram. Given that $δ: Q \cdot Σ → Q$ is the transition function of the automaton, $δ(r, 1110) =$

- [ ] A. $1$
- [ ] B. $p$
- [ ] C. $r$
- [ ] D. $q$
- [ ] E. $0$

---

<a id="applying-a-transition-function-to-a-string-given-a-transition-table"></a>
## Applying a Transition Function to a String Given a Transition Table

**Example:** Consider the deterministic finite automaton represented by the transition table above. Given that $\delta: Q \times \Sigma \to Q$ is the transition function of the automaton, find $\delta(p, 1011)$.

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ | $p$ | $r$ |
| $q$ | $p$ | $q$ |
| $\ast \: r$ | $q$ | $p$ |

**Explanation**

The diagram corresponding to our automaton is shown below.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/d65f76cdd478d7001684a0348350d90f.png>)

Let

$$
\omega = a \beta
$$

be a finite sequence of symbols (a string), where $a$ is the first symbol of $\omega$ and $\beta$ is the remainder of $\omega$ after removing the first symbol.

Then we can extend the transition function $\delta$ of the automaton to strings recursively as

$$
\begin{aligned}
δ(q, ε) &= q, \\
δ(q, ω) &= δ(δ(q, a), β)
\end{aligned}
$$

where $\varepsilon$ denotes the empty string (the string containing no symbols) and $q$ is a state of an automaton.

According to the definition of the transition function $\delta$, for

$$
\omega = 1011
$$

we obtain the following:

$$
\begin{aligned}
δ(p, 1011) &= δ(δ(p, 1), 011) \\
&= δ(r, 011) \\
&= δ(δ(r, 0), 11) \\
&= δ(q, 11) \\
&= δ(δ(q, 1), 1) \\
&= δ(q, 1) \\
&= q
\end{aligned}
$$

We can summarize the process in the diagram below.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/072b48e35d3dfa8f07716c01b3db40bb.png>)

---

**Question 3**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→ p$ | $r$ | $q$ |
| $q$ | $q$ | $p$ |
| $∗r$ | $p$ | $r$ |

Consider the deterministic finite automaton represented by the transition table above. Given that $δ: Q \cdot Σ → Q$ is the transition function of the automaton, $δ(p, 0101) =$

- [ ] A. $r$
- [ ] B. $q$
- [ ] C. $0$
- [ ] D. $p$
- [ ] E. $1$

---

**Question 4**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $p$ | $q$ | $p$ |
| $→; ∗q$ | $r$ | $s$ |
| $∗r$ | $s$ | $r$ |
| $s$ | $p$ | $r$ |

Consider the deterministic finite automaton represented by the transition table above. Given that $δ: Q \cdot Σ → Q$ is the transition function of the automaton, $δ(q, 1011) =$

- [ ] A. $1$
- [ ] B. $q$
- [ ] C. $0$
- [ ] D. $p$
- [ ] E. $r$

---

<a id="the-language-of-a-dfa"></a>
## The Language of a DFA

A finite string is **accepted** by a deterministic finite automaton if the automation ends in one of its final states after processing the entire string. Otherwise, the string is rejected. The set of all accepted finite strings is the **language of the DFA**.

For example, consider the two strings $1010$ and $11000$. Are either of these accepted by the DFA represented by the diagram below?

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/f8f5d6725d4c0c028ce24da078576104.png>)

To find out, let's apply the extended transition function to our strings.

- Processing the string $1010$, we get:

$δ(p, 1010)|= δ(δ(p, 1), 010); = δ(p, 010); = δ(δ(p, 0), 10); = δ(q, 10); = δ(δ(q, 1), 0); = δ(q, 0); = p$![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/f7eba9b9cc7300f9adce62248c0ddfb9.png>)
The process terminates at the state $p$. Since $p$ is not a final (accepting) state, the string $1010$ is rejected.
- Processing the string $11000$, we get:

$δ(p, 11000)|= δ(δ(p, 1), 1000); = δ(p, 1000); = δ(δ(p, 1), 000); = δ(p, 000); = δ(δ(p, 0), 00); = δ(q, 00); = δ(δ(q, 0), 0); = δ(p, 0); = q$![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/7dd133d30779fbffad6bc9846fbb7495.png>)
The process terminates at the state $q$. Since $q$ is a final (accepting) state, the string $11000$ is accepted.

The language of this DFA consists of all finite strings of $0$'s and $1$'s that contain an odd number of $0$'s.

We begin processing a string in state $p$. Notice that when the automaton reads a $1$, the state remains unchanged. However, when it encounters a $0$, it switches to the accepting state $q$. If the automaton is in $q$ and processes another $0$, it transitions back to the non-accepting state $p$.

Therefore, the automaton will end in the accepting state $q$ if and only if the input contains an odd number of $0$'s (since all $1$'s are ignored).

---

<a id="identifying-strings-accepted-by-a-dfa"></a>
## Identifying Strings Accepted by a DFA

**Example:** Which of the following strings are accepted by the DFA represented by the transition table above?

| $\Large _{Q} \!\backslash\! ^{\Sigma}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\to \! p$ | $q$ | $p$ |
| $\ast\:q$ | $r$ | $q$ |
| $\ast \: r$ | $p$ | $q$ |

1. $0001$
2. $1010$
3. $0011$

**Explanation**

The diagram corresponding to our automaton is shown below.

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/f22322e1b4ef3d5f4e63e5d1a26da4ea.png>)

Let's apply the extended transition function to our strings.

- Consider the string $0001{:}$

$δ(p, 0001)|= δ(δ(p, 0), 001); = δ(q, 001); = δ(δ(q, 0), 01); = δ(r, 01); = δ(δ(r, 0), 1); = δ(p, 1); = p$![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/ce3f0e70bb83850e1bc717dc266a991a.png>)
The process is terminated at the state $p$. Since $p$ is not a final (accepting) state, the string $0001$ is rejected.
- Consider the string $1010{:}$

$δ(p, 1010)|= δ(δ(p, 1), 010); = δ(p, 010); = δ(δ(p, 0), 10); = δ(q, 10); = δ(δ(q, 1), 0); = δ(q, 0); = r$![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/900f4f1e1513d229ab3d246f6e24bef9.png>)
The process is terminated at the state $r$. Since $r$ is a final (accepting) state, the string $1010$ is accepted.
- Consider the string $0011{:}$

$δ(p, 0011)|= δ(δ(p, 0), 011); = δ(q, 011); = δ(δ(q, 0), 11); = δ(r, 11); = δ(δ(r, 1), 1); = δ(q, 1); = q$![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/7aaff1dc31623019476abdac7e53bed0.png>)
The process is terminated at the state $q$. Since $q$ is a final (accepting) state, the string $0011$ is accepted.

Therefore, the correct answer is "II and III only."

---

**Question 5**

| $_{Q}∖^{Σ}$ | $0$ | $1$ |
| --- | ---: | ---: |
| $→; ∗r$ | $q$ | $r$ |
| $q$ | $q$ | $r$ |

Which of the following strings are accepted by the DFA represented by the transition table above?

| $1100$ | $$: | $\underline{\hspace{2cm}}$ |
| --- | ---: | ---: |
| $0101$ | $$: | $\underline{\hspace{2cm}}$ |
| $001$ | $$: | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] Accepted
- [ ] Rejected

#### Select 2

- [ ] Rejected
- [ ] Accepted

#### Select 3

- [ ] Rejected
- [ ] Accepted

---

**Question 6**

![](<../Source/8.2.2. Processing Strings Using Deterministic Finite Automata/Images/q-235956.png>)

Which of the following strings are accepted by the DFA represented by the above diagram?

| $000$ | $$: | $\underline{\hspace{2cm}}$ |
| --- | ---: | ---: |
| $0101$ | $$: | $\underline{\hspace{2cm}}$ |
| $1011$ | $$: | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] Rejected
- [ ] Accepted

#### Select 2

- [ ] Accepted
- [ ] Rejected

#### Select 3

- [ ] Accepted
- [ ] Rejected

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
