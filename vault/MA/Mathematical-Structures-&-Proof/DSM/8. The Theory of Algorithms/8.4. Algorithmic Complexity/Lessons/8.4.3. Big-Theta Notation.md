# Big-Theta Notation

<!--
lesson-id: 3807
topic-code: DSM.8.4.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Understanding the Definition of Big-Theta](#understanding-the-definition-of-big-theta)
- [Proving That a Function Belongs to Big-Theta Set](#proving-that-a-function-belongs-to-big-theta-set)
- [Properties of Big-Theta](#properties-of-big-theta)
- [Using the Properties of Big-Theta Notation](#using-the-properties-of-big-theta-notation)

## Prerequisites

- [Big-Omega Notation](<8.4.2. Big-Omega Notation.md>)

---

<a id="introduction"></a>
## Introduction

Let's recall some of the asymptotic notations we've seen in previous lessons, defined in the context of algorithmic complexity.

The set $O(g(n))$ contains all nonnegative functions $f(n)$ such that

$$
f(n) \leq c g(n)
$$

for $c > 0$ and sufficiently large $n$. Formally:

$$
O(g(n)) = \big\{f(n) \:: \: \exists c, n_0 > 0, \: \forall n \geq n_0, \: 0 \leq f(n) \leq c g(n) \big\}
$$

If $f\in O(g(n))$, then $c g(n)$ is an *asymptotic upper bound* of $f$, as shown below.

![](<../Source/8.4.3. Big-Theta Notation/Images/2f948c5b2ef93d491f29e7ae46268473.png>)

The set $\Omega(g(n))$ contains all nonnegative functions $f(n)$ such that

$$
f(n) \geq c g(n)
$$

for $c > 0$ and sufficiently large $n$. Formally:

$$
\Omega(g(n)) = \big\{f(n) \:: \: \exists c, n_0 > 0, \: \forall n \geq n_0, \: 0 \leq c g(n) \leq f(n) \big\}
$$

If $f\in \Omega(g(n))$, then $c g(n)$ is an *asymptotic lower bound* of $f$, as shown below.

![](<../Source/8.4.3. Big-Theta Notation/Images/3014ef545bc133c22947a6f6afb9d0d5.png>)

A related valuable concept for classifying the asymptotic behavior of algorithms is **big-theta** notation.

Suppose we have two nonnegative functions $f(n)$ and $g(n)$ such that there exist positive constants $c_1$ and $c_2$ such that, for sufficiently large $n$, we have

$$
c_1 g(n) \leq f(n) \leq c_2 g(n)
$$

as depicted in the graph below.

![](<../Source/8.4.3. Big-Theta Notation/Images/e5490c9a928d23acb3279bc5e0bcc1a8.png>)

In plain English, beyond some threshold value $n_0$, the function $f(n)$ is sandwiched between two multiples of $g(n)$, ensuring that $f(n)$ grows at the same rate as $g(n)$ up to constant factors. In such cases, we write
$f(n) \in \Theta(g(n))$
where the formal definition of the set $\Theta(g(n))$ is given below:

$$
\Theta(g(n)) = \left\{f(n): \exists c_1 c_2,n_0 \gt 0, \forall n \geq n_0, 0 \leq c_1g(n) \leq f(n) \leq c_2 g(n)\right\}
$$

Big-Theta notation is helpful because it provides a tight bound on a function's growth by specifying both an upper and a lower bound.

---

<a id="understanding-the-definition-of-big-theta"></a>
## Understanding the Definition of Big-Theta

**Example:** Suppose $n \in \mathbb{N}$. In the context of algorithmic complexity, what is the definition of $\Theta(n^{10})$?

**Explanation**

According to the definition, $\Theta(g(n))$ is the set of all functions $f(n)$ for which there exist positive constants $c_1$, $c_2$, and $n_0$ such that for all $n$ larger than $n_0$, the values of $f(n)$ lies between $c_1 \cdot g(n)$ and $c_2 \cdot g(n)$.

$$
\Theta(g(n)) = \big\{f(n) \:: \: \exists c_1,c_2, n_0 > 0, \: \forall n \geq n_0, \: 0\leq c_1\cdot g(n) \leq f(n) \leq c_2\cdot g(n) \big\}
$$

Geometrically, this means the following:

> The set $\Theta(n^{10})$ consists of all the functions $f(n)$, for which there exist positive constants $c_1, c_2$, and $n_0$ such that the graph of $f(n)$ lies between $y=c_1\cdot n^{10}$ and $y=c_2\cdot n^{10}$ for all $n\geq n_0$.

---

**Question 1**

Suppose $n ∈ N$. In the context of algorithmic complexity, select the correct options in the definition of $Θ(n^{7})$ below.The $\underline{\hspace{2cm}}$ $Θ(n^{7})$ consists of all the $\underline{\hspace{2cm}}$, for which there exist positive constants $c_{1}, c_{2}$, and $n_{0}$ such that the graph of $f(n)$ lies between
$\underline{\hspace{2cm}}$ and $\underline{\hspace{2cm}}$ for all $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] function
- [ ] set
- [ ] number

#### Select 2

- [ ] functions [MATH: f(n)]
- [ ] numbers [MATH: n]
- [ ] sets [MATH: {n}]

#### Select 3

- [ ] [MATH: y = n^{7}]
- [ ] [MATH: y = c_{1} ⋅ n^{7}]

#### Select 4

- [ ] [MATH: y = n^{7}]
- [ ] [MATH: y = c_{2} ⋅ n^{7}]

#### Select 5

- [ ] [MATH: n ≥ n_{0}]
- [ ] [MATH: n ≤ n_{0}]

---

**Question 2**

Suppose $n ∈ N$. In the context of algorithmic complexity, select the correct options in the definition of $Θ(n^{3})$ below. $∀$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: ∀]
- [ ] [MATH: ∃]

#### Select 2

- [ ] [MATH: n ≥ n_{0}]
- [ ] [MATH: c ≥ n]
- [ ] [MATH: n ≥ c]

#### Select 3

- [ ] [MATH: cf(n)]
- [ ] [MATH: n^{3}]
- [ ] [MATH: f(n)]
- [ ] [MATH: cn^{2}]

#### Select 4

- [ ] [MATH: c_{2}f(n)]
- [ ] [MATH: c_{2}]
- [ ] [MATH: n^{2}]
- [ ] [MATH: c_{2}n^{3}]

---

<a id="proving-that-a-function-belongs-to-big-theta-set"></a>
## Proving That a Function Belongs to Big-Theta Set

**Example:** Prove that $3n^2+1 \in \Theta(n^2)$ for $n\in\mathbb N$.

**Explanation**

According to the definition, $\Theta(g(n))$ is the set of all functions $f(n)$ for which there exist positive constants $c_1$, $c_2$ and $n_0$ such that for all $n$ larger than $n_0$, the values of $f(n)$ lies between $c_1 \cdot g(n)$ and $c_2 \cdot g(n)$

$$
\Theta(g(n)) = \big\{f(n) \:: \: \exists c_1,c_2, n_0 > 0, \: \forall n \geq n_0, \: 0 \leq c_1\cdot g(n)\leq f(n) \leq c_2\cdot g(n) \big\}
$$

So, we start as shown below.

> We need to show that there exists constants $c_1$ and $c_2$ such that for sufficiently large $n$, we have
> $c_1n^2 \leq 3n^2+1 \leq c_2n^2$.

Now, let's isolate $c_1$ and $c_2$ on the inequality.

> Dividing both sides of the inequality by $n^2$, we get follows:
> $\begin{bmatrix}c_{1}n^{2} & \le 3n^{2} + 1 \le c_{2}n^{2} \\ \frac{c_{1}n^{2}}{n^{2}} & \le \frac{3n^{2} + 1}{n^{2}} \le \frac{c_{2}n^{2}}{n^{2}} \\ c_{1} & \le \frac{3n^{2}}{n^{2}} + \frac{1}{n^{2}} \le c_{2} \\ c_{1} & \le 3 + \frac{1}{n^{2}} \le c_{2}\end{bmatrix}$

Notice that

$$
\dfrac{1}{n^2} = 1
$$

and

$$
3+\dfrac{1}{n^2}=4
$$

if $n=1$. Also,

$$
\dfrac{1}{n^2}
$$

monotonically approach $0$ as $n \to \infty$. That means the minimum possible value of

$$
\left(3+\dfrac{1}{n^2}\right)
$$

is $3$ and the maximum is $4$. As a result,

$$
3\leq 3+\dfrac{1}{n^2} \leq 4
$$

for all

$$
n \geq 1
$$

and we can now pick our constants.

> By setting $n_0=1$, $c_1=3$ and $c_2=4$ we make the above inequality hold for any $n \geq n_0$.

Of course, other choices for the constants $c_1$, $c_2$, and $n_0$ may do the job, but the important thing is we found at least one such combination.

> Therefore, $3n^2+1 \in \Theta(n^2)$.

The full proof is given below.

> We need to show that there exists constants $c_1$ and $c_2$ such that for sufficiently large $n$, we have
> $c_1n^2 \leq 3n^2+1 \leq c_2n^2$.
> Dividing both sides of the inequality by $n^2$, we get follows:
> $\begin{bmatrix}c_{1}n^{2} & \le 3n^{2} + 1 \le c_{2}n^{2} \\ \frac{c_{1}n^{2}}{n^{2}} & \le \frac{3n^{2} + 1}{n^{2}} \le \frac{c_{2}n^{2}}{n^{2}} \\ c_{1} & \le \frac{3n^{2}}{n^{2}} + \frac{1}{n^{2}} \le c_{2} \\ c_{1} & \le 3 + \frac{1}{n^{2}} \le c_{2}\end{bmatrix}$
> By setting $n_0=1$, $c_1=3$ and $c_2=4$ we make the above inequality hold for any $n \geq n_0$.
> Therefore, $3n^2+1 \in \Theta(n^2)$.

---

**Question 3**

Prove that $3n + 9 ∈ Θ(n)$ for $n ∈ N$.

We need to show that there exists constants $c_{1}$ and $c_{2}$ such that for $\underline{\hspace{2cm}}$, we have
$3n + 9$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] sufficiently small [MATH: n]
- [ ] sufficiently large [MATH: n]
- [ ] all natural [MATH: n]

#### Select 2

- [ ] [MATH: 3n + 9]
- [ ] [MATH: c_{1}]
- [ ] [MATH: c_{1}n]
- [ ] [MATH: n]

#### Select 3

- [ ] [MATH: c_{2}n]
- [ ] [MATH: c_{1}n]
- [ ] [MATH: 3n + 9]
- [ ] [MATH: n]

#### Select 4

- [ ] [MATH: n]
- [ ] [MATH: c_{2}n]
- [ ] [MATH: c_{2}]
- [ ] [MATH: 3n]

---

**Question 4**

Prove that $8n^{2} + 3 ∈ Θ(n^{2})$ for $n ∈ N$.

We need to show that there exists constants $c_{1}$ and $c_{2}$ such that for $\underline{\hspace{2cm}}$, we have
$=$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] sufficiently large [MATH: n]
- [ ] sufficiently small [MATH: n]
- [ ] all natural [MATH: n]

#### Select 2

- [ ] [MATH: =]
- [ ] [MATH: ≥]
- [ ] [MATH: ≤]

#### Select 3

- [ ] [MATH: n^{2}]
- [ ] [MATH: c_{2}n^{2}]
- [ ] [MATH: c_{2}(8n^{2} + 3)]
- [ ] [MATH: c_{2}]

---

<a id="properties-of-big-theta"></a>
## Properties of Big-Theta

Recall that we can summarize the scales of some frequently used functions in terms of Big-O notation using the diagram below.

![](<../Source/8.4.3. Big-Theta Notation/Images/797ecfcb56319cabe80c66f78baed504.png>)

Here, we have the following inclusions:
$O(1) \subset O(\log(n)) \subset O(n) \subset O(n\log(n)) \subset O(n^k) \subset O(a^n) \subset O(n!)$

Furthermore, $O(n^k) \subset O(n^{k+1})$ for all

$$
k=1,2,3,\ldots
$$

and $O(a^n) \subset O(b^n)$ whenever $a < b$.

We can use the properties of Big-O to simplify expressions involving Big-Theta.

In addition, we have the following two facts:

- If $c$ is any constant,
$\Theta(c g(n)) = \Theta(g(n))$.
- If $O(g(n)) \subset O(f(n))$, then
$\Theta(g(n)+f(n)) = \Theta(f(n))$.

With that in mind, let's prove that

$$
\Theta(3n!+4n^3) = \Theta(n!)
$$

Since

$$
O(3n!)=O(n!)
$$

and

$$
O(4n^3)=O(n^3)
$$

we can re-write our expression as follows:

$$
\begin{aligned}
Θ(3n! + 4n^{3}) &= Θ(n! + n^{3})
\end{aligned}
$$

Finally, since $O(n^3) \subset O(n!)$, we obtain that

$$
\Theta(n!+n^3) = \Theta(n!)
$$

---

<a id="using-the-properties-of-big-theta-notation"></a>
## Using the Properties of Big-Theta Notation

**Example:** Simplify the asymptotic notation $\Theta(2^n+3\log(n)+1)$.

**Explanation**

We can summarize the scales of some frequently used functions in terms of Big-O notation in the diagram below.

![](<../Source/8.4.3. Big-Theta Notation/Images/3e26883e4da8abd460b9de5f4aaf1c1f.png>)

Here, we have the following inclusions:

$$
O(1) \subset O(\log(n)) \subset O(n) \subset O(n\log(n)) \subset O(n^k) \subset O(a^n) \subset O(n!)
$$

Furthermore, $O(n^k) \subset O(n^{k+1})$ for all

$$
k=1,2,3,\ldots
$$

and $O(a^n) \subset O(b^n)$ whenever $a < b$.

Notice that

$$
\Theta(c g(n)) = \Theta(g(n))
$$

for any constant multiplier $c$.
Also, if $O(g(n)) \subset O(f(n))$, then

$$
\Theta(g(n)+f(n)) = \Theta(f(n))
$$

Now, since $O(1) \subset O(3\log(n))$, we can re-write our expression as follows:

$$
\begin{aligned}
Θ(2^{n} + 3\log (n) + 1) &= Θ(2^{n} + 3\log (n))
\end{aligned}
$$

Next, since

$$
O(3\log(n))=O(\log(n))
$$

we get the following:

$$
\begin{aligned}
Θ(2^{n} + 3\log (n)) &= Θ(2^{n} + \log (n))
\end{aligned}
$$

Finally, since $O(\log(n)) \subset O(2^n)$, we obtain that

$$
\Theta(2^n+\log(n)) = \Theta(2^n)
$$

Therefore, we have

$$
\Theta(2^n+3\log(n)+1) = \Theta(2^n)
$$

---

**Question 5**

Select the correct option below.$n^{2} + 1$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: n^{2} + 1]
- [ ] [MATH: n^{3} + n^{2}]
- [ ] [MATH: 3n^{2} + 5]

#### Select 2

- [ ] [MATH: 1]
- [ ] [MATH: n^{3}]
- [ ] [MATH: n^{2}]

---

**Question 6**

Select the correct option below.$n^{2} + 1$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: n^{2} + 1]
- [ ] [MATH: 7^{n} + n^{2}]
- [ ] [MATH: 2n^{2} + 1]

#### Select 2

- [ ] [MATH: 1|]
- [ ] [MATH: 7^{n}]
- [ ] [MATH: n^{2}]

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
