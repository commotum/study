# Moore Machines

<!--
lesson-id: 3796
topic-code: DSM.8.1.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Transition and Output Tables of Moore Machines](#transition-and-output-tables-of-moore-machines)
- [Constructing Transition and Output Tables Given a Diagram](#constructing-transition-and-output-tables-given-a-diagram)
- [Drawing a Diagram Given Transition and Output Tables](#drawing-a-diagram-given-transition-and-output-tables)
- [Using Moore Machines to Process Strings](#using-moore-machines-to-process-strings)
- [Applying a Moore Machine to a String](#applying-a-moore-machine-to-a-string)
- [Moore Machines vs Mealy Machines](#moore-machines-vs-mealy-machines)

## Prerequisites

- [Sets](<../../../../GEO/6. Probability & Combinatorics/6.1. Introduction to Probability/Lessons/6.1.1. Sets.md>)
- [Mealy Machines](<8.1.1. Mealy Machines.md>)

---

<a id="introduction"></a>
## Introduction

Recall that a transducer is a machine that receives a string as input and then returns another string as output. In a previous lesson, we studied Mealy machines. In this lesson, we'll look at another type of transducer called a **Moore machine.**

A Moore machine is a six-tuple of the form

$$
M = (Q, \Sigma, \Gamma, \delta, \omega, q_0)
$$

where:

- $Q$ is a nonempty finite set of **states**. These are all the possible configurations the machine can be in.
- $\Sigma$ is a nonempty finite set of **input symbols**. The machine can process this "alphabet" of symbols as inputs.
- $\Gamma$ is a nonempty finite set of **output symbols**. The machine can write this "alphabet" of symbols as outputs.
- $\delta$ is a **transition function** of the form $\delta: Q \times \Sigma \to Q$. It takes a state and an input symbol and returns a state. This function determines how the machine moves from one state to another based on a given input symbol.
- $\omega$ is an **output function** of the form $\omega: Q \to \Gamma$. It takes a state and returns an output symbol. This function determines what symbol to write based on a given state.
- $q_0 \in Q$ is a **start state** (or **initial state**). This is the state in which the machine begins.

We can visualize a Moore machine as a directed graph where

- vertices represent the states and their corresponding output symbol,
- the arcs, labeled with input symbols, represent transitions from one state to another, and
- the start state is labeled by the input arrow $\to$.

An example of a Moore machine is shown in the diagram below.

![](<../Source/8.1.2. Moore Machines/Images/cd4ce8779502132d7bb78472c387f43f.png>)

In this case, the machine has the following components:

- The set of states $Q = \{p,q\}$ (represented by the vertices).
- The set of input symbols $\Sigma = \{0,1\}$ (represented by the arcs' labels).
- The set of output symbols $\Omega = \{a,b\}$ (represented by the vertices' labels).
- The transition function $\delta$, defined by the arcs with the following transitions:
$\begin{bmatrix}δ(p, 0)|= q, & δ(p, 1)|= p, \\ δ(q, 0)|= q, & δ(q, 1)|= p\end{bmatrix}$
For instance, the arrow from $p$ to $q$, labeled $0$, means that $\delta(p,0) = q$.![](<../Source/8.1.2. Moore Machines/Images/93a2f78288c018fbc7fbaf3a4fb54ab8.png>)
- The output function $\omega$ is defined for each vertex (state) with the following outputs:
$\omega(p) = b, \qquad \omega(q) = a$
For instance, the node labeled $q\mid a$ means that $\omega(q) = a$.![](<../Source/8.1.2. Moore Machines/Images/1dbf6471a875ee316d8a3b80363a6c9b.png>)
- The start state $p$ (that is labeled by the input arrow $\to$), as shown below.![](<../Source/8.1.2. Moore Machines/Images/8c25eb9519c200e8db46ed23574abd5c.png>)

---

<a id="transition-and-output-tables-of-moore-machines"></a>
## Transition and Output Tables of Moore Machines

Like Mealy machines, transition and output functions, $\delta$ and $\omega$, are often put into tables.

Below is an example of a Moore machine's transition table (on the left) and output table (on the right). Note that we place $\to$ next to the initial state in the transition table.

| $\delta$$0$$1$$\to \! p$$p$$q$$q$$p$$q$ | StateOutput $(\omega)$$p$$1$$q$$0$ |
| --- | ---: |

Let's construct a diagram of the machine given by these tables.
From the tables, we determine that the machine has the following components:

- the set of states $Q = \{p,q\}$ (given by the rows),
- the set of input symbols $\Sigma = \{0,1\}$ (given by the columns of the transition table),
- the set of output symbols $\Gamma = \{0,1\}$ (given by the values in the output table),
- the start state $p$ (labeled by $\to$ in the transition table), and
- the transition function $\delta$ and the output function $\omega$ are given in the tables.

Now that we have the components, we can construct our diagram. First, let's draw the states of our machine.

![](<../Source/8.1.2. Moore Machines/Images/b518294caec0b59436f22e19a8e27439.png>)

According to the transition table, $p$ is the start state, so we labeled it with an input arrow and left space in each label for the corresponding outputs.

From the tables, we also get the following:

- Consider row $p$ of the tables. Its intersection with column $0$ in the transition table is $p$, indicating that $\delta(p,0)=p$. So, we draw an arrow (loop) from $p$ to $p$, labeled $0$. Its intersection with column $1$ in the transition table is $1$, indicating that $\delta(p,1)=q$. So, we draw an arrow from $p$ to $q$, labeled $1$.The $1$ in the output table means that $\omega(p)=1$. So, we write $p \mid 1$ in the vertex $p$.![](<../Source/8.1.2. Moore Machines/Images/4e6f63c210f1f949c043fbb3e9f891d7.png>)

- Consider row $q$ of the tables. Its intersection with column $0$ in the transition table is $p$, indicating that $\delta(q,0)=p$. So, we draw an arrow from $q$ to $p$, labeled $0$. Its intersection with column $1$ in the transition table is $1$, indicating that $\delta(q,1)=q$. So, we draw an arrow (loop) from $q$ to $q$, labeled $1$.The $0$ in the output table means that $\omega(q)=0$. So, we write $q \mid 0$ in the vertex $q$.![](<../Source/8.1.2. Moore Machines/Images/d753b4075098732bacdada06aa113087.png>)

We have gone through every cell in both tables and added all the arcs, so our diagram is complete.

---

<a id="constructing-transition-and-output-tables-given-a-diagram"></a>
## Constructing Transition and Output Tables Given a Diagram

**Example:** The diagram above shows a Moore machine. Fill in the blanks in the following table corresponding to the transition function $\delta$.

![](<../Source/8.1.2. Moore Machines/Images/72c7b3840089c407cd642f2eb7dec5d7.png>)

| $\delta$ | $a$ | $b$ |
| --- | ---: | ---: |
| $\to \! p$ |  |  |
| $q$ |  |  |
| $r$ |  |  |

**Explanation**

In our case, the machine has the following components:

- the set of states $Q=\{p,q,r\}$,
- the set of input symbols $\Sigma=\{a, b\}$,
- the set of output symbols $\Gamma=\{a, b\}$, and
- the start state $p$.

With that in mind, let's determine the transition function.

First, we draw an empty table with a different state written in each row and a different input symbol written in each column:

| $\delta$ | $a$ | $b$ |
| --- | ---: | ---: |
| $\to \! p$ |  |  |
| $q$ |  |  |
| $r$ |  |  |

Notice that we write $\to$ next to $p$ since it's the start state.

Next, we fill in the cells:

- There is an arrow (loop) from $p$ to $p$, labeled $a$. This means that $\delta(p, a)=p$. So, we write $p$ at the intersection of row $p$ and column $a$.
- There is an arrow from $p$ to $q$, labeled $b$. This means that $\delta(p,b)=q$. So, we write $q$ at the intersection of row $p$ and column $b$.
- There is an arrow from $q$ to $p$, labeled $a$. This means that $\delta(q,a)=p$. So, we write $p$ at the intersection of row $q$ and column $a$.
- There is an arrow from $q$ to $r$, labeled $b$. This means that $\delta(q,b)=r$. So, we write $r$ at the intersection of row $q$ and column $b$.
- There is an arrow from $r$ to $q$, labeled $a$. This means that $\delta(r,a)=q$. So, we write $q$ at the intersection of row $r$ and column $a$.
- There is an arrow (loop) from $r$ to $r$, labeled $b$. This means that $\delta(r, b)=r$. So, we write $r$ at the intersection of row $r$ and column $b$.

| $\delta$ | $a$ | $b$ |
| --- | ---: | ---: |
| $\to \! p$ | $p$ | $q$ |
| $q$ | $p$ | $r$ |
| $r$ | $q$ | $r$ |

---

**Question 1**

![](<../Source/8.1.2. Moore Machines/Images/q-238697.png>)

The diagram above shows a Moore machine. Select the correct options in the table for the corresponding output function $ω$ below.

| State | Output $(ω)$ |
| --- | ---: |
| $p$ | $\underline{\hspace{2cm}}$ |
| $q$ | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: 1]
- [ ] [MATH: 0]

#### Select 2

- [ ] [MATH: 1]
- [ ] [MATH: 0]

---

**Question 2**

![](<../Source/8.1.2. Moore Machines/Images/q-238495.png>)

The diagram above shows a Moore machine. Select the correct options in the table for the corresponding transition function below.

| $δ$ | $0$ | $1$ |
| --- | ---: | ---: |
| $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |
| $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ | $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: → p]
- [ ] [MATH: p]

#### Select 2

- [ ] [MATH: q]
- [ ] [MATH: p]

#### Select 3

- [ ] [MATH: p]
- [ ] [MATH: q]

#### Select 4

- [ ] [MATH: → q]
- [ ] [MATH: q]

#### Select 5

- [ ] [MATH: q]
- [ ] [MATH: p]

#### Select 6

- [ ] [MATH: q]
- [ ] [MATH: p]

---

<a id="drawing-a-diagram-given-transition-and-output-tables"></a>
## Drawing a Diagram Given Transition and Output Tables

**Example:** Construct a diagram representing the Moore machine given by the above transition table (on the left) and the output table (on the right).

| $\delta$$a$$b$$\to \! p$$p$$q$$q$$q$$p$ | StateOutput $(\omega)$$p$$0$$q$$1$ |
| --- | ---: |

**Explanation**

In our case, the machine has the following components:

- the set of states $Q=\{p,q\}$,
- the set of input symbols $\Sigma=\{a,b\}$,
- the set of output symbols $\Gamma=\{0,1\}$,
- the start state $p$, and
- the transition function $\delta$ and the output function $\omega$ are given in the tables.

First, let's draw the states of our machine.

![](<../Source/8.1.2. Moore Machines/Images/09be0339cfe7fb4301678719a7f30cb3.png>)

According to the transition table, $p$ is the start state, so we labeled it with an input arrow.

From the tables, we also get the following:

- Consider row $p$ of the tables. Its intersection with column $a$ in the transition table is $p$, indicating that $\delta(p,a)=p$. So, we draw an arrow (loop) from $p$ to $p$, labeled $a$.Its intersection with column $b$ in the transition table is $q$, indicating that $\delta(p,b)=q$. So, we draw an arrow from $p$ to $q$, labeled $b$.The $0$ in the output table means that $\omega(p)=0$. So, we write $p \mid 0$ in the $p$-node.![](<../Source/8.1.2. Moore Machines/Images/1602d2c878ecfb27aa13c7915b422949.png>)
- Consider row $q$ of the tables.Its intersection with column $a$ in the transition table is $q$, indicating that $\delta(q, a)=q$. So, we draw an arrow (loop) from $q$ to $q$, labeled $a$. Its intersection with column $b$ in the transition table is $p$, indicating that $\delta(q, b)=p$. So, we draw an arrow from $q$ to $p$, labeled $b$.The $1$ in the output table means that $\omega(q)=1$. So, we write $q \mid 1$ in the $q$-node.![](<../Source/8.1.2. Moore Machines/Images/4051cc544d78f149e8240e376d2afdc3.png>)

We have been through every cell in the tables, so our diagram is complete.

---

**Question 3**

| $δ$$0$$1$$→ p$$p$$q$$q$$q$$p$ | StateOutput $(ω)$$p$$b$$q$$a$ |
| --- | ---: |

Which of the following diagrams represents the Moore machine given by the above transition table (on the left) and the output table (on the right)?

- [ ] A. ![](<../Source/8.1.2. Moore Machines/Images/q-258891-a-3.png>)
- [ ] B. ![](<../Source/8.1.2. Moore Machines/Images/q-258891-a-5.png>)
- [ ] C. ![](<../Source/8.1.2. Moore Machines/Images/q-258891-a-4.png>)
- [ ] D. ![](<../Source/8.1.2. Moore Machines/Images/q-258891-a-2.png>)
- [ ] E. ![](<../Source/8.1.2. Moore Machines/Images/q-258891-a-1.png>)

---

**Question 4**

| $δ$$0$$1$$→ p$$p$$q$$q$$q$$p$ | StateOutput $(ω)$$p$$0$$q$$1$ |
| --- | ---: |

Which of the following diagrams represents the Moore machine given by the above transition table (on the left) and the output table (on the right)?

- [ ] A. ![](<../Source/8.1.2. Moore Machines/Images/q-238745-a-3.png>)
- [ ] B. ![](<../Source/8.1.2. Moore Machines/Images/q-238745-a-1.png>)
- [ ] C. ![](<../Source/8.1.2. Moore Machines/Images/q-238745-a-5.png>)
- [ ] D. ![](<../Source/8.1.2. Moore Machines/Images/q-238745-a-4.png>)
- [ ] E. ![](<../Source/8.1.2. Moore Machines/Images/q-238745-a-2.png>)

---

<a id="using-moore-machines-to-process-strings"></a>
## Using Moore Machines to Process Strings

A Moore machine can process string by extending the definition of its output function $\omega$ to process each symbol of the string in turn.

Let

$$
\sigma=a\beta
$$

be a string, where $a$ is the first symbol of $\sigma$, and $\beta$ is the remainder of $\sigma$ after removing the first symbol.
Formally, we can extend the output function $\omega$ of a Moore machine to strings recursively using

$$
\begin{aligned}
ω(δ(q, ϵ)) &= ω(q), \\
ω(δ(q, σ)) &= ω(q) \cdot ω(δ(δ(q, a), β))
\end{aligned}
$$

where $\epsilon$ denotes the empty string (the string containing no symbols), $q$ is a state of the machine, $\delta$ is the transition function, and $\cdot$ denotes a concatenation of strings.
Note that an input string of length $n$ outputs a string of length $n+1$.

Let's find the output string the Moore machine below would return if it received the input string $101$.

![](<../Source/8.1.2. Moore Machines/Images/1c6d8a899785f545ee74fa4852301f7c.png>)

According to the definition of the extension of the output function on the set of strings, we obtain the following:

$$
\begin{aligned}
ω(δ(p, 101)) &= ω(p) \cdot ω(δ(δ(p, 1), 01)) \\
&= b \cdot ω(δ(p, 01)) \\
&= b \cdot ω(p) \cdot ω(δ(δ(p, 0), 1)) \\
&= bb \cdot ω(δ(q, 1)) \\
&= bb \cdot ω(q) \cdot ω(δ(δ(q, 1), ϵ)) \\
&= bba \cdot ω(δ(p, ϵ)) \\
&= bba \cdot ω(p) \\
&= bbab
\end{aligned}
$$

Working with the formal definition can be tricky. So, let's describe what is going on in the computations from a slightly different point of view.

- *Step 0.* Before processing the input, the machine goes to the start state $p$. From the diagram, $\omega(p)=\bbox[3pt,Moccasin]{b}$. Now, the machine is ready to process inputs.
- *Step 1.* The machine is in the start state $p$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{1} \, 0 \, 1$
From the diagram, $\delta(p,1)=p$ and $\omega(p)=\bbox[3pt,Moccasin]{b}$. So, the machine switches to state $p$ (in this case, does not change the state) and moves to the next letter of the string. At the same time, the machine outputs $\bbox[3pt,Moccasin]{b}$.
- *Step 2.* The machine is in the state $p$ and observes the second letter of the string:
${\color{lightgrey}1} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the diagram, $\delta(p,0)=q$ and $\omega(q)=\bbox[3pt,Moccasin]{a}$. So, the machine switches to state $q$ and moves to the next letter of the string. At the same time, the machine outputs $\bbox[3pt,Moccasin]{a}$.
- *Step 3.* The machine is in the state $q$ and observes the third letter of the string:
${\color{lightgrey}1} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the diagram, $\delta(q,1)=p$ and $\omega(p)=\bbox[3pt,Moccasin]{b}$. So, the machine switches to state $p$ and stops since the entire string has been processed. At the same time, the machine outputs $\bbox[3pt,Moccasin]{b}$.

Therefore, the output string is

$$
\omega(\delta(p, 101))=\bbox[3pt,Moccasin]{bbab}
$$

---

<a id="applying-a-moore-machine-to-a-string"></a>
## Applying a Moore Machine to a String

**Example:** Consider the Moore machine represented from the (transition/output) table above. If the machine received the input string $001$, what output string would the machine return?

| $\delta$$0$$1$$\to \! p$$q$$p$$q$$p$$q$ | StateOutput $(\omega)$$p$$0$$q$$1$ |
| --- | ---: |

**Explanation**

Let

$$
\sigma=a\beta
$$

be a finite sequence of input symbols (string), where $a$ is the first symbol of $\sigma$ and $\beta$ is the remainder of $\sigma$ after removing the first symbol.

Then, we can extend the output function $\omega$ of a Moore machine to strings recursively using

$$
\begin{aligned}
ω(δ(q, ϵ)) &= ω(q), \\
ω(δ(q, σ)) &= ω(q) \cdot ω(δ(δ(q, a), β))
\end{aligned}
$$

where $\epsilon$ denotes the empty string (the string containing no symbols), $q$ is a state of the machine, $\delta$ is the transition function, and $\cdot$ denotes a concatenation of strings.

Now, we can compute the result using this definition of the extension of the output function $\sigma$ on the set of strings. Recall that initial state output $\omega(p)$ must be included before processing the input strings, and for any input of string of length $n$, a Moore machine outputs the string of length $n+1$. In our case, we obtain the following:

$$
\begin{aligned}
ω(δ(p, 001)) &= ω(p) \cdot ω(δ(δ(p, 0), 01)) \\
&= 0 \cdot ω(δ(q, 01)) \\
&= 0 \cdot ω(q) \cdot ω(δ(δ(q, 0), 1)) \\
&= 01 \cdot ω(δ(p, 1)) \\
&= 01 \cdot ω(p) \cdot ω(δ(δ(p, 1), ϵ)) \\
&= 010 \cdot ω(δ(p, ϵ)) \\
&= 010 \cdot ω(p) \\
&= 0100
\end{aligned}
$$

Let's describe what is going on in the computations from a slightly different point of view.

- *Step 0.* Before processing the input, the machine goes to the start state $p$. From the table, $\omega(p)=\bbox[3pt,Moccasin]{0}$. Now, the machine is ready to process inputs.
- *Step 1.* The machine is in the start state $p$ and observes the first letter of the string:
$\bbox[2pt,Gainsboro]{0} \, 0 \, 1$
From the tables, $\delta(p,0)=q$ and $\omega(q)=\bbox[3pt,Moccasin]{1}$. So, the machine switches to state $q$ and moves to the next letter of the string. At the same time, the machine outputs $\bbox[3pt,Moccasin]{1}$.
- *Step 2.* The machine is in the state $q$ and observes the second letter of the string:
${\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{0} \, 1$
From the tables, $\delta(q,0)=p$ and $\omega(p)=\bbox[3pt,Moccasin]{0}$. So, the machine switches to state $p$ and moves to the next letter of the string. At the same time, the machine outputs $\bbox[3pt,Moccasin]{0}$.
- *Step 3.* The machine is in the state $p$ and observes the third letter of the string:
${\color{lightgrey}0} \, {\color{lightgrey}0} \, \bbox[2pt,Gainsboro]{1}$
From the tables, $\delta(p,1)=p$ and $\omega(p)=\bbox[3pt,Moccasin]{0}$. So, the machine switches to state $p$ (in this case, does not change the state) and stops since the entire string has been processed. At the same time, the machine outputs $\bbox[3pt,Moccasin]{0}$.

Therefore, the output string is

$$
\omega(\delta(p,001))=\bbox[3pt,Moccasin]{0100}
$$

---

**Question 5**

![](<../Source/8.1.2. Moore Machines/Images/q-258845.png>)

Consider the Moore machine represented by the above diagram. If the machine received the input string $001$, what output string would the machine return?

- [ ] A. $0101$
- [ ] B. $1010$
- [ ] C. $0010$
- [ ] D. $1101$
- [ ] E. $1110$

---

**Question 6**

| $δ$$0$$1$$→ p$$p$$q$$q$$p$$r$$r$$r$$q$ | StateOutput $(ω)$$p$$a$$q$$b$$r$$a$ |
| --- | ---: |

Consider the Moore machine represented from the (transition/output) table above. If the machine received the input string $011$, what output string would the machine return?

- [ ] A. $abba$
- [ ] B. $baba$
- [ ] C. $aaba$
- [ ] D. $aabb$
- [ ] E. $babb$

---

<a id="moore-machines-vs-mealy-machines"></a>
## Moore Machines vs Mealy Machines

Mealy machines and Moore machines are both transducers. The difference between them lies in their output functions:

- The output function of a Mealy machine depends on the state *and* the input symbol:
$\omega_{_\text{Mealy}}: Q \times \Sigma \to \Gamma$
When represented in a diagram, each arc (transition) is labeled with an output value.
- The output function of a Moore machine depends on the state *only*:
$\omega_{_\text{Moore}}: Q \to \Gamma$.
When represented in a diagram, each vertex (state) is labeled with an output value.

Despite these differences, both machines are equivalent in that we can construct a corresponding Moore machine for any given Mealy machine that produces the same output behavior. Conversely, we can construct a corresponding Mealy machine for any given Moore machine that replicates its behavior.

**Note**. In a Mealy-to-Moore conversion, the first output of the Moore machine is fixed by its initial state. Subsequent outputs match those of the Mealy machine, possibly with a one-symbol delay.

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
