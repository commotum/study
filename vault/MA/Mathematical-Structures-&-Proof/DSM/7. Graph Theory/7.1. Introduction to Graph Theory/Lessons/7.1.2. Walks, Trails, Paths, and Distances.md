# Walks, Trails, Paths, and Distances

<!--
lesson-id: 951
topic-code: DSM.7.1.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Walks, Trails, and Paths in Undirected Graphs](#identifying-walks-trails-and-paths-in-undirected-graphs)
- [Distances](#distances)
- [Weighted Graphs](#weighted-graphs)
- [Finding Distances in Undirected Graphs](#finding-distances-in-undirected-graphs)
- [Defining Eccentricity, Radius, Diameter, and Center for Unweighted Graphs](#defining-eccentricity-radius-diameter-and-center-for-unweighted-graphs)
- [Finding Numerical Characteristics of a Graph](#finding-numerical-characteristics-of-a-graph)

## Prerequisites

- [Introduction to Graphs](<7.1.1. Introduction to Graphs.md>)
- [The Maximum and Minimum of a Set](<../../../../MOP/2. Set Theory/2.1. Introduction to Set Theory/Lessons/2.1.9. The Maximum and Minimum of a Set.md>)

---

<a id="introduction"></a>
## Introduction

A **walk** $w$ of length $n$ in an undirected graph is a sequence of $n$ edges $e_1,e_2,\ldots,e_n$ with the associated vertices $v_1,v_2,\ldots,v_{n+1}$ where the edge $e_i$ is incident on vertices $v_i$ and $v_{i+1}$ for each

$$
i=1,2,\ldots,n
$$

This can be written as follows:

$$
w=v_1 \overset{e_1}{-} v_2 \overset{e_2}{-} v_3 \overset{e_3}{-} \cdots \overset{e_n}{-} v_{n+1}
$$

In a walk, it is possible to visit any vertex or traverse any edge multiple times. For example, consider the graph below.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/5f417ba8595aaad65c2da50d1c1c2ccc.png>)

Notice that the walk $w_1$ visits vertex $v_2$ three times and traverses edges $e_2$ and $e_5$ twice each.

$$
w_1=v_1 \overset{e_1}{-} v_2 \overset{e_5}{-} v_4 \overset{e_5}{-} v_2 \overset{e_2}{-} v_3 \overset{e_2}{-} v_2
$$

In a graph with no parallel edges, we usually omit the $e_i$'s in the above notation since any corresponding pair of incident vertices uniquely determines an edge. Thus, the walk above can be written equally correctly as follows:

$$
w_1=v_1 - v_2 - v_4 - v_2 - v_3 - v_2
$$

A **trail** is a walk in which all edges are distinct. The walk $w_1$ above is not a trail because it includes the repeated edges $e_2$ and $e_5$. As an example of a trail, consider the same graph, but with a new walk highlighted.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/92cef1b7639d3d7dee362820ba18ae67.png>)

The walk $w_2$ is a valid trail because, although it has the repeated vertex $v_2$, all its edges are distinct.

$$
w_2=v_1 - v_2 - v_4 - v_3 - v_2 - v_5
$$

A **path** is a walk in which all vertices (and, as a result, all edges) are distinct. Neither $w_1$ nor $w_2$ above is a path because they have the repeated vertex $v_2$. As an example of a path, consider the same graph, but with a new walk highlighted.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/cd449fce0ac2e17922d3c2251fe551f1.png>)

The walk $w_3$ is a valid path because it does not contain any repeated vertices or edges.

$$
w_3=v_5 - v_2 - v_3 - v_4
$$

---

<a id="identifying-walks-trails-and-paths-in-undirected-graphs"></a>
## Identifying Walks, Trails, and Paths in Undirected Graphs

**Example:** Which of the following statements are true about the graph shown above?

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/bfa03ff87fe442fcbc6c0138cc6a9224.png>)

1. $v_2 - v_3 - v_4 - v_2 - v_3 - v_4$ is a walk
2. $v_1 - v_2 - v_4 - v_5 - v_6 - v_8 - v_1$ is a trail
3. $v_1 - v_3 - v_5 - v_7$ is a path

**Explanation**

A *walk* of length $n$ in an undirected graph is a sequence of $n$ edges $e_1,e_2,\ldots,e_n$ with the associated vertices $v_1,v_2,\ldots,v_{n+1}$ where the edge $e_i$ is incident on vertices $v_i$ and $v_{i+1}$ for each

$$
i=1,2,\ldots,n
$$

This can be written as follows:
$v_1 \overset{e_1}{-} v_2 \overset{e_2}{-} v_3 \overset{e_3}{-} \cdots \overset{e_n}{-} v_{n+1}$
Suppose the graph has no multiple (parallel) edges. In that case, we can drop the $e_i$'s in the above notation since any corresponding pair of incident vertices will uniquely determine any edge.

A *trail* is a walk in which all edges are distinct.

A *path* is a walk in which all vertices (and, as a result, all edges) are distinct.

With that in mind, let's examine our statements.

- Statement I is true. Indeed, it's a walk since we can write it as
$v_2 \overset{e_2}{-} v_3 \overset{e_3}{-} v_4 \overset{e_9}{-} v_2 \overset{e_2}{-} v_3 \overset{e_3}{-} v_4$
where vertices and edges can be repeated.
- Statement II is true. Indeed, it's a trail since we can write it as
$v_1 \overset{e_1}{-} v_2 \overset{e_9}{-} v_4 \overset{e_4}{-} v_5 \overset{e_5}{-} v_6 \overset{e_{11}}{-} v_8 \overset{e_8}{-} v_1$
where all edges are distinct.
- Statement III is false. Notice that it's not a walk, and consequently, it is not a path: no edge directly joins vertices $v_1$ and $v_3$.

Therefore, the correct answer is "I and II only."

---

**Question 1**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-241782.png>)

Given the graph above, which of the following statements are true?

1. $v_{1} - v_{2} - v_{3} - v_{4} - v_{2}$ is a trail
2. $v_{1} - v_{2} - v_{5} - v_{7} - v_{6}$ is a walk
3. $v_{1} - v_{2} - v_{3} - v_{5} - v_{6} - v_{7}$ is a path

- [ ] A. None
- [ ] B. II and IIII only
- [ ] C. II only
- [ ] D. I and III only
- [ ] E. I and II only

---

**Question 2**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-257168.png>)

How many distinct trails exist between the vertices $v_{1}$ and $v_{4}$?

- [ ] A. $2$
- [ ] B. Infinitely many trails
- [ ] C. $3$
- [ ] D. $0$
- [ ] E. $4$

---

<a id="distances"></a>
## Distances

The **distance** between vertices $u$ and $v$ in a graph, $d(u,v)$, is the length (i.e., the number of edges) of the shortest path from $u$ to $v$. If no path from $u$ to $v$ exists, we say that the distance is $\infty$.

For example, consider the graph below.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/8faed092f37a14206d21a4fe32c4c9c5.png>)

Let's determine the distances between the following vertices:

- The shortest path from $v_1$ to $v_4$ is $v_1-v_2-v_4$. It consists of $2$ edges. Thus,

$$
\begin{aligned}
d(v_{1}, v_{4}) &= 2
\end{aligned}
$$

- The shortest path from $v_5$ to $v_8$ is $v_5-v_6-v_7-v_8$. It consists of $3$ edges. Thus,

$$
\begin{aligned}
d(v_{5}, v_{8}) &= 3
\end{aligned}
$$

- There are no paths from $v_2$ to $v_5$. Thus,

$$
\begin{aligned}
d(v_{2}, v_{5}) &= ∞
\end{aligned}
$$

Several algorithms exist to find the shortest path between two vertices in a graph; they will be discussed in future lessons.

---

<a id="weighted-graphs"></a>
## Weighted Graphs

In a **weighted** graph, a numerical value (called a **weight**) is associated with each edge. Although weights can be any real numbers, only positive numbers are usually used for most practical purposes.

In visual representations, weights are shown as numbers or labels directly on the edges. For example:

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/e53905e52c8bbb11549845c9906db5c2.png>)

The **weight of a walk** (also referred to as *length* or *cost*) in a weighted graph is the sum of the weights of the traversed edges. The same definition applies to trails and paths. We denote the weight of a walk $w$ as $\mid w \mid$, and also $\ell(w)$ when referring to it as length.

For example, consider the walk $w_1$ in the graph above.

$$
w_1=v_1 - v_2 - v_4 - v_2 - v_3 - v_2
$$

This walk consists of five edges, so its weight is equal to the sum of their weights:

$$
\begin{aligned}
\mid w_1 &= 9+3+3+8+8=31
\end{aligned}
$$

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/d3a9099f4fba349cd10149f1cd99465f.png>)

The **distance** between vertices $u$ and $v$ in a weighted graph, $d(u,v)$, is the weight of the *shortest path* (the path with the least weight) from $u$ to $v$. As in unweighted graphs, if no path from $u$ to $v$ exists, the distance is considered $\infty$.

For example, in the graph above, the shortest path from $v_1$ to $v_3$ is $(v_1 - v_5 - v_4 - v_3)$; therefore, the distance is

$$
d(v_1, v_3)=8+1+7=16
$$

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/3b04a723292789ec81fcc7956fc77f34.png>)

---

<a id="finding-distances-in-undirected-graphs"></a>
## Finding Distances in Undirected Graphs

**Example:** Find the distance from $v_1$ to $v_5$ in the weighted graph above.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/d2913c50633aa8965a2c2f745fa756ce.png>)

**Explanation**

The distance between vertices $u$ and $v$ in a graph is the length of the shortest path from $u$ to $v$. In a weighted graph, the length of the path equals the sum of the weights of corresponding edges in the path. If no path from $u$ to $v$ exists, we say that the distance is $\infty$.

In our case, the shortest path from $v_1$ to $v_5$ is
$v_1 - v_2 - v_4 - v_5$
that contains $3$ edges of cumulative length of

$$
6+5+7=18
$$

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/97d838f73f7aea9d06278372f360df99.png>)

Therefore,

$$
d(v_1,v_5)=18
$$

**Note:** There are other paths from $v_1$ to $v_5$ with equal or fewer number of edges. However, the cumulative length of these paths is greater than $18$.

---

**Question 3**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-241807.png>)

In the graph above, the distance from $v_{1}$ to $v_{6}$ is $d(v_{1}, v_{6}) =$ $\underline{\hspace{1.5cm}}$.

---

**Question 4**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-241817.png>)

In the weighted graph above, the distance from $v_{3}$ to $v_{7}$ is $d(v_{3}, v_{7}) =$ $\underline{\hspace{1.5cm}}$.

---

<a id="defining-eccentricity-radius-diameter-and-center-for-unweighted-graphs"></a>
## Defining Eccentricity, Radius, Diameter, and Center for Unweighted Graphs

The **eccentricity** of a vertex $v$ in a graph $G=(V,E)$, denoted $\epsilon(v)$, is the maximum distance between $v$ and any other vertex of the graph:

$$
\epsilon(v) = \max_{w \in V} \left\{d(v,w) \right\}
$$

If there is at least one vertex $w$ such that no path exists from $v$ to $w$, then $\epsilon(v)$ is considered $\infty$.

For example, consider the graph $G_1$ below.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/6e6e6ec866def9479825479d6cf27876.png>)

In this graph,

$$
d(v_1,v_7)=3
$$

because the shortest path is $(v_1 - v_2 - v_4 - v_7)$. By symmetry,

$$
d(v_5,v_6)=3
$$

From any of the three remaining vertices $v_2, v_3$, and $v_4$, any other vertex can be reached by traversing at most two edges.

Therefore, the eccentricities of the vertices are as follows:

| Vertex, $v$ | $v_1$ | $v_2$ | $v_3$ | $v_4$ | $v_5$ | $v_6$ | $v_7$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Eccentricity, $\epsilon(v)$ | $3$ | $2$ | $2$ | $2$ | $3$ | $3$ | $3$ |

The following three graph parameters (namely, radius, diameter, and center) are based on the concept of eccentricity.

The **radius** of the graph is the minimum eccentricity of the graph's vertices:

$$
r(G) = \min_{v \in V} \{\epsilon(v) \}
$$

In the graph above, the minimum eccentricity is $2$; hence, $r(G_1)=2$.

The **diameter** of the graph is the maximum eccentricity of the graph vertices (alternatively, the diameter is the longest possible distance between the vertices of the graph):

$$
d(G) = \max_{v \in V} \{\epsilon(v) \} = \max_{v, w \in V} \{d(v,w) \}
$$

In the graph above, the maximum eccentricity is $3$; hence, $d(G_1)=3$.

In geometry, the diameter of a circle is always twice the radius. This is not the case for radii and diameters in graphs, as illustrated by the example above, where $d=1.5r$. For *connected* graphs, the following inequality holds:

$$
\text{radius} \leq \textrm{diameter} \leq 2 \cdot \textrm{radius}
$$

You'll learn more about connected graphs in future lessons.

The **center** of a graph is the *set* of all vertices with the minimum eccentricity (i.e., the graph's radius).

$$
{v ∈ V: ϵ(v) = r(G)}
$$

In the graph above, three vertices have the minimum eccentricity of $2$; hence, the center of $G_1$ is $\{v_2, v_3, v_4\}$.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/5846a36dcfd880928e203c3fe8cd0772.png>)

---

<a id="finding-numerical-characteristics-of-a-graph"></a>
## Finding Numerical Characteristics of a Graph

**Example:** Find the radius $r(G)$ and the diameter $d(G)$ of the graph $G$ above.

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/31bff92ef6c09ea27c256744a15d6554.png>)

**Explanation**

The *eccentricity* of a graph vertex $v$ in a connected graph $G$ is the maximum distance between $v$ and other vertex of the graph:

$$
\epsilon(v) = \max_{w \in V} \{d(v,w) \}
$$

where $V$ is the set of graph vertices.

The *radius* of the graph is the minimum eccentricity of the graph vertices:

$$
r(G) = \min_{v \in V} \{\epsilon(v) \}
$$

The *diameter* of the graph is the maximum eccentricity of the graph vertices (alternatively, the diameter is the longest possible distance between the vertices of the graph):

$$
d(G) = \max_{v \in V} \{\epsilon(v) \} = \max_{v, w \in V} \{d(v,w) \}
$$

In our case, the eccentricities of the vertices are as follows:

$$
\begin{aligned}
ϵ(v_{1}) &= 2 \\
ϵ(v_{2}) &= 3 \\
ϵ(v_{3}) &= 4 \\
ϵ(v_{4}) &= 4 \\
ϵ(v_{5}) &= 3 \\
ϵ(v_{6}) &= 4 \\
ϵ(v_{7}) &= 4
\end{aligned}
$$

Therefore, the required radius and diameter are

$$
r(G) = min_(v ∈ V){ϵ(v)} = 2,; d(G) = max_(v ∈ V){ϵ(v)} = 4
$$

**Note:** For any connected graph, we have the following inequality:

$$
\text{radius} \leq \textrm{diameter} \leq 2 \cdot \textrm{radius}
$$

---

**Question 5**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-241820.png>)

The eccentricity of the vertex $v_{2}$ in the above graph is $ϵ(v_{2}) =$ $\underline{\hspace{1.5cm}}$.

---

**Question 6**

![](<../Source/7.1.2. Walks, Trails, Paths, and Distances/Images/q-241824.png>)

Which of the following is the center of the above graph?

- [ ] A. ${v_{1}, v_{6}, v_{7}}$
- [ ] B. ${v_{3}}$
- [ ] C. ${v_{3}, v_{5}}$
- [ ] D. ${v_{2}, v_{3}}$
- [ ] E. ${v_{2}}$

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
