# Kruskal's Algorithm

<!--
lesson-id: 1393
topic-code: DSM.7.6.2
-->

## Table of Contents

- [Introduction](#introduction)
- [A Concrete Example of Kruskal's Algorithm](#a-concrete-example-of-kruskals-algorithm)
- [Applying a Step of Kruskal's Algorithm](#applying-a-step-of-kruskals-algorithm)
- [Applying Kruskal's Algorithm](#applying-kruskals-algorithm)

## Prerequisites

- [Trees and Forests](<../../7.3. Trees/Lessons/7.3.1. Trees and Forests.md>)
- [Connected Graphs](<../../7.2. Connectivity in Graphs/Lessons/7.2.2. Connected Graphs.md>)

---

<a id="introduction"></a>
## Introduction

**Kruskal's algorithm** finds a *minimum spanning tree* of a connected, weighted graph $G$. It follows a so-called **greedy approach,** selecting the edges in increasing order of weight while avoiding cycles. It is used in network design, cluster analysis in machine learning, and image segmentation.

The steps for the Kruskal's algorithm are as follows:

- **Step 1:** Initiate a forest containing all the vertices of $G$ but no edges.
- **Step 2:** Order the edges of $G$ by their weight from smallest to largest.
- **Step 3:** Iterate through this list of edges in ascending order, adding an edge to the forest only if its ends belong to different connected components (i.e., only if it doesn't create a cycle).
- **Step 4:** Terminate the iteration when all vertices are included in a single connected component with no cycles.

We can express Kruskal's algorithm using **pseudocode**. Pseudocode is a simplified, programming language-independent way of describing an algorithm. It uses a mix of natural language and programming concepts, helping programmers to plan and visualize code logic without focusing on syntax.

The pseudocode for Kruskal's algorithm is as follows.

> `Initialize a forest containing all the vertices of G and none
> of its edges`
> `def Kruskal(): # Kruskal's algorithm`
> `Order the edges of G by their weight from smallest to largest`
> `for (e in ordered list of edges): # Go over all edges`
> `if (ends of e belong to different connected components):`
> `Add e to the forest`

Let's take a look at how to apply Kruskal's algorithm to a concrete graph.

---

<a id="a-concrete-example-of-kruskals-algorithm"></a>
## A Concrete Example of Kruskal's Algorithm

![](<../Source/7.6.2. Kruskal's Algorithm/Images/92402ae32dc0f3e1070d0b97b33a60a0.png>)

Let's use Kruskal's algorithm to find a minimum spanning tree of the graph given above.

First, we put all the vertices of our graph into a forest with no edges.

![](<../Source/7.6.2. Kruskal's Algorithm/Images/a943b915ac4e05ad3f68abd6612138e2.png>)

The dashed lines denote the edges of the original graph.

Next, we order the edges of the original graph according to their weights:

$$
v_{1} - v_{4}, v_{1} - v_{5}, v_{4} - v_{5}, v_{3} - v_{5}, v_{3} - v_{4},; v_{2} - v_{3}, v_{1} - v_{2}, v_{2} - v_{5}
$$

Now, we consider each edge in turn.

- If an edge's endpoints (vertices) are in two *different* connected components of the forest, we add the edge to the forest.
- If an edge's endpoints (vertices) are in the *same* connected components of the forest, we do *not* add the edge to the forest.

We proceed step-by-step.

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/4cf6cbc7df6eb7816253ee6f99cf0e1f.png>) | Add the edge $v_1 - v_4$ of the smallest weight to the forest. |
| **Step 2.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/38f43f8cf53f2928996ab1eb3ec97915.png>) | Add the edge $v_1 - v_5$ of the smallest weight to the forest. |
| **Step 3.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/7e0c9b9aad03703fd84de95d16e3d468.png>) | The next edge in the list is $v_4 - v_5$. Since its ends belong to the same connected component of the forest, we don't add it to the forest (adding it to the forest would form a cycle). |
| **Step 4.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/e544bac829d8298749ad5150bf99a36b.png>) | Add the edge $v_3 - v_5$ of the smallest weight to the forest. |
| **Step 5.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/c2005f13a0016d8e17b1c4d3bf677733.png>) | The next edge in the list is $v_3 - v_4$. Since its ends belong to the same connected component of the forest, we don't add it to the forest (adding it to the forest would form a cycle). |
| **Step 6.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/ca2f381c20c5e73400dd25a6109e516b.png>) | Add the edge $v_2 - v_3$ of the smallest weight to the forest. |

All vertices of the initial graph are included in a single connected component with no cycles. So, we obtained a spanning tree.

The total weight of the resulting minimum spanning tree is

$$
4 + 5 + 7 + 11 = 27
$$

---

<a id="applying-a-step-of-kruskals-algorithm"></a>
## Applying a Step of Kruskal's Algorithm

**Example:** Consider the weighted graph $G$ with vertices $V=\{v_1, \ldots, v_7\}$ and edges indicated above. Kruskal's algorithm for finding a minimum spanning tree is applied to $G$, and after a few steps, the highlighted edges are added to the forest. According to the algorithm, what edge will be added next to the forest?

![](<../Source/7.6.2. Kruskal's Algorithm/Images/6b607bbf0b02f796b5f19b2ff237a4e7.png>)

**Explanation**

Kruskal's algorithm finds a minimum spanning tree of a connected, weighted graph $G$. The pseudocode for Kruskal's algorithm is the following:

> `Initialize a forest containing all the vertices of G and none
> of its edges`
> `def Kruskal(): # Kruskal's algorithm`
> `Order the edges of G by their weight from smallest to largest`
> `for (e in ordered list of edges): # Go over all edges`
> `if (ends of e belong to different connected components):`
> `Add e to the forest`

By inspection, the order of the edges of the original graph according to their weights is as follows:

$$
v_{2} - v_{5}, v_{1} - v_{4}, v_{1} - v_{2}, v_{1} - v_{5}, v_{5} - v_{6},; v_{3} - v_{6}, v_{2} - v_{3}, v_{3} - v_{5}, v_{4} - v_{5}
$$

We see that $v_2-v_5, v_1-v_4$ and $v_1-v_2$ have already been added to the forest.

$$
v_{2} - v_{5}, v_{1} - v_{4}, v_{1} - v_{2}, v_{1} - v_{5}, v_{5} - v_{6},; v_{3} - v_{6}, v_{2} - v_{3}, v_{3} - v_{5}, v_{4} - v_{5}
$$

So, at the next step of Kruskal's algorithm, we consider the edge with the smallest weight among the remaining ones. This is the edge $v_1 - v_5$. However, adding it to our forest will form a cycle since $v_1$ and $v_5$ belong to the same connected component.

![](<../Source/7.6.2. Kruskal's Algorithm/Images/71fa18be60bc040c0f17912a994ffcc6.png>)

Hence, we consider the next edge of the smallest weight: edge $v_5-v_6$. Adding it to our forest will not form a cycle since $v_5$ and $v_6$ belong to different connected components.

![](<../Source/7.6.2. Kruskal's Algorithm/Images/5f55e024da0f9c3bd5e103436d9ac348.png>)

Therefore, the correct answer is $v_5-v_6$.

---

**Question 1**

```quiz
type: radio
id: ma-259460
content: |-
  ![](<../Source/7.6.2. Kruskal's Algorithm/Images/q-259460.png>)

  Consider the weighted graph $G$ with vertices $V = {v_{1}, …, v_{5}}$ and edges indicated above. Kruskal's algorithm for finding a minimum spanning tree is applied to $G$, and after a few steps, the highlighted edges are added to the forest. According to the algorithm, what edge will be added next to the forest?
options:
- id: a
  content: |-
    $v_{1} - v_{4}$
- id: b
  content: |-
    $v_{2} - v_{3}$
- id: c
  content: |-
    $v_{3} - v_{4}$
- id: d
  content: |-
    $v_{3} - v_{5}$
- id: e
  content: |-
    $v_{2} - v_{4}$
  correct: true
```

---

**Question 2**

```quiz
type: radio
id: ma-257946
content: |-
  ![](<../Source/7.6.2. Kruskal's Algorithm/Images/q-257946.png>)

  Consider the weighted graph $G$ with vertices $V = {v_{1}, …, v_{7}}$ and edges indicated above. Kruskal's algorithm for finding a minimum spanning tree is applied to $G$, and after a few steps, the highlighted edges are added to the forest. According to the algorithm, what edge will be added next to the forest?
options:
- id: a
  content: |-
    $v_{3} - v_{6}$
- id: b
  content: |-
    $v_{2} - v_{4}$
- id: c
  content: |-
    $v_{2} - v_{6}$
  correct: true
- id: d
  content: |-
    $v_{4} - v_{7}$
- id: e
  content: |-
    $v_{3} - v_{4}$
```

---

<a id="applying-kruskals-algorithm"></a>
## Applying Kruskal's Algorithm

**Example:** Use Kruskal's algorithm to find a minimum spanning tree of the graph given above.

![](<../Source/7.6.2. Kruskal's Algorithm/Images/4381d42667d8ddee47744ad98942770b.png>)

**Explanation**

Kruskal's algorithm finds a minimum spanning tree of a connected, weighted graph $G$. The pseudocode for Kruskal's algorithm is the following:

> `Initialize a forest containing all the vertices of G and none
> of its edges`
> `def Kruskal(): # Kruskal's algorithm`
> `Order the edges of G by their weight from smallest to largest`
> `for (e in ordered list of edges): # Go over all edges`
> `if (ends of e belong to different connected components):`
> `Add e to the forest`

First, we put all the vertices of our graph into a forest with no edges.

![](<../Source/7.6.2. Kruskal's Algorithm/Images/81f7258fa68a1dcffd7768782aaef4bd.png>)

The dashed lines denote the edges of the original graph.

Next, we order the edges of the original graph according to their weights:

$$
v_{2} - v_{5}, v_{1} - v_{4}, v_{1} - v_{2}, v_{1} - v_{5}, v_{5} - v_{6},; v_{3} - v_{6}, v_{2} - v_{3}, v_{3} - v_{5}, v_{4} - v_{5}
$$

Now, we consider each edge in turn.

- If an edge's endpoints (vertices) are in two *different* connected components of the forest, we add the edge to the forest.
- If an edge's endpoints (vertices) are in the *same* connected components of the forest, we do *not* add the edge to the forest.

We proceed step-by-step.

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/ba1b7593ba54ae4692dc602d001b36cf.png>) | Add the edge $v_2 - v_5$ of the smallest weight to the forest. |
| **Step 2.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/5dee90a7e0a86edb90e8b8ce787bc5d8.png>) | Add the edge $v_1 - v_4$ of the smallest weight to the forest. |
| **Step 3.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/7264e35c1b003bf270b950c7752c2f15.png>) | Add the edge $v_1 - v_2$ of the smallest weight to the forest. |
| **Step 4.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/77c78ec5f5c4b81c9a0b579285dfed65.png>) | The next edge in the list is $v_1 - v_5$. Since its ends belong to the same connected component of the forest, we don't add it to the forest (adding it to the forest would form a cycle). |
| **Step 5.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/f0dcb4ce5e481cfc6d0aeac67b446f87.png>) | Add the edge $v_5 - v_6$ of the smallest weight to the forest. |
| **Step 6.** |  |
| ![](<../Source/7.6.2. Kruskal's Algorithm/Images/947716ab5a255a27e07ee8dece6fcf7d.png>) | Add the edge $v_3 - v_6$ of the smallest weight to the forest. |

All vertices of the initial graph are included in a single connected component with no cycles. So, we obtained a spanning tree.

The total weight of the resulting minimum spanning tree is

$$
2 + 4 + 5 + 9 + 10 = 30
$$

---

**Question 3**

![](<../Source/7.6.2. Kruskal's Algorithm/Images/q-258717.png>)

Use Kruskal's algorithm to find a minimum spanning tree of the graph given above.First, add all the $\underline{\hspace{2cm}}$ of our graph to a forest with no $\underline{\hspace{2cm}}$ and order the edges by their weight from $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] edges
- [ ] vertices

#### Select 2

- [ ] edges
- [ ] vertices

#### Select 3

- [ ] smallest to largest
- [ ] largest to smallest

---

**Question 4**

![](<../Source/7.6.2. Kruskal's Algorithm/Images/q-257916.png>)

Use Kruskal's algorithm to find a minimum spanning tree of the graph given above.First, add all the $\underline{\hspace{2cm}}$ of our graph to a forest with no $\underline{\hspace{2cm}}$ and order the edges by their weight from $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] edges
- [ ] vertices

#### Select 2

- [ ] vertices
- [ ] edges

#### Select 3

- [ ] smallest to largest
- [ ] largest to smallest

```update-progress
```

[[MA/Mathematical-Structures-&-Proof/DSM/Home|Home]]
[[MA/Mathematical-Structures-&-Proof/DSM/0. Table of Contents/TOC|Table of Contents]]
