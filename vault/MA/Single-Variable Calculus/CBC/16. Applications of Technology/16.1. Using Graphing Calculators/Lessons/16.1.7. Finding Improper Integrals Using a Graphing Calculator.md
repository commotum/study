# Finding Improper Integrals Using a Graphing Calculator

<!--
lesson-id: 4084
topic-code: CBC.16.1.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Some Important Notes](#some-important-notes)
- [Approximating an Improper Integral With an Unbounded Upper Limit](#approximating-an-improper-integral-with-an-unbounded-upper-limit)
- [Unbounded Lower Limits](#unbounded-lower-limits)
- [Approximating an Improper Integral With an Unbounded Lower Limit](#approximating-an-improper-integral-with-an-unbounded-lower-limit)
- [Improper Integrals Over the Real Line](#improper-integrals-over-the-real-line)
- [Approximating an Improper Integral Over the Real Line](#approximating-an-improper-integral-over-the-real-line)

## Prerequisites

- [Improper Integrals Over the Real Line](<../../../8. Techniques of Integration/8.6. Improper Integrals/Lessons/8.6.4. Improper Integrals Over the Real Line.md>)
- [Finding Definite Integrals Using a Graphing Calculator](<16.1.6. Finding Definite Integrals Using a Graphing Calculator.md>)

---

<a id="introduction"></a>
## Introduction

We've seen how to approximate definite integrals using a graphing calculator. In this lesson, we'll apply the techniques we've learned to approximate improper integrals.

Suppose we wish to evaluate the following improper integral to three decimal places:

$$
\int_1^\infty \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x
$$

Your graphing calculator is unlikely to include an $\infty$ button, so we cannot directly enter this improper integral into our graphing calculator.

However, recall that, by definition

$$
\int_1^\infty \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x = \lim\limits_{a\to\infty} \int_1^a \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x
$$

So instead, let's consider the definite integral

$$
\int_1^a \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x
$$

We now use our graphing calculator to approximate the above integral for increasing values of $a{:}$

- Setting $a=9$, we have
$\int_1^9 \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x \approx 0.170$
rounded to $3$ decimal places.
- Setting $a=99$, we have
$\int_1^{99} \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x \approx 0.171$
rounded to $3$ decimal places.
- Setting $a=999$, we have
$\int_1^{999} \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x \approx 0.171$
rounded to $3$ decimal places.

Since the integrals with upper limits $99$ and $999$ both give $0.171$ to $3$ decimal places, we conclude that

$$
\int_1^\infty \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x \approx 0.171
$$

rounded to $3$ decimal places.

---

<a id="some-important-notes"></a>
## Some Important Notes

Before we look at another example, you should note the following important points:

- In the previous example, you may wonder why we didn't approximate our improper integral by picking a very large value for $a$ right off the bat, e.g.,
$\int_1^{999\,999\,999} \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x$.
If we were to try this, one of two things might happen:The first is that the calculator might report an error. This is likely due to a lack of physical memory available.The second is that the calculator might give an incorrect answer! Evaluating the above on a TI-84 Plus CE-T gives
$\int_1^{999\,999\,999} \dfrac{\sqrt{\ln{x}}}{x^4}\,\textrm d x \approx 1.348\times 10^{-19}$
which is *incorrect!* Again, this incorrect answer is likely due to physical limitations that apply to all calculators.The critical takeaway is that the only safe way to approximate improper integrals is by using the method described earlier.
- In the last example, we increased the upper limit by appending an extra digit $9$ each time.
$\int_1^9\qquad \int_1^{99}, \qquad \int_1^{999}, \qquad \int_1^{9\,999}, \qquad \ldots$
If your calculator reports errors before you've reached a definite answer, consider increasing the upper limit more slowly. For example,
$\int_1^{10}\qquad \int_1^{100}, \qquad \int_1^{200}, \qquad \int_1^{300}, \qquad \ldots\,$.
- Also, bear in mind that some integrals will converge faster than others. Integrals that converge slowly may require us to pick large values for $a$ before we can be sure of our final answer.
- Finally, throughout this lesson, you may assume that all improper integrals we'll encounter are convergent and that all of the functions we'll integrate decay to zero "rapidly enough" for this method to be suitable.

---

<a id="approximating-an-improper-integral-with-an-unbounded-upper-limit"></a>
## Approximating an Improper Integral With an Unbounded Upper Limit

**Example:** Approximate $\displaystyle \int_{1}^{\infty} \sqrt{2x}e^{-x^2} \, \textrm{d}x$ to three decimal places.

**Explanation**

We bring up the integral option on most calculators in one of the following ways:

- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,2\text{nd}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{shift}\,}$) button, then the $\bbox[2px, border: 2px solid gray]{\displaystyle\color{gray}\,\int_{\bbox[gray]{\,\phantom{0}}}^{\bbox[gray]{\,\phantom{0}}} \bbox[gray]{\,\phantom{\big \mid 0\big \mid}}\,}$ button.
- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{math}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{calc}\,}$) button and choose the integral option. Note that this might be called $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{fnInt}\,}$ or similar.

To calculate our improper integral, we insert *increasingly large* numbers in place of $\infty$ until successive approximations are the same, as follows:

- Evaluating the integral with upper limit $9$ using a calculator, we get
$\int_{1}^{9} \sqrt{2x}e^{-x^2} \, \textrm{d}x \approx \bbox[2px, lightgray]{0.225}$,
rounded to $3$ decimal places.
- Evaluating the integral with upper limit $99$ using a calculator, we obtain
$\int_{1}^{99} \sqrt{2x}e^{-x^2} \, \textrm{d}x \approx \bbox[2px, lightgray]{0.225}$,
rounded to $3$ decimal places.

We can stop now since the last two approximations were $0.225$ to $3$ decimal places.
Therefore,

$$
\int_{1}^{\infty} \sqrt{2x}e^{-x^2} \, \textrm{d}x \approx 0.225
$$

rounded to $3$ decimal places.

---

**Question 1**

> A graphing calculator is required to answer this question.

Use a calculator to approximate $∫_{0}^{∞}\frac{\sin^{4} x + 1}{e^{x}}dx$ rounded to $2$ decimal places.

- [ ] A. $1.28$
- [ ] B. $1.27$
- [ ] C. $1.29$
- [ ] D. $1.30$
- [ ] E. $1.26$

---

**Question 2**

> A graphing calculator is required to answer this question.

$∫_{2}^{∞}\frac{\sqrt{x}}{2^{x} + x^{3}}dx \approx$

- [ ] A. $0.164$
- [ ] B. $0.160$
- [ ] C. $0.163$
- [ ] D. $0.161$
- [ ] E. $0.162$

---

<a id="unbounded-lower-limits"></a>
## Unbounded Lower Limits

Suppose we have an improper integral with an unbounded lower limit, e.g.,

$$
\int_{-\infty}^{-1} \dfrac{e^{-x^2}}{x^2}
$$

To approximate this integral, let's write

$$
\int_{a}^{-1} \dfrac{e^{-x^2}}{x^2}
$$

Now, we use our graphing calculator to approximate the above integral for *decreasing* values of $a{:}$

- Setting $a=-9$, we have
$\int_{-9}^{-1} \dfrac{e^{-x^2}}{x^2} \approx 0.089$
rounded to $3$ decimal places.
- Setting $a=-99$, we have
$\int_{-99}^{-1} \dfrac{e^{-x^2}}{x^2} \approx 0.089$
rounded to $3$ decimal places.

Since the integrals with upper limits $9$ and $99$ both give $0.089$ to $3$ decimal places, we conclude that

$$
\int_{-\infty}^{-1} \dfrac{e^{-x^2}}{x^2} \approx 0.089
$$

rounded to $3$ decimal places.

---

<a id="approximating-an-improper-integral-with-an-unbounded-lower-limit"></a>
## Approximating an Improper Integral With an Unbounded Lower Limit

**Example:** Approximate $\displaystyle \int_{-\infty}^{0} \dfrac{\mid x \mid ^3}{2^{-x} + x} \, \textrm{d}x$ to $2$ decimal places.

**Explanation**

We bring up the integral option on most calculators in one of the following ways:

- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,2\text{nd}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{shift}\,}$) button, then the $\bbox[2px, border: 2px solid gray]{\displaystyle\color{gray}\,\int_{\bbox[gray]{\,\phantom{0}}}^{\bbox[gray]{\,\phantom{0}}} \bbox[gray]{\,\phantom{\big \mid 0\big \mid}}\,}$ button.
- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{math}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{calc}\,}$) button and choose the integral option. Note that this might be called $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{fnInt}\,}$ or similar.
- To access the absolute value function, you can locate this function on your calculator (on some models, you can access this by typing $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{math}\,}$ and then selecting $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{abs}\,}$ under the $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{num}\,}$ submenu). Or, you can recall that $\mid x \mid = \sqrt{x^2}$ and rewrite the integral as
$\int_{-\infty}^{0} \dfrac{(\sqrt{x^2})^3}{2^{-x} + x} \, \textrm{d}x$.

To calculate our improper integral, we insert *decreasingly small* numbers in place of $-\infty$ until successive approximations are the same, as follows:

- Evaluating the integral with lower limit $-10$ using a calculator, we have
$\int_{-10}^{0} \dfrac{\mid x \mid ^3}{2^{-x} + x} \, \textrm{d}x \approx 30.954$,
rounded to $3$ decimal places.
- Evaluating the integral with lower limit $-100$ using a calculator, we get
$\int_{-100}^{0} \dfrac{\mid x \mid ^3}{2^{-x} + x} \, \textrm{d}x \approx \bbox[2px, lightgray]{33.183}$,
rounded to $3$ decimal places.
- Evaluating the integral with lower limit $-200$ using a calculator, we obtain
$\int_{-200}^{0} \dfrac{\mid x \mid ^3}{2^{-x} + x} \, \textrm{d}x \approx \bbox[2px, lightgray]{33.183}$,
rounded to $3$ decimal places.

We can stop now since the last two approximations were $33.18$ to $2$ decimal places.
Therefore,

$$
\int_{-\infty}^{0} \dfrac{\mid x \mid ^3}{2^{-x} + x} \, \textrm{d}x \approx 33.18
$$

rounded to $2$ decimal places.

---

**Question 3**

> A graphing calculator is required to answer this question.

Use a calculator to approximate $∫_{- ∞}^{-4}\frac{\sqrt[5]{\mid x \mid}}{\sqrt{e^{x}} + x^{2}}dx$ rounded to $2$ decimal places.

- [ ] A. $0.39$
- [ ] B. $0.40$
- [ ] C. $0.38$
- [ ] D. $0.41$
- [ ] E. $0.42$

---

**Question 4**

> A graphing calculator is required to answer this question.

$∫_{- ∞}^{-1}\frac{\sqrt{\mid x \mid}}{x^{10} + 1}dx \approx$

- [ ] A. $0.084$
- [ ] B. $0.085$
- [ ] C. $0.083$
- [ ] D. $0.082$
- [ ] E. $0.081$

---

<a id="improper-integrals-over-the-real-line"></a>
## Improper Integrals Over the Real Line

Suppose we wish to evaluate the following improper integral to three decimal places:

$$
\displaystyle \int_{-\infty}^{\infty} e^{-x^2}\, \textrm{d}x\,
$$

In this case, both the lower *and* upper limits of integration are unbounded.

To approximate our improper integral, we insert decreasingly small numbers in place of $-\infty$ and increasingly large numbers in place of $\infty$ until successive approximations are the same, as follows:

- Evaluating the integral with lower limit $-9$ and upper limit $9$ using a calculator, we have
$\int_{-9}^{9} e^{-x^2} \, \textrm{d}x \approx \bbox[3px, lightgray]{1.772}$,
rounded to $3$ decimal places.
- Evaluating the integral with lower limit $-99$ and upper limit $99$ using a calculator, we have
$\int_{-99}^{99} e^{-x^2} \, \textrm{d}x \approx \bbox[3px, lightgray]{1.772}$,
rounded to $3$ decimal places.

We can stop now since the last two approximations were $1.772$ to $3$ decimal places.
Therefore,

$$
\int_{-\infty}^{\infty} e^{-x^2}\, \textrm{d}x \approx 1.772
$$

rounded to $3$ decimal places.

---

<a id="approximating-an-improper-integral-over-the-real-line"></a>
## Approximating an Improper Integral Over the Real Line

**Example:** Use a calculator to approximate $\displaystyle \int_{-\infty}^{\infty} \dfrac{\ln(x^2+1)}{x^4 + 2} \, \textrm{d}x$ rounded to $2$ decimal places.

**Explanation**

We bring up the integral option on most calculators in one of the following ways:

- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,2\text{nd}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{shift}\,}$) button, then the $\bbox[2px, border: 2px solid gray]{\displaystyle\color{gray}\,\int_{\bbox[gray]{\,\phantom{0}}}^{\bbox[gray]{\,\phantom{0}}} \bbox[gray]{\,\phantom{\big \mid 0\big \mid}}\,}$ button.
- Press the $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{math}\,}$ (or $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{calc}\,}$) button and choose the integral option. Note that this might be called $\bbox[2px, border: 2px solid gray]{\color{gray}\,\text{fnInt}\,}$ or similar.

To calculate our improper integral, we insert decreasingly small numbers in place of $-\infty$ and increasingly large numbers in place of $\infty$ until successive approximations are the same, as follows:

- Evaluating the integral with lower limit $-9$ and upper limit $9$ using a calculator, we get
$\int_{-9}^{9} \dfrac{\ln(x^2+1)}{x^4 + 2} \, \textrm{d}x \approx \bbox[2px, lightgray]{0.73}$,
rounded to $2$ decimal places.
- Evaluating the integral with lower limit $-99$ and upper limit $99$ using a calculator, we obtain
$\int_{-99}^{99} \dfrac{\ln(x^2+1)}{x^4 + 2} \, \textrm{d}x \approx \bbox[2px, lightgray]{0.73}$,
rounded to $2$ decimal places.

We can stop now since the last two approximations were $0.73$, to $2$ decimal places.

Therefore,

$$
\int_{-\infty}^{\infty} \dfrac{\ln(x^2+1)}{x^4 + 2} \, \textrm{d}x \approx 0.73
$$

rounded to $2$ decimal places.

---

**Question 5**

> A graphing calculator is required to answer this question.

Use a calculator to approximate $∫_{- ∞}^{∞}(\ln (1 + x^{2}))/(1 + x^{4})dx$ rounded to $3$ decimal places.

- [ ] A. $0.987$
- [ ] B. $0.985$
- [ ] C. $0.983$
- [ ] D. $0.999$
- [ ] E. $0.984$

---

**Question 6**

> A graphing calculator is required to answer this question.

$∫_{- ∞}^{∞}e^{-x^{2}}\cos xdx \approx$

- [ ] A. $1.377$
- [ ] B. $1.385$
- [ ] C. $1.375$
- [ ] D. $1.382$
- [ ] E. $1.380$

```update-progress
```

[[MA/CBC/Home|Home]]
[[MA/CBC/0. Table of Contents/TOC|Table of Contents]]
