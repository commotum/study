# Finding Equations of Translated Functions

<!--
lesson-id: 6085
topic-code: SMF.11.3.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Vertical Translations](#vertical-translations)
- [Horizontal Translations](#horizontal-translations)
- [Horizontal Translations of Linear Functions](#horizontal-translations-of-linear-functions)
- [Horizontal Translations of Quadratic Functions](#horizontal-translations-of-quadratic-functions)
- [Lines in Standard Form](#lines-in-standard-form)
- [Translating Lines Given in Standard Form](#translating-lines-given-in-standard-form)

## Prerequisites

- [Graphs of General Quadratic Functions](<../../../13. Advanced Algebra - Polynomials/13.4. Quadratic Functions/Lessons/13.4.3. Graphs of General Quadratic Functions.md>)
- [Multiplying Binomials](<../../../13. Advanced Algebra - Polynomials/13.1. Polynomials/Lessons/13.1.7. Multiplying Binomials.md>)
- [Equations of Lines in Standard Form](<../../../3. Algebra - Linear Equations in Two Variables/3.1. Graphs of Linear Equations/Lessons/3.1.11. Equations of Lines in Standard Form.md>)
- [Vertical Translations of Functions](<11.3.1. Vertical Translations of Functions.md>)
- [Horizontal Translations of Functions](<11.3.2. Horizontal Translations of Functions.md>)

---

<a id="introduction"></a>
## Introduction

In this lesson, we'll learn how to derive the equation of a translated function given the equation of some other function.

For example, consider a line defined by

$$
y = \underbrace{-x + 2}_{\large f(x)}
$$

Let's find the equation of the line obtained by translating this line by $6$ units up.

For each point on the original line $y = f(x)$, we must add $6$ to each $y$-coordinate to get the corresponding point on the new line. So, the $y$-coordinates of the points on the new line are given by

$$
y_{\textrm{new}} = y + 6
$$

We introduce the "$\textrm{new}$" suffix to distinguish $y$-values on the translated line from those on the original line.

Substituting the equation of our original line

$$
y = -x + 2
$$

into the above equation gives

$$
y_{\textrm{new}} = (-x + 2) + 6
$$

Since our equation no longer refers to points on the original line, we can drop the "$\textrm{new}$" suffix, which gives

$$
y = (-x + 2) + 6
$$

Finally, collecting the like terms, we have

$$
y = -x + 8
$$

Therefore, we conclude that if we translate the line

$$
y = -x + 2
$$

by $6$ units *up*, we get the line

$$
y = -x + 8
$$

We can apply the same method to translate a quadratic function. Let's see how.

---

<a id="vertical-translations"></a>
## Vertical Translations

**Example:** A curve is defined by $y = f(x)$, where $f(x) = x^2 - 5x + 14$. If $y = g(x)$ is a vertical translation of $y = f(x)$ by $6$ units down, what is the function $g(x)$?

**Explanation**

We're given the curve

$$
y = x^2 - 5x + 14
$$

and we wish to translate this curve by $6$ units down.

For each point on the curve $y = f(x)$, we must subtract $6$ from each $y$-coordinate to get the corresponding point on the new curve $y = g(x)$. So, the $y$-coordinates of the points on the new curve are given by

$$
y_{\textrm{new}} = y - 6
$$

Substituting here the equation of the first curve gives

$$
y_{\textrm{new}} = (x^2 - 5x + 14) - 6
$$

Dropping the $\textrm{new}$ suffix, we get

$$
y = (x^2 - 5x + 14) - 6
$$

Finally, collecting the like terms, we have

$$
y = x^2 - 5x + 8
$$

Therefore,

$$
g(x) = x^2 - 5x + 8
$$

---

**Question 1:** A line is defined by $y = f(x)$, where $f(x) = 2x + 3$. If $y = g(x)$ is a vertical translation of $y = f(x)$ by $4$ units up, what is the function $g(x)$?

- [ ] A. $2x + 5$
- [ ] B. $2x + 4$
- [ ] C. $2x + 1$
- [ ] D. $2x - 1$
- [ ] E. $2x + 7$

---

**Question 2:** A curve is defined by $y = f(x)$, where $f(x) = x^{2} + 2x + 3$. If $y = g(x)$ is a vertical translation of $y = f(x)$ by $4$ units up, what is the function $g(x)$?

- [ ] A. $x^{2} + 2x - 1$
- [ ] B. $x^{2} + 2x - 3$
- [ ] C. $x^{2} + 2x + 8$
- [ ] D. $x^{2} + 2x + 5$
- [ ] E. $x^{2} + 2x + 7$

---

<a id="horizontal-translations"></a>
## Horizontal Translations

Let's now see how to deal with horizontal translations.

For example, suppose a line is defined by

$$
y = \underbrace{-2x + 3}_{\large f(x)}
$$

Let's find the equation of the line we get when we translate this line by $2$ units *left.*

For each point on the line $y = f(x)$, we must subtract $2$ from each $x$-coordinate to get the corresponding point on the new line. So, the $x$-coordinates of the points on the new line are given by

$$
x_{\textrm{new}} = x - 2
$$

This equation is equivalent to

$$
x = x_{\textrm{new}} + 2
$$

To get the equation of the new line, we substitute this into the equation of the first line. Doing this, we get

$$
\begin{aligned}
y &= -2x + 3 \\
y &= -2(x_{new} + 2) + 3
\end{aligned}
$$

Dropping the $\textrm{new}$ suffix, we get

$$
y = -2(x + 2) + 3
$$

Finally, simplifying this expression using the distributive law and collecting like terms, we get

$$
\begin{aligned}
y &= -2x - 4 + 3 \\
y &= -2x - 1
\end{aligned}
$$

---

<a id="horizontal-translations-of-linear-functions"></a>
## Horizontal Translations of Linear Functions

**Example:** A line is defined by $y = f(x)$, where $f(x) = 4x + 2$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $1$ unit right, what is the function $g(x)$?

**Explanation**

We're given the line

$$
y = 4x + 2
$$

and we wish to translate this line by $1$ unit right.

For each point on the line $y = f(x)$, we must add $1$ to each $x$-coordinate to get the corresponding point on the new line $y = g(x)$. So, the $x$-coordinates of the points on the new line are given by

$$
x_{\textrm{new}} = x + 1
$$

This equation is equivalent to

$$
x = x_{\textrm{new}} - 1
$$

To get the equation of the new line, we substitute this into the equation of the first line. Doing this, we get

$$
\begin{aligned}
y &= 4x + 2 \\
y &= 4(x_{new} - 1) + 2
\end{aligned}
$$

Dropping the $\textrm{new}$ suffix, we get

$$
y = 4(x - 1) + 2
$$

Finally, simplifying this expression gives

$$
\begin{aligned}
y &= 4x - 4 + 2 \\
y &= 4x - 2
\end{aligned}
$$

Therefore,

$$
g(x) = 4x - 2
$$

---

**Question 3:** A line is defined by $y = f(x)$, where $f(x) = 2x + 3$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $4$ units left, what is the function $g(x)$?

- [ ] A. $2x + 13$
- [ ] B. $2x + 6$
- [ ] C. $2x + 9$
- [ ] D. $2x + 11$
- [ ] E. $2x + 7$

---

**Question 4:** A line is defined by $y = f(x)$, where $f(x) = 2x + 1$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $3$ units right, what is the function $g(x)$?

- [ ] A. $2x - 1$
- [ ] B. $2x - 7$
- [ ] C. $2x + 1$
- [ ] D. $2x - 3$
- [ ] E. $2x - 5$

---

<a id="horizontal-translations-of-quadratic-functions"></a>
## Horizontal Translations of Quadratic Functions

**Example:** A curve is defined by $y = f(x)$, where $f(x) = -x^2 + x + 2$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $2$ units left, what is the function $g(x)$?

**Explanation**

We're given the curve

$$
y = -x^2 + x + 2
$$

and we wish to translate this curve by $2$ units left.

For each point on the curve $y = f(x)$, we must subtract $2$ from each $x$-coordinate to get the corresponding point on the new curve $y = g(x)$. So, the $x$-coordinates of the points on the new curve are given by

$$
x_{\textrm{new}} = x - 2
$$

This equation is equivalent to

$$
x = x_{\textrm{new}} + 2
$$

To get the equation of the new curve, we substitute this into the equation of the first curve. Doing this, we get

$$
\begin{aligned}
y &= -x^{2} + x + 2 \\
y &= - (x_{new} + 2)^{2} + (x_{new} + 2) + 2
\end{aligned}
$$

Dropping the $\textrm{new}$ suffix, we get

$$
y = -(x + 2)^2 + (x + 2) + 2
$$

Finally, simplifying the expression, we have

$$
\begin{aligned}
y &= - (x + 2)^{2} + (x + 2) + 2 \\
y &= -x^{2} - 4x - 4 + x + 2 + 2 \\
y &= -x^{2} - 3x
\end{aligned}
$$

Therefore,

$$
g(x) = -x^2 - 3x
$$

---

**Question 5:** A curve is defined by $y = f(x)$, where $f(x) = x^{2} + 2x + 3$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $4$ units left, what is the function $g(x)$?

- [ ] A. $x^{2} + 6x + 31$
- [ ] B. $x^{2} + 12x + 18$
- [ ] C. $x^{2} + 10x + 32$
- [ ] D. $x^{2} + 10x + 27$
- [ ] E. $x^{2} + 8x + 25$

---

**Question 6:** A curve is defined by $y = f(x)$, where $f(x) = 2x^{2} - 3x + 4$. If $y = g(x)$ is a horizontal translation of $y = f(x)$ by $3$ units right, what is the function $g(x)$?

- [ ] A. $2x^{2} - 16x + 32$
- [ ] B. $2x^{2} - 14x + 34$
- [ ] C. $2x^{2} - 15x + 31$
- [ ] D. $2x^{2} - 13x + 28$
- [ ] E. $2x^{2} - 12x + 29$

---

<a id="lines-in-standard-form"></a>
## Lines in Standard Form

Now, suppose we have the following line, given in standard form:

$$
x + y = -4
$$

Suppose we translate this line by $2$ units up. We can find the equation of the new line using the same technique as before!

For each point on the line, we must add $2$ to each $y$-coordinate to get the corresponding point on the new line. So, the $y$-coordinates of the points on the new line are given by

$$
y_{\textrm{new}} = y + 2
$$

This equation is equivalent to

$$
y = y_{\textrm{new}} - 2
$$

To get the equation of the new line, we substitute this into the equation of the first line. Doing this, we get

$$
\begin{aligned}
x + y &= -4 \\
x + (y_{new} - 2) &= -4
\end{aligned}
$$

Dropping the $\textrm{new}$ suffix, we get

$$
x + (y - 2) = -4
$$

Finally, simplifying and writing this equation in standard form, we get

$$
\begin{aligned}
x + (y - 2) &= -4 \\
x + y - 2 &= -4 \\
x + y &= -2
\end{aligned}
$$

Let's now consider a situation where we must perform a horizontal translation of a line given in standard form.

---

<a id="translating-lines-given-in-standard-form"></a>
## Translating Lines Given in Standard Form

**Example:** The line $3x + 2y = 1$ is translated by $4$ units left. What is the equation of the new line?

**Explanation**

We're given the line

$$
3x + 2y = 1
$$

and we wish to translate this line by $4$ units left.

For each point on the line, we must subtract $4$ from each $x$-coordinate to get the corresponding point on the new line. So, the $x$-coordinates of the points on the new line are given by

$$
x_{\textrm{new}} = x - 4
$$

This equation is equivalent to

$$
x = x_{\textrm{new}} + 4
$$

To get the equation of the new line, we substitute this into the equation of the first line. Doing this, we get

$$
\begin{aligned}
3x + 2y &= 1 \\
3(x_{new} + 4) + 2y &= 1
\end{aligned}
$$

Dropping the $\textrm{new}$ suffix, we get

$$
3(x + 4) + 2y = 1
$$

Finally, simplifying and writing this equation in standard form, we get

$$
\begin{aligned}
3x + 12 + 2y &= 1 \\
3x + 2y &= -11
\end{aligned}
$$

---

**Question 7:** The line $2x + 3y = 5$ is translated by $4$ units up. What is the equation of the new line?

- [ ] A. $2x + 3y = 17$
- [ ] B. $2x + 3y = 8$
- [ ] C. $2x + 3y = 9$
- [ ] D. $2x + 3y = 15$
- [ ] E. $2x + 3y = 1$

---

**Question 8:** The line $2x + 3y = 5$ is translated by $4$ units right. What is the equation of the new line?

- [ ] A. $2x + 3y = 13$
- [ ] B. $2x + 3y = 1$
- [ ] C. $2x + 3y = 11$
- [ ] D. $2x + 3y =-3$
- [ ] E. $2x + 3y = 9$

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Test-Preparation/SMF/Home|Home]]
[[MA/Test-Preparation/SMF/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
