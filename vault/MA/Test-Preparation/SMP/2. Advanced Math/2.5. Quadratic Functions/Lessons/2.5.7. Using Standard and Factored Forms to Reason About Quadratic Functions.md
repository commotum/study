# Using Standard and Factored Forms to Reason About Quadratic Functions

<!--
lesson-id: 6503
topic-code: SMP.2.5.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding Unknown Quadratic Coefficients Given Equivalent Expressions](#finding-unknown-quadratic-coefficients-given-equivalent-expressions)
- [Identifying a Sum of Coefficients of a Quadratic Function](#identifying-a-sum-of-coefficients-of-a-quadratic-function)
- [Reasoning About Quadratic Equations With Integer Roots](#reasoning-about-quadratic-equations-with-integer-roots)
- [Identifying True Statements About Quadratic Coefficients With Additional Constraints](#identifying-true-statements-about-quadratic-coefficients-with-additional-constraints)

## Prerequisites

- [Quadratic Functions](<2.5.1. Quadratic Functions.md>)
- [Reasoning About Coefficients of Quadratics Using Standard and Factored Forms](<../../../../SMF/13. Advanced Algebra - Polynomials/13.3. Quadratic Equations/Lessons/13.3.27. Reasoning About Coefficients of Quadratics Using Standard and Factored Forms.md>)

---

<a id="introduction"></a>
## Introduction

This topic focuses on understanding how the structure of a quadratic expression reveals useful information about its behavior.

By recognizing connections across different forms, we’ll be able to extract relationships between coefficients, factors, and intercepts.

---

<a id="finding-unknown-quadratic-coefficients-given-equivalent-expressions"></a>
## Finding Unknown Quadratic Coefficients Given Equivalent Expressions

**Example:** The expression $ax^{2} + bx + c$, where $a$, $b$, and $c$ are constants, can be rewritten as $(4x+2)(x+5)$. What are the values of the constants?

**Explanation**

First, expand the factorization:

$$
\begin{aligned}
(4x + 2)(x + 5) &= 4x(x + 5) + 2(x + 5) \\
&= 4x^{2} + 20x + 2x + 10 \\
&= 4x^{2} + 22x + 10
\end{aligned}
$$

Now, comparing this with the general quadratic $ax^{2} + bx + c$, we identify the following:

- The leading coefficient gives $a = 4$.
- The middle coefficient gives $b = 22$.
- The constant term gives $c = 10$.

---

**Question 1**

The expression $ax^{2} + bx + c$, where $a$, $b$, and $c$ are constants, can be rewritten as $(5x + 1)(x + 3)$. What is the value of $a + b + c$?$a + b + c =$ $\underline{\hspace{1.5cm}}$

---

**Question 2**

The expression $ax^{2} + bx + c$, where $a$, $b$, and $c$ are constants, can be rewritten as $(2x + 3)(3x + 1)$. What is the value of $a + b + c$?$a + b + c =$ $\underline{\hspace{1.5cm}}$

---

<a id="identifying-a-sum-of-coefficients-of-a-quadratic-function"></a>
## Identifying a Sum of Coefficients of a Quadratic Function

**Example:** The function $h$ is defined by $h(x) = a x^{2} + b x + c$, where $a, b$, and $c$ are constants. The graph of $y = h(x)$ in the $xy$-plane passes through the points $(2, 0)$ and $(-3, 0)$. If $a$ is an integer greater than $1$, which of the following could be the value of $a + b$?

1. $2$
2. $10$
3. $15$

**Explanation**

Since the graph passes through $(2, 0)$ and $(-3, 0)$, which are the $x$-intercepts (roots) where $y = 0$, the quadratic factored form of the function $h$ is

$$
h(x) = a(x - 2)(x + 3)
$$

Next, we expand:

$$
\begin{aligned}
h(x) &= a(x^{2} + x - 6) \\
&= ax^{2} + ax - 6a
\end{aligned}
$$

So, comparing the coefficient of $x$ in the above expression with the given form

$$
h(x) = a x^2 + b x + c
$$

we get

$$
b = a
$$

Thus,

$$
a + b = a + a = 2a
$$

Now, we're told that $a$ is *an integer greater than $1$.* This means that the possible values of $2a$ are all the even integers greater than $2{:}$

$$
4, 6, 8, 10,\ldots
$$

With that in mind, let's examine each option:

- $2$ is NOT an even integer greater than $2.\:\:{\color{red}{\times}}$
- $10$ is an even integer greater than $2.\:\:{\color{green}{\checkmark}}$
- $15$ is NOT an even integer greater than $2.\:\:{\color{red}{\times}}$

Therefore, the correct answer is "II only."

---

**Question 3**

The function $g$ is defined by $g(x) = ax^{2} + bx + c$, where $a, b$, and $c$ are constants. The graph of $y = g(x)$ in the $xy$-plane passes through the points $(4, 0)$ and $(-2, 0)$. If $a$ is an integer greater than $1$, which of the following could be the value of $a + b$?

1. $-3$
2. $0$
3. $3$

- [ ] A. None
- [ ] B. I only
- [ ] C. III only
- [ ] D. II only
- [ ] E. I and II only

---

**Question 4**

The function $h$ is defined by $h(x) = ax^{2} + bx + c$, where $a, b$, and $c$ are constants. The graph of $y = h(x)$ in the $xy$-plane passes through the points $(-1, 0)$ and $(5, 0)$. If $a$ is an integer smaller than $-1$, which of the following could be the value of $a + c$?

1. $-4$
2. $10$
3. $12$

- [ ] A. I and III only
- [ ] B. II only
- [ ] C. None
- [ ] D. I only
- [ ] E. III only

---

<a id="reasoning-about-quadratic-equations-with-integer-roots"></a>
## Reasoning About Quadratic Equations With Integer Roots

**Example:** Consider the equation
$m^2 - km + 18 = 0$.
In the equation above, $k$ is a constant with $k > 0$. If the equation has two integer solutions, what is the smallest possible value of $k$?

**Explanation**

Let $p$ and $q$ be the integer roots of the equation. Then, the equation can be factored as

$$
(m-p)(m-q) = 0
$$

Expanding the parentheses, we have

$$
m^{2} - qm - pm + pq = 0
$$

which gives

$$
m^2-(p+q)m + pq = 0
$$

Since this is equivalent to

$$
m^2 - km + 18 = 0
$$

we have

$$
m^2-(p+q)m + pq = m^2 - km + 18
$$

We now compare the coefficients on both sides.

- Comparing the constant terms, we have
$pq = 18$.
Since the right-hand side is positive, $p$ and $q$ must have the same sign. And since $p$ and $q$ are both integers, they must be factors of $18$.
- Comparing the coefficients of $m$, we have
$p + q = k$
Now, since $k > 0$ and $p$ and $q$ have the same sign, this means that $p$ and $q$ are both positive.

Therefore, listing the positive factor pairs of $18$, we get
$(1,18), \qquad (2,9), \qquad (3,6)$.

We now evaluate all possible values of $k{:}$

- For the pair $(1,18)$, we have $k = 1 + 18 = 19$.
- For the pair $(2,9)$, we have $k = 2 + 9 = 11$.
- For the pair $(3,6)$, we have $k = 3 + 6 = 9$.

Therefore, the **smallest** positive value of $k$ is $9$.

---

**Question 5**

Consider the equation
$y^{2} - ry + 30 = 0$.
In the equation above, $r$ is a constant with $r > 0$. If the equation has two integer solutions, what is the smallest possible value of $r$?The smallest positive value of $r$ is
$\underline{\hspace{1.5cm}}$.

---

**Question 6**

Consider the equation
$m^{2} - km + 18 = 0$.
In the equation above, $k$ is a constant with $k > 0$. If the equation has two integer solutions, what is the smallest possible value of $k$?The smallest positive value of $k$ is
$\underline{\hspace{1.5cm}}$.

---

<a id="identifying-true-statements-about-quadratic-coefficients-with-additional-constraints"></a>
## Identifying True Statements About Quadratic Coefficients With Additional Constraints

**Example:** The expression $6x^{2} + bx - 35$, where $b$ is a constant, can be rewritten as $(px + q)(x + r)$, where $p, q$, and $r$ are integer constants. Which of the following must be an integer?

1. $\dfrac{35}{q}$
2. $\dfrac{35}{p}$
3. $\dfrac{b}{p}$
4. $\dfrac{b}{q}$

**Explanation**

First, we expand the factorization:

$$
\begin{aligned}
(px + q)(x + r) &= px(x + r) + q(x + r) \\
&= px^{2} + prx + qx + qr \\
&= px^{2} + (pr + q)x + qr
\end{aligned}
$$

Now, comparing this with the given quadratic, $6x^{2} + bx - 35$, we have the following:

- The leading coefficient gives $p = 6$.
- The middle coefficient gives $pr + q = b$.
- The constant term gives $qr = -35$.

Next, since $q$ and $r$ are integers and $qr = -35$, it follows that $q$ and $r$ must be factors of $-35$.
With that in mind, let's check the given options in turn.

- Option I is valid. Indeed, $\dfrac{35}{q}$ is an integer since $q$ must be a factor of $-35$.
- Option II is not valid. Notice that
$\dfrac{35}{p} = \dfrac{35}{6}$,
which is not an integer.
- Option III is not valid. Notice that
$\dfrac{b}{p} = \dfrac{pr + q}{p} = \dfrac{6r + q}{6} = r + \dfrac{q}{6}$
can be non-integer, for example, when $q = 1$.
- Option IV is not valid. Notice that
$\dfrac{b}{q} = \dfrac{pr + q}{q} = \dfrac{6r + q}{q} = \dfrac{6r}{q} + 1$
can be non-integer, for example, when $r = 1$.

Therefore, the correct answer is "I only."

---

**Question 7**

The expression $4x^{2} + bx - 45$, where $b$ is a constant, can be rewritten as $(hx + k)(x + j)$, where $h, k$, and $j$ are integer constants. Which of the following must be an integer?

1. $\frac{45}{k}$
2. $\frac{45}{h}$
3. $\frac{b}{h}$
4. $\frac{b}{k}$

- [ ] A. II only
- [ ] B. IV only
- [ ] C. None
- [ ] D. III only
- [ ] E. I only

---

**Question 8**

The expression $3x^{2} + bx - 20$, where $b$ is a constant, can be rewritten as $(x + r)(px + q)$, where $p, q$, and $r$ are integer constants. Which of the following must be an integer?

1. $\frac{20}{q}$
2. $\frac{20}{p}$
3. $\frac{b}{p}$
4. $\frac{b}{q}$

- [ ] A. IV only
- [ ] B. III only
- [ ] C. None
- [ ] D. II only
- [ ] E. I only

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Test-Preparation/SMP/Home|Home]]
[[MA/Test-Preparation/SMP/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
