# The Transpose of a Matrix

<!--
lesson-id: 232
topic-code: MF3.14.1.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Transpose of a Matrix](#finding-the-transpose-of-a-matrix)
- [Finding the Transpose of a Row Vector or Column Vector](#finding-the-transpose-of-a-row-vector-or-column-vector)
- [Determining the Dimensions of the Transpose of a Matrix](#determining-the-dimensions-of-the-transpose-of-a-matrix)
- [Properties of the Transpose of a Matrix](#properties-of-the-transpose-of-a-matrix)

## Prerequisites

- [Scalar Multiplication of Matrices](<14.1.5. Scalar Multiplication of Matrices.md>)

---

<a id="introduction"></a>
## Introduction

To take the **transpose of a matrix**, we interchange each of its columns with the corresponding row. That is, row $1$ becomes column $1$, row $2$ becomes column $2$, and so on. A superscript ${}^{T}$ is used to denote the transpose operation.

For example, the transpose of the $2 \times 3$ matrix ${X}$ is the following $3 \times 2$ matrix $X^T$:

$$
{X} = \left\lbrack \matrix {{\color{red}7} & {\color{red}2} & {\color{red}-4} \ {\color{blue}-3} & {\color{blue}1} & {\color{blue}0}} \right\rbrack \qquad\implies\qquad {X}^T = \left\lbrack \matrix {{\color{red}7} & {\color{blue}-3} \ {\color{red}2} & {\color{blue}1} \ {\color{red}-4} & {\color{blue}0}} \right\rbrack
$$

The first row of $X$ becomes the first column of $X^T$, and the second row of $X$ becomes the second column of $X^T$.

Note that transposing a matrix swaps its dimensions. In general, we have the following property:

> *When a matrix is transposed, its dimensions switch from $m \times n$ to $n \times m$.*

Also, notice that the transpose of a column vector is a row vector:

$$
{A} = \left\lbrack \matrix {5 \ 4 \ 2} \right\rbrack \qquad \implies\qquad {A}^T = \left\lbrack \matrix {5 & 4 & 2} \right\rbrack
$$

Similarly, the transpose of a row vector is a column vector:

$$
{B} = \left\lbrack \matrix {8 & -10 & 0 & 6} \right\rbrack \qquad\implies\qquad {B}^T = \left\lbrack \matrix {8 \ -10 \ 0 \ 6} \right\rbrack
$$

---

<a id="finding-the-transpose-of-a-matrix"></a>
## Finding the Transpose of a Matrix

**Example:** Find $C^T$, if
$\begin{bmatrix}-1 & 1 \\ 0 & 2 \\ -2 & -3\end{bmatrix}$

**Explanation**

To find the transpose, we interchange the rows and columns:
$\begin{bmatrix}-1 & 0 & -2 \\ 1 & 2 & -3\end{bmatrix}$

---

**Question 1:**

```quiz
type: radio
id: q-1
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If
  
  $Y = \begin{bmatrix}-2 & 9 & 7 \\ 1 & 6 & 0 \\ -8 & 2 & 1\end{bmatrix}$,
  then $Y^{T} =$
options:
- id: a
  content: |-
    $\begin{bmatrix}-2 & -1 & 8 \\ 9 & 6 & 2 \\ 7 & 0 & 1\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}-2 & 1 & -8 \\ 7 & 0 & 1 \\ 9 & 6 & 2\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}-8 & 2 & 1 \\ 1 & 6 & 0 \\ -2 & 9 & 7\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}-2 & 1 & -8 \\ 9 & 6 & 2 \\ 7 & 0 & 1\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}2 & -1 & 8 \\ 9 & 6 & 2 \\ 7 & 0 & 1\end{bmatrix}$
```

---

**Question 2:**

```quiz
type: radio
id: q-2
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find $A^{T}$, given that
  
  $A = \begin{bmatrix}5 & 4 \\ 3 & 2\end{bmatrix}$.
options:
- id: a
  content: |-
    $\begin{bmatrix}5 & 3 \\ 4 & 2\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}-5 & -3 \\ -4 & -2\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}0 & 4 \\ 3 & 0\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}2 & 4 \\ 3 & 5\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}5 & 3 & 4 & 2\end{bmatrix}$
```

---

<a id="finding-the-transpose-of-a-row-vector-or-column-vector"></a>
## Finding the Transpose of a Row Vector or Column Vector

**Example:** If
${A} = \left\lbrack \matrix {5 & -1 & 0 \} \right\rbrack$,
then what is ${A^T}$?

**Explanation**

To find the transpose, we interchange the rows and columns.

Here, there is just a single row, so it becomes a single column:

$$
{A}^T = \left\lbrack \matrix {5 \ -1 \ 0 \} \right\rbrack
$$

---

**Question 3:**

```quiz
type: radio
id: q-3
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If
  
  $G = \begin{bmatrix}-4 \\ 15 \\ -1 \\ 8\end{bmatrix}$,
  then $G^{T} =$
options:
- id: a
  content: |-
    $\begin{bmatrix}-4 & 15 \\ -1 & 8\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}8 & -1 & 15 & -4\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}8 \\ -1 \\ 15 \\ -4\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}-4 & 15 & -1 & 8\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}4 & -15 & 1 & -8\end{bmatrix}$
```

---

**Question 4:**

```quiz
type: radio
id: q-4
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find $Z^{T}$ given that
  
  $Z = \begin{bmatrix}-4 & 4 & 5 & 1\end{bmatrix}$.
options:
- id: a
  content: |-
    $\begin{bmatrix}4 \\ -4 \\ -5 \\ -1\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}-4 \\ 4 \\ 5 \\ 1\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}-1 & -5 & 4 & -4\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 \\ 5 \\ 4 \\ -4\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}1 & 5 & 4 & -4\end{bmatrix}$
```

---

<a id="determining-the-dimensions-of-the-transpose-of-a-matrix"></a>
## Determining the Dimensions of the Transpose of a Matrix

**Example:** Consider the matrix
${B} = \left\lbrack \matrix {1 & 2 & 4 & 2\ 4 & 4 & 1 & 1\ 3 & -3 & 0 & -1} \right\rbrack$.
What are the dimensions of ${B}^T$?

**Explanation**

When a matrix is transposed, its dimensions switch from $N \times M$ to $M \times N$.

So, since $B$ is a $3 \times 4$ matrix, the transpose ${B}^T$ is a $4\times3$ matrix.

---

**Question 5:**

```quiz
type: radio
id: q-5
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the matrix
  
  $C = \begin{bmatrix}5 & 3 \\ 4 & 3 \\ 3 & -2 \\ 4 & 1\end{bmatrix}$.
  What are the dimensions of $C^{T}$?
options:
- id: a
  content: |-
    $2 \cdot 1$
- id: b
  content: |-
    $3 \cdot 3$
- id: c
  content: |-
    $1 \cdot 4$
- id: d
  content: |-
    $4 \cdot 2$
- id: e
  content: |-
    $2 \cdot 4$
```

---

**Question 6:**

```quiz
type: radio
id: q-6
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the matrix
  
  $B = \begin{bmatrix}2 & 3 \\ 2 & 3 \\ 3 & 2\end{bmatrix}$.
  What are the dimensions of $B^{T}$?
options:
- id: a
  content: |-
    $2 \cdot 1$
- id: b
  content: |-
    $2 \cdot 3$
- id: c
  content: |-
    $3 \cdot 2$
- id: d
  content: |-
    $1 \cdot 3$
- id: e
  content: |-
    $3 \cdot 3$
```

---

<a id="properties-of-the-transpose-of-a-matrix"></a>
## Properties of the Transpose of a Matrix

Remember that transposing a matrix swaps the rows and columns. So, if $B=A^T$, then

$$
b_{ij}=a_{ji}
$$

for all values of $i$ and $j$.

Geometrically, when a matrix is transposed, the diagonal elements (those with $i=j$) remain the same while all other elements are reflected across the diagonal. For example,

$$
{A} = \left\lbrack \matrix {\color{red}{3} & \color{blue}{7} \ 1 & \color{red}{6}} \right\rbrack \qquad \implies\qquad {A}^T = \left\lbrack \matrix {\color{red}{3} & 1 \ \color{blue}{7} & \color{red}{6}} \right\rbrack
$$

Also, transposing a matrix twice results in the original matrix. More precisely, for any matrix $A$, we have

$$
\left(A^T\right)^T = A
$$

This happens because, in the first transpose, we swap the rows of $A$ for the columns, and then in the second transpose, we swap them back again. For example,

$$
{A} = \left\lbrack \matrix {\color{red}{3} & \color{blue}{7} \ 1 & \color{red}{6}} \right\rbrack \quad \implies\quad {A}^T = \left\lbrack \matrix {\color{red}{3} & 1 \ \color{blue}{7} & \color{red}{6}} \right\rbrack \quad \implies\quad \left({A}^T\right)^T = \left\lbrack \matrix {\color{red}{3} & \color{blue}{7} \ 1 & \color{red}{6}} \right\rbrack =A
$$

Finally, for any numbers $c_1,c_2$ and any $m \times n$ matrices $A,B$ we have the following rule:

$$
(c_1 A \pm c_2 B)^T = c_1 A^T \pm c_2 B^T
$$

```update-progress
```

[[MA/Mathematical-Foundations/MF3/Home|Home]]
[[MA/Mathematical-Foundations/MF3/0. Table of Contents/TOC|Table of Contents]]
