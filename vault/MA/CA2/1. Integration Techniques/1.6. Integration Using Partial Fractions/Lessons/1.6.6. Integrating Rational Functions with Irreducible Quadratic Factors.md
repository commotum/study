# Integrating Rational Functions with Irreducible Quadratic Factors

<!--
lesson-id: 447
topic-code: CA2.1.6.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Integrating a Rational Function Given Part Of the Partial Fraction Decomposition](#integrating-a-rational-function-given-part-of-the-partial-fraction-decomposition)
- [Integrating a Rational Function With an Irreducible Binomial Quadratic Factor](#integrating-a-rational-function-with-an-irreducible-binomial-quadratic-factor)
- [Integrating a Rational Function With an Irreducible Trinomial Quadratic Factor](#integrating-a-rational-function-with-an-irreducible-trinomial-quadratic-factor)

## Prerequisites

- [Integration by Substitution With Inverse Trigonometric Functions](<../../1.1. Integration Using Substitution/Lessons/1.1.10. Integration by Substitution With Inverse Trigonometric Functions.md>)
- [Integrating Rational Functions Using Partial Fractions](<1.6.4. Integrating Rational Functions Using Partial Fractions.md>)
- [Expressing Rational Functions with Irreducible Quadratic Factors as Sums of Partial Fractions](<1.6.3. Expressing Rational Functions with Irreducible Quadratic Factors as Sums of Partial Fractions.md>)

---

<a id="introduction"></a>
## Introduction

To perform partial fraction decomposition, we always need to factor the denominator as much as possible before using the factors to write the partial fractions.

Sometimes, however, we get **irreducible quadratic factors** that cannot be written as a product of linear factors with real coefficients. For example, the factor $x^2+4$ is irreducible.

Remember that in the partial fraction corresponding to an irreducible quadratic factor, the numerator is a linear expression. For example,

$$
\begin{aligned}
(2x + 3)/((x^{2} + 4)(x - 1)) &= (Ax + B)/((x^{2} + 4)) + (C)/((x - 1))
\end{aligned}
$$

In general:

- Any expression of the form $x^2+c$, where $c>0$, is irreducible.
- Any expression of the form $ax^2 + bx + c$ is irreducible if the discriminant $\mathcal{D}$ is negative, i.e., if $\mathcal{D} = b^2 - 4ac < 0$.
- Any expression of the form $ax^2 + bx + c$ is irreducible (over the rational numbers) if the discriminant $\mathcal{D} > 0$ is not a perfect square. For example, if $\mathcal D = 5$, then the quadratic term does not factor over the rational numbers because $\sqrt{\mathcal{D}} = \sqrt 5$ is not a rational number.

---

<a id="integrating-a-rational-function-given-part-of-the-partial-fraction-decomposition"></a>
## Integrating a Rational Function Given Part Of the Partial Fraction Decomposition

**Example:** Given that
$\dfrac{4x^2-6}{(x-3)(x^2+1)} = \dfrac{A}{x-3} + \dfrac{x+3}{x^2 + 1}$,
where $A$ is a constant to be found, calculate $\displaystyle{\int \dfrac{4x^2-6}{(x-3)(x^2+1)}\,\textrm d x}$.

**Explanation**

First, we find the constant $A$. Multiplying both sides of the given equality by $(x-3)(x^2 + 1)$ gives

$$
4x^{2} - 6 = A(x^{2} + 1) + x^{2} - 9
$$

Substituting $x=0$ into the above gives

$$
\begin{aligned}
4(0)^{2} - 6 &= A(0^{2} + 1) + 0^{2} - 9 \\
- 6 &= A - 9 \\
A &= 3
\end{aligned}
$$

We now integrate this function term by term, adding the constant of integration at the very end.

$$
\begin{aligned} \int \dfrac{4x^2 - 6}{(x-3)(x^2+1)} \, \textrm{d}x & = \int \left(\dfrac{3}{x-3} + \dfrac{x+3}{x^2 + 1}\right) \textrm{d}x \\[5pt] & = 3\int\dfrac{1}{x-3}\,\textrm{d}x + \int \dfrac{x}{x^2+1}\,\textrm d x + 3\int \dfrac{1}{x^2 + 1} \, \textrm{d}x \end{aligned}
$$

For the first integral, we get

$$
\int \dfrac{1}{x-3} \textrm{d}x = \ln \mid x-3 \mid
$$

For the second integral, we use the substitution

$$
x^2 + 1 = u
$$

Then,

$$
2x\,\textrm{d}x=\textrm{d}u
$$

and we get

$$
\begin{aligned} \int \dfrac{x}{x^2+1}\,\textrm{d}x & = \int \left(\dfrac{1}{u}\right) \dfrac{\textrm d u}{2} \\[5pt] &= \dfrac{1}{2}\ln \vert u\vert \\[5pt] &= \dfrac{1}{2}\ln \vert x^2 + 1\vert \\[5pt] & = \dfrac{1}{2}\ln \left(x^2+1\right). \end{aligned}
$$

Finally, for the third integral, we use the fact that

$$
\displaystyle \int \dfrac{1}{x^2 + a^2} \, \textrm{d}x = \dfrac{1}{a}\arctan\left(\dfrac{x}{a}\right)
$$

which results in

$$
\int \dfrac{1}{x^2+1}\,\textrm{d}x = \arctan{x}
$$

So, we get the following result:

$$
\begin{aligned}
∫(4x^{2} - 6)/((x - 3)(x^{2} + 1))dx &= 3∫\frac{1}{x - 3}dx + ∫\frac{x}{x^{2} + 1}dx + 3∫\frac{1}{x^{2} + 1}dx \\
&= 3\ln \mid x - 3 \mid + \frac{1}{2}\ln (x^{2} + 1) + 3\arctan x + C
\end{aligned}
$$

---

**Question 1**

> A calculator is required to answer this question.

Given that
$(4x^{2} - 3x + 5)/((x - 1)(x^{2} + 2)) = \frac{Ax - 1}{x^{2} + 2} + \frac{2}{x - 1}$,
where $A$ is a constant to be found, calculate $∫(4x^{2} - 3x + 5)/((x - 1)(x^{2} + 2))dx$.

- [ ] A. $2\ln (x^{2} + 2) - \frac{\sqrt{2}}{2}\arctan (\sqrt{2}x) + 2\ln \mid x - 1 \mid + C$
- [ ] B. $\ln (x^{2} + 2) - \frac{\sqrt{2}}{2}\arctan (\frac{\sqrt{2}x}{2}) + 2\ln \mid x - 1 \mid + C$
- [ ] C. $\ln (x^{2} + 2) - \sqrt{2}\arctan (\sqrt{2}x) + \ln \mid x - 1 \mid + C$
- [ ] D. $\ln (x^{2} + 2) - \arctan (\frac{\sqrt{2}x}{2}) + \ln \mid x - 1 \mid + C$
- [ ] E. $2\ln (x^{2} + 2) - \frac{1}{2}\arctan (\frac{x}{2}) + 2\ln \mid x - 1 \mid + C$

---

**Question 2**

> A calculator is required to answer this question.

Given that
$(5x^{2} + x + 3)/(x(x^{2} + 1)) = \frac{2x + B}{x^{2} + 1} + \frac{3}{x}$,
where $B$ is a constant to be found, calculate $∫(5x^{2} + x + 3)/(x(x^{2} + 1))dx$.

- [ ] A. $2\ln (x^{2} + 1) + 3\ln \mid x \mid + C$
- [ ] B. $2\ln (x^{2} + 1) + \arctan x + 3\ln \mid x \mid + C$
- [ ] C. $\ln (x^{2} + 1) + 4\ln \mid x \mid + C$
- [ ] D. $\ln (x^{2} + 1) + \arctan x + 3\ln \mid x \mid + C$
- [ ] E. $\ln (x^{2} + 1) + 3\ln \mid x \mid + C$

---

<a id="integrating-a-rational-function-with-an-irreducible-binomial-quadratic-factor"></a>
## Integrating a Rational Function With an Irreducible Binomial Quadratic Factor

**Example:** Calculate $\displaystyle \int \dfrac{2x+3}{(x^2+4)(x-1)} \, \textrm{d}x$.

**Explanation**

The quadratic factor $x^2+4$ is irreducible, so we proceed with the following partial fraction decomposition:

$$
\begin{aligned}
(2x + 3)/((x^{2} + 4)(x - 1)) &= (Ax + B)/((x^{2} + 4)) + (C)/((x - 1))
\end{aligned}
$$

To determine the coefficients $A$, $B$, and $C$, we first multiply both sides of the above equation by $(x^2+4)(x-1)$, and we get

$$
\begin{aligned}
2x + 3 &= (Ax + B)(x - 1) + C(x^{2} + 4)
\end{aligned}
$$

Solving for the unknown constants using the usual methods, we get $A=-1, B=1$, and $C=1$. Therefore,

$$
\dfrac{2x+3}{(x^2+4)(x-1)} = \dfrac{-x+1}{x^2+4} +\dfrac{1}{x-1}
$$

Now we integrate this function term by term, adding the constant of integration at the very end.

$$
\begin{aligned}
∫(2x + 3)/((x^{2} + 4)(x - 1))dx &= ∫(\frac{-x + 1}{x^{2} + 4} + \frac{1}{x - 1})dx \\
&= - ∫\frac{x}{x^{2} + 4}dx + ∫\frac{1}{x^{2} + 4}dx + ∫\frac{1}{x - 1}dx
\end{aligned}
$$

For the first integral, we use the substitution

$$
u = x^2+4
$$

Then

$$
2x\,\textrm{d}x=\textrm{d}u
$$

and we get

$$
\begin{aligned}
∫\frac{x}{x^{2} + 4}dx &= ∫(\frac{1}{u})\frac{du}{2} \\
&= \frac{1}{2}\ln \mid u \mid \\
&= \frac{1}{2}\ln \mid x^{2} + 4 \mid \\
&= \frac{1}{2}\ln (x^{2} + 4)
\end{aligned}
$$

For the second integral, we use the fact that

$$
\displaystyle \int \dfrac{1}{x^2+a^2} \, \textrm{d}x = \dfrac{1}{a}\arctan\left(\dfrac{x}{a}\right)
$$

which results in

$$
\begin{aligned}
∫\frac{1}{x^{2} + 4}dx &= \frac{1}{2}\arctan (\frac{x}{2})
\end{aligned}
$$

Finally, for the third integral we have

$$
\begin{aligned}
∫\frac{1}{x - 1}dx &= \ln \mid x - 1 \mid
\end{aligned}
$$

So we get the following result:

$$
\begin{aligned}
∫(2x + 3)/((x^{2} + 4)(x - 1))dx &= - ∫\frac{x}{x^{2} + 4}dx + ∫\frac{1}{x^{2} + 4}dx + ∫\frac{1}{x - 1}dx \\
&=-\frac{1}{2}\ln (x^{2} + 4) + \frac{1}{2}\arctan (\frac{x}{2}) + \ln \mid x - 1 \mid + K
\end{aligned}
$$

---

**Question 3**

> A calculator is required to answer this question.

Evaluate $∫(x^{2} + 5x + 13)/((x^{2} + 5)(x + 3))dx$.

- [ ] A. $\frac{1}{4}\ln (x^{2} + 5) + \frac{7}{2\sqrt{5}}\arctan (\frac{x}{\sqrt{5}}) + \frac{1}{2}\ln \mid x + 3 \mid + K$
- [ ] B. $\frac{1}{2}\ln (x^{2} + 5) + \frac{1}{3}\arctan (\frac{x}{\sqrt{5}}) + \frac{1}{4}\ln \mid x + 3 \mid + K$
- [ ] C. $-5\ln (x^{2} + 5) + 13\arctan (\frac{x}{\sqrt{7}}) - \ln \mid x + 3 \mid + K$
- [ ] D. $4\ln (x^{2} + 5) + \frac{4}{5}\arctan (\frac{x}{\sqrt{4}}) + 2\ln \mid x + 3 \mid + K$
- [ ] E. $\frac{1}{4}\ln (x^{2} + 5) + \frac{7}{10}\arctan (\frac{x}{\sqrt{5}}) + \frac{1}{2}\ln \mid x + 5 \mid + K$

---

**Question 4**

> A calculator is required to answer this question.

Evaluate $∫(2)/((x - 1)(x^{2} + 1))dx$.

- [ ] A. $-2\ln \mid x - 1 \mid - \frac{1}{4}\ln (x^{2} + 1) + \arctan x + K$
- [ ] B. $-\ln \mid x - 1 \mid + 2\ln (x^{2} + 1) - \arctan x + K$
- [ ] C. $2\ln \mid x - 1 \mid + \ln (x^{2} + 1) + \arctan x + K$
- [ ] D. $\ln \mid x - 1 \mid - \frac{1}{2}\ln (x^{2} + 1) - \arctan x + K$
- [ ] E. $\ln \mid x - 1 \mid - \ln (x^{2} + 1) + K$

---

<a id="integrating-a-rational-function-with-an-irreducible-trinomial-quadratic-factor"></a>
## Integrating a Rational Function With an Irreducible Trinomial Quadratic Factor

**Example:** Evaluate $\displaystyle \int \dfrac{4x^2-x}{(x^2+x+1)(x-2)} \, \textrm{d}x$.

**Explanation**

Notice that the quadratic factor in the denominator has a negative discriminant:

$$
\mathcal{D} = 1^2 - 4(1)(1) = -3 < 0
$$

So, we proceed with the following partial fraction decomposition:

$$
\begin{aligned}
(4x^{2} - x)/((x^{2} + x + 1)(x - 2)) &= \frac{Ax + B}{x^{2} + x + 1} + \frac{C}{x - 2}
\end{aligned}
$$

To determine the coefficients $A$, $B$, and $C$, we first multiply both sides of the above equation by $(x^2+x+1)(x-2)$ and get

$$
\begin{aligned}
4x^{2} - x &= (Ax + B)(x - 2) + C(x^{2} + x + 1)
\end{aligned}
$$

Solving for the unknown constants using the usual methods, we get $A=2$, $B=1$, and $C=2$. Therefore,

$$
\dfrac{4x^2-x}{(x^2+x+1)(x-2)} = \dfrac{2x+1}{x^2+x+1} + \dfrac{2}{x-2}
$$

Now we integrate this function term by term, adding the constant of integration only at the very end.

$$
\begin{aligned}
∫(4x^{2} - x)/((x^{2} + x + 1)(x - 2))dx &= ∫(\frac{2x + 1}{x^{2} + x + 1} + \frac{2}{x - 2})dx \\
&= ∫\frac{2x + 1}{x^{2} + x + 1}dx + 2∫\frac{1}{x - 2}dx
\end{aligned}
$$

For the first integral, we use the substitution

$$
u = x^2+x+1
$$

Then

$$
(2x+1) \, \textrm{d}x=\textrm{d}u
$$

and we get

$$
\begin{aligned}
∫\frac{2x + 1}{x^{2} + x + 1}dx &= ∫(\frac{1}{u})du \\
&= \ln \mid u \mid \\
&= \ln \mid x^{2} + x + 1 \mid \\
&= \ln (x^{2} + x + 1)
\end{aligned}
$$

For the second integral we have

$$
\begin{aligned}
∫\frac{1}{x - 2}dx &= \ln \mid x - 2 \mid
\end{aligned}
$$

So we get the following result:

$$
\begin{aligned}
∫(4x^{2} - x)/((x^{2} + x + 1)(x - 2))dx &= ∫\frac{2x + 1}{x^{2} + x + 1}dx + 2∫\frac{1}{x - 2}dx \\
&= \ln (x^{2} + x + 1) + 2\ln \mid x - 2 \mid + K
\end{aligned}
$$

---

**Question 5**

> A calculator is required to answer this question.

$∫(-x^{2} - 10x + 4)/((x + 5)(x^{2} + x + 9))dx =$

- [ ] A. $\frac{1}{x + 5} + \ln (x^{2} + x + 9) + K$
- [ ] B. $\ln \mid x + 5 \mid - \ln (x^{2} + x + 9) + K$
- [ ] C. $\ln \mid x + 5 \mid + \frac{1}{x^{2} + x + 9} + K$
- [ ] D. $-\ln \mid x \mid + \ln (x^{2} + x + 9) + K$
- [ ] E. $\frac{1}{x + 5} + \frac{1}{x^{2} + x + 9} + K$

---

**Question 6**

> A calculator is required to answer this question.

Evaluate $∫(15 - 25x)/((2x^{2} - 6x + 9)(x - 4))dx$.

- [ ] A. $5\ln \mid x - 4 \mid - \frac{2}{5}\ln (2x^{2} - 6x + 9) + K$
- [ ] B. $5\ln (2x^{2} - 6x + 9) - 5\ln \mid x - 4 \mid + K$
- [ ] C. $\frac{5}{2}\ln \mid x - 4 \mid + \frac{2}{5}\ln (2x^{2} - 6x + 9) + K$
- [ ] D. $25\ln (2x^{2} - 6x + 9) - 10\ln \mid x - 4 \mid + K$
- [ ] E. $\frac{5}{2}\ln (2x^{2} - 6x + 9) - 5\ln \mid x - 4 \mid + K$

```update-progress
```

[[MA/CA2/Home|Home]]
[[MA/CA2/0. Table of Contents/TOC|Table of Contents]]
