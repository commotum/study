# Breadth-First Search

<!--
lesson-id: 2893
topic-code: DSM.7.6.1
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Applying Steps of the Breadth-First Search Algorithm](#applying-steps-of-the-breadth-first-search-algorithm)
- [Applying the Breadth-First Search Algorithm](#applying-the-breadth-first-search-algorithm)

## Prerequisites

- [Connected Graphs](<../../7.2. Connectivity in Graphs/Lessons/7.2.2. Connected Graphs.md>)

---

<a id="introduction"></a>
## Introduction

**Breadth-first search (BFS)** is a graph traversal algorithm that explores vertices level-by-level from a starting vertex. Edward F. Moore first formally described it in 1959. Since then, it has become helpful in network analysis, identifying the shortest paths in unweighted graphs, and identifying the connected components of a graph.

BFS uses a **queue,** which follows a first-in, first-out (FIFO) structure, ensuring that the earliest inserted vertex is processed first. This allows BFS to explore vertices in a structured manner, expanding outward layer by layer. The process begins by selecting a starting vertex, and BFS is guaranteed to visit every vertex within the same connected component as that vertex in an undirected graph.

The steps involved in a BFS traversal are as follows:

- **Step 1:** Initialize the empty queue and the empty list of visited vertices.
- **Step 2:** Add the starting vertex to the queue and to the list of visited vertices.
- **Step 3:** Pop the first vertex from the head of the queue.
- **Step 4:** For every unvisited neighbor of the dequeued vertex, process it and add to the list of visited vertices.
- **Step 5:** Repeat steps 3 and 4 until the queue is empty.

We can express the BFS algorithm using pseudocode. Pseudocode is a simplified, programming language-independent way of describing an algorithm. It uses a mix of natural language and programming concepts, helping programmers to plan and visualize code logic without focusing on syntax.

The pseudocode for the breadth-first search algorithm is the following:

> `Initialize the empty queue and the list of visited vertices`
> `def BFS(v): # Breadth-First Search starting from v`
> `Add the starting vertex v to queue and to visited`
> `while (queue is not empty): # Continue until the queue is empty`
> `Pop vertex w from the head of the queue`
> `for (u adjacent to w): # Go over all neighbors of w`
> `if (u not in visited):`
> `Add u to visited`
> `Add u to the end of the queue`

Before we continue, please note the following:

- In general, a BFS traversal is not unique, even for the same starting vertex, because multiple vertices can be added to the queue at any given step.
- To avoid ambiguity, we will *fix* the order of the vertices before proceeding with the algorithm. Given a choice of two or more vertices to add to the queue, we will always add the vertex with the *smallest* index first.
- Please note that, in practical applications, such as writing a BFS program on a computer, fixing the order of the vertices is unnecessary. If given a choice, we can select a vertex at random.

Let's look at how to apply BFS to a concrete graph.

---

<a id="a-worked-example"></a>
## A Worked Example

Let's apply the breadth-first search algorithm to the graph below, starting at $v_3$.

![](<../Source/7.6.1. Breadth-First Search/Images/7c2411ea43a9e46b673b73e4c5beee97.png>)

First, let's fix the following order of vertices.
$v_1, \: v_2,\: v_3,\: v_4, \: v_5, \: v_6$

Now, we proceed as follows:

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.1. Breadth-First Search/Images/b43e13032c07f1e6f5d692ef2bb53dbf.png>) | Push $v_3$ to the end of the queue and add it to visited vertices.<br>Queue: $v_3$ Visited: $v_3$ |
| **Step 2.** |  |
| ![](<../Source/7.6.1. Breadth-First Search/Images/438c5aec7feb63ba2f11811b9cc7a2ca.png>) | Pop $v_3$ from the head of the queue, visit its unvisited neighbors $v_2$ and $v_5$ and push them to the end of the queue.<br><br><br><br>**Watch out!** Notice that the vertex $v_2$ goes first since it has a smaller index.<br>Queue: $v_2$, $v_5$ Visited: $v_3$, $v_2$, $v_5$ |
| **Step 3.** |  |
| ![](<../Source/7.6.1. Breadth-First Search/Images/1044e95fa058451897716498c3ad7ed4.png>) | Pop $v_2$ from the head of the queue, visit its unvisited neighbors $v_1$ and $v_4$ and push them to the end of the queue.<br><br><br><br>**Watch out!** Notice that the vertex $v_1$ goes first since it has a smaller index.<br>Queue: $v_5$, $v_1$, $v_4$ Visited: $v_3$, $v_2$, $v_5$, $v_1$, $v_4$ |
| **Steps 4-6.** |  |
| ... | Since all the vertices have been visited, in the next steps, we pop the remaining vertices from the head of the queue in the following order:<br>$v_5$, $v_1$, $v_4$<br>Queue:Visited: $v_3$, $v_2$, $v_5$, $v_1$, $v_4$ |

And we're done. The BFS algorithm is complete, and the list of visited vertices, in the order they're visited, are as follows:

$$
v_3,\quad v_2,\quad v_5,\quad v_1,\quad v_4
$$

---

<a id="applying-steps-of-the-breadth-first-search-algorithm"></a>
## Applying Steps of the Breadth-First Search Algorithm

**Example:** Suppose we fix the following order of the vertices in the above graph: $v_1$, $v_2$, $v_3$, $v_4$, $v_5$, $v_6$.
The breadth-first search algorithm is applied to the graph, starting at $v_1$. After pushing $v_1$ to the end of the queue and adding it to visited vertices, we have the following configuration:

![](<../Source/7.6.1. Breadth-First Search/Images/59566cf5c9762afaf9e81b6b4a0b4bd9.png>)

| Queue: | $v_1$ |
| --- | ---: |
| Visited: | $v_1$ |

What are the vertices visited by the algorithm on the next iteration?

**Explanation**

Breadth-first search (BFS) is an algorithm that systematically explores a graph by starting at a source node and discovering all its directly connected neighbors. Then, it lists and visits these neighbors, layer by layer, until all reachable nodes have been visited.

The pseudocode for the breadth-first search algorithm is the following:

> `Initialize the empty queue and the list of visited vertices`
> `def BFS(v): # Breadth-First Search starting from v`
> `Add the starting vertex v to the queue and to visited`
> `while (queue is not empty): # Until the queue is not empty`
> `Pop vertex w from the head of the queue`
> `for (u adjacent to w): # Go over all neighbors of w`
> `if (u not in visited):`
> `Add u to visited`
> `Add u to the end of the queue`

So, at the next step of the breadth-first search algorithm, we pop (i.e., remove) the vertex from the head (i.e., the front) of the queue. This would be the vertex $v_1$.

Next, we traverse all the vertices that are adjacent to $v_1$. So, we add $v_{2}$ to the list of visited vertices and push them into the end of the queue.

| ![](<../Source/7.6.1. Breadth-First Search/Images/8ce852fbc421a9b9c7e16768744b848a.png>) | Queue: $v_2$, $v_3$ Visited: $v_1$, $v_2$, $v_3$ |
| --- | ---: |

---

**Question 1**

![](<../Source/7.6.1. Breadth-First Search/Images/q-249736.png>)

Suppose we fix the following order of the vertices in the above graph: $v_{1}$, $v_{2}$, $v_{3}$, $v_{4}$, $v_{5}, v_{6}, v_{7}, v_{8}$.
The breadth-first search algorithm is applied to the graph, starting at $v_{5}$. After considering all the vertices that are connected to $v_{5}$, we have the following configuration, where the queue and the array of visited vertices are ordered from left to right:

| Queue: | $v_{3}$, $v_{4}$ |
| --- | ---: |
| Visited: | $v_{5}$, $v_{3}$, $v_{4}$ |

The vertices visited by the algorithm on the next iteration are
$\underline{\hspace{2cm}}$.

#### Select

- [ ] [MATH: v_{7}] only
- [ ] [MATH: v_{1}, v_{2},] and [MATH: v_{7}]
- [ ] [MATH: v_{2}] and [MATH: v_{6}]
- [ ] [MATH: v_{1}] only
- [ ] [MATH: v_{2}] and [MATH: v_{7}]
- [ ] [MATH: v_{6}] only
- [ ] [MATH: v_{2}] only

---

**Question 2**

![](<../Source/7.6.1. Breadth-First Search/Images/q-259490.png>)

Suppose we fix the following order of the vertices in the above graph: $v_{1}$, $v_{2}$, $v_{3}$, $v_{4}$, $v_{5}, v_{6}$.
The breadth-first search algorithm is applied to the graph, starting at $v_{5}$. After considering all the vertices that are connected to $v_{5}$ and $v_{3}$, we have the following configuration, where the queue and the array of visited vertices are ordered from left to right:

| Queue: | $v_{4}$, $v_{2}$, $v_{6}$ |
| --- | ---: |
| Visited: | $v_{5}$, $v_{3}$, $v_{4}$, $v_{2}$, $v_{6}$ |

The vertices visited by the algorithm on the next iteration are
$\underline{\hspace{2cm}}$.

#### Select

- [ ] [MATH: v_{1}] and [MATH: v_{2}]
- [ ] [MATH: v_{1}] only
- [ ] [MATH: v_{5}] only
- [ ] [MATH: v_{1}, v_{3},] and [MATH: v_{5}]
- [ ] [MATH: v_{2}] only
- [ ] [MATH: v_{3}] only

---

<a id="applying-the-breadth-first-search-algorithm"></a>
## Applying the Breadth-First Search Algorithm

**Example:** Suppose we fix the following order of vertices in the above graph: $v_1$, $v_2$, $v_3$, $v_4$, $v_5$, $v_6$. Apply the breadth-first search algorithm to this graph, starting at $v_5$.

![](<../Source/7.6.1. Breadth-First Search/Images/2ec22108e242aeb5ae0a234e89e3e1a6.png>)

**Explanation**

Breadth-first search (BFS) is an algorithm that systematically explores a graph by starting at a source node and discovering all its directly connected neighbors. Then, it lists and visits these neighbors, layer by layer, until all reachable nodes have been visited.

The pseudocode for the breadth-first search algorithm is the following:

> `Initialize the empty queue and the list of visited vertices`
> `def BFS(v): # Breadth-First Search starting from v`
> `Add the starting vertex v to queue and to visited`
> `while (queue is not empty): # Continue until the queue is empty`
> `Pop vertex w from the head of the queue`
> `for (u adjacent to w): # Go over all neighbors of w`
> `if (u not in visited):`
> `Add u to visited`
> `Add u to the end of the queue`

With that in mind, we proceed as follows:

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.1. Breadth-First Search/Images/5e0dac7104ca0437da838391106efc0b.png>) | Push $v_5$ to the end of the queue and add it to visited vertices.<br>Queue: $v_5$ Visited: $v_5$ |
| **Step 2.** |  |
| ![](<../Source/7.6.1. Breadth-First Search/Images/5e14754ed9965f4f22e8c80c2d8cdb47.png>) | Pop $v_5$ from the head of the queue, visit its unvisited neighbors $v_3$ and $v_4$ and push them to the end of the queue.<br>Queue: $v_3$, $v_4$ Visited: $v_5$, $v_3$, $v_4$ |
| **Step 3.** |  |
| ![](<../Source/7.6.1. Breadth-First Search/Images/458da566e0232509215497a79814e28f.png>) | Pop $v_3$ from the head of the queue, visit its unvisited neighbors $v_2$ and $v_6$ and push them to the end of the queue.<br>Queue: $v_4$, $v_2$, $v_6$ Visited: $v_5$, $v_3$, $v_4$, $v_2$, $v_6$ |
| **Step 4.** |  |
| ![](<../Source/7.6.1. Breadth-First Search/Images/62f4d4b1f4e1823e7e31f706d9761d82.png>) | Pop $v_4$ from the head of the queue, visit its unvisited neighbor $v_1$ and push it to the end of the queue.<br>Queue: $v_2$, $v_6$, $v_1$ Visited: $v_5$, $v_3$, $v_4$, $v_2$, $v_6$, $v_1$ |
| **Steps 5-7.** |  |
| ... | Since all the vertices have been visited, in the next steps, we pop the remaining vertices from the head of the queue in the following order:<br>$v_2$, $v_6$, $v_1$<br>Queue:Visited: $v_5$, $v_3$, $v_4$, $v_2$, $v_6$, $v_1$ |

---

**Question 3**

![](<../Source/7.6.1. Breadth-First Search/Images/q-259341.png>)

Suppose we fix the following order of vertices in the above graph: $v_{1}$, $v_{2}$, $v_{3}$, $v_{4}$, $v_{5}$. Apply the breadth-first search algorithm to this graph, starting at $v_{3}$.**Step 1.**

| Push<br>$\underline{\hspace{2cm}}$<br>to the end of the queue and add it to visited vertices. |
| --- |
| Queue: $\underline{\hspace{2cm}}$ Visited: $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{4}]
- [ ] [MATH: v_{3}]

#### Select 2

- [ ] [MATH: v_{4}]
- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{3}]

#### Select 3

- [ ] [MATH: v_{3}]
- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{4}]

---

**Question 4**

![](<../Source/7.6.1. Breadth-First Search/Images/q-249953.png>)

Suppose we fix the following order of vertices in the above graph: $v_{1}$, $v_{2}$, $v_{3}$, $v_{4}$, $v_{5}$, $v_{6}$. Apply the breadth-first search algorithm to this graph, starting at $v_{5}$.**Step 1.**

| Push<br>$\underline{\hspace{2cm}}$<br>to the end of the queue and add it to visited vertices. |
| --- |
| Queue: $\underline{\hspace{2cm}}$ Visited: $\underline{\hspace{2cm}}$ |

#### Select 1

- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{6}]
- [ ] [MATH: v_{4}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{3}]
- [ ] [MATH: v_{5}]

#### Select 2

- [ ] [MATH: v_{6}]
- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{3}]
- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{4}]

#### Select 3

- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{4}]
- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{6}]
- [ ] [MATH: v_{3}]

```update-progress
```

[[MA/DSM/Home|Home]]
[[MA/DSM/0. Table of Contents/TOC|Table of Contents]]
