# Prim's Algorithm

<!--
lesson-id: 1402
topic-code: DSM.7.6.3
-->

## Table of Contents

- [Introduction](#introduction)
- [A Concrete Example of Prim's Algorithm](#a-concrete-example-of-prims-algorithm)
- [Applying Steps of Prim's Algorithm](#applying-steps-of-prims-algorithm)
- [Applying Prim's Algorithm](#applying-prims-algorithm)

## Prerequisites

- [Trees and Forests](<../../7.3. Trees/Lessons/7.3.1. Trees and Forests.md>)

---

<a id="introduction"></a>
## Introduction

**Prim's algorithm** finds a minimum spanning tree of a connected, weighted graph $G$. It follows a so-called **greedy** approach by starting from an arbitrary vertex and expanding the tree by adding the smallest edge that connects a visited vertex to an unvisited vertex. It is widely used in network design, constructing road systems, and optimizing cable connections.

The steps for Prim's algorithm are as follows:

- **Step 1:** Initialize a tree $T$ consisting of a starting vertex $v$ of $G$.
- **Step 2:** While not all vertices are in $T$, find the shortest edge $u - w$ such that $u$ is already in $T$ and $w$ is not.
- **Step 3:** Add vertex $w$ and edge $u - w$ to tree $T$.
- **Step 4:** Repeat this process until all vertices are included in $T$.

We can express Prim's algorithm using **pseudocode.** Pseudocode is a simplified, programming language-independent way of describing an algorithm. It uses a mix of natural language and programming concepts, helping programmers to plan and visualize code logic without focusing on syntax.

The pseudocode for Prim's algorithm is as follows.

> `Initialize a tree T consisting of a starting vertex v of G`
> `while (not all vertices added to T):`
> `Find the shortest edge u-w of G such that
> (u in T) and (w not in T)`
> `Add vertex w and edge u-w to tree T`

Let's take a look at how to apply Prim's algorithm to a concrete graph.

---

<a id="a-concrete-example-of-prims-algorithm"></a>
## A Concrete Example of Prim's Algorithm

![](<../Source/7.6.3. Prim's Algorithm/Images/19ce7982530cab9bea775b5a46aa379a.png>)

Let's use Prim's algorithm, starting from the vertex $v_1$, to find a minimum spanning tree of the above graph.

We proceed as follows:

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/c0a1c6ee4303e5dffffe077a6352b46b.png>) | Add the vertex $v_1$ to the tree. |
| **Step 2.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/ad28ce10b58fc1d4237d8ff98e3e5ac5.png>) | For the edge $v_1-v_3$, we have $v_1$ in the tree, $v_3$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_3$ and edge to our tree. |
| **Step 3.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/f3727c9af20848de79a1a282c0bcabe8.png>) | For the edge $v_3-v_4$, we have $v_3$ in the tree, $v_4$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_4$ and edge to our tree. |
| **Step 4.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/5d2ce15df09e9be578289b866e195448.png>) | For the edge $v_3-v_2$, we have $v_3$ in the tree, $v_2$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_2$ and edge to our tree. |
| **Step 5.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/728b6b4f9276520a8ac8a762db0d5810.png>) | For the edge $v_3-v_5$, we have $v_3$ in the tree, $v_5$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_5$ and edge to our tree. |

The total weight of the resulting minimum spanning tree is

$$
40 + 33 + 36 + 38 = 147
$$

---

<a id="applying-steps-of-prims-algorithm"></a>
## Applying Steps of Prim's Algorithm

**Example:** Consider the weighted graph $G$ with vertices $V=\{v_1, \ldots, v_7\}$ and edges indicated above. Prim's algorithm for finding a minimum spanning tree is applied to the graph, starting at the vertex $v_1$, and after a few steps, the highlighted edges have been added to the tree. According to the algorithm, what edge will be added to the tree next?

![](<../Source/7.6.3. Prim's Algorithm/Images/554f5c2fa93f6ff57bbd0f39ce0f20e0.png>)

**Explanation**

Prim's algorithm finds a minimum spanning tree of a connected, weighted graph $G$. The pseudocode for Prim's algorithm is the following:

> `Initialize a tree T consisting of a starting vertex v of G`
> `while (not all vertices added to T):`
> `Find the shortest edge u-w of G such that
> (u in T) and (w not in T)`
> `Add vertex w and edge u-w to tree T`

So, at the next step of Prim's algorithm, we consider the edge of the smallest weight such that one of its ends belongs to the tree and the other does not. This is the edge $v_3 - v_5$ of weight $6$.

![](<../Source/7.6.3. Prim's Algorithm/Images/f779177e4854f68eebd7f7638f1f8c57.png>)

Therefore, the correct answer is $v_3-v_5$.

---

**Question 1**

![](<../Source/7.6.3. Prim's Algorithm/Images/q-259302.png>)

Consider the weighted graph $G$ with vertices $V = {v_{1}, …, v_{7}}$ and edges indicated above. Prim's algorithm for finding a minimum spanning tree is applied to the graph, starting at the vertex $v_{1}$, and after a few steps, the highlighted edges have been added to the tree. According to the algorithm, what edge will be added to the tree next?

- [ ] A. $v_{4} - v_{2}$
- [ ] B. $v_{4} - v_{3}$
- [ ] C. $v_{4} - v_{7}$
- [ ] D. $v_{5} - v_{3}$
- [ ] E. $v_{7} - v_{2}$

---

**Question 2**

![](<../Source/7.6.3. Prim's Algorithm/Images/q-250778.png>)

Consider the weighted graph $G$ with vertices $V = {v_{1}, …, v_{7}}$ and edges indicated above. Prim's algorithm for finding a minimum spanning tree is applied to the graph, starting at the vertex $v_{1}$, and after a few steps, the highlighted edges have been added to the tree. According to the algorithm, what edge will be added to the tree next?

- [ ] A. $v_{3} - v_{6}$
- [ ] B. $v_{4} - v_{7}$
- [ ] C. $v_{2} - v_{3}$
- [ ] D. $v_{2} - v_{7}$
- [ ] E. $v_{3} - v_{4}$

---

<a id="applying-prims-algorithm"></a>
## Applying Prim's Algorithm

**Example:** Using Prim's algorithm starting from the vertex $v_1$, find a minimum spanning tree of the graph shown above.

![](<../Source/7.6.3. Prim's Algorithm/Images/8438ef61c31aa93c012a180489732248.png>)

**Explanation**

Prim's algorithm finds a minimum spanning tree of a connected, weighted graph $G$. The pseudocode for Prim's algorithm is the following:

> `Initialize a tree T consisting of a starting vertex v of G`
> `while (not all vertices added to T):`
> `Find the shortest edge u-w of G such that
> (u in T) and (w not in T)`
> `Add vertex w and edge u-w to tree T`

With that in mind, we proceed as follows:

| **Step 1.** |  |
| --- | ---: |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/54182e5c37bb1b6712ac0c51c0ba3879.png>) | Add the vertex $v_1$ to the tree. |
| **Step 2.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/ec5a849e1ae47d0ee89dbea79b970574.png>) | For the edge $v_1-v_3$, we have $v_1$ in the tree, $v_3$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_3$ and edge to our tree. |
| **Step 3.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/b825e106efdee9b72ab3ad9a42080a62.png>) | For the edge $v_3-v_4$, we have $v_3$ in the tree, $v_4$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_4$ and edge to our tree. |
| **Step 4.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/85cc7b93188c1b4e6540ffe4ca8b3bba.png>) | For the edge $v_1-v_5$, we have $v_1$ in the tree, $v_5$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_5$ and edge to our tree. |
| **Step 5.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/0751502fb34588bff1f9ac3b73f0199c.png>) | For the edge $v_5-v_6$, we have $v_5$ in the tree, $v_6$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_6$ and edge to our tree. |
| **Step 6.** |  |
| ![](<../Source/7.6.3. Prim's Algorithm/Images/eb518ec98dc6e461613f90964727973c.png>) | For the edge $v_5-v_2$, we have $v_5$ in the tree, $v_2$ not in the tree, and it's the shortest of the remaining edges with this property. So, we add vertex $v_2$ and edge to our tree. |

The total weight of the resulting minimum spanning tree is

$$
8 + 6 + 9 + 5 + 10 = 38
$$

---

**Question 3**

![](<../Source/7.6.3. Prim's Algorithm/Images/q-259202.png>)

Using Prim's algorithm starting from the vertex $v_{1}$, find a minimum spanning tree of the graph shown above.**Step 1.**

Add the vertex
$\underline{\hspace{2cm}}$
to the tree.

#### Select

- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{3}]
- [ ] [MATH: v_{4}]
- [ ] [MATH: v_{1}]

---

**Question 4**

![](<../Source/7.6.3. Prim's Algorithm/Images/q-250788.png>)

Using Prim's algorithm starting from the vertex $v_{1}$, find a minimum spanning tree of the graph shown above.**Step 1.**

Add the vertex
$\underline{\hspace{2cm}}$
to the tree.

#### Select

- [ ] [MATH: v_{6}]
- [ ] [MATH: v_{2}]
- [ ] [MATH: v_{5}]
- [ ] [MATH: v_{1}]
- [ ] [MATH: v_{7}]
- [ ] [MATH: v_{3}]
- [ ] [MATH: v_{4}]

```update-progress
```

[[MA/DSM/Home|Home]]
[[MA/DSM/0. Table of Contents/TOC|Table of Contents]]
