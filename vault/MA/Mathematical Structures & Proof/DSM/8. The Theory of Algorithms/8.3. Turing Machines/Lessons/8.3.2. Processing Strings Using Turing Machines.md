# Processing Strings Using Turing Machines

<!--
lesson-id: 5420
topic-code: DSM.8.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Applying a Turing Machine to a String Using a Transition Diagram](#applying-a-turing-machine-to-a-string-using-a-transition-diagram)
- [Applying a Turing Machine to a String Using a Transition Table](#applying-a-turing-machine-to-a-string-using-a-transition-table)
- [Instantaneous Descriptions](#instantaneous-descriptions)
- [Applying a Turing Machine to a String Using Instantaneous Descriptions](#applying-a-turing-machine-to-a-string-using-instantaneous-descriptions)

## Prerequisites

- [Introduction to Turing Machines](<8.3.1. Introduction to Turing Machines.md>)

---

<a id="introduction"></a>
## Introduction

In a previous lesson, we saw how to update a Turing machine's configuration. In this lesson, we'll discuss setting up and applying a Turing machine to an input string.

In a Turing machine's starting state, a given input string is written on the tape with infinite empty cells on either side.
The tape head is positioned at the leftmost cell that holds the input symbol and is labeled with the machine's start state.

Below is an example of the starting configuration of a Turing machine with a starting state of $q$ and an input string of $10\square1$.

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/dd838135b642ddd9ba248cd283f6f596.png>)

We then repeatedly update its configuration using the transition function $\delta: Q \times \Gamma \to Q \times \Gamma \times \{L,R\}$, as before. By doing so, we get one of the following three cases:

- If the machine reaches a final state in a finite number of steps, it stops.
- If the transition function $\delta(q,X)$ is undefined at some point in the process, the machine gets stuck.
- If the machine repeats a step or sequence of steps forever, it falls into an infinite cycle and never stops.

Let's demonstrate with an example.

---

<a id="a-worked-example"></a>
## A Worked Example

Suppose the string $10\square1$ is written on the tape of the Turing machine given by the diagram below, where $\square$ is the blank symbol and the tape head initially observes the left-most symbol of the string in state $q_1$.

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/cb966e7baf8a9844887857ceeb624a41.png>)

Let's see how the machine processes the data on the tape step-by-step.

- According to the description, our Turing machine starts in the following configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/8c75070bf21b64ce54975276b0914eda.png>)

- The machine observes symbol $1$ in state $q_1$. According to the transition diagram,
$(q_1,1) \to (q_1,1,R)$.
So, we should stay in state $q_1$, write $1$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/03f14026e1d4929172a50bbd204e7bfc.png>)

- The machine observes symbol $0$ in state $q_1$. According to the transition diagram,
$(q_1,0) \to (q_2,1,R)$.
So, we should swap to state $q_2$, write $1$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/14aff7bd978d729aef4e5dbd6b8d7487.png>)

- The machine observes an empty cell $\square$ in state $q_2$. According to the transition diagram,
$(q_2,\square) \to (q_0,1,R)$.
So, we should swap to state $q_0$, write $1$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/701153f8b3d6f8f791d2dc9bd2080e8b.png>)

Finally, the machine observes the symbol $1$ in state $q_0$. According to the transition diagram, $q_0$ is the final state. So, the machine stops at $q_0$.

Let's now see an example of a machine that doesn't stop at a final state.

---

<a id="applying-a-turing-machine-to-a-string-using-a-transition-diagram"></a>
## Applying a Turing Machine to a String Using a Transition Diagram

**Example:** The string $0\square$ is written on the tape of the Turing machine given by the transition table above, where $\square$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_1$. What would be the machine's configuration when it stops?

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/c0468e9617f1cd22d9339dabbc7e3f34.png>)

**Explanation**

Let's see how the machine processes the data on the tape step-by-step.

- According to the description, our Turing machine starts at the following configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/3de3bed74d656319b661e49833e822aa.png>)

- The machine observes symbol $0$ in state $q_1$. According to the transition diagram,
$(q_1,0) \to (q_2,0,R)$.
So, we should swap to state $q_2$, write $0$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/9c7de070e34c0f2ae61aad217757f30a.png>)

- The machine observes an empty cell $\square$ in state $q_2$. According to the transition diagram,
$(q_2,\square) \to (q_2,0,R)$.
So, we should stay in state $q_2$, write $0$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/f0b4ffa6e71d8d9a5d60178ff3c3037d.png>)

Notice that the final step will be repeated forever since the tape has infinitely many empty cells to the right. So, the machine falls into an infinite cycle and never stops.

---

**Question 1**

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-258782.png>)

The string $1◻11$ is written on the tape of the Turing machine given by the above diagram, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$. Which of the following shows the machine's configuration when it stops?

- [ ] A. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-258782-a-3.png>)
- [ ] B. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-258782-a-4.png>)
- [ ] C. The machine does not stop (infinite cycle).
- [ ] D. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-258782-a-1.png>)
- [ ] E. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-258782-a-2.png>)

---

**Question 2**

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238162.png>)

The string $11◻0$ is written on the tape of the Turing machine given by the above diagram, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$. Which of the following shows the machine's configuration when it stops?

- [ ] A. The machine does not stop (infinite cycle).
- [ ] B. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238162-a-2.png>)
- [ ] C. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238162-a-1.png>)
- [ ] D. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238162-a-3.png>)
- [ ] E. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238162-a-4.png>)

---

<a id="applying-a-turing-machine-to-a-string-using-a-transition-table"></a>
## Applying a Turing Machine to a String Using a Transition Table

**Example:** The string $01\square0$ is written on the tape of the Turing machine given by the transition table above, where $\square$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_1$. What would be the machine's configuration when it stops?

| $\Large _{Q} \!\backslash\! ^{\Gamma}$ | $0$ | $1$ | $\square$ |
| --- | ---: | ---: | ---: |
| $\to \! q_1$ | $(q_1,1,R)$ | $-$ | $(q_0,0,L)$ |
| $\ast \: q_0$ | $-$ | $-$ | $-$ |

**Explanation**

Let's see how the machine processes the data on the tape step-by-step.

- According to the description, our Turing machine starts in the following configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/eeac30ee94c09a2a6d79219e05937c45.png>)

- The machine observes symbol $0$ in state $q_1$. According to the transition table,
$\delta(q_1,0) = (q_1,1,R)$.
So, we should stay in state $q_1$, write $1$ into the cell, and move right. Performing this transition gives us the following new configuration:

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/5fb2fa8a06e07b234b9336104184c7cc.png>)

Finally, the machine observes the symbol $1$ in state $q_1$. According to the transition table, the transition function is not defined at $(q_1,1)$. So, the machine gets stuck.

---

**Question 3**

| $_{Q}∖^{Γ}$ | $0$ | $1$ | $◻$ |
| --- | ---: | ---: | ---: |
| $→ q_{1}$ | $-$ | $-$ | $(q_{2}, 0, R)$ |
| $q_{2}$ | $(q_{0}, 0, R)$ | $(q_{2}, 1, R)$ | $-$ |
| $∗q_{0}$ | $-$ | $-$ | $-$ |

The string $◻10$ is written on the tape of the Turing machine given by the transition table above, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$. Which of the following shows the machine's configuration when it stops?

- [ ] A. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-259073-a-2.png>)
- [ ] B. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-259073-a-1.png>)
- [ ] C. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-259073-a-4.png>)
- [ ] D. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-259073-a-3.png>)
- [ ] E. The machine does not stop (infinite cycle).

---

**Question 4**

| $_{Q}∖^{Γ}$ | $0$ | $1$ | $◻$ |
| --- | ---: | ---: | ---: |
| $→ q_{1}$ | $(q_{1}, 0, R)$ | $(q_{1}, 1, R)$ | $(q_{1}, 1, R)$ |
| $∗q_{0}$ | $-$ | $-$ | $-$ |

The string $10$ is written on the tape of the Turing machine given by the transition table above, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$. Which of the following shows the machine's configuration when it stops?

- [ ] A. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238163-a-4.png>)
- [ ] B. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238163-a-5.png>)
- [ ] C. The machine does not stop (infinite cycle).
- [ ] D. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238163-a-2.png>)
- [ ] E. ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-238163-a-3.png>)

---

<a id="instantaneous-descriptions"></a>
## Instantaneous Descriptions

Constructing a tape diagram for each configuration of a Turing machine can be cumbersome.
Fortunately, an **instantaneous description** (ID) provides a concise shorthand notation.

For a given configuration, the ID consists of the current sequence of symbols on the tape, with the current state placed just before the symbol being observed. For example, the ID of a machine with the configuration below is $1q_10$.

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/feb4f0faf1ecfbb2bf8a4f5db496a7c6.png>)

To a machine transitioning from one ID to another, we use the $\vdash$ notation.
For example, if the machine above contains the command $(q_1,0) \to (q_2, 1, R)$, the next transition is expressed as follows:
$1q_10 \:\vdash\: 11q_2\square$

In this case, we add $\square$ after $q_2$ because the head now observes a blank symbol. Let's now apply a Turing machine to a string using instantaneous descriptions.

Suppose the string $00$ is written on the tape of the Turing machine given by the transition table below, where $\square$ is the blank symbol and the tape head initially observes the left-most symbol of the string in state $q_1$.

| $\Large _{Q} \!\backslash\! ^{\Gamma}$ | $0$ | $\square$ |
| --- | ---: | ---: |
| $\to \! q_1$ | $(q_1,0,R)$ | $(q_2,0,L)$ |
| $q_2$ | $(q_2,0,L)$ | $(q_0,0,R)$ |
| $\ast \: q_0$ | $-$ | $-$ |

We'll write a sequence of the machine's moves encoded using instantaneous descriptions.
First, let's summarize the nontrivial commands from our transition table:

| Command | Descriprion |
| --- | ---: |
| $\delta(q_1, 0) = (q_1, 0, R)$ | In state $q_1$, observes $0{:}$<br>write $0$, stay in state $q_1$, and move right. |
| $\delta(q_1, \square) = (q_2, 0, L)$ | In state $q_1$, observes $\square{:}$<br>write $0$, swap to state $q_2$, and move left. |
| $\delta(q_2, 0) = (q_2, 0, L)$ | In state $q_2$, observes $0{:}$<br>write $0$, stay in state $q_2$, and move left. |
| $\delta(q_2, \square) = (q_0, 0, R)$ | In state $q_2$, observes $\square{:}$<br>write $0$, swap to state $q_0$, and move right. |

Then, the machine goes through the following configurations:

| Instantaneous Description | Tape Diagram |
| --- | ---: |
| $q_1 00$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/8dd7ce499fcd1f221673e20f566a4a7d.png>) |
| $0 q_1 0$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/fc1bc3f71afd84e5069f922fded5e632.png>) |
| $00 q_1 \square$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/cbf29350d9df0a52e48c1e820b1a98a9.png>) |
| $0 q_2 00$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/50910a4ea2749e51979beb832195a86d.png>) |
| $q_2 000$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/5f98c3478c48873203330e1098a9e704.png>) |
| $q_2 \square000$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/8692f735da837c8799dbaf09281aa189.png>) |
| $0 q_0 000$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/5e1b285552fc8f28c50b3df22b3b6518.png>) |

Finally, writing the sequence of IDs using $\vdash$ notation, we get the following:

$$
q_{1}00 \mid ⊢0q_{1}0; ⊢00q_{1}◻; ⊢0q_{2}00; ⊢q_{2}000; ⊢q_{2}◻000; ⊢0q_{0}000
$$

---

<a id="applying-a-turing-machine-to-a-string-using-instantaneous-descriptions"></a>
## Applying a Turing Machine to a String Using Instantaneous Descriptions

**Example:** The string $101$ is written on the tape of the Turing machine given by the transition table above, where $\square$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_1$. Write the sequence of the machine's configurations in terms of instantaneous descriptions.

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/6f091253d0ffa753251ca4bba531dd81.png>)

**Explanation**

First, let's summarize the nontrivial commands from our transition table:

| Command | Description |
| --- | ---: |
| $(q_1, 1) \to (q_2, 1, R)$ | In state $q_1$, observes $1{:}$<br>swap to state $q_2$, write $1$, and move right. |
| $(q_1, \square) \to (q_1, 1, L)$ | In state $q_1$, observes $\square{:}$<br>stay in state $q_1$, write $1$, and move left. |
| $(q_2, 0) \to (q_2, 1, R)$ | In state $q_2$, observes $0{:}$<br>stay in state $q_2$, write $1$, and move right. |
| $(q_2, 1) \to (q_0, 1, R)$ | In state $q_2$, observes $1{:}$<br>stay in state $q_0$, write $1$, and move right. |

The machine goes through the following configurations:

| Instantaneous Description | Tape Diagram |
| --- | ---: |
| $q_1 101$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/c5daa005d315f5edca3fa101960f5f09.png>) |
| $1 q_201$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/e558027ac7920e6bef65e24cc0795233.png>) |
| $11 q_2 1$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/91e8109c1057ac2482bdec1126f2c5b9.png>) |
| $111q_0 \square$ | ![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/847834553b8af349a13acd12316bfeab.png>) |

Using the $\vdash$ notation, we get the following:

$$
q_{1}101 \mid ⊢1q_{2}01; ⊢11q_{2}1; ⊢111q_{0}◻
$$

---

**Question 5**

| $_{Q}∖^{Γ}$ | $1$ | $◻$ |
| --- | ---: | ---: |
| $→ q_{1}$ | $(q_{1}, 1, R)$ | $(q_{2}, 1, L)$ |
| $q_{2}$ | $(q_{2}, 1, L)$ | $(q_{0}, 1, R)$ |
| $∗q_{0}$ | $-$ | $-$ |

The string $11$ is written on the tape of the Turing machine given by the transition table above, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$.

Select the correct options in the following sequence of the machine's moves encoded using instantaneous descriptions:

$q_{2}$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: q_{2}]
- [ ] [MATH: q_{1}]
- [ ] [MATH: q_{0}]

---

**Question 6**

![](<../Source/8.3.2. Processing Strings Using Turing Machines/Images/q-259198.png>)

The string $◻10$ is written on the tape of the Turing machine given by the transition table above, where $◻$ is the blank symbol. The tape head initially observes the left-most symbol of the string in state $q_{1}$. Select the correct options in the following sequence of the machine's moves encoded using instantaneous descriptions:

$q_{1}$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: q_{1}]
- [ ] [MATH: q_{2}]
- [ ] [MATH: q_{0}]

```update-progress
```

[[MA/DSM/Home|Home]]
[[MA/DSM/0. Table of Contents/TOC|Table of Contents]]
