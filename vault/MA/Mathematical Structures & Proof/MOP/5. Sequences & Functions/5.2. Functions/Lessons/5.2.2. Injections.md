# Injections

<!--
lesson-id: 2678
topic-code: MOP.5.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Injections For Functions Defined as Mapping Diagrams](#identifying-injections-for-functions-defined-as-mapping-diagrams)
- [The Horizontal Line Test](#the-horizontal-line-test)
- [Identifying Injections For Functions Defined on the Set of Real Numbers](#identifying-injections-for-functions-defined-on-the-set-of-real-numbers)
- [Identifying Injections For Functions Whose Domain or Codomain Are Real Intervals](#identifying-injections-for-functions-whose-domain-or-codomain-are-real-intervals)
- [Identifying Injections Defined on Arbitrary Sets](#identifying-injections-defined-on-arbitrary-sets)
- [Identifying Injections When the Domain is a Small Finite Set](#identifying-injections-when-the-domain-is-a-small-finite-set)
- [Identifying Injections From Functions Given Algebraically](#identifying-injections-from-functions-given-algebraically)

## Prerequisites

- [Graphing Sine and Cosine](<../../../../AG2/9. Trigonometric Functions/9.1. Graphing Trigonometric Functions/Lessons/9.1.1. Graphing Sine and Cosine.md>)
- [Graphing Logarithmic Functions](<../../../../AG2/4. Exponentials & Logarithms/4.6. Graphs of Logarithmic Functions/Lessons/4.6.1. Graphing Logarithmic Functions.md>)
- [Graphing Elementary Cubic Functions](<../../../../AG2/2. Polynomials/2.6. Graphs of Polynomials/Lessons/2.6.1. Graphing Elementary Cubic Functions.md>)
- [Graphing Reciprocal Functions](<../../../../AG2/5. Rational Expressions & Functions/5.2. Reciprocal Functions/Lessons/5.2.1. Graphing Reciprocal Functions.md>)
- [Formal and Informal Language](<../../../1. Logic/1.4. Quantifiers/Lessons/1.4.4. Formal and Informal Language.md>)
- [Sets and Functions](<5.2.1. Sets and Functions.md>)

---

<a id="introduction"></a>
## Introduction

A function $f: X\rightarrow Y$ is an **injection** (also known as **one-to-one**) if *every* element in $X$ is mapped to a *distinct* element in $Y$.

Symbolically, we can write:

> $f: X \to Y$ is an injection if $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, then $x_1=x_2$.

We can read the above statement as follows:

> *The function $f$ that maps from the domain $X$ to the codomain $Y$ is an injection if, for all $x_1$ and $x_2$ contained within the domain $X$, if $f(x_1)$ and $f(x_2)$ are equal, then $x_1$ and $x_2$ are equal*

In other words, a function is an injection if no two different elements of the domain map to the same element of the range.

For example, the following diagram shows an example of an injection.

![](<../Source/5.2.2. Injections/Images/9dcc5aee0c0e128fb654fdcd343b6488.png>)

However, the following is **not** an injection because there are two different elements in the domain ($1$ and $2$) that map to the same element of the range ($-1$).

![](<../Source/5.2.2. Injections/Images/b8c1f894e58e81c9b646664a02d3abde.png>)

---

<a id="identifying-injections-for-functions-defined-as-mapping-diagrams"></a>
## Identifying Injections For Functions Defined as Mapping Diagrams

**Example:** Which of the following statements are true regarding the function $f:X \to Y$ represented by the mapping diagram above?

![](<../Source/5.2.2. Injections/Images/d3258f1091ee98048c3bee43c9bae716.png>)

1. $f(1)=f(2)$
2. $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$
3. $f$ is an injection

**Explanation**

A function $f: X\rightarrow Y$ is an injection (also known as *one-to-one*) if *every* element in $X$ is mapped to a *distinct* element in $Y$:

> $f: X \to Y$ is an injection if $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$.

With that in mind, let's consider each statement in turn.

- Statement I is false. According to the diagram, we have $f(1)=2$ (we have an arrow from $1 \in X$ to $2 \in Y$) and $f(2)=1$ (we have an arrow from $2 \in X$ to $1 \in Y$).
- Statement II is true. As we can see from the diagram, there are no two distinct elements within $X$ that map to the same element in $Y$. So, for all $x_1$ and $x_2$ contained within the domain $X$, if $f(x_1)$ and $f(x_2)$ are equal, then we must have $x_1=x_2$.
- Statement III is true. Indeed, since statement II is true, $f$ is an injection.

Therefore, the correct answer is "II and III only."

---

**Question 1**

![](<../Source/5.2.2. Injections/Images/q-80086.png>)

Which of the following statements are true regarding the function $f: X → Y$ represented by the mapping diagram above?

1. $f(0) = f(6)$
2. $∀x_{1}, x_{2} ∈ X$, if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is an injection

- [ ] A. II only
- [ ] B. None of the statements are true
- [ ] C. I only
- [ ] D. II and III only
- [ ] E. I, II, and III

---

**Question 2**

![](<../Source/5.2.2. Injections/Images/q-80090.png>)

Which of the following statements are true regarding the function $f: X → Y$ represented by the mapping diagram above?

1. $f(-1) = f(-2)$
2. $∀x_{1}, x_{2} ∈ X$, if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is **one-to-one**

- [ ] A. I only
- [ ] B. I, II, and III
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. III only

---

<a id="the-horizontal-line-test"></a>
## The Horizontal Line Test

When a function is presented as a graph, there is a useful graphical method to determine whether a function is an injection or not. This method is called the **horizontal line test.**
Here is how it works:

**Step 1.** Plot the graph of the function.

**Step 2.** Draw a horizontal line on the plot.

**Step 3.** Imagine moving the horizontal line up and down across the entire range.

- If the horizontal line hits the graph only once no matter where we draw it, then the function is an injection. $\quad \color{green}\checkmark$
- If the horizontal line *ever* hits the function more than once, then the function is *not* an injection. $\quad \color{red}\times$

To demonstrate, let's use the horizontal line test to determine whether the following function is an injection.

![](<../Source/5.2.2. Injections/Images/3e029f93b1c14dd7065de9de5a39741a.png>)

If we draw any horizontal line on the plot of the function above, it will hit the graph only once, no matter where we place the line.

![](<../Source/5.2.2. Injections/Images/e55b42e5328db05bb9f1b3406d897669.png>)

Therefore, this function is an injection.

---

<a id="identifying-injections-for-functions-defined-on-the-set-of-real-numbers"></a>
## Identifying Injections For Functions Defined on the Set of Real Numbers

**Example:** Which of the following statements are true regarding the function $f:\mathbb{R}\to\mathbb{R}$ which is given by $f(x)=x^2$?

1. $f(-1) = f(1)$
2. $\forall\, x_1, x_2 \in \mathbb{R}$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$
3. $f$ is an injection

**Explanation**

First, we graph the function.

![](<../Source/5.2.2. Injections/Images/b528009991e75ec7c8c822857ed23147.png>)

A function $f: X\rightarrow Y$ is an injection (also known as *one-to-one*) if *every* element in $X$ is mapped to a *distinct* element in $Y$:

> $f: X \to Y$ is an injection if $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$.

With that in mind, let's consider each statement in turn.

- Statement I is true. We have $f(-1)=(-1)^2 = 1$ and $f(1)=1^2 = 1$.
- Statement II is false. We have $f(-1)=f(1)$ but $-1 \neq 1$. So, it's *not true* that for all $x_1$ and $x_2$ contained within the domain $\mathbb{R}$, if $f(x_1)$ and $f(x_2)$ are equal, then $x_1=x_2$.
- Statement III is false. Indeed, since statement II is false, $f$ is not an injection. We can also see that $f$ is not an injection because it fails the horizontal line test: since $f(-1)=f(1)=1$, the line $y=1$ passes through the points $(-1,1)$ and $(1,1)$.

Therefore, the correct answer is "I only."

---

**Question 3**

![](<../Source/5.2.2. Injections/Images/q-80091.png>)

Which of the following statements are true regarding the function $f: R → R$ whose graph is shown above?

1. $f(-2) \ne f(0)$
2. $∀x_{1}, x_{2} ∈ R$, if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is **one-to-one**

- [ ] A. II only
- [ ] B. I only
- [ ] C. None of the statements are true
- [ ] D. II and III only
- [ ] E. III only

---

**Question 4**

Which of the following statements are true regarding the function $f: R → R$ which is given by $f(x) = x^{3}$?

1. $f(-1) = f(1)$
2. $∀x_{1}, x_{2} ∈ R$, if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is an injection

- [ ] A. II only
- [ ] B. I only
- [ ] C. II and III only
- [ ] D. I, II, and III
- [ ] E. III only

---

<a id="identifying-injections-for-functions-whose-domain-or-codomain-are-real-intervals"></a>
## Identifying Injections For Functions Whose Domain or Codomain Are Real Intervals

**Example:** Which of the following statements are true regarding the function $f:[1,3]\to[0,4]$ whose graph is shown below?

1. $f(1) \neq f(3)$
2. $\forall\, x_1, x_2 \in [1,3]$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$
3. $f$ is **one-to-one**

![](<../Source/5.2.2. Injections/Images/43793438f13a10b53c35bf9b3543ed3b.png>)

**Explanation**

A function $f: X\rightarrow Y$ is an injection (also known as *one-to-one*) if *every* element in $X$ is mapped to a *distinct* element in $Y$:

> $f: X \to Y$ is an injection if $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$.

With that in mind, let's consider each statement in turn.

- Statement I is true. According to the graph, we have $f(1)=0$ and $f(3)=4$.
- Statement II is true. As we can see from the graph, it passes the horizontal line test, meaning that there are no two distinct elements within $[1,3]$ that map to the same element in $[0,4]$. So, for all $x_1$ and $x_2$ contained within the domain $[1,3]$, if $f(x_1)$ and $f(x_2)$ are equal, then we must have $x_1=x_2$.
- Statement III is true. Indeed, since statement II is true, $f$ is an injection (also known as *one-to-one*).

Therefore, the correct answer is "I, II, and III."

---

**Question 5**

Which of the following statements are true regarding the function $f: [- 1, 1] → R$ which is given by $f(x) = x^{2} + 1$?

1. $f(-1) = f(1)$
2. $∀x_{1}, x_{2} ∈ [- 1, 1]$, if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is an injection

- [ ] A. II only
- [ ] B. I only
- [ ] C. III only
- [ ] D. I, II, and III
- [ ] E. I and III only

---

**Question 6**

![](<../Source/5.2.2. Injections/Images/q-80096.png>)

Which of the following statements are true regarding the function $[MATH: f: [0, ∞] → [1, ∞)]$ whose graph is shown above?

1. $f(0) \ne f(1)$
2. $[MATH: ∀x_{1}, x_{2} ∈ [0, ∞),]$ if $f(x_{1}) = f(x_{2})$, we have $x_{1} = x_{2}$
3. $f$ is **one-to-one**

- [ ] A. I only
- [ ] B. II and III only
- [ ] C. I and III only
- [ ] D. I and II only
- [ ] E. I, II, and III

---

<a id="identifying-injections-defined-on-arbitrary-sets"></a>
## Identifying Injections Defined on Arbitrary Sets

How do we determine whether a function $f$ is an injection without using a graph or mapping diagram?

To do this, we take $x_1$ and $x_2$ (arbitrary but fixed) in the domain of $f$, and we construct the equation

$$
f(x_1)=f(x_2)
$$

Using algebraic manipulation, we aim to get some relation between the variables $x_1$ and $x_2$. If we get $x_1=x_2$, this implies that $f$ is an injection.

Let's use this idea to prove that $f:\mathbb{R}\rightarrow (0,\infty)$ defined by

$$
f(x)=2^x
$$

is an injection.

First, we construct the equation

$$
f(x_1) = f(x_2)
$$

This gives

$$
\begin{aligned}
f(x_{1}) &= f(x_{2}) \\
2^{x_{1}} &= 2^{x_{2}}
\end{aligned}
$$

Now, using algebraic manipulation, we reduce this relationship to $x_1=x_2$, as follows:

$$
\begin{aligned}
2^{x_{1}} &= 2^{x_{2}} \\
\log_{2} (2^{x_{1}}) &= \log_{2} (2^{x_{2}}) \\
x_{1} &= x_{2}
\end{aligned}
$$

Therefore, for any $x_1, x_2 \in\mathbb{R}$, if

$$
f(x_1)=f(x_2)
$$

we have $x_1=x_2$. In other words, $f$ is an injection.

---

<a id="identifying-injections-when-the-domain-is-a-small-finite-set"></a>
## Identifying Injections When the Domain is a Small Finite Set

Now, suppose that $Y$ is a relatively small finite set, and we want to know if a function $f: X \rightarrow Y$ is an injection. All we need to do is go through each element $x$ in $X$ ensure that they each map to *distinct* elements of $Y$.

For example, consider the function $f: \{-1,0,1\} \to \{{\color{blue}0},{\color{red}4}\}$ defined by

$$
f(x) =4x^2
$$

Is it an injection?

Since the domain $X$ is a small, finite set, it's easy to compute the range.

$$
f(-1)={\color{red}4},\quad f(0)={\color{blue}0}, \quad f(1)={\color{red}4}
$$

Note that

$$
f(-1) = f(1) = {\color{red}4}
$$

but

$$
-1\neq 1
$$

So, the elements in the domain $X$ do not map to distinct elements in $Y$.

Therefore, $f$ is not an injection.

---

<a id="identifying-injections-from-functions-given-algebraically"></a>
## Identifying Injections From Functions Given Algebraically

**Example:** Which of the following functions are injections?

1. $f: \mathbb{R} \to [-1,1]$ defined by $f(x) = \cos x$
2. $g: \mathbb{R} \to \mathbb{R}$ defined by $g(x) = 3x+2$

**Explanation**

A function $f: X\rightarrow Y$ is a injection (also known as one-to-one) if *every* element in $x$ in $X$ is mapped to a *distinct* element in $Y$.

Symbolically, we can write:

> $f: X \to Y$ is an injection if $\forall\, x_1, x_2 \in X$, if $f(x_1)=f(x_2)$, we have $x_1=x_2$.

With that in mind, let's consider each of the given functions.

- I is *not* an injection. For example, $f(0) = 1 = f(2\pi)$, but $0 \neq 2\pi$.
- II is an injection. Setting $g(x_1) = g(x_2)$, we get the following:
$g(x_{1})|= g(x_{2}); 3x_{1} + 2|= 3x_{2} + 2; 3x_{1}|= 3x_{2}; x_{1}|= x_{2}$

---

**Question 7**

Which of the following functions are injections?

1. $f: {1, 2, 3, 4} → {3, 4, 6, 12}$ defined by $f(x) = \frac{1}{x}$
2. $g: {0, 2, 4, 8, 10} → R$ defined by $g(x) = x^{2} - 2x$
3. $h: {1, 2, 3, 4} → {-1, 1, 3, 5}$ defined by $h(x) = 2x - 3$

- [ ] A. I only
- [ ] B. I and III only
- [ ] C. II and III only
- [ ] D. III only
- [ ] E. I and II only

---

**Question 8**

Which of the following functions are injections?

1. $f: R → R$ defined by $f(x) = x^{3}$
2. $g: R → R$ defined by $g(x) = x^{2}$
3. $h: R → R$ defined by $h(x) = \sin x$

- [ ] A. I only
- [ ] B. III only
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. II only

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
