# Power Sets

<!--
lesson-id: 51
topic-code: MOP.2.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Using a Tree Diagram](#using-a-tree-diagram)
- [Listing the Elements in a Power Set](#listing-the-elements-in-a-power-set)
- [The Cardinality of a Power Set](#the-cardinality-of-a-power-set)
- [Computing the Cardinality of a Power Set](#computing-the-cardinality-of-a-power-set)
- [Determining Cardinality in More Advanced Cases](#determining-cardinality-in-more-advanced-cases)
- [Identifying True Statements](#identifying-true-statements)
- [Power Sets of Infinite Sets](#power-sets-of-infinite-sets)

## Prerequisites

- [The Cartesian Product](<../../2.3. Set Operations/Lessons/2.3.3. The Cartesian Product.md>)

---

<a id="introduction"></a>
## Introduction

Suppose $S$ is a set. The **power set** of $S$, denoted $\mathcal{P}(S)$, is the set containing every subset of $S$, *including* the empty set.

For example, let's find the power set of the set

$$
S = \{1,2,3 \}
$$

To do this, we list out all the subsets of $S{:}$

- The subset containing no elements is $\emptyset$.
- The subsets containing $1$ element are $\{1 \}$, $\{2 \}$, and $\{3 \}$.
- The subsets containing $2$ elements are $\{1,2 \}$, $\{1,3 \}$, and $\{2,3 \}$.
- The subset containing $3$ elements is $S$ itself: $\{1,2,3 \}$.

Now, let's put all these subsets together as elements of a single set. We get

$$
\mathcal{P}(S) = \big\{\emptyset, \{1\}, \{2\}, \{3\}, \{1,2 \}, \{1,3 \}, \{2,3 \}, \{1,2,3 \} \big\}
$$

---

<a id="using-a-tree-diagram"></a>
## Using a Tree Diagram

Another way to generate all the subsets of

$$
S=\{1,2,3 \}
$$

is using a tree diagram. We start off with the empty set $\{\}$ and generate additional subsets of $S$ by systematically inserting or excluding elements of $S$.

Let's go through the process.

- Start with the empty set $\{\}$ at the root node of our tree. This is the parent node.
- Draw two branches pointing away from the parent node. The tip of each arrow gives a child node. Label one branch 'No' and the other 'Yes'.![](<../Source/2.2.2. Power Sets/Images/b4a7a88ae1f90be541367655ad6fb2dc.png>)
- We'll now write down a subset of $S$ at each child node:At the child node (tip of the arrow) whose branch is labeled 'No,' we write down the parent node's set.
$\{\}$At the child node whose branch is labeled 'Yes,' we create a new set by inserting the *first* element of $S$ into the parent node's set. This gives the following subset of $S{:}$
$\{1\}$
We now have the following diagram.![](<../Source/2.2.2. Power Sets/Images/4259611c46a8ca9601625713bc12c483.png>)

We repeat the process, setting the subsets found in the previous step as the new parent nodes.

- We start by considering the parent node $\{\}{:}$At the child node whose branch is labeled 'No', we write down the parent node's set.
$\{\}$At the child node whose branch is labeled 'Yes,' we create a new set by inserting the *second* element of $S$ into the parent node's set. This gives the following subset of $S{:}$
$\{2\}$
We now have the following diagram.![](<../Source/2.2.2. Power Sets/Images/8272d9581365eacdfa77a9cba1424918.png>)
- Then, we consider the parent node $\{1\}{:}$At the child node whose branch is labeled 'No', we write down the parent node's set.
$\{1\}$At the child node whose branch is labeled 'Yes,' we create a new set by inserting the *second* element of $S$ into the parent node's set. This gives the following subset of $S{:}$
$\{1,2\}$
We now have the following diagram.![](<../Source/2.2.2. Power Sets/Images/664f7152d9215b771aee507f9a803926.png>)
- Finally, we repeat the process for the new parent nodes $\{\}, \{2\}, \{1\}$, and $\{1,2\}$. Our final tree diagram looks as follows.![](<../Source/2.2.2. Power Sets/Images/607b826c840be9e34912dfd04a3bcc43.png>)

The final column lists all subsets of $S$.

$$
\emptyset = \{\},\quad \{1\},\quad \{2\},\quad \{3\},\quad \{1,2 \},\quad \{1,3 \},\quad \{2,3 \},\quad \{1,2,3 \}
$$

Finally then, we can write down $\mathcal P(S){:}$

$$
\mathcal{P}(S) = \big\{\emptyset, \{1\}, \{2\}, \{3\}, \{1,2 \}, \{1,3 \}, \{2,3 \}, \{1,2,3 \} \big\}
$$

---

<a id="listing-the-elements-in-a-power-set"></a>
## Listing the Elements in a Power Set

**Example:** Find $\mathcal{P}(A)$ if $A = \{2, 4, \{8 \} \}$.

**Explanation**

The power set of $A$, denoted $\mathcal{P}(A)$, is the set that contains every possible subset of $A$, including the empty set $\emptyset$.

So, let's list all possible subsets of $A$ using a tree diagram.

![](<../Source/2.2.2. Power Sets/Images/87c0e740ce1476af9e41cec575d50873.png>)

According to our diagram, the subsets of $A$ are

$$
\emptyset = \{\},\quad \{2\},\quad \{4\},\quad \{\{8\} \}, \quad \{2, 4\},\quad \{2, \{8\}\},\quad \{4, \{8\}\},\quad\{2, 4, \{8\}\}
$$

Therefore, we have

$$
\mathcal{P}(A) = \{\emptyset, \{2\}, \{4\}, \{\{8\}\}, \{2, 4\}, \{2, \{8\}\}, \{4, \{8\}\},\{2, 4, \{8\}\}\}
$$

---

**Question 1:** If $X = {m, n}$, then $P(X) =$

- [ ] A. ${{m}, {n}}$
- [ ] B. ${∅, {m, n}}$
- [ ] C. ${∅, {m}, {n}, {m, n}}$
- [ ] D. ${{m}, {n}, {m, n}}$
- [ ] E. ${{m, n}}$

---

**Question 2:** If $C = {{2}, 3}$, then $P(C) =$

- [ ] A. ${∅, {{2}}, {3}, {{2}, {3}}}$
- [ ] B. ${{2}, {3}, {{2}, 3}}$
- [ ] C. ${∅, {{2}}, {3}, {{2}, 3}}$
- [ ] D. ${∅, {{2}, 3}}$
- [ ] E. ${∅, {{2}}, {{3}}, {{2}, 3}}$

---

**Question 3:** If $T = {r, s, t}$, then $P(T) =$

- [ ] A. ${∅, {r}, {s}, {t}, {r, s}, {r, t}, {s, t}, {r, s, t}}$
- [ ] B. ${{r}, {s}, {t}, {r, s, t}}$
- [ ] C. ${∅, {r, s, t}}$
- [ ] D. ${∅, {r}, {s}, {t}, {r, s}, {r, t}, {s, t}}$
- [ ] E. ${∅, r, s, t, {r, s}, {r, t}, {s, t}, {r, s, t}}$

---

<a id="the-cardinality-of-a-power-set"></a>
## The Cardinality of a Power Set

Suppose we have a finite set $S$ with cardinality $\mid S \mid$. Then, the cardinality of $\mathcal P(S)$ is given by

$$
\left\begin{vmatrix}\mathcal{P}(S)\right & = 2^{\left| S \right\end{vmatrix}}
$$

In other words, a finite set with cardinality $\mid S \mid$ has $2^{\mid S \mid}$ subsets.

For example, for the set

$$
S = \{a,b,c \}
$$

we have

$$
\mid S \mid = 3
$$

and therefore

$$
\left\begin{vmatrix}\mathcal{P}(S)\right & = 2^{\left| S \right\end{vmatrix}} = 2^3 = 8
$$

This tells us that $S$ has $8$ subsets, and consequently, $\mathcal{P}(S)$ has $8$ elements.

$$
\mathcal{P}(S) = \big\{\underbrace{\emptyset}_1, \underbrace{\{a\}}_2, \underbrace{\{b\}}_3, \underbrace{\{c\}}_4, \underbrace{\{a,b \}}_5, \underbrace{\{a,c \}}_6, \underbrace{\{b,c \}}_7, \underbrace{\{a,b,c \}}_8 \big\}
$$

Note the following:

- It's clear from our rule that if $S$ is finite, then $\mathcal P(S)$ is finite.
- It can be shown that if $S$ is infinite, then $\mathcal P(S)$ is also infinite.

We'll discuss power sets of infinite sets at the end of this lesson and in future lessons.

---

<a id="computing-the-cardinality-of-a-power-set"></a>
## Computing the Cardinality of a Power Set

**Example:** If $Y = \{1, \{3, 5 \}, 7, X \}$, then what is $\left|\mathcal{P}(Y)\right|$?

**Explanation**

For any set $Y$, we have

$$
\left\begin{vmatrix}\mathcal{P}(Y)\right & = 2^{\left| Y \right\end{vmatrix}}
$$

In this case,

$$
\left| Y \right| = 4
$$

so we have

$$
\left\begin{vmatrix}\mathcal{P}(Y) \right & = 2^{\left| Y \right\end{vmatrix}} = 2^{4} = 16
$$

Remember that $\{3,5 \}$ is a single element in the set $Y$.

---

**Question 4:** Suppose that $A$ is a set. If $\mid A|= 5$, then $\mid P(A)|=$

- [ ] A. $8$
- [ ] B. $16$
- [ ] C. $32$
- [ ] D. $10$
- [ ] E. $5$

---

**Question 5:** If $B = {3, 5, 7, 9, 11}$, then $\mid P(B)|=$

- [ ] A. $32$
- [ ] B. $10$
- [ ] C. $5$
- [ ] D. $8$
- [ ] E. $16$

---

**Question 6:** Suppose that $X$ and $Y$ are sets. If $\mid X|= 3$ and $\mid Y|= 4$, then $\begin{vmatrix}P(X) & +\end{vmatrix}P(Y)|=$

- [ ] A. $36$
- [ ] B. $22$
- [ ] C. $16$
- [ ] D. $34$
- [ ] E. $24$

---

<a id="determining-cardinality-in-more-advanced-cases"></a>
## Determining Cardinality in More Advanced Cases

**Example:** Given that $Y =\{\mu\}$ and $Z =\{1,2\}$, find $\left|\mathcal{P}(\mathcal{P}(Y\times Z))\right|$.

**Explanation**

For any set $S$, we have

$$
\left\begin{vmatrix}\mathcal{P}(S)\right & = 2^{\left| S \right\end{vmatrix}}
$$

Also,

$$
\begin{vmatrix}S \times T & = & S & \cdot & T\end{vmatrix}
$$

In our case,

$$
\left\begin{vmatrix}Y \right & = 1, \qquad \left & Z \right\end{vmatrix} = 2
$$

As a result,

$$
\begin{aligned}
\begin{vmatrix}P(Y \cdot Z) & &= 2^{\mid Y \cdot Z\end{vmatrix}} \\
&= 2^{\begin{vmatrix}Y & \cdot & Z\end{vmatrix}} \\
&= 2^{1 \cdot 2} \\
&= 2^{2} \\
&= 4
\end{aligned}
$$

Therefore, we obtain

$$
\begin{aligned}
\begin{vmatrix}P(P(Y \cdot Z)) & &= 2^{\mid P(Y \cdot Z)\end{vmatrix}} \\
&= 2^{4} \\
&= 16
\end{aligned}
$$

---

**Question 7:** If $\mid X|= 2$ then $\mid P(P(X))|=$

- [ ] A. $32$
- [ ] B. $16$
- [ ] C. $4$
- [ ] D. $2$
- [ ] E. $8$

---

**Question 8:** If $A = {2, 3}$ and $B = ∅$ then $\mid P(A \cdot P(B))|=$

- [ ] A. $4$
- [ ] B. $128$
- [ ] C. $32$
- [ ] D. $8$
- [ ] E. $2$

---

<a id="identifying-true-statements"></a>
## Identifying True Statements

**Example:** Which of the following statements are true?

1. $\mathcal{P}(\{\{\emptyset\}\}) = \{\emptyset, \{\emptyset\} \}$
2. $\mathcal{P} (\{\emptyset, \{0\} \}) = \{\emptyset, \{0\}, \{\{\emptyset\}\}, \{0, \emptyset\} \}$
3. $\mathcal{P}(\mathcal{P}(\{y\})) = \{\emptyset, \{\emptyset\}, \{\{y\}\}, \{\emptyset, \{y\}\} \}$

**Explanation**

The power set of $T$, denoted $\mathcal{P}(T)$, is the set that contains every possible subset of $T$, including the empty set $\emptyset$.

With that in mind, let's examine our statements.

- Statement I is false. Notice that we are given the set $\{\{\emptyset\}\}$. So, its power set must be $\{\emptyset, \{\{\emptyset\}\} \}$.
- Statement II is false. Notice that we are given the set $\{\emptyset, \{0\} \}$. So, its power set must be $\{\emptyset, \{\{0\}\}, \{\emptyset\}, \{\emptyset, \{0\} \} \}$.
- Statement III is true. Notice that we are given the set $\{y\}$. So, its power set must be $\{\emptyset, \{y\} \}$. Therefore, $\mathcal{P}(\mathcal{P}(\{y\})) = \{\emptyset, \{\emptyset\}, \{\{y\}\}, \{\emptyset, \{y\}\} \}$.

Therefore, the correct answer is "III only."

---

**Question 9**

Which of the following statements are true?

1. $P(1) = {∅, 1}$
2. $P({1}) = {∅, {1}}$
3. $P({{1}}) = {∅, {1}}$

- [ ] A. I only
- [ ] B. I and II only
- [ ] C. II only
- [ ] D. I, II, and III
- [ ] E. II and III only

---

**Question 10**

Which of the following sets are finite?

1. $P(N)$
2. $P({Z, π})$
3. $P({x ∈ Z∣\sin x = 0})$

- [ ] A. II only
- [ ] B. II and III only
- [ ] C. I and III only
- [ ] D. I, II, and III
- [ ] E. I only

---

<a id="power-sets-of-infinite-sets"></a>
## Power Sets of Infinite Sets

In this lesson, our primary focus was on power sets of *finite* sets. The definition of the power set of an *infinite* set mirrors that of finite sets. Namely, it comprises all possible subsets.

As we already understand, for finite sets, the size of power sets grows exponentially based on the size of the underlying set:

$$
\big\begin{vmatrix}\mathcal{P}(X) \big & = 2^{\mid X\end{vmatrix}}
$$

When dealing with power sets of infinite sets, the situation is more intricate.

For example, let's consider $\mathbb{R}^2$, the set containing all points on the Cartesian plane. What are the elements of the power set $\mathcal P(\mathbb R^2)$?

Any collection of points on the plane is a subset of $\mathbb R^2$. Such subsets can be visualized as black-and-white images on the plane, where points belonging to the subset are drawn in black, and points not belonging to the subset are represented in white.

For instance, each of the following images gives us a specific element of $\mathcal{P}(\mathbb{R}^2){:}$

![](<../Source/2.2.2. Power Sets/Images/0a1b393548b82c5caf0d0ee4708c4e1f.png>)

It follows that any text, be it any or all existing Math Academy lessons, those planned for the future, or any other possible text (finite or infinite), including those not yet written, are all contained within $\mathcal{P}(\mathbb{R}^2)$.

![](<../Source/2.2.2. Power Sets/Images/a0bc527a79aeac68d88a4f074d63f49c.png>)

It's somewhat extraordinary that such a vast and important set can be expressed using just the symbols $\mathcal P(\mathbb R^2)$.

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
