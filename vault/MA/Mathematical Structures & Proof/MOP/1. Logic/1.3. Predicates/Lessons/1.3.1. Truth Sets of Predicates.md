# Truth Sets of Predicates

<!--
lesson-id: 4254
topic-code: MOP.1.3.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Truth Set of a Predicate](#finding-the-truth-set-of-a-predicate)
- [Predicates With More Than One Variable](#predicates-with-more-than-one-variable)
- [Identifying Predicates of More Than One Variable](#identifying-predicates-of-more-than-one-variable)
- [Finding the Truth Set of a Multivariable Predicate](#finding-the-truth-set-of-a-multivariable-predicate)

## Prerequisites

- [Statements and Predicates](<../../1.1. Statements/Lessons/1.1.1. Statements and Predicates.md>)
- [Quadratic Equations with Purely Imaginary Solutions](<../../../../AG2/1. Number Systems/1.2. Introduction to Complex Numbers/Lessons/1.2.2. Quadratic Equations with Purely Imaginary Solutions.md>)
- [Describing Planar Regions Using Set-Builder Notation](<../../../2. Set Theory/2.1. Introduction to Set Theory/Lessons/2.1.6. Describing Planar Regions Using Set-Builder Notation.md>)

---

<a id="introduction"></a>
## Introduction

Recall that a *predicate* of one variable is a sentence that contains a variable defined on some universal set and has the property that when we substitute a concrete value of the variable into the predicate, the result becomes a mathematical statement that is either true or false.

The **truth set** $T$ of a predicate $P(x)$ is the set of elements in its universal set $U$ such that substituting these elements into the predicate gives a true statement:

$$
T = \big\{x \in U \: \mid \: P(x) \big\}
$$

For example, let's find the truth set of the following predicate:

$\qquad$ $D(x)$: $x$ is divisible by $2$,

where the universal set is

$$
U = \big\{1,2, \ldots, 6 \big\}
$$

To find the truth set $T$, we need to find $x \in U$ such that the following is true:

$\qquad$ $x$ is divisible by $2$.

Let's check all the elements in our universal set $U$ to determine which of them are elements of the truth set $T{:}$

- $x=1\notin T$ since $1$ is *not* divisible by $2$
- $x=2\in T$ since $2$ is divisible by $2$
- $x=3\notin T$ since $3$ is *not* divisible by $2$
- $x=4\in T$ since $4$ is divisible by $2$
- $x=5\notin T$ since $5$ is *not* divisible by $2$
- $x=6\in T$ since $6$ is divisible by $2$

Therefore, the truth set of our predicate is

$$
T = \big\{2,4,6 \big\}
$$

---

<a id="finding-the-truth-set-of-a-predicate"></a>
## Finding the Truth Set of a Predicate

**Example:** Find the truth set of the following predicate:

$\qquad$ $P(x)$: $x^2+2x=3$

where the universal set is $\mathbb{Z}$.

**Explanation**

The *truth set* of a predicate $P(x)$ is the set of elements in its universal set $U$ such that substituting these elements for the variable makes the predicate a true statement:

$$
T = \big\{x \in U \: \mid \: P(x) \big\}
$$

The universal set for our predicate is

$$
U = \mathbb{Z}
$$

We need to find $x \in U$ such that the following is true:

$\qquad$

$$
x^2+2x=3
$$

Now, we solve the above equation by factoring:

$$
\begin{aligned}
x^{2} + 2x &= 3 \\
x^{2} + 2x - 3 &= 0 \\
(x + 3)(x - 1) &= 0
\end{aligned}
$$

So, the solutions are

$$
x=-3\in\mathbb Z
$$

and

$$
x=1\in\mathbb Z
$$

Therefore, the truth set of our predicate is

$$
T = \{-3,1\}
$$

---

**Question 1**

Find the truth set of the following predicate:

$P(x)$: $3x + 1 \le 16$,

where the universal set is $R$.

- [ ] A. $[MATH: (- ∞, 16]]$
- [ ] B. $(5, ∞)$
- [ ] C. $∅$
- [ ] D. $R$
- [ ] E. $[MATH: (- ∞, 5]]$

---

**Question 2**

Find the truth set of the following predicate:

$P(x)$: $x^{2} - 5x =-6$

where the universal set is $Q$.

- [ ] A. ${2, 3}$
- [ ] B. $∅$
- [ ] C. ${2, 5}$
- [ ] D. ${-2,-3}$
- [ ] E. ${-2, 5}$

---

<a id="predicates-with-more-than-one-variable"></a>
## Predicates With More Than One Variable

A predicate of the variables $x_1,x_2,\ldots,x_n$ is a sentence that contains these variables with the property that when we substitute concrete values for all the variables, the result becomes a statement that is either true or false.

We can denote such a predicate using the following notation:

$$
P(x_1,x_2,\ldots,x_n)
$$

If $U_1$ is the universal set for $x_1,U_2$ is the universal set for $x_2,\ldots$, etc, then the universal set $U$ for our predicate is given by the Cartesian product

$$
U = U_1 \times U_2 \times \cdots \times U_n
$$

For example, the sentence

$\qquad$ $P(x,y): x+y \lt 2$, $\quad$ $x,y \in \mathbb{R}$

is a predicate of two variables, $x$ and $y$. In this case, the universal set is

$$
U = \mathbb{R} \times \mathbb{R} = \mathbb{R}^2
$$

The **truth set** $T$ of a predicate $P(x_1,x_2,\ldots,x_n)$ is the set of $n$-tuples $(x_1, x_2, \ldots, x_n) \in U$ such that substituting these tuples into the predicate gives a true statement:

$$
T = \big\{(x_1,x_2,\ldots,x_n) \in U \: \mid \: P(x_1,x_2,\ldots,x_n) \big\}
$$

For example, to determine the truth set of the predicate $P(x,y)$ defined above, we need to find $(x,y) \in \mathbb R^2$ such that the following is true:

$\qquad$ $x+y \lt 2$

To find the truth set, we first find an explicit condition on $y$ as a function of $x{:}$

$$
\begin{bmatrix}x + y & < 2 \\ y & < 2 - x\end{bmatrix}
$$

Therefore, the truth set contains all tuples $(x,y)\in\mathbb R^2$ that satisfy this inequality. We can write the truth set $T$ of our predicate as follows:

$$
T = \big\{(x,y)\in\mathbb R^2 \: \mid \: y < 2-x\big\}
$$

Finally, since $T$ is a subset of $\mathbb R^2$, we can represent it as a region in the coordinate plane.

![](<../Source/1.3.1. Truth Sets of Predicates/Images/072221478c5610436ffb2446d3d8fc84.png>)

---

<a id="identifying-predicates-of-more-than-one-variable"></a>
## Identifying Predicates of More Than One Variable

**Example:** Which of the following are mathematical predicates?

1. $x \geq y$
2. $\dfrac{p}{q}$ is a rational number.
3. $a$ is a positive number.

**Explanation**

A *predicate* of the variables $x_1,x_2,\ldots,x_n$ is a sentence that contains the variables defined on some *universal set,* and has the property that when we substitute concrete values for all the variables into the predicate, the result becomes a statement that is either true or false.

With that in mind, let's examine our options.

- Option I is a predicate of two variables. Substituting a concrete pair of values $(x,y)$ into the sentence, we get a statement that is either true or false.
- Option II is a predicate of two variables. Substituting a concrete pair of values $(p,q)$ into the sentence, we get a statement that is either true or false.
- Option III is a predicate of one variable. Substituting a concrete value for $a$ into the sentence, we get a statement that is either true or false.

Therefore, the correct answer is "I, II, and III."

---

**Question 3**

Which of the following are mathematical predicates?

1. $x < y$
2. $n$ is a perfect square.
3. Are $n$ and $m$ coprime?

- [ ] A. I only
- [ ] B. I, II, and III
- [ ] C. I and II only
- [ ] D. None
- [ ] E. I and III only

---

**Question 4**

Which of the following are mathematical predicates?

1. $P(x)$: $5 \mid x$
2. $Q(x, y)$: $y + x^{2}$
3. $R(x, y)$: $x + y \ge 5$

- [ ] A. II only
- [ ] B. I and III only
- [ ] C. I and II only
- [ ] D. III only
- [ ] E. I only

---

<a id="finding-the-truth-set-of-a-multivariable-predicate"></a>
## Finding the Truth Set of a Multivariable Predicate

**Example:** Find the truth set of the following predicate:

$\qquad$ $p \, \mid \, q$,

where the universal set is $\big\{(p,q) \: \mid \: p = 1,2,6, \: q = 3,6,9 \big\}$.

**Explanation**

The *truth set* of a predicate $P(x_1,x_2,\ldots,x_n)$ is the set of $n$-tuples in its universal set

$$
U = U_1 \times U_2 \times \cdots \times U_n
$$

such that substituting these tuples for the variables makes the predicate a true statement:

$$
T = \big\{(x_1,x_2,\ldots,x_n) \in U \: \mid \: P(x_1,x_2,\ldots,x_n) \big\}
$$

The universal set for our predicate is

$$
\begin{aligned}
U &= {1, 2, 6} \cdot {3, 6, 9} \\
&= {(1, 3), (1, 6), (1, 9), (2, 3), (2, 6), (2, 9), (6, 3), (6, 6), (6, 9)}
\end{aligned}
$$

We need to find $(p,q) \in U$ such that the following is true:

$\qquad$ $p \, \mid \, q$

The following pairs give the elements of our truth set:

- $(1,3)$ since $1$ divides $3$,
- $(1,6)$ since $1$ divides $6$,
- $(1,9)$ since $1$ divides $9$,
- $(2,6)$ since $2$ divides $6$,
- $(6,6)$ since $6$ divides $6$.

Therefore, the truth set of our predicate is

$$
T = \big\{(1,3), (1,6), (1,9), (2,6), (6,6) \big\}
$$

---

**Question 5**

Find the truth set of the following predicate:

$x$ and $y$ are coprime,

where the universal set is ${(x, y) \mid x = 2, 3, y = 3, 4, 5}$.

*Hint: Integers $x$ and $y$ are coprime if their greatest common factor equals $1$.*

- [ ] A. ${(2, 3), (2, 4), (2, 5)}$
- [ ] B. $∅$
- [ ] C. ${(2, 3), (2, 5), (3, 3), (3, 4)}$
- [ ] D. ${(2, 3), (2, 5), (3, 4), (3, 5)}$
- [ ] E. ${(2, 4), (3, 3)}$

---

**Question 6**

Consider the following predicate:

$P(x, y)$: $x - 2y \le 2$,

where the universal set is $R^{2}$. Which of the following shaded regions represents the truth set of the predicate?

- [ ] A. ![](<../Source/1.3.1. Truth Sets of Predicates/Images/q-171536-a-4.png>)
- [ ] B. ![](<../Source/1.3.1. Truth Sets of Predicates/Images/q-171536-a-5.png>)
- [ ] C. ![](<../Source/1.3.1. Truth Sets of Predicates/Images/q-171536-a-1.png>)
- [ ] D. ![](<../Source/1.3.1. Truth Sets of Predicates/Images/q-171536-a-3.png>)
- [ ] E. ![](<../Source/1.3.1. Truth Sets of Predicates/Images/q-171536-a-2.png>)

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
