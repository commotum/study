# The Extended Euclidean Algorithm

<!--
lesson-id: 2677
topic-code: MOP.4.1.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Applying the Extended Euclidean Algorithm When the Two Numbers are Not Coprime](#applying-the-extended-euclidean-algorithm-when-the-two-numbers-are-not-coprime)
- [Applying the Extended Euclidean Algorithm When the Two Numbers are Coprime](#applying-the-extended-euclidean-algorithm-when-the-two-numbers-are-coprime)
- [Final Remarks](#final-remarks)

## Prerequisites

- [The Euclidean Algorithm](<4.1.4. The Euclidean Algorithm.md>)

---

<a id="introduction"></a>
## Introduction

For any two integers $a$ and $b$, there exist integers $u$ and $v$ such that

$$
au+bv= \textrm{gcd}(a,b)
$$

This is known as **Bézout's identity**.

To find the integers $u$ and $v$, we can use the **extended Euclidean algorithm**, which involves the following three steps:

1. Carry out the Euclidean algorithm to find $\textrm{gcd}(a,b)$.
2. Solve for the remainder at each step, up to (and including) the step where the greatest common divisor appeared as a remainder.
3. Back-substitute the remainders to reach the equation $au+bv= \textrm{gcd}(a,b)$.

For example, let's use the extended Euclidean algorithm to find integers $u$ and $v$ such that

$$
48u+18v= \textrm{gcd}(48,18)
$$

- **Step 1:** First, we carry out the Euclidean algorithm to find $\textrm{gcd}(48,18)$.
$48|=\begin{vmatrix}18 \cdot 2 & +\end{vmatrix}12; ↙\begin{vmatrix}↙ \\ 18|=\end{vmatrix}12 \cdot 1\begin{vmatrix}+ & 6 \\ ↙ & ↙ \\ 12|= & 6 \cdot 2\end{vmatrix}+ \mid 0$
So, we find that $\text{gcd}(48,18) =6$.
- **Step 2:** We solve for the remainder (the rightmost term) at each step, up to (and including) the step where the greatest common divisor appeared as an integer.
$12|= 48 - 18 \cdot 2; 6|= 18 - 12$
- **Step 3:** Finally, we back-substitute the remainders to reach the equation $48u+18v= 6$.
$6|= 18 - 12; = 18 - (48 - 18 \cdot 2); = 18 - 48 + 18 \cdot 2; = (18 + 18 \cdot 2) - 48; = 18 \cdot 3 - 48; = 48 \cdot (-1) + 18 \cdot (3)$

Therefore, $u=-1$ and $v=3$.

---

<a id="applying-the-extended-euclidean-algorithm-when-the-two-numbers-are-not-coprime"></a>
## Applying the Extended Euclidean Algorithm When the Two Numbers are Not Coprime

**Example:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $30u + 8v = \text{gcd}(30,8)$. What is the value of $u+v$?

**Explanation**

We will use the extended Euclidean algorithm. First, we apply the forward reduction:

$$
\begin{aligned}
30 &= \begin{vmatrix}8 \cdot 3 & +\end{vmatrix}6 \\
↙\begin{vmatrix}↙ \\ 8 &= \end{vmatrix}6 \cdot 1\begin{vmatrix}+ & 2 \\ ↙ & ↙ \\ 6|= & 2 \cdot 3\end{vmatrix}+ \mid 0
\end{aligned}
$$

As a result,

$$
\text{gcd}(30,8) =2
$$

Solving for the remainders (the rightmost terms) in the equations above, up to (and including) the equation where the greatest common divisor appeared as a remainder, we get

$$
\begin{aligned}
6 &= 30 - 8 \cdot 3 \\
2 &= 8 - 6
\end{aligned}
$$

Then, we back-substitute:

$$
\begin{aligned}
2 &= 8 - 6 \\
&= 8 - (30 - 8 \cdot 3) \\
&= 8 \cdot 4 - 30 \\
&= 30 \cdot (-1) + 8 \cdot 4
\end{aligned}
$$

Therefore, $u=-1$ and $v=4$.

Finally,

$$
u+v=(-1)+4=3
$$

---

**Question 1:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $56u + 21v = gcd(56, 21)$. What is the value of $u + v$?

- [ ] A. $4$
- [ ] B. $3$
- [ ] C. $2$
- [ ] D. $54$
- [ ] E. $-2$

---

**Question 2:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $28u + 16v = gcd(28, 16)$. What is the value of $u + v$?

- [ ] A. $3$
- [ ] B. $-2$
- [ ] C. $-1$
- [ ] D. $1$
- [ ] E. $2$

---

<a id="applying-the-extended-euclidean-algorithm-when-the-two-numbers-are-coprime"></a>
## Applying the Extended Euclidean Algorithm When the Two Numbers are Coprime

**Example:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $36u + 19v = \text{gcd}(36,19)$. What is the value of $u+v$?

**Explanation**

We will use the extended Euclidean algorithm. First, we apply the forward reduction:

$$
\begin{aligned}
36 &= \begin{vmatrix}19 \cdot 1 & +\end{vmatrix}17 \\
↙\begin{vmatrix}↙ \\ 19 &= \end{vmatrix}17 \cdot 1\begin{vmatrix}+ & 2 \\ ↙ & ↙ \\ 17|= & 2 \cdot 8\end{vmatrix}+ \mid 1
\end{aligned}
$$

As a result,

$$
\text{gcd}(19,36) =1
$$

Solving for the remainders (the rightmost terms) in the equations above, up to (and including) the equation where the greatest common divisor appeared as a remainder, we get

$$
\begin{aligned}
17 &= 36 - 19 \cdot 1 \\
2 &= 19 - 17 \\
1 &= 17 - 2 \cdot 8
\end{aligned}
$$

Then, we back-substitute:

$$
\begin{aligned}
1 &= 17 - 2 \cdot 8 \\
&= 17 - (19 - 17) \cdot 8 \\
&= 17 \cdot 9 - 19 \cdot 8 \\
&= [36 - 19] \cdot 9 - 19 \cdot 8 \\
&= 36 \cdot 9 - 19 \cdot 17 \\
&= 36 \cdot 9 + 19 \cdot (-17)
\end{aligned}
$$

Therefore, $u=9,v=-17$, and we can write

$$
1 = 36\cdot \bbox[2pt, lightgray]{9} + 19\cdot (\bbox[2pt, lightgray]{-17})
$$

Finally,

$$
u+v=9+(-17)=-8
$$

---

**Question 3:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $43u + 17v = gcd(43, 17)$. What is the value of $u + v$?

- [ ] A. $2$
- [ ] B. $4$
- [ ] C. $1$
- [ ] D. $-3$
- [ ] E. $-2$

---

**Question 4:** Using the extended Euclidean algorithm, compute integers $u$ and $v$ such that $53u + 12v = gcd(53, 12)$. What is the value of $u + v$?

- [ ] A. $-22$
- [ ] B. $-17$
- [ ] C. $-27$
- [ ] D. $27$
- [ ] E. $36$

---

<a id="final-remarks"></a>
## Final Remarks

Throughout this lesson we've made use of **Bézout's identity**, which states that for any two integers $a$ and $b$, there exist integers $u$ and $v$ such that

$$
au+bv= \textrm{gcd}(a,b)
$$

It can also be shown that any integer combination of $a$ and $b$ is also a multiple of $\textrm{gcd}(a,b)$. In other words, for *any* integers $u'$ and $v'$, we have

$$
\textrm{gcd}(a,b) \mid (au'+bv')
$$

Moreover, the set of integers
$\big\{au'+bv' \,:\, u', v' \in\mathbb Z \big \}$
contains *all* the multiples of $\textrm{gcd}(a,b)$.

We'll make use of these results in future lessons.

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
