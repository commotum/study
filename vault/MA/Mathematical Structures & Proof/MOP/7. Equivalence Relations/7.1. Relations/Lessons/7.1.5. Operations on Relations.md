# Operations on Relations

<!--
lesson-id: 4832
topic-code: MOP.7.1.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding Complements, Unions, and Intersections of Relations](#finding-complements-unions-and-intersections-of-relations)
- [Inverse Relations](#inverse-relations)
- [Finding the Inverse Relation](#finding-the-inverse-relation)
- [Composition of Relations](#composition-of-relations)
- [Finding a Composition of Relations](#finding-a-composition-of-relations)

## Prerequisites

- [The Difference of Sets](<../../../2. Set Theory/2.3. Set Operations/Lessons/2.3.1. The Difference of Sets.md>)
- [Graphical Representations of Relations](<7.1.4. Graphical Representations of Relations.md>)

---

<a id="introduction"></a>
## Introduction

Since relations are simply sets of ordered pairs, all the operations defined on sets, such as complement, union, and intersection, apply to relations.

The **complement** of a relation $R$ defined over the sets $A$ and $B$ is the subset of the Cartesian product $A \times B$ containing all pairs that do not belong to $R{:}$

$$
\require{cancel} \overline{R} = \big\{(x,y) \in A \times B \::\: x \cancel{R} y \big\}
$$

For example, consider the relation

$$
R = \big\{(1, 1), (1, 3), (2, 1), (2, 2), (3,3) \big\}
$$

defined on

$$
A = \{1,2,3 \}
$$

Let's write out the elements of $A^2$, highlighting the pairs that belong to $R{:}$

$$
\begin{bmatrix}(1, 1) & (1, 2) & (1, 3) \\ (2, 1) & (2, 2) & (2, 3) \\ (3, 1) & (3, 2) & (3, 3)\end{bmatrix}
$$

All the remaining pairs belong to the complement of $R{:}$

$$
\overline{R} = \big\{(1,2), (2,3), (3,1), (3,2) \big\}
$$

The intersection and union are even more straightforward.

The **intersection** of the relations $R$ and $S$, each defined over the sets $A$ and $B$, is a subset of the Cartesian product $A \times B$ that consists of all the pairs that belong to $R$ *and* $S{:}$

$$
R \cap S = \big\{(x,y) \in A \times B \,:\, x\:R\:y \:\:\textrm{and}\:\: x\:S\:y \big\}
$$

In other words, the intersection contains only the pairs that belong to both relations.

The **union** of the relations $R$ and $S$, each defined over the sets $A$ and $B$, is a subset of the Cartesian product $A \times B$ that consists of all the pairs that belong to $R$ *or* $S{:}$

$$
R \cup S = \big\{(x,y) \in A \times B \,:\, x\:R\:y \:\:\textrm{or}\:\: x\:S\:y \big\}
$$

In other words, the union contains all the pairs that belong to one of the relations.

Let's see some examples.

---

<a id="finding-complements-unions-and-intersections-of-relations"></a>
## Finding Complements, Unions, and Intersections of Relations

**Example:** Let $A = \{2, 3, 4\}$ and $B=\{3, 4\}$. Consider the relation $R$ that expresses "increased by $1$ equals to" on $A\times B$, and the relation $S$ that expresses "the sum is divisible by $3$" on $A\times B$. Express $R \cup S$ as a list of ordered pairs.

**Explanation**

The *union* of the relations $R$ and $S$, each defined over the sets $A$ and $B$, is a subset of the Cartesian product $A \times B$ that consists of all the pairs that belong to $R$ *or* $S{:}$

$$
R \cup S = \big\{(x,y) \in A \times B \,:\, x\:R\:y \:\:\textrm{or}\:\: x\:S\:y \big\}
$$

**Method 1**

- The ordered pairs that belong to $R$ are
$(2,3), \quad (3,4)$.
- The ordered pairs that belong to $S$ are
$(2,4), \quad (3,3)$.

So, the pairs that lie in at least one of the sets are
$(2,3), \quad (2,4), \quad (3,3), \quad (3,4)$.

**Method 2**

The relation $R \cup S$ contains the pairs $(x,y) \in A\times B$ such that

$$
x+1=y
$$

or $3\, \mid \, (x+y)$. The pairs that satisfy this condition are the following:

$$
R \cup S = \big\{(2,3), (2,4), (3,3), (3,4) \big\}
$$

---

**Question 1**

Consider the following relation defined on the set $A = {1, 2, 3}$.

$R = {(1, 1), (1, 3), (2, 1), (2, 2), (2, 3), (3, 3)}$
Express the complement of $R$ as a list of ordered pairs.

$R^(―) = {(1, 2), (3$,
$\underline{\hspace{1.5cm}}$ $), ($
$\underline{\hspace{1.5cm}}$ $, 2)}$

---

**Question 2**

Let $A = {2, 3, 8, 9}$. Consider the relation $R$ that expresses "is less than" ($<$) on $A$, and the relation $S$ that expresses "has the same parity" on $A$.

The relation $R∩S$ contains the pairs $(x, y) ∈ A^{2}$ such that $x < y$, $\underline{\hspace{2cm}}$ $x$ and $y$ have the same parity. Thus, we have
$8$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] and
- [ ] or
- [ ] not

#### Select 2

- [ ] [MATH: 8]
- [ ] [MATH: 2]
- [ ] [MATH: 9]
- [ ] [MATH: 3]

#### Select 3

- [ ] [MATH: 8]
- [ ] [MATH: 9]
- [ ] [MATH: 2]
- [ ] [MATH: 3]

#### Select 4

- [ ] [MATH: 3]
- [ ] [MATH: 8]
- [ ] [MATH: 2]
- [ ] [MATH: 9]

---

<a id="inverse-relations"></a>
## Inverse Relations

For a relation $R$ defined over $A$ and $B$, the **inverse relation** $R^{-1}$ is the relation defined over $B$ and $A$ that contains all the pairs from $R$ where the first and second components are swapped:

$$
\begin{aligned}
R^{-1} &= {(y, x) ∈ B \cdot A: xRy}
\end{aligned}
$$

Since $x$ and $y$ are simply labels, we often rewrite $x\to y$ and $y\to x$ writing out our definition of $R^{-1}$, as follows:

$$
\begin{aligned}
R^{-1} &= {(x, y) ∈ B \cdot A: yRx}
\end{aligned}
$$

Either of the two definitions above is valid.

For example, consider the relation $R$ on $\mathbb{Z}$ defined by $x \: R \: y$ if and only if $x \gt y$.

$$
R = \big\{(x,y) \in \mathbb{Z}^2 \:: \: x \gt y \big\}
$$

According to the definition, to express the inverse $R^{-1}$, we need to swap the variables $x$ and $y$ in the condition:

$$
x
$$

Therefore, we obtain

$$
R^{-1} = {(x, y) ∈ Z^{2}: y > x}
$$

---

<a id="finding-the-inverse-relation"></a>
## Finding the Inverse Relation

**Example:** Consider the relation $R$ defined over the sets $X= \{-2,-1,0,1, 2\}$ and $Y = \{1, 2, 3, 4, 5\}$.
$R = \big\{(-2, 5), (-1, 4), (0,3), (1, 2), (2,1) \big\}$
Which of the following pairs belong to the inverse relation $R^{-1}$?

1. $(0,4)$
2. $(4, -1)$
3. $(1, 5)$

**Explanation**

For a relation $R$ defined over $A$ and $B$, the *inverse relation* $R^{-1}$ is the relation defined over $B$ and $A$ containing all pairs from $R$ where the first and second components are swapped:

$$
R^{-1} = \big\{(y,x) \in B \times A \:: \: (x,y) \in R\big\}
$$

In our case, $R$ is the set of ordered pairs $(x,y) \in X \times Y$.

By swapping the first and second components of each pair from $R$, we obtain

$$
R^{-1} = \big\{(5, -2), (4, -1), (3, 0), (2, 1), (1, 2) \big\}
$$

Therefore, among the given options, the only pair that belongs to $R^{-1}$ is $(4, -1)$.

---

**Question 3:** Consider the relation $R$ defined over the sets $X = {1, 2, 3, 4}$ and $Y = {♡, ♣, △}$.
$R = {(1, ♣), (2, ♡), (2, △), (2, ♣), (4, ♣)}$
Which of the following pairs belong to the inverse relation $R^{-1}$?

- [ ] A. $(♡, 3)$
- [ ] B. $(3, ♡)$
- [ ] C. $(△, 3)$
- [ ] D. $(♣, 2)$
- [ ] E. $(2, ♣)$

---

**Question 4**

Consider the relation $R$ on $Z$ defined by $xRy$ if and only if $x \le y$. Express the inverse of $R$ in set-builder notation.

$Z^{2}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: Z^{2}]
- [ ] [MATH: Z|]
- [ ] [MATH: Z^{− 2}]
- [ ] [MATH: 2Z|]

#### Select 2

- [ ] [MATH: y ≠ x]
- [ ] [MATH: y ≥ x]
- [ ] [MATH: y < x]
- [ ] [MATH: y > x]
- [ ] [MATH: y ≤ x]

---

<a id="composition-of-relations"></a>
## Composition of Relations

For a relation $R$ defined over the sets $X$ and $Y$, and a relation $S$ defined over the sets $Y$ and $Z$, the **composition** $S \circ R$ is given by

$$
S \circ R = \big\{(x,z) \in X \times Z \:: \: \exists y \in Y, \: x \: R \: y \: \land \: y \: S \: z \big\}
$$

For example, consider the following relations defined on

$$
X= \{1,2,3 \}
$$

$$
R = {(2, 2), (2, 3), (3, 3)}, S = {(1, 1), (2, 1), (2, 3)}
$$

Let's express the composition $S \circ R$ as a list of ordered pairs.

Since our relations are defined on relatively small sets, we can visualize them using the following mapping diagrams, where we match the elements of the two inner sets by the dashed lines:

![](<../Source/7.1.5. Operations on Relations/Images/a6b397cdaeaa348c1d32d122e96b32d8.png>)

An ordered pair belongs to the composition $S \circ R$ if a path starts from the first component (on the left-hand side) and ends at the second component (on the right-hand side).

In our example, these complete paths are as follows:

$$
1⟶\cdot; 2⟶2- - -2⟶1Therefore,(2, 1) ∈ S∘R; 2⟶2- - -2⟶3Therefore,(2, 3) ∈ S∘R; 2⟶3- - -3⟶\cdot; 3⟶3- - -3⟶\cdot
$$

So, two complete paths start on the left-hand side and end on the right-hand side. They correspond to the following ordered pairs:

$$
S \circ R = \big\{(2,1), (2,3) \big\}
$$

---

<a id="finding-a-composition-of-relations"></a>
## Finding a Composition of Relations

**Example:** Consider the relation $R$ defined over $X=\{a, b, c, d, e\}$ and $Y=\{0,1\}$, and the relation $S$ defined over $Y=\{0,1\}$ and $Z=\{4, 7, 10\}$, given below.
$R|= {(a, 0), (a, 1), (c, 0), (d, 1), (e, 0), (e, 1)}; S|= {(1, 4), (1, 7), (1, 10)}$
Express the composition $S \circ R$ as a list of ordered pairs.

**Explanation**

For a relation $R$ defined over the sets $X$ and $Y$, and a relation $S$ defined over the sets $Y$ and $Z$, the *composition* $S \circ R$ is given by

$$
S \circ R = \big\{(x,z) \in X \times Z \:: \: \exists y \in Y, \: x \: R \: y \: \land \: y \: S \: z \big\}
$$

Since our relations are defined on relatively small sets, we can visualize them using the following mapping diagrams, where we match the elements of the two inner sets by the dashed lines:

![](<../Source/7.1.5. Operations on Relations/Images/252995ae0f0217b3a5146c975a5da5e2.png>)

An ordered pair belongs to the composition $S \circ R$ if there is a path starting from the first component (on the left-hand side) and ending at the second component (on the right-hand side).

In our example, these paths are the following:

$$
a⟶0- - -0⟶\cdot; a⟶1- - -1⟶4\begin{vmatrix}Therefore,(a, 4) ∈ S∘R \\ a⟶1- - -1⟶7\end{vmatrix}Therefore,(a, 7) ∈ S∘R; a⟶1- - -1⟶10\begin{vmatrix}Therefore,(a, 10) ∈ S∘R \\ c⟶0- - -0⟶\cdot \\ d⟶1- - -1⟶4\end{vmatrix}Therefore,(d, 4) ∈ S∘R; d⟶1- - -1⟶7\begin{vmatrix}Therefore,(d, 7) ∈ S∘R \\ d⟶1- - -1⟶10\end{vmatrix}Therefore,(d, 10) ∈ S∘R; e⟶0- - -0⟶\cdot; e⟶1- - -1⟶4\begin{vmatrix}Therefore,(e, 4) ∈ S∘R \\ e⟶1- - -1⟶7\end{vmatrix}Therefore,(e, 7) ∈ S∘R; e⟶1- - -1⟶10 \mid Therefore,(e, 10) ∈ S∘R
$$

So, nine complete paths start on the left-hand side and end on the right-hand side. They correspond to the following ordered pairs:

$$
S \circ R = \big\{(a,4), (a,7), (a,10), (d,4), (d,7), (d,10), (e,4), (e,7), (e,10)\big\}
$$

---

**Question 5**

Consider the following relations defined on $R$.
$R|= {(x, y) ∈ R^{2}: y = \ln x}; S|= {(x, y) ∈ R^{2}: x = y^{2}}$

Which of the following pairs are elements of the relation $S∘R$?

1. $(1, 0)$
2. $(e^{2}, \sqrt{2})$
3. $(e^{2},-\sqrt{2})$
4. $(e^{-1},-1)$

- [ ] A. I and IV only
- [ ] B. I and II only
- [ ] C. II and III only
- [ ] D. I, II, and III only
- [ ] E. I, II, III, IV

---

**Question 6**

Consider the following relations defined on $X = {1, 2, 3, 4}$.
$R|= {(1, 3), (2, 1), (2, 3), (3, 4)}; S|= {(2, 1), (3, 1), (3, 2)}$
Express the composition $S∘R$ as a list of ordered pairs.$S∘R = {(1, 1), (1$,
$\underline{\hspace{1.5cm}}$ $), ($
$\underline{\hspace{1.5cm}}$ $, 1), ($
$\underline{\hspace{1.5cm}}$ $, 2)}$

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
