# Disproving Implications

<!--
lesson-id: 4927
topic-code: MOP.3.1.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Disproving a Divisibility Statement](#disproving-a-divisibility-statement)
- [Stating the Full Counterexample](#stating-the-full-counterexample)
- [Stating Counterexample Conditions](#stating-counterexample-conditions)
- [Disproving Divisibility Statements](#disproving-divisibility-statements)
- [Disproving Congruences](#disproving-congruences)

## Prerequisites

- [The Division Properties of Modular Arithmetic](<../../../6. Congruence of Integers/6.1. Modular Congruence/Lessons/6.1.5. The Division Properties of Modular Arithmetic.md>)
- [Disproving Universal Statements](<3.1.5. Disproving Universal Statements.md>)

---

<a id="introduction"></a>
## Introduction

In this lesson, we'll learn how to disprove conditional statements.

Suppose we wish to disprove a conditional statement of the following form:
$\forall x \in \mathbb{Z}, \: P(x) \Rightarrow Q(x)$

To disprove this statement, we must show its negation is true.

The negation of our statement is

$$
¬(∀x ∈ Z, P⇒Q) \mid ≡∃x ∈ Z, ¬(P⇒Q); ≡∃x ∈ Z, ¬(¬P∨Q); ≡∃x ∈ Z, P∧¬Q
$$

Therefore, to disprove the implication $P\Rightarrow Q$, it's sufficient to find one value of the variable such that $P$ and $\lnot Q$ are both true. This value of $x$ is a *counterexample* that disproves the original implication.

Let's see an example.

---

<a id="disproving-a-divisibility-statement"></a>
## Disproving a Divisibility Statement

Suppose we wish to disprove the following statement:

> *For any $a \in \mathbb{Z}$, if $2 \mid a$, then $4 \mid a$*

By denoting the predicates $P$ and $Q$ as

- $P(a){:} \quad 2 \mid a$
- $Q(a){:} \quad 4 \mid a$

our statement can be written as
$\forall a \in \mathbb{Z}, \: P \Rightarrow Q$.

We can disprove this statement by finding a counterexample. In other words, we need to find a value of $a$ such that its negation is true.

The negation of our statement is

$$
¬(∀a ∈ Z, P⇒Q) \mid ≡∃a ∈ Z, ¬(P⇒Q); ≡∃a ∈ Z, ¬(¬P∨Q); ≡∃a ∈ Z, P∧¬Q; ≡∃a ∈ Z, (2∣a)∧(4∤a)
$$

So, we begin as follows:

> *To disprove a universal statement, it's sufficient to find a counterexample. In other words, we need to show that there exists an integer $a$ such that*
> $2 \mid a \qquad\textrm{and}\qquad 4 \not\mid a$.

Finding a counterexample is sometimes a nontrivial task, but we only need one. In this case, substituting $a=2$ suffices.

> *Let's choose $a = 2$. Then, we have that*
> $2 \mid 2 \qquad\textrm{and}\qquad 4 \not\mid 2$.

Finally, we state our conclusion:

> *So, $a=2$ is our counterexample. Therefore, the given statement is false.*

Now that we've figured out the details, let's write down the full argument.

---

<a id="stating-the-full-counterexample"></a>
## Stating the Full Counterexample

Disprove the following statement:

> *For any $a \in \mathbb{Z}$, if $2 \mid a$, then $4 \mid a$.*

Counterexample:

> *To disprove a universal statement, it's sufficient to find a counterexample. In other words, we need to show that there exists an integer $a$ such that*
> $2 \mid a \qquad\textrm{and}\qquad 4 \not\mid a$.
> *Let's choose $a = 2$. Then, we have that*
> $2 \mid 2 \qquad\textrm{and}\qquad 4 \not\mid 2$.
> *So, $a=2$ is our counterexample. Therefore, the given statement is false.*

---

<a id="stating-counterexample-conditions"></a>
## Stating Counterexample Conditions

**Example:** Consider the following false statement.

> If $2a \equiv 2b \: (\textrm{mod}\:4)$, then $a \equiv b \: (\textrm{mod}\:4)$ for any $a,b \in \mathbb{Z}$.

A counterexample for the above statement would be a pair of integers $a$ and $b$ such that
$\bbox[3px, border: 2px solid black]{\phantom{RRR}}\, (\textrm{mod}\:4) \qquad\textrm{and}\qquad \bbox[3px, border: 2px solid black]{\phantom{RRR}}\, (\textrm{mod}\:4)$.

Find the missing expressions.

**Explanation**

By denoting $P(a)$ and $Q(a)$ as

- $P(a,b){:} \quad 2a \equiv 2b \: (\textrm{mod}\:4)$
- $Q(a,b){:} \quad a \equiv b \: (\textrm{mod}\:4)$

our statement can be written as

$$
\forall a,b \in \mathbb{Z}, \: P \Rightarrow Q
$$

Since this statement is false, its negation is true.

A *counterexample* is a particular case that disproves the statement. Equivalently, it's a case that proves the negation is true.

The negation of our statement is

$$
¬(∀a, b ∈ Z, P⇒Q) \mid ≡∃a, b ∈ Z, ¬(P⇒Q); ≡∃a, b ∈ Z, ¬(¬P∨Q); ≡∃a, b ∈ Z, P∧¬Q; ≡∃a, b ∈ Z, (2a≡2b(mod4))∧(a≢b(mod4))
$$

Therefore, we have the following:

> A counterexample for the above statement would be a pair of integers $a$ and $b$ such that
> $\bbox[3pt,Gainsboro]{\color{blue}2a \equiv 2b} \: (\textrm{mod}\:4) \qquad\textrm{and}\qquad \bbox[3pt,Gainsboro]{\color{blue}a \not\equiv b} \: (\textrm{mod}\:4)$.

---

**Question 1**

Consider the following false statement.

> If $a \ge 7$, then $a \ge 9$ for any $a ∈ Z$.

A counterexample for the above statement would be an integer $a$ such that
$a < 7$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: a < 7]
- [ ] [MATH: a ≥ 9]
- [ ] [MATH: a ≥ 7]

#### Select 2

- [ ] [MATH: a < 7]
- [ ] [MATH: a ≥ 9]
- [ ] [MATH: a < 9]

---

**Question 2**

Consider the following false statement.

> If $7∣a$, then $21∣a$ for any $a ∈ Z$.

A counterexample for the above statement would be an integer $a$ such that
$7∤a$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: 7∤a]
- [ ] [MATH: 21∣a]
- [ ] [MATH: 7∣a]

#### Select 2

- [ ] [MATH: 7∤a]
- [ ] [MATH: 21∣a]
- [ ] [MATH: 21∤a]

---

<a id="disproving-divisibility-statements"></a>
## Disproving Divisibility Statements

**Example:** Disprove the following statement:

> For any $a \in \mathbb{Z}$, if $4 \mid a$ and $6 \mid a$, then $24 \mid a$.

**Explanation**

By denoting $P(a)$, $Q(a)$, and $R(a)$ as

- $P(a){:} \quad 4 \mid a$
- $Q(a){:} \quad 6 \mid a$
- $R(a){:} \quad 24 \mid a$

our statement can be written as
$\forall a \in \mathbb{Z}, \:(P \land Q) \Rightarrow R$.

We want to disprove this statement. In other words, we must show that its negation is true:

$$
¬(∀a ∈ Z, (P∧Q)⇒R) \mid ≡∃a ∈ Z, ¬((P∧Q)⇒R); ≡∃a ∈ Z, ¬(¬(P∧Q)∨R); ≡∃a ∈ Z, (P∧Q)∧¬R; ≡∃a ∈ Z, P∧Q∧¬R; ≡∃a ∈ Z, (4∣a)∧(6∣a)∧(24∤a)
$$

So, we begin as follows:

> To disprove a universal statement, it's sufficient to find a counterexample.
> In other words, we need to show that there exists an integer $a$ such that
> $4 \mid a \qquad\textrm{and}\qquad 6 \mid a \qquad\textrm{and}\qquad 24 \not\mid a$.

Finding a counterexample is sometimes a nontrivial task, but we only need one. In this case, substituting $a=12$ suffices.

> Let's choose $a =12$.
> Then, we have that
> $4 \mid 12 \qquad\textrm{and}\qquad 6 \mid 12 \qquad\textrm{and}\qquad 24 \not\mid 12$.

Finally, we state our conclusion:

> So, $a=12$ is our counterexample. Therefore, the given statement is false.

---

**Question 3**

Disprove the following statement:

> For any $a ∈ Z$, if $7∣a$, then $21∣a$.

To $\underline{\hspace{2cm}}$ a universal statement, it's sufficient to find $\underline{\hspace{2cm}}$.
In other words, we need to show that $\underline{\hspace{2cm}}$ $7∤a$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] disprove
- [ ] prove

#### Select 2

- [ ] an example
- [ ] a counterexample

#### Select 3

- [ ] there exists an integer [MATH: a] such that
- [ ] for all integers [MATH: a,]

#### Select 4

- [ ] [MATH: 7∤a]
- [ ] [MATH: 21∣a]
- [ ] [MATH: 7∣a]

#### Select 5

- [ ] [MATH: 21∤a]
- [ ] [MATH: 21∣a]
- [ ] [MATH: 7∤a]

---

**Question 4**

Disprove the following statement:

> For any $a ∈ Z$, if $6∣4a$, then $6∣a$.

To $\underline{\hspace{2cm}}$ a universal statement, it's sufficient to find $\underline{\hspace{2cm}}$.
In other words, we need to show that $\underline{\hspace{2cm}}$ $6∣a$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] prove
- [ ] disprove

#### Select 2

- [ ] a counterexample
- [ ] an example

#### Select 3

- [ ] for all integers [MATH: a,]
- [ ] there exists an integer [MATH: a] such that

#### Select 4

- [ ] [MATH: 6∣a]
- [ ] [MATH: 6∤4a]
- [ ] [MATH: 6∣4a]

#### Select 5

- [ ] [MATH: 6∤4a]
- [ ] [MATH: 6∣a]
- [ ] [MATH: 6∤a]

---

<a id="disproving-congruences"></a>
## Disproving Congruences

**Example:** Disprove the following statement:

> For any $a,b \in \mathbb{Z}$, if $3a \equiv 3b \: (\textrm{mod}\:6)$, then $a \equiv b \: (\textrm{mod}\:6)$.

**Explanation**

By denoting $P(a,b)$ and $Q(a,b)$ as

- $P(a,b){:} \quad 3a \equiv 3b \: (\textrm{mod}\:6)$
- $Q(a,b){:} \quad a \equiv b \: (\textrm{mod}\:6)$

our statement can be written as
$\forall a,b \in \mathbb{Z}, \: P \Rightarrow Q$.
We want to disprove this statement. In other words, we must show that its negation is true:
$¬(∀a, b ∈ Z, P⇒Q) \mid ≡∃a, b ∈ Z, ¬(P⇒Q); ≡∃a, b ∈ Z, ¬(¬P∨Q); ≡∃a, b ∈ Z, P∧¬Q; ≡∃a, b ∈ Z, (3a≡3b(mod6))∧(a≢b(mod6))$
So, we begin as follows:

> To disprove a universal statement, it's sufficient to find a counterexample.
> In other words, we need to show that there exist integers $a$ and $b$ such that
> $3a \equiv 3b \: (\textrm{mod}\:6) \qquad\textrm{and}\qquad a \not\equiv b\: (\textrm{mod}\:6)$.

Finding a counterexample is sometimes a nontrivial task, but we only need one. In this case, substituting

$$
(a,b) =(0,2)
$$

suffices.

> Let's choose
> $a = 0, \qquad b = 2$.
> Then, $3a=0$ and $3b=6$.

To check that this is a required counterexample, we proceed as follows:

> Hence, we have that
> $\underbrace{0}_{3a} \equiv \underbrace{6}_{3b} \: (\textrm{mod}\:6) \qquad\textrm{and}\qquad \underbrace{0}_{a} \not\equiv \underbrace{2}_{b} \: (\textrm{mod}\:6)$.

Finally, we state our conclusion:

> So, the pair $a=0$ and $b=2$ is our counterexample. Therefore, the given statement is false.

---

**Question 5**

Disprove the following statement:

> For any $a, b ∈ Z$, if $5a≡5b(mod20)$, then $a≡b(mod20)$.

To $\underline{\hspace{2cm}}$ a universal statement, it's sufficient to find $\underline{\hspace{2cm}}$.
In other words, we need to show that $\underline{\hspace{2cm}}$ $a≡b$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] disprove
- [ ] prove

#### Select 2

- [ ] a counterexample
- [ ] an example

#### Select 3

- [ ] for all integers [MATH: a] and [MATH: b]
- [ ] there exist integers [MATH: a] and [MATH: b] such that

#### Select 4

- [ ] [MATH: a≡b]
- [ ] [MATH: 5a≡5b]
- [ ] [MATH: 5a≢5b]

#### Select 5

- [ ] [MATH: a≡b]
- [ ] [MATH: a≢b]
- [ ] [MATH: 5a≢5b]

---

**Question 6**

Disprove the following statement:

> For any $a, b ∈ Z$, if $a≡b(mod20)$, then $a≡b(mod60)$.

To $\underline{\hspace{2cm}}$ a universal statement, it's sufficient to find $\underline{\hspace{2cm}}$.
In other words, we need to show that $\underline{\hspace{2cm}}$ $a≡b(mod60)$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] disprove
- [ ] prove

#### Select 2

- [ ] a counterexample
- [ ] an example

#### Select 3

- [ ] there exist integers [MATH: a] and [MATH: b] such that
- [ ] for all integers [MATH: a] and [MATH: b]

#### Select 4

- [ ] [MATH: a≡b(mod60)]
- [ ] [MATH: a≡b(mod20)]
- [ ] [MATH: a≢b(mod20)]

#### Select 5

- [ ] [MATH: a≡b(mod60)]
- [ ] [MATH: a≢b(mod20)]
- [ ] [MATH: a≢b(mod60)]

```update-progress
```

[[MA/MOP/Home|Home]]
[[MA/MOP/0. Table of Contents/TOC|Table of Contents]]
