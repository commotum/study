# Unit Conversions Using Units of Time

<!--
lesson-id: 3870
topic-code: HI1.3.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Converting From Hours to Minutes](#converting-from-hours-to-minutes)
- [Converting From Minutes to Seconds](#converting-from-minutes-to-seconds)
- [Using Reciprocals to Convert From Smaller Units to Larger Units](#using-reciprocals-to-convert-from-smaller-units-to-larger-units)
- [Converting From Smaller Units to Larger Units](#converting-from-smaller-units-to-larger-units)

## Prerequisites

- [Simplifying Rational Expressions](<../../../4. Radicals and Rationals/4.2. Rational Expressions/Lessons/4.2.2. Simplifying Rational Expressions.md>)
- [Reciprocals of Rational Numbers](<../../../../../Elementary-&-Middle-School-Mathematics/PAL/2. The Number System/2.2. Multiplication and Division of Rational Numbers/Lessons/2.2.6. Reciprocals of Rational Numbers.md>)
- [Units of Time](<../../../../../Elementary-&-Middle-School-Mathematics/4GM/7. Measurement & Data/7.1. Units/Lessons/7.1.3. Units of Time.md>)

---

<a id="introduction"></a>
## Introduction

The base unit of time is the "second." However, measuring time in seconds is not always the best option in real-world situations. It's often more convenient to write time in "minutes" or "hours."

We can use units to convert between all these different units of time. We can manipulate units the same way as variables, which often provides a convenient way to carry out unit conversions.

For example, let's convert $3$ hours into minutes. Let's start by creating a conversion table for time units.

| Name | Abbreviation | Conversion |
| --- | ---: | ---: |
| Second | $\textrm{s}$ | Base Unit |
| Minute | $\textrm{min}$ | $1\,\textrm{min} = 60\,\textrm{s}$ |
| Hour | $\textrm{h}$ | $1\,\textrm{h} = 60\,\textrm{min}$ |

Since we're converting from hours to minutes, we need a conversion between these two units. According to our table, there are $60$ minutes per hour. We can write this ratio as a fraction, as follows:

$$
\dfrac{60\,\textrm{min}}{\textrm{h}}
$$

This is our **conversion factor.** It's important to write the conversion factor with the units included.

Now, multiplying $3 \, \textrm{h}$ by our conversion factor, we get

$$
\begin{aligned}
3h &= 3h \cdot \frac{60min}{h} \\
&= 3h \cdot \frac{60min}{h} \\
&= 3 \cdot 60min \\
&= 180min
\end{aligned}
$$

Therefore, $3 \, \textrm{h}$ is equivalent to $180 \, \textrm{min}$.

Note the following:

- When multiplying by the conversion factor, the units we want to remove should *always* cancel, and the units we wish to keep should *always* remain. If this isn't the case, we've made a mistake somewhere.
- We can think of the conversion factor as equivalent to the number $1$, and multiplying a quantity by $1$ doesn't change it. Therefore, multiplying $3\,\textrm{h}$ by the conversion factor doesn't change the *size* of the quantity, though it does change the units.

Finally, notice that, in this example, we've converted from a larger unit of time $(\textrm{h})$ to a smaller one $(\textrm{min})$. We'll see how to use units to convert from smaller units of time to larger ones later in this lesson.

---

<a id="converting-from-hours-to-minutes"></a>
## Converting From Hours to Minutes

**Example:** What is $0.5 \,\textrm{h}$ expressed in $\textrm{min}$?

**Explanation**

There are $60\,\textrm{min}$ per $\,\textrm{h}$, which we can write as follows:

$$
\dfrac{60\,\textrm{min}}{\textrm{h}}
$$

This is our conversion factor.

Now, multiplying $0.5\,\textrm{h}$ by our conversion factor, we get

$$
\begin{aligned}
0.5h &= 0.5h \cdot \frac{60min}{h} \\
&= 0.5h \cdot \frac{60min}{h} \\
&= 0.5 \cdot 60min \\
&= 30min
\end{aligned}
$$

Therefore, $0.5\,\textrm{h}$ is equivalent to $30\,\textrm{min}$.

---

**Question 1**

> A calculator is required to answer this question.

What is $18h$ expressed in $min$?

- [ ] A. $540min$
- [ ] B. $300min$
- [ ] C. $64800min$
- [ ] D. $1080min$
- [ ] E. $2800min$

---

**Question 2**

> A calculator is required to answer this question.

$0.4h =$ $\underline{\hspace{1.5cm}}$ $min$

---

<a id="converting-from-minutes-to-seconds"></a>
## Converting From Minutes to Seconds

**Example:** What is $15 \, \textrm{min}$ expressed in $\textrm{s}$?

**Explanation**

There are $60\,\textrm{s}$ per $\textrm{min}$, which we can write as follows:

$$
\dfrac{60\,\textrm{s}}{\textrm{min}}
$$

This is our conversion factor.

Now, multiplying $15\,\textrm{min}$ by our conversion factor, we get

$$
\begin{aligned}
15min &= 15min \cdot \frac{60s}{min} \\
&= 15min \cdot \frac{60s}{min} \\
&= 15 \cdot 60s \\
&= 900s
\end{aligned}
$$

Therefore, $15\,\textrm{min}$ is equivalent to $900\,\textrm{s}$.

---

**Question 3**

> A calculator is required to answer this question.

What is $12min$ expressed in $s$?

- [ ] A. $60s$
- [ ] B. $360s$
- [ ] C. $720s$
- [ ] D. $120s$
- [ ] E. $1200s$

---

**Question 4**

> A calculator is required to answer this question.

$3.3min =$ $\underline{\hspace{1.5cm}}$ $s$

---

<a id="using-reciprocals-to-convert-from-smaller-units-to-larger-units"></a>
## Using Reciprocals to Convert From Smaller Units to Larger Units

We've seen how to use units to convert from larger units of time to smaller ones. Let's now learn how to convert from smaller units to larger ones.

For example, let's express $36\,\textrm{min}$ in hours.

First, we note that there are $60\,\textrm{min}$ per hour, which we can write as follows:

$$
\dfrac{60\,\textrm{min}}{\textrm{h}}
$$

However, since we're converting from a smaller unit of time $(\textrm{min})$ to a larger one $(\textrm{h})$, we need to multiply $36\,\textrm{min}$ by the *reciprocal* of the above:

$$
\dfrac{\textrm{h}}{60\,\textrm{min}}
$$

This is our conversion factor.

Now, multiplying $36\,\textrm{min}$ by our conversion factor, we get

$$
\begin{aligned}
36min &= 36min \cdot \frac{h}{60min} \\
&= 36min \cdot \frac{h}{60min} \\
&= 36 \cdot \frac{h}{60} \\
&= \frac{36}{60}h \\
&= 0.6h
\end{aligned}
$$

Therefore, $36\,\textrm{min}$ is equivalent to $0.6$ hours.

Note the following:

- When we say that hours are a "larger unit of time" than minutes, this simply means that $1\,\textrm{h}$ is a longer time duration than $1\,\textrm{min}$.
- If you forget to take the reciprocal to find the conversion factor, the units will not cancel when you multiply. This indicates that a mistake has occurred. In such cases, it's best to double-check that you're using the correct conversion factor.

---

<a id="converting-from-smaller-units-to-larger-units"></a>
## Converting From Smaller Units to Larger Units

**Example:** What is $3\,600\,\textrm{s}$ expressed in $\textrm{min}$?

**Explanation**

There are $60\,\textrm{s}$ per $\textrm{min}$, which we can write as follows:

$$
\dfrac{60\,\textrm{s}}{\textrm{min}}
$$

However, since we're converting from a smaller unit of time $(\textrm{s})$ to a larger one $(\textrm{min})$, we need to multiply $3\,600\,\textrm{s}$ by the reciprocal of the above:

$$
\dfrac{\textrm{min}}{60\,\textrm{s}}
$$

This is our conversion factor.

Now, multiplying $3\,600\,\textrm{s}$ by our conversion factor, we get

$$
\begin{aligned}
3600s &= 3600s \cdot \frac{min}{60s} \\
&= 3600s \cdot \frac{min}{60s} \\
&= 3600 \cdot \frac{min}{60} \\
&= \frac{3600}{60}min \\
&= 60min
\end{aligned}
$$

Therefore, $3\,600\,\textrm{s}$ is equivalent to $60\,\textrm{min}$.

---

**Question 5**

> A calculator is required to answer this question.

$4200s =$ $\underline{\hspace{1.5cm}}$ $min$

---

**Question 6**

> A calculator is required to answer this question.

$330min =$ $\underline{\hspace{1.5cm}}$ $h$

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Integrated-High-School-Mathematics/HI1/Home|Home]]
[[MA/Integrated-High-School-Mathematics/HI1/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
