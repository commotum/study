# Constructing Revenue, Cost, and Profit Functions

<!--
lesson-id: 3667
topic-code: HI2.3.2.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Constructing Revenue Functions](#constructing-revenue-functions)
- [Cost Functions](#cost-functions)
- [Constructing Cost Functions](#constructing-cost-functions)
- [Profit Functions](#profit-functions)
- [Constructing Profit Functions](#constructing-profit-functions)

## Prerequisites

- [Modeling With Linear Equations](<../../../../../Traditional-High-School-Mathematics/AG1/1. Equations & Inequalities/1.2. Modeling With Linear Equations/Lessons/1.2.2. Modeling With Linear Equations.md>)
- [Revenue, Cost, and Profit Functions](<3.2.4. Revenue, Cost, and Profit Functions.md>)

---

<a id="introduction"></a>
## Introduction

An important function that company managers and economists often track is the **price function** of a good, usually denoted $p(x)$. The price function gives the **price per unit** of selling $x$ units of a product.

When purchasing items in large quantities, there is often a discount which causes the price per unit to decrease as the number of units increases. This is one motivation behind introducing a price function.

For example, suppose that a textile factory manufactures pants. If the price per unit (in dollars) is given by

$$
p(x) = 500-x^2
$$

then the price for $10$ units is

$$
p(10) = 500-(10)^2 = \[math]400
$$

It's important to remember that this is the price *per unit*. The total amount that the company would charge for selling $10$ units is

$$
10\cdot p(10) = 10\cdot \[/math]400 = \[math]4\,000
$$

This example shows that the price function is closely connected to the revenue function $R(x)$. Typically, if a company sells $x$ units at a price $p(x)$, then the total revenue is given by

$$
\begin{aligned}
R(x) &= (number of units) \cdot (price per unit) \\
&= x \cdot p(x)
\end{aligned}
$$

So, in the case of the above example, the revenue function $R(x)$ is given by

$$
\begin{aligned}
R(x) &= x \cdot p(x) \\
&= x \cdot (500 - x^{2}) \\
&= 500x - x^{3}
\end{aligned}
$$

While the price of a good sometimes varies with $x$, other times it may be constant. In cases when the unit price is constant, you'll see the price per unit quoted as a single figure (usually in dollars).

---

<a id="constructing-revenue-functions"></a>
## Constructing Revenue Functions

**Example:** A company produces jeans. The revenue function, in dollars, of this company is given by $R(x) =5x(10-0.02x)$, where $x$ represents the number of units produced and sold per month. Calculate the price function $p(x)$.

**Explanation**

Given a price function $p(x)$, the revenue function is given by

$$
\begin{aligned}
R(x) &= (number of units) \cdot (price per unit) \\
&= x \cdot p(x)
\end{aligned}
$$

Therefore, the price function is

$$
p(x) = \dfrac{R(x)}{x}
$$

In our case, we have

$$
\begin{aligned}
p(x) &= (5x(10 - 0.02x))/(x) \\
&= (5x(10 - 0.02x))/(x) \\
&= 5(10 - 0.02x) \\
&= 50 - 0.1x
\end{aligned}
$$

---

**Question 1**

> A calculator is required to answer this question.

A company produces bicycles. The price per unit, in dollars, charged by the company is given by the price function $p(x) = 200 - 0.2x$, where $x$ represents the number of units produced and sold per month. Calculate the revenue function $R(x)$.

- [ ] A. $R(x) = 200x^{2} - 0.2x^{3}$
- [ ] B. $R(x) = 200x - 0.2x^{2}$
- [ ] C. $R(x) = 0.2 - \frac{200}{x}$
- [ ] D. $R(x) = \frac{200}{x} - 0.2$
- [ ] E. $R(x) = 0.2x^{2} - 200x$

---

**Question 2**

> A calculator is required to answer this question.

A company produces school furniture. The revenue function of this company for a particular type of desk, in dollars, is given by $R(x) = 80x - 3x^{2}$, where $x$ represents the number of units produced and sold per month. Calculate the price function $p(x)$.$p(x) =$ $\underline{\hspace{1.5cm}}$

---

<a id="cost-functions"></a>
## Cost Functions

We can construct a cost function $C(x)$ for a business using the following equation:

$$
C(x) = (\text{fixed cost}) + (\textrm{number of units})\cdot (\textrm{cost per unit})
$$

Let's break this equation down a bit:

- The **fixed cost** is the part of the cost function that does not change. Irrespective of how many units are produced, fixed costs are always the same. Fixed costs typically incorporate rent, utilities, communications, employee wages, taxes, etc.
- The term $(\textrm{number of units})\cdot (\textrm{cost per unit})$ represents the **variable cost.** This part of the cost function changes depending on how many units are produced.

For example, suppose that a company produces $x$ units of sweaters per month. Each unit costs $\[math]49$ to make, and the company's fixed costs are $\[/math]4\,500$ per month.

We can calculate the cost function $C(x)$ for this company as follows:

$$
\begin{aligned}
C(x) &= (fixed cost) + (number of units) \cdot (cost per unit) \\
&= 4500 + x \cdot 49 \\
&= 4500 + 49x
\end{aligned}
$$

To calculate the cost of producing $50$ units, we substitute $x=50$ into $C(x)$. This gives

$$
\begin{aligned}
C(50) &= 4500 + 49(50) \\
&= 4500 + 2450 \\
&= 6950
\end{aligned}
$$

Therefore, it costs the company $\[math]6\,950$ to produce $50$ units.

---

<a id="constructing-cost-functions"></a>
## Constructing Cost Functions

**Example:** A company produces $x$ units of baseball caps per month. Each unit costs $\[math]1.50$ to make, and the company’s fixed costs are $\[/math]3\,700$ per month. Calculate the total cost of producing $400$ units of the product.

**Explanation**

The cost function $C(x)$ can be calculated as follows:

$$
C(x) = (\text{fixed cost}) + (\textrm{number of units})\cdot (\textrm{cost per unit})
$$

In this case, we're told that the fixed costs are $\[math]3\,700$ per month, and each unit costs $\[/math]1.50$ to make. Therefore, the cost function is

$$
\begin{aligned}
C(x) &= 3700 + x \cdot 1.5 \\
&= 3700 + 1.5x
\end{aligned}
$$

To calculate the cost of producing $400$ units, we substitute $x=400$ into $C(x)$. This gives

$$
\begin{aligned}
C(400) &= 3700 + 1.5(400) \\
&= 3700 + 600 \\
&= 4300
\end{aligned}
$$

Therefore, it costs the company $\[math]4\,300$ to produce $400$ units.

---

**Question 3**

> A calculator is required to answer this question.

A company produces $x$ units of fertilizer per month. Each unit costs $$40$ to make, and the company’s fixed costs are $$5000$ per month. Calculate the cost function $C(x)$.

- [ ] A. $C(x) = 5000x - 40x^{2}$
- [ ] B. $C(x) = 5000 + 40x$
- [ ] C. $C(x) = 5000 - 40x$
- [ ] D. $C(x) = \frac{5000}{x} + 40$
- [ ] E. $C(x) = 5000x + 40x^{2}$

---

**Question 4**

> A calculator is required to answer this question.

A company produces $x$ kilograms of sugar per day. Each kilogram costs $$2.50$ to make, and the company’s fixed costs are $$625$ per day.
Calculate the total cost of producing $120$ kilograms of sugar in a day.It costs the company $$$ $\underline{\hspace{1.5cm}}$ to produce $120$ kilograms of sugar in a day.

---

<a id="profit-functions"></a>
## Profit Functions

A company manufactures canned mixed nuts. The price function for this company is given by

$$
p(x) = 60-0.01x
$$

where $x$ represents the number of units produced and sold per month. Additionally, each unit costs $\[math]25$ to make, and the company's fixed costs are $\[/math]8\,800$ per month.

Let's calculate the profit function $P(x)$ for this company.

Recall that profit function $P(x)$ is equal to the revenue function $R(x)$ minus the cost function $C(x)$:

$$
P(x)=R(x)-C(x)
$$

So, to calculate the profit function, we first need to construct $R(x)$ and $C(x)$.

- We're given the price function $p(x)$, so the revenue function is
$R(x)|= (number of units) \cdot (price per unit); = x \cdot p(x); = x(60 - 0.01x); = 60x - 0.01x^{2}$.
- We're also given that the fixed costs are $\[math]8\,800$ per month and that each unit costs $\[/math]25$ to make. Therefore, the cost function is
$C(x)|= (fixed cost) + (number of units) \cdot (cost per unit); = 8800 + x \cdot 25; = 8800 + 25x$.

Finally, we can now calculate the profit function:

$$
\begin{aligned}
P(x) &= R(x) - C(x) \\
&= (60x - 0.01x^{2}) - (8800 + 25x) \\
&= 60x - 0.01x^{2} - 8800 - 25x \\
&=-0.01x^{2} + 35x - 8800
\end{aligned}
$$

---

<a id="constructing-profit-functions"></a>
## Constructing Profit Functions

**Example:** To produce a certain brand of t-shirts, a manufacturing company bases the price of each unit on the price function $p(x) = 50-0.1x$, where $x$ represents the number of units produced and sold per month. Each unit costs $\[math]20$ to make, and the company’s fixed costs are $\[/math]2\,000$ per month. Calculate the profit function $P(x)$.

**Explanation**

We know that the profit function is given by

$$
P(x)=R(x)-C(x)
$$

where $x$ is the number of units, $C(x)$ is the cost function and $R(x)$ is the revenue function.

We're given the price function $p(x)$, so the revenue function is

$$
\begin{aligned}
R(x) &= (number of units) \cdot (price per unit) \\
&= x \cdot p(x) \\
&= x(50 - 0.1x) \\
&= 50x - 0.1x^{2}
\end{aligned}
$$

We're also told that the fixed costs are $\[math]2\,000$ per month, and each unit costs $\[/math]20$ to make. Therefore, the cost function is

$$
\begin{aligned}
C(x) &= (fixed cost) + (number of units) \cdot (cost per unit) \\
&= 2000 + x \cdot 20 \\
&= 2000 + 20x
\end{aligned}
$$

Finally, we can now calculate the profit function:

$$
\begin{aligned}
P(x) &= R(x) - C(x) \\
&= (50x - 0.1x^{2}) - (2000 + 20x) \\
&= 50x - 0.1x^{2} - 2000 - 20x \\
&=-0.1x^{2} + 30x - 2000
\end{aligned}
$$

---

**Question 5**

> A calculator is required to answer this question.

To produce smartphones, a manufacturing company models the price per unit by the price function $p(x) = 250 - 0.05x$, where $x$ represents the number of units produced per month, and $p$ is in dollars. Each unit costs $$170$ to make, and the company’s fixed costs are $$7000$ per month. Calculate the profit function $P(x)$.$P(x) =$ $\underline{\hspace{1.5cm}}$

---

**Question 6**

> A calculator is required to answer this question.

To produce Halloween costumes, a manufacturing company models the price per unit by the price function $p(x) = 70 - 0.1x$, where $x$ represents the number of units produced per month, and $p$ is in dollars. Each unit costs $$25$ to make, and the company’s fixed costs are $$5000$ per month. Calculate the profit function $P(x)$.

- [ ] A. $P(x) =-0.1x^{2} + 70x - 5000$
- [ ] B. $P(x) =-0.1x^{2} + 45x - 1000$
- [ ] C. $P(x) =-0.1x^{2} + 25x - 5000$
- [ ] D. $P(x) =-0.1x^{2} + 45x - 5000$
- [ ] E. $P(x) = 0.1x^{2} + 45x - 5000$

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Integrated-High-School-Mathematics/HI2/Home|Home]]
[[MA/Integrated-High-School-Mathematics/HI2/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
