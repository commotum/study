# Determining Unknown Parameters in Quadratic Equations: Advanced Cases

<!--
lesson-id: 6307
topic-code: HI3.5.1.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining Unknown Coefficients in Quadratic Equations With Real Solutions](#determining-unknown-coefficients-in-quadratic-equations-with-real-solutions)
- [Determining Unknown Coefficients in Quadratic Equations With No Real Solutions](#determining-unknown-coefficients-in-quadratic-equations-with-no-real-solutions)

## Prerequisites

- [Solving Quadratic Inequalities Using the Graphical Method](<5.1.3. Solving Quadratic Inequalities Using the Graphical Method.md>)
- [Determining Unknown Parameters in Quadratic Equations With No Real Solutions](<5.1.6. Determining Unknown Parameters in Quadratic Equations With No Real Solutions.md>)

---

<a id="introduction"></a>
## Introduction

In previous lessons, we learned how to determine conditions on unknown parameters in quadratic equations to force a particular number of solutions. As we saw, this process often involves solving a quadratic inequality.

In this lesson, we'll consider cases where the quadratic inequality that arises from these problems must be solved using a graphical method.

For example, for which values of $k$ does the equation

$$
3x^2-6kx+k=0
$$

have two distinct real solutions?

Recall that a quadratic equation has two distinct real solutions if its discriminant is positive: $\mathcal{D}>0$.

To compute the discriminant, we first note the equation's coefficients:

$$
a=3,\quad b=-6k,\quad c=k
$$

So, we require

$$
\begin{bmatrix}D & > 0 \\ b^{2} - 4ac & > 0 \\ (-6k)^{2} - 4(3)(k) & > 0 \\ 36k^{2} - 12k & > 0 \\ 3k^{2} - k & > 0\end{bmatrix}
$$

Now, let's consider the parabola $y=3k^2-k$ and find its solutions. We obtain

$$
\begin{aligned}
3k^{2} - k &= 0 \\
k(3k - 1) &= 0
\end{aligned}
$$

so the solutions are

$$
k=0\quad \text{and}\quad k=\dfrac{1}{3}
$$

Since the leading coefficient is positive, the parabola opens upward. Therefore, we get the following graph:

![](<../Source/5.1.7. Determining Unknown Parameters in Quadratic Equations- Advanced Cases/Images/afddb7615a89e921b1b4abfc6be6e77b.png>)

We want

$$
y = 3k^2 - k > 0
$$

meaning that the graph lies above the $k$-axis. Based on the graph, the corresponding intervals are $(-\infty,0)$ or

$$
\left(\dfrac{1}{3},\infty\right)
$$

Therefore, our equation has two distinct real solutions when

$$
k \in (-\infty,0) \cup \left(\dfrac{1}{3},\infty\right)
$$

---

<a id="determining-unknown-coefficients-in-quadratic-equations-with-real-solutions"></a>
## Determining Unknown Coefficients in Quadratic Equations With Real Solutions

**Example:** Given that $kx^2+kx+1= 0$, where $k$ is a non-zero real number, find the values of $k$ for which the equation has real solutions.

**Explanation**

A quadratic equation has real solutions if it has two distinct real solutions ($\mathcal{D}>0$) or one distinct real solution (

$$
\mathcal{D}=0
$$

). Putting these conditions together, we must have

$$
\mathcal{D} \geq 0
$$

To compute the discriminant, first note the following coefficients: $a=k$, $b=k$, and $c=1$. So, we require

$$
\begin{bmatrix}D & \ge 0 \\ b^{2} - 4ac & \ge 0 \\ k^{2} - 4(k)(1) & \ge 0 \\ k^{2} - 4k & \ge 0\end{bmatrix}
$$

Now, let's consider the parabola $y=k^2-4k$ and find its solutions. We obtain

$$
\begin{aligned}
k^{2} - 4k &= 0 \\
k(k - 4) &= 0
\end{aligned}
$$

so the solutions are $k=0$ and $k=4$. Since the leading coefficient is positive, the parabola opens upward. Therefore, we get the following graph:

![](<../Source/5.1.7. Determining Unknown Parameters in Quadratic Equations- Advanced Cases/Images/db93d48fb394eee06dd62adbd573698c.png>)

We want

$$
y = k^2 -4 k \geq 0
$$

meaning that the graph lies above or intersect the $k$-axis. Based on the graph, the corresponding interval is $[MATH: (-\infty, 0] \cup [4, \infty).]$

Finally, taking into account that

$$
k\neq 0
$$

we have $[MATH: k\in(-\infty, 0) \cup [4, \infty).]$

---

**Question 1:** Given that $kx^{2} + 2kx + 2 = 0$, where $k$ is a non-zero real number, find the values of $k$ for which the equation has real solutions.

- [ ] A. $[MATH: k ∈ (- ∞,-2]∪(0, ∞)]$
- [ ] B. $[MATH: k ∈ (0, 2]]$
- [ ] C. $[MATH: k ∈ [- 2, 0)]$
- [ ] D. $k ∈ (0, ∞)$
- [ ] E. $[MATH: k ∈ (- ∞, 0)∪[2, ∞)]$

---

**Question 2:** Given that $2x^{2} + (k - 4)x + (k + 2) = 0$, where $k$ is a real number, find the values of $k$ for which the equation has real solutions.

- [ ] A. $[MATH: k ∈ (- ∞, 0]∪[16, ∞)]$
- [ ] B. $k ∈ (- ∞, 0)∪(16, ∞)$
- [ ] C. $k ∈ (0, 16)$
- [ ] D. $k ∈ (0, ∞)$
- [ ] E. $k ∈ [0, 16]$

---

<a id="determining-unknown-coefficients-in-quadratic-equations-with-no-real-solutions"></a>
## Determining Unknown Coefficients in Quadratic Equations With No Real Solutions

**Example:** For which values of $k$ does the equation $2x^2-2kx+3k-4=0$ have no real solutions?

**Explanation**

To compute the discriminant, note the following coefficients: $a=2$, $b=-2k$ and $c=3k-4$.

So, we require

$$
\begin{bmatrix}D & < 0 \\ b^{2} - 4ac & < 0 \\ (-2k)^{2} - 4(2)(3k - 4) & < 0 \\ 4k^{2} - 8(3k - 4) & < 0 \\ 4k^{2} - 24k + 32 & < 0 \\ k^{2} - 6k + 8 & < 0\end{bmatrix}
$$

Now, let's consider the parabola

$$
y=k^2 - 6k + 8
$$

and find its solutions. We obtain

$$
\begin{aligned}
k^{2} - 6k + 8 &= 0 \\
k^{2} - 2k - 4k + 8 &= 0 \\
k(k - 2) - 4(k - 2) &= 0 \\
(k - 2)(k - 4) &= 0
\end{aligned}
$$

so the solutions are $k=2$ and $k=4$. Since the leading coefficient is positive, the parabola opens upward. Therefore, we get the following graph:

![](<../Source/5.1.7. Determining Unknown Parameters in Quadratic Equations- Advanced Cases/Images/fb68955859166bca946572f5b481dc0e.png>)

We want

$$
y =k^2 - 6k + 8 < 0
$$

meaning that the graph lies below the $k$-axis. Based on the graph, the corresponding interval is $(2,4)$.

Therefore, the given equation has no real solutions when $k \in (2, 4)$.

---

**Question 3:** Given that $2kx^{2} - 4kx + 5 = 0$, where $k$ is a non-zero real number, find the values of $k$ for which the equation has no real solutions.

- [ ] A. $k ∈ (\frac{7}{2}, ∞)$
- [ ] B. $k ∈ (- ∞,-\frac{5}{2})∪(\frac{5}{2}, ∞)$
- [ ] C. $k ∈ (0, \frac{7}{2})$
- [ ] D. $k ∈ (0, \frac{5}{2})$
- [ ] E. $k ∈ (- ∞, 0)∪(\frac{5}{2}, ∞)$

---

**Question 4:** Given that $32x^{2} - 4kx + \frac{1}{4}k = 0$, find the values of $k$ for which the equation has no real solutions.

- [ ] A. $k ∈ (-2, 4)$
- [ ] B. $k ∈ (0, 2)$
- [ ] C. $k ∈ (- ∞, 0)∪(2, ∞)$
- [ ] D. $k ∈ (- ∞, 2)∪(4, ∞)$
- [ ] E. $k ∈ (2, ∞)$

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Integrated-High-School-Mathematics/HI3/Home|Home]]
[[MA/Integrated-High-School-Mathematics/HI3/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
