# Linear Transformations of Points and Lines in the Plane

<!--
lesson-id: 2706
topic-code: HI3.14.5.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing the Image of a Point Under a Linear Transformation of a Plane](#computing-the-image-of-a-point-under-a-linear-transformation-of-a-plane)
- [The Image of a Line Under the Action of a Linear Transformation](#the-image-of-a-line-under-the-action-of-a-linear-transformation)
- [Computing the Image of a Line Given by an Equation in Slope-Intercept Form](#computing-the-image-of-a-line-given-by-an-equation-in-slope-intercept-form)
- [Computing the Image of a Line Given by an Equation in Standard Form](#computing-the-image-of-a-line-given-by-an-equation-in-standard-form)
- [The Proof That a Linear Transformation Maps a Straight Line Onto a Straight Line](#the-proof-that-a-linear-transformation-maps-a-straight-line-onto-a-straight-line)

## Prerequisites

- [Introduction to the Inverse of a Matrix](<../../14.4. The Inverse of a Matrix/Lessons/14.4.1. Introduction to the Inverse of a Matrix.md>)
- [The Standard Matrix of a Linear Transformation](<14.5.2. The Standard Matrix of a Linear Transformation.md>)
- [Properties of Lines Given in Standard Form](<../../../../../Traditional-High-School-Mathematics/AG1/2. Two-Variable Equations & Inequalities/2.1. Graphs of Linear Equations/Lessons/2.1.5. Properties of Lines Given in Standard Form.md>)

---

<a id="introduction"></a>
## Introduction

Consider the linear transformation $\mathbf T$ with matrix representation $T$, given by

$$
\begin{bmatrix}-2 & 1 \\ 0 & -2\end{bmatrix}
$$

Given a point $A(1,1)$, let $A'$ be the image of $A$ under the action of $\mathbf T$. How can we find the coordinates of $A'$?

First, we create a column vector $\mathbf{x}$ containing the coordinates of $A{:}$

$$
\begin{bmatrix}1 \\ 1\end{bmatrix}
$$

Now, we compute the image of $\mathbf{x}$ under the action of $\mathbf T$ by calculating the matrix product $T\mathbf{x}$:

$$
\begin{aligned}
Tx &= \begin{bmatrix}-2 & 1 \\ 0 & -2\end{bmatrix}\begin{bmatrix}1 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}-1 \\ -2\end{bmatrix}
\end{aligned}
$$

The result, $T \mathbf{x}$, contains the coordinates of $A'$. Therefore, $A'(-1,-2)$.

---

<a id="computing-the-image-of-a-point-under-a-linear-transformation-of-a-plane"></a>
## Computing the Image of a Point Under a Linear Transformation of a Plane

**Example:** Consider the linear transformation $\mathbf T$ with matrix representation $T$, given by

$\begin{bmatrix}3 & 1 \\ -4 & -1\end{bmatrix}$

What are the coordinates of the point $B'$, the image of $B(2,-3)$ under the action of $\mathbf T$?

**Explanation**

To find the image of $B$ under the action of $\mathbf T$, we first create a column vector $\mathbf{x}$ containing the coordinates of $B{:}$

$$
\begin{bmatrix}2 \\ -3\end{bmatrix}
$$

Now, we compute the image of $\mathbf{x}$ under the action of $\mathbf T$ by calculating the matrix product $T\mathbf{x}$:

$$
\begin{aligned}
Tx &= \begin{bmatrix}3 & 1 \\ -4 & -1\end{bmatrix}\begin{bmatrix}2 \\ -3\end{bmatrix} \\
&= \begin{bmatrix}3 \\ -5\end{bmatrix}
\end{aligned}
$$

The result, $T \mathbf{x}$, contains the coordinates of $B'$. Therefore, $B'(3,-5)$.

---

**Question 1**

Consider the linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}-3 & \sqrt{3} \\ 1 & \sqrt{3}\end{bmatrix}$.

What are the coordinates of the point $S^{′}$, the image of $S(-4, \sqrt{3})$ under the action of $T$?

- [ ] A. $(15,-4\sqrt{3})$
- [ ] B. $(15,-7)$
- [ ] C. $(5,-15)$
- [ ] D. $(15,-1)$
- [ ] E. $(12\sqrt{3},-1)$

---

**Question 2**

Consider the linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}-2 & 0 \\ 1 & 2\end{bmatrix}$.

What are the coordinates of the point $A^{′}$, the image of $A(-1, 1)$ under the action of $T$?

- [ ] A. $(1, 0)$
- [ ] B. $(0, 2)$
- [ ] C. $(2, 1)$
- [ ] D. $(-2, 0)$
- [ ] E. $(-2, 1)$

---

<a id="the-image-of-a-line-under-the-action-of-a-linear-transformation"></a>
## The Image of a Line Under the Action of a Linear Transformation

Linear transformations of a plane have the following important property:

> *A non-singular linear transformation in the plane maps a straight line into a straight line.*

Therefore, to find the image of a line $l$ under the action of a linear transformation $\mathbf{T}$ (represented by its standard matrix $T$), we do the following:

1. Find two distinct points $A$ and $B$ that lie on the line $l$.
2. Compute the images $A'$ and $B'$ of the points $A$ and $B$ under the action of $\mathbf{T}$.
3. Write down the equation of the line that passes through $A'$ and $B'$.

---

<a id="computing-the-image-of-a-line-given-by-an-equation-in-slope-intercept-form"></a>
## Computing the Image of a Line Given by an Equation in Slope-Intercept Form

**Example:** Consider the line $y=3x-2$ and linear transformation $\mathbf T$ with matrix representation $T$, given by

$\begin{bmatrix}-1 & 0 \\ 3 & 1\end{bmatrix}$

What is the equation of the image of the line under the action of $\mathbf T$?

**Explanation**

First, let's find two points on the given line. Substituting $x=0$ and $x=1$, we get

$$
\begin{bmatrix}x = 0 & → y = 3(0) - 2 =-2, \\ x = 1 & → y = 3(1) - 2 = 1\end{bmatrix}
$$

So we have two points on the line, $P(0,-2)$ and $Q(1,1)$.

To find the images of $P$ and $Q$ under the action of $\mathbf T$, we first create a matrix $X$ containing the coordinates of $P$ and $Q$ in its columns:

$$
\begin{bmatrix}0 & 1 \\ -2 & 1\end{bmatrix}
$$

Now, we compute the image of $X$ under the action of $\mathbf T$ by calculating the matrix product $TX$:

$$
\begin{aligned}
TX &= \begin{bmatrix}-1 & 0 \\ 3 & 1\end{bmatrix}\begin{bmatrix}0 & 1 \\ -2 & 1\end{bmatrix} \\
&= \begin{bmatrix}0 & -1 \\ -2 & 4\end{bmatrix}
\end{aligned}
$$

Therefore, the coordinates of the respective image points are $P'(0,-2)$ and $Q'(-1,4)$.

Finally, we write the equation of the line that passes through $P'$ and $Q'$:

$$
\begin{aligned}
y - y_{1} &= \frac{y_{2} - y_{1}}{x_{2} - x_{1}}(x - x_{1}) \\
y - (-2) &= (4 - (-2))/(-1 - 0)(x - 0) \\
y + 2 &= -6x \\
6x + y + 2 &= 0
\end{aligned}
$$

---

**Question 3**

Consider the line $y = 2x - 5$ and linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}1 & -1 \\ 2 & 0\end{bmatrix}$.

What is the equation of the image of the line under the action of $T$?

- [ ] A. $2x - y - 5 = 0$
- [ ] B. $2x - y + 10 = 0$
- [ ] C. $x + y - 5 = 0$
- [ ] D. $2x + y - 10 = 0$
- [ ] E. $x + y - 10 = 0$

---

**Question 4**

Consider the line $y = 2x + 3$ and linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}-2 & -1 \\ 1 & -3\end{bmatrix}$.

What is the equation of the image of the line under the action of $T$?

- [ ] A. $5x + 4y + 21 = 0$
- [ ] B. $5x - 4y - 21 = 0$
- [ ] C. $5x - y - 21 = 0$
- [ ] D. $5x + 4y - 12 = 0$
- [ ] E. $4x - 5y - 21 = 0$

---

<a id="computing-the-image-of-a-line-given-by-an-equation-in-standard-form"></a>
## Computing the Image of a Line Given by an Equation in Standard Form

**Example:** Consider the line $5x-y-3=0$ and linear transformation $\mathbf T$ with matrix representation $T$, given by

$\begin{bmatrix}-3 & 1 \\ -1 & 2\end{bmatrix}$

What is the equation of the image of the line under the action of $\mathbf T$?

**Explanation**

First, let's find two points on the given line. Solving for $y$, we get $y=5x-3$, and substituting $x=0$ and $x=1$, we get

$$
\begin{bmatrix}x = 0 & → y = 5(0) - 3 =-3, \\ x = 1 & → y = 5(1) - 3 = 2\end{bmatrix}
$$

So we have two points on the line, $P(0,-3)$ and $Q(1,2)$.

To find the images of $P$ and $Q$ under the action of $\mathbf T$, we first create a matrix $X$ containing the coordinates of $P$ and $Q$ in its columns:

$$
\begin{bmatrix}0 & 1 \\ -3 & 2\end{bmatrix}
$$

Now, we compute the image of $X$ under the action of $\mathbf T$ by calculating the matrix product $TX$:

$$
\begin{aligned}
TX &= \begin{bmatrix}-3 & 1 \\ -1 & 2\end{bmatrix}\begin{bmatrix}0 & 1 \\ -3 & 2\end{bmatrix} \\
&= \begin{bmatrix}-3 & -1 \\ -6 & 3\end{bmatrix}
\end{aligned}
$$

Therefore, the coordinates of the respective image points are $P'(-3,-6)$ and $Q'(-1,3)$.

Finally, we write the equation of the line that passes through $P'$ and $Q'$:

$$
\begin{aligned}
y - y_{1} &= \frac{y_{2} - y_{1}}{x_{2} - x_{1}}(x - x_{1}) \\
y - (-6) &= (3 - (-6))/(-1 - (-3))(x - (-3)) \\
y + 6 &= \frac{9}{2}(x + 3) \\
2y + 12 &= 9x + 27 \\
9x - 2y + 15 &= 0
\end{aligned}
$$

---

**Question 5**

Consider the line $2x + y - 3 = 0$ and linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}-2 & 0 \\ 1 & 2\end{bmatrix}$.

What is the equation of the image of the line under the action of $T$?

- [ ] A. $2x + y - 3 = 0$
- [ ] B. $3x - 2y + 12 = 0$
- [ ] C. $2x + 3y - 6 = 0$
- [ ] D. $x + 2y - 3 = 0$
- [ ] E. $2x - y + 12 = 0$

---

**Question 6**

Consider the line $x = 5$ and linear transformation $T$ with matrix representation $T$, given by

$T = \begin{bmatrix}-1 & 1 \\ 0 & 1\end{bmatrix}$.

What is the equation of the image of the line under the action of $T$?

- [ ] A. $y = 5$
- [ ] B. $x = 15$
- [ ] C. $5x - y + 10 = 0$
- [ ] D. $x - 5y + 1 = 0$
- [ ] E. $x - y + 5 = 0$

---

<a id="the-proof-that-a-linear-transformation-maps-a-straight-line-onto-a-straight-line"></a>
## The Proof That a Linear Transformation Maps a Straight Line Onto a Straight Line

Consider a non-singular linear transformation $\mathbf{T}$ and a line that is given by the vector equation

$$
\mathbf{r} = \mathbf{p}+\mathbf{d}t, \qquad t \in (-\infty,\infty)
$$

Note that, for the above equation to represent a line (and not just a point), we must have

$$
\mathbf{d} \neq \mathbf{0}
$$

Now, using the linearity of the transformation, we obtain

$$
\begin{aligned}
T(r) &= T(p + dt) \\
&= T(p) + T(td) \\
&= T(p) + tT(d) \\
&= p^{′} + td^{′}
\end{aligned}
$$

where

$$
\mathbf{T}(\mathbf{p})=\mathbf{p}'
$$

and

$$
\mathbf{T}(\mathbf{d})=\mathbf{d}'
$$

Because $\mathbf{T}$ is non-singular and

$$
\mathbf{d} \neq \mathbf{0}
$$

we know that

$$
\mathbf{T}(\mathbf{d}) \neq \mathbf{0}
$$

which means

$$
\mathbf{d}' \neq \mathbf{0}
$$

Therefore, the equation $\mathbf{p}'+t\mathbf{d}'$ represents a straight line, and we can make the following conclusion:

> *A non-singular linear transformation maps a straight line onto a straight line.*

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Integrated-High-School-Mathematics/HI3/Home|Home]]
[[MA/Integrated-High-School-Mathematics/HI3/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
