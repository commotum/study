# Quotients of Complex Numbers Expressed in Polar Form

<!--
lesson-id: 1232
topic-code: HI3.12.3.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Quotient of Two Complex Numbers Expressed in Polar Form](#calculating-the-quotient-of-two-complex-numbers-expressed-in-polar-form)
- [Calculating the Quotient of Two Complex Numbers Expressed in Polar Form With Negative Angles](#calculating-the-quotient-of-two-complex-numbers-expressed-in-polar-form-with-negative-angles)
- [Justification for the Formula](#justification-for-the-formula)

## Prerequisites

- [Products of Complex Numbers Expressed in Polar Form](<12.3.2. Products of Complex Numbers Expressed in Polar Form.md>)

---

<a id="introduction"></a>
## Introduction

To compute the quotient of two complex numbers

$$
z_1 = {\color{blue}r_1} \left[ \cos({\color{blue}\theta_1}) + \textrm{i} \sin({\color{blue}\theta_1}) \right]
$$

and

$$
z_2 = {\color{red}r_2} \left[ \cos({\color{red}\theta_2}) + \textrm{i} \sin({\color{red}\theta_2}) \right]
$$

expressed in polar form, we can use the following formula:

$$
\frac{z_{1}}{z_{2}} = \frac{r_{1}}{r_{2}}[\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2})]
$$

For example, let's use the above formula to compute the quotient of the following two complex numbers:

$$
\begin{aligned}
z_{1} &= 4[\cos (\frac{2π}{3}) + \textrm{i}\sin (\frac{2π}{3})] \\
z_{2} &= 2[\cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})]
\end{aligned}
$$

Here, we have

$$
{\color{blue}r_1}={\color{blue}4}
$$

$$
{\color{red}r_2}={\color{red}2}
$$

$$
{\color{blue}\theta_1}={\color{blue}\dfrac{2\pi}{3}}
$$

and

$$
{\color{red}\theta_2}={\color{red}\dfrac{\pi}{3}}
$$

So, the formula gives

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= \frac{4}{2}[\cos (\frac{2π}{3} - \frac{π}{3}) + \textrm{i}\sin (\frac{2π}{3} - \frac{π}{3})] \\
&= 2[\cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})]
\end{aligned}
$$

By evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= 2[(\frac{1}{2}) + \textrm{i}(\frac{\sqrt{3}}{2})] \\
&= 1 + \sqrt{3}\textrm{i}
\end{aligned}
$$

**Note:** At the end of this lesson, we will show where the formula comes from. But for now, let's focus on using it.

---

<a id="calculating-the-quotient-of-two-complex-numbers-expressed-in-polar-form"></a>
## Calculating the Quotient of Two Complex Numbers Expressed in Polar Form

**Example:** If $z_1=2\left[\cos \left(\dfrac{5\pi}{6} \right) +\textrm{i}\sin \left(\dfrac{5\pi}{6} \right) \right]$ and $z_2=4\left[\cos \left(\dfrac{\pi}{2} \right) +\textrm{i}\sin \left(\dfrac{\pi}{2} \right) \right]$, find the quotient $\dfrac{z_1}{z_2}$.

**Explanation**

To compute the quotient of two complex numbers, we use the formula

$$
\frac{z_{1}}{z_{2}} = \frac{r_{1}}{r_{2}}[\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2})]
$$

Substituting in $r_1=2$, $r_2=4$,

$$
\theta_1=\dfrac{5\pi}{6}
$$

and

$$
\theta_2=\dfrac{\pi}{2}
$$

we get

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= \frac{2}{4}[\cos (\frac{5π}{6} - \frac{π}{2}) + \textrm{i}\sin (\frac{5π}{6} - \frac{π}{2})] \\
&= \frac{1}{2}[\cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})]
\end{aligned}
$$

By evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= \frac{1}{2}[\frac{1}{2} + \textrm{i}(\frac{\sqrt{3}}{2})] \\
&= \frac{1}{4} + \frac{\sqrt{3}}{4}\textrm{i}
\end{aligned}
$$

---

**Question 1:** If $z_{1} = 3[\cos (\frac{5π}{6}) + \textrm{i}\sin (\frac{5π}{6})]$ and $z_{2} = 4[\cos (\frac{π}{6}) + \textrm{i}\sin (\frac{π}{6})]$, find the quotient $\frac{z_{1}}{z_{2}}$.

- [ ] A. $\frac{3}{8} + \frac{3\sqrt{3}}{8}\textrm{i}$
- [ ] B. $-\frac{3}{8} + \frac{3\sqrt{3}}{8}\textrm{i}$
- [ ] C. $-\frac{3}{8} - \frac{3\sqrt{2}}{8}\textrm{i}$
- [ ] D. $-\frac{3}{8} + \frac{3\sqrt{2}}{8}\textrm{i}$
- [ ] E. $-\frac{3}{8} - \frac{3\sqrt{3}}{8}\textrm{i}$

---

**Question 2:** If $z_{1} = 3[\cos (\frac{2π}{3}) + \textrm{i}\sin (\frac{2π}{3})]$ and $z_{2} = 9[\cos (\frac{π}{2}) + \textrm{i}\sin (\frac{π}{2})]$, find the quotient $\frac{z_{1}}{z_{2}}$.

- [ ] A. $\frac{\sqrt{2}}{6} + \frac{1}{6}\textrm{i}$
- [ ] B. $-\frac{\sqrt{3}}{6} - \frac{1}{6}\textrm{i}$
- [ ] C. $-\frac{\sqrt{2}}{6} + \frac{1}{6}\textrm{i}$
- [ ] D. $\frac{\sqrt{3}}{6} - \frac{1}{6}\textrm{i}$
- [ ] E. $\frac{\sqrt{3}}{6} + \frac{1}{6}\textrm{i}$

---

<a id="calculating-the-quotient-of-two-complex-numbers-expressed-in-polar-form-with-negative-angles"></a>
## Calculating the Quotient of Two Complex Numbers Expressed in Polar Form With Negative Angles

**Example:** Calculate the quotient $\dfrac{z_1}{z_2}$ given that $z_1=6\left[\cos \left(\dfrac{\pi}{12} \right) +\textrm{i}\sin \left(\dfrac{\pi}{12}\right) \right]$ and $z_2=4\left[\cos\left(-\dfrac{\pi}{6}\right)+\textrm{i}\sin\left(-\dfrac{\pi}{6}\right)\right]$.

**Explanation**

To compute the quotient of two complex numbers, we use the formula

$$
\frac{z_{1}}{z_{2}} = \frac{r_{1}}{r_{2}}[\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2})]
$$

Substituting in $r_1=6$, $r_2=4$,

$$
\theta_1=\dfrac{\pi}{12}
$$

and

$$
\theta_2=-\dfrac{\pi}{6}
$$

we get

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= \frac{6}{4}[\cos (\frac{π}{12} - (-\frac{π}{6})) + \textrm{i}\sin (\frac{π}{12} - (-\frac{π}{6}))] \\
&= \frac{3}{2}[\cos (\frac{π}{4}) + \textrm{i}\sin (\frac{π}{4})]
\end{aligned}
$$

By evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= \frac{3}{2}[\frac{\sqrt{2}}{2} + \textrm{i}(\frac{\sqrt{2}}{2})] \\
&= \frac{3\sqrt{2}}{4} + \frac{3\sqrt{2}}{4}\textrm{i}
\end{aligned}
$$

---

**Question 3:** If $z_{1} = 5[\cos (-\frac{π}{3}) + \textrm{i}\sin (-\frac{π}{3})]$ and $z_{2} = 4[\cos (\frac{π}{6}) + \textrm{i}\sin (\frac{π}{6})]$, find the quotient $\frac{z_{1}}{z_{2}}$.

- [ ] A. $\frac{5}{4}\textrm{i}$
- [ ] B. $\frac{5}{4} + \frac{5}{4}\textrm{i}$
- [ ] C. $\frac{5}{4} - \frac{5}{4}\textrm{i}$
- [ ] D. $-\frac{5}{4} - \frac{5}{4}\textrm{i}$
- [ ] E. $-\frac{5}{4}\textrm{i}$

---

**Question 4:** If $z_{1} = 2[\cos (-\frac{π}{12}) + \textrm{i}\sin (-\frac{π}{12})]$ and $z_{2} = 4[\cos (\frac{π}{12}) + \textrm{i}\sin (\frac{π}{12})]$, find the quotient $\frac{z_{1}}{z_{2}}$.

- [ ] A. $\frac{\sqrt{3}}{4} + \frac{1}{4}\textrm{i}$
- [ ] B. $-\frac{\sqrt{3}}{4} + \frac{1}{4}\textrm{i}$
- [ ] C. $-\frac{\sqrt{3}}{4} - \frac{1}{4}\textrm{i}$
- [ ] D. $\frac{\sqrt{2}}{4} + \frac{1}{4}\textrm{i}$
- [ ] E. $\frac{\sqrt{3}}{4} - \frac{1}{4}\textrm{i}$

---

<a id="justification-for-the-formula"></a>
## Justification for the Formula

To compute the quotient of two complex numbers

$$
z_1 = r_1 \left[ \cos(\theta_1) + \textrm{i} \sin(\theta_1) \right]
$$

and

$$
z_2 = r_2 \left[ \cos(\theta_2) + \textrm{i} \sin(\theta_2) \right]
$$

expressed in polar form, we have been using the following formula:

$$
\frac{z_{1}}{z_{2}} = \frac{r_{1}}{r_{2}}[\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2})]
$$

To see why the formula works, let's compute the quotient manually:

$$
\begin{aligned}
\frac{z_{1}}{z_{2}} &= (r_{1}[\cos (θ_{1}) + \textrm{i}\sin (θ_{1})])/(r_{2}[\cos (θ_{2}) + \textrm{i}\sin (θ_{2})]) \\
&= \frac{r_{1}}{r_{2}} \cdot (\cos (θ_{1}) + \textrm{i}\sin (θ_{1}))/(\cos (θ_{2}) + \textrm{i}\sin (θ_{2})) \cdot (\cos (θ_{2}) - \textrm{i}\sin (θ_{2}))/(\cos (θ_{2}) - \textrm{i}\sin (θ_{2})) \\
&= \frac{r_{1}}{r_{2}} \cdot ([\cos (θ_{1})\cos (θ_{2}) + \sin (θ_{1})\sin (θ_{2})]^(⏞)^(\cos (θ_{1} - θ_{2})) + \textrm{i}[\sin (θ_{1})\cos (θ_{2}) - \cos (θ_{1})\sin (θ_{2})]^(⏞)^(\sin (θ_{1} - θ_{2})))/(\cos^{2} (θ_{2}) + \sin^{2} (θ_{2})) \\
&= \frac{r_{1}}{r_{2}} \cdot (\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2}))/(1) \\
&= \frac{r_{1}}{r_{2}}[\cos (θ_{1} - θ_{2}) + \textrm{i}\sin (θ_{1} - θ_{2})]
\end{aligned}
$$

So, this formula is a consequence of the difference formulas for cosine and sine!

By working out the product manually, we get the same result, but with a lot more effort and calculations. The formula is the quickest way to go.

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Integrated-High-School-Mathematics/HI3/Home|Home]]
[[MA/Integrated-High-School-Mathematics/HI3/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
