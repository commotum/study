# The Gamma Distribution

<!--
lesson-id: 3075
topic-code: PAS.4.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Relationship to the Exponential Distribution](#relationship-to-the-exponential-distribution)
- [Computing a Probability Using the Gamma Distribution](#computing-a-probability-using-the-gamma-distribution)
- [The Mean and Variance of the Gamma Distribution](#the-mean-and-variance-of-the-gamma-distribution)
- [The Mean and Variance of the Gamma Distribution](#the-mean-and-variance-of-the-gamma-distribution)
- [Modeling With the Gamma Distribution](#modeling-with-the-gamma-distribution)

## Prerequisites

- [Mean and Variance of the Exponential Distribution](<../../4.3. The Exponential Distribution/Lessons/4.3.3. Mean and Variance of the Exponential Distribution.md>)
- [The Gamma Function](<4.4.1. The Gamma Function.md>)

---

<a id="introduction"></a>
## Introduction

The **gamma distribution** is a two-parameter continuous probability distribution with parameters $\alpha$, called the **shape parameter**, and $\lambda$, called the **rate parameter**.

If a random variable $X$ is gamma-distributed with shape parameter $\alpha$ and rate parameter $\lambda$, we write $X \sim \Gamma(\alpha, \lambda)$.

The probability density function of $X\sim \Gamma(\alpha, \lambda)$ is given by

$$
{(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

where $\Gamma(\alpha)$ is the gamma function.

We can use the gamma distribution to model the amount of continuous space or time that passes until the $\alpha$th event occurs in a Poisson process with success rate $\lambda$.

For example, suppose that a call center receives $6$ calls every minute on average, and we're interested to know how many minutes $X$ will pass until the $30$th call is received. Then,

- $\alpha = 30$, because we're interested in the number of minutes that pass until the $30$th call is received, and
- $\lambda = 6$, because the center receives an average of $6$ calls every minute.

In this situation, we can write $X\sim\Gamma(30, 6)$.

---

<a id="relationship-to-the-exponential-distribution"></a>
## Relationship to the Exponential Distribution

The probability density function of $X\sim \Gamma(\alpha, \lambda)$ is given by

$$
{(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

Notice that if we set $\alpha=1$, we obtain
${λe^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$.

This is the probability density function of an exponential distribution with rate $\lambda$. Thus,

$$
\Gamma(1, \lambda)=\textrm{Exp}(\lambda)
$$

In this sense, the gamma distribution extends the exponential distribution. Let's go back to our call center example to put this idea into context.

Recall that if a call center receives $6$ calls every minute on average, and we're interested to know how many minutes $X$ will pass until the call center receives the $\alpha$th call, then $X\sim \Gamma(\alpha, 6)$.

Now, by setting $\alpha = 1$, we specify that we're interested in the total number of minutes that pass until the *first* call is received. But this can simply be modeled using the random variable $X\sim \textrm{Exp}(6)$.

In general, if $X_1, X_2, \ldots X_{\alpha} \sim \textrm{Exp}(\lambda)$ are independent and identically distributed, then
$X_1+X_2+\cdots + X_{\alpha} \sim \Gamma(\alpha, \lambda)$.

---

<a id="computing-a-probability-using-the-gamma-distribution"></a>
## Computing a Probability Using the Gamma Distribution

**Example:** Let $\Gamma(\alpha, \lambda)$ denote the gamma distribution with shape parameter $\alpha$ and rate parameter $\lambda$. Given that $X \sim \Gamma\left(4,\dfrac{1}{5}\right)$, express $P(X < 1)$ as an integral.

*A gamma random variable $X \sim \Gamma(\alpha, \lambda)$ with shape parameter $\alpha$ and rate parameter $\lambda$ has the following probability density function:*

${(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $\Gamma (\alpha)$ is the gamma function.*

**Explanation**

A gamma random variable $X \sim \Gamma(\alpha, \lambda)$ with shape parameter $\alpha$ and rate parameter $\lambda$ has the following probability density function:

$$
{(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

We're given that

$$
X \sim \Gamma\left(4,\dfrac{1}{5}\right)
$$

so $X$ has the following probability density function:

$$
{((\frac{1}{5})^{4})/(Γ(4))x^{3}e^{-x/5},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

Recalling that

$$
\Gamma(n) = (n-1)
$$

for any natural number

$$
n \geq 1
$$

we have

$$
\Gamma(4) = 3! = 6
$$

So, the probability distribution becomes

$$
{\frac{1}{3750}x^{3}e^{-x/5},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

Finally, we can express

$$
P\left(X < 1\right)
$$

as follows:

$$
\begin{aligned}
P(X < 1) &= ∫_{0}^{1}f(x)dx \\
&= ∫_{0}^{1}\frac{1}{3750}x^{3}e^{-x/5}dx \\
&= \frac{1}{3750}∫_{0}^{1}x^{3}e^{-x/5}dx
\end{aligned}
$$

---

**Question 1**

Let $Γ(α, λ)$ denote the gamma distribution with shape parameter $α$ and rate parameter $λ$. Given that $X∼Γ(5, 2)$, express $P(X < 4)$ as an integral.

*Hint: A gamma random variable $X∼Γ(α, λ)$ with shape parameter $α$ and rate parameter $λ$ has the following probability density function:*

$f(x) = {(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $Γ(α)$ is the gamma function.*

- [ ] A. $\frac{3}{5}∫_{0}^{4}x^{5}e^{-2x}dx$
- [ ] B. $\frac{4}{3}∫_{0}^{4}x^{2}e^{-5x}dx$
- [ ] C. $\frac{5}{3}∫_{0}^{4}x^{5}e^{-2x}dx$
- [ ] D. $\frac{4}{3}∫_{0}^{4}x^{4}e^{-2x}dx$
- [ ] E. $\frac{3}{4}∫_{0}^{4}x^{4}e^{-2x}dx$

---

**Question 2**

Let $Γ(α, λ)$ denote the gamma distribution with shape parameter $α$ and rate parameter $λ$. Given that $X∼Γ(\frac{3}{2}, 4)$, express $P(X > 3)$ as an integral.

*Hint: A gamma random variable $X∼Γ(α, λ)$ with shape parameter $α$ and rate parameter $λ$ has the following probability density function:*

$f(x) = {(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $Γ(α)$ is the gamma function.*

- [ ] A. $8\sqrt{π}∫_{3}^{∞}xe^{-4x}dx$
- [ ] B. $\frac{\sqrt{π}}{16}∫_{3}^{∞}\sqrt{x}e^{-4x}dx$
- [ ] C. $\frac{16}{\sqrt{π}}∫_{3}^{∞}\sqrt{x}e^{-4x}dx$
- [ ] D. $\frac{16}{\sqrt{π}}∫_{3}^{∞}x^{3}\sqrt{e^{-x}}dx$
- [ ] E. $\frac{\sqrt{π}}{16}∫_{3}^{∞}x^{3}\sqrt{e^{-x}}dx$

---

<a id="the-mean-and-variance-of-the-gamma-distribution"></a>
## The Mean and Variance of the Gamma Distribution

If $X \sim \Gamma(\alpha, \lambda)$, then we can show that the following formulas give the mean and variance of $X$:

$$
\textrm E[X]=\dfrac{\alpha}{\lambda},\qquad \textrm{Var}[X]=\dfrac{\alpha}{\lambda^2}
$$

Notice that if we set $\alpha = 1$, we get the same results as those for an exponential distribution with rate parameter $\lambda$.

The formula for the mean of a gamma distribution is quite intuitive. Let's illustrate using an example.

Recall that if a call center receives $6$ calls every minute on average, and we're interested to know how much time $X$ will pass until the call center receives the following $30$ calls, then $X\sim \Gamma(30, 6)$.

Computing $\textrm E[X]$ using the above formula, we have

$$
\textrm E[X] = \dfrac{30}{6} = 5
$$

This result makes intuitive sense. If the call center receives an average of $6$ calls per minute, we expect it to take an average of $5$ minutes to receive $30$ calls.

---

<a id="the-mean-and-variance-of-the-gamma-distribution"></a>
## The Mean and Variance of the Gamma Distribution

**Example:** Let $\Gamma(\alpha, \lambda)$ denote the gamma distribution with shape parameter $\alpha$ and rate parameter $\lambda$. Given that $X \sim \Gamma(3,4)$, compute $\textrm E[X]$ and $\textrm{Var}[X]$.

**Explanation**

Given a gamma random variable $X \sim \Gamma(\alpha, \lambda)$ with shape parameter $\alpha$ and rate parameter $\lambda$, the expected value and the variance of $X$ are given by

$$
E[X] = \frac{α}{λ}, Var[X] = \frac{α}{λ^{2}}
$$

In our case, $\alpha = 3$ and

$$
\lambda = 4
$$

Therefore,

$$
\begin{aligned}
E[X] &= \frac{3}{4} \\
Var[X] &= \frac{3}{4^{2}} = \frac{3}{16}
\end{aligned}
$$

---

**Question 3:** Let $Γ(α, λ)$ denote the gamma distribution with shape parameter $α$ and rate parameter $λ$. Given that $X∼Γ(5, 2)$, compute $Var[X]$.

- [ ] A. $\frac{5}{4}$
- [ ] B. $4$
- [ ] C. $\frac{4}{5}$
- [ ] D. $\frac{2}{5}$
- [ ] E. $\frac{5}{2}$

---

**Question 4:** Let $Γ(α, λ)$ denote the gamma distribution with shape parameter $α$ and rate parameter $λ$. Given that $X∼Γ(5, 2)$, compute $E[X]$.

- [ ] A. $\frac{5}{2}$
- [ ] B. $\frac{5}{3}$
- [ ] C. $\frac{2}{5}$
- [ ] D. $2$
- [ ] E. $\frac{1}{2}$

---

<a id="modeling-with-the-gamma-distribution"></a>
## Modeling With the Gamma Distribution

**Example:** At a cafe, $10$ customers arrive per hour on average. What is the probability that the $4$th customer will arrive within half an hour of the cafe opening? Round your answer to $3$ decimal places. You may assume arrivals occur continuously in time, independently, and at a constant average rate.

*Hint: You may use the fact that*
$\int_0^{0.5} x^3 e^{-10x} \, \textrm dx \approx 0.000\,441$.

*A gamma random variable $X \sim \Gamma(\alpha, \lambda)$ with shape parameter $\alpha$ and rate parameter $\lambda$ has the following probability density function:*

${(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $\Gamma (\alpha)$ is the gamma function.*

**Explanation**

If the random variable $X$ measures the amount of continuous space or time until the $\alpha$th event occurs in a Poisson process with rate $\lambda$, then $X \sim \Gamma(\alpha, \lambda)$.

Let $X$ be the number of hours until the $4$th customer arrives. Since the rate at which customers arrive is

$$
\lambda = 10
$$

per hour, and we want to measure the number of hours until the $\alpha = 4$th customer arrives, we have

$$
X \sim \Gamma \left(4, 10 \right)
$$

We want to find the probability that the $10$th customer will arrive within half an hour of the cafe opening, which we can express as

$$
P(X \leq 0.5)
$$

We first need to construct the probability density function to compute this probability. In general, a gamma random variable $X \sim \Gamma(\alpha, \lambda)$ with shape parameter $\alpha$ and rate parameter $\lambda$ has the following probability density function:

$$
{(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

In our case, we have that

$$
X \sim \Gamma \left(4, 10 \right)
$$

so $X$ has the following probability density function:

$$
{(10^{4})/(Γ(4))x^{3}e^{-10x},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

Recalling that

$$
\Gamma(n) = (n-1)
$$

for any natural number

$$
n \geq 1
$$

we have

$$
\Gamma(4)= 3! = 6
$$

So, the probability distribution becomes

$$
{\frac{5000}{3}x^{3}e^{-10x},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise
$$

Finally, we compute

$$
P\left(X < 0.5 \right)
$$

as follows:

$$
\begin{aligned}
P(X < 0.5) &= ∫_{0}^{0.5}f(x)dx \\
&= ∫_{0}^{0.5}\frac{5000}{3}x^{3}e^{-10x}dx \\
&= \frac{5000}{3}∫_{0}^{0.5}x^{3}e^{-10x}dx \\
&\approx \frac{5000}{3} \cdot (0.000441) \\
&\approx 0.735
\end{aligned}
$$

---

**Question 5**

> A scientific calculator is required to answer this question.

At a convenience store, $5$ customers arrive per hour on average. What is the probability that the $3$rd customer will arrive within an hour of the store opening? Round your answer to $4$ decimal places. You may assume arrivals occur continuously in time, independently, and at a constant average rate.

*Hint: You may use the fact that*
$∫_{0}^{1}x^{2}e^{-5x}dx \approx 0.014006$.

*A gamma random variable $X∼Γ(α, λ)$ with shape parameter $α$ and rate parameter $λ$ has the following probability density function:*

$f(x) = {(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $Γ(α)$ is the gamma function.*

- [ ] A. $0.8240$
- [ ] B. $0.8470$
- [ ] C. $0.8298$
- [ ] D. $0.8754$
- [ ] E. $0.8560$

---

**Question 6**

> A scientific calculator is required to answer this question.

At a particular underwater location, earthquakes occur every $3$ months on average. What is the probability that the $2$nd earthquake will occur at least $2$ months after the start of the observations? Round your answer to $4$ decimal places. You may assume earthquakes occur continuously in time, independently, and at a constant average rate.

*Hint: You may use the fact that*
$∫_{2}^{∞}xe^{-x/3}dx \approx 7.701257$.

*A gamma random variable $X∼Γ(α, λ)$ with shape parameter $α$ and rate parameter $λ$ has the following probability density function:*

$f(x) = {(λ^{α})/(Γ(α))x^{α - 1}e^{- λx},\begin{vmatrix}x > 0 \\ 0,\end{vmatrix}otherwise$
*where $Γ(α)$ is the gamma function.*

- [ ] A. $0.8519$
- [ ] B. $0.8557$
- [ ] C. $0.8621$
- [ ] D. $0.8748$
- [ ] E. $0.8673$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/PAS/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/PAS/0. Table of Contents/TOC|Table of Contents]]
