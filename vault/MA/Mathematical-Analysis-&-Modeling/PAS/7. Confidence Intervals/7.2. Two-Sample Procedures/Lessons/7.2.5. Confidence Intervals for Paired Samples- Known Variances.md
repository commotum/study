# Confidence Intervals for Paired Samples: Known Variances

<!--
lesson-id: 3970
topic-code: PAS.7.2.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding Confidence Intervals for Mean for Paired Sample](#finding-confidence-intervals-for-mean-for-paired-sample)
- [Interpreting Confidence Intervals](#interpreting-confidence-intervals)
- [Finding Confidence Intervals for Paired Samples: Applications](#finding-confidence-intervals-for-paired-samples-applications)
- [Deriving Confidence Intervals for Paired Samples](#deriving-confidence-intervals-for-paired-samples)

## Prerequisites

- [Confidence Intervals for Two Means: Known and Unequal Population Variances](<7.2.1. Confidence Intervals for Two Means- Known and Unequal Population Variances.md>)

---

<a id="introduction"></a>
## Introduction

Suppose a doctor wishes to determine the effect of a new diabetes treatment on patient blood sugar levels before and after treatment.

A sample of $n = 65$ patients is conducted, yielding two data sets:

$$
\big\{x_1,x_2,\ldots,x_{65}\big\}, \qquad \big\{y_1,y_2,\ldots,y_{65}\big\}
$$

These data sets are *dependent* because each pair $(x_i, y_i)$ is collected from the *same patient*. For this reason, we call this a **paired sample**.

Let's define the following variables:

- $X_i$ is the blood sugar level (in $\mathrm{mg/dL}$) of the $i$th patient before the treatment
- $Y_i$ the blood sugar level (in $\mathrm{mg/dL}$) of the $i$th patient after the treatment
- $\textrm E[X_i] = \mu_x$ is the (population) mean blood sugar level of the patients before the treatment
- $\textrm E[Y_i] = \mu_y$ is the (population) mean blood sugar level of the patients after the treatment

In a previous lesson, we learned how to construct a confidence interval for $\mu_x - \mu_y$, the difference between two *independent* samples. However, these techniques cannot be applied here since our samples are dependent. So, how do we compare these two samples? The answer lies in analyzing the differences.

Instead of working with two datasets, we compute the difference for each pair:

$$
D_i = X_i - Y_i
$$

By reducing the problem to a single dataset of differences, we can analyze the distribution of $D_i$ to draw inferences about the mean difference.

Let's assume that the set $D_1, D_2, \ldots D_n$ are independent and identically distributed (I.I.D). Then, we have two possible cases:

- Suppose we run some tests, and it's confirmed that $D_i$ is normally distributed. Then, the distribution of the sample mean $\overline D$ is given by
$\overline{D} \sim N\left(\mu, \dfrac{\sigma^2}{n}\right)$
where $\mu$ is the population mean of $D_i$, and $\sigma^2$ is the population variance of $D_i$.
- If $D_i$ is not normally distributed but the sample size is large, then by the central limit theorem,
$\overline{D} \approx N\left(\mu, \dfrac{\sigma^2}{n}\right)$.

In both cases, we can use one-sample procedures to construct a confidence interval for $\mu$, the mean difference between pre- and post-treatment blood sugar levels.

Let's take a look at a concrete example.

---

<a id="finding-confidence-intervals-for-mean-for-paired-sample"></a>
## Finding Confidence Intervals for Mean for Paired Sample

**Example:** Consider two independent paired samples of size $n=60$ from the random variables $X$ and $Y$. You're given that the means for these samples are $\overline{x} = 11.8$ and $\overline{y} = 9.6$, respectively. Given that the differences between the paired observations $D_i=X_i-Y_i$ are I.I.D. with variance $\sigma^2 = 4.53^2$, find a $95\%$ confidence interval for the population mean of $D_i$.

*Hint: Use the fact that $P(Z > 1.96) = 0.025$, where $Z\sim N(0,1)$, as shown in the diagram below.*

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/5f713fb74d8ab1ecf77bef6b96218cb0.png>)

**Explanation**

We do not know the distribution of the differences $D_i$. However, the sample size

$$
n = 60\geq 30
$$

is *sufficiently large*. Therefore, according to the central limit theorem, we may use the following approximation:

$$
\overline{D} \sim N\left(\mu, \dfrac{\sigma^2}{n}\right)
$$

where

- $\overline{D}$ is the sample mean,
- $\mu$ is the population mean of $D_i$,
- $\sigma^2$ is the population variance of $D_i$, and
- $n$ is the sample size.

As a result, given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the mean difference $\mu$ between $X$ and $Y$ can be written as

$$
\overline{d} \pm E
$$

where

- $\overline{d}=\overline{x}-\overline{y}\,$ is a point estimate for $\mu$,
- $E=z_{\alpha/2} \cdot \dfrac{\sigma}{\sqrt{n}}$ is the margin of error,
- $P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}$ and $Z\sim N(0,1)$.

Now, to construct the confidence interval, we proceed as follows:

- **Step 1.** Find a point estimate for $\mu{:}$We compute our point estimate as follows:
$\overline{d} = \overline{x} - \overline{y} = 11.8-9.6= 2.2$
- **Step 2.** Find the margin of error. Since we are interested in finding a $95\%$ confidence interval, we have
$\alpha=1-0.95=0.05 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.025$.
We are given that $P(Z > 1.96) = 0.025$. As a result,
$z_{\alpha/2} =1.96$.
Now, we can compute the margin of error:
$E = 1.96 \cdot \dfrac{4.53}{\sqrt{60}} = 1.146$
- **Step 3.** Construct the $95\%$ confidence interval for the population mean of $D{:}$Our $95\%$ confidence interval is
$2.2 ± 1.146$.

---

**Question 1**

> A scientific calculator is required to answer this question.

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/q-224532.png>)

Consider a sample $x_{1}, x_{2}, x_{3}, x_{4}$ from a random variable $X$, and a sample $y_{1}, y_{2}, y_{3}, y_{4}$ from a random variable $Y$. Given that the two samples are paired and the differences between the paired observations $D_{i} = X_{i} - Y_{i}$ are I.I.D normally distributed variables with mean $μ$ and variance $σ^{2} = 3^{2}$, find a $95%$ confidence interval for $μ$.

The sample data is given in the table below.

| $x_{i}$ | $y_{i}$ |
| --- | ---: |
| $15$ | $13$ |
| $15$ | $14$ |
| $19$ | $15$ |
| $19$ | $16$ |

*Hint: Use the fact that $P(Z > 1.96) = 0.025$, where $Z∼N(0, 1)$, as shown in the diagram above.*

- [ ] A. $2.5 ± 4.41$
- [ ] B. $2.5 ± 2.94$
- [ ] C. $2.5 ± 3.92$
- [ ] D. $2.5 ± 3.21$
- [ ] E. $2.5 ± 1.47$

---

**Question 2**

> A scientific calculator is required to answer this question.

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/q-224533.png>)

Consider two independent paired samples of size $n = 40$ from the random variables $X$ and $Y$. You're given that the means for these samples are $x^(―) = 10.5$ and $y^(―) = 9.8$, respectively. Given that the differences between the paired observations $D_{i} = X_{i} - Y_{i}$ are I.I.D. with variance $σ^{2} = 1.35^{2}$, find a $90%$ confidence interval for the population mean of $D_{i}$.

*Hint: Use the fact that $P(Z > 1.645) = 0.05$, where $Z∼N(0, 1)$, as shown in the diagram above.*

- [ ] A. $0.7 ± 0.057$
- [ ] B. $0.7 ± 0.818$
- [ ] C. $0.7 ± 1.228$
- [ ] D. $0.7 ± 0.351$
- [ ] E. $0.7 ± 1.162$

---

<a id="interpreting-confidence-intervals"></a>
## Interpreting Confidence Intervals

A confidence interval for the difference $\mu_x-\mu_y$ between the population means can be interpreted in the following ways:

- If all values within the confidence interval are *positive*, this suggests that $\mu_x - \mu_y > 0$. In other words
$\mu_x > \mu_y$.
We should carry out a statistical hypothesis test to formally verify this claim.
- If all values within the confidence interval are *negative*, this suggests that $\mu_x - \mu_y < 0$. In other words
$\mu_x < \mu_y$.
Again, we should conduct a statistical hypothesis test to verify this claim.
- If the confidence interval contains zero, then there is *insufficient evidence* of a statistically significant difference between the two means. Therefore, we conclude
$\mu_x \approx \mu_y$.

Let's see an example.

---

<a id="finding-confidence-intervals-for-paired-samples-applications"></a>
## Finding Confidence Intervals for Paired Samples: Applications

**Example:** A scientist is studying the effect of a new negative catalyst on $n=45$ chemical reactions. Suppose that $x_1,x_2,\ldots,x_{45}$ are the reaction times observed without the catalyst and $y_1,y_2,\ldots,y_{45}$ are the reaction times obtained with the catalyst. It is known that the differences $X_i - Y_i$ between the reaction times without and with the catalyst are I.I.D. random variables with population variance $\sigma^2=(22\,\textrm{min})^2$. You're given that the sample means of these samples are $\overline x=36\,\textrm{min}$ and $\overline y=30\,\textrm{min}$.

Find a $95\%$ confidence interval for the population mean of $X_i-Y_i$ and interpret the result.

*Hint: Use the fact that $P(Z > 1.96) = 0.025$, where $Z\sim N(0,1)$, as shown in the diagram above.*

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/7dd3585a305c9f1626d15f2af33c2a32.png>)

**Explanation**

Let us call

$$
D_i = X_i-Y_i
$$

the random variable that represents the difference in the reaction times of a randomly selected reaction.

We do not know the distribution of the differences $D_i$. However, the sample size

$$
n = 45\geq 30
$$

is *sufficiently large*. Therefore, according to the central limit theorem, we may use the following approximation:

$$
\overline{D} \sim N\left(\mu, \dfrac{\sigma^2}{n}\right)
$$

where

- $\overline{D}$ is the sample mean,
- $\mu$ is the population mean of $D_i$,
- $\sigma^2$ is the population variance of $D_i$, and
- $n$ is the sample size.

As a result, given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the mean difference $\mu$ between $X$ and $Y$ can be written as

$$
\overline{d} \pm E
$$

where

- $\overline{d}=\overline{x}-\overline{y}$ is a point estimate for $\mu$,
- $E=z_{\alpha/2} \cdot \dfrac{\sigma}{\sqrt{n}}$ is the margin of error,
- $P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}$ and $Z\sim N(0,1)$.

Now, to construct the confidence interval, we proceed as follows:

- **Step 1.** Find a point estimate for $\mu{:}$
$\overline{d} = \overline{x} - \overline{y} = 36-30 = 6$
- **Step 2.** Find the margin of error. Since we are interested in finding a $95\%$ confidence interval, we have
$\alpha=1-0.95=0.05 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.025$.
We are given that $P(Z > 1.96) = 0.025$. As a result,
$z_{\alpha/2} =1.96$.
Now, we can compute the margin of error:
$E = 1.96 \cdot \dfrac{22}{\sqrt{45}} \approx 6.43$
- **Step 3.** Construct the $95\%$ confidence interval for the population mean of $D{:}$
$6 ± 6.43$

Note that the confidence interval contains zero.

$$
(6-6.43, \, 6+6.43) = (-0.43,\,12.43)
$$

Since the confidence interval contains zero, the data suggests that the average reaction time with the catalyst is approximately the same as without the catalyst.

---

**Question 3**

> A scientific calculator is required to answer this question.

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/q-251528.png>)

A tech company is evaluating the performance of a new processor on $n = 50$ computers. Suppose that $x_{1}, x_{2}, …, x_{50}$ are the performance scores of the computers with the new processor and $y_{1}, y_{2}, …, y_{50}$ are the scores of the computers with the old processor. It is known that the differences $X_{i} - Y_{i}$ are I.I.D random variables with population variance $σ^{2} = 16^{2}$. You're given that the sample means are $x^(―) = 255$ and $y^(―) = 250$.

Fill in the missing information in the following statements.

*Hint: Use the fact that $P(Z > 2.326) = 0.01$, where $Z∼N(0, 1)$, as shown in the diagram above.*

The end-points of the $98%$ confidence interval for the difference between the average scores, rounded to two decimal places, are approximately
$5$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
The $98%$ confidence interval $\underline{\hspace{2cm}}$.
This suggests that the average score of the new processor $\underline{\hspace{2cm}}$ the average score of the old processor.

#### Select 1

- [ ] [MATH: 5]
- [ ] [MATH: 10]

#### Select 2

- [ ] [MATH: 4.35]
- [ ] [MATH: 5.26]
- [ ] [MATH: 4.65]
- [ ] [MATH: 5.72]

#### Select 3

- [ ] contains only positive values
- [ ] contains zero

#### Select 4

- [ ] is larger than
- [ ] is approximately the same as
- [ ] is smaller than

---

**Question 4**

> A scientific calculator is required to answer this question.

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/q-224595.png>)

A speech therapist is testing a new method to treat dyslexia on a sample of $n = 60$ patients. Suppose that $x_{1}, x_{2}, …, x_{60}$ are the scores of the patients in a dyslexia test after the treatment and $y_{1}, y_{2}, …, y_{60}$ are the scores of the same patients before the treatment. It is known that the differences $X_{i} - Y_{i}$ between the scores after and before the treatment are I.I.D. random variables with population variance $σ^{2} = 8^{2}$. You're given that the means for these samples are $x^(―) = 72$ and $y^(―) = 68$.

Fill in the missing information in the following statements.

*Hint: Use the fact that $P(Z > 2.326) = 0.01$, where $Z∼N(0, 1)$, as shown in the diagram above.*

The end-points of the $98%$ confidence interval for the difference between the average scores, rounded to two decimal places, are approximately
$2$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$
The $98%$ confidence interval $\underline{\hspace{2cm}}$.
This suggests that the average score after the treatment $\underline{\hspace{2cm}}$ the average score before the treatment.

#### Select 1

- [ ] [MATH: 2]
- [ ] [MATH: 4]

#### Select 2

- [ ] [MATH: 3.25]
- [ ] [MATH: 2.33]
- [ ] [MATH: 3.02]
- [ ] [MATH: 2.40]

#### Select 3

- [ ] contains only positive values
- [ ] contains zero

#### Select 4

- [ ] is larger than
- [ ] is smaller than
- [ ] is approximately the same as

---

<a id="deriving-confidence-intervals-for-paired-samples"></a>
## Deriving Confidence Intervals for Paired Samples

**Example:** Consider two paired random samples $X_1, X_2, \ldots, X_{27}$ and $Y_1, Y_2, \ldots, Y_{27}$ of size $n = 27$. It is known that the differences between paired observations $D_i = X_i - Y_i$ are normally distributed with population mean $\mu$ and population variance $\sigma^2 = 3$. Derive the expression used to construct a $95\%$ confidence interval for $\mu$.

**Explanation**

To construct a confidence interval, we start with a random variable that depends on the samples and has a known distribution under certain conditions.

> Consider the random variable
> $Z|= ((X^(―) - Y^(―)) - μ)/(\frac{σ}{\sqrt{n}}); = ((X^(―) - Y^(―)) - μ)/((\frac{\sqrt{3}}{\sqrt{27}})); = ((X^(―) - Y^(―)) - μ)/((\frac{1}{3}))$,
> where $\overline{X}$ and $\overline{Y}$ are the sample means of the given samples.

It is known that if the samples are paired and the differences between each pair are normally distributed with a population mean $\mu$ and known population variance, $Z$ has a standard normal distribution.

> We have that
> $Z \sim {\color{black}N(0,1)}$.

We wish to find an interval that we're $95\%$ confident that the random variable $Z$ lies within. This interval is indicated in the diagram below:

![](<../Source/7.2.5. Confidence Intervals for Paired Samples- Known Variances/Images/8fe194faaa3282ed2778ad9c87de9844.png>)

Since we're computing a $95\%$ confidence interval, the area bounded by each "tail" that we're excluding from our confidence interval must be

$$
\dfrac{1 - 0.95}{2} = 0.025
$$

We can find the endpoints of such interval using a table of values for the normal distribution.

> Using a table of values for the normal distribution, we require a value $c$ such that
> $P(Z > c) = {\color{black}0.025}$.
> In other words,
> $P(-c < Z < c) = 0.95$.
> Let's call $c = z_{0.025}$.
> Now, we find the appropriate confidence interval by solving the inequality inside the parentheses of the last probability statement for the difference mean $\mu$.
> $\begin{bmatrix}-z_{0.025} & < Z < z_{0.025} \\ -z_{0.025} & < ((x^(―) - y^(―)) - μ)/((\frac{1}{3})) < z_{0.025}\end{bmatrix}$

To solve this inequality, we first multiply by

$$
\dfrac{1}{3}
$$

to get rid of the denominator:

> We solve the inequality as follows:
> $-\frac{z_{0.025}}{3} \mid < (x^(―) - y^(―)) - μ < \frac{z_{0.025}}{3}$

Then, we multiply all sides by $-1$:

> $\frac{z_{0.025}}{3} \mid > μ - (x^(―) - y^(―)) > - \frac{z_{0.025}}{3}$

Next, we add $\overline{x} - \overline{y}$:

> $\frac{z_{0.025}}{3} + x^(―) - y^(―) \mid > μ > - \frac{z_{0.025}}{3} + x^(―) - y^(―)$

Next, we rewrite our inequality so that the smaller terms are on the left-hand side.

> $x^(―) - y^(―) - \frac{z_{0.025}}{3} \mid < μ < x^(―) - y^(―) + \frac{z_{0.025}}{3}$

Now, we write our result more compactly.

> Finally, we can write our interval as follows:
> $(x^(―) - y^(―)) ± z_{0.025} \cdot \frac{1}{3}$

The full proof is given below:

> Consider the random variable
> $Z|= ((X^(―) - Y^(―)) - μ)/(\frac{σ}{\sqrt{n}}); = ((X^(―) - Y^(―)) - μ)/((\frac{\sqrt{3}}{\sqrt{27}})); = ((X^(―) - Y^(―)) - μ)/((\frac{1}{3}))$,
> where $\overline{X}$ and $\overline{Y}$ are the sample means of the given samples.
> We have that
> $Z \sim {\color{black}N(0,1)}$.
> Using a table of values for the normal distribution, we require a value $c$ such that
> $P(Z > c) = {\color{black}0.025}$.
> In other words,
> $P(-c < Z < c) = 0.95$.
> Let's call $c = z_{0.025}$.
> Now, we find the appropriate confidence interval by solving the inequality inside the parentheses of the last probability statement for the difference mean $\mu$.
> $\begin{bmatrix}-z_{0.025} & < Z < z_{0.025} \\ -z_{0.025} & < ((x^(―) - y^(―)) - μ)/((\frac{1}{3})) < z_{0.025}\end{bmatrix}$
> We solve the inequality as follows:
> $-\frac{z_{0.025}}{3} \mid < (x^(―) - y^(―)) - μ < \frac{z_{0.025}}{3}$
> $\frac{z_{0.025}}{3} \mid > μ - (x^(―) - y^(―)) > - \frac{z_{0.025}}{3}$
> $\frac{z_{0.025}}{3} + x^(―) - y^(―) \mid > μ > - \frac{z_{0.025}}{3} + x^(―) - y^(―)$
> $x^(―) - y^(―) - \frac{z_{0.025}}{3} \mid < μ < x^(―) - y^(―) + \frac{z_{0.025}}{3}$
> Finally, we can write our interval as follows:
> $(x^(―) - y^(―)) ± z_{0.025} \cdot \frac{1}{3}$

---

**Question 5**

Consider two paired random samples $X_{1}, X_{2}, …, X_{10}$ and $Y_{1}, Y_{2}, …, Y_{10}$ of size $n = 10$. It is known that the differences between paired observations $D_{i} = X_{i} - Y_{i}$ are normally distributed with population mean $μ$ and population variance $σ^{2} = 2$.

Fill in the missing statements below to construct a $90%$ confidence interval for $μ$.

Consider the random variable
$\frac{1}{10}$ $\underline{\hspace{2cm}}$
where $X^(―)$ and $X^(―)$ are the sample means of the given samples.

We have that
$χ_{9}^{2}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: (1)/(10)]
- [ ] [MATH: (1)/(5)]
- [ ] [MATH: (1)/(sqrt(5))]
- [ ] [MATH: (2)/(sqrt(10))]

#### Select 2

- [ ] [MATH: χ_{9}^{2}]
- [ ] [MATH: t_{18}]
- [ ] [MATH: t_{9}]
- [ ] [MATH: N(0.1)]

---

**Question 6**

Consider two paired random samples $X_{1}, X_{2}, …, X_{9}$ and $Y_{1}, Y_{2}, …, Y_{9}$ of size $n = 9$. It is known that the differences between paired observations $D_{i} = X_{i} - Y_{i}$ are normally distributed with population mean $μ$ and population variance $σ^{2} = 3$.

Fill in the missing statements below to construct a $95%$ confidence interval for $μ$.

Consider the random variable
$\frac{1}{\sqrt{3}}$ $\underline{\hspace{2cm}}$
where $X^(―)$ and $Y^(―)$ are the sample means of the given samples.

We have that
$N(0.1)$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: (1)/(sqrt(3))]
- [ ] [MATH: (1)/(9)]
- [ ] [MATH: (1)/(2sqrt(3))]
- [ ] [MATH: (1)/(3)]

#### Select 2

- [ ] [MATH: N(0.1)]
- [ ] [MATH: t_{8}]
- [ ] [MATH: χ_{8}^{2}]
- [ ] [MATH: t_{16}]

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/PAS/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/PAS/0. Table of Contents/TOC|Table of Contents]]
