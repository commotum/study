# Estimating Sample Sizes for Proportions: Finite Population Correction

<!--
lesson-id: 3321
topic-code: PAS.7.3.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Minimum Sample Size When a Point Estimate Is Given](#finding-the-minimum-sample-size-when-a-point-estimate-is-given)
- [Finding the Minimum Sample Size When a Point Estimate Is Not Given](#finding-the-minimum-sample-size-when-a-point-estimate-is-not-given)
- [Finding the Minimum Sample Size When an Interval Estimate Is Given](#finding-the-minimum-sample-size-when-an-interval-estimate-is-given)

## Prerequisites

- [Estimating Sample Sizes for Proportions](<7.3.2. Estimating Sample Sizes for Proportions.md>)
- [Confidence Intervals for One Proportion: Finite Population Corrections](<../../7.1. One-Sample Procedures/Lessons/7.1.5. Confidence Intervals for One Proportion- Finite Population Corrections.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we have a finite population of size $N$ in which a proportion $p$ of individuals has a particular characteristic.

In a previous lesson, we saw that we can estimate the sampling distribution of $p$ as

$$
\widehat{\,p} \sim N\left(p, \dfrac{p(1-p)}{n}\cdot \dfrac{N-n}{N-1} \right)
$$

where

- $\!\widehat{\,p}$ is the sample proportion,
- $p$ is the population proportion,
- $N$ is the population size, and
- $n$ is the sample size.

Recall that the quantity

$$
\dfrac{N-n}{N-1}
$$

is the *finite population correction factor* applied to the variance of $\widehat{\,p}$. It is appropriate to apply this correction factor whenever $n > 5\%\cdot N$.

Now, recall that the $[100(1-\alpha)]\%$ confidence interval for $p$ in the cases where $n > 5\%\cdot N$ is given by

$$
\widehat{\,p} \pm \overbrace{z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n}\cdot \dfrac{N-n}{N-1}}}^{\textrm{margin of error}}
$$

where

$$
P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}
$$

and $Z\sim N(0,1)$.

Suppose we require the margin of error to be smaller than $\varepsilon$. Then, we must have

$$
z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n}\cdot \dfrac{N-n}{N-1}} \leq \varepsilon
$$

By isolating $n$, we can find the minimum sample size needed for the margin or error to be smaller than $\varepsilon$. We learned how to do this for large populations in a previous lesson.

To isolate $n$, we start by squaring both sides:

$$
z_{\alpha/2}^2 \cdot \dfrac{\widehat{\,p}(1-\widehat{\,p})}{n}\cdot \dfrac{N-n}{N-1} \leq \varepsilon^2
$$

Dividing this inequality by $\varepsilon^2$ and rearranging slightly, we get

$$
z_{\alpha/2}^2 \cdot \dfrac{\widehat{\,p}(1-\widehat{\,p})}{\varepsilon^2}\cdot \dfrac{N-n}{n(N-1)} \leq 1
$$

Now, we let

$$
m = z_{\alpha/2}^2 \cdot \dfrac{\widehat{\,p}(1-\widehat{\,p})}{\varepsilon^2}
$$

Then, our inequality becomes

$$
m\cdot \dfrac{N-n}{n(N-1)} \leq 1
$$

We can solve for $n$ as follows:

$$
\begin{bmatrix}m \cdot (N - n) & \le n(N - 1) \\ mN - mn & \le nN - n \\ mN & \le mn + nN - n \\ mN & \le n(m + N - 1) \\ n & \ge \frac{Nm}{m + N - 1}\end{bmatrix}
$$

You should practice deriving this result for yourself without referring to these notes.

Now, let's use this result to find some optimal sample sizes.

---

<a id="finding-the-minimum-sample-size-when-a-point-estimate-is-given"></a>
## Finding the Minimum Sample Size When a Point Estimate Is Given

**Example:** A scientist is studying a population of $630$ cats of a particular breed. They believe that $10\%$ of the cats have a certain genetic disorder.

Find a lower bound for the number of cats the scientist should examine to ensure that the margin of error for the $99\%$ confidence interval for the population proportion of the cats with the disorder is at most $0.02$.

*Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 2.576) = 0.005$, where $Z \sim N(0,1)$.*

**Explanation**

Given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the population proportion $p$ for a small population can be written as

$$
\widehat{\,p} \pm \overbrace{z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}}}^{\large \textrm{margin of error}}
$$

where

- $\!\widehat{\,p}$ is the sample proportion,
- $p$ is the population proportion,
- $n$ is the sample size,
- $N$ is the population size satisfying the condition $n > 0.05\cdot N$,
- $P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}$ and $Z \sim N(0,1)$.

If we require the margin of error to be at most $\varepsilon$, then

$$
z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}} \leq \varepsilon
$$

Doing some algebra to isolate $n$, we get

$$
n \geq \dfrac{Nm}{N + m - 1}
$$

where

$$
m = \dfrac{z_{\alpha/2}^2 \, \widehat{\,p}(1-\widehat{\,p})}{\varepsilon^2}
$$

is the minimum sample size needed when the population is large.

We are interested in finding a $99\%$ confidence interval. So, we have

$$
\alpha=1-0.99=0.01 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.005
$$

We are given that

$$
P(Z > 2.576) = 0.005
$$

As a result,

$$
z_{\alpha/2} = z_{0.005} = 2.576
$$

We're given the estimate

$$
\widehat{\,p} = 10\% = 0.1
$$

Therefore, substituting

$$
\varepsilon=0.02, \qquad \widehat{\,p}=0.1, \qquad z_{\alpha/2}=2.576
$$

into the inequality for $n$, we obtain

$$
\begin{aligned}
m &= (z_{α/2}^{2}p^(^)(1 - p^(^)))/(ε^{2}) \\
&= ((2.576)^{2}0.1(1 - 0.1))/((0.02)^{2}) \\
&\approx 1493.05, \\
&= n \mid \ge \frac{Nm}{N + m - 1} \\
&= \frac{630 \cdot 1493.05}{630 + 1493.05 - 1} \\
&\approx 443.26
\end{aligned}
$$

Finally, rounding up to the nearest integer, we get that $n=444$ or larger.

---

**Question 1**

```quiz
type: radio
id: ma-226147
content: |-
  > A scientific calculator is required to answer this question.

  Consider a population of size $N = 2000$ where it's known that approximately $36%$ of individuals have a particular characteristic.
  Find a lower bound for sample size $n$ required to ensure that the margin of error for the $95%$ confidence interval for the population proportion of individuals having the characteristic is at most $0.04$.

  *Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.96) = 0.025$, where $Z∼N(0, 1)$.*
options:
- id: a
  content: |-
    $n = 314$
- id: b
  content: |-
    $n = 510$
- id: c
  content: |-
    $n = 480$
- id: d
  content: |-
    $n = 434$
  correct: true
- id: e
  content: |-
    $n = 563$
```

---

**Question 2**

```quiz
type: radio
id: ma-251278
content: |-
  > A scientific calculator is required to answer this question.

  A farmer planted $N = 700$ vines of a new tomato variety. He believes that approximately $87%$ of vines will produce at least $30$ tomatoes in one season.

  Find a lower bound for the number of vines $n$ the farmer should count to ensure that the margin of error for the $95%$ confidence interval for the population proportion of vines that produced at least $30$ tomatoes is at most $0.04$.

  *Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.96) = 0.025$, where $Z∼N(0, 1)$.*
options:
- id: a
  content: |-
    $185$
- id: b
  content: |-
    $199$
- id: c
  content: |-
    $200$
- id: d
  content: |-
    $217$
- id: e
  content: |-
    $196$
  correct: true
```

---

<a id="finding-the-minimum-sample-size-when-a-point-estimate-is-not-given"></a>
## Finding the Minimum Sample Size When a Point Estimate Is Not Given

**Example:** Consider a population of size $N=900$ where some individuals have a particular characteristic. Find a lower bound for the sample size $n$ required to ensure that the margin of error for the $95\%$ confidence interval for the population proportion of individuals having the characteristic is at most $0.06$.

*Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.96) = 0.025$, where $Z \sim N(0,1)$.*

**Explanation**

Given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the population proportion $p$ for a small population can be written as

$$
\widehat{\,p} \pm \overbrace{z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}}}^{\large \textrm{margin of error}}
$$

where

- $\!\widehat{\,p}$ is the sample proportion,
- $p$ is the population proportion,
- $n$ is the sample size,
- $N$ is the population size satisfying the condition $n > 0.05\cdot N$,
- $P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}$ and $Z \sim N(0,1)$.

If we require the margin of error to be at most $\varepsilon$, then

$$
z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}} \leq \varepsilon
$$

Doing some algebra to isolate $n$, we get

$$
n \geq \dfrac{Nm}{N + m - 1}
$$

where

$$
m = \dfrac{z_{\alpha/2}^2 \, \widehat{\,p}(1-\widehat{\,p})}{\varepsilon^2}
$$

is the minimum sample size needed when the population is large.

We are interested in finding a $95\%$ confidence interval. So, we have

$$
\alpha=1-0.95=0.05 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.025
$$

We are given that

$$
P(Z > 1.96) = 0.025
$$

As a result,

$$
z_{\alpha/2} = z_{0.025} = 1.96
$$

We do not know $\widehat{\,p}$, and consequently, we do not know $\widehat{\,p}(1-\widehat{\,p})$ either. However, to find a lower bound for $n$, we can simply replace the expression $\widehat{\,p}(1-\widehat{\,p})$ with its maximum value for $\widehat{\,p} \in [0,1]$.

![](<../Source/7.3.3. Estimating Sample Sizes for Proportions- Finite Population Correction/Images/b3cea8d8474f28b947c7d2720a57985e.png>)

From the diagram, we see that the maximum value of $\widehat{\,p}(1-\widehat{\,p})$ occurs when

$$
\widehat{\,p} = \dfrac12
$$

and therefore, the maximum value of $\widehat{\,p}(1-\widehat{\,p})$ is

$$
\dfrac{1}{2} \cdot \left(1-\dfrac{1}{2}\right) = \dfrac{1}{4}
$$

Therefore, substituting

$$
\varepsilon=0.06, \qquad \widehat{\,p}=\dfrac 12, \qquad z_{\alpha/2}=1.96
$$

into the inequality for $n$, we obtain

$$
\begin{aligned}
m &= (z_{α/2}^{2}p^(^)(1 - p^(^)))/(ε^{2}) \\
&= ((1.96)^{2}0.25)/((0.06)^{2}) \\
&\approx 266.78, \\
&= n \mid \ge \frac{Nm}{N + m - 1} \\
&= \frac{900 \cdot 266.78}{900 + 266.78 - 1} \\
&\approx 205.96
\end{aligned}
$$

Finally, rounding up to the nearest integer, we get that $n=206$ or larger.

---

**Question 3**

```quiz
type: radio
id: ma-251304
content: |-
  > A scientific calculator is required to answer this question.

  Consider a population of size $N = 1000$ where some individuals have a particular characteristic. Find a lower bound for the sample size $n$ required to ensure that the margin of error for the $90%$ confidence interval for the population proportion of individuals having the characteristic is at most $0.07$.

  *Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.645) = 0.05$, where $Z∼N(0, 1)$.*
options:
- id: a
  content: |-
    $n = 134$
- id: b
  content: |-
    $n = 117$
- id: c
  content: |-
    $n = 138$
- id: d
  content: |-
    $n = 122$
  correct: true
- id: e
  content: |-
    $n = 129$
```

---

**Question 4**

```quiz
type: radio
id: ma-226164
content: |-
  > A scientific calculator is required to answer this question.

  A gardener has planted $N = 600$ trees of a new variety of cypresses, and she wants to estimate the proportion of trees that will reach at least one meter of height after one year.

  Find a lower bound for the number of trees $n$ the gardener has to measure after one year to ensure that the margin of error for the $90%$ confidence interval for the population proportion of trees that reached one meter of height is at most $0.05$.

  *Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.645) = 0.05$, where $Z∼N(0, 1)$.*
options:
- id: a
  content: |-
    $n = 176$
- id: b
  content: |-
    $n = 170$
- id: c
  content: |-
    $n = 192$
- id: d
  content: |-
    $n = 187$
  correct: true
- id: e
  content: |-
    $n = 163$
```

---

<a id="finding-the-minimum-sample-size-when-an-interval-estimate-is-given"></a>
## Finding the Minimum Sample Size When an Interval Estimate Is Given

**Example:** Consider a population of size $N=8\,000$ where it's known that between $10\%$ and $30\%$ of individuals have a particular characteristic.
Find a lower bound for sample size $n$ required to ensure that the margin of error for the $99\%$ confidence interval for the population proportion of individuals having the characteristic is at most $0.03$.

*Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 2.576) = 0.005$, where $Z \sim N(0,1)$.*

**Explanation**

Given a value $\alpha$ between $0$ and $1$, the corresponding $[100(1-\alpha)]\%$ confidence interval for the population proportion $p$ for a small population can be written as

$$
\widehat{\,p} \pm \overbrace{z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}}}^{\large \textrm{margin of error}}
$$

where

- $\!\widehat{\,p}$ is the sample proportion,
- $p$ is the population proportion,
- $n$ is the sample size,
- $N$ is the population size satisfying the condition $n > 0.05\cdot N$,
- $P(Z > z_{\alpha/2}) = \dfrac{\alpha}{2}$ and $Z \sim N(0,1)$.

If we require the margin of error to be at most $\varepsilon$, then

$$
z_{\alpha/2} \cdot \sqrt{\dfrac{\widehat{\,p}(1-\widehat{\,p})}{n} \cdot \dfrac{N-n}{N-1}} \leq \varepsilon
$$

Doing some algebra to isolate $n$ we get

$$
n \geq \dfrac{Nm}{N + m - 1}
$$

where

$$
m = \dfrac{z_{\alpha/2}^2 \, \widehat{\,p}(1-\widehat{\,p})}{\varepsilon^2}
$$

is the minimum sample size needed when the population is large.

We are interested in finding a $99\%$ confidence interval. So, we have

$$
\alpha=1-0.99=0.01 \qquad\Longrightarrow\qquad \dfrac{\alpha}{2}=0.005
$$

We are given that

$$
P(Z > 2.576) = 0.005
$$

As a result,

$$
z_{\alpha/2} = z_{0.005} = 2.576
$$

We have the estimate $p \in [0.1, 0.3]$. So, to found our lower bound for $n$, we replace $\widehat{\,p}(1-\widehat{\,p})$ with the maximum of the function

$$
f(p) = p(1-p)
$$

for $p\in[0.1, 0.3]$.

![](<../Source/7.3.3. Estimating Sample Sizes for Proportions- Finite Population Correction/Images/01ce939a1083b05a9d327d090f7bc100.png>)

Since $f$ increases in the given interval, the maximum occurs at $p=0.3$. Therefore, we replace $\widehat{\,p}(1-\widehat{\,p})$ with

$$
0.3(1-0.3)=0.21
$$

Therefore, substituting

$$
\varepsilon=0.03, \qquad \widehat{\,p}=0.3, \qquad z_{\alpha/2}= 2.576
$$

into the inequality for $n$, we obtain

$$
\begin{aligned}
m &= (z_{α/2}^{2}p^(^)(1 - p^(^)))/(ε^{2}) \\
&= ((2.576)^{2}0.3(1 - 0.3))/((0.03)^{2}) \\
&\approx 1548.35, \\
n &= \frac{Nm}{N + m - 1} \\
&\ge \frac{8000 \cdot 1548.35}{8000 + 1548.35 - 1} \\
&= 1297.41
\end{aligned}
$$

Finally, rounding up to the nearest integer, we get that $n=1298$ or larger.

---

**Question 5**

> A scientific calculator is required to answer this question.

Consider a population of size $N = 10000$ where it's known that between $10%$ and $20%$ of individuals have a particular characteristic.
Find a lower bound for sample size $n$ required to ensure that the margin of error for the $98%$ confidence interval for the population proportion of individuals having the characteristic is at most $0.02$.

*Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 2.326) = 0.01$, where $Z∼N(0, 1)$.*

$n_{min} =$
$\underline{\hspace{1.5cm}}$

---

**Question 6**

> A scientific calculator is required to answer this question.

A farmer has planted $N = 1200$ corn plants and believes that the proportion of plants that will produce corn cobs is between $0.60$ and $0.75$.

Find a lower bound for the number of corn plants $n$ the farmer needs to inspect to ensure that the margin of error for the $95%$ confidence interval for the population proportion of corn plants that produce corn cobs is at most $0.02$.

*Hint: Use a finite population correction factor. You may also use the fact that $P(Z > 1.96) = 0.025$, where $Z∼N(0, 1)$.*

$n_{min} =$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/PAS/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/PAS/0. Table of Contents/TOC|Table of Contents]]
