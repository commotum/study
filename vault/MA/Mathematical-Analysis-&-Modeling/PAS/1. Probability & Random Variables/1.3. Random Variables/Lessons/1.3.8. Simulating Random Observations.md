# Simulating Random Observations

<!--
lesson-id: 3867
topic-code: PAS.1.3.8
-->

## Table of Contents

- [Introduction](#introduction)
- [Simulating Observations Given a CDF](#simulating-observations-given-a-cdf)
- [Simulating Observations Given a PDF](#simulating-observations-given-a-pdf)

## Prerequisites

- [Inverses of Exponential and Logarithmic Functions](<../../../../AG2/4. Exponentials & Logarithms/4.6. Graphs of Logarithmic Functions/Lessons/4.6.4. Inverses of Exponential and Logarithmic Functions.md>)
- [Cumulative Distribution Functions for Continuous Random Variables](<1.3.4. Cumulative Distribution Functions for Continuous Random Variables.md>)
- [Inverses of Quadratic Functions](<../../../../AG2/3. Functions/3.1. Functions/Lessons/3.1.7. Inverses of Quadratic Functions.md>)
- [Inverses of Radical Functions](<../../../../AG2/6. Radical Expressions & Functions/6.2. Graphs of Radical Functions/Lessons/6.2.8. Inverses of Radical Functions.md>)
- [Inverses of Reciprocal Functions](<../../../../AG2/5. Rational Expressions & Functions/5.2. Reciprocal Functions/Lessons/5.2.5. Inverses of Reciprocal Functions.md>)

---

<a id="introduction"></a>
## Introduction

Most statistical computer packages have functionality that allows data from a particular probability distribution to be simulated. In this lesson, we'll learn how this process works.

For example, suppose we have a random variable with the cumulative distribution function

$$
F(x) = \dfrac{1}{1+e^{-x}}\,, \qquad x \in (-\infty, \infty)
$$

The graph of $y=F(x)$ is shown below:

![](<../Source/1.3.8. Simulating Random Observations/Images/909c7d3e196b4253805c60a4ae56a0e4.png>)

Suppose we want to generate a sample of size $n=3$ from this distribution. To do this, we proceed as follows:

- First, draw a random sample of size $n=3$ from the uniform distribution $Y \sim U[0,1]$.
- Then, find the values $x_i$ such that $y_i=F(x_i)$. In other words,
$x_i=F^{-1}(y_i), \qquad i=1,2,3$.

Note that $F(x)$ is strictly increasing and therefore $F^{-1}(y)$ exists for all $y\in (0,1)$.

Suppose that we run our simulation and generate $n=3$ numbers from $Y\sim U[0,1]$, and that our three numbers (rounded to $3$ decimal places) are

$$
y_1 = 0.214, \qquad y_2 = 0.707, \qquad y_3 = 0.819
$$

We'll now use this data to generate three numbers $x_1, x_2$, and $x_3$ drawn from the distribution with CDF $F(x)$. The three $x_i$'s are the numbers shown in the diagram below.

![](<../Source/1.3.8. Simulating Random Observations/Images/1b5938a05499ec0d20b10c070bf13bf6.png>)

To compute our $x_i$'s, we first need to find the inverse of $F(x)$. So we first write $y=F(x)$:

$$
y = \frac{1}{1 + e^{-x}}
$$

Solving this equation for $x$, we get

$$
\begin{aligned}
1 + e^{-x} &= \frac{1}{y} \\
e^{-x} &= \frac{1}{y} - 1 \\
e^{-x} &= \frac{1 - y}{y} \\
- x &= \ln (\frac{1 - y}{y}) \\
x &= -\ln (\frac{1 - y}{y}) \\
x &= \ln (\frac{1 - y}{y})^{-1} \\
x &= \ln (\frac{y}{1 - y})
\end{aligned}
$$

where $0 < y < 1$.

Finally, substituting the values $y_1,y_2,y_3$ into the formula for

$$
x=F^{-1}(y)
$$

we obtain

$$
x_1 \approx -1.301, \qquad x_2 \approx 0.881, \qquad x_3 \approx 1.510
$$

The algorithm above can be formalized in the following theorem:

> *Let the random variable $Y \sim U[0,1]$ be uniformly distributed. Suppose that the function $F(x)$ is a cumulative distribution function, where $F(a) = 0, F(b) = 1$, and $F$ is strictly monotonic on $a \lt x \lt b$, where $a$ and $b$ could be infinite. Then, the random variable $X$, given by $X = F^{-1}(Y)$ is a continuous random variable with the cumulative distribution function $F(x)$.*

---

<a id="simulating-observations-given-a-cdf"></a>
## Simulating Observations Given a CDF

**Example:** Suppose the following random sample was drawn from the uniform distribution $U[0,1]$.
$0.352, \quad 0.567, \quad 0.801$

Use these numbers to generate three random numbers that follow a probability distribution with the CDF $F(x)$, given by
${0,\begin{vmatrix}x < 1, \\ 2 - \frac{2}{x},\end{vmatrix}1 \le x \le 2,; 1, \mid x > 2$.

Round your answers to $3$ decimal places.

**Explanation**

Let the random variable $Y \sim U[0,1]$ be uniformly distributed. Suppose that the function $F(x)$ is a cumulative distribution function, where

$$
F(a) = 0, F(b) = 1
$$

and $F$ is strictly monotonic on $a < x < b$. Then, the random variable $X$, given by

$$
X = F^{-1}(Y)
$$

is a continuous random variable with the cumulative distribution function $F(x)$.

So, we need to find the inverse of $y = F(x)$. To do this, we solve the following equation for $x$, taking into account that

$$
1 \leq x \leq 2
$$

and $0 \lt y \lt 1{:}$

$$
\begin{aligned}
y &= 2 - \frac{2}{x} \\
y - 2 &= -\frac{2}{x} \\
2 - y &= \frac{2}{x} \\
x &= \frac{2}{2 - y}
\end{aligned}
$$

Now, we substitute the numbers drawn from $U[0,1]$ into the above equation:

- For $y = 0.352$, we get $x = \dfrac{2}{2 - 0.352} \approx 1.214$
- For $y = 0.567$, we get $x = \dfrac{2}{2 - 0.567} \approx 1.396$
- For $y = 0.801$, we get $x = \dfrac{2}{2 - 0.801} \approx 1.668$

---

**Question 1**

> A scientific calculator is required to answer this question.

Suppose the following random sample was drawn from the uniform distribution $U[0, 1]$.

$0.103, 0.534, 0.847$

Use these numbers to generate three random numbers that follow a probability distribution with the CDF $F(x)$, given by

$F(x) = \frac{\arctan x}{π} + \frac{1}{2}, x ∈ (- ∞, ∞)$.

Round your answers to $3$ decimal places.

- [ ] A. $-3.511, 0.761, 1.157$
- [ ] B. $-1.125, 0.097, 2.156$
- [ ] C. $-2.125, 0.791, 2.117$
- [ ] D. $-3.237, 0.713, 1.584$
- [ ] E. $-2.982, 0.107, 1.918$

---

**Question 2**

> A scientific calculator is required to answer this question.

Suppose the following random sample was drawn from the uniform distribution $U[0, 1]$.

$0.224, 0.389, 0.701$

Use these numbers to generate three random numbers that follow a probability distribution with the CDF $F(x)$, given by

$F(x) = {0,\begin{vmatrix}x < 2, \\ 1 - (16)/((2 + x)^{2}),\end{vmatrix}x \ge 2$.

Round your answers to $3$ decimal places.

- [ ] A. $0.281, 0.546, 0.771$
- [ ] B. $1.121, 3.015, 4.134$
- [ ] C. $2.901, 4.415, 7.401$
- [ ] D. $1.471, 2.401, 5.199$
- [ ] E. $2.541, 3.117, 5.315$

---

<a id="simulating-observations-given-a-pdf"></a>
## Simulating Observations Given a PDF

**Example:** Suppose the following random sample was drawn from the uniform distribution $U[0,1]$.
$0.354, \quad 0.593, \quad 0.792$
Use these numbers to generate three random numbers that follow a probability distribution with the PDF $f(x)$, given by
${\cos x,\begin{vmatrix}0 \le x \le \frac{π}{2}, \\ 0,\end{vmatrix}otherwise$.
Round your answers to $3$ decimal places.

**Explanation**

Let the random variable $Y \sim U[0,1]$ be uniformly distributed. Suppose that the function $F(x)$ is a cumulative distribution function, where

$$
F(a) = 0, F(b) = 1
$$

and $F$ is strictly monotonic on $a < x < b$. Then, the random variable $X$, given by

$$
X = F^{-1}(Y)
$$

is a continuous random variable with the cumulative distribution function $F(x)$.

First, we must find the cumulative distribution function $F(x)$ corresponding to our random variable. Since our PDF $f(x)$ is non-negative only for

$$
0 \leq x \leq \dfrac{\pi}{2}
$$

we have

$$
{0,\begin{vmatrix}x < 0, \\ ∫_{0}^{x}f(t)dt,\end{vmatrix}0 \le x \le \frac{π}{2},; 1, \mid x > \frac{π}{2}
$$

Evaluating the integral, we obtain

$$
\begin{aligned}
∫_{0}^{x}f(t)dt &= ∫_{0}^{x}\cos tdt \\
&= \sin t \mid _{0}^{x} \\
&= \sin x
\end{aligned}
$$

So, the cumulative distribution function is given by

$$
{0,\begin{vmatrix}x < 0, \\ \sin x,\end{vmatrix}0 \le x \le \frac{π}{2},; 1, \mid x > \frac{π}{2}
$$

Next, we need to find the inverse of $y = F(x)$. So, we solve the following equation for $x$, taking into account that

$$
0 \leq x \leq \dfrac{\pi}{2}
$$

and $0 \lt y \lt 1{:}$

$$
\begin{aligned}
y &= \sin x \\
x &= \arcsin y
\end{aligned}
$$

Now, we substitute the numbers drawn from $U[0,1]$ into the above equation:

- For $y = 0.354$, we get $x = \arcsin(0.354) \approx 0.362$
- For $y = 0.593$, we get $x = \arcsin(0.593) \approx 0.635$
- For $y = 0.792$, we get $x = \arcsin(0.792) \approx 0.914$

---

**Question 3**

> A scientific calculator is required to answer this question.

Suppose the following random sample was drawn from the uniform distribution $U[0, 1]$.

$0.231, 0.412, 0.602$
Use these numbers to generate three random numbers that follow a probability distribution with the PDF $f(x)$, given by

$f(x) = {\frac{x^{2}}{72},\begin{vmatrix}0 \le x \le 6, \\ 0,\end{vmatrix}otherwise$.

Round your answers to $3$ decimal places.

- [ ] A. $2.391, 5.149, 7.102$
- [ ] B. $-2.247, 3.482, 6.182$
- [ ] C. $3.681, 4.465, 5.066$
- [ ] D. $-5.456, 2.185, 4.401$
- [ ] E. $2.115, 3.119, 5.044$

---

**Question 4**

> A scientific calculator is required to answer this question.

Suppose the following random sample was drawn from the uniform distribution $U[0, 1]$.
$0.241, 0.532, 0.824$
Use these numbers to generate three random numbers that follow a probability distribution with the PDF $f(x)$, given by
$f(x) = {\sin x,\begin{vmatrix}0 \le x \le \frac{π}{2}, \\ 0,\end{vmatrix}otherwise$.
Round your answers to $3$ decimal places.

- [ ] A. $0.678, 1.141, 1.356$
- [ ] B. $0.897, 0.978, 1.076$
- [ ] C. $0.709, 1.084, 1.394$
- [ ] D. $0.699, 1.256, 1.463$
- [ ] E. $0.914, 1.146, 1.497$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/PAS/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/PAS/0. Table of Contents/TOC|Table of Contents]]
