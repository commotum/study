# Properties of Moment-Generating Functions

<!--
lesson-id: 3070
topic-code: PAS.2.2.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the MGF of a Linearly Transformed Random Variable](#finding-the-mgf-of-a-linearly-transformed-random-variable)
- [Sums of Independent Random Variables](#sums-of-independent-random-variables)
- [Finding the MGF of a Sum of Random Variables](#finding-the-mgf-of-a-sum-of-random-variables)
- [Further Properties of Moment-Generating Functions](#further-properties-of-moment-generating-functions)
- [Finding the MGF of a Linear Combination of Random Variables](#finding-the-mgf-of-a-linear-combination-of-random-variables)

## Prerequisites

- [Constructing Moment-Generating Functions for Continuous Probability Distributions](<2.2.5. Constructing Moment-Generating Functions for Continuous Probability Distributions.md>)
- [Independence of Continuous Random Variables](<../../../5. Combining Random Variables/5.2. Distributions of Two Continuous Random Variables/Lessons/5.2.3. Independence of Continuous Random Variables.md>)

---

<a id="introduction"></a>
## Introduction

If we know the moment-generating function (MGF) for a random variable $X$, then we can easily find the MGF for any linear transformation of $X$ using the following theorem:

> *If $Y = aX+b$, where $X$ is a random variable with the MGF $M_X(t)$, then the MGF of $Y$ is
> $M_Y(t) = e^{bt} M_X(at)$.*

For example, consider a binomial random variable $X \sim B(0.3, 4)$, which has the MGF

$$
M_X(t) = (0.7 + 0.3e^t)^4
$$

If we have another random variable

$$
Y = 5X+6
$$

then we can use the theorem above to conclude that the MGF for $Y$ is

$$
\begin{aligned}
M_{Y}(t) &= e^{6t}M_{X}(5t) \\
&= e^{6t}M_{X}(5t) \\
&= e^{6t}(0.7 + 0.3e^{5t})^{4}
\end{aligned}
$$

To see where this theorem comes from, we apply the definition of the MGF:

$$
\begin{aligned}
M_{Y}(t) &= E[e^{tY}] \\
&= E[e^{t(aX + b)}] \\
&= E[e^{taX} \cdot e^{tb}] \\
&= e^{tb} \cdot E[e^{(at)X}] \\
&= e^{tb} \cdot M_{X}(at)
\end{aligned}
$$

Note that for a fixed value of $t$, we have that $e^{tb}$ is constant (i.e., it does not depend on $X$), which allows us to take it outside the expectation operator.

---

<a id="finding-the-mgf-of-a-linearly-transformed-random-variable"></a>
## Finding the MGF of a Linearly Transformed Random Variable

**Example:** You're given that if $X \sim \textrm{Exp}(\lambda)$, the moment-generating function (MGF) of $X$ is
$M_X(t) = \dfrac{\lambda}{\lambda - t}, \qquad t < \lambda$.

Let $Y = 3X+2$, where $X \sim \textrm{Exp}(2)$. What is the MGF of $Y$ for $t < \dfrac23$?

**Explanation**

If

$$
Y = aX+b
$$

where $X$ is a random variable with the MGF $M_X(t)$, then the MGF of $Y$ is

$$
M_Y(t) = e^{bt} M_X(at)
$$

For an exponential random variable $X \sim \textrm{Exp}(\lambda)$, we have

$$
M_X(t) = \dfrac{\lambda}{\lambda - t}, \qquad t < \lambda
$$

In our case, since $X \sim \textrm{Exp}(2)$, we have

$$
M_X(t) = \dfrac{2}{2 - t}, \qquad t < 2
$$

Therefore, since

$$
Y = 3X+2
$$

we have

$$
\begin{aligned}
M_{Y}(t) &= e^{2t}M_{X}(3t) \\
&= e^{2t} \cdot \frac{2}{2 - 3t} \\
&= \frac{2e^{2t}}{2 - 3t}
\end{aligned}
$$

where we assume that $3t < 2$. Finally, we get

$$
M_{Y}(t) = \frac{2e^{2t}}{2 - 3t}, t < \frac{2}{3}
$$

---

**Question 1**

You're given that if $X∼U{x_{1}, x_{2}, …, x_{n}}$, the moment-generating function (MGF) of $X$ is
$M_{X}(t) = \frac{1}{n}(e^{x_{1}t} + e^{x_{2}t} + … + e^{x_{n}t}), t ∈ R$.
Let $Y = \frac{X}{3} + 5$, where $X∼U{3, 6, 9}$. What is the MGF of $Y$ for $t ∈ R$?$M_{Y}(t) =$
$\underline{\hspace{1.5cm}}$

---

**Question 2**

```quiz
type: radio
id: ma-108874
content: |-
  You're given that if $X∼Bernoulli(p)$, the moment-generating function (MGF) of $X$ is
  $M_{X}(t) = 1 - p + pe^{t}, t ∈ R$.

  Let $Y = 2X - 4$, where $X∼Bernoulli(\frac{1}{4})$. What is the MGF of $Y$ for $t ∈ R$?
options:
- id: a
  content: |-
    $M_{Y}(t) = \frac{3}{4} + \frac{1}{4}e^{4t}$
- id: b
  content: |-
    $M_{Y}(t) = \frac{1}{4}e^{-2t} + \frac{3}{4}e^{2t}$
- id: c
  content: |-
    $M_{Y}(t) = \frac{3}{4}e^{2t} + \frac{1}{4}e^{4t}$
- id: d
  content: |-
    $M_{Y}(t) = \frac{1}{4}e^{-4t} + \frac{3}{4}e^{-2t}$
- id: e
  content: |-
    $M_{Y}(t) = \frac{3}{4}e^{-4t} + \frac{1}{4}e^{-2t}$
  correct: true
```

---

<a id="sums-of-independent-random-variables"></a>
## Sums of Independent Random Variables

If we know the moment-generating functions (MGFs) for independent random variables $X_1$ and $X_2$, then we can easily find the MGF for their sum using the following theorem:

> *If $Y = X_1 + X_2$, where $X_1$ and $X_2$ are independent random variables with MGF's $M_{X_1}(t)$ and $M_{X_2}(t)$, respectively, then the MGF of $Y$ is
> $M_Y(t) = M_{X_1}(t) M_{X_2}(t)$.*

Let's prove this when $X_1$ and $X_2$ are continuous (the proof is similar for discrete random variables). We start with the definition of the MGF:

$$
M_Y(t) = \textrm E \left[ e^{tY} \right] = \textrm E \left[ e^{t(X_1 + X_2)} \right]
$$

Now, if $X_1$ defined on $[a,b]$ and $X_2$ is defined on $[c,d]$, then by the definition of the expected value, we have

$$
\textrm E \left[ e^{t(X_1 + X_2)} \right] = \int_c^d \int_a^b f_{X_1,X_2} (x_1, x_2) e^{t(x_1 + x_2)} \, \textrm dx_1 \textrm dx_2
$$

where $f_{X_1,X_2}(x_1,x_2)$ is the joint probability density function of $X_1$ and $X_2$.

Finally, since $X_1$ and $X_2$ are independent, we have

$$
f_{X_1,X_2}(x_1,x_2) = f_{X_1}(x_1) f_{X_2}(x_2)
$$

and therefore

$$
\begin{aligned}
M_{Y}(t) &= ∫_{c}^{d}∫_{a}^{b}f_{X_{1}, X_{2}}(x_{1}, x_{2})e^{t(x_{1} + x_{2})}dx_{1}dx_{2} \\
&= ∫_{c}^{d}∫_{a}^{b}f_{X_{1}}(x_{1})f_{X_{2}}(x_{2})e^{t(x_{1} + x_{2})}dx_{1}dx_{2} \\
&= ∫_{c}^{d}∫_{a}^{b}f_{X_{1}}(x_{1})f_{X_{2}}(x_{2})e^{tx_{1}}e^{tx_{2}}dx_{1}dx_{2} \\
&= ∫_{a}^{b}f_{X_{1}}(x_{1})e^{tx_{1}}dx_{1} \cdot ∫_{c}^{d}f_{X_{2}}(x_{2})e^{tx_{2}}dx_{2} \\
&= E[e^{tX_{1}}] \cdot E[e^{tX_{2}}] \\
&= M_{X_{1}}(t)M_{X_{2}}(t)
\end{aligned}
$$

---

<a id="finding-the-mgf-of-a-sum-of-random-variables"></a>
## Finding the MGF of a Sum of Random Variables

**Example:** You're given that if $X \sim \textrm{Po}(\lambda)$, the moment-generating function (MGF) of $X$ is
$M(t) = e^{\lambda(e^t-1)}, \qquad t\in\mathbb R$.

Let $Y = X_1 + X_2$, where $X_1 \sim \textrm{Po}(4)$ and $X_2 \sim \textrm{Po}(7)$ are independent. What is the moment-generating function (MGF) of $Y$ for $t\in \mathbb{R}$?

**Explanation**

If

$$
Y = X_1 + X_2
$$

where $X_1$ and $X_2$ are independent random variables with MGF's $M_{X_1}(t)$ and $M_{X_2}(t)$, respectively, then the MGF of $Y$ is

$$
M_Y(t) = M_{X_1}(t) M_{X_2}(t)
$$

For a Poisson random variable $X_1 \sim \textrm{Po}(\lambda)$, we have

$$
M_{X_1}(t) = e^{\lambda(e^t-1)}
$$

In our case, since $X_1 \sim \textrm{Po}(4)$, we have

$$
M_{X_1}(t) = e^{4(e^t-1)}
$$

Then, since $X_2 \sim \textrm{Po}(7)$, we have

$$
M_{X_2}(t) = e^{7(e^t-1)}
$$

Therefore, we have

$$
\begin{aligned}
M_{Y}(t) &= e^{4(e^{t} - 1)} \cdot e^{7(e^{t} - 1)} \\
&= e^{11(e^{t} - 1)}
\end{aligned}
$$

---

**Question 3**

```quiz
type: radio
id: ma-108876
content: |-
  You're given that if $X∼χ^{2}(k)$, the moment-generating function (MGF) of $X$ is
  $M_{X}(t) = (1 - 2t)^{-k/2}, t < \frac{1}{2}$.

  Let $Y = X_{1} + X_{2}$, where $X_{1}∼χ^{2}(8)$ and $X_{2}∼χ^{2}(12)$ are independent. What is the MGF of $Y$ for $t < \frac{1}{2}$.
options:
- id: a
  content: |-
    $M_{Y}(t) = (1 - 2t)^{-4}$
- id: b
  content: |-
    $M_{Y}(t) = (1 - 2t)^{-6}$
- id: c
  content: |-
    $M_{Y}(t) = (1 - 2t)^{-10}$
  correct: true
- id: d
  content: |-
    $M_{Y}(t) = (1 - 2t)^{6}$
- id: e
  content: |-
    $M_{Y}(t) = (1 - 2t)^{2}$
```

---

**Question 4**

You're given that if $X∼N(μ, σ^{2})$, the moment-generating function (MGF) of $X$ is
$M_{X}(t) = e^{μt + \frac{1}{2}σ^{2}t^{2}}, t ∈ R$.

Let $Y = X_{1} + X_{2}$, where $X_{1}∼N(5, 2^{2})$ and $X_{2}∼N(3, 6^{2})$ are independent. What is the moment-generating function of $Y$ for $t ∈ R$?

$M_{Y}(t) =$
$\underline{\hspace{1.5cm}}$

---

<a id="further-properties-of-moment-generating-functions"></a>
## Further Properties of Moment-Generating Functions

If we know the moment-generating functions (MGF's) for independent random variables $X_1$ and $X_2$, then we can easily find the MGF for any linear combination of them using the following theorem:

> *If $Y = aX_1 + bX_2$, where $X_1$ and $X_2$ are independent random variables with MGF's $M_{X_1}(t)$ and $M_{X_2}(t)$, respectively, then the MGF of $Y$ is
> $M_Y(t) = M_{X_1}(at) M_{X_2}(bt)$.*

Note that this is just a consequence of using the two formulas we've already covered. Since $Y$ is a sum of independent random variables $aX_1$ and $bX_2$, we have

$$
M_Y = M_{aX_1}(t) M_{bX_2} (t)
$$

and since

$$
M_{aX}(t) = M_X(at)
$$

for any random variable $X$, we have

$$
M_Y = M_{X_1}(at) M_{X_2} (bt)
$$

---

<a id="finding-the-mgf-of-a-linear-combination-of-random-variables"></a>
## Finding the MGF of a Linear Combination of Random Variables

**Example:** You're given that if $X\sim\chi^2(k)$, the moment-generating function (MGF) of $X$ is
$M(t) = (1-2t)^{-k/2}, \qquad t < \dfrac12$.

Let $Y = 8X_1 - 2X_2$, where $X_1 \sim \chi^2(4)$ and $X_2 \sim \chi^2(2)$ are independent. What is the moment-generating function (MGF) of $Y$ for $-\dfrac14 < t < \dfrac1{16}$?

**Explanation**

If

$$
Y = a_1X_1 + a_2X_2
$$

where $X_1$ and $X_2$ are independent random variables with MGF's $M_{X_1}(t)$ and $M_{X_2}(t)$, respectively, and $a_1$ and $a_2$ are constants, then the MGF of $Y$ is

$$
M_Y(t) = M_{X_1}(a_1t) M_{X_2}(a_2t)
$$

Here, we're given

$$
Y = 8X_1 - 2X_2
$$

so we have

$$
M_Y(t) = M_{X_1}(8t) M_{X_2}(-2t)
$$

If $X \sim \chi^2(k)$, then the MGF of $X$ is given by

$$
M_X(t) = (1-2t)^{-k/2}, \qquad t < \dfrac12
$$

Since $X_1 \sim \chi^2(4)$, we have

$$
\begin{aligned}
M_{X_{1}}(t) &= (1 - 2t)^{-2}, t < \frac{1}{2} \\
M_{X_{1}}(8t) &= (1)/((1 - 16t)^{2}), t < \frac{1}{16}
\end{aligned}
$$

Likewise, since $X_2 \sim \chi^2(2)$, we have

$$
\begin{aligned}
M_{X_{2}}(t) &= (1 - 2t)^{-1}, t < \frac{1}{2} \\
M_{X_{2}}(-2t) &= (1)/((1 + 4t)), t > - \frac{1}{4}
\end{aligned}
$$

Therefore, for

$$
-\dfrac14 < t < \dfrac1{16}
$$

we have

$$
\begin{aligned}
M_{Y}(t) &= (1)/((1 - 16t)^{2}(1 + 4t))
\end{aligned}
$$

That is,

$$
M_Y(t) = \dfrac{1}{(1-16t)^{2}(1+4t)}, \quad-\dfrac14 < t < \dfrac1{16}
$$

---

**Question 5**

You're given that if $X∼Bernoulli(p)$, the moment-generating function (MGF) of $X$ is
$M(t) = 1 - p + pe^{t}, t ∈ R$.

Let $Y = X_{1} - 3X_{2}$, where $X_{1}∼Bernoulli(0.25)$ and $X_{2}∼Bernoulli(0.75)$ are independent. What is the MGF of $Y$ for $t ∈ R$?

$M_{Y}(t) =$
$\underline{\hspace{1.5cm}}$

---

**Question 6**

```quiz
type: radio
id: ma-108706
content: |-
  You're given that if $X∼U[a, b]$, the moment-generating function (MGF) of $X$ is
  $M(t) = {(e^{bt} - e^{at})/((b - a)t),\begin{vmatrix}t \ne 0, \\ 1,\end{vmatrix}t = 0$.

  Let $Y = 2X_{1} + 4X_{2}$, where $X_{1}∼U[2, 6]$ and $X_{2}∼U[0, 2]$ are independent. What is the moment-generating function of $Y$ for $t \ne 0$?
options:
- id: a
  content: |-
    $M_{Y}(t) = ((1 + e^{4t})(e^{8t} - 1))/(8t)$
- id: b
  content: |-
    $M_{Y}(t) = (e^{2t}(e^{6t} - 1)^{2})/(4t^{2})$
- id: c
  content: |-
    $M_{Y}(t) = (e^{4t}(e^{8t} + 1)^{2})/(64t)$
- id: d
  content: |-
    $M_{Y}(t) = e^{4t}$
- id: e
  content: |-
    $M_{Y}(t) = (e^{4t}(e^{8t} - 1)^{2})/(64t^{2})$
  correct: true
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/PAS/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/PAS/0. Table of Contents/TOC|Table of Contents]]
