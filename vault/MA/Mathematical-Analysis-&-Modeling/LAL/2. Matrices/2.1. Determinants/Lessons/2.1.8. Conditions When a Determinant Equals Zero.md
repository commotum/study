# Conditions When a Determinant Equals Zero

<!--
lesson-id: 2201
topic-code: LAL.2.1.8
-->

## Table of Contents

- [Introduction](#introduction)
- [Determinants with Identical Rows or Identical Columns](#determinants-with-identical-rows-or-identical-columns)
- [Calculating the Determinant of a Matrix With A Zero Row/Column or Two Identical Rows/Columns](#calculating-the-determinant-of-a-matrix-with-a-zero-rowcolumn-or-two-identical-rowscolumns)
- [Determinants with Proportional Rows/Columns](#determinants-with-proportional-rowscolumns)
- [Calculating the Determinant of a Matrix With Two Proportional Rows/Columns](#calculating-the-determinant-of-a-matrix-with-two-proportional-rowscolumns)
- [Conditions When a Determinant Equals Zero](#conditions-when-a-determinant-equals-zero)
- [Geometric Interpretation of a Zero Determinant](#geometric-interpretation-of-a-zero-determinant)
- [Identifying the Reasons Why a Determinant Equals Zero](#identifying-the-reasons-why-a-determinant-equals-zero)

## Prerequisites

- [Volumes of Parallelepipeds](<../../../../PCL/5. Vectors/5.5. The Cross Product/Lessons/5.5.6. Volumes of Parallelepipeds.md>)
- [Row and Column Operations on Determinants](<2.1.7. Row and Column Operations on Determinants.md>)

---

<a id="introduction"></a>
## Introduction

If an $n\!\times \!n$ matrix $A$ has an entire row or column of zeros, then its determinant is equal to zero.

For example, consider the matrix $A$ given below:

$$
\begin{bmatrix}∗ & ∗ & ∗ \\ 0 & 0 & 0 \\ ∗ & ∗ & ∗\end{bmatrix}
$$

The second row consists of all zeros, and the asterisks ($\ast$) may represent any number.

Let's compute the determinant using the Laplace expansion across the second row:

$$
\begin{aligned}
det(A) &= \begin{vmatrix}∗ & ∗ & ∗ \\ 0 & 0 & 0 \\ ∗ & ∗ & ∗\end{vmatrix} \\
&= (-1)^{2 + 1}(0)\begin{vmatrix}∗ & ∗ \\ ∗ & ∗\end{vmatrix}+ (-1)^{2 + 2}(0)\begin{vmatrix}∗ & ∗ \\ ∗ & ∗\end{vmatrix}+ (-1)^{2 + 3}(0)\begin{vmatrix}∗ & ∗ \\ ∗ & ∗\end{vmatrix} \\
&= 0 + 0 + 0 \\
&= 0
\end{aligned}
$$

Notice that it doesn't matter what numbers are represented by the asterisks ($\ast$). Each cofactor is multiplied by zero, so the final result must be zero.

---

<a id="determinants-with-identical-rows-or-identical-columns"></a>
## Determinants with Identical Rows or Identical Columns

Now, suppose a matrix has no zero rows but it has two identical rows:

$$
\begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ 1 & 2 & 1\end{bmatrix}
$$

We can subtract the $1$st row from the $3$rd row, as follows:

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ 1 & 2 & 1\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-1)R_{1} \\
&= ∼\begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ 0 & 0 & 0\end{bmatrix} \\
&= B
\end{aligned}
$$

Since this operation does not change the determinant, we get

$$
\det(A) = \det(B)
$$

Furthermore, as $B$ contains a zero row, we obtain

$$
\det(A) = \det(B) = 0
$$

Notice that we can use this method with columns too. Therefore, we have the following property:

> *If a square matrix has at least two identical rows/columns, its determinant equals zero.*

---

<a id="calculating-the-determinant-of-a-matrix-with-a-zero-rowcolumn-or-two-identical-rowscolumns"></a>
## Calculating the Determinant of a Matrix With A Zero Row/Column or Two Identical Rows/Columns

**Example:** Find $5\det(A)-\det(B)$, where
$\begin{bmatrix}4 & \sqrt{13} & 4 \\ 4 & 11 & 4 \\ \sqrt{3} & 9 & \sqrt{3}\end{bmatrix}$

**Explanation**

Let's examine each matrix in turn.

- Since $A$ has two identical columns, we obtain
$\begin{bmatrix}4 & \sqrt{13} & 4 \\ 4 & 11 & 4 \\ \sqrt{3} & 9 & \sqrt{3}\end{bmatrix}$

- Since $B$ is a diagonal matrix, its determinant is given by the product of the elements of the main diagonal:
$\det(B) = 3 \cdot (-4) \cdot (-2) = 24$

Therefore,

$$
5\det(A)-\det(B) = 5 \cdot 0 - 24 = -24
$$

---

**Question 1:** Find $2det(A) + 4det(B)$, where
$A = \begin{bmatrix}1 & 0 & \sqrt{3} & 1 \\ 0 & 2 & 4 & 2 \\ 0 & 0 & 2 & -4 \\ 0 & 0 & 0 & 1\end{bmatrix}, B = \begin{bmatrix}3 & \sqrt{2} & 2 & 3 \\ 1 & 2 & 1 & 3 \\ 3 & 1 & 0 & \sqrt{2} \\ 1 & 2 & 1 & 3\end{bmatrix}$.

- [ ] A. $8$
- [ ] B. $6$
- [ ] C. $10$
- [ ] D. $0$
- [ ] E. $14$

---

**Question 2**

Which of the following determinants are equal to $0$?

$\begin{vmatrix}A|= & 2 & 0\end{vmatrix}1; \sqrt{5}\begin{vmatrix}0 & 4 \\ -1 & 0\end{vmatrix}0\begin{vmatrix}, & B|= & 3 & 0\end{vmatrix}0; 0\begin{vmatrix}-1 & 0 \\ 0 & 0\end{vmatrix}5\begin{vmatrix}, & C|= & 4 & \sqrt{3}\end{vmatrix}4; 2\begin{vmatrix}4 & 2 \\ 1 & \sqrt{5}\end{vmatrix}1 \mid$

- [ ] A. $\mid A \mid$ only
- [ ] B. $\mid B \mid$ only
- [ ] C. $\mid A \mid$, $\mid B \mid$, and $\mid C \mid$
- [ ] D. $\mid A \mid$ and $\mid B \mid$ only
- [ ] E. $\mid A \mid$ and $\mid C \mid$ only

---

<a id="determinants-with-proportional-rowscolumns"></a>
## Determinants with Proportional Rows/Columns

Now, suppose a matrix has no zero rows and no identical rows but it has two proportional rows:

$$
\begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ -3 & -6 & -3\end{bmatrix}
$$

We can add three times the $1$st row to the $3$rd row, as follows:

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ -3 & -6 & -3\end{bmatrix} \mid R_{3} \mid:=R_{3} + 3R_{1} \\
&= ∼\begin{bmatrix}1 & 2 & 1 \\ 1 & 2 & 3 \\ 0 & 0 & 0\end{bmatrix} \\
&= B
\end{aligned}
$$

Since this operation does not change the determinant, we get

$$
\det(A) = \det(B)
$$

Furthermore, as $B$ contains a zero row, we obtain

$$
\det(A) = \det(B) = 0
$$

Notice that we can use this method with columns too. Therefore, we have the following property:

> *If a square matrix has at least two proportional rows/columns, its determinant equals zero.*

---

<a id="calculating-the-determinant-of-a-matrix-with-two-proportional-rowscolumns"></a>
## Calculating the Determinant of a Matrix With Two Proportional Rows/Columns

**Example:** Which of the following determinants are equal to $0$?

$\begin{bmatrix}2 & \sqrt{3} & 1 \\ 1 & 1 & 2 \\ 3 & 3 & 6\end{bmatrix}$

**Explanation**

Let's examine each of the given determinants in turn.

- The matrix $A$ has proportional rows:
$\begin{bmatrix}2 & \sqrt{3} & 1 \\ 1 & 1 & 2 \\ 3 & 3 & 6\end{bmatrix}$
As a result, $\mid A \mid = 0$.
- The matrix $B$ is a triangular matrix, so
$\mid B \mid = (-1) \cdot 4 \cdot (-3) = 12 \neq 0$.
- The matrix $C$ has a zero row:
$\begin{bmatrix}-1 & \sqrt{2} & \sqrt{3} \\ 0 & 0 & 0 \\ 1 & 0 & -1\end{bmatrix}$
As a result, $\mid C \mid = 0$.

Therefore, only $\mid A \mid$ and $\mid C \mid$ are equal to $0$.

---

**Question 3:** Find $4det(A) + 4det(B)$, where
$A = \begin{bmatrix}2\sqrt{2} & 1 \\ 1 & \sqrt{2}\end{bmatrix}, B = \begin{bmatrix}1 & \sqrt{2} & 2 \\ 2 & 1 & 4 \\ 3 & 6 & 6\end{bmatrix}$.

- [ ] A. $6$
- [ ] B. $10$
- [ ] C. $12$
- [ ] D. $7$
- [ ] E. $0$

---

**Question 4**

Which of the following determinants are equal to $0$?

$\begin{vmatrix}A|= & 2 & -1\end{vmatrix}\sqrt{3}; \sqrt[5]{2}\begin{vmatrix}1 & 4 \\ 0 & 0\end{vmatrix}0\begin{vmatrix}, & B|= & 1 & \sqrt{2}\end{vmatrix}4; 0\begin{vmatrix}3 & 6 \\ 0 & 0\end{vmatrix}4\begin{vmatrix}, & C|= & 1 & \sqrt{2}\end{vmatrix}4; 2\begin{vmatrix}3 & 8 \\ 1 & 0\end{vmatrix}4 \mid$

- [ ] A. $\mid A \mid$, $\mid B \mid$, and $\mid C \mid$
- [ ] B. $\mid B \mid$ only
- [ ] C. $\mid A \mid$ and $\mid B \mid$ only
- [ ] D. $\mid A \mid$ only
- [ ] E. $\mid A \mid$ and $\mid C \mid$ only

---

<a id="conditions-when-a-determinant-equals-zero"></a>
## Conditions When a Determinant Equals Zero

In conclusion, if an $n\!\times \!n$ matrix $A$ satisfies any one of the following conditions, then $\det(A)=0$.

1. $A$ has a zero row/column
2. $A$ has two identical rows/columns
3. $A$ has two proportional rows/columns
4. $A$ has a row/column that is a linear combination of other rows/columns

Notice that condition IV encompasses each of the conditions I, II, and III. If we have a zero row/column, two identical rows/columns, or proportional rows/columns, then the rows/columns form a linearly dependent set of vectors.
In turn, this means that one of the rows/columns is a linear combination of other rows/columns.

**Watch out!** While all of the above conditions are *sufficient* to conclude that $\det(A)=0$, only condition IV is *necessary* to conclude that $\det(A)=0$.
In other words, if we have a matrix $A$ with $\det(A)=0$, then conditions I, II, and III may or may not be satisfied, while condition IV *must* be satisfied.

So, we have the following theorem:

> *$\det(A)=0$ if and only if one of the rows/columns of $A$ is a linear combination of other rows/columns of $A$.*

Finally, note also that in the case of a $2\times 2$ matrix *only*, statements III and IV mean the same thing.

---

<a id="geometric-interpretation-of-a-zero-determinant"></a>
## Geometric Interpretation of a Zero Determinant

Geometrically, the absolute value of an $n \times n$ determinant can be interpreted as the volume of the $n$-dimensional parallelepiped spanned by the columns of the determinant.

For example:

- Suppose the columns of a $3\times 3$ matrix $A$ are ${\color{black}\mathbf{a}}, {\color{black}\mathbf{b}}$, and ${\color{black}\mathbf{c}}$.
$[\mid \mid \mid \mid \mid; a \mid b \mid c; \mid \mid \mid \mid \mid]$
The $3$-dimensional parallelepiped spanned by the columns of $A$ is depicted below:![](<../Source/2.1.8. Conditions When a Determinant Equals Zero/Images/2dd6c41a3db7336520a034a51b742bbb.png>)
The volume $V$ of this parallelepiped equals the absolute value of the determinant of $A{:}$
$V = \left|\det(A)\right|$
- Now suppose the columns of a $2\times 2$ matrix $B$ are ${\color{black}\mathbf{p}}$ and ${\color{black}\mathbf{q}}$.
$[\mid \mid \mid; p \mid q; \mid \mid \mid]$
The $2$-dimensional parallelepiped (i.e., the parallelogram) spanned by vectors $\mathbf{p}$ and $\mathbf{q}$ is shown below:![](<../Source/2.1.8. Conditions When a Determinant Equals Zero/Images/47c3f62329b5f9c05401af301b3d2660.png>)
The two-dimensional volume $V$ (i.e., the area) of this parallelepiped equals the absolute value of the determinant of $B{:}$
$V = \left|\det(B)\right|$
- In general, the absolute value of the determinant of an $n\times n$ matrix $M$ gives the $n$-dimensional volume of the parallelepiped spanned by the columns of $M$.

In this lesson, we studied the following property:

> *$\det(A) = 0$ if and only if one of the columns of $A$ is a linear combination of other columns of $A$.*

What does this property mean in geometric terms?

The component $\det(A) = 0$ implies that the $n$-dimensional parallelepiped formed by the columns of $A$ has zero volume. Conversely, when one of the columns of $A$ is a linear combination of other columns, it indicates that the corresponding parallelepiped possesses at most $n-1$ distinct dimensions.

Consider the following examples:

- In the case of a $3$-dimensional parallelepiped, if the vector $\mathbf{c}$ is a linear combination of the vectors $\mathbf{a}$ and $\mathbf{b}$, then $\mathbf{c}$ lies within the plane containing $\mathbf{a}$ and $\mathbf{b}$. In other words, the vectors $\mathbf{a}$, $\mathbf{b}$, and $\mathbf{c}$ are coplanar. Consequently, we obtain a "flat" ($2$-dimensional) figure with a volume of $0$.
- For the parallelogram example, if the vector $\mathbf{q}$ is a linear combination of the vector $\mathbf{p}$ (i.e., $\mathbf{p}$ and $\mathbf{q}$ are proportional), then $\mathbf{q}$ resides on the line containing vector $\mathbf{p}$. In other words, the vectors $\mathbf{p}$ and $\mathbf{q}$ are collinear. This results in a $1$-dimensional object with a $2$-dimensional volume (i.e., area) of $0$.

---

<a id="identifying-the-reasons-why-a-determinant-equals-zero"></a>
## Identifying the Reasons Why a Determinant Equals Zero

**Example:** $\begin{bmatrix}1 & 2 & 0 & -1 \\ 3 & 6 & 0 & 1 \\ 2 & 4 & 0 & -2 \\ 6 & 8 & 0 & 2\end{bmatrix}$

Consider the matrix given above. Identify **all** of the reasons why one can conclude that $\mid A|=0$.

1. $A$ has a zero column
2. $A$ has proportional rows
3. $A$ has identical columns

**Explanation**

Let's examine each of the statements in turn.

- $A$ has a zero column:
$\begin{bmatrix}1 & 2 & 0 & -1 \\ 3 & 6 & 0 & 1 \\ 2 & 4 & 0 & -2 \\ 6 & 8 & 0 & 2\end{bmatrix}$
- $A$ has proportional rows:
$\begin{bmatrix}1 & 2 & 0 & -1 \\ 3 & 6 & 0 & 1 \\ 2 & 4 & 0 & -2 \\ 6 & 8 & 0 & 2\end{bmatrix}$
- $A$ does *not* have identical columns.

Therefore, the correct answer is "I and II only."

---

**Question 5**

$A = \begin{bmatrix}2 & 0 & 4 \\ 1 & 0 & 3 \\ 2 & 0 & 4\end{bmatrix}$

Consider the matrix given above. Identify **all** of the reasons why one can conclude that $\mid A|= 0$?

1. $A$ has a zero column
2. $A$ has identical columns
3. $A$ has identical rows

- [ ] A. I and II only
- [ ] B. I only
- [ ] C. III only
- [ ] D. I and III only
- [ ] E. II and III only

---

**Question 6**

Let $A$ be a $2 \cdot 2$ matrix such that $\mid A|= 0$. Which of the following statements must be true?

1. $A$ has two identical rows
2. One of the rows/columns of $A$ is proportional to another row/column of $A$
3. $A$ has two identical columns

- [ ] A. I and III only
- [ ] B. I, II, and III
- [ ] C. II only
- [ ] D. I only
- [ ] E. I and II only

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
