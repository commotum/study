# LU Factorization of NxN Matrices

<!--
lesson-id: 1781
topic-code: LAL.2.5.4
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Finding Missing Numbers in the Matrix U of an LU Factorization](#finding-missing-numbers-in-the-matrix-u-of-an-lu-factorization)
- [Finding Missing Numbers in the Matrix L of an LU Factorization](#finding-missing-numbers-in-the-matrix-l-of-an-lu-factorization)
- [Finding the LU Factorization of a 3x3 Matrix](#finding-the-lu-factorization-of-a-3x3-matrix)
- [Finding the LU Factorization of a 4x4 Matrix](#finding-the-lu-factorization-of-a-4x4-matrix)

## Prerequisites

- [LU Factorization of 3x3 Matrices](<MA/Mathematical-Analysis-&-Modeling/LAL/2. Matrices/2.5. LU Factorization/Lessons/2.5.3. LU Factorization of 3x3 Matrices.md>)

---

<a id="introduction"></a>
## Introduction

In general, the LU factorization $A=LU$ of an $n\times n$ matrix $A$ is the product of two matrices $L$ and $U$, where

$$
[1\begin{vmatrix}0 & ⋯ & 0 \\ ∗ & 1 & 0 \\ ⋮ & ⋱ & ⋮ \\ ∗ & ∗ & ⋯\end{vmatrix}1]
$$

is a *unit* lower triangular matrix, and

$$
[∗\begin{vmatrix}∗ & ⋯ & ∗ \\ 0 & ∗ & ∗ \\ ⋮ & ⋱ & ⋮ \\ 0 & 0 & ⋯\end{vmatrix}∗]
$$

is an upper triangular matrix.

As it turns out, there is a quick way of finding a matrix's LU factorization, which is particularly helpful when factoring large matrices.

Let's see a concrete example of how this works.

---

<a id="a-worked-example"></a>
## A Worked Example

Suppose we want to find the LU factorization $A=LU$ of the matrix

$$
\begin{bmatrix}2 & 1 & 1 \\ 6 & 1 & 1 \\ 4 & 6 & 0\end{bmatrix}
$$

We proceed by reducing $A$ to row echelon form using Gaussian elimination.

To start, we want to make $0$ at the position $a_{{\color{blue}2}{\color{red}1}}$. To do this, we take the ${\color{blue}2}$nd row of $A$, and we add the ${\color{red}1}$st row of $A$ multiplied by ${\color{purple}-3}{:}$

$$
\begin{aligned}
A &= \begin{bmatrix}2 & 1 & 1 \\ 6 & 1 & 1 \\ 4 & 6 & 0\end{bmatrix} \mid R_{2} \mid:=R_{2} + (-3)R_{1} \\
&= ∼\begin{bmatrix}2 & 1 & 1 \\ 0 & -2 & -2 \\ 4 & 6 & 0\end{bmatrix}
\end{aligned}
$$

Now, here's the trick. We take the factor $\color{purple}-3$ that we multiplied $R_{\color{red}1}$ by, change its sign, and write it as the entry $a_{{\color{blue}2}{\color{red}1}}$ with a box around it. This gives

$$
A \mid ∼\begin{bmatrix}2 & 1 & 1 \\ 3 & -2 & -2 \\ 4 & 6 & 0\end{bmatrix}.(∗)
$$

**Important**:

- You can think of the box $\bbox\begin{bmatrix}FloralWhite, 2pt, border-radius: 5px \\ border: 1px solid black\end{bmatrix}{\color{purple}\:\phantom{3}\:}$ as representing the original zero.
- In addition, we store the value $\color{purple}3$ to use later.
- When doing this in practice, you might find it helpful to write a large zero with a small $\color{purple}3$ inside it. This is represented by $\bbox\begin{bmatrix}FloralWhite, 2pt, border-radius: 5px \\ border: 1px solid black\end{bmatrix}{\color{purple}\:3\:}$ in our matrix above.

Let's now continue with our Gaussian elimination.

We want to make $0$ at the position $a_{{\color{blue}3}{\color{red}1}}$. To do this, we take the ${\color{blue}3}$rd row of $(\ast)$ and we add the ${\color{red}1}$st row multiplied by ${\color{purple}-2}$. Then, we place

$$
-({\color{purple}-2})={\color{purple}2}
$$

with a box around it at the position $a_{{\color{blue}3}{\color{red}1}}{:}$

$$
A\begin{vmatrix}∼\begin{bmatrix}2 & 1 & 1 \\ 3 & -2 & -2 \\ 4 & 6 & 0\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + (-2)R_{1}; ∼\begin{bmatrix}2 & 1 & 1 \\ 3 & -2 & -2 \\ 2 & 4 & -2\end{bmatrix}
$$

Finally, we want to make $0$ at the position $a_{{\color{blue}3}{\color{red}2}}$. To do this, we take the ${\color{blue}3}$rd row and we add the ${\color{red}2}$nd row multiplied by ${\color{purple}2}$. Then, we place

$$
-({\color{purple}2})={\color{purple}-2}
$$

with a box around it at the position $a_{{\color{blue}3}{\color{red}2}}{:}$

$$
\begin{aligned}
A &= \begin{bmatrix}2 & 1 & 1 \\ 3 & -2 & -2 \\ 2 & 4 & -2\end{bmatrix} \mid R_{3} \mid:=R_{3} + 2R_{2} \\
&= ∼\begin{bmatrix}2 & 1 & 1 \\ 3 & -2 & -2 \\ 2 & -2 & -6\end{bmatrix}
\end{aligned}
$$

The matrix is now in row echelon form (remember that the boxes each represent zero), and we're almost done.

Now, here's the neat bit. Both $L$ and $U$ can be extracted from the matrix above:

- The matrix $L$ takes all elements that lie *below* the main diagonal:
$\begin{bmatrix}1 & 0 & 0 \\ 3 & 1 & 0 \\ 2 & -2 & 1\end{bmatrix}$
Remember that $L$ must be *unit* lower triangular.
- The matrix $U$ takes the remaining elements:
$\begin{bmatrix}2 & 1 & 1 \\ 0 & -2 & -2 \\ 0 & 0 & -6\end{bmatrix}$
Remember that $U$ must be upper triangular.

---

<a id="finding-missing-numbers-in-the-matrix-u-of-an-lu-factorization"></a>
## Finding Missing Numbers in the Matrix U of an LU Factorization

**Example:** $\begin{bmatrix}1 & -5 & 6 \\ 4 & -9 & 8 \\ 6 & -8 & 10\end{bmatrix}$

Consider the LU factorization of the matrix $A$ above. What is the value of $u_{33}$ in the matrix $U$?

**Explanation**

The matrix $U$ in the LU decomposition is obtained from the original matrix $A$ by row-reducing using Gaussian elementation. In particular, to make zeros below the main diagonal, we proceed as follows:

$$
\begin{aligned}
A &= \begin{bmatrix}1 & -5 & 6 \\ 4 & -9 & 8 \\ 6 & -8 & 10\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + (-4)R_{1} \\ ∼\begin{bmatrix}1 & -5 & 6 \\ 0 & 11 & -16 \\ 6 & -8 & 10\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + (-6)R_{1} \\
&= ∼\begin{bmatrix}1 & -5 & 6 \\ 0 & 11 & -16 \\ 0 & 22 & -26\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-2)R_{2} \\
&= ∼\begin{bmatrix}1 & -5 & 6 \\ 0 & 11 & -16 \\ 0 & 0 & 6\end{bmatrix}
\end{aligned}
$$

Therefore, $u_{33}=6$.

---

**Question 1**

$\begin{bmatrix}2 & 3 & -1 \\ 6 & 4 & 1 \\ 4 & 6 & -4\end{bmatrix}_(⏟)_(A) = \begin{bmatrix}1 & 0 & 0 \\ 3 & 1 & 0 \\ 2 & 0 & 1\end{bmatrix}_(⏟)_(L) \cdot \begin{bmatrix}2 & 3 & -1 \\ N & N & N \\ 0 & 0 & -2\end{bmatrix}_(⏟)_(U)$.

Consider the LU factorization of the matrix $A$ above. From left to right, what are the missing elements from the second row of the matrix $U$?

- [ ] A. $0,-2, 4$
- [ ] B. $0,-5, 3$
- [ ] C. $0,-5, 4$
- [ ] D. $0,-2, 3$
- [ ] E. $0,-5, 1$

---

**Question 2**

$\begin{bmatrix}4 & 2 & -2 \\ 16 & 4 & 6 \\ 4 & -2 & 8\end{bmatrix}_(⏟)_(A) = \begin{bmatrix}1 & 0 & 0 \\ 4 & 1 & 0 \\ 1 & 1 & 1\end{bmatrix}_(⏟)_(L) \cdot \begin{bmatrix}4 & 2 & -2 \\ 0 & -4 & 14 \\ 0 & 0 & N\end{bmatrix}_(⏟)_(U)$.

Consider the LU factorization of the matrix $A$ above. What is the value of $u_{33}$ in the matrix $U$?

- [ ] A. $14$
- [ ] B. $-14$
- [ ] C. $2$
- [ ] D. $10$
- [ ] E. $-4$

---

<a id="finding-missing-numbers-in-the-matrix-l-of-an-lu-factorization"></a>
## Finding Missing Numbers in the Matrix L of an LU Factorization

**Example:** $\begin{bmatrix}1 & 2 & -3 \\ 2 & -6 & 1 \\ 4 & 8 & -10\end{bmatrix}$

Consider the LU factorization of the matrix $A$ above. What is the value of the entry $l_{31}$ in the matrix $L$?

**Explanation**

We want to make $0$ at the position $a_{{\color{blue}3}{\color{red}1}}$ using Gaussian elimination. To do this, we take the ${\color{blue}3}$rd row of $A$, and we add the ${\color{red}1}$st row of $A$ multiplied by ${\color{purple}-4}{:}$

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 2 & -3 \\ 2 & -6 & 1 \\ 4 & 8 & -10\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-4)R_{1} \\
&= ∼\begin{bmatrix}1 & 2 & -3 \\ 2 & -6 & 1 \\ 0 & 0 & 2\end{bmatrix}
\end{aligned}
$$

The entry $l_{{\color{blue}3}{\color{red}1}}$ of the matrix $L$ is the factor next to $R_1$, taken with the opposite sign:

$$
l_{31}=-({\color{purple}-4})=4
$$

---

**Question 3**

$\begin{bmatrix}1 & 2 & -3 \\ 1 & -2 & 0 \\ 2 & 4 & 2\end{bmatrix}_(⏟)_(A) = \begin{bmatrix}1 & 0 & 0 \\ 1 & 1 & 0 \\ N & 0 & 1\end{bmatrix}_(⏟)_(L) \cdot \begin{bmatrix}1 & 2 & -3 \\ 0 & -4 & 3 \\ 0 & 0 & 8\end{bmatrix}_(⏟)_(U)$

Consider the LU factorization of the matrix $A$ above. What is the value of the entry $l_{31}$ in the matrix $L$?

- [ ] A. $-4$
- [ ] B. $4$
- [ ] C. $-2$
- [ ] D. $2$
- [ ] E. $0$

---

**Question 4**

$\begin{bmatrix}3 & 2 & -5 \\ 12 & 9 & 0 \\ 9 & 7 & -12\end{bmatrix}_(⏟)_(A) = \begin{bmatrix}1 & 0 & 0 \\ 4 & 1 & 0 \\ 3 & N & 1\end{bmatrix}_(⏟)_(L) \cdot \begin{bmatrix}3 & 2 & -5 \\ 0 & 1 & 20 \\ 0 & 0 & -17\end{bmatrix}_(⏟)_(U)$

Consider the LU factorization of the matrix $A$ above. What is the value of the entry $l_{32}$ in the matrix $L$?

- [ ] A. $-3$
- [ ] B. $3$
- [ ] C. $-4$
- [ ] D. $1$
- [ ] E. $4$

---

<a id="finding-the-lu-factorization-of-a-3x3-matrix"></a>
## Finding the LU Factorization of a 3x3 Matrix

**Example:** $\begin{bmatrix}2 & 7 & 1 \\ -6 & 3 & -12 \\ -4 & 10 & 1\end{bmatrix}$

If $A=LU$ is the LU decomposition of the matrix $A$ given above, what is the value of $l_{31} \cdot u_{23}$?

**Explanation**

Let's apply our standard LU factorization algorithm.

We reduce the matrix $A$ to row echelon form to obtain the matrix $U$, as usual. However, instead of writing zeros below the main diagonal, we will instead write down the entries of the matrix $L$.

$$
\begin{aligned}
A &= \begin{bmatrix}2 & 7 & 1 \\ -6 & 3 & -12 \\ -4 & 10 & 1\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + 3R_{1} \\ ∼\begin{bmatrix}2 & 7 & 1 \\ -3 & 24 & -9 \\ -4 & 10 & 1\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + 2R_{1} \\
&= ∼\begin{bmatrix}2 & 7 & 1 \\ -3 & 24 & -9 \\ -2 & 24 & 3\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-1)R_{2} \\
&= ∼\begin{bmatrix}2 & 7 & 1 \\ -3 & 24 & -9 \\ -2 & 1 & 12\end{bmatrix}
\end{aligned}
$$

Therefore, we obtain
$\begin{bmatrix}1 & 0 & 0 \\ -3 & 1 & 0 \\ -2 & 1 & 1\end{bmatrix}$

Finally, since $l_{31}=-2$ and $u_{23}=-9$, we get

$$
l_{31} \cdot u_{23} = (-2) \cdot (-9) =18
$$

---

**Question 5**

$A = \begin{bmatrix}2 & -1 & 3 \\ 4 & -1 & 7 \\ -2 & 4 & -3\end{bmatrix}$

If $A = LU$ is the LU decomposition of the matrix $A$ given above, what is the value of $l_{21} \cdot u_{33}$?

- [ ] A. $0$
- [ ] B. $10$
- [ ] C. $12$
- [ ] D. $-14$
- [ ] E. $-6$

---

**Question 6**

$D = \begin{bmatrix}3 & -2 & 1 \\ -3 & -2 & -2 \\ 12 & 8 & 5\end{bmatrix}$

If $D = LU$ is the LU decomposition of the matrix $D$ given above, what is the value of $l_{32} \cdot u_{22}$?

- [ ] A. $12$
- [ ] B. $4$
- [ ] C. $16$
- [ ] D. $-16$
- [ ] E. $-12$

---

<a id="finding-the-lu-factorization-of-a-4x4-matrix"></a>
## Finding the LU Factorization of a 4x4 Matrix

**Example:** $\begin{bmatrix}6 & 6 & -6 & 8 \\ 0 & -2 & 0 & -3 \\ -6 & -6 & 9 & -7 \\ 0 & 4 & 6 & 9\end{bmatrix}$

If $A=LU$ is the LU decomposition of the matrix $A$ given above, what is the value of $l_{42} \cdot u_{24}$?

**Explanation**

Let's apply our standard LU factorization algorithm.

We reduce the matrix $A$ to row echelon form to obtain the matrix $U$, as usual. However, instead of writing zeros below the main diagonal, we will instead write down the entries of the matrix $L$.

$$
\begin{aligned}
A &= \begin{bmatrix}6 & 6 & -6 & 8 \\ 0 & -2 & 0 & -3 \\ -6 & -6 & 9 & -7 \\ 0 & 4 & 6 & 9\end{bmatrix}\begin{vmatrix}R_{3} &:=R_{3} + 1R_{1} \\ ∼\begin{bmatrix}6 & 6 & -6 & 8 \\ 0 & -2 & 0 & -3 \\ -1 & 0 & 3 & 1 \\ 0 & 4 & 6 & 9\end{bmatrix} & R_{4}\end{vmatrix}:=R_{4} + 2R_{2} \\
&= ∼\begin{bmatrix}6 & 6 & -6 & 8 \\ 0 & -2 & 0 & -3 \\ -1 & 0 & 3 & 1 \\ 0 & -2 & 6 & 3\end{bmatrix} \mid R_{4} \mid:=R_{4} + (-2)R_{3} \\
&= ∼\begin{bmatrix}6 & 6 & -6 & 8 \\ 0 & -2 & 0 & -3 \\ -1 & 0 & 3 & 1 \\ 0 & -2 & 2 & 1\end{bmatrix}
\end{aligned}
$$

Therefore, we obtain
$\begin{bmatrix}1 & 0 & 0 & 0 \\ 0 & 1 & 0 & 0 \\ -1 & 0 & 1 & 0 \\ 0 & -2 & 2 & 1\end{bmatrix}$

Finally, since $l_{42}=-2$ and $u_{24}=-3$, we get

$$
l_{42} \cdot u_{24} = (-2) \cdot (-3) =6
$$

---

**Question 7**

$A = \begin{bmatrix}1 & 5 & 1 & -3 \\ -2 & -8 & -5 & 12 \\ 0 & 6 & -2 & 4 \\ 0 & 0 & 14 & -30\end{bmatrix}$

If $A = LU$ is the LU decomposition of the matrix $A$ given above, what is the value of $l_{43} \cdot u_{33}$?

- [ ] A. $18$
- [ ] B. $-15$
- [ ] C. $14$
- [ ] D. $8$
- [ ] E. $-12$

---

**Question 8**

$A = \begin{bmatrix}1 & -3 & -1 & 3 \\ 3 & -11 & 8 & -2 \\ 0 & 0 & -5 & -9 \\ -3 & 9 & 3 & 2\end{bmatrix}$

If $A = LU$ is the LU decomposition of the matrix $A$ given above, what is the value of $l_{41} \cdot u_{44}$?

- [ ] A. $-33$
- [ ] B. $0$
- [ ] C. $15$
- [ ] D. $-15$
- [ ] E. $33$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
