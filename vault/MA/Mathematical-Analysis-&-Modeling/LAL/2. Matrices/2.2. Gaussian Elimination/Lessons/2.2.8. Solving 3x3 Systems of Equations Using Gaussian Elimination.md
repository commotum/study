# Solving 3x3 Systems of Equations Using Gaussian Elimination

<!--
lesson-id: 1048
topic-code: LAL.2.2.8
-->

## Table of Contents

- [Introduction](#introduction)
- [General Algorithm](#general-algorithm)
- [Solving a 3x3 System of Equations Using Standard Gaussian Elimination](#solving-a-3x3-system-of-equations-using-standard-gaussian-elimination)
- [Solving a 3x3 System of Equations Using Standard Gaussian Elimination with Row Exchanges](#solving-a-3x3-system-of-equations-using-standard-gaussian-elimination-with-row-exchanges)

## Prerequisites

- [Solving 2x2 Systems of Equations Using Gaussian Elimination](<2.2.6. Solving 2x2 Systems of Equations Using Gaussian Elimination.md>)

---

<a id="introduction"></a>
## Introduction

Consider a system of linear equations and its respective augmented matrix $M$:

$$
x - y + z = 6; y - 3z =-13; - x - y - z =-4
$$

We can reduce $M$ to row echelon form using the standard Gaussian elimination algorithm. The algorithm that we use is very similar to the case of $2 \times 2$ systems.

Our goal is to create a descending "staircase" of $1$'s with $0$'s below, and we achieve this using elementary row operations. So, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ -1 & -1 & -1 & -4\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + R_{1}; ∼\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ 0 & -2 & 0 & 2\end{bmatrix}\begin{vmatrix}R_{3} &:=R_{3} + 2R_{2} \\ ∼\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ 0 & 0 & -6 & -24\end{bmatrix} & R_{3}\end{vmatrix}:=-\frac{1}{6}R_{3}; ∼\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ 0 & 0 & 1 & 4\end{bmatrix}
$$

The matrix is now in row echelon form, and we can apply back substitution to solve the original system of equations.

**Note:** The row echelon form of a matrix is *not* unique. In the previous example, after the second row operation we arrived at

$$
\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ 0 & 0 & -6 & -24\end{bmatrix}
$$

This augmented matrix is already in row echelon form! However, it is always useful to do an extra step and obtain $1$'s along the main diagonal, like we did previously:

$$
\begin{bmatrix}1 & -1 & 1 & 6 \\ 0 & 1 & -3 & -13 \\ 0 & 0 & 1 & 4\end{bmatrix}
$$

Having $1$'s along the main diagonal makes the final back substitution process a little easier.

---

<a id="general-algorithm"></a>
## General Algorithm

The **standard Gaussian elimination** for a $3 \times 3$ system can be described as follows:

1. If $a_{11} \neq 0$ and $a_{11} \neq 1$, then we use the row operation $R_1:=R_1/a_{11}$.
2. We use first row and the necessary row operations to make the remaining entries of the first column below $a_{11}$ equal to $0$. Namely, if $a_{21} \neq 0$, then we use the row operation $R_2:=R_2 + (-a_{21})R_1$;if $a_{31} \neq 0$, then we use the row operation $R_3:=R_3 + (-a_{31})R_1$.
3. If $a_{22} \neq 1$, then we use the row operation $R_2:=R_2/a_{22}$.
4. We use the second row and the necessary row operation to make the entry below $a_{22}$ equal to $0$. Namely, if $a_{32} \neq 0$, then we use the row operation $R_3:=R_3 + (-a_{32})R_2$.

1. If $a_{33} \neq 1$, then we use the row operation $R_3:=R_3/a_{33}$.
2. In some exceptional cases, such as when the element on the diagonal is equal to $0$, we first need to exchange rows. Then proceed using the same steps as above.

Carrying out the above algorithm will convert the matrix to row echelon form.

---

<a id="solving-a-3x3-system-of-equations-using-standard-gaussian-elimination"></a>
## Solving a 3x3 System of Equations Using Standard Gaussian Elimination

**Example:** Consider the system of linear equations

$x - y + z = 1; y + z =-2; - x + 2y + z =-1$.

Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.

**Explanation**

Reducing $M$ to row echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & -1 & 1 & 1 \\ 0 & 1 & 1 & -2 \\ -1 & 2 & 1 & -1\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + R_{1}; ∼\begin{bmatrix}1 & -1 & 1 & 1 \\ 0 & 1 & 1 & -2 \\ 0 & 1 & 2 & 0\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-1)R_{2}; ∼\begin{bmatrix}1 & -1 & 1 & 1 \\ 0 & 1 & 1 & -2 \\ 0 & 0 & 1 & 2\end{bmatrix}
$$

The system is now

$$
x - y + z = 1; y + z =-2; z = 2
$$

and we can apply back substitution. The final equation $z=2$ tells us the value of $z$. Substituting this value into the second equation, we get

$$
y + (2) =-2⟹y =-4
$$

Finally, substituting both $y=-4$ and $z=2$ into the first equation, we get

$$
x - (-4) + (2) = 1⟹x =-5
$$

Therefore, the solution is $x=-5$, $y=-4$, and $z=2$. Or, in vector form, $\langle -5,-4,2 \rangle$.

---

**Question 1:**

```quiz
type: radio
id: ma-10676
content: |-
  Consider the augmented matrix $M$ given by

  $M = \begin{bmatrix}1 & 1 & 2 & 19 \\ 0 & 1 & 2 & 13 \\ 1 & 3 & 2 & 25\end{bmatrix}$.
  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.
options:
- id: a
  content: |-
    $⟨1, 2, 5⟩$
- id: b
  content: |-
    $⟨ - 1, 2, 5⟩$
- id: c
  content: |-
    $⟨ - 3, 1, 2⟩$
- id: d
  content: |-
    $⟨6, 3, 5⟩$
  correct: true
- id: e
  content: |-
    $⟨6, 3, 2⟩$
```

---

**Question 2:**

```quiz
type: radio
id: ma-20505
content: |-
  Consider the system of linear equations

  ${x + 2y + 6z = 5; y - 10z =-11; 4x + 8y + 13z = 9$
  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form.
options:
- id: a
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 6 & 5 \\ 0 & 0 & -10 & -1 \\ 0 & 0 & 1 & -2\end{bmatrix}$
- id: b
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 6 & 5 \\ 0 & 1 & -10 & -11 \\ 0 & 0 & 0 & 0\end{bmatrix}$
- id: c
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 6 & 5 \\ 0 & 1 & -10 & -11 \\ 0 & 0 & 0 & 1\end{bmatrix}$
- id: d
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 6 & 5 \\ 0 & 1 & -4 & -7 \\ 0 & 0 & 1 & -1\end{bmatrix}$
- id: e
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 6 & 5 \\ 0 & 1 & -10 & -11 \\ 0 & 0 & 1 & 1\end{bmatrix}$
  correct: true
```

---

<a id="solving-a-3x3-system-of-equations-using-standard-gaussian-elimination-with-row-exchanges"></a>
## Solving a 3x3 System of Equations Using Standard Gaussian Elimination with Row Exchanges

**Example:** Consider the system of linear equations
$x + y + z =-2; x + y + 2z =-1; x + 2y + z = 1$.

Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.

**Explanation**

The augmented matrix for this system is

$$
\begin{bmatrix}1 & 1 & 1 & -2 \\ 1 & 1 & 2 & -1 \\ 1 & 2 & 1 & 1\end{bmatrix}
$$

Reducing $M$ to row echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & 1 & 1 & -2 \\ 1 & 1 & 2 & -1 \\ 1 & 2 & 1 & 1\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + (-1)R_{1}; ∼\begin{bmatrix}1 & 1 & 1 & -2 \\ 0 & 0 & 1 & 1 \\ 1 & 2 & 1 & 1\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-1)R_{1}; ∼\begin{bmatrix}1 & 1 & 1 & -2 \\ 0 & 0 & 1 & 1 \\ 0 & 1 & 0 & 3\end{bmatrix}
$$

Note that we get $a_{22}=0$. Therefore, we swap the $2$nd and $3$rd rows:
$M\begin{vmatrix}∼\begin{bmatrix}1 & 1 & 1 & -2 \\ 0 & 0 & 1 & 1 \\ 0 & 1 & 0 & 3\end{bmatrix} & R_{2}\end{vmatrix}↔R_{3}; ∼\begin{bmatrix}1 & 1 & 1 & -2 \\ 0 & 1 & 0 & 3 \\ 0 & 0 & 1 & 1\end{bmatrix}$

The system is now

$$
x + y + z =-2; y = 3; z = 1
$$

and we can apply back substitution. The third equation $z=1$ tells us the value of $z$, and the second equation $y=3$ tells us the value of $y$. Substituting these values into the first equation, we get

$$
x + (3) + (1) =-2⟹x =-6
$$

Therefore, the solution is $x=-6$, $y=3$, and $z=1$. Or, in vector form, $\langle -6,3,1 \rangle$.

---

**Question 3**

```quiz
type: radio
id: ma-73396
content: |-
  Consider the system of linear equations
  ${x - y + 2z = 8; - 2x + 2y - 3z =-11; x + z = 9$.

  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.
options:
- id: a
  content: |-
    $x = 4$, $y =-5$, and $z = 2$
- id: b
  content: |-
    $x = 4$, $y = 5$, and $z = 2$
- id: c
  content: |-
    $x = 4$, $y = 6$, and $z = 5$
  correct: true
- id: d
  content: |-
    $x = 1$, $y =-6$, and $z =-5$
- id: e
  content: |-
    $x =-1$, $y = 2$, and $z = 5$
```

---

**Question 4**

```quiz
type: radio
id: ma-73397
content: |-
  Consider the augmented matrix $M$ given by
  $M = \begin{bmatrix}1 & 2 & 3 & 1 \\ 1 & 2 & 5 & 3 \\ 2 & 1 & 4 & 9\end{bmatrix}$.

  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form.
options:
- id: a
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 3 & 1 \\ 0 & 1 & -\frac{1}{2} & \frac{1}{2} \\ 0 & 0 & 1 & -5\end{bmatrix}$
- id: b
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 3 & 1 \\ 0 & 1 & \frac{2}{3} & -\frac{7}{3} \\ 0 & 0 & 1 & 1\end{bmatrix}$
  correct: true
- id: c
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 3 & 1 \\ 0 & 1 & \frac{1}{2} & -\frac{1}{2} \\ 0 & 0 & 1 & 3\end{bmatrix}$
- id: d
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 3 & 1 \\ 0 & 1 & 2 & 7 \\ 0 & 0 & 1 & 2\end{bmatrix}$
- id: e
  content: |-
    $M∼\begin{bmatrix}1 & 2 & 3 & 1 \\ 0 & 1 & \frac{1}{3} & \frac{7}{3} \\ 0 & 0 & 1 & -2\end{bmatrix}$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
