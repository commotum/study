# Solving 2x2 Singular Systems of Equations Using Gaussian Elimination

<!--
lesson-id: 1373
topic-code: LAL.2.2.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Reducing an Augmented Matrix for a Singular 2x2 System to REF and Solving the Resulting System](#reducing-an-augmented-matrix-for-a-singular-2x2-system-to-ref-and-solving-the-resulting-system)
- [Singular Systems with No Solution](#singular-systems-with-no-solution)
- [Solving a Singular 2x2 System of Equations Using Gaussian Elimination](#solving-a-singular-2x2-system-of-equations-using-gaussian-elimination)
- [The Number of Solutions of a 2x2 System](#the-number-of-solutions-of-a-2x2-system)

## Prerequisites

- [Solving 2x2 Systems of Equations Using Gaussian Elimination](<2.2.6. Solving 2x2 Systems of Equations Using Gaussian Elimination.md>)
- [Consistency and Dependency in Linear Systems](<../../../../AG1/2. Two-Variable Equations & Inequalities/2.3. Systems of Equations/Lessons/2.3.7. Consistency and Dependency in Linear Systems.md>)

---

<a id="introduction"></a>
## Introduction

Let's consider a system of linear equations where the corresponding coefficient matrix is singular. For example, consider the following system and its associated augmented matrix:

$$
x - 2y =-5; - 3x + 6y = 15
$$

To solve the system, we can reduce $M$ to row echelon form using Gaussian elimination, as usual. We have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & -2 & -5 \\ -3 & 6 & 15\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + 3R_{1}; ∼\begin{bmatrix}1 & -2 & -5 \\ 0 & 0 & 0\end{bmatrix}
$$

The corresponding system is now

$$
x - 2y =-5; 0x + 0y = 0
$$

Since any pair of numbers $x,y$ is a solution of the final equation

$$
0x+0y = 0
$$

we can remove this equation from the system. So, our system now consists of a single equation

$$
x - 2y = -5
$$

Finally, we describe the **general solution** of our system.

- The first variable in our equation, $x$, is called a **basic variable**.
- The second variable in our equation, $y$, is called a **free variable**.

Solving for the basic variable $(x)$ in terms of the free variable $(y)$, we get

$$
x = -5+2y
$$

So the general solution of the system is

$$
x =-5 + 2y; y = y
$$

where the free variable $y$ is any number.

In vector form, this solution is written as

$$
\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-5 + 2y \\ y\end{bmatrix} = \begin{bmatrix}-5 \\ 0\end{bmatrix} + y\begin{bmatrix}2 \\ 1\end{bmatrix}, y ∈ (- ∞, ∞)
$$

Notice that this is the equation of a straight line on a plane. Any point on this line satisfies our system of equations.

If we want to find some **particular solution** of the system, we simply choose some particular value of $y$ and find the corresponding value of $x$ on the line. For example, if we choose $y=0$, then the corresponding value is $x=-5$ and the particular solution is $x=-5$, $y=0$.

---

<a id="reducing-an-augmented-matrix-for-a-singular-2x2-system-to-ref-and-solving-the-resulting-system"></a>
## Reducing an Augmented Matrix for a Singular 2x2 System to REF and Solving the Resulting System

**Example:** Consider the augmented matrix $M$ given by
$\begin{bmatrix}1 & -3 & 1 \\ 4 & -12 & 4\end{bmatrix}$

Use the standard method of Gaussian elimination to reduce $M$ to row echelon form, and then find the general solution of the linear system represented by the matrix.

**Explanation**

Reducing $M$ to echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & -3 & 1 \\ 4 & -12 & 4\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + (-4)R_{1}; ∼\begin{bmatrix}1 & -3 & 1 \\ 0 & 0 & 0\end{bmatrix}
$$

The corresponding system is now

$$
x - 3y = 1; 0x + 0y = 0
$$

Since any pair of numbers $x,y$ is a solution of the final equation

$$
0x+0y = 0
$$

we can remove this equation from the system. So, our system now consists of a single equation

$$
x - 3y = 1
$$

The first variable $(x)$ is the basic variable, and the second variable $(y)$ is the free variable. Solving for the basic variable $(x)$ in terms of the free variable $(y)$, we get

$$
x=1+3y
$$

and the general solution is

$$
x = 1 + 3y; y = y
$$

where $y$ is an arbitrary number.

In vector form, the general solution can be written as

$$
\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}1 + 3y \\ y\end{bmatrix} = ⟨1 + 3y, y⟩, y ∈ (- ∞, ∞)
$$

---

**Question 1**

```quiz
type: radio
id: ma-42155
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the augmented matrix $M$ given by
  $M = \begin{bmatrix}1 & -1 & 3 \\ -3 & 3 & -9\end{bmatrix}$.

  Use the standard method of Gaussian elimination to reduce $M$ to row echelon form, and then find the general solution of the linear system represented by the matrix.
options:
- id: a
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-3 - y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: b
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}3 - y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: c
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}1 + y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: d
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-3 + y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: e
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}3 + y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
```

---

**Question 2:**

```quiz
type: radio
id: ma-42156
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the augmented matrix $M$ given by

  $M = \begin{bmatrix}1 & -2 & 1 \\ 0 & 0 & 0\end{bmatrix}$.
  Find the general solution of the linear system represented by the matrix.
options:
- id: a
  content: |-
    $⟨1 + 2y, y⟩, y ∈ (- ∞, ∞)$
- id: b
  content: |-
    $⟨ - 1 + 2y, y⟩, y ∈ (- ∞, ∞)$
- id: c
  content: |-
    $⟨1, 0⟩$
- id: d
  content: |-
    $⟨1,-2⟩$
- id: e
  content: |-
    $⟨1 - 2y, y⟩, y ∈ (- ∞, ∞)$
```

---

<a id="singular-systems-with-no-solution"></a>
## Singular Systems with No Solution

Sometimes, a system of equations might not have any solutions. For example, consider the following augmented matrix:

$$
M = \begin{bmatrix}1 & -2 & 3 \\ 0 & 0 & 10\end{bmatrix}
$$

The corresponding system is

$$
x - 2y = 3; 0x + 0y = 10
$$

The final equation,

$$
0x+0y=10
$$

has no solution. It simplifies to $0=10$, which is a false statement.

So, there is no pair $x,y$ that satisfies both equations in the system. Consequently, the overall system has no solution.

---

<a id="solving-a-singular-2x2-system-of-equations-using-gaussian-elimination"></a>
## Solving a Singular 2x2 System of Equations Using Gaussian Elimination

**Example:** Consider the system of linear equations
$2x + 5y = 4; 4x + 10y = 0$.
Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then find the general solution of the linear system.

**Explanation**

First, we set up the augmented matrix for the system:

$$
\begin{bmatrix}2 & 5 & 4 \\ 4 & 10 & 0\end{bmatrix}
$$

Reducing $M$ to row echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}2 & 5 & 4 \\ 4 & 10 & 0\end{bmatrix} & R_{1}\end{vmatrix}:=\frac{1}{2}R_{1}; ∼\begin{bmatrix}1 & \frac{5}{2} & 2 \\ 4 & 10 & 0\end{bmatrix} \mid R_{2} \mid:=R_{2} + (-4)R_{1}; ∼\begin{bmatrix}1 & \frac{5}{2} & 2 \\ 0 & 0 & -8\end{bmatrix}
$$

The corresponding system is now

$$
x + \frac{5}{2}y = 2; 0x + 0y =-8
$$

However, the final equation

$$
0x+0y=-8
$$

has no solution. It simplifies to $0=-8$, which is a false statement.

So, there is no pair $x,y$ that satisfies both equations in the system. Consequently, the overall system has no solution.

---

**Question 3:**

```quiz
type: radio
id: ma-42167
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the system of linear equations

  ${7x + 14y = 21; 3x + 6y = 9$.
  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then find the general solution of the linear system.
options:
- id: a
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-3 - 2y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: b
  content: |-
    No solution
- id: c
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}3 + 2y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: d
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}3 - 2y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: e
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-3 + 2y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
```

---

**Question 4:**

```quiz
type: radio
id: ma-42157
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the system of linear equations

  ${x - 6y = 2; - 4x + 24y =-10$.
  Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then find the general solution of the linear system.
options:
- id: a
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}2 + 6y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: b
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-2 + 6y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: c
  content: |-
    No solution
- id: d
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-2 - 6y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
- id: e
  content: |-
    $\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}2 - 6y \\ y\end{bmatrix}, y ∈ (- ∞, ∞)$
```

---

<a id="the-number-of-solutions-of-a-2x2-system"></a>
## The Number of Solutions of a 2x2 System

Given a $2 \times 2$ system of linear equations, we have the following possibilities for the number of solutions:

- The system has exactly *one solution* if and only if the respective coefficient matrix is non-singular (meaning that the determinant is not zero). Such systems are called **consistent independent**.
- The system has *infinitely many solutions* if the second row in the standard echelon form contains only $0$'s. These systems are called **consistent dependent**.
- The system has *no solution* if the second row in the standard echelon form contains only $0$'s on the left-hand side and a non-zero element on the right-hand side. These are called **inconsistent** systems.

![](<../Source/2.2.7. Solving 2x2 Singular Systems of Equations Using Gaussian Elimination/Images/b0ff3e456c0e81795741fc6b263169da.png>)

**Note:** A system of linear equations of the form

$$
ax + by = 0; cx + dy = 0
$$

where we have only $0$'s on the right-hand side, is called **homogeneous**. A homogeneous system always has at least one solution, namely, the solution $x=0$ and $y=0$. Or, in vector form, $\langle 0,0 \rangle$.

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
