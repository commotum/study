# Multiple Linear Regression With Matrices

<!--
lesson-id: 2171
topic-code: LAL.8.3.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Elements of a Multiple Linear Regression](#identifying-elements-of-a-multiple-linear-regression)
- [Regression Planes Passing Through the Origin](#regression-planes-passing-through-the-origin)
- [Identifying Elements of a Multiple Linear Regression That Passes Through the Origin](#identifying-elements-of-a-multiple-linear-regression-that-passes-through-the-origin)

## Prerequisites

- [Linear Regression With Matrices](<8.3.1. Linear Regression With Matrices.md>)

---

<a id="introduction"></a>
## Introduction

When carrying out a linear regression in two variables, say $x$ and $y$, our task is to find a straight line that best fits a set of data points $(x_i,y_i)$. We can easily extend this technique to situations involving three variables.

The following table shows the number of units $x_1$ and $x_2$ (in hundreds) of two goods produced by a small company in several production runs and the respective profits $y$ (in thousands of dollars) obtained by the company.

| Good Type 1 $(x_1)$ | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: |
| Good Type 2 $(x_2)$ | **$2$** | **$3$** | **$4$** | **$6$** |
| Profit $(y)$ | $6$ | $15$ | $18$ | $24$ |

Since we now have three variables, $x_1, x_2$, and $y$, our task is to find a *plane* of the form

$$
y = \beta_0+ \beta_1x_1 + \beta_2 x_2
$$

that best fits the data.

![](<../Source/8.3.3. Multiple Linear Regression With Matrices/Images/ce577d42e7ec709994f1904ff5657bba.png>)

Substituting the values of $x_1$ and $x_2$ into the regression equation and equating the results to the corresponding observed values of $y$, we obtain the following system:

$$
{β_{0} + β_{1} + 2β_{2} = 6; β_{0} + 2β_{1} + 3β_{2} = 15; β_{0} + 3β_{1} + 4β_{2} = 18; β_{0} + 4β_{1} + 6β_{2} = 24
$$

This system can be expressed in matrix notation as

$$
\begin{aligned}
\begin{bmatrix}1 & 1 & 2 \\ 1 & 2 & 3 \\ 1 & 3 & 4 \\ 1 & 4 & 6\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}6 \\ 15 \\ 18 \\ 24\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

As usual, $X$ is the design matrix, $\boldsymbol{\beta}$ is the coefficient vector, and $\mathbf{y}$ is the observation vector.

Since our points do not all lie on a plane, there will be no parameters $\beta_0$, $\beta_1$, and $\beta_2$ such that this equation is true. So instead, we seek parameters $\beta_0$, $\beta_1$, and $\beta_2$ that best approximate the solution.

In other words, we seek to find a column vector $\boldsymbol\beta$ such that

$$
\| \mathbf{y} - X\boldsymbol{\beta}\|
$$

is as small as possible.

Notice that this is exactly the least-squares problem for the system

$$
X \boldsymbol{\beta} = \mathbf{y}
$$

Therefore, the coefficients of the plane that best fits the data can be found as the solution to the corresponding least-squares problem!

Pre-multiplying the equation

$$
X \boldsymbol{\beta} = \mathbf{y}
$$

by $X^T$, we obtain the corresponding normal equation

$$
X^T \! X {\hat{\boldsymbol{\beta}}} = X^T \mathbf{y}
$$

Since the columns of $X$ are linearly independent, the matrix ${X^T}X$ is invertible, and

$$
{\hat{\boldsymbol{\beta}}} = (X^T \! X)^{-1} X^T \mathbf{y}
$$

Substituting $X$ and $\mathbf{y}$ into the formula above and simplifying, we get

$$
β^(^) = \begin{bmatrix}2 \\ 7 \\ -1\end{bmatrix}
$$

Therefore, the plane of best fit (regression plane) is

$$
y = 2+ 7x_1-x_2
$$

Note that we can use the same technique to fit data involving more variables. For example, if we have four variables $x_1, x_2, x_3$, and $y$, then we can find a four-dimensional **hyperplane** of the form

$$
y = \beta_0+ \beta_1x_1 + \beta_2 x_2 + \beta_3 x_3
$$

that best fits a set of data points $(x_{1i},x_{2i}, x_{3i}, y)$. The coefficients of this hyperplane are found by solving the corresponding least-squares problem.

Finally, applying linear regression to situations involving three or more variables is called **multiple linear regression**.

---

<a id="identifying-elements-of-a-multiple-linear-regression"></a>
## Identifying Elements of a Multiple Linear Regression

**Example:** The vector of coefficients of the plane $y=\beta_0 + \beta_1 x_1 + \beta_2 x_2$ that best fits the data above can be found using multiple linear regression as

| $x_1$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $x_2$ | **$7$** | **$6.5$** | **$6$** | **$5$** | **$3$** |
| $y$ | $1$ | $2$ | $5$ | $9$ | $15$ |

$\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2}\end{bmatrix}$

where $X$ is the corresponding design matrix and $\mathbf{y}$ is the observation vector. Find $X$ and $\mathbf{y}$ in this context.

**Explanation**

| $x_1$ | **$\color{blue}1$** | **$\color{blue}2$** | **$\color{blue}3$** | **$\color{blue}4$** | **$\color{blue}5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $x_2$ | **$\color{purple}7$** | **$\color{purple}6.5$** | **$\color{purple}6$** | **$\color{purple}5$** | **$\color{purple}3$** |
| $y$ | $\color{red}1$ | $\color{red}2$ | $\color{red}5$ | $\color{red}9$ | $\color{red}15$ |

The coefficients of the plane

$$
y=\beta_0 + \beta_1 x_1 + \beta_2 x_2
$$

that best fits the data can be found as the least-squares solution ${\hat{\boldsymbol{\beta}}}$ to the system

$$
\begin{aligned}
\begin{bmatrix}1 & 1 & 7 \\ 1 & 2 & 6.5 \\ 1 & 3 & 6 \\ 1 & 4 & 5 \\ 1 & 5 & 3\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}1 \\ 2 \\ 5 \\ 9 \\ 15\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

Thus, we have
$\begin{bmatrix}1 & 1 & 7 \\ 1 & 2 & 6.5 \\ 1 & 3 & 6 \\ 1 & 4 & 5 \\ 1 & 5 & 3\end{bmatrix}$

---

**Question 1**

```quiz
type: radio
id: ma-42323
content: |-
  | $x_{1}$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
  | --- | ---: | ---: | ---: | ---: | ---: |
  | $x_{2}$ | **$10$** | **$25$** | **$32$** | **$42$** | **$15$** |
  | $y$ | $0$ | $-5$ | $-2$ | $2$ | $7$ |

  The vector of coefficients of the plane $y = β_{0} + β_{1}x_{1} + β_{2}x_{2}$ that best fits the data above can be found using multi linear regression as

  $\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

  where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the observation vector $y$ in this context?
options:
- id: a
  content: |-
    $\begin{bmatrix}1 \\ 2 \\ 3 \\ 4 \\ 5\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & 1 & 10 \\ 1 & 2 & 25 \\ 1 & 3 & 32 \\ 1 & 4 & 42 \\ 1 & 5 & 15\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}1 & 10 \\ 2 & 25 \\ 3 & 32 \\ 4 & 42 \\ 5 & 15\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}0 \\ -5 \\ -2 \\ 2 \\ 7\end{bmatrix}$
  correct: true
- id: e
  content: |-
    $\begin{bmatrix}10 \\ 25 \\ 32 \\ 42 \\ 15\end{bmatrix}$
```

---

**Question 2**

```quiz
type: radio
id: ma-139857
content: |-
  | $x_{1}$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
  | --- | ---: | ---: | ---: | ---: | ---: |
  | $x_{2}$ | **$10$** | **$12$** | **$15$** | **$18$** | **$22$** |
  | $x_{3}$ | **$-2$** | **$0$** | **$3$** | **$7$** | **$12$** |
  | $y$ | $1$ | $2$ | $4$ | $6$ | $8$ |

  The vector of coefficients of the hyperplane $y = β_{0} + β_{1}x_{1} + β_{2}x_{2} + β_{3}x_{3}$ that best fits the data above can be found using multiple linear regression as

  $\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

  where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?
options:
- id: a
  content: |-
    $\begin{bmatrix}1 & 10 \\ 2 & 12 \\ 3 & 15 \\ 4 & 18 \\ 5 & 22\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & 1 & 10 & -2 \\ 1 & 2 & 12 & 0 \\ 1 & 3 & 15 & 3 \\ 1 & 4 & 18 & 7 \\ 1 & 5 & 22 & 12\end{bmatrix}$
  correct: true
- id: c
  content: |-
    $\begin{bmatrix}1 & 10 & -2 \\ 2 & 12 & 0 \\ 3 & 15 & 3 \\ 4 & 18 & 7 \\ 5 & 22 & 12\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 & 1 & 10 & -2 \\ 2 & 2 & 12 & 0 \\ 4 & 3 & 15 & 3 \\ 6 & 4 & 18 & 7 \\ 8 & 5 & 22 & 12\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}1 \\ 2 \\ 4 \\ 6 \\ 8\end{bmatrix}$
```

---

<a id="regression-planes-passing-through-the-origin"></a>
## Regression Planes Passing Through the Origin

Sometimes, we might need to place a constraint on the regression plane that forces it to pass through the origin.

Let's once again consider our small company. The following table shows the number of units $x_1$ and $x_2$ (in hundreds) of two goods produced by the company in several production runs and the respective profits $y$ (in thousands of dollars) obtained by the company.

| Good Type 1 $(x_1)$ | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: |
| Good Type 2 $(x_2)$ | **$2$** | **$3$** | **$4$** | **$6$** |
| Profit $(y)$ | $6$ | $15$ | $18$ | $24$ |

It's known that the profit equals zero when the company does not produce any goods, so $y=0$ when

$$
x_1=x_2 = 0
$$

The company wishes to find a plane that best fits the data *subject to* the constraint that it should pass through the point $(0,0, 0)$.

Therefore, we want to find the plane

$$
y = \beta_1 x_1 + \beta_2 x_2
$$

that best fits the given data points.

Substituting the values of $x_1$ and $x_2$ into the regression equation and equating the results to the corresponding observed values of $y$, we obtain the following system:

$$
\begin{aligned}
\begin{bmatrix}1 & 2 \\ 2 & 3 \\ 3 & 4 \\ 4 & 6\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{1} \\ β_{2}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}6 \\ 15 \\ 18 \\ 24\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

The rest of the arguments are the same as before. Since not all points lie on a plane, there will be no parameters $\beta_1$ and $\beta_2$ such that this equation is true. So instead, we seek parameters $\beta_1$ and $\beta_2$ that best approximate the solution.

---

<a id="identifying-elements-of-a-multiple-linear-regression-that-passes-through-the-origin"></a>
## Identifying Elements of a Multiple Linear Regression That Passes Through the Origin

**Example:** The vector of coefficients of the hyperplane $y = \beta_1 x_1 + \beta_2 x_2 +\beta_3 x_3$ that best fits the data above can be found using multiple linear regression as

| $x_1$ | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: |
| $x_2$ | **$-8$** | **$-2$** | **$6$** | **$15$** |
| $x_3$ | **$8$** | **$6$** | **$4$** | **$2$** |
| $y$ | $10$ | $20$ | $40$ | $60$ |

$\begin{bmatrix}β_{1} \\ β_{2} \\ β_{3}\end{bmatrix}$

where $X$ is the corresponding design matrix and $\mathbf{y}$ is the observation vector. Find $X$ and $\mathbf{y}$ in this context.

**Explanation**

| $x_1$ | **$\color{blue}1$** | **$\color{blue}2$** | **$\color{blue}3$** | **$\color{blue}4$** |
| --- | ---: | ---: | ---: | ---: |
| $x_2$ | **$\color{purple}-8$** | **$\color{purple}-2$** | **$\color{purple}6$** | **$\color{purple}15$** |
| $x_3$ | **$\color{green}8$** | **$\color{green}6$** | **$\color{green}4$** | **$\color{green}2$** |
| $y$ | $\color{red}10$ | $\color{red}20$ | $\color{red}40$ | $\color{red}60$ |

The coefficients of the hyperplane

$$
y= \beta_1 x_1 + \beta_2 x_2 +\beta_3 x_3
$$

that best fits the data can be found as the least-squares solution ${\hat{\boldsymbol{\beta}}}$ to the system

$$
\begin{aligned}
\begin{bmatrix}1 & -8 & 8 \\ 2 & -2 & 6 \\ 3 & 6 & 4 \\ 4 & 15 & 2\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{1} \\ β_{2} \\ β_{3}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}10 \\ 20 \\ 40 \\ 60\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

Thus, we have

$$
\begin{bmatrix}1 & -8 & 8 \\ 2 & -2 & 6 \\ 3 & 6 & 4 \\ 4 & 15 & 2\end{bmatrix}
$$

---

**Question 3**

```quiz
type: radio
id: ma-42300
content: |-
  | $x_{1}$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
  | --- | ---: | ---: | ---: | ---: | ---: |
  | $x_{2}$ | **$10$** | **$25$** | **$32$** | **$42$** | **$15$** |
  | $y$ | $0$ | $-5$ | $-2$ | $2$ | $7$ |

  The vector of coefficients of the plane $y = β_{1}x_{1} + β_{2}x_{2}$ that best fits the data above can be found using multiple linear regression as

  $\begin{bmatrix}β_{1} \\ β_{2}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

  where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?
options:
- id: a
  content: |-
    $\begin{bmatrix}1 & 10 & 0 \\ 2 & 25 & -5 \\ 3 & 32 & -2 \\ 4 & 42 & 2 \\ 5 & 15 & 7\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & 10 \\ 2 & 25 \\ 3 & 32 \\ 4 & 42 \\ 5 & 15\end{bmatrix}$
  correct: true
- id: c
  content: |-
    $\begin{bmatrix}1 & 1 & 10 \\ 1 & 2 & 25 \\ 1 & 3 & 32 \\ 1 & 4 & 42 \\ 1 & 5 & 15\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 \\ 2 \\ 3 \\ 4 \\ 5\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}0 \\ -5 \\ -2 \\ 2 \\ 7\end{bmatrix}$
```

---

**Question 4**

```quiz
type: radio
id: ma-139935
content: |-
  | $x_{1}$ | **$1$** | **$2$** | **$4$** | **$8$** | **$16$** |
  | --- | ---: | ---: | ---: | ---: | ---: |
  | $x_{2}$ | **$2$** | **$4$** | **$6$** | **$8$** | **$10$** |
  | $x_{3}$ | **$-10$** | **$-8$** | **$-5$** | **$-1$** | **$2$** |
  | $y$ | $5$ | $10$ | $16$ | $22$ | $30$ |

  The vector of coefficients of the hyperplane $y = β_{1}x_{1} + β_{2}x_{2} + β_{3}x_{3}$ that best fits the data above can be found using multi linear regression as

  $\begin{bmatrix}β_{1} \\ β_{2} \\ β_{3}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

  where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?
options:
- id: a
  content: |-
    $\begin{bmatrix}1 & 1 & 2 & -10 \\ 1 & 2 & 4 & -8 \\ 1 & 4 & 6 & -5 \\ 1 & 8 & 8 & -1 \\ 1 & 16 & 10 & 2\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & 2 \\ 2 & 4 \\ 4 & 6 \\ 8 & 8 \\ 16 & 10\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}5 \\ 10 \\ 16 \\ 22 \\ 30\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 & 2 & 5 \\ 2 & 4 & 10 \\ 4 & 6 & 16 \\ 8 & 8 & 22 \\ 16 & 10 & 30\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}1 & 2 & -10 \\ 2 & 4 & -8 \\ 4 & 6 & -5 \\ 8 & 8 & -1 \\ 16 & 10 & 2\end{bmatrix}$
  correct: true
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
