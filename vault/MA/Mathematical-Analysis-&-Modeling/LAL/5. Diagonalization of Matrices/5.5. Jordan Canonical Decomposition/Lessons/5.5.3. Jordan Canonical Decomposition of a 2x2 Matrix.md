# Jordan Canonical Decomposition of a 2x2 Matrix

<!--
lesson-id: 3652
topic-code: LAL.5.5.3
-->

## Table of Contents

- [Introduction](#introduction)
- [An Introductory Example](#an-introductory-example)
- [Identifying Columns of a Transformation Matrix in a Jordan Decomposition](#identifying-columns-of-a-transformation-matrix-in-a-jordan-decomposition)
- [Finding the Jordan Decomposition of a Non-Diagonalizable 2x2 Matrix](#finding-the-jordan-decomposition-of-a-non-diagonalizable-2x2-matrix)
- [Finding Elements of a Jordan Canonical Decomposition for a 2x2 Matrix: The First Column Case](#finding-elements-of-a-jordan-canonical-decomposition-for-a-2x2-matrix-the-first-column-case)
- [Finding Elements of a Jordan Canonical Decomposition for a 2x2 Matrix: The Second Column Case](#finding-elements-of-a-jordan-canonical-decomposition-for-a-2x2-matrix-the-second-column-case)

## Prerequisites

- [Jordan Canonical Form of a 2x2 Matrix](<5.5.2. Jordan Canonical Form of a 2x2 Matrix.md>)

---

<a id="introduction"></a>
## Introduction

Every square matrix $A$ can be written as

$$
A = PJP^{-1}
$$

where $J$ is a Jordan canonical form and $P$ is an invertible matrix.

Expressing $A$ in the above form is called a **Jordan canonical decomposition** of $A$, and is an extension of diagonalization that works for all square matrices.

So, to find a Jordan canonical decomposition of $A$ means to find both

- a Jordan canonical form $J$, and
- an invertible matrix $P$ that satisfies the equation $A = PJP^{-1}$.

If $A$ is diagonalizable, finding a Jordan canonical decomposition is the same as diagonalizing. In this lesson, we will focus on non-diagonalizable matrices.

---

<a id="an-introductory-example"></a>
## An Introductory Example

As an example, consider the following matrix:
$\begin{bmatrix}0 & 1 \\ -4 & 4\end{bmatrix}$

This matrix has a single eigenvalue

$$
\lambda = {\color{red}2}
$$

and, since $A$ is not scalar, it is not diagonalizable. Thus, the Jordan canonical form of $A$ is

$$
\begin{bmatrix}2 & 1 \\ 0 & 2\end{bmatrix}
$$

We can show that $A$ has the Jordan canonical decomposition

$$
A = PJP^{-1}
$$

where

$$
\begin{bmatrix}1 & 0 \\ 2 & 1\end{bmatrix}
$$

Notice that the columns of $P$ form a basis of $\mathbb{R}^2$ that generates the following chains:

$$
v_{2}→^(A - 2 I)v_{1}→^(A - 2 I)0
$$

The vectors ${\color{blue}\mathbf{v}_1}$ and ${\color{DarkBlue}\mathbf{v}_2}$ are generalized eigenvectors corresponding to

$$
\lambda = {\color{red}2}
$$

Here, ${\color{blue}\mathbf{v}_1}$ is an eigenvector of $A$, while ${\color{DarkBlue}\mathbf{v}_2}$ is a generalized eigenvector of rank $2$.

In general, the matrix $P$ of any Jordan canonical decomposition of a non-diagonalizable $2\times 2$ matrix $A$ with eigenvalue $\lambda$ must satisfy the following property:

- The columns of $P$ associated with a Jordan block $J_2(\lambda)$ are generalized eigenvectors that correspond to the eigenvalue $\lambda$, wherethe first column of $P$ is an eigenvector, andthe second column of $P$ is a generalized eigenvector of rank $2$ that is mapped to the first column under the action of $A-\lambda I$.

**Watch out!** The order of the columns of $P$ is important! The first column should *always* be an eigenvector.

---

<a id="identifying-columns-of-a-transformation-matrix-in-a-jordan-decomposition"></a>
## Identifying Columns of a Transformation Matrix in a Jordan Decomposition

**Example:** $\begin{bmatrix}3 & -4 \\ 1 & -1\end{bmatrix}$

Consider the matrices above. Given that $A = P J P^{-1}$ is a Jordan canonical decomposition of $A$, which of the following could be the second column of the matrix $P$?

$\begin{bmatrix}1 \\ 0\end{bmatrix}$

**Explanation**

First, notice that the Jordan matrix $J$ consists of one Jordan block $J = J_2(1)$.

Therefore, the columns of $P$ form the following chain:

$$
J_{2}(1) \mid: \mid v_{2}→^(A - 1 I)v_{1}→^(A - 1 I)0
$$

As a result, the second column of $P$ is a generalized eigenvector of rank $2$ corresponding to $\lambda=1$.

Now, computing $A-1I$, we get

$$
\begin{aligned}
A - 1I &= \begin{bmatrix}3 & -4 \\ 1 & -1\end{bmatrix} - 1\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} = \begin{bmatrix}2 & -4 \\ 1 & -2\end{bmatrix}
\end{aligned}
$$

Among the given vectors, only $\mathbf x$ is a generalized eigenvector of rank $2$, since

$\begin{bmatrix}2 & -4 \\ 1 & -2\end{bmatrix}$
and
$\begin{bmatrix}2 & -4 \\ 1 & -2\end{bmatrix}$

Therefore, we can write

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}→^(A - 1 I)\begin{bmatrix}2 \\ 1\end{bmatrix}→^(A - 1 I)0
$$

The other vectors do not satisfy this condition.

---

**Question 1**

```quiz
type: radio
id: ma-112432
content: |-
  $A = \begin{bmatrix}-1 & 1 \\ -1 & 1\end{bmatrix}, J = \begin{bmatrix}0 & 1 \\ 0 & 0\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, which of the following could be the first column of the matrix $P$?

  $x = \begin{bmatrix}1 \\ 1\end{bmatrix}, y = \begin{bmatrix}-1 \\ 0\end{bmatrix}, z = \begin{bmatrix}2 \\ 0\end{bmatrix}$
options:
- id: a
  content: |-
    $x$ and $z$ only
- id: b
  content: |-
    $y$ only
- id: c
  content: |-
    $z$ only
- id: d
  content: |-
    $x$ only
  correct: true
- id: e
  content: |-
    $y$ and $z$ only
```

---

**Question 2**

```quiz
type: radio
id: ma-112431
content: |-
  $A = \begin{bmatrix}6 & 9 \\ -1 & 0\end{bmatrix}, J = \begin{bmatrix}3 & 1 \\ 0 & 3\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, which of the following could be the second column of the matrix $P$?

  $x = \begin{bmatrix}3 \\ -1\end{bmatrix}, y = \begin{bmatrix}-6 \\ 2\end{bmatrix}, z = \begin{bmatrix}1 \\ 0\end{bmatrix}$
options:
- id: a
  content: |-
    $x$ and $y$ only
- id: b
  content: |-
    $y$ only
- id: c
  content: |-
    $z$ only
  correct: true
- id: d
  content: |-
    $y$ and $z$ only
- id: e
  content: |-
    $x$ only
```

---

<a id="finding-the-jordan-decomposition-of-a-non-diagonalizable-2x2-matrix"></a>
## Finding the Jordan Decomposition of a Non-Diagonalizable 2x2 Matrix

Let's find the Jordan canonical decomposition of the matrix $A$, given by

$$
\begin{bmatrix}-4 & 4 \\ -1 & 0\end{bmatrix}
$$

To find a Jordan canonical decomposition of a $2\times 2$ matrix, we follow a three-step **Jordan decomposition algorithm:**

**Step 1:** Find the eigenvalues of the matrix.

We solve the characteristic equation

$$
\vert A - \lambda I \vert = 0
$$

For the given matrix, there is only one eigenvalue, namely

$$
\lambda = -2
$$

**Step 2:** Find a basis of $\mathbb{R}^2$ consisting of the generalized eigenvectors corresponding to each eigenvalue.

Applying the usual method for computing eigenvectors, we get an eigenvector

$$
\begin{bmatrix}2 \\ 1\end{bmatrix}
$$

corresponding to the eigenvalue

$$
\lambda = -2
$$

This is the first vector of our basis.

To find the second vector of our basis, we have to find a generalized eigenvector ${\color{blue}\mathbf{v_2}}$ of $A$ corresponding to

$$
\lambda = -2
$$

So, our vectors ${\color{red}\mathbf{v_1}}$ and ${\color{blue}\mathbf{v_2}}$ must form the following chain:

$$
{\color{blue}\mathbf{v_2}} \xrightarrow{A+2I} {\color{red}\mathbf{v_1}} \xrightarrow{A+2I} \mathbf{0}
$$

Therefore, to find ${\color{blue}\mathbf{v}_2}$ we must solve the equation

$$
(A +2I){\color{blue}\mathbf{v}_2} = {\color{red}\mathbf{v_1}}
$$

Solving this equation using our usual methods gives us the solution

$$
\begin{bmatrix}-1 \\ 0\end{bmatrix}
$$

Notice that the set

$$
\left\{{\color{red}\mathbf{v_1}}, {\color{blue}\mathbf{v_2}}\right\}
$$

is linearly independent and thus forms a basis of $\mathbb{R}^2$.

**Step 3:** Construct the matrices $J$ and $P$.

Now that we have our special basis, we can construct a Jordan canonical form $J$ and invertible matrix $P$.
The matrix $J$ is represented by a single $2 \times 2$ Jordan block:

$$
\begin{bmatrix}-2 & 1 \\ 0 & -2\end{bmatrix}
$$

Then, the columns of $P$ are the vectors $\color{red}\mathbf{v}_1$ and $\color{blue}\mathbf{v}_2$, where the first column *must* be the eigenvector ${\color{red}\mathbf{v_1}}$. So, we get

$$
[ \mid \mid \mid; v_{1} \mid v_{2}; \mid \mid \mid ]
$$

Finally, the Jordan canonical decomposition of $A$ is as follows:

$$
\begin{bmatrix}-4 & 4 \\ -1 & 0\end{bmatrix}
$$

**Watch out!** In Jordan decomposition, the order in which we write our vectors in the matrix $P$ is important! The first column of $P$ must *always* be the eigenvector.

---

<a id="finding-elements-of-a-jordan-canonical-decomposition-for-a-2x2-matrix-the-first-column-case"></a>
## Finding Elements of a Jordan Canonical Decomposition for a 2x2 Matrix: The First Column Case

**Example:** $\begin{bmatrix}8 & 4 \\ -4 & 0\end{bmatrix}$

Consider the matrices above. Given that $A = P J P^{-1}$ is a Jordan canonical decomposition of $A$, find the value of $\dfrac{a}{b}$.

**Explanation**

From the Jordan matrix $J$, we deduce that the eigenvalues of $A$ are

$$
\lambda_1=\lambda_2=4
$$

As a result, the first column of $P$ corresponds to an eigenvector of the eigenvalue $\lambda=4$.

Now, computing $A-4I$, we get

$$
\begin{aligned}
A - 4I &= \begin{bmatrix}8 & 4 \\ -4 & 0\end{bmatrix} - (4)\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \\
&= \begin{bmatrix}4 & 4 \\ -4 & -4\end{bmatrix}
\end{aligned}
$$

So, we have a system of linear equations with the augmented matrix $M$, which we reduce to row echelon form using Gaussian elimination, as follows:

$$
\begin{aligned}
M &= \begin{bmatrix}4 & 4 & 0 \\ -4 & -4 & 0\end{bmatrix} \mid R_{2} \mid:=R_{2} + R_{1} \\
&= ∼\begin{bmatrix}4 & 4 & 0 \\ 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

The reduced matrix above has one pivot column (the $1$st one). Thus, $x_2$ is a free variable. From the first equation, we obtain $x_1=-x_2$. Hence, the general solution is

$$
\begin{bmatrix}-x_{2} \\ x_{2}\end{bmatrix}
$$

Therefore, setting $x_2=1$, we get the eigenvector

$$
\begin{bmatrix}a \\ b\end{bmatrix}
$$

Finally,

$$
\dfrac{a}{b}=\dfrac{-1}{1} = -1
$$

---

**Question 3**

```quiz
type: radio
id: ma-109368
content: |-
  $A = \begin{bmatrix}9 & 4 \\ -1 & 5\end{bmatrix}, J = \begin{bmatrix}7 & 1 \\ 0 & 7\end{bmatrix}, P = \begin{bmatrix}a & ∗ \\ b & ∗\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, find the value of $\frac{b}{a}$.
options:
- id: a
  content: |-
    $-\frac{1}{2}$
  correct: true
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $1$
- id: d
  content: |-
    $-2$
- id: e
  content: |-
    $-1$
```

---

**Question 4**

```quiz
type: radio
id: ma-108732
content: |-
  $A = \begin{bmatrix}4 & 4 \\ -1 & 0\end{bmatrix}, J = \begin{bmatrix}2 & 1 \\ 0 & 2\end{bmatrix}, P = \begin{bmatrix}a & ∗ \\ b & ∗\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, find the value of $\frac{a}{b}$.
options:
- id: a
  content: |-
    $-4$
- id: b
  content: |-
    $1$
- id: c
  content: |-
    $-2$
  correct: true
- id: d
  content: |-
    $3$
- id: e
  content: |-
    $0$
```

---

<a id="finding-elements-of-a-jordan-canonical-decomposition-for-a-2x2-matrix-the-second-column-case"></a>
## Finding Elements of a Jordan Canonical Decomposition for a 2x2 Matrix: The Second Column Case

**Example:** $\begin{bmatrix}9 & 5 \\ -5 & -1\end{bmatrix}$

Consider the matrices above. Given that $A = P J P^{-1}$ is a Jordan canonical decomposition of $A$, find the second column of the matrix $P$.

**Explanation**

From the Jordan matrix $J$, we deduce that the eigenvalues of $A$ are

$$
\lambda_1=\lambda_2=4
$$

As a result, we obtain that

- the first column of $P$ corresponds to the eigenvector $\begin{bmatrix}-1 \\ 1\end{bmatrix}$ of the eigenvalue $\lambda=4$,
- the second column of $P$ corresponds to the vector $\mathbf{v}_2$ such that $(A-4I)\mathbf{v}_2 = \mathbf{v}_1$.

Now, computing $A-4I$, we get

$$
\begin{aligned}
A - 4I &= \begin{bmatrix}9 & 5 \\ -5 & -1\end{bmatrix} - (4)\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \\
&= \begin{bmatrix}5 & 5 \\ -5 & -5\end{bmatrix}
\end{aligned}
$$

So, the matrix equation

$$
(A - 4I)\mathbf{v}_2 = \mathbf{v}_1
$$

is equivalent to the system of linear equations with the augmented matrix $M$ which we reduce to row echelon form using Gaussian elimination, as follows:

$$
\begin{aligned}
M &= \begin{bmatrix}5 & 5 & -1 \\ -5 & -5 & 1\end{bmatrix} \mid R_{2} \mid:=R_{2} + R_{1} \\
&= ∼\begin{bmatrix}5 & 5 & -1 \\ 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

The reduced matrix above has one pivot column (the $1$st one). Thus, $x_2$ is a free variable. From the first equation, we obtain

$$
x_1=-\dfrac 15 - x_2
$$

Hence, the general solution is

$$
\begin{bmatrix}-\frac{1}{5} - x_{2} \\ x_{2}\end{bmatrix}
$$

Therefore, setting $x_2=0$, we get

$$
\begin{bmatrix}a \\ b\end{bmatrix}
$$

---

**Question 5**

```quiz
type: radio
id: ma-109381
content: |-
  $A = \begin{bmatrix}3 & -4 \\ 1 & -1\end{bmatrix}, J = \begin{bmatrix}1 & 1 \\ 0 & 1\end{bmatrix}, P = \begin{bmatrix}2 & a \\ 1 & b\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, find the value of $a - 2b$.
options:
- id: a
  content: |-
    $-1$
- id: b
  content: |-
    $2$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $1$
  correct: true
- id: e
  content: |-
    $-2$
```

---

**Question 6**

```quiz
type: radio
id: ma-108728
content: |-
  $A = \begin{bmatrix}1 & 4 \\ -1 & 5\end{bmatrix}, J = \begin{bmatrix}3 & 1 \\ 0 & 3\end{bmatrix}, P = \begin{bmatrix}2 & a \\ 1 & b\end{bmatrix}$

  Consider the matrices above. Given that $A = PJP^{-1}$ is a Jordan canonical decomposition of $A$, find the value of $a - 2b$.
options:
- id: a
  content: |-
    $2$
- id: b
  content: |-
    $-1$
  correct: true
- id: c
  content: |-
    $-2$
- id: d
  content: |-
    $0$
- id: e
  content: |-
    $3$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
