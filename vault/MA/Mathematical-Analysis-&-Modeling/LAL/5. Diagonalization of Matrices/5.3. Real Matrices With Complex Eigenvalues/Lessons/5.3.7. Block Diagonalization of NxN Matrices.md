# Block Diagonalization of NxN Matrices

<!--
lesson-id: 2019
topic-code: LAL.5.3.7
-->

## Table of Contents

- [Introduction](#introduction)
- [The Block Diagonalization Theorem for a 3x3 Matrix](#the-block-diagonalization-theorem-for-a-3x3-matrix)
- [Finding the Eigenvalues and Corresponding Eigenvectors of a Matrix in Block Diagonal Form](#finding-the-eigenvalues-and-corresponding-eigenvectors-of-a-matrix-in-block-diagonal-form)
- [Finding a Block Diagonalization of a 3x3 Matrix](#finding-a-block-diagonalization-of-a-3x3-matrix)
- [Calculating the Invertible Matrix in Block Diagonal Form of a 3x3 Matrix](#calculating-the-invertible-matrix-in-block-diagonal-form-of-a-3x3-matrix)
- [Generalization of Block Diagonalization to NxN Matrices](#generalization-of-block-diagonalization-to-nxn-matrices)
- [Calculating the Invertible Matrix in Block Diagonal Form of an NxN Matrix](#calculating-the-invertible-matrix-in-block-diagonal-form-of-an-nxn-matrix)

## Prerequisites

- [Partitioned and Block Matrices](<../../../2. Matrices/2.1. Determinants/Lessons/2.1.10. Partitioned and Block Matrices.md>)
- [Diagonalizing a 3x3 Matrix in the General Case](<../../5.2. Diagonalization/Lessons/5.2.4. Diagonalizing a 3x3 Matrix in the General Case.md>)
- [Reducing Real 2x2 Matrices to Rotation-Scaling Form](<5.3.6. Reducing Real 2x2 Matrices to Rotation-Scaling Form.md>)

---

<a id="introduction"></a>
## Introduction

A square matrix $A$ is **block diagonalizable** if it is similar to a block diagonal matrix $B$. That is, there exists a block diagonal matrix $B$ and an invertible matrix $P$ such that

$$
A=PBP^{-1}
$$

For example, the matrix

$$
\begin{bmatrix}2 & 0 & 0 \\ -2 & 3 & 2 \\ -1 & -2 & 3\end{bmatrix}
$$

is block diagonalizable because it can be factored as follows:

$$
\begin{bmatrix}2 & 0 & 0 \\ -2 & 3 & 2 \\ -1 & -2 & 3\end{bmatrix}
$$

Here, the matrix $A$ has eigenvalues

$$
{\color{red}\lambda_1 = 2},\qquad {\color{blue}\lambda_2=3-2\textrm i},\qquad {\color{blue}\lambda_3=3+2\textrm i}
$$

Notice that

$$
\lambda_2 = \overline{\lambda_3}
$$

The eigenvectors of ${\color{red}\lambda_1}, {\color{blue}\lambda_2}$, and ${\color{blue}\lambda_3}$, respectively, are

$$
\begin{bmatrix}1 \\ 0 \\ 1\end{bmatrix}
$$

Again, notice that

$$
\mathbf{v}_2 = \overline{\mathbf{v}_3}
$$

The first column of $P$ corresponds to the eigenvector ${\color{red}\mathbf{v}_1}$, and the second and third columns correspond to the real and the imaginary parts of ${\color{blue}\mathbf{v}_2}$.

---

<a id="the-block-diagonalization-theorem-for-a-3x3-matrix"></a>
## The Block Diagonalization Theorem for a 3x3 Matrix

We have the following theorem regarding the block diagonalization of a real $3\!\times\! 3$ matrix:

> *If $A$ is a real $3\times 3$ matrix with one real eigenvalue ${\color{red}\lambda_1}$ with corresponding eigenvector ${\color{red}{\mathbf{v}_1}}$, and two complex eigenvalues ${\color{blue}\lambda_2} = \overline{\color{blue}\lambda}_{\color{blue}3}$ with corresponding eigenvectors ${\color{blue}\mathbf{v}_2} = \overline{\color{blue}\mathbf{v}}_{\color{blue}3}$, then*
> $A=PBP^{-1}$,
> *where the matrices $B$ and $P$ are given by*
> $\begin{bmatrix}λ_{1} & 0 & 0 \\ 0 & Re(λ_{2}) & Im(λ_{2}) \\ 0 & -Im(λ_{2}) & Re(λ_{2})\end{bmatrix}$

In other words, if $A$ is a real $3\!\times\! 3$ matrix with one real eigenvalue and two complex eigenvalues, then $A$ is block diagonalizable.

Note the following:

- The block of $B$ constructed from the real and imaginary parts of $\color{blue}\lambda_2$ is a rotation-scaling matrix.
- We can write the blocks of $B$ in either order. However, the order of the columns of $P$ *must* correspond to the order of the blocks.

---

<a id="finding-the-eigenvalues-and-corresponding-eigenvectors-of-a-matrix-in-block-diagonal-form"></a>
## Finding the Eigenvalues and Corresponding Eigenvectors of a Matrix in Block Diagonal Form

**Example:** $\begin{bmatrix}4 & -1 & 1 \\ 1 & 1 & 3 \\ 1 & 2 & 1\end{bmatrix}$

Consider the nonsingular matrix $P$ and the block diagonal matrix $B$ shown above, where the $2 \times 2$ block of $B$ is a rotation-scaling matrix.
Given that $A=PBP^{-1}$, what are the complex (not real) eigenvalues and the corresponding eigenvectors of $A$?

**Explanation**

Recall that if $A$ is a $3\times 3$ real matrix that has one real eigenvalue ${\color{red}\lambda_1}$ with corresponding eigenvector ${\color{red}{\mathbf{v}_1}}$, and two complex eigenvalues

$$
{\color{blue}\lambda_2} = \overline{\color{blue}\lambda}_{\color{blue}3}
$$

with corresponding eigenvectors

$$
{\color{blue}\mathbf{v}_2} = \overline{\color{blue}\mathbf{v}}_{\color{blue}3}
$$

then $A=PBP^{-1}$, where
$\begin{bmatrix}λ_{1} & 0 & 0 \\ 0 & Re(λ_{2}) & Im(λ_{2}) \\ 0 & -Im(λ_{2}) & Re(λ_{2})\end{bmatrix}$
Notice that swapping the blocks on the main diagonal of $B$ will result in switching the corresponding columns in $P$.

- From our block diagonal matrix
$\begin{bmatrix}1 & 3 & 0 \\ -3 & 1 & 0 \\ 0 & 0 & -3\end{bmatrix}$
we deduce the complex eigenvalues are ${\color{blue}\lambda_{1,2}=1 \pm 3\textrm{i}}$ and the real eigenvalue is ${\color{red}\lambda_3=-3}$.
- From our matrix
$\begin{bmatrix}4 & -1 & 1 \\ 1 & 1 & 3 \\ 1 & 2 & 1\end{bmatrix}$
we deduce that the complex and real eigenvectors are
$\begin{bmatrix}4∓\textrm{i} \\ 1 ± \textrm{i} \\ 1 ± 2\textrm{i}\end{bmatrix}$

---

**Question 1:**

```quiz
type: radio
id: ma-38656
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $P = \begin{bmatrix}1 & 4 & -1 \\ 0 & 3 & -2 \\ 2 & 0 & 3\end{bmatrix}, B = \begin{bmatrix}1 & 2 & 0 \\ -2 & 1 & 0 \\ 0 & 0 & -2\end{bmatrix}$

  Consider the nonsingular matrix $P$ and the block diagonal matrix $B$ shown above, where the $2 \cdot 2$ block of $B$ is a rotation-scaling matrix.
  Given that $A = PBP^{-1}$, what are the complex (not real) eigenvalues and the corresponding eigenvectors of $A$?
options:
- id: a
  content: |-
    $λ =-1 ± 2\textrm{i}$ and $v = \begin{bmatrix}4 ± \textrm{i} \\ 3 \\ ± 2\textrm{i}\end{bmatrix}$
- id: b
  content: |-
    $λ = 1 ± 2\textrm{i}$ and $v = \begin{bmatrix}1 ± 4\textrm{i} \\ ± 3\textrm{i} \\ 2\end{bmatrix}$
- id: c
  content: |-
    $λ = 1 ± 2\textrm{i}$ and $v = \begin{bmatrix}4 ± \textrm{i} \\ 3\textrm{i} \\ ± 2\textrm{i}\end{bmatrix}$
- id: d
  content: |-
    $λ = 1 ± 4\textrm{i}$ and $v = \begin{bmatrix}1 ± 2\textrm{i} \\ -2 ± 3\textrm{i} \\ 3\end{bmatrix}$
- id: e
  content: |-
    $λ =-1 ± 4\textrm{i}$ and $v = \begin{bmatrix}1 ± 2\textrm{i} \\ 3 ± 2\textrm{i} \\ ± 3\textrm{i}\end{bmatrix}$
```

---

**Question 2**

```quiz
type: radio
id: ma-38665
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $P = \begin{bmatrix}1 & -2 & 0 \\ 5 & 0 & -2 \\ 0 & 1 & -1\end{bmatrix}, B = \begin{bmatrix}-7 & 0 & 0 \\ 0 & -2 & 5 \\ 0 & -5 & -2\end{bmatrix}$

  Consider the nonsingular matrix $P$ and the block diagonal matrix $B$ shown above, where the $2 \cdot 2$ block of $B$ is a rotation-scaling matrix.
  Given that $A = PBP^{-1}$, what are the complex (not real) eigenvalues and the corresponding eigenvectors of $A$?
options:
- id: a
  content: |-
    $λ = 1 ± 5\textrm{i}$ and $v = \begin{bmatrix}-2 \\ ∓2\textrm{i} \\ 1∓\textrm{i}\end{bmatrix}$
- id: b
  content: |-
    $λ = 5 ± 2\textrm{i}$ and $v = \begin{bmatrix}2\textrm{i} \\ ∓2\textrm{i} \\ 1∓\textrm{i}\end{bmatrix}$
- id: c
  content: |-
    $λ =-2 ± 5\textrm{i}$ and $v = \begin{bmatrix}-2 \\ 1 ± 2\textrm{i} \\ ∓\textrm{i}\end{bmatrix}$
- id: d
  content: |-
    $λ =-2 ± 5\textrm{i}$ and $v = \begin{bmatrix}-2 \\ ∓2\textrm{i} \\ 1∓\textrm{i}\end{bmatrix}$
- id: e
  content: |-
    $λ = 5 ± 2\textrm{i}$ and $v = \begin{bmatrix}-2 \\ ± 2 \\ 1∓\textrm{i}\end{bmatrix}$
```

---

<a id="finding-a-block-diagonalization-of-a-3x3-matrix"></a>
## Finding a Block Diagonalization of a 3x3 Matrix

**Example:** $\begin{bmatrix}-2 & 0 & 1 \\ 3 & -1 & 5 \\ -1 & 0 & -2\end{bmatrix}$
Consider the matrix $A$ shown above. Find a block diagonal matrix $B$, where the $2 \times 2$ block represents a rotation-scaling matrix, such that $A=PBP^{-1}$.

**Explanation**

Recall that if $A$ is a real $3\times 3$ matrix that has one real eigenvalue ${\color{red}\lambda_1}$ with corresponding eigenvector ${\color{red}{\mathbf{v}_1}}$, and two complex eigenvalues

$$
{\color{blue}\lambda_2} = \overline{\color{blue}\lambda}_{\color{blue}3}
$$

with corresponding eigenvectors

$$
{\color{blue}\mathbf{v}_2} = \overline{\color{blue}\mathbf{v}}_{\color{blue}3}
$$

then $A=PBP^{-1}$, where
$\begin{bmatrix}λ_{1} & 0 & 0 \\ 0 & Re(λ_{2}) & Im(λ_{2}) \\ 0 & -Im(λ_{2}) & Re(λ_{2})\end{bmatrix}$
Swapping the blocks on the main diagonal of $B$ will result in switching the corresponding columns in $P$.

To compute the eigenvalues of $A$, we solve the characteristic equation as follows:

$$
\begin{aligned}
det(A - λI) &= 0 \\
\begin{vmatrix}-2 - λ & 0 & 1 \\ 3 & -1 - λ & 5 \\ -1 & 0 & -2 - λ\end{vmatrix} &= 0 \\
(-1 - λ)((-2 - λ)^{2} - 1(-1)) &= 0 \\
(-1 - λ)(λ^{2} + 4λ + 5) &= 0
\end{aligned}
$$

So, the real eigenvalue is

$$
\lambda_1={\color{red}-1}
$$

Now, using the quadratic formula, we get

$$
\begin{aligned}
λ_{2, 3} &= (-4 ± \sqrt{(4)^{2} - 4 \cdot 1 \cdot 5})/(2) \\
&= \frac{-4 ± \sqrt{-4}}{2} \\
&= \frac{-4 ± 2\textrm{i}}{2} \\
&=-2 ± \textrm{i}
\end{aligned}
$$

Therefore, the block diagonal matrix is

$$
\begin{bmatrix}-1 & 0 & 0 \\ 0 & -2 & 1 \\ 0 & -1 & -2\end{bmatrix}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-38655
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $A = \begin{bmatrix}1 & 0 & -1 \\ -2 & 3 & 6 \\ 1 & 0 & 1\end{bmatrix}$

  Consider the matrix $A$ shown above. Find a block diagonal matrix $B$, where the $2 \cdot 2$ block represents a rotation-scaling matrix, such that $A = PBP^{-1}$.
options:
- id: a
  content: |-
    $B = \begin{bmatrix}0 & 0 & 3 \\ 1 & 1 & 0 \\ -1 & 1 & 0\end{bmatrix}$
- id: b
  content: |-
    $B = \begin{bmatrix}0 & 3 & 0 \\ 1 & 0 & 1 \\ -1 & 0 & 1\end{bmatrix}$
- id: c
  content: |-
    $B = \begin{bmatrix}1 & 0 & 0 \\ 0 & 3 & 1 \\ 0 & -1 & 1\end{bmatrix}$
- id: d
  content: |-
    $B = \begin{bmatrix}1 & 0 & 0 \\ 0 & 1 & 1 \\ 0 & -1 & 3\end{bmatrix}$
- id: e
  content: |-
    $B = \begin{bmatrix}3 & 0 & 0 \\ 0 & 1 & 1 \\ 0 & -1 & 1\end{bmatrix}$
```

---

**Question 4:**

```quiz
type: radio
id: ma-38662
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $A = \begin{bmatrix}2 & 5 & 0 \\ -1 & -2 & 0 \\ 1 & 7 & -1\end{bmatrix}$

  Consider the matrix $A$ shown above. Find a block diagonal matrix $B$, where the $2 \cdot 2$ block represents a rotation-scaling matrix, such that $A = PBP^{-1}$.
options:
- id: a
  content: |-
    $B = \begin{bmatrix}-1 & 0 & 0 \\ 0 & -1 & 0 \\ 0 & 0 & 1\end{bmatrix}$
- id: b
  content: |-
    $B = \begin{bmatrix}1 & 0 & 0 \\ 0 & 0 & 1 \\ 0 & -1 & 1\end{bmatrix}$
- id: c
  content: |-
    $B = \begin{bmatrix}-1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & -1\end{bmatrix}$
- id: d
  content: |-
    $B = \begin{bmatrix}0 & 1 & 0 \\ -1 & 0 & 0 \\ 0 & 0 & 1\end{bmatrix}$
- id: e
  content: |-
    $B = \begin{bmatrix}-1 & 0 & 0 \\ 0 & 0 & 1 \\ 0 & -1 & 0\end{bmatrix}$
```

---

<a id="calculating-the-invertible-matrix-in-block-diagonal-form-of-a-3x3-matrix"></a>
## Calculating the Invertible Matrix in Block Diagonal Form of a 3x3 Matrix

**Example:** Consider the vectors $\mathbf{v}_1$ and $\mathbf{v}_2$ and the block diagonal matrix $B$ shown below, where the $2 \times 2$ block of $B$ represents a rotation-scaling matrix:
$\begin{bmatrix}2 \\ -1 \\ 5\end{bmatrix}$
The vectors $\mathbf{v}_1$ and $\mathbf{v}_2$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $\lambda_1=2$ and $\lambda_2=1-\textrm{i}$, respectively.
Given that $A=PBP^{-1}$, find the matrix $P$.

**Explanation**

Recall that if $A$ is a real $3\times 3$ matrix that has one real eigenvalue ${\color{red}\lambda_1}$ with corresponding eigenvector ${\color{red}{\mathbf{v}_1}}$, and two complex eigenvalues

$$
{\color{blue}\lambda_2} = \overline{\color{blue}\lambda}_{\color{blue}3}
$$

with corresponding eigenvectors

$$
{\color{blue}\mathbf{v}_2} = \overline{\color{blue}\mathbf{v}}_{\color{blue}3}
$$

then $A=PBP^{-1}$, where
$\begin{bmatrix}λ_{1} & 0 & 0 \\ 0 & Re(λ_{2}) & Im(λ_{2}) \\ 0 & -Im(λ_{2}) & Re(λ_{2})\end{bmatrix}$
Swapping the blocks on the main diagonal of $B$ will result in switching the corresponding columns in $P$.

From our block diagonal matrix
$\begin{bmatrix}2 & 0 & 0 \\ 0 & 1 & -1 \\ 0 & 1 & 1\end{bmatrix}$
we deduce that the first column of $P$ will correspond to the real eigenvector ${{\color{red}{\mathbf{v}_1}}}$, and the second and third columns of $P$ will correspond to the complex eigenvector ${\color{blue}\mathbf{v}_2}$. Therefore,

$$
\begin{bmatrix}2 & 7 & -2 \\ -1 & 0 & -3 \\ 5 & -2 & -1\end{bmatrix}
$$

---

**Question 5:**

```quiz
type: radio
id: ma-38660
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $v_{1}$ and $v_{3}$ and the block diagonal matrix $B$ shown below, where the $2 \cdot 2$ block of $B$ represents a rotation-scaling matrix:

  $v_{1} = \begin{bmatrix}5 + 7\textrm{i} \\ 1 + \textrm{i} \\ \textrm{i}\end{bmatrix}, v_{3} = \begin{bmatrix}1 \\ 0 \\ -2\end{bmatrix}, B = \begin{bmatrix}-1 & -2 & 0 \\ 2 & -1 & 0 \\ 0 & 0 & -2\end{bmatrix}$
  The vectors $v_{1}$ and $v_{3}$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $λ_{1} =-1 - 2\textrm{i}$ and $λ_{3} =-2$, respectively.
  Given that $A = PBP^{-1}$, which of the following could be the matrix $P$?
options:
- id: a
  content: |-
    $\begin{bmatrix}5 & 1 & 7 \\ 1 & 0 & 1 \\ 0 & -2 & 1\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}7 & 5 & 1 \\ 1 & 1 & 0 \\ 1 & 0 & -2\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}1 & 7 & 5 \\ 0 & 1 & 1 \\ -2 & 1 & 1\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 & 5 & 7 \\ 0 & 1 & 1 \\ -2 & 0 & 1\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}5 & 7 & 1 \\ 1 & 1 & 0 \\ 0 & 1 & -2\end{bmatrix}$
```

---

**Question 6:**

```quiz
type: radio
id: ma-38661
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $v_{1}$ and $v_{2}$ and the block diagonal matrix $B$ shown below, where the $2 \cdot 2$ block of $B$ represents a rotation-scaling matrix:

  $v_{1} = \begin{bmatrix}1 \\ -7 \\ 0\end{bmatrix}, v_{2} = \begin{bmatrix}2\textrm{i} - 1 \\ -\textrm{i} \\ -2 + 4\textrm{i}\end{bmatrix}, B = \begin{bmatrix}-1 & 0 & 0 \\ 0 & -2 & 3 \\ 0 & -3 & -2\end{bmatrix}$
  The vectors $v_{1}$ and $v_{2}$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $λ_{1} =-1$ and $λ_{2} =-2 + 3\textrm{i}$, respectively.
  Given that $A = PBP^{-1}$, which of the following could be the matrix $P$?
options:
- id: a
  content: |-
    $\begin{bmatrix}1 & 2 & -1 \\ -7 & -1 & 0 \\ 0 & 4 & -2\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & -1 & 2 \\ -7 & 0 & -1 \\ 0 & -2 & 4\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}-1 & 1 & 2 \\ 0 & -7 & -1 \\ -2 & 0 & 4\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}2 & -1 & 1 \\ -1 & 0 & -7 \\ 4 & -2 & 0\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}-1 & 2 & 1 \\ 0 & -1 & -7 \\ -2 & 4 & 0\end{bmatrix}$
```

---

<a id="generalization-of-block-diagonalization-to-nxn-matrices"></a>
## Generalization of Block Diagonalization to NxN Matrices

We can find a block diagonalization for any real $n\!\times\! n$ matrix $A$. This means that for any square matrix $A$, we can construct a block diagonal matrix $B$ and invertible matrix $P$ such that

$$
A=PBP^{-1}
$$

where the blocks of $B$ are either:

- $1\!\times\! 1$ blocks containing the real eigenvalues of $A$, or
- $2\!\times\! 2$ blocks containing the rotation-scaling matrices corresponding to the complex (not real) eigenvalues of $A$.

The columns of $P$ form bases of the eigenspaces for the real eigenvectors, or come in the pairs

$$
\left(\textrm{Re}(\mathbf{v}), \textrm{Im}(\mathbf{v})\right)
$$

for the non-real eigenvectors.

Let's see an example of block diagonalization for higher-dimensional matrices.

---

<a id="calculating-the-invertible-matrix-in-block-diagonal-form-of-an-nxn-matrix"></a>
## Calculating the Invertible Matrix in Block Diagonal Form of an NxN Matrix

**Example:** Consider the vectors $\mathbf{v}_1$ and $\mathbf{v}_3$ and the block diagonal matrix $B$ shown below, where both $2 \times 2$ blocks of $B$ represent rotation-scaling matrices:
$\begin{bmatrix}-3 + 2\textrm{i} \\ -1 + 3\textrm{i} \\ 4 + \textrm{i} \\ -\textrm{i}\end{bmatrix}$
The vectors $\mathbf{v}_1$ and $\mathbf{v}_3$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $\lambda_1=7 + 4\textrm{i}$ and $\lambda_3=-3 + 2\textrm{i}$, respectively.
Given that $A=PBP^{-1}$, find a possible matrix $P$.

**Explanation**

Recall that if $A$ is a $4\times 4$ real matrix that has complex eigenvalues

$\qquad$

$$
{\color{red}\lambda_1}=\overline{\color{red}\lambda}_{\color{red}2} \quad
$$

and

$$
\quad {\color{blue}\lambda_3}=\overline{\color{blue}\lambda}_{\color{blue}4}
$$

with corresponding eigenvectors

$\qquad$

$$
{\color{red}\mathbf{v}_1} = \overline{\color{red}\mathbf{v}}_{\color{red}2} \quad
$$

and

$$
\quad {\color{blue}\mathbf{v}_3} = \overline{\color{blue}\mathbf{v}}_{\color{blue}4}
$$

then $A=PBP^{-1}$, where
$\begin{bmatrix}Re(λ_{1}) & Im(λ_{1}) & 0 & 0 \\ -Im(λ_{1}) & Re(λ_{1}) & 0 & 0 \\ 0 & 0 & Re(λ_{3}) & Im(λ_{3}) \\ 0 & 0 & -Im(λ_{3}) & Re(λ_{3})\end{bmatrix}$
and
$[\mid \mid \mid \mid \mid \mid \mid; Re(v_{1})\begin{vmatrix}Im(v_{1}) & Re(v_{3})\end{vmatrix}Im(v_{3}); \mid \mid \mid \mid \mid \mid \mid]$
Swapping the blocks on the main diagonal of $B$ will result in switching the corresponding columns in $P$.

From our block diagonal matrix
$\begin{bmatrix}7 & 4 & 0 & 0 \\ -4 & 7 & 0 & 0 \\ 0 & 0 & -3 & 2 \\ 0 & 0 & -2 & -3\end{bmatrix}$
we deduce that the first and second columns of $P$ will correspond to the complex eigenvector ${\color{red}{\mathbf{v}_1}}$, and the third and forth columns of $P$ will correspond to the complex eigenvector ${\color{blue}\mathbf{v}_3}$. Therefore,
$\begin{bmatrix}-3 & 2 & 0 & 1 \\ -1 & 3 & 7 & 0 \\ 4 & 1 & 1 & -2 \\ 0 & -1 & 1 & -1\end{bmatrix}$

---

**Question 7:**

```quiz
type: radio
id: ma-38667
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $v_{1}$ and $v_{3}$ and the block diagonal matrix $B$ shown below, where both $2 \cdot 2$ blocks of $B$ represent rotation-scaling matrices:

  $v_{1} = \begin{bmatrix}1 - \textrm{i} \\ 2 + \textrm{i} \\ 5\textrm{i} \\ 2 - \textrm{i}\end{bmatrix}, v_{3} = \begin{bmatrix}3 + 2\textrm{i} \\ 1 - \textrm{i} \\ 3 - \textrm{i} \\ -5 + 2\textrm{i}\end{bmatrix}, B = \begin{bmatrix}2 & -1 & 0 & 0 \\ 1 & 2 & 0 & 0 \\ 0 & 0 & 5 & 1 \\ 0 & 0 & -1 & 5\end{bmatrix}$
  The vectors $v_{1}$ and $v_{3}$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $λ_{1} = 2 - \textrm{i}$ and $λ_{3} = \textrm{i} + 5$, respectively.
  Given that $A = PBP^{-1}$, which of the following could be the matrix $P$?
options:
- id: a
  content: |-
    $\begin{bmatrix}-1 & 1 & 3 & 2 \\ 1 & 2 & 1 & -1 \\ 5 & 0 & 3 & -1 \\ -1 & 2 & -5 & 2\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & -1 & 3 & 2 \\ 2 & 1 & 1 & -1 \\ 0 & 5 & 3 & -1 \\ 2 & -1 & -5 & 2\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}2 & 3 & -1 & 1 \\ -1 & 1 & 1 & 2 \\ -1 & 3 & 5 & 0 \\ 2 & -5 & -1 & 2\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 & -1 & 2 & 3 \\ 2 & 1 & -1 & 1 \\ 0 & 5 & -1 & 3 \\ 2 & -1 & 2 & -5\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}3 & 2 & 1 & -1 \\ 1 & -1 & 2 & 1 \\ 3 & -1 & 0 & 5 \\ -5 & 2 & 2 & -1\end{bmatrix}$
```

---

**Question 8:**

```quiz
type: radio
id: ma-135542
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $v_{1}$ and $v_{3}$ and the block diagonal matrix $B$ shown below, where both $2 \cdot 2$ blocks of $B$ represent rotation-scaling matrices:

  $v_{1} = \begin{bmatrix}5\textrm{i} \\ -7 + 3\textrm{i} \\ 4 - \textrm{i} \\ -6 - 4\textrm{i}\end{bmatrix}, v_{3} = \begin{bmatrix}4 - 2\textrm{i} \\ 2 - 6\textrm{i} \\ 4 - 6\textrm{i} \\ -5\textrm{i}\end{bmatrix}, B = \begin{bmatrix}2 & -2 & 0 & 0 \\ 2 & 2 & 0 & 0 \\ 0 & 0 & -3 & 5 \\ 0 & 0 & -5 & -3\end{bmatrix}$
  The vectors $v_{1}$ and $v_{3}$ are the eigenvectors of a matrix $A$ corresponding to the eigenvalues $λ_{1} = 2 - 2\textrm{i}$ and $λ_{3} =-3 + 5\textrm{i}$, respectively.
  Given that $A = PBP^{-1}$, which of the following could be the matrix $P$?
options:
- id: a
  content: |-
    $\begin{bmatrix}-7 & 3 & 2 & -6 \\ 4 & -1 & 4 & -6 \\ 0 & 5 & 4 & -2 \\ -6 & -4 & 0 & -5\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}-7 & 3 & -6 & 2 \\ 4 & -1 & -6 & 4 \\ 0 & 5 & -2 & 4 \\ -6 & -4 & -5 & 0\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}-7 & 3 & 2 & -6 \\ 0 & 5 & 4 & -2 \\ -6 & -4 & 0 & -5 \\ 4 & -1 & 4 & -6\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}0 & 5 & 4 & -2 \\ -7 & 3 & 2 & -6 \\ 4 & -1 & 4 & -6 \\ -6 & -4 & 0 & -5\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}5 & 0 & 4 & -2 \\ 3 & -7 & 2 & -6 \\ -1 & 4 & 4 & -6 \\ -4 & -6 & 0 & -5\end{bmatrix}$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
