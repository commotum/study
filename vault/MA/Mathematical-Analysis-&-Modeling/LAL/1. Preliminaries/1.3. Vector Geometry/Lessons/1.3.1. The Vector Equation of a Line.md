# The Vector Equation of a Line

<!--
lesson-id: 1376
topic-code: LAL.1.3.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Vector Equation of a Line Given a Point and Direction Vector](#finding-a-vector-equation-of-a-line-given-a-point-and-direction-vector)
- [Finding a Vector Equation of the Line Passing Through Two Given Points](#finding-a-vector-equation-of-the-line-passing-through-two-given-points)
- [Geometric Intuition Behind the Formula](#geometric-intuition-behind-the-formula)
- [Finding Points on a Line That Are a Fixed Distance From Another Point on the Line](#finding-points-on-a-line-that-are-a-fixed-distance-from-another-point-on-the-line)
- [The Vector Equation of a Segment](#the-vector-equation-of-a-segment)
- [Finding the Vector Equation of a Line Segment](#finding-the-vector-equation-of-a-line-segment)

## Prerequisites

- [Graphing Curves Defined Parametrically](<../../../../PCL/8. Parametric Equations/8.1. Parametric Equations/Lessons/8.1.1. Graphing Curves Defined Parametrically.md>)
- [Describing the Position Vector of a Point Using Known Vectors](<../../../../PCL/5. Vectors/5.1. Introduction to Vectors/Lessons/5.1.8. Describing the Position Vector of a Point Using Known Vectors.md>)
- [Calculating the Magnitude of Cartesian Vectors in 3D](<../../../../PCL/5. Vectors/5.3. Vectors in 3D Cartesian Coordinates/Lessons/5.3.3. Calculating the Magnitude of Cartesian Vectors in 3D.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we are given a point $P$ with position vector $\mathbf{p}$, and another vector $\mathbf{v}$.

![](<../Source/1.3.1. The Vector Equation of a Line/Images/864b778401c952feb7ce11b7c884d1bc.png>)

The **vector equation** of the line that is parallel to $\mathbf{v}$ and passes through $P$ is given by

$$
r = p + tv, t ∈ (- ∞, ∞)
$$

The vector $\mathbf{v}$ is often called the **direction vector** of the line, while the vector $\mathbf{r}$ denotes the position vector of an arbitrary point on the line.

For example, if
$\begin{bmatrix}1 \\ 2 \\ -1\end{bmatrix}$
the vector equation of the line that is parallel to $\mathbf{v}$ and passes through $P$ is given by

$$
\begin{aligned}
r &= \begin{bmatrix}-1 \\ 0 \\ -3\end{bmatrix} + t\begin{bmatrix}1 \\ 2 \\ -1\end{bmatrix}, t ∈ (- ∞, ∞)
\end{aligned}
$$

---

<a id="finding-a-vector-equation-of-a-line-given-a-point-and-direction-vector"></a>
## Finding a Vector Equation of a Line Given a Point and Direction Vector

**Example:** Find a vector equation of the straight line passing through $P(3,-1,7)$ and parallel to $\begin{bmatrix}0 \\ 5 \\ -3\end{bmatrix}$

**Explanation**

The vector equation of the straight line passing through $P$ and parallel to $\mathbf{v}$ is given by

$$
\mathbf{r} = \mathbf{p} + t \mathbf{v}
$$

where $t \in (-\infty, \infty)$ and $\mathbf{p}$ is the position vector of $P$.

In our case, the position vector of the point $P$ is $\begin{bmatrix}3 \\ -1 \\ 7\end{bmatrix}$

Using the formula for the vector equation of a line, we have

$$
\begin{aligned}
r &= \begin{bmatrix}3 \\ -1 \\ 7\end{bmatrix} + t\begin{bmatrix}0 \\ 5 \\ -3\end{bmatrix}, t ∈ (- ∞, ∞)
\end{aligned}
$$

---

**Question 1:** Find a vector equation of the straight line passing through $Q(\sqrt{3}, \frac{11}{2},-7)$ and parallel to $v = ⟨1, 1,-2⟩$.

- [ ] A. $r = ⟨1, 1,-2⟩ + ⟨ - 7, \frac{11}{2}, \sqrt{3}⟩$
- [ ] B. $r = ⟨1, 1,-2⟩ + t⟨\sqrt{3}, \frac{11}{2},-7⟩, t ∈ (- ∞, ∞)$
- [ ] C. $r = ⟨\sqrt{3}, \frac{11}{2},-7⟩ + t⟨1, 1,-2⟩, t ∈ (- ∞, ∞)$
- [ ] D. $r = ⟨\frac{11}{2}, \sqrt{3},-7⟩ + t⟨ - 1, 1,-2⟩, t ∈ (- ∞, ∞)$
- [ ] E. $r = ⟨1, 1,-2⟩ + t⟨ - 7, \frac{11}{2}, \sqrt{3}⟩, t ∈ (- ∞, ∞)$

---

**Question 2:** Find a vector equation of the straight line passing through $P(-1,-4, 5)$ and parallel to $v = \begin{bmatrix}1 \\ 0 \\ -1\end{bmatrix}$.

- [ ] A. $r = \begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix} + t\begin{bmatrix}5 \\ -4 \\ -1\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] B. $r = \begin{bmatrix}1 \\ 0 \\ -1\end{bmatrix} + t\begin{bmatrix}-1 \\ -4 \\ 5\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] C. $r = \begin{bmatrix}-1 \\ -4 \\ 5\end{bmatrix} + t\begin{bmatrix}1 \\ 0 \\ -1\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] D. $r = \begin{bmatrix}-1 \\ -4 \\ 5\end{bmatrix} + t\begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] E. $r = \begin{bmatrix}-1 \\ -4 \\ 5\end{bmatrix} + \begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix}$

---

<a id="finding-a-vector-equation-of-the-line-passing-through-two-given-points"></a>
## Finding a Vector Equation of the Line Passing Through Two Given Points

**Example:** Find a vector equation of the line that passes through the points $P(1,-1,2)$ and $Q(3,2,7)$.

**Explanation**

The vector equation of the straight line passing through $P$ and parallel to $\mathbf{v}$ is given by

$$
\mathbf{r} = \mathbf{p} + t \mathbf{v}
$$

where $t \in (-\infty, \infty)$ and $\mathbf{p}$ is the position vector of $P$.

We can find a direction vector by computing the displacement vector between the given points $P(1,-1,2)$ and $Q(3,2,7)$, as follows:

$$
\begin{aligned}
v &= PQ^(→) \\
&= ⟨3, 2, 7⟩ - ⟨1,-1, 2⟩ \\
&= ⟨2, 3, 5⟩
\end{aligned}
$$

Therefore, the vector equation of the line is given by

$$
\begin{aligned}
r &= \begin{bmatrix}1 \\ -1 \\ 2\end{bmatrix} + t\begin{bmatrix}2 \\ 3 \\ 5\end{bmatrix}, t ∈ (- ∞, ∞)
\end{aligned}
$$

---

**Question 3:** Find a vector equation of the line that passes through the points $A(4, 5, 3)$ and $B(-1, 2, 0)$.

- [ ] A. $r = \begin{bmatrix}4 \\ 5 \\ 3\end{bmatrix} + t\begin{bmatrix}-5 \\ -3 \\ -3\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] B. $r = \begin{bmatrix}-1 \\ 2 \\ 0\end{bmatrix} + t\begin{bmatrix}-5 \\ -3 \\ 3\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] C. $r = \begin{bmatrix}-5 \\ -3 \\ -3\end{bmatrix} + t\begin{bmatrix}4 \\ 5 \\ 3\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] D. $r = \begin{bmatrix}5 \\ 3 \\ 3\end{bmatrix} + t\begin{bmatrix}4 \\ 5 \\ 3\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] E. $r = \begin{bmatrix}4 \\ 5 \\ 3\end{bmatrix} + t\begin{bmatrix}-1 \\ 2 \\ 0\end{bmatrix}, t ∈ (- ∞, ∞)$

---

**Question 4:** Find a vector equation of the line that passes through the points $P(0,-2,-4)$ and $Q(5, 0, 1)$.

- [ ] A. $r = \begin{bmatrix}0 \\ -2 \\ -4\end{bmatrix} + t\begin{bmatrix}5 \\ 0 \\ 1\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] B. $r = \begin{bmatrix}5 \\ 0 \\ 1\end{bmatrix} + t\begin{bmatrix}5 \\ -2 \\ 5\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] C. $r = \begin{bmatrix}0 \\ -2 \\ -4\end{bmatrix} + t\begin{bmatrix}5 \\ 2 \\ 5\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] D. $r = \begin{bmatrix}0 \\ -2 \\ -4\end{bmatrix} + t\begin{bmatrix}5 \\ -2 \\ 5\end{bmatrix}, t ∈ (- ∞, ∞)$
- [ ] E. $r = \begin{bmatrix}5 \\ 0 \\ 1\end{bmatrix} + t\begin{bmatrix}0 \\ -2 \\ -4\end{bmatrix}, t ∈ (- ∞, ∞)$

---

<a id="geometric-intuition-behind-the-formula"></a>
## Geometric Intuition Behind the Formula

There is a geometric intuition behind the formula

$$
\mathbf{r} = \mathbf{p} + t \mathbf{v}
$$

as shown in the diagram below.

![](<../Source/1.3.1. The Vector Equation of a Line/Images/9d52762911c1c854095be3160f00bb4e.png>)

To get to any point $A(x,y,z)$ on the desired line, starting from the origin $O$, we can

- first travel along the vector $\mathbf{p}$ to get to the point $P$ on the line, and
- then travel along the vector $t \mathbf{v}$ to get to $A$. (The scalar $t$ represents how far we travel forward or backward along the line in the direction of $\mathbf{v}$.)

---

<a id="finding-points-on-a-line-that-are-a-fixed-distance-from-another-point-on-the-line"></a>
## Finding Points on a Line That Are a Fixed Distance From Another Point on the Line

**Example:** Consider the straight line that passes through $P(1,2,0)$ and parallel to $\mathbf{v}= \langle -4,1,1 \rangle$. Find all the possible points $A$ on that line such that $PA=6\sqrt{2}$.

**Explanation**

First, we find the vector equation of the line, as follows:

$$
\begin{aligned}
r &= p + tv \\
r &= ⟨1, 2, 0⟩ + t⟨ - 4, 1, 1⟩
\end{aligned}
$$

where $t \in (-\infty, \infty)$.

Since $A$ lies on the line, we know that there must be a real number $t$ such that

$$
\overrightarrow{OA} = \langle 1, 2, 0 \rangle + t \langle -4, 1, 1 \rangle
$$

and

$$
\begin{aligned}
PA^(→) &= OA^(→) - OP^(→) \\
&= (⟨1, 2, 0⟩ + t⟨ - 4, 1, 1⟩) - ⟨1, 2, 0⟩ \\
&= t⟨ - 4, 1, 1⟩ \\
&= ⟨ - 4t, t, t⟩
\end{aligned}
$$

Using the fact that

$$
PA=6\sqrt{2}
$$

we get:

$$
\begin{aligned}
PA &= 6\sqrt{2} \\
\mid PA^(→) \mid &= 6\sqrt{2} \\
\mid ⟨ - 4t, t, t⟩ \mid &= 6\sqrt{2} \\
\sqrt{(-4t)^{2} + t^{2} + t^{2}} &= 6\sqrt{2} \\
\sqrt{18t^{2}} &= 6\sqrt{2} \\
± 3t\sqrt{2} &= 6\sqrt{2} \\
t &= ± 2
\end{aligned}
$$

Finally, putting the values $t=\pm 2$ into the equation for the line, we obtain

$$
OA_{1}^(→) = ⟨1, 2, 0⟩ + 2⟨ - 4, 1, 1⟩ = ⟨ - 7, 4, 2⟩,; OA_{2}^(→) = ⟨1, 2, 0⟩ - 2⟨ - 4, 1, 1⟩ = ⟨9, 0,-2⟩
$$

Therefore, the points are $A_1(-7, 4, 2)$ and $A_2(9, 0, -2)$.

---

**Question 5:** Consider the straight line that passes through $P(-2, 5,-3)$ and is parallel to $v = ⟨1,-1, 2⟩$. Find all the possible points $A$ on that line such that $AP = \sqrt{24}$.

- [ ] A. $A_{1}(-3, 9,-4)$ and $A_{2}(5,-11, 8)$
- [ ] B. $A_{1}(0, 3, 1)$ and $A_{2}(-4, 7,-7)$
- [ ] C. $A_{1}(4,-4, 8)$ and $A_{2}(-4, 4,-8)$
- [ ] D. $A_{1}(-3, 9,-4)$ and $A_{2}(-5, 11,-8)$
- [ ] E. $A_{1}(0, 3, 1)$ and $A_{2}(4,-7, 7)$

---

**Question 6:** Consider the straight line that passes through $P(3, 1,-1)$ and is parallel to $v = ⟨2, 1, 3⟩$. Find all the possible points $A$ on that line such that $AP = \sqrt{14}$.

- [ ] A. $A_{1}(5, 2, 2)$ and $A_{2}(1, 0,-4)$
- [ ] B. $A_{1}(5, 2, 3)$ and $A_{2}(1,-1,-4)$
- [ ] C. $A_{1}(4, 2, 2)$ and $A_{2}(1, 0,-4)$
- [ ] D. $A_{1}(5, 3, 2)$ and $A_{2}(1, 0, 4)$
- [ ] E. $A_{1}(2, 5, 2)$ and $A_{2}(0, 1,-4)$

---

<a id="the-vector-equation-of-a-segment"></a>
## The Vector Equation of a Segment

![](<../Source/1.3.1. The Vector Equation of a Line/Images/ebc3359b51513d2cf52986e9f1f66682.png>)

Suppose we wish to find a vector equation of the line *segment* whose endpoints are $P(3,-1,7)$ and $Q(3,4,4)$.

To do this, we need to find the vector equation of the corresponding line and then restrict the domain of the parameter $t$.

First, we need to find a direction vector parallel to the given line. We can use the vector $\overrightarrow{PQ}$ as follows:

$$
\begin{aligned}
v &= PQ^(→) \\
&= OQ^(→) - OP^(→) \\
&= \begin{bmatrix}3 \\ 4 \\ 4\end{bmatrix} - \begin{bmatrix}3 \\ -1 \\ 7\end{bmatrix} \\
&= \begin{bmatrix}0 \\ 5 \\ -3\end{bmatrix}
\end{aligned}
$$

So, the vector equation of the line $\overset{\longleftrightarrow}{PQ}$ is
$\begin{bmatrix}3 \\ -1 \\ 7\end{bmatrix}$

This equation describes every point on the line $\overset{\longleftrightarrow}{PQ}$.

To restrict this to points on the segment $\overline{PQ}$ only, we set $t\in [0,1]$. Thus, the vector equation of the line segment $\overline{PQ}$ is
$\begin{bmatrix}3 \\ 5t - 1 \\ 7 - 3t\end{bmatrix}$

Writing the equation of a line segment in this way is sometimes called **parametrizing** the line segment since each point on the segment corresponds to a single parameter $t$.

- Notice that $t=0$ gives us the position vector of $P{:}$

$$
\begin{bmatrix}3 \\ 5(0) - 1 \\ 7 - 3(0)\end{bmatrix}
$$

- Similarly, $t=1$ gives us the position vector of $Q{:}$

$$
\begin{bmatrix}3 \\ 5(1) - 1 \\ 7 - 3(1)\end{bmatrix}
$$

Since increasing $t$ moves our vector $\mathbf r (t)$ from $P$ to $Q$, we say that the line segment $\overline{PQ}$ is **traversed** *from* the point $P$ *to* the point $Q$ under this parametrization.

---

<a id="finding-the-vector-equation-of-a-line-segment"></a>
## Finding the Vector Equation of a Line Segment

**Example:** Using $\mathbf{i},\mathbf{j},\mathbf{k}$ notation, find a parametrization of the line segment $\overline{PQ}$, where $P(-4,1,2)$ and $Q(-2,5,-1)$.

**Explanation**

We need to find a direction vector that is parallel to the given line. We can use the vector $\overrightarrow{PQ}$ as follows:

$$
\begin{aligned}
v &= PQ^(→) \\
&= OQ^(→) - OP^(→) \\
&= (-2i + 5j - k) - (-4i + j + 2k) \\
&= 2i + 4j - 3k
\end{aligned}
$$

So, the vector equation of the line is

$$
\mathbf{r} = \mathbf{p} + t \mathbf{v} \qquad\Longrightarrow\qquad \mathbf{r} = (-4 \, \mathbf{i} + \mathbf{j} + 2\,\mathbf{k}) + t (2 \, \mathbf{i} + 4 \, \mathbf{j} - 3 \, \mathbf{k})
$$

and the parametrization of the line segment $\overline{PQ}$ is

$$
\begin{aligned}
r(t) &= (-4i + j + 2k) + t(2i + 4j - 3k) \\
&= (2t - 4)i + (4t + 1)j + (2 - 3t)k, t ∈ [0, 1]
\end{aligned}
$$

---

**Question 7:** The equation below describes all the points on the line segment $PQ^(―)$, where $P(1, 4,-2)$ and $Q(-2, 3, 1)$.
$r(t) = \begin{bmatrix}1 \\ a \\ -2\end{bmatrix} + t\begin{bmatrix}b \\ -1 \\ 3\end{bmatrix}, t ∈ [0, c]$
What is the value of $a + b + c$?

- [ ] A. $3$
- [ ] B. $-1$
- [ ] C. $2$
- [ ] D. $-4$
- [ ] E. $0$

---

**Question 8:** The equation below gives a parametrization of the line segment $PQ^(―)$, where $P(1, 0, 3)$ and $Q(1, 2, 0)$.
$r(t) = i + aj + bk, t ∈ [c, 1]$
What is $a + b + c$?

- [ ] A. $t^{2} - 2t$
- [ ] B. $3 - 5t^{2}$
- [ ] C. $4 - 2t$
- [ ] D. $1 + 2t$
- [ ] E. $3 - t$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
