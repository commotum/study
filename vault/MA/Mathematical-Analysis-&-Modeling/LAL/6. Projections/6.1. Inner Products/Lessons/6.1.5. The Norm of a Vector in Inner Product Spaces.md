# The Norm of a Vector in Inner Product Spaces

<!--
lesson-id: 2098
topic-code: LAL.6.1.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Norm of a Vector With Respect to a Given Inner Product](#calculating-the-norm-of-a-vector-with-respect-to-a-given-inner-product)
- [Calculating the Norm of a Vector in an Inner Product Space of Polynomials](#calculating-the-norm-of-a-vector-in-an-inner-product-space-of-polynomials)
- [Calculating the Norm of a Vector in an Inner Product Space of Continuous Functions](#calculating-the-norm-of-a-vector-in-an-inner-product-space-of-continuous-functions)
- [Normalizing a Vector in Inner Product Spaces](#normalizing-a-vector-in-inner-product-spaces)
- [Normalizing a Vector in an Inner Product Space](#normalizing-a-vector-in-an-inner-product-space)

## Prerequisites

- [The Norm of a Vector in N-Dimensional Euclidean Space](<6.1.2. The Norm of a Vector in N-Dimensional Euclidean Space.md>)
- [Inner Product Spaces](<6.1.3. Inner Product Spaces.md>)

---

<a id="introduction"></a>
## Introduction

Any vector space $V$ combined with an inner product $\langle \cdot, \cdot \rangle$ on $V$ is called an **inner product space.**

When referring to inner product spaces, we often say that the vector space $V$ is *equipped* with the inner product $\langle \cdot, \cdot \rangle$.

The **norm** of a vector $\mathbf v\in V$ in an inner product space is defined as

$$
‖v‖ = \sqrt{⟨v, v⟩}
$$

This definition allows us to apply the geometric concept of length to vectors defined in abstract vector spaces. This is because the norm of a vector $\mathbf v$ in an inner product space is a generalization of the norm (or length) of a vector in Euclidean space. To see this, recall that for $\mathbf x\in\mathbb R^n$, we have

$$
\Vert \mathbf{x} \Vert = \sqrt{\mathbf{x} \cdot \mathbf{x}}
$$

and we have already seen that the dot product is itself an inner product.

Let's consider an example. Suppose that an inner product on

$$
V = \mathbb{R}^3
$$

defined as

$$
\langle \mathbf{x}, \mathbf{y} \rangle = 3x_1 y_1 + 2x_2 y_2 + 2x_3 y_3
$$

Now consider the vector $\mathbf v\in V$, given by
$\begin{bmatrix}1 \\ 2 \\ 1\end{bmatrix}$

Computing $\|\mathbf{v}\|$ using our definition, we get

$$
\begin{aligned}
‖v‖ &= \sqrt{⟨v, v⟩} \\
&= \sqrt{3 \cdot (1 \cdot 1) + 2 \cdot (2 \cdot 2) + 2 \cdot (1 \cdot 1)} \\
&= \sqrt{3 + 8 + 2} \\
&= \sqrt{13}
\end{aligned}
$$

---

<a id="calculating-the-norm-of-a-vector-with-respect-to-a-given-inner-product"></a>
## Calculating the Norm of a Vector With Respect to a Given Inner Product

**Example:** An inner product on $V = \mathbb{R}^3$ is defined as $\langle \mathbf{x}, \mathbf{y} \rangle = 2x_1 y_1 + x_2 y_2 + 3x_3 y_3$.
Calculate the norm of the vector $\begin{bmatrix}3 \\ 2 \\ 1\end{bmatrix}$ under this inner product.

**Explanation**

The norm of a vector $\mathbf v$ with respect to an inner product $\langle \cdot, \cdot \rangle$ is defined as

$$
‖v‖ = \sqrt{⟨v, v⟩}
$$

Computing $\|\mathbf{v}\|$ using our definition, we get

$$
\begin{aligned}
‖v‖ &= \sqrt{⟨v, v⟩} \\
&= \sqrt{2 \cdot (3 \cdot 3) + 2 \cdot 2 + 3 \cdot (1 \cdot 1)} \\
&= \sqrt{18 + 4 + 3} \\
&= \sqrt{25} \\
&= 5
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-42714
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  An inner product on $V = R^{3}$ is defined as $⟨x, y⟩ = x_{1}y_{1} + x_{2}y_{2} + 5x_{3}y_{3}$.

  Calculate the norm of the vector $v = \begin{bmatrix}1 \\ 2 \\ 3\end{bmatrix}$ under this inner product.
options:
- id: a
  content: |-
    $5$
- id: b
  content: |-
    $10$
- id: c
  content: |-
    $5\sqrt{2}$
- id: d
  content: |-
    $2\sqrt{10}$
- id: e
  content: |-
    $\sqrt{14}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-43925
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  An inner product on $V = R^{3}$ is defined as $⟨x, y⟩ = x_{1}y_{1} + x_{2}y_{2} + 3x_{3}y_{3}$.

  Calculate the norm of the vector $v = \begin{bmatrix}0 \\ 2 \\ 2\end{bmatrix}$ under this inner product.
options:
- id: a
  content: |-
    $4$
- id: b
  content: |-
    $2\sqrt{2}$
- id: c
  content: |-
    $8$
- id: d
  content: |-
    $16$
- id: e
  content: |-
    $2\sqrt{3}$
```

---

<a id="calculating-the-norm-of-a-vector-in-an-inner-product-space-of-polynomials"></a>
## Calculating the Norm of a Vector in an Inner Product Space of Polynomials

**Example:** Let $V=\mathbb{R}_2[t]$ be the vector space of all polynomials of degree at most $2$, equipped with the inner product
$\langle {x}(t), {y}(t) \rangle = x(1)y(1) + x(2)y(2) + x(3)y(3)$.

Calculate the norm of the vector $p(t) = 1 - t^2$.

**Explanation**

The norm of a vector $p(t)$ with respect to an inner product $\langle \cdot, \cdot \rangle$ is defined as

$$
‖p(t)‖ = \sqrt{⟨p(t), p(t)⟩}
$$

Since

$$
{p}(t) = 1-t^2
$$

we obtain

$$
p(1)=0,\qquad p(2)= -3,\qquad p(3)=-8
$$

Computing $\|p(t)\|$ using our definition, we get

$$
\begin{aligned}
‖p(t)‖ &= \sqrt{⟨p(t), p(t)⟩} \\
&= \sqrt{p(1)p(1) + p(2)p(2) + p(3)p(3)} \\
&= \sqrt{[p(1)]^{2} + [p(2)]^{2} + [p(3)]^{2}} \\
&= \sqrt{(0)^{2} + (-3)^{2} + (-8)^{2}} \\
&= \sqrt{0 + 9 + 64} \\
&= \sqrt{73}
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-97420
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $V = R_{2}[t]$ be the vector space of all polynomials of degree at most $2$, equipped with the inner product

  $⟨x(t), y(t)⟩ = x(0)y(0) + x(2)y(2) + x(3)y(3)$.
  Calculate the norm of the vector $p(t) = 4 - 2t + t^{2}$.
options:
- id: a
  content: |-
    $4\sqrt{6}$
- id: b
  content: |-
    $4\sqrt{5}$
- id: c
  content: |-
    $8$
- id: d
  content: |-
    $5\sqrt{3}$
- id: e
  content: |-
    $9$
```

---

**Question 4:**

```quiz
type: radio
id: ma-42726
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $V = R_{2}[t]$ be the vector space of all polynomials of degree at most $2$, equipped with the inner product

  $⟨x(t), y(t)⟩ = x(1)y(1) + x(2)y(2) + x(3)y(3)$.
  Calculate the norm of the vector $p(t) = 1 - 2t$.
options:
- id: a
  content: |-
    $3$
- id: b
  content: |-
    $\sqrt{35}$
- id: c
  content: |-
    $5$
- id: d
  content: |-
    $\sqrt{15}$
- id: e
  content: |-
    $\sqrt{7}$
```

---

<a id="calculating-the-norm-of-a-vector-in-an-inner-product-space-of-continuous-functions"></a>
## Calculating the Norm of a Vector in an Inner Product Space of Continuous Functions

**Example:** Let $V=\mathcal{C}\left[0,1\right]$ be the vector space of all functions that are continuous on $\left[0,1\right]$, equipped with the inner product

$\langle x(t),y(t) \rangle = \displaystyle\int_{0}^{1} x(t) y(t) \, \textrm{d}t$.

Calculate the norm of the vector $f(t)= 2t-1$.

**Explanation**

The norm of a vector $f(t)$ with respect to an inner product $\langle \cdot, \cdot \rangle$ is defined as

$$
\| f(t)\| = \sqrt{\langle f(t), f(t) \rangle}
$$

Computing the inner product using our definition, we get

$$
\begin{aligned}
⟨f(t), f(t)⟩ &= ∫_{0}^{1}(f(t))^{2}dt \\
&= ∫_{0}^{1}(2t - 1)^{2}dt \\
&= ∫_{0}^{1}(4t^{2} - 4t + 1)dt \\
&= (\frac{4t^{3}}{3} - 2t^{2} + t) \mid _{0}^{1} \\
&= \frac{4}{3} - 2 + 1 \\
&= \frac{1}{3}
\end{aligned}
$$

Therefore,

$$
\| f(t)\| = \sqrt{\dfrac{1}{3}} = \dfrac{\sqrt 3}{3}
$$

---

**Question 5**

```quiz
type: radio
id: ma-42734
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $V = C[π, 2π]$ be the vector space of all functions that are continuous on $[π, 2π]$, equipped with the inner product

  $⟨x(t), y(t)⟩ = ∫_{π}^{2π}x(t)y(t)dt$.

  Calculate the norm of the vector $f(t) = \cos t$.
options:
- id: a
  content: |-
    $\sqrt{\frac{π}{2}}$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $\sqrt{\frac{3π}{2}}$
- id: d
  content: |-
    $2\sqrt{π}$
- id: e
  content: |-
    $\sqrt{π}$
```

---

**Question 6**

```quiz
type: radio
id: ma-42727
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $V = C[0, 1]$ be the vector space of all functions that are continuous on $[0, 1]$, equipped with the inner product

  $⟨x(t), y(t)⟩ = ∫_{0}^{1}x(t)y(t)dt$.

  Calculate the norm of the vector $f(t) = t + 2$.
options:
- id: a
  content: |-
    $\sqrt{\frac{10}{3}}$
- id: b
  content: |-
    $4$
- id: c
  content: |-
    $\sqrt{\frac{19}{3}}$
- id: d
  content: |-
    $\sqrt{3}$
- id: e
  content: |-
    $6$
```

---

<a id="normalizing-a-vector-in-inner-product-spaces"></a>
## Normalizing a Vector in Inner Product Spaces

Recall that a unit vector is a vector whose length is equal to $1$. If we divide a non-zero vector $\mathbf{x}\in\mathbb R^n$ by its norm $\Vert \mathbf{x} \Vert$, we obtain a unit vector:

$$
\mathbf{u}=\dfrac{\mathbf{x}}{\Vert\mathbf{x}\Vert}
$$

The process of constructing a unit vector by dividing $\mathbf x$ by its norm is called **normalization.**

We normalize vectors in more general inner product spaces using the same method.

For example, consider an inner product on

$$
V = \mathbb{R}^3
$$

defined as

$$
\langle \mathbf{x}, \mathbf{y} \rangle = 2x_1 y_1 + 3x_2 y_2 + x_3 y_3
$$

Let's normalize the following vector with respect to our inner product:

$$
\begin{bmatrix}2 \\ 1 \\ 2\end{bmatrix}
$$

The norm of a vector $\mathbf v$ with respect to an inner product $\langle \cdot, \cdot \rangle$ is defined as

$$
‖v‖ = \sqrt{⟨v, v⟩}
$$

Computing the inner product using our definition, we get

$$
\begin{aligned}
⟨v, v⟩ &= 2 \cdot (2 \cdot 2) + 3 \cdot (1 \cdot 1) + 2 \cdot 2 \\
&= 8 + 3 + 4 \\
&= 15
\end{aligned}
$$

Therefore,

$$
\begin{aligned}
‖v‖ &= \sqrt{15}
\end{aligned}
$$

To normalize the vector $\mathbf v$, we divide $\mathbf v$ by its norm $\|\mathbf v\|$:

$$
\begin{aligned}
u &= \frac{v}{‖v‖} \\
&= \frac{1}{\sqrt{15}}\begin{bmatrix}2 \\ 1 \\ 2\end{bmatrix} \\
&= \frac{\sqrt{15}}{15}\begin{bmatrix}2 \\ 1 \\ 2\end{bmatrix}
\end{aligned}
$$

---

<a id="normalizing-a-vector-in-an-inner-product-space"></a>
## Normalizing a Vector in an Inner Product Space

**Example:** Let $V=\mathcal{C}\left[0,\pi\right]$ be the vector space of all functions that are continuous on $\left[0,\pi\right]$, equipped with the inner product

$\langle x(t),y(t) \rangle = \displaystyle\int_{0}^{\pi} x(t) y(t) \, \textrm{d}t$.

Normalize the vector $f(t)= 3t$.

**Explanation**

The norm of a vector $f(t)$ with respect to an inner product $\langle \cdot, \cdot \rangle$ is defined as

$$
\| f(t)\| = \sqrt{\langle f(t), f(t) \rangle}
$$

Computing the inner product using our definition, we get

$$
\begin{aligned}
⟨f(t), f(t)⟩ &= ∫_{0}^{π}(3t)^{2}dt \\
&= ∫_{0}^{π}9t^{2}dt \\
&= 3t^{3} \mid _{0}^{π} \\
&= 3π^{3}
\end{aligned}
$$

Therefore,

$$
\| f(t) \| = \sqrt{3\pi^3} = \pi \sqrt{3\pi}
$$

To normalize the vector $f(t)$, we divide $f(t)$ by its norm $\| f(t) \|$:

$$
\begin{aligned}
u(t) &= (f(t))/(‖f(t)‖) \\
&= \frac{1}{π\sqrt{3π}} \cdot 3t \\
&= \frac{t\sqrt{3}}{π\sqrt{π}}
\end{aligned}
$$

---

**Question 7:**

```quiz
type: radio
id: ma-42741
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $V = R_{1}[t]$ be the vector space of all polynomials of degree at most $1$, equipped with the inner product

  $⟨x(t), y(t)⟩ = x(1)y(1) + x(2)y(2)$.
  Normalize the vector $p(t) = 2 + t$.
options:
- id: a
  content: |-
    $\frac{1}{\sqrt{2}}(2 + t)$
- id: b
  content: |-
    $\frac{1}{5}(2 + t)$
- id: c
  content: |-
    $\frac{\sqrt{10}}{50}(2 + t)$
- id: d
  content: |-
    $\sqrt{2}(2 + t)$
- id: e
  content: |-
    $\sqrt{5}(2 + t)$
```

---

**Question 8**

```quiz
type: radio
id: ma-97723
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  An inner product on $V = R^{3}$ is defined as $⟨x, y⟩ = x_{1}y_{1} + x_{2}y_{2} + 5x_{3}y_{3}$.

  Normalize the vector $v = \begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$ under this inner product.
options:
- id: a
  content: |-
    $\frac{\sqrt{2}}{2}\begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$
- id: b
  content: |-
    $\frac{\sqrt{2}}{10}\begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$
- id: c
  content: |-
    $\frac{\sqrt{6}}{6}\begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$
- id: d
  content: |-
    $\frac{\sqrt{10}}{10}\begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$
- id: e
  content: |-
    $\frac{1}{6}\begin{bmatrix}-1 \\ 2 \\ 1\end{bmatrix}$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
