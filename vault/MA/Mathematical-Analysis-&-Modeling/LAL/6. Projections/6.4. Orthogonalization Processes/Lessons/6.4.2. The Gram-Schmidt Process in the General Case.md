# The Gram-Schmidt Process in the General Case

<!--
lesson-id: 2128
topic-code: LAL.6.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding an Orthogonal Basis of a Span Given Some Orthogonal Vectors](#finding-an-orthogonal-basis-of-a-span-given-some-orthogonal-vectors)
- [Finding an Orthogonal Basis For the Column Space of a Matrix Given Some Orthogonal Columns](#finding-an-orthogonal-basis-for-the-column-space-of-a-matrix-given-some-orthogonal-columns)
- [Gram-Schmidt Process](#gram-schmidt-process)
- [Finding an Orthogonal Basis Using the Gram-Schmidt Process](#finding-an-orthogonal-basis-using-the-gram-schmidt-process)

## Prerequisites

- [Projecting Vectors Onto Subspaces in Euclidean Spaces (Orthogonal Bases)](<../../6.3. Orthogonal Projections/Lessons/6.3.3. Projecting Vectors Onto Subspaces in Euclidean Spaces (Orthogonal Bases).md>)
- [The Gram-Schmidt Process for Two Vectors](<6.4.1. The Gram-Schmidt Process for Two Vectors.md>)

---

<a id="introduction"></a>
## Introduction

Consider the subspace

$$
V=\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3\}
$$

where

$$
\begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix}
$$

We already know how to find an orthogonal basis for $2$-dimensional subspaces. But, how can we do it for subspaces with higher dimensions, like $V$?

Good news is that we can extend the **Gram-Schmidt process**. Since the first two vectors are already orthogonal, we proceed to find an orthogonal basis using the Gram-Schmidt process, as follows:

**Step 1**: We set $\begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix}$

**Step 2**: We set $\begin{bmatrix}1 \\ 1 \\ -1\end{bmatrix}$

**Step 3**. Next, let

$$
S=\textrm{Span}(\mathbf{a}_1,\mathbf{a}_2)
$$

We find ${\color{blue}\mathbf{v}_3}$ using the formula

$$
\begin{aligned}
v_{3} &= a_{3}- proj_{S}a_{3} \\
&= a_{3}- proj_{v_{1}}a_{3} - proj_{v_{2}}a_{3} \\
&= a_{3} - \frac{a_{3} \cdot v_{1}}{v_{1} \cdot v_{1}}v_{1} - \frac{a_{3} \cdot v_{2}}{v_{2} \cdot v_{2}}v_{2}
\end{aligned}
$$

![](<../Source/6.4.2. The Gram-Schmidt Process in the General Case/Images/6792939d6c6ef02f1ec400a09d60f437.png>)

Notice that $\color{blue}\mathbf{v}_3$ is written as a linear combination of

$$
\mathbf{a}_1(={\color{blue}\mathbf{v}_1})
$$

$$
\mathbf{a}_2(={\color{blue}\mathbf{v}_2})
$$

and $\mathbf{a}_3$. This means that ${\color{blue}\mathbf{v}_3} \in V$, and also ${\color{blue}\mathbf{v}_3} \perp S$. Moreover,

$$
\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3\} = V = \textrm{Span}\{{\color{blue}\mathbf{v}_1}, {\color{blue}\mathbf{v}_2}, {\color{blue}\mathbf{v}_3} \}
$$

Applying the formula for $\mathbf{v}_3$ in our case gives

$$
\begin{aligned}
v_{3} &= \begin{bmatrix}1 \\ -1 \\ 3\end{bmatrix} - (\frac{2}{2})\begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix} - (-\frac{3}{3})\begin{bmatrix}1 \\ 1 \\ -1\end{bmatrix} \\
&= \begin{bmatrix}1 \\ -1 \\ 3\end{bmatrix} - \begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix} + \begin{bmatrix}1 \\ 1 \\ -1\end{bmatrix} \\
&= \begin{bmatrix}1 \\ 1 \\ 2\end{bmatrix}
\end{aligned}
$$

Finally, $\begin{bmatrix}1 \\ -1 \\ 0\end{bmatrix}$ is an orthogonal basis for $V$.

---

<a id="finding-an-orthogonal-basis-of-a-span-given-some-orthogonal-vectors"></a>
## Finding an Orthogonal Basis of a Span Given Some Orthogonal Vectors

**Example:** Consider the following vectors:

$\begin{bmatrix}1 \\ 1 \\ 1 \\ 1\end{bmatrix}$

Suppose that $V = \textrm{Span}\{\mathbf a_1, \mathbf a_2, \mathbf a_3\}$. Given that $\mathcal B = \{\mathbf a_1, \mathbf a_2, \mathbf b\}$ is an orthogonal basis for $V, \mathbf{a}_1 \perp \mathbf{a}_2$, and that $\mathcal B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\dfrac{c}{a}$?

**Explanation**

Since the first two vectors are already orthogonal, we proceed to find an orthogonal basis using the Gram-Schmidt process, as follows:

**Step 1**: We set $\begin{bmatrix}1 \\ 1 \\ 1 \\ 1\end{bmatrix}$

**Step 2**: We set $\begin{bmatrix}1 \\ 1 \\ -1 \\ -1\end{bmatrix}$

**Step 3**. Next, we find $\mathbf{v}_3$ using the formula

$$
\begin{aligned}
v_{3} &= a_{3} - proj_{v_{1}}a_{3} - proj_{v_{2}}a_{3} \\
&= a_{3} - \frac{a_{3} \cdot v_{1}}{v_{1} \cdot v_{1}}v_{1} - \frac{a_{3} \cdot v_{2}}{v_{2} \cdot v_{2}}v_{2}
\end{aligned}
$$

Applying the formula for $\mathbf{v}_3$ in our case gives

$$
\begin{aligned}
v_{3} &= \begin{bmatrix}-1 \\ 0 \\ -2 \\ 1\end{bmatrix} - (-\frac{2}{4})\begin{bmatrix}1 \\ 1 \\ 1 \\ 1\end{bmatrix} - \frac{0}{4}\begin{bmatrix}1 \\ 1 \\ -1 \\ -1\end{bmatrix} \\
&= \begin{bmatrix}-1 \\ 0 \\ -2 \\ 1\end{bmatrix} + \frac{1}{2}\begin{bmatrix}1 \\ 1 \\ 1 \\ 1\end{bmatrix} - 0\begin{bmatrix}1 \\ 1 \\ -1 \\ -1\end{bmatrix} \\
&= \frac{1}{2}(2\begin{bmatrix}-1 \\ 0 \\ -2 \\ 1\end{bmatrix} + \begin{bmatrix}1 \\ 1 \\ 1 \\ 1\end{bmatrix}) \\
&= \begin{bmatrix}-\frac{1}{2} \\ \frac{1}{2} \\ -\frac{3}{2} \\ \frac{3}{2}\end{bmatrix}
\end{aligned}
$$

Finally,

$$
a=-\dfrac 1 2
$$

$$
c=-\dfrac 3 2
$$

and

$$
\dfrac{c}{a} = \dfrac{\left(-\dfrac 3 2\right)}{\left(-\dfrac 1 2\right)} = 3
$$

---

**Question 1**

$a_{1} = \begin{bmatrix}-1 \\ 1 \\ 1 \\ -1\end{bmatrix}, a_{2} = \begin{bmatrix}-1 \\ -3 \\ 1 \\ -1\end{bmatrix}, a_{3} = \begin{bmatrix}2 \\ 1 \\ -1 \\ 2\end{bmatrix}, B = {[ \mid; \frac{a_{1}}{‖a_{1}‖}; \mid ], [ \mid; \frac{a_{2}}{‖a_{2}‖}; \mid ], \begin{bmatrix}a \\ b \\ c \\ d\end{bmatrix}}$

Suppose that $V = Span{a_{1}, a_{2}, a_{3}}$. Given that $B$ is an **orthonormal** basis for $V, a_{1}⊥a_{2}$, and that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\mid c \mid$?

- [ ] A. $0$
- [ ] B. $2$
- [ ] C. $\frac{\sqrt{6}}{2}$
- [ ] D. $\frac{\sqrt{6}}{6}$
- [ ] E. $\frac{\sqrt{6}}{3}$

---

**Question 2**

Consider the following vectors:

$a_{1} = \begin{bmatrix}-1 \\ 1 \\ 1 \\ -1\end{bmatrix}, a_{2} = \begin{bmatrix}-1 \\ -3 \\ -1 \\ -3\end{bmatrix}, a_{3} = \begin{bmatrix}5 \\ 5 \\ 5 \\ 5\end{bmatrix}, b = \begin{bmatrix}a \\ b \\ c \\ d\end{bmatrix}$

Suppose that $V = Span{a_{1}, a_{2}, a_{3}}$. Given that $B = {a_{1}, a_{2}, b}$ is an orthogonal basis for $V, a_{1}⊥a_{2}$, and that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\frac{a}{c}$?

- [ ] A. $2$
- [ ] B. $4$
- [ ] C. $0$
- [ ] D. $1$
- [ ] E. $-2$

---

<a id="finding-an-orthogonal-basis-for-the-column-space-of-a-matrix-given-some-orthogonal-columns"></a>
## Finding an Orthogonal Basis For the Column Space of a Matrix Given Some Orthogonal Columns

**Example:** $\begin{bmatrix}2 & 1 & 2 \\ -1 & 3 & 0 \\ 1 & 3 & 6 \\ 2 & -1 & 0\end{bmatrix}$

Consider the subspace $V=\textrm{Col}(A)$ spanned by the columns $\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3$ of the matrix $A$, and an **orthonormal** basis $\mathcal{B}$ of $\textrm{Col}(A)$, shown above. Given that $\mathbf{a}_1 \perp \mathbf{a}_2$, and that $\mathcal B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\mid \,d\, \mid$?

**Explanation**

Since the first two vectors are already orthogonal, we proceed to find an orthogonal basis using the Gram-Schmidt process.

**Step 1**: We set $\begin{bmatrix}2 \\ -1 \\ 1 \\ 2\end{bmatrix}$

**Step 2**: We set $\begin{bmatrix}1 \\ 3 \\ 3 \\ -1\end{bmatrix}$

**Step 3**. Next, we find $\mathbf{v}_3$ using the formula

$$
\begin{aligned}
v_{3} &= a_{3} - proj_{v_{1}}a_{3} - proj_{v_{2}}a_{3} \\
&= a_{3} - \frac{a_{3} \cdot v_{1}}{v_{1} \cdot v_{1}}v_{1} - \frac{a_{3} \cdot v_{2}}{v_{2} \cdot v_{2}}v_{2}
\end{aligned}
$$

Applying the formula for $\mathbf{v}_3$ in our case gives

$$
\begin{aligned}
v_{3} &= \begin{bmatrix}2 \\ 0 \\ 6 \\ 0\end{bmatrix} - \frac{10}{10}\begin{bmatrix}2 \\ -1 \\ 1 \\ 2\end{bmatrix} - \frac{20}{20}\begin{bmatrix}1 \\ 3 \\ 3 \\ -1\end{bmatrix} \\
&= \begin{bmatrix}2 \\ 0 \\ 6 \\ 0\end{bmatrix} - \begin{bmatrix}2 \\ -1 \\ 1 \\ 2\end{bmatrix} - \begin{bmatrix}1 \\ 3 \\ 3 \\ -1\end{bmatrix} \\
&= \begin{bmatrix}-1 \\ -2 \\ 2 \\ -1\end{bmatrix}
\end{aligned}
$$

Now, we normalize $\mathbf{v}_3$ dividing it by

$$
\|\mathbf{v}_3\| = \sqrt{10}
$$

$$
\begin{aligned}
u_{3} &= \frac{v_{3}}{‖v_{3}‖} = \frac{1}{\sqrt{10}}\begin{bmatrix}-1 \\ -2 \\ 2 \\ -1\end{bmatrix} = \begin{bmatrix}-\frac{\sqrt{10}}{10} \\ -\frac{\sqrt{10}}{5} \\ \frac{\sqrt{10}}{5} \\ -\frac{\sqrt{10}}{10}\end{bmatrix}
\end{aligned}
$$

Finally,

$$
\begin{aligned}
\begin{vmatrix}\,d\, & = \left\end{vmatrix}-\dfrac{\sqrt{10}}{10}\right &= \dfrac{\sqrt{10}}{10}
\end{aligned}
$$

---

**Question 3**

$A = \begin{bmatrix}2 & 2 & -4 \\ -2 & 1 & -2 \\ -2 & 1 & 4 \\ 2 & 0 & -2\end{bmatrix}, B = {[ \mid; \frac{a_{1}}{‖a_{1}‖}; \mid ], [ \mid; \frac{a_{2}}{‖a_{2}‖}; \mid ], \begin{bmatrix}a \\ b \\ c \\ d\end{bmatrix}}$

Consider the subspace $V = Col(A)$ spanned by the columns $a_{1}, a_{2}, a_{3}$ of the matrix $A$, and an **orthonormal** basis $B$ of $Col(A)$, shown above. Given that $a_{1}⊥a_{2}$, and that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\mid c \mid$?

- [ ] A. $2$
- [ ] B. $\frac{1}{3}$
- [ ] C. $\frac{\sqrt{2}}{2}$
- [ ] D. $1$
- [ ] E. $\frac{\sqrt{3}}{6}$

---

**Question 4**

$A = \begin{bmatrix}-1 & 1 & 2 \\ 1 & 3 & -1 \\ -1 & 1 & 2 \\ -1 & 1 & 1\end{bmatrix}, B = {\begin{bmatrix}-1 \\ 1 \\ -1 \\ -1\end{bmatrix}, \begin{bmatrix}1 \\ 3 \\ 1 \\ 1\end{bmatrix}, \begin{bmatrix}a \\ b \\ c \\ d\end{bmatrix}}$

Consider the subspace $V = Col(A)$ spanned by the columns $a_{1}, a_{2}, a_{3}$ of the matrix $A$ and an orthogonal basis $B$ of $Col(A)$, shown above. Given that $a_{1}⊥a_{2}$, and that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\frac{a}{d}$?

- [ ] A. $-2$
- [ ] B. $-1$
- [ ] C. $1$
- [ ] D. $-\frac{1}{2}$
- [ ] E. $2$

---

<a id="gram-schmidt-process"></a>
## Gram-Schmidt Process

But what do we do if the original basis we are given doesn't contain any orthogonal vectors? Well, then we make them orthogonal using the Gram-Schmidt process!

Given a basis $\,\{\mathbf{a}_1,\mathbf{a}_2,\ldots,\mathbf{a}_n\}$ for a non zero subspace $V$, we can get an orthogonal basis for $V$ by defining the vectors

$$
\begin{aligned}
v_{1} &= a_{1} \\
v_{2} &= a_{2} - proj_{v_{1}}a_{2} \\
v_{3} &= a_{3} - proj_{v_{1}}a_{3} - proj_{v_{2}}a_{3} \\
⋮ &= ⋮ \\
v_{n} &= a_{n} - proj_{v_{1}}a_{n} - proj_{v_{2}}a_{n} - ⋯ - proj_{v_{n - 1}}a_{n}
\end{aligned}
$$

The set of vectors $\{\mathbf{v}_1,\mathbf{v}_2,\ldots, \mathbf{v}_n\}$ will be an orthogonal basis for $V$.

Here, we build up the orthogonal basis one vector at a time, by taking away from each basis vector its orthogonal projection onto each of the $1$-dimensional subspaces spanned by one of the orthogonal basis vectors found already.

Furthermore, if we want to find an *orthonormal* basis, we just have to normalize the vectors $\mathbf{v}_i$ for

$$
i=1,2,\dots,n
$$

---

<a id="finding-an-orthogonal-basis-using-the-gram-schmidt-process"></a>
## Finding an Orthogonal Basis Using the Gram-Schmidt Process

**Example:** $\begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix}$

Suppose that $V = \textrm{Span}\{\mathbf a_1, \mathbf a_2, \mathbf a_3\}$. Given that $\mathcal B$ is an orthogonal basis for $V$, and that $\mathcal B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\dfrac{b}{c}$?

**Explanation**

We find an orthogonal basis $\mathcal B$ of $V$ using the Gram-Schmidt process, as follows:

**Step 1.** We set $\begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix}$

**Step 2.** We find the vector $\mathbf{v}_2$ orthogonal to $\text{Span}\{\mathbf{v}_1\}$ using the usual formula:

$$
\begin{aligned}
v_{2} &= a_{2} - proj_{v_{1}}a_{2} \\
&= a_{2} - \frac{a_{2} \cdot v_{1}}{v_{1} \cdot v_{1}}v_{1} \\
&= \begin{bmatrix}1 \\ 2 \\ 2\end{bmatrix} - \frac{3}{3}\begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix}
\end{aligned}
$$

**Step 3.** We find the vector $\mathbf{v}_3$ orthogonal to $\textrm{Span}\{\mathbf{v}_1,\mathbf{v}_2 \}$ using the usual formula:

$$
\begin{aligned}
v_{3} &= a_{3} - proj_{v_{1}}a_{3} - proj_{v_{2}}a_{3} \\
&= a_{3} - \frac{a_{3} \cdot v_{1}}{v_{1} \cdot v_{1}}v_{1} - \frac{a_{3} \cdot v_{2}}{v_{2} \cdot v_{2}}v_{2} \\
&= \begin{bmatrix}2 \\ 1 \\ -2\end{bmatrix} - (-\frac{3}{3})\begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix} - \frac{3}{6}\begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}2 \\ 1 \\ -2\end{bmatrix} + \begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix} - \frac{1}{2}\begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix} \\
&= \frac{1}{2}(2\begin{bmatrix}2 \\ 1 \\ -2\end{bmatrix} + 2\begin{bmatrix}-1 \\ 1 \\ 1\end{bmatrix} - \begin{bmatrix}2 \\ 1 \\ 1\end{bmatrix}) \\
&= \begin{bmatrix}0 \\ \frac{3}{2} \\ -\frac{3}{2}\end{bmatrix}
\end{aligned}
$$

Finally,

$$
b=\dfrac 3 2
$$

$$
c=-\dfrac 3 2
$$

and

$$
\dfrac{b}{c} = \dfrac{\left(\dfrac 3 2\right)}{\left(-\dfrac 3 2\right)}=-1
$$

---

**Question 5**

$A = \begin{bmatrix}1 & 4 & 3 \\ 0 & 1 & -1 \\ 1 & -2 & -1 \\ 0 & 1 & -1\end{bmatrix}, B = {[ \mid; v_{1}; \mid ], [ \mid; v_{2}; \mid ], \begin{bmatrix}a \\ b \\ c \\ d\end{bmatrix}}$

Consider the subspace $V = Col(A)$ spanned by the columns $a_{1}, a_{2}, a_{3}$ of the matrix $A$, and an orthogonal basis $B$ of $Col(A)$, shown above. Given that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\frac{d}{a}$?

- [ ] A. $-3$
- [ ] B. $0$
- [ ] C. $4$
- [ ] D. $2$
- [ ] E. $-2$

---

**Question 6**

$a_{1} = \begin{bmatrix}2 \\ 1 \\ 0\end{bmatrix}, a_{2} = \begin{bmatrix}7 \\ -4 \\ 1\end{bmatrix}, a_{3} = \begin{bmatrix}10 \\ -5 \\ -14\end{bmatrix}, B = {[ \mid; v_{1}; \mid ], [ \mid; v_{2}; \mid ], \begin{bmatrix}a \\ b \\ c\end{bmatrix}}$

Suppose that $V = Span{a_{1}, a_{2}, a_{3}}$. Given that $B$ is an orthogonal basis for $V$, and that $B$ is derived from $V$ using the Gram-Schmidt process, what is the value of $\frac{a}{b}$?

- [ ] A. $\frac{1}{2}$
- [ ] B. $1$
- [ ] C. $\frac{1}{3}$
- [ ] D. $-\frac{1}{2}$
- [ ] E. $-1$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/LAL/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/LAL/0. Table of Contents/TOC|Table of Contents]]
