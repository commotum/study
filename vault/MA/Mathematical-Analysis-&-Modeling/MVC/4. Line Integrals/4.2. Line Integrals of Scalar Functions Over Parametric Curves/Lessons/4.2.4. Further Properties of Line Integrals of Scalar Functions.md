# Further Properties of Line Integrals of Scalar Functions

<!--
lesson-id: 3354
topic-code: MVC.4.2.4
-->

## Table of Contents

- [Introduction](#introduction)
- [The Arc Length Element for Polar Curves](#the-arc-length-element-for-polar-curves)
- [Constructing Line Integrals of Unity Over Polar Curves](#constructing-line-integrals-of-unity-over-polar-curves)
- [Evaluating Line Integrals of Unity Over Polar Curves](#evaluating-line-integrals-of-unity-over-polar-curves)
- [Geometric Interpretation of the Arc Length Element for a Parametric Curve](#geometric-interpretation-of-the-arc-length-element-for-a-parametric-curve)
- [Geometric Interpretation of the Arc Length Element for a Polar Curve](#geometric-interpretation-of-the-arc-length-element-for-a-polar-curve)

## Prerequisites

- [The Arc Length of a Polar Curve](<../../../../CBC/12. Polar Equations/12.1. Polar Coordinates/Lessons/12.1.10. The Arc Length of a Polar Curve.md>)
- [Properties of Line Integrals of Scalar Functions](<../../4.1. Line Integrals of Scalar Functions/Lessons/4.1.3. Properties of Line Integrals of Scalar Functions.md>)

---

<a id="introduction"></a>
## Introduction

Recall that for a path $C$ in the $xy$-plane, the arc length $L$ of $C$ can be computed as the line integral of unity with respect to arc length over $C$:

$$
L = \displaystyle\int_C \, \textrm{d}s
$$

Now, if $C$ is defined parametrically as

$$
\mathbf r(t) = \langle x(t),\, y(t)\rangle, \quad a\leq t \leq b
$$

then we can also calculate the arc length of $C$ as

$$
L = \int_{a}^{b} \mid \mid \mathbf r'(t) \mid \mid \,\textrm d t = \int_{a}^{b}\sqrt{\left(\dfrac{\textrm d x}{\textrm d t}\right)^2 + \left(\dfrac{\textrm d y}{\textrm d t}\right)^2}\,\textrm d t\,
$$

By comparing our two expressions for $L$, we say that the **arc-length element** $\textrm ds$ for a parametrically defined curve is given by

$$
\textrm ds = \sqrt{\left(\dfrac{\textrm d x}{\textrm d t}\right)^2 + \left(\dfrac{\textrm d y}{\textrm d t}\right)^2}\,\textrm d t\,
$$

Geometrically, we can think of $\textrm ds$ as representing the change in arc length that arises due to a vanishingly small change in $t$. By summing (integrating) all of these small changes, we arrive at the arc length of $C$. We'll explore this in more detail at the end of the lesson.

---

<a id="the-arc-length-element-for-polar-curves"></a>
## The Arc Length Element for Polar Curves

Now suppose that a curve $C$ is defined using plane polar coordinates as

$$
r = r(\theta), \qquad \theta_1\leq\theta\leq\theta_2
$$

We've already seen that a polar curve is just a special type of parametric curve in the parameter $\theta$, where

$$
x = r(\theta)\cos\theta, \qquad y = r(\theta)\sin\theta, \qquad \theta_1\leq\theta\leq\theta_2
$$

Using the usual formula for the arc length of a polar curve, the arc length of $C$ can be calculated as

$$
L = \int_{\theta_1}^{\theta_2}\sqrt{r^2 + \left(\dfrac{\textrm d r}{\textrm d \theta}\right)^2}\,\textrm d \theta
$$

which is the same as the line integral

$$
L = \displaystyle\int\limits_C \, \textrm{d}s
$$

By comparing our two expressions for $L$, the arc-length element $\textrm ds$ for a curve can also be defined using polar coordinates as follows:

$$
\textrm ds = \sqrt{r^2 + \left(\dfrac{\textrm d r}{\textrm d \theta}\right)^2}\,\textrm d \theta
$$

We'll build up a geometric understanding of this result at the end of the lesson. But for now, let's use this result to calculate some line integrals across polar curves.

---

<a id="constructing-line-integrals-of-unity-over-polar-curves"></a>
## Constructing Line Integrals of Unity Over Polar Curves

**Example:** Find a definite integral with respect to $\theta$ that's equivalent to $\displaystyle\int_C\,\textrm d s$, where $C$ is the path along the polar curve $r=1-\sin{\theta}\,$ for $0 \leq \theta \leq \pi$, shown above.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/51abfb11c3d6ee34e7a5825e34b8c9af.png>)

**Explanation**

The line integral of unity with respect to arc length over $C$ is equal to the arc length $L$ of $C$, i.e.,

$$
\int_C \,\textrm d s = L
$$

In polar coordinates, the arc length element $\textrm d s$ is given by

$$
\textrm ds = \sqrt{r^2 +\left(\frac{\textrm{d}r}{\textrm{d}\theta}\right)^2}\,\textrm{d}\theta
$$

So, for a polar curve defined as

$$
r = r(\theta)
$$

for

$$
\theta_1 \leq \theta \leq \theta_2
$$

we have

$$
L= \int_C \,\textrm d s = \int_{\theta_1}^{\theta_2} \sqrt{r^2 +\left(\frac{\textrm{d}r}{\textrm{d}\theta}\right)^2}\,\textrm{d}\theta
$$

Since

$$
r=1-\sin{\theta}
$$

we have

$$
\dfrac{\textrm{d}r}{\textrm{d}\theta} = -\cos{\theta}
$$

Therefore, the expression for our line integral is

$$
\begin{aligned}
∫_{C}ds &= ∫_{θ_{1}}^{θ_{2}}\sqrt{r^{2} + (\frac{dr}{dθ})^{2}}dθ \\
&= ∫_{0}^{π}\sqrt{(1 - \sin θ)^{2} + (-\cos θ)^{2}}dθ \\
&= ∫_{0}^{π}\sqrt{1 - 2\sin θ + \sin^{2} θ + \cos^{2} θ}dθ \\
&= ∫_{0}^{π}\sqrt{2 - 2\sin θ}dθ \\
&= \sqrt{2}∫_{0}^{π}\sqrt{1 - \sin θ}dθ
\end{aligned}
$$

---

**Question 1**

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/q-123355.png>)

Which of the following is equivalent to $∫_{C}ds$, where $C$ is the path along the polar curve $r = 1 - 2\cos θ$ for $\frac{π}{3} \le θ \le π$, shown above?

- [ ] A. $∫_{π/3}^{π}\sqrt{5 - 4\cos θ}dθ$
- [ ] B. $∫_{π/3}^{π}\sqrt{5 + 4\cos^{2} θ}dθ$
- [ ] C. $∫_{π/3}^{π}\sqrt{1 - 2\cos θ}dθ$
- [ ] D. $∫_{π/3}^{π}\sqrt{4 - 5\cos^{2} θ}dθ$
- [ ] E. $∫_{π/3}^{π}\sqrt{1 - 2\cos^{2} θ}dθ$

---

**Question 2:** Which of the following is equivalent to $∫_{C}ds$, where $C$ is the path along the polar curve $r = \sec^{3} θ$ for $- π/2 \le θ \le π/2$?

- [ ] A. $∫_{- π/2}^{π/2}\sec^{3} θ\sqrt{1 + 9\tan^{2} θ}dθ$
- [ ] B. $∫_{0}^{π}\sec^{2} θ\tan^{2} θdθ$
- [ ] C. $∫_{- π/2}^{π/2}\sec^{3} θ\sqrt{9 + \tan^{2} θ}dθ$
- [ ] D. $∫_{0}^{π}\sec^{3} θ\sqrt{1 + \tan^{2} θ}dθ$
- [ ] E. $2∫_{0}^{π}\sec^{3} θ\sqrt{1 + \sec^{2} θ}dθ$

---

<a id="evaluating-line-integrals-of-unity-over-polar-curves"></a>
## Evaluating Line Integrals of Unity Over Polar Curves

**Example:** Calculate $\displaystyle\int_C\,\textrm d s$, where $C$ is the path along the polar curve $r=2(1+\sin\theta)$ for $0 \leq \theta \leq {\pi}$, shown above.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/59d37d12b3beaf320cda903e9d7a14d4.png>)

**Explanation**

The line integral of unity with respect to arc length over $C$ is equal to the arc length $L$ of $C$, i.e.,

$$
\int_C \,\textrm d s = L
$$

In polar coordinates, the arc length element $\textrm d s$ is given by

$$
\textrm ds = \sqrt{r^2 +\left(\frac{\textrm{d}r}{\textrm{d}\theta}\right)^2}\,\textrm{d}\theta
$$

So, for a polar curve defined as

$$
r = r(\theta)
$$

for

$$
\theta_1 \leq \theta \leq \theta_2
$$

we have

$$
L= \int_C \,\textrm d s = \int_{\theta_1}^{\theta_2} \sqrt{r^2 +\left(\frac{\textrm{d}r}{\textrm{d}\theta}\right)^2}\,\textrm{d}\theta
$$

Since

$$
r=2(1+\sin\theta)
$$

we have

$$
\dfrac{\textrm{d}r}{\textrm{d}\theta} = 2\cos{\theta}
$$

Therefore, we evaluate our line integral as follows:

$$
\begin{aligned}
L &= ∫_{θ_{1}}^{θ_{2}}\sqrt{r^{2} + (\frac{dr}{dθ})^{2}}dθ \\
&= ∫_{0}^{π}\sqrt{4(1 + \sin θ)^{2} + 4(\cos θ)^{2}}dθ \\
&= 2∫_{0}^{π}\sqrt{1 + 2\sin θ + \sin^{2} θ + \cos^{2} θ}dθ \\
&= 2∫_{0}^{π}\sqrt{1 + 2\sin θ + 1}dθ \\
&= 2∫_{0}^{π}\sqrt{2 + 2\sin θ}dθ \\
&= 2\sqrt{2}∫_{0}^{π}\sqrt{1 + \sin θ}dθ
\end{aligned}
$$

Next, we recall the following identities:

$$
\sin^{2} (\frac{θ}{2}) + \cos^{2} (\frac{θ}{2}) = 1, \sin θ = 2\sin (\frac{θ}{2})\cos (\frac{θ}{2})
$$

Rewriting our integral using the above identities, we can evaluate it as follows:

$$
\begin{aligned}
L &= 2\sqrt{2}∫_{0}^{π}\sqrt{1 + \sin θ}dθ \\
&= 2\sqrt{2}∫_{0}^{π}\sqrt{\sin^{2} (\frac{θ}{2}) + \cos^{2} (\frac{θ}{2}) + 2\sin (\frac{θ}{2})\cos (\frac{θ}{2})}dθ \\
&= 2\sqrt{2}∫_{0}^{π}\sqrt{(\sin (\frac{θ}{2}) + \cos (\frac{θ}{2}))^{2}}dθ \\
&= 2\sqrt{2}∫_{0}^{π} \mid \sin (\frac{θ}{2}) + \cos (\frac{θ}{2}) \mid dθ \\
&= 4\sqrt{2}[- \cos (\frac{θ}{2}) + \sin (\frac{θ}{2})]_{0}^{π} \\
&= 4\sqrt{2}(-\cos (\frac{π}{2}) + \sin (\frac{π}{2})) - (-\cos (0) + \sin (0)) \\
&= 4\sqrt{2}((0 + 1) - (-1 + 0)) \\
&= 8\sqrt{2}
\end{aligned}
$$

---

**Question 3**

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/q-123354.png>)

Calculate $∫_{C}ds$, where $C$ is the path along the polar curve $r = e^{2θ}$ for $0 \le θ \le \frac{π}{2}$, shown above.

- [ ] A. $\frac{\sqrt{5}}{2}(e^{π} - 1)$
- [ ] B. $\sqrt{5}(e^{π} - 1)$
- [ ] C. $2\sqrt{5}e^{2π}$
- [ ] D. $\frac{\sqrt{5}}{2}(e^{2π} + 1)$
- [ ] E. $\sqrt{5}(2e^{2π} - 1)$

---

**Question 4:** Calculate $∫_{C}ds$, where $C$ is the path along the polar curve $r = \sec θ$ for $\frac{π}{6} \le θ \le \frac{π}{3}$.

- [ ] A. $\frac{\sqrt{3}}{3}$
- [ ] B. $\sqrt{3}$
- [ ] C. $2\sqrt{3}$
- [ ] D. $\frac{\sqrt{3}}{2}$
- [ ] E. $\frac{2\sqrt{3}}{3}$

---

<a id="geometric-interpretation-of-the-arc-length-element-for-a-parametric-curve"></a>
## Geometric Interpretation of the Arc Length Element for a Parametric Curve

Let's now build up a geometric understanding of what the arc length element $\textrm d s$ means. First, we'll consider the case of a parametric curve defined as

$$
\mathbf r(t) = \langle x(t),\, y(t)\rangle
$$

Suppose that the point $P$ lies on this curve and has position vector $\mathbf r$.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/ed5c08671f8238fad1f90fe8d5e9f2aa.png>)

Now consider a small change $\Delta t$ in the parameter $t$. This change gives a new point $P'$ on the curve with position vector $\mathbf r + \Delta\mathbf r$, where $\Delta \mathbf r$ gives the total change (as a vector) in moving from $P$ to $P'$, as shown below.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/bdb1f578aa0f473bef8d85dc39a56104.png>)

According to the diagram we have

$$
\Delta x = x(t+\Delta t) - x(t), \qquad \Delta y = y(t+\Delta t) - y(t)
$$

where $\Delta x$ and $\Delta y$ are the changes in $x$ and $y$, respectively, corresponding to the change in $t$.

Using the fact that a difference quotient can be approximated as derivative, these quantities can be approximated as

$$
\Delta x \approx \dfrac{\textrm d x}{\textrm d t}\Delta t, \qquad \Delta y \approx \dfrac{\textrm d y}{\textrm d t}\Delta t
$$

The change in arc length $\Delta s$ can be approximated as the magnitude of $\Delta \mathbf r$. Therefore, by the Pythagorean theorem, we get

$$
(Δs)^{2} \mid \approx \mid \mid Δr \mid \mid ^{2}; = (Δx)^{2} + (Δy)^{2}; \approx (\frac{dx}{dt}Δt)^{2} + (\frac{dy}{dt}Δt)^{2}; = (\frac{dx}{dt})^{2}(Δt)^{2} + (\frac{dy}{dt})^{2}(Δt)^{2}; = [(\frac{dx}{dt})^{2} + (\frac{dy}{dt})^{2}](Δt)^{2}
$$

Therefore,

$$
\Delta s \approx \sqrt{\left(\dfrac{\textrm d x}{\textrm d t}\right)^2 + \left(\dfrac{\textrm d y}{\textrm d t}\right)^2}\, \Delta t
$$

If we imagine the limiting case where $\Delta t \to 0$, we arrive at the arc length element $\textrm d s$:

$$
\textrm d s = \sqrt{\left(\dfrac{\textrm d x}{\textrm d t}\right)^2 + \left(\dfrac{\textrm d y}{\textrm d t}\right)^2}\, \textrm d t
$$

---

<a id="geometric-interpretation-of-the-arc-length-element-for-a-polar-curve"></a>
## Geometric Interpretation of the Arc Length Element for a Polar Curve

Now, let's derive the formula for the arc length element $\textrm d s$ for a curve defined using plane polar coordinates. We will use a similar geometric approach.

Consider a curve

$$
r= r(\theta)
$$

defined using polar coordinates. Suppose that the point $P$ lies on this curve and has position vector $\mathbf r$.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/8f6578dee117c4a3811c4d54089fba84.png>)

Now consider a small change $\Delta \theta$ in the parameter $\theta$. This change gives a new point $P'$ on the curve with position vector $\mathbf r + \Delta\mathbf r$, where $\Delta \mathbf r$ gives the total change (as a vector) in moving from $P$ to $P'$, as shown below.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/18de4f57e53c517c96e93efeb22f346e.png>)

From here, we see that the vector $\Delta \mathbf r$ can be approximated as the vector addition of two components:

- a vector of length $\Delta r$ acting parallel to $\mathbf r$, and
- a vector of length $r\Delta \theta$ acting perpendicular to $\mathbf r$.

![](<../Source/4.2.4. Further Properties of Line Integrals of Scalar Functions/Images/01fc2a6e22d32e1ae9f41d43533acc57.png>)

Note that $\Delta r$ is simply the difference of the radial lengths between the points $P$ and $P'$. The expression $r\Delta \theta$ comes from the formula for the arc length of a sector of a circle.

Now, the change in arc length $\Delta s$ can be approximated as the magnitude of $\Delta \mathbf r$. Since our two components are perpendicular, we have

$$
(Δs)^{2} \mid \approx \mid \mid Δr \mid \mid ^{2}; \approx (Δr)^{2} + (rΔθ)^{2}; = Δr^{2} + r^{2}(Δθ)^{2}
$$

Using the fact that a difference quotient can be approximated as derivative, we have

$$
\Delta r = r(\theta + \Delta\theta) - r(\theta) = \dfrac{\textrm d r}{\textrm d \theta}\Delta\theta
$$

Substituting this into our expression for $(\Delta s)^2$ above, we get

$$
(Δs)^{2} \mid \approx (\frac{dr}{dθ})^{2}(Δθ)^{2} + r^{2}(Δθ)^{2}; = r^{2}(Δθ)^{2} + (\frac{dr}{dθ})^{2}(Δθ)^{2}; = [r^{2} + (\frac{dr}{dθ})^{2}](Δθ)^{2}
$$

Therefore,

$$
\Delta s = \sqrt{r^2 + \left(\dfrac{\textrm d r}{\textrm d \theta}\right)^2}\Delta\theta
$$

If we imagine the limiting case where $\Delta \theta \to 0$, we arrive at the arc length element $\textrm d s$:

$$
\textrm d s = \sqrt{r^2 + \left(\dfrac{\textrm d r}{\textrm d \theta}\right)^2}\textrm d\theta
$$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
