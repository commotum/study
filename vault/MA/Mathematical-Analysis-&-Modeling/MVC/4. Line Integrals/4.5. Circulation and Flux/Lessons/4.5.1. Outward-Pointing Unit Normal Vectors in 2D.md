# Outward-Pointing Unit Normal Vectors in 2D

<!--
lesson-id: 1401
topic-code: MVC.4.5.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating an Outward-Pointing Unit Normal Vector for a Positively Oriented Curve](#calculating-an-outward-pointing-unit-normal-vector-for-a-positively-oriented-curve)
- [Case of Negatively Oriented Curves](#case-of-negatively-oriented-curves)
- [Calculating an Outward-Pointing Unit Normal Vector for a Negatively Oriented Curve](#calculating-an-outward-pointing-unit-normal-vector-for-a-negatively-oriented-curve)
- [Deriving the General Formula for a Positively Oriented Curve](#deriving-the-general-formula-for-a-positively-oriented-curve)

## Prerequisites

- [Parametric Equations of Circles](<../../../../PCL/8. Parametric Equations/8.1. Parametric Equations/Lessons/8.1.5. Parametric Equations of Circles.md>)
- [Right-Angle Rotations as Linear Transformations](<../../../../PCL/6. Matrices/6.5. Linear Transformations/Lessons/6.5.7. Right-Angle Rotations as Linear Transformations.md>)
- [Unit Tangent Vectors](<../../../1. Vector Functions and Vector Fields/1.1. Vector-Valued Functions/Lessons/1.1.8. Unit Tangent Vectors.md>)
- [Parametric Equations of Ellipses](<../../../../PCL/8. Parametric Equations/8.1. Parametric Equations/Lessons/8.1.6. Parametric Equations of Ellipses.md>)
- [Simple, Closed, and Oriented Curves](<../../4.1. Line Integrals of Scalar Functions/Lessons/4.1.1. Simple, Closed, and Oriented Curves.md>)

---

<a id="introduction"></a>
## Introduction

When $C$ is a closed curve, there is always a well-defined notion of "inside" and "outside" $C$. It is usually easy to determine which region is inside or outside an arbitrary closed curve using our natural intuition.

One important quantity that we often need when dealing with closed, oriented curves is the **outward-pointing unit normal vector,** usually denoted $\mathbf n$.

As the name suggests:

- $\mathbf n$ is normal (i.e., perpendicular) to the curve $C$ at *every* point,
- it is a unit vector (i.e., its magnitude equals $1$), and
- it always points in the outward direction.

Since $\mathbf n$ must be normal to $C$ at *every* point, it must vary as we move around $C$. Therefore, it is usually written either as a function of the parameter space (i.e., $\mathbf n(t)$) or as a function of the coordinates of the points on $C$ (i.e., $\mathbf n(x,y)$).

As a concrete example, let's consider the closed, positively oriented ellipse $C$, parameterized as

$[MATH: \mathbf r(t) = 3\cos t\,\mathbf{i} + 2\sin t\,\mathbf{j}, \qquad t \in [0,2\pi).]$

![](<../Source/4.5.1. Outward-Pointing Unit Normal Vectors in 2D/Images/480a386111d0649afef49e31ff21b21c.png>)

For convenience, we first express $\mathbf r(t)$ in column vector notation:

$$
\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}
$$

Let's calculate the vector $\mathbf n$ for this curve. To do this, we follow three steps.

**Step 1**: We compute the tangent vector $\mathbf r'(t)$.

Differentiating the expression for $\mathbf r(t)$, we get

$$
\begin{aligned}
r^{′}(t) &= \begin{bmatrix}x^{′}(t) \\ y^{′}(t)\end{bmatrix} = \begin{bmatrix}-3\sin t \\ 2\cos t\end{bmatrix}
\end{aligned}
$$

A typical tangent vector for our curve is shown below.

![](<../Source/4.5.1. Outward-Pointing Unit Normal Vectors in 2D/Images/629dab8d0a63b38ee3a39515cb45fe68.png>)

**Step 2**: Since $C$ is oriented counterclockwise, we rotate $\mathbf r'(t)$ by $90^\circ$ *clockwise* to obtain an outward-pointing normal vector.

The rotation matrix that corresponds to a $90^\circ$ clockwise rotation is

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

Rotating the vector $\mathbf r'(t)$ by $90^\circ$ clockwise, we get the vector $\tilde{\mathbf n}(t)$, given by

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

The vector $\tilde{\mathbf n}(t)$ is shown below:

![](<../Source/4.5.1. Outward-Pointing Unit Normal Vectors in 2D/Images/54a7ab966461346aaca4262674b9f479.png>)

**Step 3**: Finally, we normalize the vector found in step 2.

The vector $\tilde{\mathbf n}(t)$ in an outward-pointing normal vector, but it is not a unit vector. Therefore, we need to normalize it.

Notice that

$$
\mid \mid \mathbf r'(t) \mid \mid = \sqrt{(3\sin t)^2 + (2\cos t)^2} = \sqrt{9\sin^2 t + 4\cos^2 t}
$$

Furthermore, the magnitude of the vector found in step 2 equals the magnitude of $\mathbf r'(t)$ (since they differ only by a rotation). Therefore, we can calculate our outward-pointing unit vector $\mathbf n$ as follows:

$$
\begin{aligned}
n &= (n^(~)(t))/(\mid \mid r^{′}(t) \mid \mid) \\
&= (1)/(\mid \mid r^{′}(t) \mid \mid)\begin{bmatrix}2\cos t \\ 3\sin t\end{bmatrix} \\
&= (2\cos ti + 3\sin tj)/(\mid \mid r^{′}(t) \mid \mid) \\
&= \frac{2\cos ti + 3\sin tj}{\sqrt{9\sin^{2} t + 4\cos^{2} t}}
\end{aligned}
$$

In conclusion, the vector

$$
\mathbf n = \dfrac{2\cos t\,\mathbf i + 3\sin t\,\mathbf j}{\sqrt{9\sin^2 t + 4\cos^2 t}}
$$

gives us an outward-pointing unit normal vector to every point on our ellipse.

---

<a id="calculating-an-outward-pointing-unit-normal-vector-for-a-positively-oriented-curve"></a>
## Calculating an Outward-Pointing Unit Normal Vector for a Positively Oriented Curve

**Example:** The closed curve $C$ is defined as $x^2+y^2 = 2$. Find the outward-pointing unit normal vector on $C$.

**Explanation**

We can parameterize $C$ as follows:

$$
\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}
$$

To calculate the outward-pointing normal vector of a closed, positively oriented curve, we follow three steps:

**Step 1**: We compute the tangent vector $\mathbf r'(t)$.

Differentiating the expression for $\mathbf r(t)$, we get

$$
\begin{aligned}
r^{′}(t) &= \begin{bmatrix}x^{′}(t) \\ y^{′}(t)\end{bmatrix} = \begin{bmatrix}-\sqrt{2}\sin t \\ \sqrt{2}\cos t\end{bmatrix}
\end{aligned}
$$

**Step 2**: Since $C$ is oriented counterclockwise, we rotate $\mathbf r'(t)$ by $90^\circ$ *clockwise* to obtain an outward-pointing normal vector.

The rotation matrix that corresponds to a $90^\circ$ clockwise rotation is

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

Rotating the vector $\mathbf r'(t)$ by $90^\circ$ clockwise, we get

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

**Step 3**: Finally, we normalize the vector found in step 2.

Note that the magnitude of the vector found in step 2 equals the magnitude of $\mathbf r'(t)$ (since they differ only by a rotation).

Now, since

$$
\mid \mid \mathbf r'(t) \mid \mid = \sqrt{(-\sqrt{2}\sin t)^2 + (\sqrt{2}\cos t)^2} = \sqrt{2}
$$

and since for our curve

$$
x(t) = \sqrt{2}\cos{t}
$$

and

$$
y(t) = \sqrt{2}\sin{t}
$$

it follows that

$$
\begin{aligned}
n &= (1)/(\mid \mid r^{′}(t) \mid \mid)\begin{bmatrix}\sqrt{2}\cos t \\ \sqrt{2}\sin t\end{bmatrix} \\
&= \frac{1}{\sqrt{2}}\begin{bmatrix}\sqrt{2}\cos t \\ \sqrt{2}\sin t\end{bmatrix} \\
&= \frac{1}{\sqrt{2}}\begin{bmatrix}x \\ y\end{bmatrix}
\end{aligned}
$$

---

**Question 1**

The closed, positively oriented curve $C$ is defined as $r(t) = 3\cos ti + 4\sin tj$ for $[MATH: t ∈ [0, 2π).]$ The outward-pointing unit normal vector on $C$ can be expressed as

$n = (a(t)i + b(t)j)/(\mid \mid r^{′}(t) \mid \mid)$.
Find the function $a(t)$.

- [ ] A. $4\cos t$
- [ ] B. $-3\sin t$
- [ ] C. $4\sin t$
- [ ] D. $-3\cos t$
- [ ] E. $-4\sin t$

---

**Question 2:** The closed curve $C$ is defined as $x^{2} + y^{2} = 9$. Find the outward-pointing unit normal vector on $C$.

- [ ] A. $\frac{1}{3}\begin{bmatrix}-x \\ y\end{bmatrix}$
- [ ] B. $\frac{1}{3}\begin{bmatrix}x \\ y\end{bmatrix}$
- [ ] C. $\begin{bmatrix}3x \\ 3y\end{bmatrix}$
- [ ] D. $\begin{bmatrix}x \\ y\end{bmatrix}$
- [ ] E. $\frac{1}{3}\begin{bmatrix}y \\ -x\end{bmatrix}$

---

<a id="case-of-negatively-oriented-curves"></a>
## Case of Negatively Oriented Curves

When curves are *negatively* oriented, the procedure of finding the outward-pointing unit normal vector is almost the same.

The only difference is the second step. If $C$ is negatively oriented (i.e., oriented *clockwise*), we must rotate $\mathbf r'(t)$ by $90^\circ$ *counterclockwise* to obtain an outward-pointing normal vector.

![](<../Source/4.5.1. Outward-Pointing Unit Normal Vectors in 2D/Images/9da1bd93090395f677d8a9e1c8876050.png>)

Let's take a look at an example.

---

<a id="calculating-an-outward-pointing-unit-normal-vector-for-a-negatively-oriented-curve"></a>
## Calculating an Outward-Pointing Unit Normal Vector for a Negatively Oriented Curve

**Example:** The closed curve $C$ is defined as $\mathbf r(t) = 12\sin t\,\mathbf{i} + 9\cos t \,\mathbf{j}$ for $[MATH: t \in [0,2\pi).]$ Find the outward-pointing unit normal vector on $C$.

*Hint: Observe that $C$ is traversed clockwise.*

**Explanation**

For convenience, we express $\mathbf r(t)$ in column vector notation:

$$
\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}
$$

To calculate the outward-pointing normal vector of a closed, *negatively* oriented curve, we follow three steps:

**Step 1**: We compute the tangent vector $\mathbf r'(t)$.

Differentiating the expression for $\mathbf r(t)$, we get

$$
\begin{aligned}
r^{′}(t) &= \begin{bmatrix}x^{′}(t) \\ y^{′}(t)\end{bmatrix} = \begin{bmatrix}12\cos t \\ -9\sin t\end{bmatrix}
\end{aligned}
$$

**Step 2**: Since $C$ is oriented clockwise, we rotate $\mathbf r'(t)$ by $90^\circ$ *counterclockwise* to obtain an outward-pointing normal vector.

The rotation matrix that corresponds to a $90^\circ$ counterclockwise rotation is

$$
\begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}
$$

Rotating the vector $\mathbf r'(t)$ by $90^\circ$ counterclockwise, we get

$$
\begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}
$$

**Step 3**: Finally, we normalize the vector found in step 2.

Note that the magnitude of the vector found in step 2 equals the magnitude of $\mathbf r'(t)$ (since they differ only by a rotation). Therefore,

$$
\begin{aligned}
n &= (1)/(\mid \mid r^{′}(t) \mid \mid)\begin{bmatrix}9\sin t \\ 12\cos t\end{bmatrix} = \frac{9\sin ti + 12\cos tj}{\sqrt{144\cos^{2} t + 81\sin^{2} t}}
\end{aligned}
$$

---

**Question 3**

The closed curve $C$ is defined as $r(t) = 7\sin ti + 24\cos tj$ for $[MATH: t ∈ [0, 2π).]$ The outward-pointing unit normal vector for $C$ can be expressed as

$n = (a(t)i + b(t)j)/(\mid \mid r^{′}(t) \mid \mid)$.
Find the function $b(t)$.

*Hint: Observe that $C$ is traversed clockwise.*

- [ ] A. $-7\cos t$
- [ ] B. $-24\cos t$
- [ ] C. $7\cos t$
- [ ] D. $7\sin t$
- [ ] E. $24\sin t$

---

**Question 4**

The closed curve $C$ is defined as $r(t) = 4\sin ti + 8\cos tj$ for $[MATH: t ∈ [0, 2π).]$ The outward-pointing unit normal vector for $C$ can be expressed as

$n = (a(t)i + b(t)j)/(\mid \mid r^{′}(t) \mid \mid)$.
Find the function $a(t)$.

*Hint: Observe that $C$ is traversed clockwise.*

- [ ] A. $4\sin t$
- [ ] B. $8\sin t$
- [ ] C. $-8\sin t$
- [ ] D. $4\cos t$
- [ ] E. $-8\cos t$

---

<a id="deriving-the-general-formula-for-a-positively-oriented-curve"></a>
## Deriving the General Formula for a Positively Oriented Curve

Let's consider a general, closed curve $C$ in the plane, given by

$$
\mathbf r(t) = x(t)\,\mathbf{i} + y(t)\,\mathbf{j}
$$

We will assume that $C$ is oriented counterclockwise. Our goal is to derive a general formula for $\mathbf n$.

As always, we first express $\mathbf r(t)$ in column vector notation:

$$
\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}
$$

To calculate $\mathbf n$, we follow our usual three steps.

**Step 1**: We compute the tangent vector $\mathbf r'(t)$.

Differentiating the expression for $\mathbf r(t)$, we get

$$
\begin{aligned}
r^{′}(t) &= \begin{bmatrix}x^{′}(t) \\ y^{′}(t)\end{bmatrix}
\end{aligned}
$$

**Step 2**: Since $C$ is oriented counterclockwise, we rotate $\mathbf r'(t)$ by $90^\circ$ *clockwise* to obtain an outward-pointing normal vector.

The rotation matrix that corresponds to a $90^\circ$ clockwise rotation is

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

Rotating the vector $\mathbf r'(t)$ by $90^\circ$ clockwise, we get the vector
$\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}$

**Step 3**: Finally, we normalize the vector found in step 2.

Notice that

$$
\mid \mid \mathbf r'(t) \mid \mid = \sqrt{(x'(t))^2 + (y'(t))^2}
$$

Furthermore, the magnitude of the vector found in step 2 equals the magnitude of $\mathbf r'(t)$ (since they differ only by a rotation). Therefore, we can calculate our outward-pointing unit vector $\mathbf n$ as follows:

$$
\begin{aligned}
n &= (1)/(\mid \mid r^{′}(t) \mid \mid)\begin{bmatrix}y^{′}(t) \\ -x^{′}(t)\end{bmatrix} \\
&= (y^{′}(t))/(\mid \mid r^{′}(t) \mid \mid)i - (x^{′}(t))/(\mid \mid r^{′}(t) \mid \mid)j \\
&= (y^{′}(t)i - x^{′}(t)j)/(\sqrt{(x^{′}(t))^{2} + (y^{′}(t))^{2}})
\end{aligned}
$$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
