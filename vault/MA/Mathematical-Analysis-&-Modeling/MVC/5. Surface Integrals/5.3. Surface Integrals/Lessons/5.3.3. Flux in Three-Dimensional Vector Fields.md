# Flux in Three-Dimensional Vector Fields

<!--
lesson-id: 3178
topic-code: MVC.5.3.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Orientable vs. Non-Orientable Surfaces](#orientable-vs-non-orientable-surfaces)
- [Flux in Three-Dimensional Vector Fields](#flux-in-three-dimensional-vector-fields)
- [A Worked Example](#a-worked-example)
- [Rewriting a Flux Integral as a Surface Integral](#rewriting-a-flux-integral-as-a-surface-integral)
- [The Fundamental Vector Product for a Horizontal Plane](#the-fundamental-vector-product-for-a-horizontal-plane)
- [Computing the Flux of a Vector Field Through a Horizontal Plane](#computing-the-flux-of-a-vector-field-through-a-horizontal-plane)
- [Computing the Flux of a Vector Field Through a Tilted Plane](#computing-the-flux-of-a-vector-field-through-a-tilted-plane)

## Prerequisites

- [Surface Integrals Over Cartesian Surfaces](<5.3.2. Surface Integrals Over Cartesian Surfaces.md>)
- [Flux in Two-Dimensional Vector Fields](<../../../4. Line Integrals/4.5. Circulation and Flux/Lessons/4.5.3. Flux in Two-Dimensional Vector Fields.md>)

---

<a id="introduction"></a>
## Introduction

Recall that the flux of a two-dimensional vector field $\mathbf F(x,y)$ across a curve $C$ with respect to a unit normal vector $\mathbf n(x,y)$ is given by the line integral

$$
\int\limits_C \mathbf F\cdot\mathbf n\,\textrm d s
$$

The flux measures the total flow of $\mathbf F$ through $C$, as depicted below.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/d0ac58663b7e555d37d7229aefad1b61.png>)

Flux is always measured with respect to a preselected unit normal vector $\mathbf n$, and we have two choices of orientation for this vector. If we measure the flux relative to the unit normal pointing in the *opposite* direction, our flux will have the *same* magnitude but *opposite* sign.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/58056eccdaad2e8c6dae70eeec2ecbf5.png>)

This lesson aims to extend the concept of flux to three-dimensional vector fields. But first, we need a brief discussion on surface orientations.

---

<a id="orientable-vs-non-orientable-surfaces"></a>
## Orientable vs. Non-Orientable Surfaces

A surface is **orientable** if it has two distinct sides. So, for example, the surface $S$ shown below is orientable.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/863a0f4f50aff75d548f442023613b08.png>)

If a unit vector $\mathbf n(x,y,z)$ to our orientable surface varies continuously over $S$, we say that $S$ is **oriented** and the choice of $\mathbf n$ gives an **orientation** of $S$.

We can choose the opposite orientation for our surface by selecting a vector that points in the opposite direction, as shown below.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/5c21bead69939e5258200d46e5d45041.png>)

When dealing with two-sided (i.e., orientable) surfaces, we must always be clear about our chosen orientation.

Note that some surfaces are *not* orientable. This means they do not have two distinct sides. An example of a non-orientable surface is a Möbius strip, shown below.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/cc9510a809bc946615d31fe3f4e2ba0b.png>)

A Möbius strip can be made by taking a long rectangular piece of paper, twisting one of its ends by $180^\circ$ and gluing the two ends together.

A Möbius strip does not have two distinct sides because if you were to start at a point $P$ on the strip and move along it (following the dark blue curve shown), you'd eventually end up at the same point but on the opposite side! Thus, a Möbius strip only has one distinct side.

In the work that follows, we will only consider orientable surfaces.

---

<a id="flux-in-three-dimensional-vector-fields"></a>
## Flux in Three-Dimensional Vector Fields

In three dimensions, flux measures the total flow of a vector field $\mathbf F(x,y,z)$ *through an orientable surface.*

Suppose that $\mathbf F(x,y,z)$ is a three-dimensional vector field, and $S$ is an orientable surface with the choice of orientation given by the unit normal vector $\mathbf n(x,y,z)$, as shown below.

![](<../Source/5.3.3. Flux in Three-Dimensional Vector Fields/Images/d059a376a3f6a12aea460953bbf34358.png>)

The **flux of $\mathbf F$ across $\boldsymbol{S}$** with respect to $\mathbf n$, which we write as

$$
\iint\limits_S \mathbf F \cdot \textrm d \mathbf S
$$

is given by the surface integral

$$
\iint\limits_S \mathbf F \cdot \mathbf n \,\textrm d S
$$

It's worth taking a moment to compare this to the flux formula for two-dimensional vector fields:

- The quantity $\mathbf F \cdot \mathbf n$ evaluated at a point $P$ on $S$ measures the projection of $\mathbf F$ in the direction of $\mathbf n$ at $P$.
- By summing (integrating) the quantity $\mathbf F \cdot \mathbf n$ over the entire surface, we get the total flow of $\mathbf F$ through $S$.

Our flux depends on our choice of orientation. If we were to select the *opposite* orientation, we'd get a result with the same magnitude but opposite sign.

---

<a id="a-worked-example"></a>
## A Worked Example

Consider the vector field $\mathbf F$ and the surface $S$ with unit normal vector $\mathbf n$, where

$$
\mathbf{F}(x,y,z) = \left\langle xz + y,\: x^2,\: 2y-z^2 \right\rangle\!, \qquad \mathbf{n}(x,y,z) = \dfrac{1}{\sqrt5}\left\langle 2,\: 0,\: -1 \right\rangle\!
$$

Let's construct a surface integral representing the flux of $\mathbf F$ across $S$ with respect to $\mathbf{n}$.

First, we recall that if $S$ is an oriented surface with unit normal vector $\mathbf n$, and $\mathbf F$ is continuous vector field, then the flux of $\mathbf F$ across $S$ measured with respect to $\mathbf n$ is given by

$$
\iint\limits_S \mathbf{F} \cdot \textrm{d}\mathbf{S} = \iint\limits_S \mathbf{F} \cdot \mathbf{n} \,\textrm{d}S
$$

Calculating the dot product, we have

$$
\begin{aligned}
F \cdot n &= ⟨xz + y, x^{2}, 2y - z^{2}⟩ \cdot \frac{1}{\sqrt{5}}⟨2, 0,-1⟩ \\
&= \frac{1}{\sqrt{5}}⟨xz + y, x^{2}, 2y - z^{2}⟩ \cdot ⟨2, 0,-1⟩ \\
&= \frac{1}{\sqrt{5}}((xz + y) \cdot 2 + x^{2} \cdot 0 + (2y - z^{2}) \cdot (-1)) \\
&= \frac{1}{\sqrt{5}}(2xz + 2y - 2y + z^{2}) \\
&= \frac{2xz + z^{2}}{\sqrt{5}}
\end{aligned}
$$

Therefore, the surface integral representing the flux of $\mathbf F$ across $S$ with respect to $\mathbf n$ can be written as follows:

$$
\begin{aligned}
∬_(S)F \cdot dS &= ∬_(S)F \cdot ndS \\
&= ∬_(S)\frac{2xz + z^{2}}{\sqrt{5}}dS \\
&= \frac{1}{\sqrt{5}}∬_(S)2xz + z^{2}dS
\end{aligned}
$$

We can then evaluate this integral using the usual methods.

---

<a id="rewriting-a-flux-integral-as-a-surface-integral"></a>
## Rewriting a Flux Integral as a Surface Integral

**Example:** Consider the vector field $\mathbf F$ and the surface $S$ with normal vector $\widetilde{\mathbf n}$, where

$\mathbf{F}(x,y,z) = \left\langle x,\: 2z,\: z-y \right\rangle\!, \quad \widetilde{\mathbf{n}}(x,y,z) = \left\langle z,\: -y,\: -x \right\rangle\!, \quad \mathbf n = \dfrac{\widetilde{\mathbf{n}}}{\Vert \widetilde{\mathbf{n}} \Vert}$.

If the flux of $\mathbf F$ across $S$ with respect to $\mathbf{n}$ is given by

$AAAAAAA$

what is the missing expression?

**Explanation**

If $S$ is an oriented surface with unit normal vector $\mathbf n$, and $\mathbf F$ is continuous vector field, then the flux of $\mathbf F$ across $S$ measured with respect to $\mathbf n$ is given by

$$
\iint\limits_S \mathbf{F} \cdot \textrm{d}\mathbf{S} = \iint\limits_S \mathbf{F} \cdot \mathbf{n} \,\textrm{d}S
$$

To compute the above dot product, we must normalize the vector $\widetilde{\mathbf n}$. By doing this, we get

$$
\begin{aligned}
n &= (n^(~))/(‖n^(~)‖) \\
&= (⟨z,-y,-x⟩)/(\sqrt{(z)^{2} + (-y)^{2} + (-x)^{2}}) \\
&= \frac{⟨z,-y,-x⟩}{\sqrt{z^{2} + y^{2} + x^{2}}} \\
&= \frac{⟨z,-y,-x⟩}{\sqrt{x^{2} + y^{2} + z^{2}}}
\end{aligned}
$$

Calculating the dot product, we have

$$
\begin{aligned}
F \cdot n &= ⟨x, 2z, z - y⟩ \cdot \frac{⟨z,-y,-x⟩}{\sqrt{x^{2} + y^{2} + z^{2}}} \\
&= \frac{⟨x, 2z, z - y⟩ \cdot ⟨z,-y,-x⟩}{\sqrt{x^{2} + y^{2} + z^{2}}} \\
&= (xz - 2yz - x(z - y))/(\sqrt{x^{2} + y^{2} + z^{2}}) \\
&= \frac{xy - 2yz}{\sqrt{x^{2} + y^{2} + z^{2}}}
\end{aligned}
$$

Therefore, the flux can be written as

$$
\begin{aligned}
∬_(S)F \cdot dS &= ∬_(S)F \cdot ndS \\
&= ∬_(S)\frac{x y - 2 y z}{\sqrt{x^{2} + y^{2} + z^{2}}}dS
\end{aligned}
$$

---

**Question 1**

```quiz
type: radio
id: ma-161149
content: |-
  Consider the vector field $F$ and the surface $S$ with unit normal vector $n$, where

  $F(x, y, z) = ⟨2z, x + y, y - z⟩, n(x, y, z) = \frac{1}{\sqrt{3}}⟨1,-1, 1⟩$.

  If the flux of $F$ across $S$ with respect to $n$ is given by

  $∬_(S)F \cdot dS = \frac{1}{\sqrt{3}}∬_(S)AAAAAAAdS$,

  what is the missing expression?
options:
- id: a
  content: |-
    $-y - z$
- id: b
  content: |-
    $y + z$
- id: c
  content: |-
    $x + 2y + z$
- id: d
  content: |-
    $z - x$
  correct: true
- id: e
  content: |-
    $x + y + 2z$
```

---

**Question 2**

```quiz
type: radio
id: ma-160838
content: |-
  Consider the vector field $F$ and the surface $S$ with normal vector $n^(~)$, where

  $F(x, y, z) = ⟨z, y, x⟩, n^(~)(x, y, z) = ⟨2y,-z, 2x⟩, n = (n^(~))/(‖n^(~)‖)$.

  If the flux of $F$ across $S$ with respect to $n$ is given by

  $∬_(S)F \cdot dS = ∬_(S)\frac{AAAAAAA}{\sqrt{4x^{2} + 4y^{2} + z^{2}}}dS$

  what is the missing expression?
options:
- id: a
  content: |-
    $2xy + yz + xz$
- id: b
  content: |-
    $2xy - yz + 2xz$
- id: c
  content: |-
    $2x^{2} + 2yz$
- id: d
  content: |-
    $2x^{2} + 3yz$
- id: e
  content: |-
    $2x^{2} + yz$
  correct: true
```

---

<a id="the-fundamental-vector-product-for-a-horizontal-plane"></a>
## The Fundamental Vector Product for a Horizontal Plane

Suppose we have the horizontal plane $z=k$, where $k$ is a fixed parameter.

The magnitude of the fundamental vector product is given by

$$
\begin{aligned}
\mid \mid r_{x} \cdot r_{y} \mid \mid &= \sqrt{1 + (\frac{∂z}{∂x})^{2} + (\frac{∂z}{∂y})^{2}} \\
&= \sqrt{1 + (\frac{∂k}{∂x})^{2} + (\frac{∂k}{∂y})^{2}} \\
&= \sqrt{1 + 0^{2} + 0^{2}} \\
&= \sqrt{1} \\
&= 1
\end{aligned}
$$

Therefore, in such cases, we can write

$$
\begin{aligned}
∬_(S)f(x, y, z)dS &= ∬_(D)f(x, y, z) \cdot \mid \mid r_{x} \cdot r_{y} \mid \mid dA \\
&= ∬_(D)f(x, y, z) \cdot 1dA \\
&= ∬_(D)f(x, y, z)dA
\end{aligned}
$$

Restating, we have

$$
\iint\limits_S f(x,y,z)\, \textrm d S = \iint\limits_D f(x,y,z) \,\textrm d A
$$

We arrive at a similar result when considering vertical planes parallel to the $xz$- or $yz$-axes.

This observation can help provide a shortcut when calculating the flux of a vector field through a horizontal plane. Let's see an example.

---

<a id="computing-the-flux-of-a-vector-field-through-a-horizontal-plane"></a>
## Computing the Flux of a Vector Field Through a Horizontal Plane

**Example:** Consider the vector field $\mathbf F(x,y,z) = \langle z,\: z,\: 4xy\rangle$ and the surface $S$ defined in Cartesian coordinates as $z=1$ for $0\leq x\leq 1$ and $0\leq y\leq 1$. Calculate the flux of $\mathbf F$ through $S$ with respect to the *downward*-pointing unit normal vector $\mathbf n$.

**Explanation**

If $S$ is an oriented surface with unit normal vector $\mathbf n$, and $\mathbf F$ is continuous vector field, then the flux of $\mathbf F$ across $S$ measured with respect to $\mathbf n$ is given by

$$
\iint\limits_S \mathbf{F} \cdot \textrm{d}\mathbf{S} = \iint\limits_S \mathbf{F} \cdot \mathbf{n} \,\textrm{d}S
$$

For the surface $z=1$ for

$$
0\leq x\leq 1
$$

and

$$
0\leq y\leq 1
$$

the downward-pointing unit normal vector is

$$
\mathbf n = \langle 0,\, 0,\, -1\rangle
$$

Using the fact that $z=1$ on $S$, we have

$$
\begin{aligned}
F \cdot n &= ⟨z, z, 4xy⟩ \cdot ⟨0, 0,-1⟩ \\
&= ⟨1, 1, 4xy⟩ \cdot ⟨0, 0,-1⟩ \\
&=-4xy
\end{aligned}
$$

Therefore, we can calculate the flux of $\mathbf F$ across $S$ as follows:

$$
\begin{aligned}
∬_(S)F \cdot dS &= ∬_(S)F \cdot ndS \\
&= ∬_(S)(-4xy)dS \\
&= ∬_(D)(-4xy)dA \\
&= ∫_{0}^{1}∫_{0}^{1}(-4xy)dxdy \\
&= - ∫_{0}^{1}2ydy∫_{0}^{1}2xdx \\
&= - [y^{2}]_{0}^{1} \cdot [x^{2}]_{0}^{1} \\
&=-1
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-161152
content: |-
  Consider the vector field $F(x, y, z) = ⟨z, y, x⟩$ and the surface $S$ defined in Cartesian coordinates as $z = 1$ for $0 \le x \le 1$ and $0 \le y \le 1$. Calculate the flux of $F$ through $S$ with respect to the upward-pointing unit normal vector $n$.
options:
- id: a
  content: |-
    $\frac{1}{4}$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $1$
- id: d
  content: |-
    $\frac{1}{2}$
  correct: true
- id: e
  content: |-
    $2$
```

---

**Question 4:**

```quiz
type: radio
id: ma-162146
content: |-
  Consider the vector field $F(x, y, z) = ⟨x, y, z + 3⟩$ and a finite region $S$ of the plane $z = x + y$ such that $Area(S) = \sqrt{3}$. Calculate the flux of $F$ through $S$ with respect to the upward-pointing unit normal vector $n$.
options:
- id: a
  content: |-
    $3$
  correct: true
- id: b
  content: |-
    $\frac{1}{\sqrt{3}}$
- id: c
  content: |-
    $\frac{1}{3}$
- id: d
  content: |-
    $1$
- id: e
  content: |-
    $\sqrt{3}$
```

---

<a id="computing-the-flux-of-a-vector-field-through-a-tilted-plane"></a>
## Computing the Flux of a Vector Field Through a Tilted Plane

**Example:** Consider the vector field $\mathbf F(x,y,z) = \langle y,\: 0,\: z\rangle$ and the surface $S$ defined in Cartesian coordinates as $z=2x+2y$ for $0\leq x\leq 2$ and $0\leq y\leq 1$. Calculate the flux of $\mathbf F$ through $S$ with respect to the upward-pointing unit normal vector $\mathbf n$.

*Hint: You're given that the fundamental vector product of $S$ with respect to its canonical parametrization is $\mid \mid \mathbf r_x' \times \mathbf r_y' \mid \mid =3$.*

**Explanation**

If $S$ is an oriented surface with unit normal vector $\mathbf n$, and $\mathbf F$ is continuous vector field, then the flux of $\mathbf F$ across $S$ measured with respect to $\mathbf n$ is given by

$$
\iint\limits_S \mathbf{F} \cdot \textrm{d}\mathbf{S} = \iint\limits_S \mathbf{F} \cdot \mathbf{n} \,\textrm{d}S
$$

Notice that our surface

$$
z = 2x+2y
$$

can be written using the dot product as

$$
\langle x,\: y,\: z\rangle \cdot \underbrace{\langle -2,\: -2, \: 1\rangle}_{\widetilde{\mathbf n}} = 0, \quad \textrm{or}\quad \langle x,\: y,\: z\rangle \cdot \underbrace{\langle 2,\: 2, \: -1\rangle}_{-\widetilde{\mathbf n}}= 0
$$

Since we require an upward-pointing normal vector to our surface $S$, we must select one where the $z$-component is positive. This gives us the normal vector

$$
\widetilde{\mathbf n} = \langle -2,\, -2,\, 1\rangle
$$

Normalizing this vector, we get

$$
\begin{aligned}
n &= (n^(~))/(‖n^(~)‖) \\
&= (⟨ - 2,-2, 1⟩)/(\sqrt{(-2)^{2} + (-2)^{2} + 1^{2}}) \\
&= \frac{1}{3}⟨ - 2,-2, 1⟩
\end{aligned}
$$

Using the fact that

$$
z=2x+2y
$$

on $S$, we have

$$
\begin{aligned}
F \cdot n &= ⟨y, 0, z⟩ \cdot \frac{1}{3}⟨ - 2,-2, 1⟩ \\
&= \frac{1}{3}⟨y, 0, 2x + 2y⟩ \cdot ⟨ - 2,-2, 1⟩ \\
&= \frac{1}{3}(-2y + 0x + 2x + 2y) \\
&= \frac{1}{3}(2x) \\
&= \frac{2}{3}x
\end{aligned}
$$

Therefore, we can calculate the flux of $\mathbf F$ across $S$ as follows:

$$
\begin{aligned}
∬_(S)F \cdot dS &= ∬_(S)F \cdot ndS \\
&= ∬_(S)\frac{2}{3}xdS \\
&= ∬_(D)\frac{2}{3}x \cdot \mid \mid r_{x}^{′} \cdot r_{y}^{′} \mid \mid dA \\
&= ∬_(D)\frac{2}{3}x \cdot 3dA \\
&= ∬_(D)\frac{2}{3}x \cdot 3dA \\
&= ∬_(D)2xdA \\
&= ∫_{0}^{1}∫_{0}^{2}2xdxdy \\
&= ∫_{0}^{1}dy \cdot ∫_{0}^{2}2xdx \\
&= [y]_{0}^{1} \cdot [x^{2}]_{0}^{2} \\
&= 1 \cdot 4 \\
&= 4
\end{aligned}
$$

---

**Question 5**

```quiz
type: radio
id: ma-161158
content: |-
  Consider the vector field $F(x, y, z) = ⟨x, y, 2z⟩$ and the surface $S$ defined in Cartesian coordinates as $z = 2x + 3y$ for $0 \le x \le 1$ and $0 \le y \le 1$. Calculate the flux of $F$ through $S$ with respect to the upward-pointing unit normal vector $n$.

  *Hint: You're given that the fundamental vector product of $S$ with respect to its canonical parametrization is $\mid \mid r_{x}^{′} \cdot r_{y}^{′} \mid |= \sqrt{14}$.*
options:
- id: a
  content: |-
    $\frac{3}{2}$
- id: b
  content: |-
    $\frac{5}{2\sqrt{14}}$
- id: c
  content: |-
    $\frac{3}{2\sqrt{14}}$
- id: d
  content: |-
    $\frac{5}{2}$
  correct: true
- id: e
  content: |-
    $\frac{3}{4\sqrt{14}}$
```

---

**Question 6**

```quiz
type: radio
id: ma-161244
content: |-
  Consider the vector field $F(x, y, z) = ⟨x, y, z⟩$ and the surface $S$ defined in Cartesian coordinates as $z = 2x + 2y - 2$ for $0 \le x \le 3$ and $-1 \le y \le 1$. Calculate the flux of $F$ through $S$ with respect to the *downward*-pointing unit normal vector $n$.

  *Hint: You're given that the fundamental vector product of $S$ with respect to its canonical parametrization is $\mid \mid r_{x}^{′} \cdot r_{y}^{′} \mid |= 3$.*
options:
- id: a
  content: |-
    $12$
  correct: true
- id: b
  content: |-
    $-4$
- id: c
  content: |-
    $-2$
- id: d
  content: |-
    $2$
- id: e
  content: |-
    $4$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
