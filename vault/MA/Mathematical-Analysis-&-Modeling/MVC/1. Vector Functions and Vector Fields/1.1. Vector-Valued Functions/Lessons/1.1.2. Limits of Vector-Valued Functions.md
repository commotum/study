# Limits of Vector-Valued Functions

<!--
lesson-id: 1741
topic-code: MVC.1.1.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Limit of a Vector Function](#finding-the-limit-of-a-vector-function)
- [Basic Properties of Limits of Vector Functions](#basic-properties-of-limits-of-vector-functions)
- [Finding the Limit of the Magnitude of a Vector Function](#finding-the-limit-of-the-magnitude-of-a-vector-function)
- [Finding the Limit of a Sum of Vector Functions](#finding-the-limit-of-a-sum-of-vector-functions)
- [Limits of Vector Products](#limits-of-vector-products)
- [Finding the Limit of a Product of Vector Functions](#finding-the-limit-of-a-product-of-vector-functions)

## Prerequisites

- [Calculating the Dot Product Using Components](<../../../../PCL/5. Vectors/5.4. The Dot Product/Lessons/5.4.2. Calculating the Dot Product Using Components.md>)
- [Calculating the Cross Product Using Determinants](<../../../../PCL/5. Vectors/5.5. The Cross Product/Lessons/5.5.3. Calculating the Cross Product Using Determinants.md>)
- [L'Hopital's Rule](<../../../../CAB/5. Analytical Applications of Differentiation/5.1. L'Hopital's Rule/Lessons/5.1.1. L'Hopital's Rule.md>)
- [Special Limits Involving Sine](<../../../../CAB/1. Limits and Continuity/1.5. Special Limits/Lessons/1.5.2. Special Limits Involving Sine.md>)
- [The Domain of a Vector-Valued Function](<1.1.1. The Domain of a Vector-Valued Function.md>)
- [Limits Involving the Exponential Function](<../../../../CBC/1. Limits/1.5. Special Limits/Lessons/1.5.5. Limits Involving the Exponential Function.md>)

---

<a id="introduction"></a>
## Introduction

To find the limit of a vector function, we compute the limit of each component individually. So, if

$$
\mathbf f (t) = x(t) \ \mathbf {i} + y(t) \ \mathbf {j} +z(t) \ \mathbf {k}
$$

is our vector function, then the limit is computed as follows:

$$
\displaystyle \lim_{t\to a} \mathbf f (t) = \bigg[ \lim_{t \to a} x(t) \bigg] \mathbf {i} + \bigg[ \lim_{t \to a}y(t) \bigg] \mathbf {j} +\bigg[ \lim_{t \to a}z(t) \bigg] \mathbf {k}
$$

The limit on the left exists *if and only if* all the limits on the right exist too.

For example, suppose we are given the vector function

$$
\mathbf f (t) = \left(5+t \right) \ \mathbf {i} + (t^{2}-1) \ \mathbf {j} + \sin{t} \ \mathbf {k}
$$

and we want to compute the limit $\lim\limits_{t \to 0} \mathbf f (t)$. To do this, we find the limit of each component function as follows:

$$
\begin{aligned}
lim_(t → 0)f(t) &= [lim_(t → 0)(5 + t)_(⏟)_(= 5)]i + [lim_(t → 0)(t^{2} - 1)_(⏟)_(=-1)]j + [lim_(t → 0)\sin t_(⏟)_(= 0)]k \\
&= 5i + (-1)j + 0k \\
&= ⟨5,-1, 0⟩
\end{aligned}
$$

---

<a id="finding-the-limit-of-a-vector-function"></a>
## Finding the Limit of a Vector Function

**Example:** Calculate $\lim\limits_{t \to 0} \mathbf{f}(t)$, given that
$\mathbf f (t) = \dfrac{1- e^{t}}{\sin t} \ \mathbf {i}+ \dfrac{\sin t}{t+t^2} \ \mathbf {j} + \dfrac{e^{t} - e^{-t}}{\sin t} \ \mathbf {k}$.

**Explanation**

To find the limit of our vector function, we compute the limit of each component individually:

$$
\lim_{t \to 0}\mathbf f(t) = \left(\lim_{t \to 0} \dfrac{1-e^t}{\sin t}\right)\,\mathbf i + \left(\lim_{t \to 0} \dfrac{\sin t}{t+t^2}\right)\,\mathbf j + \left(\lim_{t \to 0} \dfrac{e^t-e^{-t}}{\sin t} \right)\,\mathbf k
$$

Now, we find the limits one-by-one.

- The first component can be found using L'Hopital's Rule:
$lim_(t → 0)\frac{1 - e^{t}}{\sin t}|= lim_(t → 0)((1 - e^{t})^{′})/((\sin t)^{′}); = lim_(t → 0)\frac{-e^{t}}{\cos t}; = \frac{-e^{0}}{\cos 0}; =-\frac{1}{1}; =-1$
- The second component can be found using a special limit:
$lim_(t → 0)\frac{\sin t}{t + t^{2}}|= lim_(t → 0)(\sin t)/(t(t + 1)); = (lim_(t → 0)\frac{1}{t + 1}) \cdot (lim_(t → 0)\frac{\sin t}{t}); = (\frac{1}{0 + 1}) \cdot 1; = 1$
- The third component can be found using L'Hopital's Rule:
$lim_(t → 0)\frac{e^{t} - e^{-t}}{\sin t}|= lim_(t → 0)((e^{t} - e^{-t})^{′})/((\sin t)^{′}); = lim_(t → 0)\frac{e^{t} + e^{-t}}{\cos t}; = \frac{e^{0} + e^{-0}}{\cos 0}; = \frac{1 + 1}{1}; = 2$

Therefore,

$$
\lim\limits_{t \to 0}\mathbf f(t) = -\mathbf i + \mathbf j +2\,\mathbf k
$$

---

**Question 1:** Calculate $lim_(s → 0)f(s)$, if $f(s) = ⟨\frac{1}{e^{s}}, \frac{\sin 3s}{6s}, s^{2} - 1⟩$.

- [ ] A. $⟨1, \frac{1}{2}, 1⟩$
- [ ] B. $⟨1, 0,-1⟩$
- [ ] C. $⟨0, \frac{1}{2},-1⟩$
- [ ] D. $⟨0, 0,-1⟩$
- [ ] E. $⟨1, \frac{1}{2},-1⟩$

---

**Question 2:** Find the limit $lim_(t → 2)f(t)$, if $f(t) = t^{2}i + \frac{t - 4}{2}j + \frac{1}{t}k$.

- [ ] A. $4i + 2j + \frac{1}{2}k$
- [ ] B. $4i + j + \frac{1}{2}k$
- [ ] C. $2i - 4j + \frac{1}{2}k$
- [ ] D. $4i - 2j + \frac{1}{2}k$
- [ ] E. $4i - j + \frac{1}{2}k$

---

<a id="basic-properties-of-limits-of-vector-functions"></a>
## Basic Properties of Limits of Vector Functions

Many properties of limits of scalar functions extend naturally to limits of vector functions. For instance, if $\mathbf f(t)$ and $\mathbf g(t)$ are vector functions and $p,q$ are constants, then

$$
\lim_{t \to a}\left[p \ \mathbf f(t) + q \ \mathbf g(t)\right] = p \ \lim_{t \to a}\mathbf f(t) + q \ \lim_{t \to a}\mathbf g(t)
$$

We also have the following property for the magnitude:

$$
\begin{aligned}
lim_(t → a)‖f(t)‖ &= ‖lim_(t → a)f(t)‖
\end{aligned}
$$

where

$$
\left\| \mathbf f(t) \right\|
$$

denotes the magnitude of the vector $\mathbf f(t)$.

---

<a id="finding-the-limit-of-the-magnitude-of-a-vector-function"></a>
## Finding the Limit of the Magnitude of a Vector Function

**Example:** Calculate $\lim\limits_{t \to \infty} \| \mathbf f(t) \|$ if $\mathbf f(t) = \left\langle t^{-1}, \: \left(1+\dfrac{3}{t}\right)^t, \: \dfrac{t^2}{1-t^2} \right\rangle$.

**Explanation**

To find the limit of our vector function, we compute the limit of each component individually:

$$
\begin{aligned}
lim_(t → ∞)f(t) &= ⟨lim_(t → ∞)t^{-1}, lim_(t → ∞)(1 + \frac{3}{t})^{t}, lim_(t → ∞)\frac{t^{2}}{1 - t^{2}}⟩
\end{aligned}
$$

Now, we find the limits one-by-one.

- For the first component, we have
$lim_(t → ∞)t^{-1}|= lim_(t → ∞)\frac{1}{t} = 0$.
- The second component can be found using a special limit:
$lim_(t → ∞)(1 + \frac{3}{t})^{t}|= lim_(t → ∞)[(1 + \frac{1}{t/3})^{t/3}]^{3}; = [lim_(t → ∞)(1 + \frac{1}{t/3})^{t/3}]^{3}; = [e]^{3}; = e^{3}$
- For the third component, we have
$lim_(t → ∞)\frac{t^{2}}{1 - t^{2}}|= lim_(t → ∞)\frac{1}{\frac{1}{t^{2}} - 1} =-1$.

Therefore,

$$
\lim\limits_{t \to \infty}\mathbf f(t) = \left\langle 0, \: e^3, \: -1\right\rangle
$$

Finally, the limit of

$$
\left\| \mathbf f(t) \right\|
$$

as $t$ approaches $\infty$ is

$$
\begin{aligned}
lim_(t → ∞)‖f(t)‖ &= ‖lim_(t → ∞)f(t)‖ \\
&= ‖⟨0, e^{3},-1⟩‖ \\
&= \sqrt{0^{2} + (e^{3})^{2} + (-1)^{2}} \\
&= \sqrt{e^{6} + 1}
\end{aligned}
$$

---

**Question 3:** Find the limit $lim_(t → 0)‖f(t)‖$, if $f(t) = (t^{2} - 1)i + \frac{1 - \cos t}{t}j + \frac{\sin t}{t}k$.

- [ ] A. $2$
- [ ] B. $1$
- [ ] C. $\sqrt{2}$
- [ ] D. $0$
- [ ] E. $\sqrt{3}$

---

**Question 4:** Calculate $lim_(s → 0)‖f(s)‖$, if $f(s) = \begin{bmatrix}s^{2} + 6 \\ \frac{s}{s - 10} \\ e^{5s}\end{bmatrix}$.

- [ ] A. $1$
- [ ] B. $\sqrt{37}$
- [ ] C. $\sqrt{35}$
- [ ] D. $\sqrt{38}$
- [ ] E. $6$

---

<a id="finding-the-limit-of-a-sum-of-vector-functions"></a>
## Finding the Limit of a Sum of Vector Functions

**Example:** Find $\displaystyle \lim_{t \to 0} \big[\mathbf f(t) - 2 \mathbf g(t) \big]$ given that $\mathbf f(t)$ and $\mathbf g(t)$ are defined as:

$f(t) = \begin{bmatrix}\sin t \\ e^{t} \\ t\end{bmatrix}, g(t) = \begin{bmatrix}\cos t \\ 1 \\ 2t\end{bmatrix}$

**Explanation**

First, we find the limit of each function:

$$
lim_(t → 0)f(t) = \begin{bmatrix}lim_(t → 0)\sin t \\ lim_(t → 0)e^{t} \\ lim_(t → 0)t\end{bmatrix} = \begin{bmatrix}\sin 0 \\ e^{0} \\ 0\end{bmatrix} = \begin{bmatrix}0 \\ 1 \\ 0\end{bmatrix}; lim_(t → 0)g(t) = \begin{bmatrix}lim_(t → 0)\cos t \\ lim_(t → 0)1 \\ lim_(t → 0)2t\end{bmatrix} = \begin{bmatrix}\cos 0 \\ 1 \\ 2(0)\end{bmatrix} = \begin{bmatrix}1 \\ 1 \\ 0\end{bmatrix}
$$

Now, we use properties of limits to calculate the final result:

$$
\begin{aligned}
lim_(t → 0)[f(t) - 2g(t)] &= lim_(t → 0)f(t) - 2lim_(t → 0)g(t) \\
&= \begin{bmatrix}0 \\ 1 \\ 0\end{bmatrix} - 2\begin{bmatrix}1 \\ 1 \\ 0\end{bmatrix} \\
&= \begin{bmatrix}-2 \\ -1 \\ 0\end{bmatrix}
\end{aligned}
$$

---

**Question 5**

Find $lim_(t → 0)[3f(t) - g(t)]$ given that
$f(t)$ and $g(t)$ are defined as:

$f(t)|= ⟨\cos t, t^{2} - 2, \frac{t - 3}{t^{2} + 1}⟩; g(t)|= ⟨\sin t, \sqrt{t} + \sqrt{2}, 2⟩$

- [ ] A. $⟨3, 6, 11⟩$
- [ ] B. $⟨3,-6,-9⟩$
- [ ] C. $⟨ - 3, 6 + \sqrt{2}, 11⟩$
- [ ] D. $⟨3,-\sqrt{2},-11⟩$
- [ ] E. $⟨3,-6 - \sqrt{2},-11⟩$

---

**Question 6**

Find $lim_(t → 1)[5f(t) + 2g(t)]$ given that $f(t)$ and $g(t)$ are defined as:

$f(t)|= ⟨t^{2}, t^{3}, \frac{1}{t^{2}}⟩; g(t)|= ⟨5 - t^{3}, t^{2} - t + 2, 4⟩$

- [ ] A. $⟨13, 9, 13⟩$
- [ ] B. $⟨8, 4, 8⟩$
- [ ] C. $⟨9, 13, 9⟩$
- [ ] D. $⟨8, 8, 8⟩$
- [ ] E. $⟨5, 5, 5⟩$

---

<a id="limits-of-vector-products"></a>
## Limits of Vector Products

For two vector functions, the limit of a product is equal to the product of the limits:

$$
\begin{aligned}
lim_(t → a)[f(t) \cdot g(t)] &= lim_(t → a)f(t) \cdot lim_(t → a)g(t) \\
lim_(t → a)[f(t) \cdot g(t)] &= lim_(t → a)f(t) \cdot lim_(t → a)g(t)
\end{aligned}
$$

Here, $\mathbf f(t)\cdot \mathbf g(t)$ and $\mathbf f(t) \times \mathbf g(t)$ are the dot product and the cross product of $\mathbf f(t)$ and $\mathbf g(t)$, respectively.

---

<a id="finding-the-limit-of-a-product-of-vector-functions"></a>
## Finding the Limit of a Product of Vector Functions

**Example:** Find $\displaystyle \lim_{t \to 2} \left[\mathbf f(t) \cdot \mathbf g(t)\right]$ given that $\mathbf f(t)$ and $\mathbf g(t)$ are defined as follows:

$f(t)|= \ln ti + tj + t^{2}k; g(t)|= (2 - t)i + \frac{6}{t}j - 4k$

**Explanation**

First, we find the limit of each function:

$$
\begin{aligned}
lim_(t → 2)f(t) &= (lim_(t → 2)\ln t)i + (lim_(t → 2)t)j + (lim_(t → 2)t^{2})k \\
&= (\ln 2)i + 2j + 2^{2}k \\
&= \ln 2i + 2j + 4k, \\
lim_(t → 2)g(t) &= (lim_(t → 2)(2 - t))i + (lim_(t → 2)\frac{6}{t})j + (lim_(t → 2)(-4))k \\
&= (2 - 2)i + \frac{6}{2}j + (-4)k \\
&= 0i + 3j - 4k \\
&= 3j - 4k
\end{aligned}
$$

Now, we use properties of limits to calculate the final result:

$$
\begin{aligned}
lim_(t → 2)[f(t) \cdot g(t)] &= lim_(t → 2)f(t) \cdot lim_(t → 2)g(t) \\
&= (\ln 2i + 2j + 4k) \cdot (3j - 4k) \\
&= \ln 2 \cdot 0 + 2 \cdot 3 + 4 \cdot (-4) \\
&=-10
\end{aligned}
$$

---

**Question 7:** Find $lim_(t → 1)[f(t) \cdot g(t)]$ given that $f(t)$ and $g(t)$ are defined as:
$f(t)|= ⟨\sin πt, \frac{1}{t}, e^{t}⟩; g(t)|= ⟨(\tan (t - 1))/(t - 1), \frac{t^{2}}{2 - t}, \sqrt{t}⟩$

- [ ] A. $⟨e - 1, 0, 1⟩$
- [ ] B. $⟨1 - e, e,-1⟩$
- [ ] C. $⟨1, e, 1⟩$
- [ ] D. $⟨1, 0, 1⟩$
- [ ] E. $⟨\frac{1}{e}, e, 0⟩$

---

**Question 8:** Find the limit $lim_(t → π)[f(t) \cdot g(t)]$ given that $f(t)$ and $g(t)$ are defined as:
$f(t) = \begin{bmatrix}\sin t \\ 1 + \cos t \\ \frac{4t}{π}\end{bmatrix}, g(t) = \begin{bmatrix}\cos t \\ 2 + \tan t \\ \cos t\end{bmatrix}$

- [ ] A. $-6$
- [ ] B. $-12$
- [ ] C. $-5$
- [ ] D. $-4$
- [ ] E. $-9$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
