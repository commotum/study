# Principal Normal Vectors

<!--
lesson-id: 1795
topic-code: MVC.1.1.9
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Principal Normal at a Given Point](#calculating-the-principal-normal-at-a-given-point)
- [Calculating the Principal Normal at an Arbitrary Point](#calculating-the-principal-normal-at-an-arbitrary-point)
- [Deriving the Formula for the Principal Normal](#deriving-the-formula-for-the-principal-normal)
- [Calculating a Principal Normal Given Some Information About a Curve](#calculating-a-principal-normal-given-some-information-about-a-curve)
- [Lines Do Not Have Principal Normal Vectors](#lines-do-not-have-principal-normal-vectors)

## Prerequisites

- [Unit Tangent Vectors](<1.1.8. Unit Tangent Vectors.md>)

---

<a id="introduction"></a>
## Introduction

The **principal normal vector** to a curve $C$ at a point $P$ is a unit vector that

- is perpendicular to the unit tangent vector, and
- points in the direction that the curve is bending.

In general, if the unit tangent vector is given by $\mathbf T(t)$, then the principal normal vector is given by

$$
\mathbf N(t)= \dfrac{\mathbf T'(t)}{\|\mathbf T'(t)\|}
$$

where it is assumed that

$$
\|\mathbf T'(t)\|\neq 0
$$

To demonstrate, suppose that the curve $C$ is parametrized by the vector function

$$
\mathbf f(t) = \left\langle 2\cos t, \: 2\sin t \right\rangle, \qquad t \in [0, \pi]
$$

We will compute the principal normal vector at

$$
t=\dfrac{\pi}{4}
$$

First, using the formula for the unit tangent,

$$
\mathbf T(t) = \dfrac{\mathbf f'(t)}{\mid \mid \mathbf f'(t) \mid \mid}
$$

we find that the general unit tangent vector is

$$
\mathbf T(t)= \left\langle {-\sin t}, \cos t\right\rangle
$$

Now, when

$$
t=\dfrac{\pi}{4}
$$

we have the point $P(\sqrt{2},\sqrt{2})$. The unit tangent vector

$$
\mathbf{T}\left(\dfrac{\pi}{4}\right)
$$

and the principal normal vector

$$
\mathbf{N}\left(\dfrac{\pi}{4}\right)
$$

at this point are shown below:

![](<../Source/1.1.9. Principal Normal Vectors/Images/63520e4af319020827604026c4274aaf.png>)

To calculate the principal normal vector, we start by calculating the derivative of the unit tangent function. This gives

$$
\begin{aligned}
T(t) &= ⟨- \sin t, \cos t⟩ \\
T^{′}(t) &= ⟨ - \cos t,-\sin t⟩
\end{aligned}
$$

Therefore, the general principal normal vector is

$$
\begin{aligned}
N(t) &= (T^{′}(t))/(\mid \mid T^{′}(t) \mid \mid) \\
&= (⟨ - \cos t,-\sin t⟩)/(\sqrt{(-\cos t)^{2} + (\sin t)^{2}}) \\
&= \frac{⟨ - \cos t,-\sin t⟩}{\sqrt{\cos^{2} t + \sin^{2} t}} \\
&= \frac{⟨ - \cos t,-\sin t⟩}{\sqrt{1}} \\
&= ⟨ - \cos t,-\sin t⟩
\end{aligned}
$$

We can now find

$$
\mathbf N\left(\dfrac\pi 4\right)
$$

as follows:

$$
\begin{aligned}
N(\frac{π}{4}) &= ⟨ - \cos (\frac{π}{4}),-\sin (\frac{π}{4})⟩ \\
&= ⟨ - \frac{\sqrt{2}}{2},-\frac{\sqrt{2}}{2}⟩
\end{aligned}
$$

Finally, it's easy to check that $\mathbf T'(t)$ and $\mathbf N'(t)$ are perpendicular for all $t$ by computing the dot product and showing that we get zero:

$$
\begin{aligned}
T(t) \cdot N(t) &= ⟨ - \sin t, \cos t⟩ \cdot ⟨ - \cos t,-\sin t⟩ \\
&= \sin t\cos t - \sin t\cos t \\
&= 0✓
\end{aligned}
$$

---

<a id="calculating-the-principal-normal-at-a-given-point"></a>
## Calculating the Principal Normal at a Given Point

**Example:** The unit tangent vector to a curve at an arbitrary point is given by
$\mathbf T(t) =\sin{t} \,\mathbf i -\cos{t}\,\mathbf j$.
Find the principal normal vector at the point where $t=\dfrac{\pi}{4}$.

**Explanation**

Remember that in general, the principal normal vector is given by

$$
\mathbf N(t)= \dfrac{\mathbf T'(t)}{\|\mathbf T'(t)\|}
$$

First, we calculate $\mathbf T'(t)$ and evaluate it at

$$
t=\dfrac{\pi}{4}
$$

$$
\begin{aligned}
T^{′}(t) &= \frac{d}{dt}(\sin ti - \cos tj) \\
&= \frac{d}{dt}(\sin t)i - \frac{d}{dt}(\cos t)j \\
&= \cos ti - (-\sin t)j \\
&= \cos ti + \sin tj \\
T^{′}(\frac{π}{4}) &= \cos (\frac{π}{4})i + \sin (\frac{π}{4})j \\
&= \frac{\sqrt{2}}{2}i + \frac{\sqrt{2}}{2}j \\
&= \frac{\sqrt{2}}{2}(i + j)
\end{aligned}
$$

Next, we find the magnitude of

$$
\mathbf T'\left(\dfrac{\pi}{4}\right)
$$

$$
\begin{aligned}
‖T^{′}(\frac{π}{4})‖ &= \frac{\sqrt{2}}{2}‖i + j‖ \\
&= \frac{\sqrt{2}}{2}\sqrt{(1)^{2} + (1)^{2}} \\
&= \frac{\sqrt{2}}{2}\sqrt{2} \\
&= 1
\end{aligned}
$$

Finally, we normalize

$$
\mathbf T'\left(\dfrac{\pi}{4}\right)
$$

and obtain the principal normal vector to the curve at the point where

$$
t=\dfrac{\pi}{4}
$$

$$
\begin{aligned}
N(\frac{π}{4}) &= (T^{′}(\frac{π}{4}))/(‖T^{′}(\frac{π}{4})‖) = (\frac{\sqrt{2}}{2}(i + j))/(1) = \frac{\sqrt{2}}{2}i + \frac{\sqrt{2}}{2}j
\end{aligned}
$$

To check our result, we can verify that $\mathbf T$ and $\mathbf N$ are perpendicular by computing their dot product:

$$
\begin{aligned}
T(\frac{π}{4}) \cdot N(\frac{π}{4}) &= (\frac{\sqrt{2}}{2}i - \frac{\sqrt{2}}{2}j) \cdot (\frac{\sqrt{2}}{2}i + \frac{\sqrt{2}}{2}j) \\
&= \frac{1}{2} - \frac{1}{2} \\
&= 0✓
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-33493
content: |-
  The unit tangent vector to a curve at an arbitrary point is given by

  $T(t) = \frac{1}{t^{2} + 2}⟨ - 2, 2t, t^{2}⟩$.
  Find the principal normal vector at the point where $t = 2$.
options:
- id: a
  content: |-
    $⟨ - \frac{2}{3}, \frac{2}{3},-\frac{1}{3}⟩$
- id: b
  content: |-
    $⟨\frac{2}{3},-\frac{2}{3}, \frac{1}{3}⟩$
- id: c
  content: |-
    $⟨\frac{2}{3}, \frac{1}{3}, \frac{2}{3}⟩$
- id: d
  content: |-
    $⟨\frac{2}{3},-\frac{1}{3}, \frac{2}{3}⟩$
  correct: true
- id: e
  content: |-
    $⟨\frac{2}{3},-\frac{1}{3},-\frac{2}{3}⟩$
```

---

**Question 2:**

```quiz
type: radio
id: ma-33233
content: |-
  The equations of a curve $r(t)$ and its corresponding unit tangent vector $T(t)$ at an arbitrary point are given below. Find the principal normal vector to that curve at the point $P(0, 3, 4)$.

  $[MATH: r(t)|= 5\cos ti + 3\sin tj + 4\sin tk, t ∈ [0, 2π); T(t)|=-\sin ti + \frac{3}{5}\cos tj + \frac{4}{5}\cos tk, t ∈ [0, 2π)]$
options:
- id: a
  content: |-
    $\frac{4}{5}j - \frac{3}{5}k$
- id: b
  content: |-
    $\frac{3}{5}i - \frac{4}{5}k$
- id: c
  content: |-
    $\frac{3}{5}i + \frac{4}{5}j$
- id: d
  content: |-
    $-\frac{3}{5}j - \frac{4}{5}k$
  correct: true
- id: e
  content: |-
    $-\frac{4}{5}i - \frac{3}{5}j$
```

---

<a id="calculating-the-principal-normal-at-an-arbitrary-point"></a>
## Calculating the Principal Normal at an Arbitrary Point

**Example:** Given the curve $\mathbf r(t)=\left\langle \sqrt 3 t, \: \cos t, \: \sin{t}\right\rangle$, find the principal normal vector $\mathbf N(t)$ at an arbitrary point on the curve.

**Explanation**

Remember that in general, the principal normal vector is given by

$$
\mathbf N(t)= \dfrac{\mathbf T'(t)}{\|\mathbf T'(t)\|}
$$

First, we find the unit tangent vector $\mathbf T(t)$. To do this, we calculate $\mathbf r'(t)$ and its magnitude as follows:

$$
\begin{aligned}
r^{′}(t) &= ⟨\frac{d}{dt}(\sqrt{3}t), \frac{d}{dt}(\cos t), \frac{d}{dt}(\sin t)⟩ \\
&= ⟨\sqrt{3},-\sin t, \cos t⟩ \\
‖r^{′}(t)‖ &= \sqrt{(\sqrt{3})^{2} + (-\sin t)^{2} + (\cos t)^{2}} \\
&= \sqrt{3 + \sin^{2} t + \cos^{2} t} \\
&= \sqrt{4} \\
&= 2
\end{aligned}
$$

Therefore, the unit tangent vector is

$$
\begin{aligned}
T(t) &= (r^{′}(t))/(‖r^{′}(t)‖) \\
&= \frac{1}{2}⟨\sqrt{3},-\sin t, \cos t⟩ \\
&= ⟨\frac{\sqrt{3}}{2},-\frac{\sin t}{2}, \frac{\cos t}{2}⟩
\end{aligned}
$$

Next, we find the principal normal $\mathbf{N}(t)$. To do this, we calculate $\mathbf T'(t)$ and its magnitude as follows:

$$
\begin{aligned}
T^{′}(t) &= ⟨\frac{d}{dt}(\frac{\sqrt{3}}{2}), \frac{d}{dt}(-\frac{\sin t}{2}), \frac{d}{dt}(\frac{\cos t}{2})⟩ \\
&= ⟨0,-\frac{1}{2}\cos t,-\frac{1}{2}\sin t⟩ \\
&= \frac{1}{2}⟨0,-\cos t,-\sin t⟩ \\
‖T^{′}(t)‖ &= \frac{1}{2}\sqrt{0^{2} + (-\cos t)^{2} + (-\sin t)^{2}} \\
&= \frac{1}{2}\sqrt{\cos^{2} t + \sin^{2} t} \\
&= \frac{1}{2}
\end{aligned}
$$

Finally, we normalize $\mathbf T'(t)$ and obtain the principal normal to the curve:

$$
\begin{aligned}
N(t) &= (T^{′}(t))/(‖T^{′}(t)‖) = (\frac{1}{2}⟨0,-\cos t,-\sin t⟩)/((\frac{1}{2})) = ⟨0,-\cos t,-\sin t⟩
\end{aligned}
$$

To check our result, we can verify that $\mathbf T$ and $\mathbf N$ are perpendicular by computing their dot product:

$$
\begin{aligned}
T(t) \cdot N(t) &= ⟨\frac{\sqrt{3}}{2},-\frac{\sin t}{2}, \frac{\cos t}{2}⟩ \cdot ⟨0,-\cos t,-\sin t⟩ \\
&= \frac{\sqrt{3}}{2} \cdot 0 + \frac{\sin t\cos t}{2} - \frac{\cos t\sin t}{2} \\
&= 0✓
\end{aligned}
$$

---

**Question 3**

$T(t) = \frac{1}{\sqrt{3}}((\cos t - \sin t)i - (\cos t + \sin t)j - k)$.
The unit tangent vector to a curve at an arbitrary point is given above. Find the principal normal vector to this curve at an arbitrary point on the curve.$N(t) =$
$($
$\underline{\hspace{1.5cm}}$
$)$
$i +$
$($
$\underline{\hspace{1.5cm}}$
$)$
$j$

---

**Question 4**

Given the curve $r(t) = ⟨3\cos t, 3\sin t, 4t⟩$, find the principal normal vector $N(t)$ at an arbitrary point on the curve.$N(t) =$
$⟨$
$\underline{\hspace{1.5cm}}$
$$,
$\underline{\hspace{1.5cm}}$
$$,
$\underline{\hspace{1.5cm}}$
$⟩$

---

<a id="deriving-the-formula-for-the-principal-normal"></a>
## Deriving the Formula for the Principal Normal

Let $\mathbf T(t)$ be the unit tangent vector to some differentiable vector-valued function. Since

$$
\mid \mid \mathbf T(t) \mid \mid = 1
$$

and $\mathbf T(t)$ is obviously parallel to itself, we must have

$$
\mathbf T(t) \cdot \mathbf T(t) = 1
$$

Differentiating the above relationship with respect to $t$, we get

$$
\mathbf T'(t) \cdot \mathbf T(t) + \mathbf T(t) \cdot \mathbf T'(t) = 0
$$

which can be simplified as follows:

$$
\begin{aligned}
T^{′}(t) \cdot T(t) + T^{′}(t) \cdot T(t) &= 0 \\
2T^{′}(t) \cdot T(t) &= 0 \\
T^{′}(t) \cdot T(t) &= 0
\end{aligned}
$$

Therefore, provided that

$$
\mathbf T'(t)\neq \mathbf 0
$$

then it must be the case that $\mathbf T'(t)$ is perpendicular to $\mathbf T(t)$.

The vector $\mathbf T'(t)$ measures the rate of change of $\mathbf T(t)$. However, since $\mathbf T(t)$ is a unit vector for all $t$, it changes only in its direction. The vector $\mathbf T'(t)$ measures this rate of change in direction.

To form the principal normal $\mathbf N(t)$, we normalize $\mathbf T'(t)$, which leads to the definition

$$
\mathbf N(t) = \dfrac{\mathbf T'(t)}{\mid \mid \mathbf T'(t) \mid \mid}
$$

---

<a id="calculating-a-principal-normal-given-some-information-about-a-curve"></a>
## Calculating a Principal Normal Given Some Information About a Curve

**Example:** The unit tangent vector to a curve at an arbitrary point is given by
$\mathbf T(t) = \sin{t} \cdot \mathbf q(t)$.
Find the principal normal vector at the point where $t = \dfrac{\pi}{4}$, if
$q(\frac{π}{4}) =-\frac{\sqrt{2}}{2}i - \frac{\sqrt{2}}{2}j + k, q^{′}(\frac{π}{4}) =-2k$.

**Explanation**

Remember that in general, the principal normal vector is given by

$$
\mathbf N(t)= \dfrac{\mathbf T'(t)}{\|\mathbf T'(t)\|}
$$

First, we calculate $\mathbf T'(t)$ and evaluate it at

$$
t=\dfrac{\pi}{4}
$$

$$
\begin{aligned}
T^{′}(t) &= \frac{d}{dt}(\sin t \cdot q(t)) \\
&= \frac{d}{dt}(\sin t) \cdot q(t) + \sin t \cdot \frac{d}{dt}(q(t)) \\
&= \cos t \cdot q(t) + \sin t \cdot q^{′}(t) \\
T^{′}(\frac{π}{4}) &= \cos (\frac{π}{4}) \cdot q(\frac{π}{4}) + \sin (\frac{π}{4}) \cdot q^{′}(\frac{π}{4}) \\
&= \frac{\sqrt{2}}{2}(-\frac{\sqrt{2}}{2}i - \frac{\sqrt{2}}{2}j + k) + \frac{\sqrt{2}}{2}(-2k) \\
&=-\frac{1}{2}i - \frac{1}{2}j - \frac{\sqrt{2}}{2}k
\end{aligned}
$$

Next, we find the magnitude of

$$
\mathbf T'\left(\dfrac{\pi}{4}\right)
$$

$$
\begin{aligned}
‖T^{′}(\frac{π}{4})‖ &= \sqrt{(-\frac{1}{2})^{2} + (-\frac{1}{2})^{2} + (-\frac{\sqrt{2}}{2})^{2}} \\
&= \sqrt{\frac{1}{4} + \frac{1}{4} + \frac{1}{2}} \\
&= 1
\end{aligned}
$$

Finally, we normalize

$$
\mathbf T'\left(\dfrac{\pi}{4}\right)
$$

and obtain the principal normal to the curve at the point where

$$
t=\dfrac{\pi}{4}
$$

$$
\begin{aligned}
N(\frac{π}{4}) &= (T^{′}(\frac{π}{4}))/(‖T^{′}(\frac{π}{4})‖) \\
&= \frac{-\frac{1}{2}i - \frac{1}{2}j - \frac{\sqrt{2}}{2}k}{1} \\
&=-\frac{1}{2}i - \frac{1}{2}j - \frac{\sqrt{2}}{2}k
\end{aligned}
$$

---

**Question 5**

```quiz
type: radio
id: ma-74864
content: |-
  The unit tangent vector to a curve at an arbitrary point is given by
  $T(t) = \sin t \cdot q(t)$.

  Find the principal normal vector at the point where $t = \frac{π}{2}$, if
  $q(\frac{π}{2}) = i, q^{′}(\frac{π}{2}) =-j$.
options:
- id: a
  content: |-
    $\frac{\sqrt{5}}{5}i - \frac{2\sqrt{5}}{5}j$
- id: b
  content: |-
    $\frac{\sqrt{2}}{2}i + \frac{\sqrt{2}}{2}j$
- id: c
  content: |-
    $i + j$
- id: d
  content: |-
    $-\frac{\sqrt{2}}{2}i + \frac{\sqrt{2}}{2}j$
- id: e
  content: |-
    $-j$
  correct: true
```

---

**Question 6:**

```quiz
type: radio
id: ma-75492
content: |-
  The unit tangent vector to a curve at an arbitrary point is given by

  $T(t) = \frac{1}{\sqrt{t^{2} + 1}} \cdot q(t)$.
  Find the principal normal vector at the point where $t = 3$, if
  $q(3) = ⟨3, 1⟩, q^{′}(3) = ⟨1, 0⟩$.
options:
- id: a
  content: |-
    $⟨1, 0⟩$
- id: b
  content: |-
    $\frac{\sqrt{5}}{5}⟨2, 1⟩$
- id: c
  content: |-
    $\frac{\sqrt{2}}{2}⟨ - 1, 1⟩$
- id: d
  content: |-
    $\frac{\sqrt{10}}{10}⟨1,-3⟩$
  correct: true
- id: e
  content: |-
    $⟨0,-1⟩$
```

---

<a id="lines-do-not-have-principal-normal-vectors"></a>
## Lines Do Not Have Principal Normal Vectors

Consider the straight line given by

$$
\begin{aligned}
r(t) &= \begin{bmatrix}-2 \\ 1 \\ 0\end{bmatrix} + t\begin{bmatrix}5 \\ 0 \\ \sqrt{2}\end{bmatrix}, t ∈ (- ∞, ∞)
\end{aligned}
$$

If we attempt to calculate its principal normal vector, we will get an interesting result. First, we calculate $\mathbf r'(t)$ and $\|\mathbf r'(t)\|$:

$$
\begin{aligned}
r^{′}(t) &= \frac{d}{dt}\begin{bmatrix}-2 + 5t \\ 1 \\ \sqrt{2}t\end{bmatrix} = \begin{bmatrix}5 \\ 0 \\ \sqrt{2}\end{bmatrix} \\
‖r^{′}(t)‖ &= \sqrt{5^{2} + (\sqrt{2})^{2}} = \sqrt{27} = 3\sqrt{3}
\end{aligned}
$$

Therefore, the unit tangent vector is

$$
\begin{aligned}
T(t) &= (r^{′}(t))/(‖r^{′}(t)‖) = \frac{1}{3\sqrt{3}}\begin{bmatrix}5 \\ 0 \\ \sqrt{2}\end{bmatrix}
\end{aligned}
$$

Notice that in the case of a straight line, $\mathbf T(t)$ is simply the (normalized) direction vector, as we would expect.

Next, we calculate $\mathbf T'(t)$:

$$
\begin{aligned}
T^{′}(t) &= \frac{1}{3\sqrt{3}}\begin{bmatrix}\frac{d}{dt}(5) \\ \frac{d}{dt}(0) \\ \frac{d}{dt}(\sqrt{2})\end{bmatrix} = 0
\end{aligned}
$$

Since $\mathbf{T}'(t)$ is the zero vector, we can't normalize it as usual. If we try to apply the usual formula we get an undefined result due to division by zero:

$$
\begin{aligned}
N(t) &= (T^{′}(t))/(‖T^{′}(t)‖) = \frac{0}{‖0‖} = \frac{0}{0} = undefined
\end{aligned}
$$

Therefore, a straight line does not have a principal normal.

The intuition behind this result is that the principal normal points in the direction in which a curve is bending, but a straight line does not bend at all.

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
