# Continuity and Differentiability of Vector-Valued Functions

<!--
lesson-id: 1739
topic-code: MVC.1.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining the Continuity of a Vector Function](#determining-the-continuity-of-a-vector-function)
- [Finding the Value of a Vector Function Using Properties of Continuous Functions](#finding-the-value-of-a-vector-function-using-properties-of-continuous-functions)
- [Differentiability of Vector Functions](#differentiability-of-vector-functions)
- [Determining the Differentiability of a Vector Function](#determining-the-differentiability-of-a-vector-function)
- [Finding the Interval on Which a Product of Functions is Differentiable](#finding-the-interval-on-which-a-product-of-functions-is-differentiable)

## Prerequisites

- [Defining Continuity at a Point](<../../../../CAB/1. Limits and Continuity/1.6. Continuity/Lessons/1.6.2. Defining Continuity at a Point.md>)
- [Limits of Vector-Valued Functions](<1.1.2. Limits of Vector-Valued Functions.md>)
- [Differentiating Vector-Valued Functions](<../../../../CBC/11. Parametric Equations/11.2. Vector-Valued Functions/Lessons/11.2.2. Differentiating Vector-Valued Functions.md>)

---

<a id="introduction"></a>
## Introduction

Determining the continuity of a vector function is very similar to determining the continuity of a scalar function. A vector function $\mathbf f (t)$ is **continuous** at a point $t_0$ (in its domain) if

$$
\displaystyle \lim_{t \rightarrow t_0} \mathbf f (t) = \mathbf f (t_0)
$$

For example, the vector function

$$
\mathbf f(t) = \left \langle e^t, \dfrac{1}{t} \right \rangle
$$

is continuous at $t=1$ because

$$
\begin{aligned}
lim_(t → 1)f(t) &= ⟨lim_(t → 1)(e^{t}), lim_(t → 1)(\frac{1}{t})⟩ \\
&= ⟨e^{1}, \frac{1}{1}⟩ \\
&= ⟨e, 1⟩ \\
&= f(1)
\end{aligned}
$$

Since the limit of a vector function is determined by the limits of the components, a vector function is continuous at a point $t_0$ *if and only if* each of its components is continuous at that point.

We say that a vector function is **continuous on the interval** $t\in[a,b]$ if it is continuous at every point of this interval.

---

<a id="determining-the-continuity-of-a-vector-function"></a>
## Determining the Continuity of a Vector Function

**Example:** Is the vector function $\mathbf f(t) = -\sin t \,\mathbf i + (t^2 + 1)\,\mathbf j + \tan t\,\mathbf k$ continuous at $t = 0$?

**Explanation**

Let's compute the limit of $\mathbf f (t)$ as $t$ approaches $0$:

$$
\begin{aligned}
lim_(t → 0)f(t) &= ⟨lim_(t → 0)(-\sin t), lim_(t → 0)(t^{2} + 1), lim_(t → 0)\tan t⟩ \\
&= ⟨ - \sin 0, 0^{2} + 1, \tan 0⟩ \\
&= ⟨0, 1, 0⟩ \\
&= f(0)
\end{aligned}
$$

So

$$
\displaystyle \lim_{t \rightarrow t_0} \mathbf f (t) = \mathbf f (t_0)
$$

and therefore $\mathbf f (t)$ is continuous at $t=0$.

---

**Question 1:**

```quiz
type: radio
id: ma-32292
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following vector-valued functions are continuous at $t = 0$?

  $f(t)|= \cot ti + 3t^{3}j + (2t + 1)k; g(t)|= \frac{2}{t}i + t^{3}j + \sin tk; h(t)|= 2ti + \cos tj + t^{4}k$
options:
- id: a
  content: |-
    $f(t)$ only
- id: b
  content: |-
    $g(t)$ and $h(t)$ only
- id: c
  content: |-
    All the functions are continuous at $t = 0$
- id: d
  content: |-
    $f(t)$ and $h(t)$ only
- id: e
  content: |-
    $h(t)$ only
```

---

**Question 2**

```quiz
type: radio
id: ma-32559
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following vector-valued functions are continuous on $[- π, π]$?

  $p(s)|= 2si + \frac{s}{\sin s}j - 2s^{3}k; q(s)|= 3\cos si + \sin (2s)j + \frac{1}{s}k; r(s)|= \cot (s + \frac{π}{3})i - \sin (2s)j + \cos (2s)k$
options:
- id: a
  content: |-
    $p(s)$ only
- id: b
  content: |-
    None of the functions are continuous on $[- π, π]$
- id: c
  content: |-
    All the functions are continuous on $[- π, π]$
- id: d
  content: |-
    $q(s)$ only
- id: e
  content: |-
    $q(s)$ and $r(s)$ only
```

---

<a id="finding-the-value-of-a-vector-function-using-properties-of-continuous-functions"></a>
## Finding the Value of a Vector Function Using Properties of Continuous Functions

**Example:** Find $\mathbf g(1)$ given that $\mathbf f(t)$ and $\mathbf g(t)$ are two continuous vector-valued functions for which $\mathbf f(1) = \left\langle 1, 1, -1 \right\rangle$ and
$\lim_\limits{t\rightarrow 1} \big[2\mathbf f(t)-\mathbf g(t) \big] = \left\langle 2, 1, -1 \right\rangle$.

**Explanation**

Using the constant multiple rule and the addition rule for limits, and the fact that both functions are continuous, we get

$$
\begin{aligned}
lim_(t → 1)[2f(t) - g(t)] &= lim_(t → 1)2f(t) + lim_(t → 1)[- g(t)] \\
&= 2lim_(t → 1)f(t) - lim_(t → 1)g(t) \\
&= 2f(1) - g(1) \\
&= 2⟨1, 1,-1⟩_(⏟)_(f(1)) - g(1) \\
&= ⟨2, 2,-2⟩ - g(1)
\end{aligned}
$$

On the other hand, we are told that

$$
\lim_\limits{t\rightarrow 1} \big[ 2\mathbf f(t)-\mathbf g(t) \big] = \left\langle 2, 1, -1 \right\rangle
$$

Therefore, we obtain the following equation which we can solve for $\mathbf{g} (1)$:

$$
\begin{aligned}
lim_(t → 1)[2f(t) - g(t)] &= ⟨2, 1,-1⟩ \\
⟨2, 2,-2⟩ - g(1) &= ⟨2, 1,-1⟩ \\
g(1) &= ⟨2, 2,-2⟩ - ⟨2, 1,-1⟩ \\
g(1) &= ⟨0, 1,-1⟩
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-35104
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find $g(2)$ given that $f(t)$ and $g(t)$ are two continuous vector-valued functions for which $f(2) = ⟨1, 1, 2⟩$ and

  $lim_(t → 2)[f(t) + 2g(t)] = ⟨ - 1, 7, 0⟩$.
options:
- id: a
  content: |-
    $⟨0, 4, 2⟩$
- id: b
  content: |-
    $⟨ - 1, 3,-1⟩$
- id: c
  content: |-
    $⟨ - 2, 6,-2⟩$
- id: d
  content: |-
    $⟨\frac{1}{2}, \frac{1}{2}, 1⟩$
- id: e
  content: |-
    $⟨0, 8, 4⟩$
```

---

**Question 4:**

```quiz
type: radio
id: ma-35085
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider a vector-valued function $f(u)$ that is continuous over all real numbers and the real-valued function $u(t) = \sqrt{9 - t^{2}}$. Then,

  $lim_(t → 0)f(u(t)) =$
options:
- id: a
  content: |-
    $3f(3)$
- id: b
  content: |-
    $f(3)$
- id: c
  content: |-
    $f(0)$
- id: d
  content: |-
    $3f(0)$
- id: e
  content: |-
    $f(9)$
```

---

<a id="differentiability-of-vector-functions"></a>
## Differentiability of Vector Functions

Similar to scalar-valued functions, the vector function $\mathbf{f}(t)$ is **differentiable** at a point $t = t_0$ in the domain of the function if the following limit exists:

$$
\lim_{h \to 0} \dfrac{\mathbf{f}(t_0+h) - \mathbf{f}(t_0)}{h}
$$

The value of the limit is then called the **derivative** of $\mathbf{f}(t)$ at $t = t_0$ and is denoted as $\mathbf{f}'(t_0)$. In general, a vector function is differentiable *if and only if* all of its components are differentiable.

For example, is the following vector function $\mathbf{g}(t)$ differentiable at $t=0$?

$$
\mathbf{g}(t) = \big\langle t, \: \mid \,t\, \mid, \: \sin t \big\rangle
$$

Notice that the second component, $\mid \,t\, \mid$, is not differentiable at $t=0$. Since one of the component functions is not differentiable at $t=0$, we conclude that the overall vector function $\mathbf{g}(t)$ is not differentiable at $t=0$.

We say that a vector function is **differentiable on an open interval** $t \in (a,b)$ if it is differentiable at *every* point $t \in (a,b)$. This means that each component of the function must be differentiable at every point of $(a,b)$.

Our function $\mathbf{g}(t)$, mentioned above, is differentiable on $(-\infty, 0) \cup (0, \infty)$ since all of its components are differentiable there.

Finally, a function is said to be **continuously differentiable** if it is differentiable and its derivative is continuous.

---

<a id="determining-the-differentiability-of-a-vector-function"></a>
## Determining the Differentiability of a Vector Function

**Example:** Is the vector function $\mathbf{f}(t) = \left\langle 2e^t, \: \dfrac{\pi}{t-3}, \: \sqrt{t} \right\rangle$ differentiable with respect to $t$ on $t \in [0, 2]$?

**Explanation**

The function is defined on $t \in [0, 2]$. Remember that a function is differentiable on the interval $[0, 2]$ if and only if all of its component functions are differentiable on that interval.

Let's now differentiate $\mathbf f(t)$ to find $\mathbf f'(t)$:

$$
\begin{aligned}
f^{′}(t) &= ⟨\frac{d}{dt}(2e^{t}), \frac{d}{dt}(\frac{π}{t - 3}), \frac{d}{dt}(\sqrt{t})⟩ \\
&= ⟨2e^{t}, - (π)/((t - 3)^{2}), \frac{1}{2\sqrt{t}}⟩
\end{aligned}
$$

Notice that the third component of this vector,

$$
\dfrac{1}{2\sqrt{t}}
$$

is undefined at $t = 0$. Therefore, we conclude that the function $\mathbf f(t)$ is **not** differentiable on $[0,2]$.

---

**Question 5**

```quiz
type: radio
id: ma-32568
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following vector-valued functions are differentiable with respect to $t$ on $t ∈ (0, π)$?

  $f(t)|= ⟨\sin (2t), \cos t, 2 - \cos (2t)⟩; g(t)|= ⟨1 + t^{2},-t + 2, \sqrt{t}⟩; h(t)|= ⟨\frac{1}{t}, \tan t, \cos t⟩$
options:
- id: a
  content: |-
    $f(t)$ and $g(t)$ only
- id: b
  content: |-
    $f(t)$ and $h(t)$ only
- id: c
  content: |-
    All of them are differentiable on $(0, π)$
- id: d
  content: |-
    None of them are differentiable on $(0, π)$
- id: e
  content: |-
    $g(t)$ and $h(t)$ only
```

---

**Question 6:**

```quiz
type: radio
id: ma-32698
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following vector-valued functions are differentiable with respect to $t$ on $t ∈ (0, 1)$?

  $p(t)|= ⟨2t - 1, t^{2} + 4,-e^{t}⟩; q(t)|= ⟨(t + 1)^{4}, \sqrt[3]{t}, \ln t⟩; r(t)|= ⟨3t + 2, \arctan t, t^{2} - 2⟩$
options:
- id: a
  content: |-
    All the functions are differentiable on $t ∈ (0, 1)$
- id: b
  content: |-
    $p(t)$ and $r(t)$ only
- id: c
  content: |-
    $q(t)$ and $r(t)$ only
- id: d
  content: |-
    None of the functions are differentiable on $t ∈ (0, 1)$
- id: e
  content: |-
    $p(t)$ and $q(t)$ only
```

---

<a id="finding-the-interval-on-which-a-product-of-functions-is-differentiable"></a>
## Finding the Interval on Which a Product of Functions is Differentiable

**Example:** Find the interval where the function $\mathbf g(t)\cdot\mathbf h(t)$ is differentiable, given the following functions $\mathbf g(t)$ and $\mathbf h(t)$:
$g(t)|= 3\sin ti - \cos (2t)j - \sqrt{t}k; h(t)|= 2ti + t^{2}j - 3\cos tk$

**Explanation**

The function

$$
\mathbf F(t) = \mathbf g(t)\cdot\mathbf h(t)
$$

is differentiable over the common domain where both $\mathbf g(t)$ and $\mathbf h(t)$ are differentiable. Remember that a vector function is differentiable if and only if each component is differentiable.

So, let's start by finding the intervals where each of the functions $\mathbf g(t)$ and $\mathbf h(t)$ are differentiable.

For the function $\mathbf g(t)$:

- The first component, $3\sin t$, and the second component, $-\cos(2t)$, are differentiable over $(-\infty, \infty)$.

- The third component $-\sqrt t$ is differentiable on $(0, \infty)$. Its derivative is not defined at $t=0$ and the function is not defined for $t <0$.

The intersection of the intervals of differentiability of all three components of $\mathbf g(t)$ is $(0,\infty)$. So, $\mathbf g(t)$ is differentiable on $(0,\infty)$.

For the function $\mathbf h(t)$:

- All three component functions are defined and are differentiable for all values of $t \in (-\infty, \infty)$. So, $\mathbf h(t)$ is differentiable on $(-\infty, \infty)$.

Finally, the common domain of differentiability for $\mathbf g(t)$ and $\mathbf h(t)$ is

$$
(0, \infty) \cap (-\infty, \infty) = (0,\infty)
$$

---

**Question 7:**

```quiz
type: radio
id: ma-35117
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the interval where the function $f(t) \cdot g(t)$ is differentiable, given that:

  $f(t)|= \mid t - 1 \mid i + \cos tj + \sqrt{1 - t}k; g(t)|= t^{2}i + \sqrt{t^{2} - 9}j + \ln (3 - t)k$
options:
- id: a
  content: |-
    $(-1,-3)$
- id: b
  content: |-
    $(- ∞,-3)$
- id: c
  content: |-
    $(-3, 3)$
- id: d
  content: |-
    $(- ∞, 3)$
- id: e
  content: |-
    $(3, ∞)$
```

---

**Question 8:**

```quiz
type: radio
id: ma-35086
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the interval where the function $f(t) = u(t)g(t)$ is differentiable, given that

  $u(t)|= \ln (t - 1); g(t)|= \sqrt{t}i + \sin^{2} tj + \sqrt{1 + t}k$
options:
- id: a
  content: |-
    $(- ∞, ∞)$
- id: b
  content: |-
    $(0, 1)$
- id: c
  content: |-
    $(0, ∞)$
- id: d
  content: |-
    $(-1, ∞)$
- id: e
  content: |-
    $(1, ∞)$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
