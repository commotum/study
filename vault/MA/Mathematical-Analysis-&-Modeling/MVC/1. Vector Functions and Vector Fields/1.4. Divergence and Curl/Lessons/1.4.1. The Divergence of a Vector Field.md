# The Divergence of a Vector Field

<!--
lesson-id: 2131
topic-code: MVC.1.4.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Some Important Notes About the Divergence of a Vector Field](#some-important-notes-about-the-divergence-of-a-vector-field)
- [Finding the Divergence of a Vector Field in Two Dimensions](#finding-the-divergence-of-a-vector-field-in-two-dimensions)
- [Finding the Divergence of a Vector Field in Three Dimensions](#finding-the-divergence-of-a-vector-field-in-three-dimensions)
- [Physical Interpretation of Divergence](#physical-interpretation-of-divergence)
- [Examples of Vector Field With Divergence and Solenoidal Vector Fields](#examples-of-vector-field-with-divergence-and-solenoidal-vector-fields)
- [Interpreting Divergence](#interpreting-divergence)

## Prerequisites

- [Partial Differentiability of Multivariable Functions](<../../../2. Multivariable Functions/2.3. Partial Derivatives/Lessons/2.3.4. Partial Differentiability of Multivariable Functions.md>)
- [Visualizing Vector Fields](<../../1.3. Vector Fields/Lessons/1.3.2. Visualizing Vector Fields.md>)

---

<a id="introduction"></a>
## Introduction

Suppose that $\mathbf F$ is a vector field on $\mathbb R^2$, expressed in terms of its component functions as

$$
\mathbf F(x,y) = P(x,y)\,\mathbf i + Q(x,y)\,\mathbf j
$$

If the first partial derivatives of $P$ and $Q$ exist, then the **divergence** of $\mathbf F$, denoted $\textrm{div}\,\mathbf F$, is the scalar given by

$$
\textrm{div}\,\mathbf F = \dfrac{\partial P}{\partial x} + \dfrac{\partial Q}{\partial y}
$$

Notice that if we define the differential operator $\nabla$ (a.k.a. the "del" operator) as

$$
\nabla = \mathbf i\,\dfrac{\partial}{\partial x} + \mathbf j\,\dfrac{\partial}{\partial y}
$$

then the divergence of $\mathbf F$ is given by the dot product

$$
\textrm{div}\,\mathbf F = \nabla \cdot \mathbf F
$$

For example, suppose that the vector field $\mathbf F$ is given by

$$
\mathbf F(x,y) = 2xy\, \mathbf i + e^{yx} \, \mathbf j
$$

Then, we can calculate $\textrm{div}\,\mathbf{F}$ using the dot product formula, as follows:

$$
\begin{aligned}
divF &= ∇ \cdot F \\
&= (i\frac{∂}{∂x} + j\frac{∂}{∂y}) \cdot (2xyi + e^{yx}j) \\
&= \frac{∂}{∂x}(2xy) + \frac{∂}{∂y}(e^{yx}) \\
&= 2y + xe^{yx}
\end{aligned}
$$

---

<a id="some-important-notes-about-the-divergence-of-a-vector-field"></a>
## Some Important Notes About the Divergence of a Vector Field

There are a few things we should note about the divergence:

- We can only compute the divergence of a *vector* field, and a vector field's divergence is a *scalar* field. So if $G(x,y) = \textrm{div}\,\mathbf F$, then we have the following map:
$\underbrace{\,\,\,\mathbf F(x,y)\,\,\,}_{\textrm{vector field}} \mapsto\underbrace{\,\,\,G(x,y)\,\,\,}_{\textrm{scalar field}}$
In contrast, if $f(x,y)$ is a scalar field, then the statement $\textrm{div}\, f$ has no meaning.
- We can easily extend the notion of divergence to three dimensions. So, if $\mathbf F(x,y,z) = P\,\mathbf i + Q\,\mathbf j + R\,\mathbf k$, then $\textrm{div}\,\mathbf F$ is given by
$\textrm{div}\,\mathbf F = \nabla\cdot \mathbf F = \dfrac{\partial P}{\partial x} + \dfrac{\partial Q}{\partial y} + \dfrac{\partial R}{\partial z}$,
where the three-dimensional differential operator $\nabla$ is given by
$\nabla = \mathbf i\,\dfrac{\partial}{\partial x} + \mathbf j\,\dfrac{\partial}{\partial y} + \mathbf k\,\dfrac{\partial}{\partial z}$.
- The divergence of a vector field at a point $P$ gives us helpful information about the tendency of the vector field to behave like a source or a sink at $P$. We'll describe this in more detail shortly.

---

<a id="finding-the-divergence-of-a-vector-field-in-two-dimensions"></a>
## Finding the Divergence of a Vector Field in Two Dimensions

**Example:** Consider the vector field $\mathbf{F}=\ln(2x+y)\,\mathbf{i}+ x^2e^{3y}\,\mathbf{j}$.
Evaluate $\nabla \cdot\mathbf{F}$ at the point $(1,0)$.

**Explanation**

If

$$
\mathbf{F}=P\,\mathbf{i}+Q\,\mathbf{j}
$$

is a vector field on $\mathbb{R}^2$ and the partial derivatives of the components of $\mathbf F$ all exist, then the divergence of $\mathbf F$ is the scalar field given by

$$
\nabla\cdot\mathbf{F}=\textrm{div}\,\mathbf F=\dfrac{\partial P}{\partial x}+\dfrac{\partial Q}{\partial y}
$$

Therefore, we have

$$
\begin{aligned}
∇ \cdot F &= \frac{∂P}{∂x} + \frac{∂Q}{∂y} \\
&= \frac{∂}{∂x}(\ln (2x + y)) + \frac{∂}{∂y}(x^{2}e^{3y}) \\
&= \frac{2}{2x + y} + 3x^{2}e^{3y}
\end{aligned}
$$

Finally, we evaluate $\textrm{div}\,\mathbf {F}$ at the point $(1,0)$, and obtain

$$
\begin{aligned}
(∇ \cdot F)(1, 0) &= (2)/(2(1) + 0) + 3(1)^{2}e^{3(0)} \\
&= 1 + 3 \\
&= 4
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-41817
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Given that $F = 2xy^{2}i + 4x^{2}yj$, what is $divF$?
options:
- id: a
  content: |-
    $2y^{2}i + 4x^{2}j$
- id: b
  content: |-
    $4xyi + 8xyj$
- id: c
  content: |-
    $8xy$
- id: d
  content: |-
    $2x^{2} + 4y^{2}$
- id: e
  content: |-
    $4x^{2} + 2y^{2}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-105561
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector field $F = (e^{x}\cos x)i + xe^{xy}j$.

  Evaluate $∇ \cdot F$ at the point $(0, 1)$.
options:
- id: a
  content: |-
    $3$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $-1$
- id: d
  content: |-
    $1$
- id: e
  content: |-
    $2$
```

---

<a id="finding-the-divergence-of-a-vector-field-in-three-dimensions"></a>
## Finding the Divergence of a Vector Field in Three Dimensions

**Example:** Consider the vector field $\mathbf{F}=(2x-y)^2\,\mathbf{i}+(3y-2z)^2\,\mathbf{j}+(x-2z)^2\,\mathbf{k}$. Evaluate $\textrm{div}\,\mathbf{F}$ at the point $(1,2,1)$.

**Explanation**

If

$$
\mathbf{F}=P\,\mathbf{i}+Q\,\mathbf{j} + R\,\mathbf k
$$

is a vector field on $\mathbb{R}^3$ and the partial derivatives of the components of $\mathbf F$ all exist, then the divergence of $\mathbf F$ is the scalar field given by

$$
\textrm{div}\,\mathbf F = \nabla\cdot\mathbf{F}=\dfrac{\partial P}{\partial x}+\dfrac{\partial Q}{\partial y}+\dfrac{\partial R}{\partial z}
$$

Therefore, we have

$$
\begin{aligned}
∇ \cdot F &= \frac{∂P}{∂x} + \frac{∂Q}{∂y} + \frac{∂R}{∂z} \\
&= \frac{∂}{∂x}((2x - y)^{2}) + \frac{∂}{∂y}((3y - 2z)^{2}) + \frac{∂}{∂z}((x - 2z)^{2}) \\
&= 4(2x - y) + 6(3y - 2z) - 4(x - 2z) \\
&= 4x + 14y - 4z
\end{aligned}
$$

Finally, we evaluate $\textrm{div}\,\mathbf {F}$ at the point $(1,2,1)$, and obtain

$$
\begin{aligned}
divF(1, 2, 1) &= 4(1) + 14(2) - 4(1) \\
&= 28
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-105555
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector field $F = \ln (xy)i - y\cos xj + yz^{3}k$.

  Find $∇ \cdot F$.
options:
- id: a
  content: |-
    $\frac{1}{xy}i - \cos xj + z^{3}k$
- id: b
  content: |-
    $\frac{1}{xy} - \cos x + z^{3}$
- id: c
  content: |-
    $\frac{1}{x} - \cos x + 3yz^{2}$
- id: d
  content: |-
    $\frac{1}{y}i - \sin xj + 3yz^{2}k$
- id: e
  content: |-
    $\frac{1}{x} + \sin x + 3yz^{2}$
```

---

**Question 4:**

```quiz
type: radio
id: ma-41839
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector field $F = xyzi + e^{-y}\cos πxj + (e^{-y}\sin πz + 2xz)k$.

  Evaluate $∇ \cdot F$ at the point $(1, 0, 0)$.
options:
- id: a
  content: |-
    $π + 1$
- id: b
  content: |-
    $3 - π$
- id: c
  content: |-
    $1$
- id: d
  content: |-
    $π + 3$
- id: e
  content: |-
    $1 - π$
```

---

<a id="physical-interpretation-of-divergence"></a>
## Physical Interpretation of Divergence

Our goal now is to develop an intuitive understanding of what the divergence of a vector field measures.

Consider the vector field $\mathbf F(x,y)$ and the point $S$, shown below. Imagine that this vector field represents the velocity field of a stream of flowing water.

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/0ebf9f474e4eac749e182bd3a5ca9607.png>)

We can see from our diagram that $S$ acts like a **source**. You can think of this as an open faucet, pushing water onto the plane and away from itself.

Since $S$ acts like a source, $\textrm{div}\,\mathbf F$ is nonzero. By convention, if a point $S$ acts like a source, the divergence is positive:
$\textrm{div}\,\mathbf F > 0$
In this particular case, *every* point in our vector field acts as a source. So $\textrm{div}\,\mathbf F > 0$ everywhere.

If we reverse the situation, we get the following:

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/2318cee55cbaa4a0987c34237058fd13.png>)

We can see from our diagram that $T$ acts like a **sink**. You can think of this as an open plughole that sucks in the surrounding water.

Since $T$ acts like a sink, $\textrm{div}\,\mathbf F$ is also nonzero. By convention, if a point $T$ acts like a sink, the divergence is negative:
$\textrm{div}\,\mathbf F < 0$
Every point in our vector field acts like a sink in this case. So $\textrm{div}\,\mathbf F < 0$ everywhere.

Finally, note that if

$$
\textrm{div}\,\mathbf F = 0
$$

everywhere in the domain of some vector field $\mathbf F$, we say that $\mathbf F$ is **solenoidal.**

---

<a id="examples-of-vector-field-with-divergence-and-solenoidal-vector-fields"></a>
## Examples of Vector Field With Divergence and Solenoidal Vector Fields

The following two vector fields behave either as a source or a sink. Therefore, they both have divergence.

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/16a17c2a3c044bf77fbc0bdb7183a3e5.png>)

The following vector fields are solenoidal (i.e., they each have zero divergence).

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/37a7a5ff90ddca750011652d02f71633.png>)

---

<a id="interpreting-divergence"></a>
## Interpreting Divergence

**Example:** Consider the vector field $\mathbf F$ shown above. Which of the following statements are true everywhere in the domain of $\mathbf F$?

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/b5bfdb81e347992660d868824f19dfd3.png>)

1. $\textrm{div}\,\mathbf F = 0$
2. $\textrm{div}\,\mathbf F > 0$
3. $\textrm{div}\,\mathbf F < 0$
4. $\mathbf F$ is solenoidal

**Explanation**

Recall that

$$
\textrm{div}\, \mathbf F = \nabla\cdot\mathbf F
$$

denotes the divergence of the vector field $\mathbf F$.

Intuitively, the divergence of $\mathbf F$ at a point $P$ measures how much $\mathbf F$ acts like a source or a sink at $P$:

- If $\mathbf F$ acts like a source at $P$, then $\nabla \cdot \mathbf F > 0$ at $P$.
- If $\mathbf F$ acts like a sink at $P$, then $\nabla \cdot \mathbf F < 0$ at $P$.
- If $\mathbf F$ acts like neither a source nor a sink at $P$, then $\nabla \cdot \mathbf F =0$ at $P$.

![](<../Source/1.4.1. The Divergence of a Vector Field/Images/a3948ed745c39ad63bafd542288743db.png>)

If

$$
\nabla \cdot \mathbf F = 0
$$

everywhere in the domain of $\mathbf F$, we say that $\mathbf F$ is solenoidal.

With that in mind, let's examine each statement:

- Statement I is true, whereas statements II and III are false. For any point $P$, the sum of the inward-pointing vectors equals the sum of the outward-pointing vectors. Therefore, $\mathbf F$ acts neither like a source nor a sink.
- Statement IV is true. Since $\nabla \cdot \mathbf F = 0$ everywhere, the vector field $\mathbf F$ is solenoidal.

Therefore, the correct answer is "I and IV only."

---

**Question 5**

```quiz
type: radio
id: ma-105571
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/1.4.1. The Divergence of a Vector Field/Images/q-105571.png>)

  Consider the vector field $F$ shown above. Which of the following statements are true everywhere in the domain of $F$?

  1. $divF = 0$
  2. $divF > 0$
  3. $divF < 0$
  4. $F$ is solenoidal
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    II only
- id: c
  content: |-
    I and IV only
- id: d
  content: |-
    I only
- id: e
  content: |-
    II and IV only
```

---

**Question 6**

```quiz
type: radio
id: ma-105075
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/1.4.1. The Divergence of a Vector Field/Images/q-105075.png>)

  Consider the vector field $F$ shown above. Which of the following statements are true everywhere in the domain of $F$?

  1. $divF = 0$
  2. $divF > 0$
  3. $divF < 0$
  4. $F$ is solenoidal
options:
- id: a
  content: |-
    I and IV only
- id: b
  content: |-
    II only
- id: c
  content: |-
    III and IV only
- id: d
  content: |-
    I only
- id: e
  content: |-
    III only
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
