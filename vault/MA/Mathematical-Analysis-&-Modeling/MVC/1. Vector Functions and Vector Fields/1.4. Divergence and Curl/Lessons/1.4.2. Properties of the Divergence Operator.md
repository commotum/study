# Properties of the Divergence Operator

<!--
lesson-id: 3701
topic-code: MVC.1.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Divergence of a Vector Field Using Linearity Properties](#finding-the-divergence-of-a-vector-field-using-linearity-properties)
- [The Product Rule for Divergence](#the-product-rule-for-divergence)
- [Finding the Divergence of a Vector Field Using the Product Rule](#finding-the-divergence-of-a-vector-field-using-the-product-rule)

## Prerequisites

- [The Divergence of a Vector Field](<1.4.1. The Divergence of a Vector Field.md>)
- [Gradient Vector Fields](<../../1.3. Vector Fields/Lessons/1.3.3. Gradient Vector Fields.md>)

---

<a id="introduction"></a>
## Introduction

Recall that if

$$
\mathbf F = P\,\mathbf i + Q\,\mathbf j + R\,\mathbf k
$$

is a vector field on $\mathbb R^3$, then the divergence of $\mathbf F$ is given by

$$
\textrm{div}\,\mathbf F = \nabla\cdot \mathbf F = \dfrac{\partial P}{\partial x} + \dfrac{\partial Q}{\partial y} + \dfrac{\partial R}{\partial z}
$$

Since divergence is computed using derivatives, it's unsurprising that it shares similar properties with derivatives.

For example, if $\mathbf F$ and $\mathbf H$ are vector fields and $k$ is a scalar constant, then we have the following linearity properties:

$$
∇ \cdot (kF) = k(∇ \cdot F); ∇ \cdot (F + H) = ∇ \cdot F + ∇ \cdot H
$$

Applying these rules can often help us to save time when computing divergence. Let's see some examples.

---

<a id="finding-the-divergence-of-a-vector-field-using-linearity-properties"></a>
## Finding the Divergence of a Vector Field Using Linearity Properties

**Example:** Calculate $\nabla \cdot (\mathbf{F}-3\mathbf{G})$ for the vector fields $\mathbf F$ and $\mathbf G$, where
$\mathbf{F}=\left(x^2+y\right)\,\mathbf{i}+ \left(xy-y^2\right)\,\mathbf{j}, \quad \nabla \cdot \mathbf{G}=x+y$.

**Explanation**

If $\mathbf F$ and $\mathbf G$ are vector fields and $k$ is a constant, then we have the following linearity properties for divergence:

- $\nabla \cdot (k \mathbf F) = k \nabla \cdot \mathbf F$
- $\nabla \cdot (\mathbf F + \mathbf G) = \nabla \cdot \mathbf F + \nabla \cdot \mathbf G$

In our case, we have

$$
\begin{aligned}
∇ \cdot (F - 3G) &= ∇ \cdot (F) + ∇ \cdot (-3G) \\
&= ∇ \cdot F - 3∇ \cdot G
\end{aligned}
$$

First, we need to calculate $\nabla \cdot \mathbf{F}$. In doing so, we get

$$
\begin{aligned}
∇ \cdot F &= \frac{∂}{∂x}(x^{2} + y) + \frac{∂}{∂y}(xy - y^{2}) \\
&= 2x + x - 2y \\
&= 3x - 2y
\end{aligned}
$$

Applying the linearity properties, we get

$$
\begin{aligned}
∇ \cdot (F - 3G) &= ∇ \cdot F - 3∇ \cdot G \\
&= (3x - 2y) - 3 \cdot (x + y) \\
&=-5y
\end{aligned}
$$

---

**Question 1:** Calculate $∇ \cdot (\frac{1}{2}F)$ for the vector field $F$ where
$F = xe^{2x}i - ye^{2x}j + xz^{2}k$.

- [ ] A. $2e^{2x} + z$
- [ ] B. $2xe^{2x} + z^{2}$
- [ ] C. $(1 + 2x)e^{2x} + 2xz$
- [ ] D. $(1 + x)e^{2x} + xz$
- [ ] E. $xe^{2x} + xz$

---

**Question 2:** Calculate $∇ \cdot (F + 2G)$ for the vector fields $F$ and $G$, where
$F = \ln (x^{2} + y^{2})i + \ln y^{2}j, ∇ \cdot G =-\frac{x}{x^{2} + y^{2}} + \frac{1}{y^{2}}$.

- [ ] A. $-\frac{2x}{y^{2}}$
- [ ] B. $(2(1 + x))/(y^{2})$
- [ ] C. $-\frac{2}{y^{2}}$
- [ ] D. $(2(1 + xy))/(y^{2})$
- [ ] E. $(2(1 + y))/(y^{2})$

---

<a id="the-product-rule-for-divergence"></a>
## The Product Rule for Divergence

Recall that for functions $f(x)$ and $g(x)$, the product rule for differentiation is given by

$$
\dfrac{\textrm d}{\textrm d x}(f\cdot g) = f\dfrac{\textrm d}{\textrm d x}(g) + g\dfrac{\textrm d}{\textrm d x}(f)
$$

There is an analogous rule for computing the divergence of the product of a vector field and a scalar field.

Suppose that $\mathbf F$ is a vector field and $f$ is a scalar field. Then, we have the following identity:

$$
\nabla \cdot (f\,\mathbf F) = f\, \nabla \cdot \mathbf {F}+ \mathbf F \cdot \nabla \! f
$$

where $\nabla f$ is the gradient of $f$.

Notice that the del operator $\nabla$ acts on the terms in the same way as the

$$
\dfrac{\textrm d}{\textrm d x}
$$

operator in the product rule for differentiation!

There are a few other tips that we can use to remember this rule:

- The term $\nabla \cdot (f\,\mathbf F)$ on the left-hand side must be a scalar field because it's a divergence. So, the two terms on the right-hand side must be scalar fields, too.
- The term $f\, \nabla \cdot \mathbf {F}$ on the right-hand side is a scalar field because both $f$ and $\nabla \cdot \mathbf F$ are scalar fields, and the product of two scalar fields is a scalar field. Note that this "product" is just regular multiplication.
- The term $\mathbf F \cdot \nabla \! f$ on the right-hand side is a scalar field because both $\mathbf F$ and $\nabla f$ are *vector* fields, and the *dot product* of two vector fields is a scalar field.

---

<a id="finding-the-divergence-of-a-vector-field-using-the-product-rule"></a>
## Finding the Divergence of a Vector Field Using the Product Rule

**Example:** Given that $\mathbf F(x,y) = \left\langle \dfrac 1x, xy \right \rangle$ and $f(x,y) = \dfrac 1{x}$, calculate $\nabla \cdot (f\,\mathbf F)$.

**Explanation**

We will use the following identity:

$$
\nabla \cdot (f\,\mathbf F) = f\, \nabla \cdot \mathbf {F}+ \mathbf F \cdot \nabla f
$$

First, we compute $\nabla \cdot \mathbf {F}$ and $\nabla f$. We get

$$
\begin{aligned}
∇f &= ⟨ - \frac{1}{x^{2}}, 0⟩, \\
∇ \cdot F &= \frac{∂}{∂x}(\frac{1}{x}) + \frac{∂}{∂y}(xy) =-\frac{1}{x^{2}} + x
\end{aligned}
$$

Therefore, we have

$$
\begin{aligned}
∇ \cdot (fF) &= f∇ \cdot F + F \cdot ∇f \\
&= \frac{1}{x}(-\frac{1}{x^{2}} + x) + ⟨\frac{1}{x}, xy⟩ \cdot ⟨ - \frac{1}{x^{2}}, 0⟩ \\
&=-\frac{1}{x^{3}} + 1 - \frac{1}{x^{3}} \\
&= 1 - \frac{2}{x^{3}}
\end{aligned}
$$

---

**Question 3:** Given that $F(x, y, z) = ⟨e^{3x},-2ye^{3x}⟩$ and $f(x, y) = 2x$, calculate $∇ \cdot (fF)$.

- [ ] A. $(x^{2} + 1)e^{3x}$
- [ ] B. $2(x + 3)e^{3x}$
- [ ] C. $(2x^{2} + 2x + 1)e^{3x}$
- [ ] D. $2(x + 1)e^{3x}$
- [ ] E. $(3x + 1)e^{3x}$

---

**Question 4:** Given that $F(x, y) = ⟨x, x^{2}y⟩$ and $f(x, y) = xy$, calculate $∇ \cdot (fF)$.

- [ ] A. $2xy(1 + y^{3})$
- [ ] B. $2xy(1 + x^{2})$
- [ ] C. $2xy(1 + x)$
- [ ] D. $2xy(1 + y^{2})$
- [ ] E. $2xy(1 + xy)$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
