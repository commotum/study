# Transformations of Regions Between Curves

<!--
lesson-id: 4134
topic-code: MVC.2.6.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Rectangular Preimage Given a Transformation](#finding-a-rectangular-preimage-given-a-transformation)
- [Finding a Transformation That Maps a Rectangular Region Onto a Given One](#finding-a-transformation-that-maps-a-rectangular-region-onto-a-given-one)
- [Transformations That Map a Region to a Type I Region](#transformations-that-map-a-region-to-a-type-i-region)
- [Finding a Transformation That Maps a Region Onto a Type I Region](#finding-a-transformation-that-maps-a-region-onto-a-type-i-region)

## Prerequisites

- [Type I and II Regions in Two-Dimensional Space](<../../../3. Multiple Integrals/3.2. Double Integrals/Lessons/3.2.4. Type I and II Regions in Two-Dimensional Space.md>)
- [Nonlinear Transformations of Plane Regions](<2.6.4. Nonlinear Transformations of Plane Regions.md>)

---

<a id="introduction"></a>
## Introduction

The plane region $D$ shown below is defined as

$$
D = \big\{(x,y) \:: \: 1 \leq xe^{y}\leq 4, \: \: 1 \leq x^2e^{-2y} \leq 3 \big\}
$$

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/f0f6555012fb9b42e42a08e18d19b11c.png>)

This region is pretty complicated, which could make it difficult to work with.

So, we'd like to define a transformation

$\mathbf T^{-1}: (x,y)\to (u(x,y),v(x,y))$
that maps the region $D$ to a rectangular region $\Delta$ in the $uv$-plane.

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/a42c74596902461c18937bafae457866.png>)

How can we find such a transformation?

Looking at the bounding curves that make up the structure of $D$, we consider the transformation

$$
{\color{red}u}={\color{red}{xe^{y}}}, \qquad {\color{blue}v}={\color{blue}{x^2e^{-2y}}}
$$

Next, we find our expression for $\Delta$ by writing $D$ in terms of $u$ and $v$, as follows:

$$
\begin{aligned}
Δ = T^{-1}(D) &= {(u, v): 1 \le xe^{y} \le 4, 1 \le x^{2}e^{-2y} \le 3} \\
&= {(u, v): 1 \le u \le 4, 1 \le v \le 3}
\end{aligned}
$$

And that's all there is to it! We've successfully found a transformation $\mathbf T^{-1}$ that maps the complicated region $D$ in the $xy$-plane to the rectangle $[1,4]\times[1,3]$ in the $uv$-plane.

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/22efa363fb615d6f3a2839b1ca0d3a71.png>)

---

<a id="finding-a-rectangular-preimage-given-a-transformation"></a>
## Finding a Rectangular Preimage Given a Transformation

**Example:** Consider the region $D$, given by
$D = \big\{(x,y) \:: \: 2 \leq \sqrt{xy} \leq 4, \: 2x \leq y \leq 3x \big\}$
and the transformation $\mathbf T$, given by
$\mathbf{T}:(u,v) \to (x(u,v),y(u,v))$,
where $u=\dfrac{y}{x}$ and $v=xy$. Find the representation of $\mathbf{T}^{-1}(D)$ in the $uv$-plane.

**Explanation**

Notice that $\mathbf{T}^{-1}(D)$ is the image of $D$ under the action of the transformation $\mathbf{T}^{-1}$.

Let's find the image of $\mathbf{T}^{-1}(D)$ in the $uv$-plane. To do that, we write the region $D$ in terms of $u$ and $v$, as follows:

$$
\begin{aligned}
T^{-1}(D) &= {(u, v): 2 \le \sqrt{xy} \le 4, 2x \le y \le 3x} \\
&= {(u, v): 2^{2} \le (\sqrt{xy})^{2} \le 4^{2}, 2 \le \frac{y}{x} \le 3} \\
&= {(u, v): 4 \le xy \le 16, 2 \le \frac{y}{x} \le 3} \\
&= {(u, v): 4 \le v \le 16, 2 \le u \le 3} \\
&= {(u, v): 2 \le u \le 3, 4 \le v \le 16}
\end{aligned}
$$

Notice that since

$$
2 \leq \sqrt{xy}\leq 4
$$

we must have

$$
x \neq 0
$$

Therefore,

$$
u=\dfrac{y}{x}
$$

is well-defined.

---

**Question 1:**

```quiz
type: radio
id: ma-82725
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the region $D$, given by

  $D = {(x, y): 1 \le x + y \le 2, 1 \le x - y \le 3}$
  and the transformation $T$, given by
  $T: (u, v) → (x(u, v), y(u, v))$,
  where $u = x + y$ and $v = x - y + 1$. Find the representation of $T^{-1}(D)$ in the $uv$-plane.
options:
- id: a
  content: |-
    ${(u, v): 1 \le u \le 2, 1 \le v \le 3}$
- id: b
  content: |-
    ${(u, v): 2 \le u \le 3, 1 \le v \le 3}$
- id: c
  content: |-
    ${(u, v): 1 \le u + v \le 2, 1 \le u - v + 1 \le 3}$
- id: d
  content: |-
    ${(u, v): 1 \le u + v \le 2, 1 \le u - v \le 3}$
- id: e
  content: |-
    ${(u, v): 1 \le u \le 2, 2 \le v \le 4}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-38306
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the region $D$, given by

  $D = {(x, y): 1 \le xe^{2y} \le 2, 2 \le x^{2}e^{-4y} \le 3}$
  and the transformation $T$, given by
  $T: (u, v) → (x(u, v), y(u, v))$,
  where $u = xe^{2y}$ and $v = xe^{-2y}$. Find the representation of $T^{-1}(D)$ in the $uv$-plane.
options:
- id: a
  content: |-
    ${(u, v): \frac{1}{2} \le u \le 1, 2 \le v \le 3}$
- id: b
  content: |-
    ${(u, v): 1 \le u \le 2, 2 \le v \le 3}$
- id: c
  content: |-
    ${(u, v): \frac{1}{2} \le u \le 1, \frac{1}{3} \le v \le \frac{1}{2}}$
- id: d
  content: |-
    ${(u, v): 2 \le u \le 3, 1 \le v \le 2}$
- id: e
  content: |-
    ${(u, v): 1 \le u \le 2, \sqrt{2} \le v \le \sqrt{3}}$
```

---

<a id="finding-a-transformation-that-maps-a-rectangular-region-onto-a-given-one"></a>
## Finding a Transformation That Maps a Rectangular Region Onto a Given One

**Example:** Consider the finite region $D$ enclosed in the first quadrant of the $xy$-plane between the curves
$y=x, \qquad y=7x, \qquad x=\dfrac{3}{y^2}, \qquad x=\dfrac{5}{y^2}$.

A transformation $\mathbf{T}$, where

$\mathbf{T}:(u,v) \to (x(u,v),y(u,v))$,

maps a rectangular region $\Delta$ with sides parallel to the $u$- and $v$-axes from the $uv$-plane onto $D$ in the $xy$-plane. Given that
$\mathbf{T}: (u,v) \to \Big(\sqrt[3]{\dfrac{v}{u^2}}, \: \bbox[5pt, white, border: 1pt solid black]{\phantom{AA}} \, \Big)$,
what could be the missing expression?

**Explanation**

We would like to find a rectangular region of the form

$$
\begin{aligned}
Δ &= {(u, v): a \le u \le b, c \le v \le d} \\
&= {(u, v): a \le u(x, y) \le b, c \le v(x, y) \le d}
\end{aligned}
$$

where the equations of the left, right, bottom, and top sides are

$$
{\color{blue}u(x,y)} = a, \qquad {\color{blue}u(x,y)} = b, \qquad {\color{red}v(x,y)} = c, \qquad {\color{red}v(x,y)} = d
$$

With that in mind, let's analyze the boundary of our region $D$.

Rewriting the equations

$$
y=x, \qquad y=7x, \qquad x=\dfrac{3}{y^2}, \qquad x=\dfrac{5}{y^2}
$$

by moving both variables $x$ and $y$ to the left-hand side, we obtain

$$
\frac{y}{x} = 1, \frac{y}{x} = 7, xy^{2} = 3, xy^{2} = 5
$$

Therefore, if we put

$$
u(x,y)={\color{blue}\dfrac{y}{x}}
$$

and

$$
v(x,y)={\color{red}xy^2}
$$

we can define the transformation $\mathbf{T}^{-1}$ as

$$
\mathbf{T}^{-1}: (x,y) \to \left(\, {\color{blue}\dfrac{y}{x}}\,, \: \bbox[2pt, white, border: 1pt solid black]{\color{red}xy^2} \right)
$$

To find $\mathbf{T}$, we should express $x$ and $y$ in terms of $u$ and $v$. Recall that we have

$$
{u = \frac{y}{x}; v = xy^{2}
$$

From the first equation, $y=ux$. Substituting this into the second equation, we get

$$
v = u^2x^3 \qquad\Longrightarrow\qquad x^3 = \dfrac{v}{u^2} \qquad\Longrightarrow\qquad x = \sqrt[3]{\dfrac{v}{u^2}}
$$

Finally, substituting

$$
x = \sqrt[3]{\dfrac{v}{u^2}}
$$

back into $y=ux$, we obtain

$$
y =u\sqrt[3]{\dfrac{v}{u^2}} = \sqrt[3]{uv}
$$

Therefore, the transformation $\mathbf{T}$ can be given by

$$
\mathbf{T}: (u,v) \to \left(\sqrt[3]{\dfrac{v}{u^2}}, \, \bbox[2pt, white, border: 1pt solid black]{\sqrt[3]{uv}} \right)
$$

---

**Question 3:**

```quiz
type: radio
id: ma-123917
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the finite region $D$ enclosed in the first quadrant of the $xy$-plane between the curves

  $y = x, y = 2x, y = \frac{2}{x}, y = \frac{6}{x}$.
  A transformation $T$, given by
  $T: (u, v) → (x(u, v), y(u, v))$,
  maps a rectangular region $Δ$ with sides parallel to the $u$- and $v$-axes from the $uv$-plane onto $D$ in the $xy$-plane. Given that
  $T^{-1}: (x, y) → (A A, xy)$,
  what could be the missing expression?
options:
- id: a
  content: |-
    $\frac{y}{x}$
- id: b
  content: |-
    $\frac{1}{xy}$
- id: c
  content: |-
    $xy$
- id: d
  content: |-
    $2xy$
- id: e
  content: |-
    $x$
```

---

**Question 4**

```quiz
type: radio
id: ma-160559
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the finite region $D$ enclosed in the first quadrant of the $xy$-plane between the curves
  $y = x, y = 3x, y = \frac{1}{x}, y = \frac{5}{x}$.

  A transformation $T$, where

  $T: (u, v) → (x(u, v), y(u, v))$,

  maps a rectangular region $Δ$ with sides parallel to the $u$- and $v$-axes from the $uv$-plane onto $D$ in the $xy$-plane. Given that
  $T: (u, v) → (\sqrt{\frac{v}{u}}, A A)$,
  what could be the missing expression?
options:
- id: a
  content: |-
    $\sqrt{\frac{u}{v}}$
- id: b
  content: |-
    $\sqrt{\frac{v}{u}}$
- id: c
  content: |-
    $\sqrt{uv}$
- id: d
  content: |-
    $uv$
- id: e
  content: |-
    $\frac{u}{v}$
```

---

<a id="transformations-that-map-a-region-to-a-type-i-region"></a>
## Transformations That Map a Region to a Type I Region

We often use transformations to map a region in the $xy$-plane to a type I region in the $uv$-plane.

For example, let $D$ be the region bounded by the curve

$$
(x+y)^2 - y = 2
$$

and the line $y=1$, as shown in the diagram below.

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/710f02a5b5f67688a63cf695163daf48.png>)

This is not a type I region because the lower curve is not an explicit function of $x$.

Let's map $D$ to a type I region in the $uv$-plane. First, let $\mathbf T$ be the transformation

$$
\mathbf{T}:(u,v) \to (x(u,v),y(u,v))
$$

The equations of the bounding curves suggest the following for the transformation $\mathbf T^{-1}{:}$

$$
{\color{blue}{u}}={\color{blue}{x+y}}, \qquad {\color{red}{v}}={\color{red}{y}}
$$

Substituting

$$
{\color{blue}{u}}={\color{blue}{x+y}}
$$

and

$$
{\color{red}{v}}={\color{red}{y}}
$$

into the equation of the lower curve, we get

$$
(x + y)^{2} - y = 2⟹u^{2} - v = 2⟹v = u^{2} - 2
$$

which is the equation of an upward-opening parabola.

Substituting $y = v$ into the equation of the line, we obtain

$$
y = 1⟹v = 1
$$

To find the intersections of the parabola and line in the $uv$-plane, we solve the following system:

$$
{v = u^{2} - 2; v = 1⟹{u = ± \sqrt{3}; v = 1
$$

So, the intersection points are $(\pm \sqrt3, 1)$.

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/943becf92c901c5b3676f07951e27167.png>)

Finally, our domain in the $uv$-plane has the following type I representation:

$$
\mathbf{T}^{-1}(D) = \big\{(u,v) \:: \: -\sqrt3 \leq u \leq \sqrt3, \quad u^2-2 \leq v \leq 1 \big\}
$$

---

<a id="finding-a-transformation-that-maps-a-region-onto-a-type-i-region"></a>
## Finding a Transformation That Maps a Region Onto a Type I Region

**Example:** Let $D$ be the finite region bounded by the curve
$(x+y)^2 + y = 3$ and the line $y=-1$.
Consider the transformation $\mathbf T$, given by
$\mathbf{T}:(u,v) \to (x(u,v),y(u,v))$,
where $u=x+y$ and $v=y$. Find the representation of $\mathbf{T}^{-1}(D)$ in the $uv$-plane.

**Explanation**

First, notice that we are given that

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

Substituting

$$
x+y = u
$$

and $y = v$ into the equation of the first curve, we get

$$
(x + y)^{2} + y = 3⟹u^{2} + v = 3⟹v = 3 - u^{2}
$$

while substituting $y = v$ into the equation of the second curve, we obtain

$$
y =-1⟹v =-1
$$

To find the intersections of the curves in the $uv$-plane, we solve the following system:

$$
{v = 3 - u^{2}; v =-1⟹{u = ± 2; v =-1
$$

So, the intersection points are $(\pm 2, -1)$.

![](<../Source/2.6.6. Transformations of Regions Between Curves/Images/8e7ff7e676e27ebe66efc5c8ebc464f5.png>)

As a result, our domain in the $uv$-plane has the type I representation

$$
\mathbf{T}^{-1}(D) = \big\{(u,v) \:: \: -2 \leq u \leq 2, \quad -1 \leq v \leq 3-u^2 \big\}
$$

---

**Question 5:**

```quiz
type: radio
id: ma-82669
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $D$ be the finite region bounded by the curve

  $(2x - y)^{2} - y = 3$ and the line $y = 1$. Consider the transformation $T$, given by
  $T: (u, v) → (x(u, v), y(u, v))$,
  where $u = 2x - y$ and $v = y$. Find the representation of $T^{-1}(D)$ in the $uv$-plane.
options:
- id: a
  content: |-
    ${(u, v): - 2 \le u \le 2, u^{2} - 3 \le v \le u^{2}}$
- id: b
  content: |-
    ${(u, v): - 3 \le u \le 3, u^{2} - 3 \le v \le 1}$
- id: c
  content: |-
    ${(u, v): - 2 \le u \le 2, u^{2} - 3 \le v \le 1}$
- id: d
  content: |-
    ${(u, v): - 1 \le u \le 1, u^{2} - 1 \le v \le 1}$
- id: e
  content: |-
    ${(u, v): - 2 \le u \le 2,-3 \le v \le 1}$
```

---

**Question 6:**

```quiz
type: radio
id: ma-82777
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $D$ be the finite region bounded by the curve

  $(x + y)^{2} - x + y = 0$ and the line $y = 0$.
  Consider the transformation $T$, given by
  $T: (u, v) → (x(u, v), y(u, v))$,
  where $u = x + y$ and $v = x - y$. Find the representation of $T^{-1}(D)$ in the $uv$-plane.
options:
- id: a
  content: |-
    ${(u, v): 0 \le u \le 1, u^{2} \le v \le u}$
- id: b
  content: |-
    ${(u, v): 0 \le u \le v, 0 \le v \le u}$
- id: c
  content: |-
    ${(u, v): 0 \le u \le 1, 0 \le v \le u}$
- id: d
  content: |-
    ${(u, v): 0 \le u \le v, 0 \le v \le u^{2}}$
- id: e
  content: |-
    ${(u, v): - v \le u \le 1, u^{2} \le v \le 1}$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
