# Differentials

<!--
lesson-id: 1950
topic-code: MVC.2.5.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing the Differential of a Multivariable Function](#computing-the-differential-of-a-multivariable-function)
- [Computing a Differential in a Geometrical Context](#computing-a-differential-in-a-geometrical-context)
- [Approximating Change Using Differentials](#approximating-change-using-differentials)
- [Using Differentials to Approximate the Change in a Multivariable Function](#using-differentials-to-approximate-the-change-in-a-multivariable-function)
- [Using Differentials to Approximate the Value of a Function](#using-differentials-to-approximate-the-value-of-a-function)

## Prerequisites

- [The Multivariable Chain Rule](<2.5.1. The Multivariable Chain Rule.md>)

---

<a id="introduction"></a>
## Introduction

The **differential** of a multivariable function $f(x,y)$ is denoted $\textrm df(x,y)$ and represents the infinitesimal change in the function value that occurs when the inputs $x$ and $y$ are changed by infinitesimal amounts $\textrm dx$ and $\textrm dy$.

The formula for the differential comes from the multivariable chain rule, which can be expressed as

$$
\begin{aligned}
df(x, y) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy
\end{aligned}
$$

Note that the differential can be extended to any number of variables. For example, for a three-variable function $f(x,y,z)$, we have

$$
\begin{aligned}
df(x, y, z) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy + (\frac{∂f}{∂z})dz
\end{aligned}
$$

---

<a id="computing-the-differential-of-a-multivariable-function"></a>
## Computing the Differential of a Multivariable Function

**Example:** Find the differential for $f(x,y) = x^2 \sin y$.

**Explanation**

We compute the differential using the formula:

$$
\begin{aligned}
df(x, y) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy \\
&= (2x\sin y)dx + (x^{2}\cos y)dy \\
&= 2x\sin ydx + x^{2}\cos ydy
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-37282
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the differential $df$ for the function $f(x, y, z) = \cos (xy) - 2z^{2}y$.
options:
- id: a
  content: |-
    $-y\sin (xy)dx - (2z^{2} + x\sin (xy))dy - 4yzdz$
- id: b
  content: |-
    $-y\sin xdx - (2z^{2} + x\sin (xy))dy - 4yzdz$
- id: c
  content: |-
    $y\cos (xy)dx + (2z^{2} + x\cos (xy))dy + 4yzdz$
- id: d
  content: |-
    $-2z^{2} + (x - y)\sin (xy) - 4yz$
- id: e
  content: |-
    $-y\sin ydx - (2z^{2} - x\sin x)dy - 4yzdz$
```

---

**Question 2:**

```quiz
type: radio
id: ma-37307
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the differential $df$ for the function $f(x, y) = \ln (x + y^{2})$.
options:
- id: a
  content: |-
    $(x + y^{2})dx + (x + y^{2})dy$
- id: b
  content: |-
    $\frac{2dx}{x + y^{2}} - \frac{2dy}{x + y^{2}}$
- id: c
  content: |-
    $\frac{xdx}{x + y^{2}} + \frac{2ydy}{x + y^{2}}$
- id: d
  content: |-
    $\frac{dx}{x + y^{2}} + \frac{2ydy}{x + y^{2}}$
- id: e
  content: |-
    $\frac{dx}{x + y^{2}} + \frac{ydy}{x + y^{2}}$
```

---

<a id="computing-a-differential-in-a-geometrical-context"></a>
## Computing a Differential in a Geometrical Context

**Example:** Given a cone, find the differential $\textrm d V$ for the volume $V$ of the cone if the radius is $r=6$ and the height is $h = 3$.

**Explanation**

The volume of the cone is

$$
V = \dfrac{1}{3} \pi r^2h
$$

Let's find the differential:

$$
\begin{aligned}
dV(r, h) &= (\frac{∂V}{∂r})dr + (\frac{∂V}{∂h})dh \\
&= \frac{2}{3}πrhdr + \frac{1}{3}πr^{2}dh
\end{aligned}
$$

For $r=6$ and $h=3$, the differential is

$$
\begin{aligned}
dV(6, 3) &= \frac{2}{3}π \cdot 6 \cdot 3dr + \frac{1}{3}π \cdot 6^{2}dh \\
&= 12πdr + 12πdh
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-37295
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Given a sector of a circle, find the differential $dS$ for the surface area $S$ if the radius is $r = 4$ and the central angle is $θ = \frac{π}{8}$.
options:
- id: a
  content: |-
    $\frac{π}{4}dr + 4dθ$
- id: b
  content: |-
    $\frac{π}{8}dr + \frac{1}{8}dθ$
- id: c
  content: |-
    $\frac{π}{2}dr + 8dθ$
- id: d
  content: |-
    $πdr + 4dθ$
- id: e
  content: |-
    $\frac{π}{4}dr + \frac{1}{8}dθ$
```

---

**Question 4:**

```quiz
type: radio
id: ma-37308
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Given a cylinder, find the differential $dV$ for the volume $V$ of the cylinder if the radius is $r = 2$ and the height is $h = 4$.
options:
- id: a
  content: |-
    $4πdr + πdh$
- id: b
  content: |-
    $8πdr + 2πdh$
- id: c
  content: |-
    $16πdr + 4πdh$
- id: d
  content: |-
    $16dr + 4dh$
- id: e
  content: |-
    $4πdr + 16πdh$
```

---

<a id="approximating-change-using-differentials"></a>
## Approximating Change Using Differentials

Given a multivariable function $f(x,y)$, the differential

$$
\begin{aligned}
df(x, y) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy
\end{aligned}
$$

tells us how infinitesimal changes in $x$ and $y$, denoted $\textrm dx$ and $\textrm dy$, result in an infinitesimal change in $f$, denoted $\textrm df$.

Consequently, we can use the differential to *approximate* the change in $f$ as we move from one point to another. If we move from a point $(x,y)$ to a point $(x + \Delta x, y + \Delta y)$, then the approximate change in $f$ is given by

$$
Δf(x, y) \mid \approx (\frac{∂f}{∂x})Δx + (\frac{∂f}{∂y})Δy
$$

The above formula is very similar to the differential formula, except the $\textrm d$'s are replaced with $\Delta$'s. The $\textrm d$'s refer to infinitesimal changes, while the $\Delta$'s refer to changes that can actually be measured.

---

<a id="using-differentials-to-approximate-the-change-in-a-multivariable-function"></a>
## Using Differentials to Approximate the Change in a Multivariable Function

**Example:** If $f (x, y) = xe^y$, use the differential $\textrm d f$ to estimate the change $\Delta f$ in the value of $f(x, y)$ from $(5,0)$ to $(2,1)$.

**Explanation**

First, let's find the differential:

$$
\begin{aligned}
df(x, y) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy \\
&= e^{y}dx + xe^{y}dy
\end{aligned}
$$

At the point $(5,0)$, the differential is

$$
\begin{aligned}
df(5, 0) &= e^{0}dx + 5e^{0}dy \\
&= 1dx + 5dy
\end{aligned}
$$

So, if we move to the point $(5 + \Delta x, 0 + \Delta y)$, then we can approximate the change in $f$ as

$$
\Delta f \approx 1 \, \Delta x + 5 \, \Delta y
$$

Moving from $(5,0)$ to $(2,1)$, we have

$$
\begin{aligned}
Δx &= 2 - 5 =-3, \\
Δy &= 1 - 0 = 1
\end{aligned}
$$

So, we have

$$
Δf \mid \approx 1Δx + 5Δy; \approx 1 \cdot (-3) + 5 \cdot (1); \approx 2
$$

Therefore, the value of $f(x,y)$ increases by approximately $2$.

---

**Question 5:**

```quiz
type: radio
id: ma-79814
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If $f(x, y, z) = ye^{x} + xz$, use the differential $df$ to estimate the change $Δf$ in the value of $f(x, y, z)$ from $(0, 1, 2)$ to $(-1, 0, 3)$.
options:
- id: a
  content: |-
    $4$
- id: b
  content: |-
    $-3$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $-2$
- id: e
  content: |-
    $-4$
```

---

**Question 6:**

```quiz
type: radio
id: ma-37306
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If $f(x, y) = \cos x + y$, use the differential $df$ to estimate the change $Δf$ in the value of $f(x, y)$ from $(4π, 8)$ to $(13, 8.5)$.
options:
- id: a
  content: |-
    $Δf \approx - 0.5$
- id: b
  content: |-
    $Δf \approx - 0.3$
- id: c
  content: |-
    $Δf \approx 0.5$
- id: d
  content: |-
    $Δf \approx 0.1$
- id: e
  content: |-
    $Δf \approx 0.3$
```

---

<a id="using-differentials-to-approximate-the-value-of-a-function"></a>
## Using Differentials to Approximate the Value of a Function

**Example:** Estimate $\sin (3) + \cos (0.2)$ using a differential of the function $f(x,y) = \sin x + \cos y$ at the point $\left(\pi, 0 \right)$.

**Explanation**

First, let's find the differential:

$$
\begin{aligned}
df(x, y) &= (\frac{∂f}{∂x})dx + (\frac{∂f}{∂y})dy \\
&= \cos xdx - \sin ydy
\end{aligned}
$$

At the point

$$
\left(\pi, 0 \right)
$$

the differential is

$$
\begin{aligned}
df(x, y) &= \cos πdx - \sin 0dy \\
&=-1dx + 0dy
\end{aligned}
$$

So, if we move to the point $(\pi + \Delta x, 0 + \Delta y)$, then we can approximate the change in $f$ as

$$
\Delta f \approx -1 \, \Delta x + 0 \, \Delta y
$$

Note that

$$
\sin (3) + \cos (0.2)= f(3,0.2)
$$

so we want to move to the point $(3,0.2)$.

Moving from $(\pi, 0)$ to $(3,0.2)$, we have

$$
\begin{aligned}
Δx &= 3 - π \approx - 0.141, \\
Δy &= 0.2 - 0 = 0.2
\end{aligned}
$$

So, we have

$$
Δf \mid \approx - 1(-0.141) + 0(0.2); \approx 0.141
$$

This means that the value of $f$ increases by about $0.141$. Consequently, we have

$$
f(3, 0.2)\begin{vmatrix}\approx f(π, 0) + 0.141 \\ \sin (3) + \cos (0.2)\end{vmatrix}\approx \sin (π) + \cos (0) + 0.141; \approx 0 + 1 + 0.141; \approx 1.141
$$

---

**Question 7:**

```quiz
type: radio
id: ma-79837
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Estimate $(1.81) \cdot \ln (0.91)$ using a differential $df$ of the function $f(x, y) = x\ln y$ at the point $(2, 1)$.
options:
- id: a
  content: |-
    $-0.20$
- id: b
  content: |-
    $-0.19$
- id: c
  content: |-
    $-0.15$
- id: d
  content: |-
    $-0.16$
- id: e
  content: |-
    $-0.18$
```

---

**Question 8:**

```quiz
type: radio
id: ma-37281
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Estimate $\sqrt[3]{10} + \sqrt{150}$ using a differential $df$ of the function $f(x, y) = \sqrt[3]{x} + \sqrt{y}$ at the point $(8, 144)$. Round your answer to $3$ decimal places.
options:
- id: a
  content: |-
    $11.215$
- id: b
  content: |-
    $18.556$
- id: c
  content: |-
    $10.235$
- id: d
  content: |-
    $14.417$
- id: e
  content: |-
    $15.873$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/MVC/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/MVC/0. Table of Contents/TOC|Table of Contents]]
