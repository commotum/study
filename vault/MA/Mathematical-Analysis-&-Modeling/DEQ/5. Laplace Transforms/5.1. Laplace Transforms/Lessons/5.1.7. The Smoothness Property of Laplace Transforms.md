# The Smoothness Property of Laplace Transforms

<!--
lesson-id: 6402
topic-code: DEQ.5.1.7
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [A Table of Laplace Transforms](#a-table-of-laplace-transforms)
- [Finding Laplace Transforms Using the Smoothness Property (1st Derivative)](#finding-laplace-transforms-using-the-smoothness-property-1st-derivative)
- [Finding Laplace Transforms Using the Smoothness Property (2nd Derivative)](#finding-laplace-transforms-using-the-smoothness-property-2nd-derivative)
- [Laplace Transforms of Polynomials](#laplace-transforms-of-polynomials)
- [Finding Laplace Transforms of Polynomials](#finding-laplace-transforms-of-polynomials)
- [Proof of the Smoothness Property](#proof-of-the-smoothness-property)
- [Proving the Smoothness Property](#proving-the-smoothness-property)
- [Updated Table of Laplace Transforms](#updated-table-of-laplace-transforms)

## Prerequisites

- [Linearity of Laplace Transforms](<5.1.5. Linearity of Laplace Transforms.md>)
- [Differentiating Under the Integral Sign](<../../../../MVC/2. Multivariable Functions/2.3. Partial Derivatives/Lessons/2.3.9. Differentiating Under the Integral Sign.md>)
- [Laplace Transforms of Piecewise Continuous Functions](<5.1.6. Laplace Transforms of Piecewise Continuous Functions.md>)

---

<a id="introduction"></a>
## Introduction

When studying differential equations, we often face expressions like

$$
t\,e^{kt},\qquad t\,\sin(\omega t), \qquad t^2\,\cosh(kt)
$$

or higher powers of $t$ multiplying familiar functions. The Laplace transform gives a systematic way to handle these by converting “multiply by $t$” into “differentiate with respect to $s$.”

In this topic, we focus on a key regularity fact: even if $f(t)$ has corners or jumps, its Laplace transform $F(s)$ is *perfectly smooth* as a function of $s$ (within its region of convergence). This is called the **smoothness property** (or the **differentiation of transforms** property).

First, let's assume that

- $f(t)$ is *piecewise continuous* on $[MATH: [0,\infty)]$, and
- $f(t)$ is of exponential order $a$ (so $\mid f(t) \mid = O(e^{at})$ as $t\to\infty$).

Then the Laplace transform

$$
F(s)=\mathcal L\{f(t)\}
$$

exists for all $s>a$, and $F$ is *infinitely differentiable* with respect to $s$ on this interval.

Moreover, for every integer

$$
n\ge 0
$$

and every $s>a$,

$$
\mathcal L\{t^n f(t)\}(s)=(-1)^n F^{(n)}(s)
$$

where $F^{(n)}$ denotes the $n$th derivative of $F$ with respect to $s$.

In particular,

$$
\mathcal L\{t f(t)\}=-F'(s), \qquad \mathcal L\{t^2 f(t)\}=F''(s)
$$

This matters because it turns multiplication by powers of $t$ into differentiation with respect to $s$, which is often much faster than re-integrating from scratch or applying integration by parts multiple times.

In the next slide, we will apply this property to work through a concrete example.

---

<a id="a-worked-example"></a>
## A Worked Example

Let's use the smoothness property to find the Laplace transform of $\mathcal L\{t e^{-9t}\}$.

If a function $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and has exponential order $a$, then its Laplace transform

$$
F(s) = \mathcal{L}\{f(t)\}
$$

exists for every real $s > a$, and $F$ is infinitely differentiable in this region of convergence. Moreover, for each integer

$$
n\geq 0
$$

and every such $s$, we have

$$
\mathcal{L}\{t^n f(t) \} = (-1)^n F^{(n)}(s)
$$

In particular, for $n=1$, we have

$$
\mathcal{L}\{tf(t) \} = -F'(s)
$$

We have that

$$
f(t) = e^{-9t}
$$

Therefore, for $s > -9$, we have

$$
F(s) = \dfrac{1}{s+9}
$$

Differentiating with respect to $s$, we have

$$
\begin{aligned}
F^{′}(s) &= \frac{d}{ds}(\frac{1}{s + 9}) \\
&= \frac{d}{ds}((s + 9)^{-1}) \\
&= - (s + 9)^{-2} \\
&= - (1)/((s + 9)^{2})
\end{aligned}
$$

Therefore, by the smoothness property,

$$
\begin{aligned}
L{te^{-9t}} &= -F^{′}(s) \\
&= - [- (1)/((s + 9)^{2})] \\
&= (1)/((s + 9)^{2}), s > - 9
\end{aligned}
$$

---

<a id="a-table-of-laplace-transforms"></a>
## A Table of Laplace Transforms

In previous lessons, we derived several basic Laplace transforms directly from the definition.

| $f(t)$ | $F(s)=\mathcal{L}\left\{f(t)\right\}$ | Domain |
| --- | ---: | ---: |
| $1$ | $\dfrac{1}{s}$ | $s > 0$ |
| $e^{at}$ | $\dfrac{1}{s-a}$ | $s > a$ |
| $e^{\mathrm i \omega t}$ | $\dfrac{1}{s-\mathrm i \omega}$ | $s > 0$ |
| $\sin \omega t$ | $\dfrac{\omega}{s^2+\omega^2}$ | $s > 0$ |
| $\cos \omega t$ | $\dfrac{s}{s^2+\omega^2}$ | $s > 0$ |
| $\sinh \omega t$ | $\dfrac{\omega}{s^2 - \omega^2}$ | $s > \mid \omega \mid$ |
| $\cosh \omega t$ | $\dfrac{s}{s^2-\omega^2}$ | $s > \mid \omega \mid$ |
| $u(t-a), \ a \ge 0$ | $\dfrac{e^{-as}}{s}$ | $s > 0$ |

One goal of this lesson is to expand this table by using the **smoothness property**. In the next slide, we will apply this property to derive a new transform without computing integrals from scratch.

---

<a id="finding-laplace-transforms-using-the-smoothness-property-1st-derivative"></a>
## Finding Laplace Transforms Using the Smoothness Property (1st Derivative)

**Example:** Use the smoothness property to find the Laplace transform of
$\mathcal L\{t \cos 5t\}$.
You may make use of the following result:
$\mathcal L \{\cos \omega t\} = \dfrac{s}{s^2+\omega^2}, \qquad s > 0$.

**Explanation**

If a function $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and has exponential order $a$, then its Laplace transform

$$
F(s) = \mathcal{L}\{f(t)\}
$$

exists for every real $s > a$, and $F$ is infinitely differentiable in this region of convergence. Moreover, for each integer

$$
n\geq 0
$$

and every such $s$, we have

$$
\mathcal{L}\{t^n f(t) \} = (-1)^n F^{(n)}(s)
$$

In particular, for $n=1$, we have

$$
\mathcal{L}\{tf(t) \} = -F'(s)
$$

We have that

$$
f(t) = \cos 5t
$$

Using the given result with $\omega = 5$, the Laplace transform of $f(t)$ is

$$
F(s) = \dfrac{s}{s^2+25}, \qquad s > 0
$$

Differentiating with respect to $s$, we have

$$
\begin{aligned}
F^{′}(s) &= \frac{d}{ds}(\frac{s}{s^{2} + 25}) \\
&= \frac{d}{ds}(s(s^{2} + 25)^{-1}) \\
&= (s^{2} + 25)^{-1} + s(- (s^{2} + 25)^{-2} \cdot 2s) \\
&= (s^{2} + 25)/((s^{2} + 25)^{2}) - (2s^{2})/((s^{2} + 25)^{2}) \\
&= (25 - s^{2})/((s^{2} + 25)^{2})
\end{aligned}
$$

Therefore, by the smoothness property,

$$
L{t\cos 5t} =-F^{′}(s) = (s^{2} - 25)/((s^{2} + 25)^{2}), s > 0
$$

---

**Question 1**

Suppose we wish to calculate the Laplace transform $L{te^{4t}}$ using the smoothness property. Select the correct options in the reasoning below.Let $f(t) = e^{4t}$ and $F(s) = L{f(t)}$. By the smoothness property of Laplace transforms, we have
$-F^{′}(s)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: − F^{′}(s)]
- [ ] [MATH: F^{′}(s)]
- [ ] [MATH: − F(s)]
- [ ] [MATH: − sF^{′}(s)]
- [ ] [MATH: sF^{′}(s)]

---

**Question 2**

Suppose we wish to calculate the Laplace transform
$L{t\sin 4t}$.
Select the correct options in the reasoning below. You may make use of the following result:
$L{\sin ωt} = \frac{ω}{s^{2} + ω^{2}}, s > 0$.Let $f(t) = \sin 4t$ and $L{f(t)} = F(s)$. By the smoothness property of Laplace transforms,
$-F(s)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: − F(s)]
- [ ] [MATH: − sF^{′}(s)]
- [ ] [MATH: F^{′}(s)]
- [ ] [MATH: − F^{′}(s)]
- [ ] [MATH: sF^{′}(s)]

---

<a id="finding-laplace-transforms-using-the-smoothness-property-2nd-derivative"></a>
## Finding Laplace Transforms Using the Smoothness Property (2nd Derivative)

**Example:** Use the smoothness property to find the Laplace transform of
$\mathcal L\{t^2 \sinh{5t}\}$.
You may make use of the following result:
$\mathcal L \{\sinh \omega t\} = \dfrac{\omega}{s^2-\omega^2}, \qquad s > \omega$.

**Explanation**

If a function $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and has exponential order $a$, then its Laplace transform

$$
F(s) = \mathcal{L}\{f(t)\}
$$

exists for every real $s > a$, and $F$ is infinitely differentiable in this region of convergence. Moreover, for each integer

$$
n\geq 0
$$

and every such $s$, we have

$$
\mathcal{L}\{t^n f(t) \} = (-1)^n F^{(n)}(s)
$$

In particular, for $n=2$, we have

$$
\mathcal{L}\{t^2 f(t) \} = F''(s)
$$

We have that

$$
f(t) = \sinh 5t
$$

Using the given result with $\omega = 5$, the Laplace transform of $f(t)$ is

$$
F(s) = \dfrac{5}{s^2-25}, \qquad s > 5
$$

Differentiating with respect to $s$, we have

$$
\begin{aligned}
F^{′}(s) &= \frac{d}{ds}(\frac{5}{s^{2} - 25}) \\
&= \frac{d}{ds}(5(s^{2} - 25)^{-1}) \\
&=-5(s^{2} - 25)^{-2} \cdot 2s \\
&= - (10s)/((s^{2} - 25)^{2}) \\
F^{″}(s) &= \frac{d}{ds}(- (10s)/((s^{2} - 25)^{2})) \\
&=-10 \cdot \frac{d}{ds}((s)/((s^{2} - 25)^{2})) \\
&=-10 \cdot (((s)^{′} \cdot (s^{2} - 25)^{2} - s \cdot ((s^{2} - 25)^{2})^{′})/((s^{2} - 25)^{4})) \\
&=-10 \cdot (((s^{2} - 25)^{2} - s \cdot 2(s^{2} - 25) \cdot 2s)/((s^{2} - 25)^{4})) \\
&=-10 \cdot (((s^{2} - 25)^{2} - 4s^{2}(s^{2} - 25))/((s^{2} - 25)^{4})) \\
&=-10 \cdot ((s^{2} - 25 - 4s^{2})/((s^{2} - 25)^{3})) \\
&=-10 \cdot ((-3s^{2} - 25)/((s^{2} - 25)^{3})) \\
&= (30s^{2} + 250)/((s^{2} - 25)^{3})
\end{aligned}
$$

Note that we used the quotient rule to find $F''(s)$.

Therefore, by the smoothness property,

$$
L{t^{2}\sinh 5t} = F^{″}(s) = (30s^{2} + 250)/((s^{2} - 25)^{3}), s > 5
$$

---

**Question 3**

Suppose we wish to calculate the Laplace transform $L{t^{2}e^{4t}}$ using the smoothness property. Select the correct options in the reasoning below.Let $f(t) = e^{4t}$ and $F(s) = L{f(t)}$. By the smoothness property of Laplace transforms, we have
$-F^{″}(s)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: − F^{″}(s)]
- [ ] [MATH: − s^{2}F^{′}(s)]
- [ ] [MATH: s^{2}F^{″}(s)]
- [ ] [MATH: F^{″}(s)]
- [ ] [MATH: − s^{2}F^{″}(s)]

---

**Question 4**

Suppose we wish to calculate the Laplace transform
$L{t^{2}\sin 4t}$.
Select the correct options in the reasoning below. You may make use of the following result:
$L{\sin ωt} = \frac{ω}{s^{2} + ω^{2}}, s > 0$.Let $f(t) = \sin 4t$ and $F(s) = L{f(t)}$. By the smoothness property of Laplace transforms, we have
$F^{″}(s)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: F^{″}(s)]
- [ ] [MATH: − F^{″}(s)]
- [ ] [MATH: s^{2}F^{″}(s)]
- [ ] [MATH: − s^{2}F^{′}(s)]
- [ ] [MATH: − s^{2}F^{″}(s)]

---

<a id="laplace-transforms-of-polynomials"></a>
## Laplace Transforms of Polynomials

A particularly important special case occurs when

$$
f(t)=1
$$

. Since

$$
\mathcal L\{1\}=\frac{1}{s}, \qquad s>0
$$

we can use the smoothness property to compute **Laplace transforms of powers of $t$** by *repeated differentiation with respect to $s$*.

First, applying the smoothness property once,

$$
\mathcal L\{t\}=-\frac{\mathrm{d}}{\mathrm{d} s}\left(\frac{1}{s}\right) =\frac{1}{s^2}, \qquad s>0
$$

Applying it again,

$$
\mathcal L\{t^2\} =\frac{\mathrm{d}^2}{\mathrm{d} s^2}\left(\frac{1}{s}\right) =\frac{2}{s^3}, \qquad s>0
$$

Applying it a third time (noting that

$$
3 \times 2 = 6 = 3
$$

),

$$
\mathcal L\{t^3\} =-\frac{\mathrm{d}^3}{\mathrm{d} s^3}\left(\frac{1}{s}\right) =\frac{6}{s^4} = \frac{3!}{s^4}, \qquad s>0
$$

From these examples, a clear pattern emerges. For every integer

$$
n\ge 0
$$

,

$$
\mathcal L\{t^n\}=\frac{n!}{s^{n+1}}, \qquad s>0
$$

This formula allows us to compute the Laplace transform of *any polynomial* by combining linearity with this result.

---

<a id="finding-laplace-transforms-of-polynomials"></a>
## Finding Laplace Transforms of Polynomials

**Example:** Given that $\mathcal L\left\{t^n\right\} = \dfrac{n!}{s^{n+1}}$, find the Laplace transform of $f(t) = (t - 5)^2$ for $s > 0$.

**Explanation**

The linearity property of the Laplace transform states that for functions $f(t)$ and $g(t)$, we have

$$
\mathcal L\{\alpha f(t) + \beta g(t)\} = \alpha\cdot \mathcal L\{f(t)\} + \beta\cdot \mathcal L \{g(t)\}
$$

where $\alpha$ and $\beta$ are constants.

First, we expand:

$$
f(t) = (t - 5)^2= t^2 - 10t + 25
$$

Now, taking the Laplace transform and applying the linearity properties, we have

$$
\begin{aligned}
L{f(t)} &= L{t^{2} - 10t + 25} \\
&= L{t^{2}} + L{-10t} + L{25} \\
&= L{t^{2}} - 10L{t} + 25L{1}
\end{aligned}
$$

We're given that

$$
\mathcal L\left\{t^n\right\} = \dfrac{n!}{s^{n+1}}
$$

Therefore, we have the following:

$$
\begin{aligned}
L{t^{2}} &= \frac{2}{s^{3}}, s > 0 \\
L{t} &= \frac{1}{s^{2}}, s > 0 \\
L{1} &= \frac{1}{s}, s > 0
\end{aligned}
$$

Therefore, the Laplace transform of $f(t)$ is

$$
\begin{aligned}
L{f(t)} &= L{t^{2}} - 10L{t} + 25L{1} \\
&= \frac{2}{s^{3}} - 10 \cdot \frac{1}{s^{2}} + 25 \cdot \frac{1}{s} \\
&= \frac{2}{s^{3}} - \frac{10}{s^{2}} + \frac{25}{s} \\
&= \frac{2 - 10s + 25s^{2}}{s^{3}} \\
&= \frac{25s^{2} - 10s + 2}{s^{3}}, s > 0
\end{aligned}
$$

---

**Question 5:** Given that $L{t^{n}} = \frac{n!}{s^{n + 1}}$, find the Laplace transform of $f(t) = t^{2} + 3t - 2$ for $s > 0$.

- [ ] A. $\frac{-s^{2} - s + 3}{s^{3}}$
- [ ] B. $\frac{3s - 2}{s^{2}}$
- [ ] C. $\frac{-2s^{2} + 3s + 2}{s^{3}}$
- [ ] D. $\frac{s^{2} + 2}{s^{2}}$
- [ ] E. $\frac{s^{2} + 2s + 3}{s^{3}}$

---

**Question 6:** Given that $L{t^{n}} = \frac{n!}{s^{n + 1}}$, find the Laplace transform of $f(t) = (2t - 3)^{2}$ for $s > 0$.

- [ ] A. $\frac{9 - 12s + 4s^{2}}{s^{2}}$
- [ ] B. $\frac{3 - s + 3s^{2}}{s^{3}}$
- [ ] C. $\frac{4 - 6s + 3s^{2}}{s^{4}}$
- [ ] D. $\frac{4 - 12s + 9s^{2}}{s^{2}}$
- [ ] E. $\frac{8 - 12s + 9s^{2}}{s^{3}}$

---

<a id="proof-of-the-smoothness-property"></a>
## Proof of the Smoothness Property

Suppose that $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and of exponential
order $a$. This means that there exist constants $K>0$ and $T>0$ such that

$$
\mid f(t) \mid \le K e^{a t} \qquad \text{for all} t > T
$$

We define the Laplace transform of $f$ by

$$
F(s) = \mathcal{L}\{f(t)\}(s) = \int_0^\infty e^{-st} f(t)\,\mathrm{d}t
$$

If $s > a$, then for all sufficiently large $t$,

$$
\mid e^{-st} f(t) \mid \le K e^{-(s-a)t}
$$

Since $s-a>0$, the function $e^{-(s-a)t}$ is integrable on $[MATH: [T,\infty).]$
In addition, because $f$ is piecewise continuous, it is bounded on $[0,T]$.
Together, these observations show that the integral defining $F(s)$ converges,
so $F(s)$ is well-defined for all $s>a$.

Now fix $s>a$ and compute the derivative of $F(s)$. Starting from the definition,

$$
F'(s) = \frac{\mathrm{d}}{\mathrm{d}s} \int_0^\infty e^{-st} f(t)\,\mathrm{d}t
$$

The exponential-order assumption ensures that this integral converges, which allows us to differentiate under the integral sign.
Therefore,

$$
F'(s) = \int_0^\infty \frac{\partial}{\partial s}\big(e^{-st} f(t)\big)\, \mathrm{d}t
$$

Since $f(t)$ does not depend on $s$, applying the chain rule gives

$$
\frac{\partial}{\partial s}\big(e^{-st} f(t)\big) = -t e^{-st} f(t)
$$

Substituting this expression into the integral, we obtain

$$
F'(s) = -\int_0^\infty t e^{-st} f(t)\,\mathrm{d}t
$$

Finally, by definition of the Laplace transform,

$$
\int_0^\infty t e^{-st} f(t)\,\mathrm{d}t = \mathcal{L}\{t f(t)\}(s)
$$

Hence,

$$
F'(s) = -\mathcal{L}\{t f(t)\}(s)
$$

which proves the smoothness property in the case $n=1$.

Repeating this argument shows that the Laplace transform is infinitely differentiable in its region of convergence.

---

<a id="proving-the-smoothness-property"></a>
## Proving the Smoothness Property

**Example:** Suppose that $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and its Laplace transform
$F(s) = \mathcal{L}\{f(t)\}$
exists for $s > a$. Then, $F(s)$ is twice differentiable at every point $s > a$, and we have
$F''(s) = \mathcal{L}\{t^2\cdot f(t) \}$.

A proof of this statement is given below.

> $\textrm{L1}{:}\:$ $F(s) = \displaystyle \int_0^\infty e^{-st} f(t)\,\textrm{d}t$ exists for $s > a$.
> $\textrm{L2}{:}\:$ $F''(s) = \displaystyle \frac{\textrm{d}^2}{\textrm{d}s^2}\int_0^\infty e^{-st} f(t)\,\textrm{d}t$
> $\textrm{L3}{:}\:$ $F''(s) = \displaystyle \int_0^\infty \frac{\partial^2}{\partial s^2}\big(e^{-st} f(t)\big)\,\textrm{d}t$
> $\textrm{L4}{:}\:$ $\displaystyle \frac{\partial^2}{\partial s^2}\big(e^{-st} f(t)\big) = t^2 e^{-st} f(t)$
> $\textrm{L5}{:}\:$ $F''(s) = \displaystyle \int_0^\infty t^2 e^{-st} f(t)\,\textrm{d}t$
> $\textrm{L6}{:}\:$ $F''(s) = \mathcal{L}\{t^2\cdot f(t)\}$

Explain the reasoning behind:

- How line $\textrm{L2}$ follows from line $\textrm{L1}$.
- How line $\textrm{L4}$ follows from its preceding line.
- How line $\textrm{L6}$ follows from $\textrm{L5}$.

**Explanation**

If a function $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$ and has exponential order $a$, then its Laplace transform

$$
F(s) = \mathcal{L}\{f(t)\}
$$

exists for every real $s > a$, and $F$ is infinitely differentiable in this region of convergence.

The smoothness property states that, for each integer

$$
n\geq 0
$$

and $s > a$, we have

$$
F^{(n)}(s) = (-1)^n\, \mathcal{L}\{t^n f(t) \}
$$

We're asked to prove the smoothness property in the case $n = 2$.

With that in mind, let's examine each statement in turn.

- We first consider line $\textrm{L2}$. From line $\textrm{L1}$ we know that
$F(s) = \int_0^\infty e^{-st} f(t)\,\mathrm{d}t$.
By the definition of the second derivative with respect to $s$, we get
$F'(s) = \frac{\mathrm{d}^2}{\mathrm{d}s^2} F(s) = \frac{\mathrm{d}^2}{\mathrm{d}s^2}\int_0^\infty e^{-st} f(t)\,\mathrm{d}t$,
which is exactly the statement in line $\textrm{L2}$.
- We now consider line $\textrm{L4}$. The function $f(t)$ does not depend on $s$, so
$\frac{\partial^2}{\partial s^2}\big(e^{-st} f(t)\big) = f(t)\,\frac{\partial^2}{\partial s^2}(e^{-st}) = f(t)\,\frac{\partial}{\partial s}(-te^{-st}) = f(t)\,(t^2 e^{-st}) = t^2 e^{-st} f(t)$.
Thus line $\textrm{L4}$ follows from the chain rule.
- Finally, we consider line $\textrm{L6}$. By the definition of the Laplace transform,
$\mathcal{L}\{t^2\cdot f(t)\} = \int_0^\infty e^{-st} \big(t^2 f(t)\big)\,\mathrm{d}t$.
Comparing this with the integral in line $\textrm{L5}$, we see that
$F''(s) = \int_0^\infty t^2 e^{-st} f(t)\,\mathrm{d}t = \int_0^\infty e^{-st} \big(t^2 f(t)\big)\,\mathrm{d}t = \mathcal{L}\{t^2\cdot f(t)\}$,
which is exactly the expression in line $\textrm{L6}$.

---

**Question 7**

Suppose that $f(t)$ is piecewise continuous on $[MATH: [0, ∞)]$ and its Laplace transform
$F(s) = L{f(t)}$
exists for $s > a$. Then, $F(s)$ is differentiable at every point $s > a$, and we have
$F^{′}(s) =-L{t \cdot f(t)}$.

A proof of this statement is given below.

> $L1$: $F(s) = ∫_{0}^{∞}e^{-st}f(t)dt$ exists for $s > a$.
> $L2$: $F^{′}(s) = \frac{d}{ds}∫_{0}^{∞}e^{-st}f(t)dt$
> $L3$: $F^{′}(s) = ∫_{0}^{∞}\frac{∂}{∂s}(e^{-st}f(t))dt$
> $L4$: $\frac{∂}{∂s}(e^{-st}f(t)) =-te^{-st}f(t)$
> $L5$: $F^{′}(s) = - ∫_{0}^{∞}te^{-st}f(t)dt$
> $L6$: $F^{′}(s) =-L{t \cdot f(t)}$

Select the correct options in the following reasoning.

The integral in line $L1$ converges since
$\underline{\hspace{2cm}}$.

Line $L3$ follows from $L2$ and $\underline{\hspace{2cm}}$.

Line $L4$ follows from $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] $t\in[0
- [ ] the Laplace transform is linear
- [ ] \infty)$
- [ ] [MATH: e^{− st}] is infinitely differentiable
- [ ] [MATH: f] is of exponential order and is piecewise continuous
- [ ] [MATH: f] is of exponential order

#### Select 2

- [ ] integration by parts
- [ ] differentiating under the integral sign
- [ ] the continuity of partial derivatives

#### Select 3

- [ ] the product rule
- [ ] the chain rule
- [ ] the linearity of the Laplace transform

---

**Question 8**

Suppose that $f(t)$ is piecewise continuous on $[MATH: [0, ∞)]$ and its Laplace transform
$F(s) = L{f(t)}$
exists for $s > a$. Then, $F(s)$ is twice differentiable at every point $s > a$, and we have
$F^{″}(s) = L{t^{2} \cdot f(t)}$.

A proof of this statement is given below.

> $L1$: $F(s) = ∫_{0}^{∞}e^{-st}f(t)dt$ exists for $s > a$.
> $L2$: $F^{″}(s) = \frac{d^{2}}{ds^{2}}∫_{0}^{∞}e^{-st}f(t)dt$
> $L3$: $F^{″}(s) = ∫_{0}^{∞}\frac{∂^{2}}{∂s^{2}}(e^{-st}f(t))dt$
> $L4$: $\frac{∂^{2}}{∂s^{2}}(e^{-st}f(t)) = t^{2}e^{-st}f(t)$
> $L5$: $F^{″}(s) = ∫_{0}^{∞}t^{2}e^{-st}f(t)dt$
> $L6$: $F^{″}(s) = L{t^{2} \cdot f(t)}$

Select the correct options in the following reasoning.

Line $L2$ follows from line $L1$ and
$\underline{\hspace{2cm}}$.

Line $L3$ follows from $L2$ and $\underline{\hspace{2cm}}$.

Line $L4$ follows from $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] the definition of the Laplace transform
- [ ] integration by parts
- [ ] the definition of the second derivative with respect to [MATH: s]
- [ ] differentiating under the integral sign

#### Select 2

- [ ] the continuity of partial derivatives
- [ ] integration by parts
- [ ] differentiating under the integral sign

#### Select 3

- [ ] the linearity of the Laplace transform
- [ ] the product rule
- [ ] the chain rule

---

<a id="updated-table-of-laplace-transforms"></a>
## Updated Table of Laplace Transforms

Using the smoothness property, we can now add transforms involving factors of $t$ and higher powers of $t$ by differentiating previously known formulas.

| $f(t)$ | $F(s)=\mathcal{L}\left\{f(t)\right\}$ | Domain |
| --- | ---: | ---: |
| $t e^{kt}$ | $\dfrac{1}{(s-k)^2}$ | $s>k$ |
| $t^2 e^{kt}$ | $\dfrac{2}{(s-k)^3}$ | $s>k$ |
| $t\sin \omega t$ | $\dfrac{2\omega s}{(s^2+\omega^2)^2}$ | $s>0$ |
| $t^2\sin \omega t$ | $\dfrac{2\omega(3s^2-\omega^2)}{(s^2+\omega^2)^3}$ | $s>0$ |
| $t\cos \omega t$ | $\dfrac{s^2-\omega^2}{(s^2+\omega^2)^2}$ | $s>0$ |
| $t^2\cos \omega t$ | $\dfrac{2s(s^2-3\omega^2)}{(s^2+\omega^2)^3}$ | $s>0$ |
| $t\sinh \omega t$ | $\dfrac{2\omega s}{(s^2-\omega^2)^2}$ | $s> \mid \omega \mid$ |
| $t^2\sinh \omega t$ | $\dfrac{2\omega(3s^2+\omega^2)}{(s^2-\omega^2)^3}$ | $s> \mid \omega \mid$ |
| $t\cosh \omega t$ | $\dfrac{s^2+\omega^2}{(s^2-\omega^2)^2}$ | $s> \mid \omega \mid$ |
| $t^2\cosh \omega t$ | $\dfrac{2s(s^2+3\omega^2)}{(s^2-\omega^2)^3}$ | $s> \mid \omega \mid$ |

Finally, note that we have a shortcut pattern for exponentials. If

$$
\mathcal L\{e^{kt}\}=\frac{1}{s-k}, \qquad s>k
$$

then repeated differentiation shows that

$$
\mathcal L\{t^n e^{kt}\}=\frac{n!}{(s-k)^{n+1}}, \qquad s>k
$$

This pattern is a direct consequence of the smoothness property and provides a fast way to compute many common transforms.

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
