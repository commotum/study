# Laplace Transforms of Derivatives

<!--
lesson-id: 2538
topic-code: DEQ.5.1.8
-->

## Table of Contents

- [Introduction](#introduction)
- [Deriving the Derivative Formula](#deriving-the-derivative-formula)
- [Calculating the Laplace Transform of the Derivative of a Function](#calculating-the-laplace-transform-of-the-derivative-of-a-function)
- [Laplace Transforms of Second Derivatives](#laplace-transforms-of-second-derivatives)
- [Calculating the Laplace Transform of the Second Derivative of a Function](#calculating-the-laplace-transform-of-the-second-derivative-of-a-function)
- [The General Result](#the-general-result)

## Prerequisites

- [Introduction to Laplace Transforms](<5.1.4. Introduction to Laplace Transforms.md>)

---

<a id="introduction"></a>
## Introduction

The Laplace transform is a powerful tool for studying differential equations because it converts differentiation of a function $f(t)$ with respect to $t$ into algebraic operations in the variable $s$.

In this topic, we study how the Laplace transform interacts with derivatives of a function. This leads to another fundamental result, called the **derivative property** of the Laplace transform.

Let's assume that:

- $f(t)$ is *continuous* on $[MATH: [0,\infty)]$,
- $f'(t)$ is *piecewise continuous* on $[MATH: [0,\infty)]$, and
- $f(t)$ is of exponential order $a$.

Then the Laplace transform

$$
F(s) = \mathcal L\{f(t)\}
$$

exists for all $s>a$.

Under these assumptions, the Laplace transform of the *first derivative* of $f$ satisfies a simple and extremely useful formula:

$$
\mathcal L\{f'(t)\} = sF(s) - f(0)
$$

This formula shows that differentiating with respect to $t$ corresponds to multiplying by $s$ *and subtracting an initial-value term*.

For example, let

$$
f(t) = t
$$

We know

$$
\mathcal L\{t\} = \dfrac{1}{s^2}
$$

and we also have

$$
f(0)=0
$$

Using our formula, we get

$$
\begin{aligned}
L{f^{′}(t)} &= sF(s) - f(0) \\
&= s(\frac{1}{s^{2}}) - 0 \\
&= \frac{1}{s}
\end{aligned}
$$

On the other hand, we know $f'(t) = 1$ and

$$
\mathcal L\{1\} = \dfrac{1}{s}
$$

So, we have

$$
\mathcal L\{f'(t)\} = \mathcal L\{1\} = \dfrac{1}{s}
$$

which means that our formula gives the correct result in this case.

We can derive this formula from the definition of the Laplace transform. Let's see how.

---

<a id="deriving-the-derivative-formula"></a>
## Deriving the Derivative Formula

Assume that $f(t)$ is continuous on $[MATH: [0,\infty)]$ and of exponential order $s_0$, and that
$f'(t)$ is piecewise continuous on $[MATH: [0,\infty)]$.

The Laplace transform of $f$ is defined by

$$
\mathcal{L}\{f(t)\} = \int_{0}^{\infty} e^{-st} f(t)\,\textrm{d}t
$$

Replacing $f(t)$ with $f'(t)$ in this definition gives

$$
\mathcal{L}\{f'(t)\} = \int_{0}^{\infty} e^{-st} f'(t)\,\textrm{d}t
$$

Writing the improper integral as a limit of proper integrals, we have

$$
\begin{aligned}
L{f^{′}(t)} &= ∫_{0}^{∞}e^{-st}f^{′}(t)dt \\
&= lim_(a → ∞)∫_{0}^{a}e^{-st}f^{′}(t)dt
\end{aligned}
$$

We now apply integration by parts on $[0,a]$, choosing

$$
u = e^{-st}, \qquad \textrm{d}v = f'(t)\,\textrm{d}t
$$

Then

$$
\textrm{d}u = -s e^{-st}\,\textrm{d}t, \qquad v = f(t)
$$

Using the integration-by-parts formula, we obtain

$$
\begin{aligned}
∫_{0}^{a}e^{-st}f^{′}(t)dt &= e^{-st}f(t) \mid _{0}^{a} - ∫_{0}^{a}(-se^{-st})f(t)dt \\
&= e^{-sa}f(a) - f(0) + s∫_{0}^{a}e^{-st}f(t)dt
\end{aligned}
$$

Substituting this into the previous expression gives

$$
\begin{aligned}
L{f^{′}(t)} &= lim_(a → ∞)(e^{-sa}f(a) - f(0) + s∫_{0}^{a}e^{-st}f(t)dt) \\
&= s∫_{0}^{∞}e^{-st}f(t)dt - f(0) + lim_(a → ∞)e^{-sa}f(a)
\end{aligned}
$$

Since $f$ is of exponential order $s_0$, there exist constants $K>0$ and $T>0$ such that

$$
\mid f(t) \mid \le K e^{s_0 t} \qquad \text{for all} t>T
$$

Therefore, for $s>s_0$,

$$
\mid e^{-sa} f(a) \mid \le K e^{-(s-s_0)a} \longrightarrow 0 \quad \text{as} a\to\infty
$$

Thus, we have established the **derivative property**:

$$
\mathcal{L}\{f'(t)\} = s\,\mathcal{L}\{f(t)\} - f(0), \qquad s>s_0
$$

---

<a id="calculating-the-laplace-transform-of-the-derivative-of-a-function"></a>
## Calculating the Laplace Transform of the Derivative of a Function

**Example:** Find the Laplace transform of $f'(t)$ for $s > 3$ given that the function $f(t)$ is continuous over $[MATH: [0,\infty),]$ $f(0)=0$, and
$\mathcal{L}\left\{f(t)\right\} = \dfrac{6}{s^2-5s+6}, \quad s>3$.
Assume that $f'$ is piecewise continuous on $[MATH: [0,\infty).]$

**Explanation**

Suppose

$$
F(s) = \mathcal L \{f(t)\}
$$

where $f(t)$ is a continuous function of exponential order $a$ on $[MATH: [0,\infty).]$ If $f'(t)$ is piecewise continuous on $[MATH: [0,\infty),]$ then

$$
\mathcal{L}\left\{f'(t) \right\} = s \mathcal{L}\left\{f(t) \right\} - f(0)
$$

Therefore, we can calculate the Laplace transform of $f'(t)$ as follows:

$$
\begin{aligned}
L{f^{′}(t)} &= sL{f(t)} - f(0) \\
&= s \cdot \frac{6}{s^{2} - 5s + 6} - 0 \\
&= \frac{6s}{s^{2} - 5s + 6}
\end{aligned}
$$

valid for $s > 3$.

---

**Question 1:**

```quiz
type: radio
id: ma-64323
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the Laplace transform of $f^{′}(t)$ for $s > 3$ given that the function $f(t)$ is continuous over $[MATH: [0, ∞),]$ $f(0) = 2$, and

  $L{f(t)} = \frac{2s - 1}{s^{2} - s - 6}, s > 3$.
  Assume that $f^{′}$ is piecewise continuous on $[MATH: [0, ∞).]$
options:
- id: a
  content: |-
    $\frac{2s - 6}{s^{2} - s - 6}$
- id: b
  content: |-
    $\frac{s + 12}{s^{2} - s - 6}$
- id: c
  content: |-
    $\frac{2s^{2} - s}{s^{2} - s - 6}$
- id: d
  content: |-
    $\frac{s^{2} + 4s}{s^{2} - s - 6}$
- id: e
  content: |-
    $\frac{s - 4}{s^{2} - s - 6}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-64246
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the Laplace transform of $f^{′}(t)$ for $s > 2$ given that the function $f(t)$ is continuous over $[MATH: [0, ∞),]$ $f(0) = 3$, and

  $L{f(t)} = \frac{3s - 4}{s^{2} - 3s + 2}, s > 2$.
  Assume that $f^{′}$ is piecewise continuous on $[MATH: [0, ∞).]$
options:
- id: a
  content: |-
    $\frac{6s - 2}{s^{2} - 3s + 2}$
- id: b
  content: |-
    $\frac{4s - 3}{s^{2} - 3s + 2}$
- id: c
  content: |-
    $\frac{5s - 6}{s^{2} - 3s + 2}$
- id: d
  content: |-
    $\frac{3s}{s^{2} - 3s + 2}$
- id: e
  content: |-
    $\frac{s - 3}{s^{2} - 3s + 2}$
```

---

<a id="laplace-transforms-of-second-derivatives"></a>
## Laplace Transforms of Second Derivatives

In differential equations, second derivatives arise naturally when modeling acceleration, curvature, or restoring forces. To use Laplace transforms effectively in these settings, we need to understand how the transform handles *higher-order derivatives*.

As with the first derivative, the key idea is that differentiation with respect to $t$ becomes an algebraic operation in $s$, together with terms that depend on initial values. We now derive the corresponding formula for the second derivative.

Assume that

- $y(t)$ is *continuous* on $[MATH: [0,\infty)]$ and of exponential order $s_0$,
- $y'(t)$ is *continuous* on $[MATH: [0,\infty)]$,
- $y''(t)$ is *piecewise continuous* on $[MATH: [0,\infty)]$.

Then the Laplace transform of $y''(t)$ exists for all $s>s_0$, and

$$
\mathcal{L}\{y''(t)\} = s^2 \mathcal{L}\{y(t)\} - s y(0) - y'(0)
$$

To derive this result, recall the formula for the Laplace transform of a first derivative:

$$
\mathcal{L}\{y'(t)\} = s \mathcal{L}\{y(t)\} - y(0), \qquad s > s_0
$$

Since $y'(t)$ is continuous and $y''(t)$ is piecewise continuous, we may apply this formula to $y'(t)$:

$$
\mathcal{L}\{y''(t)\} = s \mathcal{L}\{y'(t)\} - y'(0)
$$

Substituting

$$
\mathcal{L}\{y'(t)\} = s \mathcal{L}\{y(t)\} - y(0)
$$

, we obtain

$$
\begin{aligned}
L{y^{″}(t)} &= s[sL{y(t)} - y(0)] - y^{′}(0) \\
&= s^{2}L{y(t)} - sy(0) - y^{′}(0)
\end{aligned}
$$

as claimed.

---

<a id="calculating-the-laplace-transform-of-the-second-derivative-of-a-function"></a>
## Calculating the Laplace Transform of the Second Derivative of a Function

**Example:** Find the Laplace transform $\mathcal{L}\left\{f''(t) \right\}$ for $s > 0$, given that the function $f(t)$ is continuous on $[MATH: [0,\infty),]$ $f'(0)=2$, $f(0)=-1$, and
$\mathcal{L}\left\{f(t)\right\} = \dfrac{2 - s}{s^2+4}, \quad s>0$.
Assume that $f'$ is continuous and $f''$ piecewise continuous on $[MATH: [0,\infty).]$

**Explanation**

Suppose

$$
F(s) = \mathcal L \{f(t)\}
$$

where $f(t)$ is a continuous function of exponential order $a$ on $[MATH: [0,\infty).]$ If $f'(t)$ is continuous and $f''(t)$ is piecewise continuous on $[MATH: [0,\infty),]$ then

$$
\begin{aligned}
L{f^{″}(t)} &= s^{2}L{f(t)} - sf(0) - f^{′}(0)
\end{aligned}
$$

Therefore, we can calculate the Laplace transform of $f''(t)$ as follows:

$$
\begin{aligned}
L{f^{″}(t)} &= s^{2}L{f(t)} - sf(0) - f^{′}(0) \\
&= s^{2} \cdot \frac{2 - s}{s^{2} + 4} - s \cdot (-1) - 2 \\
&= (s^{2}(2 - s))/(s^{2} + 4) + s - 2 \\
&= - (s^{2}(s - 2))/(s^{2} + 4) + (s - 2) \\
&= (s - 2)(-\frac{s^{2}}{s^{2} + 4} + 1) \\
&= (s - 2)(\frac{4}{s^{2} + 4}) \\
&= (4(s - 2))/(s^{2} + 4)
\end{aligned}
$$

valid for $s > 0$.

---

**Question 3:**

```quiz
type: radio
id: ma-64288
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the Laplace transform $L{f^{″}(t)}$ for $s > 0$, given that the function $f(t)$ is continuous on $[MATH: [0, ∞),]$ $f^{′}(0) =-1$, $f(0) = 1$, and

  $L{f(t)} = \frac{s - 1}{s^{2} + 1}, s > 0$.
  Assume that $f^{′}$ is continuous and $f^{″}$ piecewise continuous on $[MATH: [0, ∞).]$
options:
- id: a
  content: |-
    $(2 - s)/(s(s^{2} + 1))$
- id: b
  content: |-
    $\frac{s}{s^{2} + 1}$
- id: c
  content: |-
    $\frac{s + 2}{s^{2} + 1}$
- id: d
  content: |-
    $(1)/(s(s^{2} + 1))$
- id: e
  content: |-
    $\frac{1 - s}{s^{2} + 1}$
```

---

**Question 4:**

```quiz
type: radio
id: ma-64405
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the Laplace transform $L{f^{″}(t)}$ for $s > 0$, given that the function $f(t)$ is continuous on $[MATH: [0, ∞),]$ $f^{′}(0) = 2$, $f(0) = 1$, and

  $L{f(t)} = \frac{s + 2}{s^{2} + 4}, s > 0$.
  Assume that $f^{′}$ is continuous and $f^{″}$ piecewise continuous on $[MATH: [0, ∞).]$
options:
- id: a
  content: |-
    $- (4(s + 2))/(s^{2} + 4)$
- id: b
  content: |-
    $-\frac{s + 4}{s^{2} + 4}$
- id: c
  content: |-
    $\frac{4s}{s^{2} + 4}$
- id: d
  content: |-
    $-\frac{2s + 1}{s^{2} + 4}$
- id: e
  content: |-
    $\frac{4s - 1}{s^{2} + 4}$
```

---

<a id="the-general-result"></a>
## The General Result

If $f(t)$ is piecewise continuous on $[MATH: [0,\infty)]$, of exponential order $a$, and

$$
F(s)=\mathcal L\{f(t)\}
$$

then for any integer

$$
n\ge 1
$$

and all $s>a$,

$$
\mathcal L\{f^{(n)}(t)\} = s^n F(s) - s^{n-1} f(0) - s^{n-2} f'(0) - \cdots - f^{(n-1)}(0)
$$

This formula is the key tool for systematically solving linear differential equations with given initial conditions using Laplace transforms.

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
