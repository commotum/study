# The Second Shifting Theorem of Laplace Transforms

<!--
lesson-id: 6370
topic-code: DEQ.5.1.11
-->

## Table of Contents

- [Introduction](#introduction)
- [Deriving the Second Shifting Theorem](#deriving-the-second-shifting-theorem)
- [Calculating Laplace Transforms Using the Second Shifting Theorem](#calculating-laplace-transforms-using-the-second-shifting-theorem)
- [Laplace Transforms of Trig Functions Using the Second Shifting Theorem](#laplace-transforms-of-trig-functions-using-the-second-shifting-theorem)
- [Alternative Form of the Second Shifting Theorem](#alternative-form-of-the-second-shifting-theorem)
- [Using the Alternative Form of the Second Shifting Theorem](#using-the-alternative-form-of-the-second-shifting-theorem)

## Prerequisites

- [Calculating Trigonometric Ratios Using the Sum Formula for Sine](<../../../../PCL/4. Trigonometry/4.3. The Sum and Difference Formulas/Lessons/4.3.4. Calculating Trigonometric Ratios Using the Sum Formula for Sine.md>)
- [Calculating Trigonometric Ratios Using the Sum Formula for Cosine](<../../../../PCL/4. Trigonometry/4.3. The Sum and Difference Formulas/Lessons/4.3.5. Calculating Trigonometric Ratios Using the Sum Formula for Cosine.md>)
- [The Smoothness Property of Laplace Transforms](<5.1.7. The Smoothness Property of Laplace Transforms.md>)

---

<a id="introduction"></a>
## Introduction

The **unit step function** $u(t-a)$ acts like a switch:

- If $t < a$, then $u(t-a)=0$.
- If $t \ge a$, then $u(t-a)=1$.

So, when we multiply a function $g(t)$ by $u(t-a)$, the function becomes

$$
{0,\begin{vmatrix}t < a, \\ g(t),\end{vmatrix}t \ge a
$$

This operation "switches on" the function $g(t)$ at time $t=a$.

For example, consider the function

$$
y(t)=u(t-2) \cdot \dfrac14 t^2
$$

Here, $a=2$ and

$$
g(t) = \dfrac{1}{4}t^2
$$

so

$$
{0,\begin{vmatrix}t < 2, \\ \frac{1}{4}t^{2},\end{vmatrix}t \ge 2
$$

The plot of this function is shown below. Note that the curve follows the parabola for

$$
t \ge 2
$$

but is zero for $t < 2$.

![](<../Source/5.1.11. The Second Shifting Theorem of Laplace Transforms/Images/0099a1ba0e0a991be8c83eaec4015e90.png>)

Suppose we know the Laplace transform of $g(t)$. We'll now learn how to find the Laplace transform of $u(t-a)\,g(t)$.

---

<a id="deriving-the-second-shifting-theorem"></a>
## Deriving the Second Shifting Theorem

From the definition of the Laplace transform, we have

$$
\mathcal{L}\{u(t-a)\,g(t)\} =\int_0^\infty e^{-st}u(t-a)g(t)\,\textrm{d}t
$$

Because $u(t-a)=0$ for $t < a$ and $u(t-a)=1$ for

$$
t\ge a
$$

, the integrand is zero until $t=a$.
So the integral reduces to

$$
\mathcal{L}\{u(t-a)\,g(t)\} =\int_a^\infty e^{-st}g(t)\,\textrm{d}t
$$

To compare this with a standard Laplace transform, we shift the variable of integration so that the lower limit is $0$.

Let $\tau=t-a$. Then

$$
t=\tau+a
$$

and

$$
\textrm{d}t=\textrm{d}\tau
$$

. Also, when $t=a$, we have $\tau=0$.

Substituting gives

$$
\int_a^\infty e^{-st}g(t)\,\textrm{d}t =\int_0^\infty e^{-s(\tau+a)}g(\tau+a)\,\textrm{d}\tau
$$

Now separate the exponential term:

$$
e^{-s(\tau+a)}=e^{-as}e^{-s\tau}
$$

This allows us to factor out the constant $e^{-as}$:

$$
\int_0^\infty e^{-s(\tau+a)}g(\tau+a)\,\textrm{d}\tau =e^{-as}\int_0^\infty e^{-s\tau}g(\tau+a)\,\textrm{d}\tau
$$

The remaining integral has exactly the form of a Laplace transform, so recognize

$$
\int_0^\infty e^{-s\tau}g(\tau+a)\,\textrm{d}\tau =\mathcal{L}\{g(t+a)\}
$$

Putting everything together, we obtain the **second shifting theorem**:

$$
\mathcal{L}\{u(t-a)\,g(t)\} =e^{-as}\,\mathcal{L}\{g(t+a)\}, \qquad \text{for} s>s_0
$$

The key takeaway is that the unit step shifts the *start time* of the function to $t=a$, and changing variables shifts it back to $0$. The exponential factor $e^{-as}$ records this delay in the $s$-domain.

---

<a id="calculating-laplace-transforms-using-the-second-shifting-theorem"></a>
## Calculating Laplace Transforms Using the Second Shifting Theorem

**Example:** Given that $\mathcal{L} \big\{t^n\big\}=\dfrac{n!}{s^{n+1}}$ for $s>0$, find $\mathcal{L}\left\{u(t-2)\big(t^2+3t+2\big) \right\}$, where $u(t)$ is the unit step function.

**Explanation**

The second shifting theorem states that if

$$
a \ge 0
$$

and $\mathcal{L} \big\{u(t-a)\,g(t)\big\}$ exists for $s > s_0$, then

$$
\mathcal{L} \big\{u(t-a)\,g(t)\big\} = e^{-as}\,\mathcal{L}\big\{g(t+a)\big\}, \qquad \text{for} \; s > s_0
$$

where $u(t)$ is the unit step function.

In our case, we wish to find

$$
\mathcal{L}\left\{u(t-2)\big(t^2+3t+2\big) \right\}
$$

Therefore, $a=2$, and we have

$$
\mathcal{L} \big\{u(t-2)\,g(t)\big\} = e^{-2s}\,\mathcal{L}\big\{g(t+2)\big\}
$$

where

$$
g(t)=t^2+3t+2
$$

Notice that

$$
\begin{aligned}
g(t + 2) &= (t + 2)^{2} + 3(t + 2) + 2 \\
&= t^{2} + 4t + 4 + 3t + 6 + 2 \\
&= t^{2} + 7t + 12
\end{aligned}
$$

Since

$$
\mathcal{L} \big\{t^n\big\}=\dfrac{n!}{s^{n+1}}
$$

and

$$
\mathcal{L} \{1\} = \dfrac{1}{s}
$$

for $s>0$, we have

$$
\begin{aligned}
L{g(t + 2)} &= L{t^{2} + 7t + 12} \\
&= L{t^{2}} + 7L{t} + 12L{1} \\
&= \frac{2}{s^{3}} + 7 \cdot \frac{1}{s^{2}} + 12 \cdot \frac{1}{s} \\
&= \frac{2}{s^{3}} + \frac{7}{s^{2}} + \frac{12}{s}
\end{aligned}
$$

Therefore, applying the second shifting theorem, we get

$$
\begin{aligned}
L{u(t - 2)g(t)} &= e^{-2s}L{g(t + 2)} \\
&= e^{-2s}(\frac{2}{s^{3}} + \frac{7}{s^{2}} + \frac{12}{s})
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-331016
content: |-
  Given that $L{t^{n}} = \frac{n!}{s^{n + 1}}$ for $s > 0$, find $L{u(t - 5)(t^{2} + 4t + 1)}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $e^{-s}(\frac{2}{s^{3}} + \frac{14}{s^{2}} + \frac{46}{s})$
- id: b
  content: |-
    $e^{-5s}(\frac{2}{s^{3}} + \frac{14}{s^{2}})$
- id: c
  content: |-
    $e^{-5s}(\frac{1}{s^{3}} + \frac{4}{s^{2}} + \frac{1}{s})$
- id: d
  content: |-
    $e^{-5s}(\frac{2}{s^{3}} + \frac{14}{s^{2}} + \frac{46}{s})$
  correct: true
- id: e
  content: |-
    $e^{5s}(\frac{2}{s^{3}} - \frac{14}{s^{2}})$
```

---

**Question 2:**

```quiz
type: radio
id: ma-326384
content: |-
  Given that $L{t^{n}} = \frac{n!}{s^{n + 1}}$ for $s > 0$, find $L{u(t - 1)(t^{2} + 1)}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $e^{-s}(\frac{2}{s^{3}} + \frac{2}{s^{2}} + \frac{2}{s})$
  correct: true
- id: b
  content: |-
    $e^{2s}(\frac{2}{s^{3}} - \frac{2}{s^{2}})$
- id: c
  content: |-
    $e^{-s}(\frac{2}{s^{3}} + \frac{2}{s^{2}})$
- id: d
  content: |-
    $e^{-s}(\frac{1}{s^{3}} - \frac{1}{s^{2}} - \frac{2}{s})$
- id: e
  content: |-
    $e^{-s}(\frac{1}{s^{3}} + \frac{1}{s^{2}} + \frac{1}{s})$
```

---

<a id="laplace-transforms-of-trig-functions-using-the-second-shifting-theorem"></a>
## Laplace Transforms of Trig Functions Using the Second Shifting Theorem

**Example:** Given that
$\begin{bmatrix}L{\sin ωt}|= \frac{ω}{s^{2} + ω^{2}}, & s > 0, \\ L{\cos ωt}|= \frac{s}{s^{2} + ω^{2}}, & s > 0\end{bmatrix}$,
find $\mathcal{L}\left\{u(t-4)\,\cos(9\pi t) \right\}$, where $u(t)$ is the unit step function.

**Explanation**

The second shifting theorem states that if

$$
a \ge 0
$$

and $\mathcal{L} \big\{u(t-a)\,g(t)\big\}$ exists for $s > s_0$, then

$$
\mathcal{L} \big\{u(t-a)\,g(t)\big\} = e^{-as}\,\mathcal{L}\big\{g(t+a)\big\}, \qquad \text{for} \; s > s_0
$$

where $u(t)$ is the unit step function.

In our case, we wish to find

$$
\mathcal{L}\left\{u(t -4)\,\cos(9\pi t) \right\}
$$

Therefore, $a=4$, and we have

$$
\mathcal{L} \big\{u(t-4)\,g(t)\big\} = e^{-4s}\,\mathcal{L}\big\{g(t+4)\big\}
$$

where

$$
g(t)= \cos(9\pi t)
$$

Notice that

$$
\begin{aligned}
g(t + 4) &= \cos(9π(t + 4)) \\
&= \cos (9πt + 36π) \\
&= \cos 9πt\cos 36π - \sin 9πt\sin 36π \\
&= \cos 9πt \cdot 1 - \sin 9πt \cdot 0 \\
&= \cos 9πt
\end{aligned}
$$

Since

$$
\mathcal{L} \left\{\cos(\omega t)\right\}=\dfrac{s}{s^2 + \omega^2}
$$

for $s>0$, we have

$$
\begin{aligned}
L{g(t + 4)} &= L{\cos (9πt)} \\
&= \frac{s}{s^{2} + 81π^{2}}
\end{aligned}
$$

Therefore, applying the second shifting theorem, we get

$$
\begin{aligned}
L{u(t - 4)g(t)} &= e^{-4s}L{g(t + 4)} \\
&= e^{-4s} \cdot \frac{s}{s^{2} + 81π^{2}}
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-331035
content: |-
  Given that

  $\begin{bmatrix}L{\sin ωt}|= \frac{ω}{s^{2} + ω^{2}}, & s > 0, \\ L{\cos ωt}|= \frac{s}{s^{2} + ω^{2}}, & s > 0\end{bmatrix}$,
  find $L{u(t - 4)\sin (10πt)}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $e^{-4s} \cdot \frac{10π}{s^{2} + 25π^{2}}$
- id: b
  content: |-
    $e^{-4s} \cdot \frac{10π}{s^{2} + 100π^{2}}$
  correct: true
- id: c
  content: |-
    $e^{-4s} \cdot \frac{s}{s^{2} + 100π^{2}}$
- id: d
  content: |-
    $e^{4s} \cdot \frac{10π}{s^{2} + 100π^{2}}$
- id: e
  content: |-
    $e^{4s} \cdot \frac{s}{s^{2} + 100π^{2}}$
```

---

**Question 4:**

```quiz
type: radio
id: ma-331005
content: |-
  Given that

  $\begin{bmatrix}L{\sin ωt}|= \frac{ω}{s^{2} + ω^{2}}, & s > 0, \\ L{\cos ωt}|= \frac{s}{s^{2} + ω^{2}}, & s > 0\end{bmatrix}$,
  find $L{u(t - 1)\cos (3πt)}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $-e^{-s} \cdot \frac{s}{s^{2} + 9π^{2}}$
  correct: true
- id: b
  content: |-
    $e^{-s} \cdot \frac{s}{s^{2} + 9π^{2}}$
- id: c
  content: |-
    $e^{-s} \cdot \frac{3π}{s^{2} + 9π^{2}}$
- id: d
  content: |-
    $e^{-3s} \cdot \frac{s}{s^{2} + 9π^{2}}$
- id: e
  content: |-
    $-e^{-s} \cdot \frac{3π}{s^{2} + 9π^{2}}$
```

---

<a id="alternative-form-of-the-second-shifting-theorem"></a>
## Alternative Form of the Second Shifting Theorem

We begin with the second shifting theorem:

$$
\mathcal{L}\{u(t-a) g(t)\} = e^{-as}\,\mathcal{L}\{g(t+a)\}
$$

In many applications, the function multiplied by the unit step already appears in the shifted form
$u(t-a)\,f(t-a)$.
Our goal is to rewrite the theorem so that this structure can be handled directly.

Suppose $f(t)$ is a function with Laplace transform

$$
\mathcal{L}\{f(t)\}=F(s)
$$

To match the form $u(t-a)f(t-a)$, define

$$
g(t)=f(t-a)
$$

Then the left-hand side becomes

$$
\mathcal{L}\{u(t-a) g(t)\} =\mathcal{L}\{u(t-a)\,f(t-a)\}
$$

Next, determine what happens to the right-hand side.
If $g(t)=f(t-a)$, then

$$
g(t+a)=f\big((t+a)-a\big)=f(t)
$$

So,

$$
\mathcal{L}\{g(t+a)\}=\mathcal{L}\{f(t)\}=F(s)
$$

Substituting these results into the theorem gives

$$
\mathcal{L}\{u(t-a)\,f(t-a)\} = e^{-as}F(s)
$$

This is the **alternative form of the second shifting theorem**.

Next, we will apply this formula to a concrete example.

---

<a id="using-the-alternative-form-of-the-second-shifting-theorem"></a>
## Using the Alternative Form of the Second Shifting Theorem

**Example:** Find $\mathcal{L}\left\{u(t-1)(t-1)^3 \right\}$, where $u(t)$ is the unit step function.

**Explanation**

The second shifting theorem states that if

$$
a \ge 0
$$

then

$$
\mathcal{L} \big\{u(t-a)\,f(t-a)\big\} = e^{-as}F(s)
$$

where

$$
F(s) = \mathcal{L}\{f(t)\}
$$

In our case, we wish to find the transform of
$u(t-1)(t-1)^3$.

By comparing this expression to the form in the theorem, we can identify the following:

- The shift is $a = 1$.
- The shifted function is $f(t-1) = (t-1)^3$.

From the shifted function, we can deduce the original unshifted function $f(t)$ by simply replacing $(t-1)$ with $t{:}$

$$
f(t) = t^3
$$

Now, we calculate the Laplace transform $F(s)$ of the unshifted function using the rule

$$
\mathcal{L}\{t^n\} = \dfrac{n!}{s^{n+1}}{:}
$$

$$
F(s) = \mathcal{L}\{t^3\} = \dfrac{3!}{s^{3+1}} = \dfrac{6}{s^4}
$$

Finally, we apply the shifting theorem formula

$$
\mathcal{L} \big\{u(t-a)\,f(t-a)\big\} = e^{-as}F(s)
$$

by multiplying $F(s)$ by the exponential delay factor $e^{-s}{:}$

$$
\begin{aligned}
L{u(t - 1)(t - 1)^{3}} &= e^{-s} \cdot F(s) \\
&= e^{-s} \cdot \frac{6}{s^{4}}
\end{aligned}
$$

---

**Question 5:**

```quiz
type: radio
id: ma-342319
content: |-
  Find $L{u(t - 2)(t - 2)^{2}}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $e^{-2s} \cdot \frac{2}{s^{3}}$
  correct: true
- id: b
  content: |-
    $e^{-2s} \cdot \frac{1}{s^{3}}$
- id: c
  content: |-
    $e^{-2s} \cdot (2)/((s + 2)^{3})$
- id: d
  content: |-
    $e^{-2s} \cdot \frac{2}{s^{2}}$
- id: e
  content: |-
    $e^{2s} \cdot \frac{2}{s^{3}}$
```

---

**Question 6:**

```quiz
type: radio
id: ma-342318
content: |-
  Find $L{u(t - 3)e^{-2(t - 3)}}$, where $u(t)$ is the unit step function.
options:
- id: a
  content: |-
    $e^{-2s} \cdot \frac{1}{s + 3}$
- id: b
  content: |-
    $e^{-3s} \cdot \frac{1}{s + 3}$
- id: c
  content: |-
    $e^{-2s} \cdot \frac{1}{s + 2}$
- id: d
  content: |-
    $e^{-3s} \cdot \frac{1}{s + 2}$
  correct: true
- id: e
  content: |-
    $e^{-3s} \cdot \frac{1}{s - 2}$
```

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
