# Laplace Transforms of Integrals

<!--
lesson-id: 6372
topic-code: DEQ.5.1.9
-->

## Table of Contents

- [Introduction](#introduction)
- [The General Result](#the-general-result)
- [Finding the Laplace Transform of an Integral](#finding-the-laplace-transform-of-an-integral)
- [Finding an Inverse Laplace Transform Using the Integral Rule](#finding-an-inverse-laplace-transform-using-the-integral-rule)
- [Proving the Formula for Laplace Transforms of Integrals](#proving-the-formula-for-laplace-transforms-of-integrals)

## Prerequisites

- [Introduction to Integration by Parts](<../../../../CBC/8. Techniques of Integration/8.4. Integration by Parts/Lessons/8.4.1. Introduction to Integration by Parts.md>)
- [The Integral as an Accumulation Function](<../../../../CAB/6. Integration/6.5. Accumulation Functions/Lessons/6.5.1. The Integral as an Accumulation Function.md>)
- [Inverse Laplace Transforms](<5.1.12. Inverse Laplace Transforms.md>)

---

<a id="introduction"></a>
## Introduction

Suppose a quantity increases over time at a rate proportional to its current value, such as the balance of a bank account growing with continuous interest. The Laplace transform can be used to analyze how such exponential growth behaves, especially when the function is **integrated over time** to represent total accumulation.

To illustrate this, we’ll find the Laplace transform of a function defined as the integral of an exponential function. This shows how *integration in the time domain* corresponds to *division by $s$* in the $s$-domain.

Suppose we are given that

$$
\mathcal{L}\{e^{at}\} = \dfrac{1}{s-a}, \qquad s>a
$$

Let's find the Laplace transform of

$$
g(t) = \int_{0}^{t} e^{\tau/2}\,\mathrm{d}\tau, \qquad s > \dfrac{1}{2}
$$

We apply the **Integration in Time Property**: if

$$
\mathcal{L}\{f(t)\} = F(s)
$$

, then

$$
\mathcal{L}\!\left\{\int_{0}^{t} f(\tau)\,\mathrm{d}\tau \right\} = \frac{F(s)}{s}
$$

In our case,

$$
f(t)=e^{t/2}
$$

. Thus, for

$$
s>\dfrac{1}{2}
$$

$$
\begin{aligned}
F(s) &= L{f(t)} \\
&= L{e^{t/2}} \\
&= (1)/((s - \frac{1}{2}))
\end{aligned}
$$

Therefore, applying the integral identity above, we have for

$$
s>\dfrac{1}{2}
$$

$$
\begin{aligned}
L{g(t)} &= L{∫_{0}^{t}e^{τ/2}dτ} \\
&= (F(s))/(s) \\
&= (1)/(s(s - \frac{1}{2})) \\
&= (2)/(s(2s - 1))
\end{aligned}
$$

---

<a id="the-general-result"></a>
## The General Result

Assume that

- $f(t)$ is *piecewise continuous* on $[MATH: [0,\infty)]$, and
- $f(t)$ is of exponential order $a$.

Then the Laplace transform

$$
F(s)=\mathcal L\{f(t)\}
$$

exists for all $s>a$, and we have the **Integration Property**:

$$
\mathcal L\!\left\{\int_0^t f(\tau)\,\textrm{d}\tau\right\} = \frac{F(s)}{s}, \qquad s>a
$$

This result shows that *integrating a function with respect to time* corresponds to *dividing its Laplace transform by $s$*.

This result allows us to handle accumulated quantities without re-integrating from scratch.

Next, we will apply this property to a concrete example.

---

<a id="finding-the-laplace-transform-of-an-integral"></a>
## Finding the Laplace Transform of an Integral

**Example:** Given that $\mathcal{L}\{\cos(\omega t)\} = \dfrac{s}{s^2+\omega^2}$ for $s>0$, find the Laplace transform of
$\displaystyle g(t)= \int_{0}^{t} \cos\big(\pi\tau\big) \, \textrm{d}\tau, \quad s>0$.

**Explanation**

We know that if

$$
\mathcal{L}\{f(t)\} = F(s)
$$

, then

$$
\mathcal{L}\left\{\int_{0}^{t} f(\tau) \, \text{d}\tau \right\} = \frac{F(s)}{s}
$$

In our case,

$$
f(t) = \cos(\pi t)
$$

Thus, for $s > 0$,

$$
\begin{aligned}
F(s) &= L{f(t)} \\
&= L{\cos (πt)} \\
&= \frac{s}{s^{2} + π^{2}}
\end{aligned}
$$

Therefore, applying the integral identity above, we have that for $s > 0$,

$$
\begin{aligned}
L{g(t)} &= L{∫_{0}^{t}\cos (πτ)dτ} \\
&= (F(s))/(s) \\
&= \frac{1}{s^{2} + π^{2}}
\end{aligned}
$$

---

**Question 1:** Given that $L{t^{n}} = \frac{n!}{s^{n + 1}}$ for $s > 0$, find the Laplace transform of
$g(t) = ∫_{0}^{t}τ^{2}dτ$
for $s > 0$.

- [ ] A. $\frac{6}{s^{4}}$
- [ ] B. $\frac{2}{s^{4}}$
- [ ] C. $\frac{2}{s^{3}}$
- [ ] D. $\frac{6}{s^{3}}$
- [ ] E. $\frac{1}{s^{3}}$

---

**Question 2:** Given that $L{e^{at}} = \frac{1}{s - a}$ for $s > a$, find the Laplace transform of
$g(t) = ∫_{0}^{t}e^{2τ}dτ$
for $s > 2$.

- [ ] A. $\frac{2}{s}$
- [ ] B. $(1)/(s(s - 2))$
- [ ] C. $\frac{s}{s - 2}$
- [ ] D. $\frac{1}{s - 2}$
- [ ] E. $\frac{2}{s - 2}$

---

<a id="finding-an-inverse-laplace-transform-using-the-integral-rule"></a>
## Finding an Inverse Laplace Transform Using the Integral Rule

**Example:** For the function
$G(s) = \dfrac{1}{s(s-1)}, \qquad s > 1$
find the inverse Laplace transform of $G(s)$, given that
$\mathcal{L}\{e^{at}\}=\dfrac{1}{s-a},\ s>a$.

**Explanation**

Notice that we can write $G(s)$ as

$$
G(s) = \frac{1}{s(s-1)} = \frac{F(s)}{s}
$$

where

$$
F(s) = \dfrac{1}{s-1}
$$

Also, we know that

$$
\mathcal{L}\{e^{at}\}=\frac{1}{s-a}
$$

Thus, we get

$$
\begin{aligned}
L^{-1}{F(s)} &= L^{-1}{\frac{1}{s - 1}} \\
&= e^{t}
\end{aligned}
$$

Additionally, we know that if

$$
\mathcal{L}\{f(t)\} = F(s)
$$

, then

$$
\mathcal{L}\left\{\int_{0}^{t} f(\tau)\, \text{d}\tau \right\} = \frac{F(s)}{s}
$$

Using the property above in reverse, we obtain

$$
\begin{aligned}
L^{-1}{G(s)} &= L^{-1}{(F(s))/(s)} \\
&= ∫_{0}^{t}f(τ)dτ \\
&= ∫_{0}^{t}e^{τ}dτ \\
&= e^{τ} \mid _{0}^{t} \\
&= e^{t} - 1
\end{aligned}
$$

---

**Question 3:** For the function
$G(s) = \frac{2}{s^{4}}, s > 0$
find the inverse Laplace transform of $G(s)$, given that
$L{t^{n}} = \frac{n!}{s^{n + 1}}$.

- [ ] A. $\frac{2}{t^{3}}$
- [ ] B. $\frac{t^{3}}{3}$
- [ ] C. $\frac{t^{4}}{12}$
- [ ] D. $t^{2}$
- [ ] E. $\frac{t^{2}}{2}$

---

**Question 4:** For the function
$G(s) = (1)/(s(s - 3)), s > 3$
find the inverse Laplace transform of $G(s)$, given that
$L{e^{at}} = \frac{1}{s - a}, s > a$.

- [ ] A. $e^{3t}$
- [ ] B. $\frac{1}{3}(e^{t} - 1)$
- [ ] C. $\frac{1}{3}(e^{3t} - 1)$
- [ ] D. $\frac{1}{3}(1 - e^{3t})$
- [ ] E. $\frac{1}{t - 3}$

---

<a id="proving-the-formula-for-laplace-transforms-of-integrals"></a>
## Proving the Formula for Laplace Transforms of Integrals

**Example:** Prove that if $\mathcal{L}\{f(t)\} = F(s)$, then
$\mathcal{L}\left\{\int_{0}^{t} f(\tau)\, d\tau \right\} = \frac{F(s)}{s}$.

**Explanation**

Let $A(t)$ be the anti-derivative of $f(t)$ such that

$$
A'(t) = f(t)
$$

and $A(0) ={0}$. Then,

$$
\begin{aligned}
∫_{0}^{t}f(τ)dτ &= A(τ) \mid _{0}^{t} \\
&= A(t) - A(0) \\
&= A(t)
\end{aligned}
$$

By definition of the Laplace transform, we can write

$$
\begin{aligned}
L{∫_{0}^{t}f(τ)dτ} &= L{A(t)} = ∫_{0}^{∞}e^{-st}A(t)dt
\end{aligned}
$$

Now, let's use integration by parts:

$$
\begin{bmatrix}u = A(t) & ⇒ & du = A^{′}(t)dt \\ dv = e^{-st}dt & ⇒ & v =-\frac{1}{s}e^{-st}\end{bmatrix}
$$

Substituting into the integration by parts formula, we get

$$
\begin{aligned}
∫_{0}^{∞}e^{-st}A(t)dt &= A(t)(-\frac{1}{s}e^{-st}) \mid _{0}^{∞} - ∫_{0}^{∞}(-\frac{1}{s}e^{-st})A^{′}(t)dt
\end{aligned}
$$

Since $A(0)=0$ and

$$
\dfrac{1}{s}e^{-st} \to 0
$$

as $t \to \infty$, we have

$$
-\dfrac{1}{s}A(t)e^{-st}\bigg \mid _0^{\infty} = {0}
$$

and

$$
\int_0^{\infty} e^{-st}A(t)\,\mathrm{d}t = \dfrac{1}{s}\int_0^{\infty}{e^{-st} A'(t)} \,\mathrm{d}t
$$

Finally, using that

$$
A'(t) = f(t)
$$

we obtain

$$
\begin{aligned}
\frac{1}{s}∫_{0}^{∞}e^{-st}A^{′}(t)dt &= \frac{1}{s}∫_{0}^{∞}e^{-st}f(t)dt \\
&= \frac{1}{s} \cdot F(s) \\
&= (F(s))/(s)
\end{aligned}
$$

---

**Question 5**

Suppose we wish to find $L{∫_{0}^{t}f(τ)dτ}$ given $L{f(t)} = F(s)$ for $s > s_{0}$. Select the correct options in the reasoning below.Let $A(t)$ be the anti-derivative of $f(t)$ such that $A^{′}(t) = f(t)$ and $A(0) = 0$. Then,

$f(τ)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: f(τ)]
- [ ] [MATH: A(τ)]
- [ ] [MATH: f^{′}(τ)]
- [ ] [MATH: f^{′}(t)]

---

**Question 6**

Suppose we wish to find $L{∫_{0}^{t}f(τ)dτ}$ given $L{f(t)} = F(s)$ for $s > s_{0}$. Select the correct options in the reasoning below.Let $A(t)$ be the anti-derivative of $f(t)$ such that $A^{′}(t) = f(t)$ and $A(0) = 0$. Then,

$f^{′}(τ)$ $\underline{\hspace{2cm}}$

#### Select

- [ ] [MATH: f^{′}(τ)]
- [ ] [MATH: A^{′}(τ)]
- [ ] [MATH: f(τ)]
- [ ] [MATH: A(τ)]

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
