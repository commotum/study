# Using the Implicit Euler Method With Newton's Method

<!--
lesson-id: 6686
topic-code: DEQ.7.4.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining the Function on Which to Apply a Root-Finding Method When Applying the Implicit Euler Method](#determining-the-function-on-which-to-apply-a-root-finding-method-when-applying-the-implicit-euler-method)
- [Approximating the Solution to an Initial Value Problem Using the Implicit Euler Method: One Step](#approximating-the-solution-to-an-initial-value-problem-using-the-implicit-euler-method-one-step)

## Prerequisites

- [The Newton-Raphson Method](<../../../../CA1/7. Analytical Applications of Differentiation/7.4. Optimization/Lessons/7.4.9. Newton's Method.md>)
- [The Implicit Euler Method](<7.4.1. The Implicit Euler Method.md>)

---

<a id="introduction"></a>
## Introduction

We know how to apply the implicit Euler method to initial value problems

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

that are linear in $y$, that is, where

$$
f(x,y) = a(x)y + b(x)
$$

However, in general, $f$ can be a nasty, nonlinear function in $y$. For example, consider the following initial value problem:

$$
y' = x^2 - xy^4,\qquad y(0)=1
$$

Can we apply the implicit Euler method with step size

$$
\Delta x = 0.5
$$

to this IVP to approximate the value of $y(0.5)$?

Yes, we can! However, doing so requires defining a function $g$ to which the next $y$-value $y_\text{new}$ is a solution of the equation

$$
g(y_\text{new})=0
$$

Then, we apply a root-finding method to solve for $y_\text{new}$.

To see how, let's start by applying the implicit Euler method as we normally would. In general, the change $\Delta y$ for our ODE is given by

$$
\begin{aligned}
Δy &= y_{new}^{′} \cdot Δx \\
&= ((x_{new})^{2} - x_{new}(y_{new})^{4}) \cdot 0.5 \\
&= 0.5(x_{new})^{2} - 0.5x_{new}(y_{new})^{4}
\end{aligned}
$$

We substitute this into the update formula

$$
y_\text{new} = y + \Delta y
$$

and move all terms to the left-hand side:

$$
\begin{aligned}
y_{new} &= y + Δy \\
y_{new} &= y + 0.5(x_{new})^{2} - 0.5x_{new}(y_{new})^{4} \\
0.5x_{new}(y_{new})^{4} + y_{new} - y - 0.5(x_{new})^{2} &= 0
\end{aligned}
$$

Now, with terms such as $0.5x_\text{new}(y_\text{new})^4$ and $y_\text{new}$, there’s no simple algebraic rearrangement, and solving exactly is impractical.

Instead, let's define a new function $g$ of $y_\text{new}$ by the expression on the left-hand side:

$$
g(y_\text{new}) = 0.5x_\text{new}(y_\text{new})^4 + y_\text{new} - y - 0.5(x_\text{new})^2
$$

Note that, at any step, $y$ and $x_\text{new}$ are known constants, so $g$ is truly a one-variable function in $y_\text{new}$.

Then, the next $y$-value $y_\text{new}$ is a root of this function:

$$
g(y_\text{new}) = 0
$$

Therefore, to find $y_\text{new}$, we apply a root-finding method, such as Newton's method.

For instance, in the first step, the new $x$-value is

$$
x_\text{new}=0+0.5=0.5
$$

So, the function used in the first step is

$$
\begin{aligned}
g(y_{new}) &= 0.5 \cdot 0.5 \cdot (y_{new})^{4} + y_{new} - 1 - 0.5 \cdot (0.5)^{2} \\
&= 0.25(y_{new})^{4} + y_{new} - 1.125
\end{aligned}
$$

Then, we would proceed to use a root-finding method with starting point $y=1$ to solve

$$
g(y_\text{new}) = 0
$$

---

<a id="determining-the-function-on-which-to-apply-a-root-finding-method-when-applying-the-implicit-euler-method"></a>
## Determining the Function on Which to Apply a Root-Finding Method When Applying the Implicit Euler Method

**Example:** Consider the following initial value problem:
$y' = y^3 - x\,y, \qquad y(1)=1$
Using the implicit Euler method with step size $\Delta x = 1$, the new value $y_\text{new}$ is obtained by solving the equation $g(y_\text{new}) = 0$ at each step.
Which of the following is the **specific function** $g(y_\text{new})$ used in the first step?

1. $g(y_\text{new}) = (y_\text{new})^3 - 3y_\text{new} - 1$
2. $g(y_\text{new}) = 3y_\text{new} - (y_\text{new})^3 + 1$
3. $g(y_\text{new}) = 3y_\text{new} - (y_\text{new})^3 - 1$
4. $g(y_\text{new}) = y_\text{new} - (y_\text{new})^3 + x_\text{new}y_\text{new}$

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the implicit Euler method with step size $\Delta x$ is given by

$$
\Delta y = y'_\text{new} \cdot \Delta x
$$

with

$$
y'_\text{new} = f(x_\text{new},y_\text{new})
$$

evaluated at the next $x$ and $y$ values.

In general, $\Delta y$ is given by

$$
\begin{aligned}
Δy &= y_{new}^{′} \cdot Δx \\
&= (y_{new})^{3} - x_{new}y_{new} \\
&= (y_{new})^{3} - x_{new}y_{new}
\end{aligned}
$$

We substitute this into

$$
y_\text{new} = y + \Delta y
$$

and move all terms to the left-hand side and define the resulting expression as $g(y_\text{new}){:}$

$$
\begin{aligned}
y_{new} &= y + Δy \\
y_{new} &= y + (y_{new})^{3} - x_{new}y_{new} \\
y_{new} - (y_{new})^{3} + x_{new}y_{new} - y_(⏟)_(g(y_{new})) &= 0
\end{aligned}
$$

So, the general form of the function $g$ for step size

$$
\Delta x = 1
$$

is

$$
g(y_\text{new}) = y_\text{new} - (y_\text{new})^3 + x_\text{new}y_\text{new} - y
$$

Now, in the first step, the new $x$ value is

$$
x_\text{new} = x + \Delta x = 1 + 1 = 2
$$

Therefore, the function used in the first step is

$$
\begin{aligned}
g(y_{new}) &= y_{new} - (y_{new})^{3} + 2y_{new} - 1 \\
&= 3y_{new} - (y_{new})^{3} - 1
\end{aligned}
$$

---

**Question 1:** Consider the following initial value problem:
$y^{′} = x^{2} - y^{2}, y(0) = 1$
Using the implicit Euler method with step size $Δx = 0.25$, the new value $y_{new}$ is obtained by solving the equation $g(y_{new}) = 0$ at each step.
Which of the following is the **general form** of $g(y_{new})$ in terms of the new values $x_{new}$ and $y_{new}$, and the current value $y$?

- [ ] A. $g(y_{new}) = 0.25(y_{new})^{2} - y_{new} + y - 0.25(x_{new})^{2}$
- [ ] B. $g(y_{new}) = 0.25(x_{new})^{2} - y_{new} + y - 0.25y^{2}$
- [ ] C. $g(y_{new}) = y_{new} - y - 0.25(x_{new})^{2} - 0.25(y_{new})^{2}$
- [ ] D. $g(y_{new}) = 0.25(y_{new})^{2} + y_{new} - y - 0.25(x_{new})^{2}$
- [ ] E. $g(y_{new}) = 0.25y^{2} + y_{new} - y - 0.25(x_{new})^{2}$

---

**Question 2:** Consider the following initial value problem:
$y^{′} = y^{3} - x, y(1) = 1$
Using the implicit Euler method with step size $Δx = 1$, the new value $y_{new}$ is obtained by solving the equation $g(y_{new}) = 0$ at each step.
Which of the following is the **specific function** $g(y_{new})$ used in the first step?

- [ ] A. $g(y_{new}) = y_{new} - (y_{new})^{3} - 1$
- [ ] B. $g(y_{new}) = y_{new} - (y_{new})^{3} + x_{new}$
- [ ] C. $g(y_{new}) = (y_{new})^{3} - y_{new} + 1$
- [ ] D. $g(y_{new}) = y_{new} - (y_{new})^{3} + 1$
- [ ] E. $g(y_{new}) = (y_{new})^{3} - y_{new} - 1$

---

<a id="approximating-the-solution-to-an-initial-value-problem-using-the-implicit-euler-method-one-step"></a>
## Approximating the Solution to an Initial Value Problem Using the Implicit Euler Method: One Step

**Example:** Consider the following initial value problem:
$y' = 2xy + y^3, \qquad y(1.5)=-0.3$
Use the implicit Euler method with one step and Newton's method to approximate $y(2)$. Round your answer to two decimal places.

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the implicit Euler method with step size $\Delta x$ is given by

$$
\Delta y = y'_\text{new} \cdot \Delta x
$$

with

$$
y'_\text{new} = f(x_\text{new},y_\text{new})
$$

evaluated at the next $x$ and $y$ values.

Since we want to find the value of $y$ at $x=2$, we will use a step size of

$$
\Delta x = 2 - 1.5 = 0.5
$$

Now, let's proceed with the implicit Euler method. In general, $\Delta y$ is given by

$$
\begin{aligned}
Δy &= y_{new}^{′} \cdot Δx \\
&= (2x_{new}y_{new} + (y_{new})^{3}) \cdot 0.5 \\
&= x_{new}y_{new} + 0.5(y_{new})^{3}
\end{aligned}
$$

We substitute this into

$$
y_\text{new} = y + \Delta y
$$

and move all terms to the left-hand side and define the resulting expression as $g(y_\text{new}){:}$

$$
\begin{aligned}
y_{new} &= y + Δy \\
y_{new} &= y + x_{new}y_{new} + 0.5(y_{new})^{3}
\end{aligned}
$$

which gives the equation

$$
0.5(y_\text{new})^3 + (x_\text{new}-1)y_\text{new} + y = 0
$$

So, the general form of the function $g$ for step size

$$
\Delta x = 0.5
$$

is

$$
g(y_\text{new}) = 0.5(y_\text{new})^3 + (x_\text{new}-1)y_\text{new} + y
$$

Now, in the first step, the new $x$ value is

$$
x_\text{new} = x + \Delta x = 1.5 + 0.5 = 2
$$

The old $y$ value is $y = -0.3$. Then, the function $g$ is

$$
\begin{aligned}
g(y_{new}) &= 0.5(y_{new})^{3} + (2 - 1)y_{new} - 0.3 \\
&= 0.5(y_{new})^{3} + y_{new} - 0.3
\end{aligned}
$$

We use a root-finding method with starting point $y=-0.3$ to solve

$$
g(y_\text{new}) = 0
$$

We'll use Newton's method.

$$
(y_\text{new})_{n+1} = (y_\text{new})_n - \dfrac{g(y_\text{new})}{g'(y_\text{new})}
$$

Note that the derivative of $g$ is

$$
g'(y_\text{new}) = 1.5(y_\text{new})^2 + 1
$$

Therefore, our Newton's method iterative scheme is as follows:

$$
(y_\text{new})_{n+1} = (y_\text{new})_n - \dfrac{0.5((y_\text{new})_n)^3 + (y_\text{new})_n - 0.3}{1.5((y_\text{new})_n)^2 + 1}
$$

Applying several iterations of Newton's method starting with

$$
y(1.5) = -0.3
$$

we get the following:

$$
\begin{aligned}
(y_{new})_{1} &= -0.3 \\
&= (y_{new})_{2}\begin{vmatrix}\approx 0.240529 \\ (y_{new})_{3}\end{vmatrix}\approx 0.288849 \\
&= (y_{new})_{4} \mid \approx 0.288050
\end{aligned}
$$

Therefore, we conclude that

$$
y(2) \approx 0.29
$$

rounded to two decimal places.

---

**Question 3**

> A scientific calculator is required to answer this question.

Consider the following initial value problem:
$y^{′} = x^{2} - y^{3}, y(0.5) = 0.5$
Use the implicit Euler method with one step and Newton's method to approximate $y(1)$. Round your answer to two decimal places.$y(1) \approx$
$\underline{\hspace{1.5cm}}$

---

**Question 4**

> A scientific calculator is required to answer this question.

Consider the following initial value problem:
$y^{′} = 2x^{2} - y^{3}, y(0.5) = 1$
Use the implicit Euler method with one step and Newton's method to approximate $y(1)$. Round your answer to two decimal places.$y(1) \approx$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
