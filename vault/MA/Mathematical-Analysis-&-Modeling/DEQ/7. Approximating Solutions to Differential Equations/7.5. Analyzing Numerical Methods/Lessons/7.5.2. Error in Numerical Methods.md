# Error in Numerical Methods

<!--
lesson-id: 3246
topic-code: DEQ.7.5.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing Local Truncation Error Given Numerical Solutions to an Initial Value Problem](#computing-local-truncation-error-given-numerical-solutions-to-an-initial-value-problem)
- [Computing the LTE for a Numerical Method Given an Initial Value Problem](#computing-the-lte-for-a-numerical-method-given-an-initial-value-problem)
- [Writing an Expression for the LTE for a Given Numerical Method](#writing-an-expression-for-the-lte-for-a-given-numerical-method)
- [Global Truncation Error](#global-truncation-error)
- [Computing Global Truncation Error Given Numerical Solutions to an Initial Value Problem](#computing-global-truncation-error-given-numerical-solutions-to-an-initial-value-problem)
- [Relationship Between Local and Global Truncation Errors](#relationship-between-local-and-global-truncation-errors)

## Prerequisites

- [Euler's Method: Calculating Multiple Steps](<../../7.2. Euler's Method/Lessons/7.2.2. Euler's Method- Calculating Multiple Steps.md>)

---

<a id="introduction"></a>
## Introduction

Even when a numerical method begins a step from the exact solution value, it typically does not land exactly on the true solution at the next point.
This discrepancy reflects how accurately the method approximates the solution over a single step.

The **local truncation error** (**LTE**) of a numerical method is the error made in a *single step* that begins from an *exact value*. The LTE of a numerical method at step $n$ from $x$ with exact value $y_\text{exact}(x)$ to

$$
x_\text{new} = x + \Delta x
$$

is

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - y_{new} \\
&= y_{exact}(x_{new}) - (y_{exact}(x) + Δy)
\end{aligned}
$$

LTE isolates the intrinsic one-step accuracy of a method. It answers: "*How well does a single step approximate the exact solution if there were no accumulated errors?*"

![](<../Source/7.5.2. Error in Numerical Methods/Images/2578457f719f62e50b0c824edbe4b4eb.png>)

Suppose a numerical method advances the solution using the increment formula

$$
\Delta y = \Phi(x, y, \Delta x) \cdot \Delta x
$$

for some function $\Phi$. Then, the LTE at step $n$ is

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - (y_{exact}(x) + Δy) \\
&= y_{exact}(x_{new}) - (y_{exact}(x) + Φ(x, y_{exact}(x), Δx) \cdot Δx) \\
&= y_{exact}(x_{new}) - y_{exact}(x) - Φ(x, y_{exact}(x), Δx) \cdot Δx
\end{aligned}
$$

In the next slide, we will apply this formula to calculate the local truncation error for a given numerical method.

---

<a id="computing-local-truncation-error-given-numerical-solutions-to-an-initial-value-problem"></a>
## Computing Local Truncation Error Given Numerical Solutions to an Initial Value Problem

**Example:** An initial value problem has the exact solution $y_{\textrm{exact}}(x) = \cos(2x)$. A numerical method is used to approximate the solution to the initial value problem, resulting in the table below, where $y$ denotes the approximate solution at each step, and $\tau_n$ is the local truncation error at each step. At each step, the exact value is used to approximate the new value.

Calculate the local truncation error at each step. Round your answers to three decimal places.

| $n$ | $x$ | $y$ |
| --- | ---: | ---: |
| $0$ | $0$ | $1.000$ |
| $1$ | $0.2$ | $0.925$ |
| $2$ | $0.4$ | $0.692$ |
| $3$ | $0.6$ | $0.368$ |

**Explanation**

The local truncation error (LTE) is the error made in a *single step* of a numerical method, when the step begins from an *exact value*. The LTE of a numerical method at step $n$ from $x$ with exact value $y_{\textrm{exact}}(x)$ to

$$
x_\text{new} = x + \Delta x
$$

is

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - y_{new} \\
&= y_{exact}(x_{new}) - (y_{exact}(x) + Δy)
\end{aligned}
$$

We find the LTE at each step, rounding to three decimal places in each case:

- After the first step, we have $y_\text{new} = 0.925$. So, the LTE of the first step is
$τ_{1}|= y_{exact}(0.2) - 0.925; = \cos (0.4) - 0.925; \approx - 0.004$.
- After the second step, we have $y_\text{new} = 0.692$. So, the LTE of the second step is
$τ_{2}|= y_{exact}(0.4) - 0.692; = \cos (0.8) - 0.692; \approx 0.005$.
- After the third step, we have $y_\text{new} = 0.368$. So, the LTE of the third step is
$τ_{3}|= y_{exact}(0.6) - 0.368; = \cos (1.2) - 0.368; \approx - 0.006$.

Therefore, the completed table is as given below.

| $n$ | $x$ | $y$ | $\tau_n$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $1.000$ | $0$ |
| $1$ | $0.2$ | $0.925$ | $-0.004$ |
| $2$ | $0.4$ | $0.692$ | $0.005$ |
| $3$ | $0.6$ | $0.368$ | $-0.006$ |

---

**Question 1**

> A scientific calculator is required to answer this question.

An initial value problem has the exact solution $y_{exact}(x) = e^{x}$. A numerical method is used to approximate the solution to the initial value problem, resulting in the table below, where $y$ denotes the approximate solution at each step, and $τ_{n}$ is the local truncation error at each step. At each step, the exact value is used to approximate the new value.

Calculate the local truncation error at each step. Round your answers to three decimal places.

| $n$ | $x$ | $y$ | $τ_{n}$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $1.000$ | $0$ |
| $1$ | $0.25$ | $1.283$ | $\underline{\hspace{1.5cm}}$ |
| $2$ | $0.5$ | $1.644$ | $\underline{\hspace{1.5cm}}$ |
| $3$ | $0.75$ | $2.104$ | $\underline{\hspace{1.5cm}}$ |

---

**Question 2**

> A scientific calculator is required to answer this question.

An initial value problem has the exact solution $y_{exact}(x) = \sin x$. A numerical method is used to approximate the solution to the initial value problem, resulting in the table below, where $y$ denotes the approximate solution at each step, and $τ_{n}$ is the local truncation error at each step. At each step, the exact value is used to approximate the new value.

Calculate the local truncation error at each step. Round your answers to three decimal places.

| $n$ | $x$ | $y$ | $τ_{n}$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $0$ | $0$ |
| $1$ | $0.25$ | $0.250$ | $\underline{\hspace{1.5cm}}$ |
| $2$ | $0.5$ | $0.483$ | $\underline{\hspace{1.5cm}}$ |
| $3$ | $0.75$ | $0.688$ | $\underline{\hspace{1.5cm}}$ |

---

<a id="computing-the-lte-for-a-numerical-method-given-an-initial-value-problem"></a>
## Computing the LTE for a Numerical Method Given an Initial Value Problem

**Example:** Consider the following initial value problem:
$y' = -\sin x, \qquad y(0) = 1$
Suppose we wish to approximate the solution using the following method:
$Δy|= y^{′} \cdot Δx; y_{new}|= y + Δy$
Given that the exact solution is $y_{\textrm{exact}}(x) = \cos x$, compute the local truncation error $\tau_{11}$ for the step starting at $x=1$ with step size $\Delta x=0.2$. Round your answer to three decimal places.

**Explanation**

The local truncation error (LTE) is the error made in a *single step* of a numerical method, when the step begins from an *exact value*. The LTE of a numerical method at step $n$ from $x$ with exact value $y_{\textrm{exact}}(x)$ to

$$
x_\text{new} = x + \Delta x
$$

is

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - y_{new} \\
&= y_{exact}(x_{new}) - (y_{exact}(x) + Δy)
\end{aligned}
$$

We're given that the exact solution is

$$
y_{\textrm{exact}}(x) = \cos x
$$

So, the $y$-value at the start of the step $x=1$ is

$$
y_{\textrm{exact}}(1)=\cos(1)
$$

Now, let's find the $y$-increment given by the numerical method.

First, the slope at the start of the step is

$$
\begin{aligned}
y_{exact}^{′}(1) &= -\sin (1) \\
&=-0.8415
\end{aligned}
$$

So, the increment at this step is

$$
\begin{aligned}
Δy &= y_{exact}^{′}(1) \cdot Δx \\
&=-0.8415 \cdot 0.2 \\
&\approx - 0.1683
\end{aligned}
$$

Next, we compute the exact value at $x_\text{new}{:}$

$$
\begin{aligned}
y_{exact}(x_{new}) &= y_{exact}(x + Δx) \\
&= y_{exact}(1 + 0.2) \\
&= y_{exact}(1.2) \\
&= \cos (1.2)
\end{aligned}
$$

Finally, we calculate the LTE for the step starting at $x=1{:}$

$$
\begin{aligned}
τ_{11} &= \cos (1.2) - (\cos (1) + Δy) \\
&= \cos (1.2) - (\cos (1) - 0.1683) \\
&\approx - 0.010
\end{aligned}
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

Consider the following initial value problem:
$y^{′} = 2y, y(0) = 1$
Suppose we wish to approximate the solution using Euler's method:
$Δy|= y^{′} \cdot Δx; y_{new}|= y + Δy$
Given that the exact solution is $y_{exact}(x) = e^{2x}$, compute the local truncation error $τ_{1}$ for the first step starting at $x = 0$ with step size $Δx = 0.1$. Round your answer to three decimal places.$τ_{1} \approx$
$\underline{\hspace{1.5cm}}$

---

**Question 4**

> A scientific calculator is required to answer this question.

Consider the following initial value problem:
$y^{′} = x + 1, y(0) = 0$
Suppose we wish to approximate the solution using the following numerical method:
$Δy|= \frac{1}{2}(x + y^{′}) \cdot Δx; y_{new}|= y + Δy$
Given that the exact solution is $y_{exact}(x) = \frac{1}{2}x^{2} + x$, compute the local truncation error, $τ_{1}$, for the first step starting at $x = 0$ with step size $Δx = 0.2$.$τ_{1} =$
$\underline{\hspace{1.5cm}}$

---

<a id="writing-an-expression-for-the-lte-for-a-given-numerical-method"></a>
## Writing an Expression for the LTE for a Given Numerical Method

**Example:** Suppose we wish to approximate the solution to an initial value problem using Euler's method:
$Δy|= y^{′} \cdot Δx; y_{new}|= y + Δy$
If the exact solution is $y_{\textrm{exact}}(x) = x^3$, find an expression for the local truncation error for the step from $x$ to $x_\text{new} = x + \Delta x$.

**Explanation**

The local truncation error (LTE) is the error made in a *single step* of a numerical method, when the step begins from an *exact value*. The LTE of a numerical method at step $n$ from $x$ with exact value $y_{\textrm{exact}}(x)$ to

$$
x_\text{new} = x + \Delta x
$$

is

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - y_{new} \\
&= y_{exact}(x_{new}) - (y_{exact}(x) + Δy)
\end{aligned}
$$

We're given that the exact solution of the initial value problem is

$$
y_{\textrm{exact}}(x) = x^3
$$

Now, let's find the $y$-increment given by the numerical method.

First, the slope at a step starting at $x$ is

$$
y_{exact}^{′}(x) = (x^{3})^{′} = 3x^{2}
$$

So, the increment given by the numerical method is

$$
\begin{aligned}
Δy &= y_{exact}^{′}(x) \cdot Δx \\
&= 3x^{2} \cdot Δx
\end{aligned}
$$

Next, we compute the exact value at $x_\text{new}{:}$

$$
\begin{aligned}
y_{exact}(x_{new}) &= y_{exact}(x + Δx) \\
&= (x + Δx)^{3} \\
&= x^{3} + 3x^{2} \cdot Δx + 3x \cdot (Δx)^{2} + (Δx)^{3}
\end{aligned}
$$

Finally, we calculate the LTE for step $n$ starting at $x{:}$

$$
\begin{aligned}
τ_{n} &= y_{exact}(x_{new}) - (y_{exact}(x) + Δy) \\
&= [x^{3} + 3x^{2} \cdot Δx + 3x \cdot (Δx)^{2} + (Δx)^{3}] - [x^{3} + 3x^{2} \cdot Δx] \\
&= x^{3} - x^{3} + (3x^{2} - 3x^{2}) \cdot Δx + 3x \cdot (Δx)^{2} + (Δx)^{3} \\
&= 3x \cdot (Δx)^{2} + (Δx)^{3}
\end{aligned}
$$

---

**Question 5:**

```quiz
type: radio
id: ma-331513
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Suppose we wish to approximate the solution to an initial value problem using Euler's method:

  $Δy|= y^{′} \cdot Δx; y_{new}|= y + Δy$
  If the exact solution is $y_{exact}(x) = x^{2}$, find an expression for the local truncation error for the step from $x$ to $x_{new} = x + Δx$.
options:
- id: a
  content: |-
    $2x \cdot Δx$
- id: b
  content: |-
    $(Δx)^{2}$
- id: c
  content: |-
    $2x \cdot (Δx)^{2}$
- id: d
  content: |-
    $0$
- id: e
  content: |-
    $Δx$
```

---

**Question 6:**

```quiz
type: radio
id: ma-331510
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Suppose we wish to approximate the solution to an initial value problem using the following numerical method:

  $Δy|= (x^{2} + y) \cdot Δx; y_{new}|= y + Δy$
  If the exact solution is $y_{exact}(x) = x^{3}$, find an expression for the local truncation error for the step from $x$ to $x_{new} = x + Δx$.
options:
- id: a
  content: |-
    $(x^{2} - 2x^{3})Δx + 3x(Δx)^{2} + 4x(Δx)^{3}$
- id: b
  content: |-
    $(2x^{2} - x^{3})Δx + 3x(Δx)^{2} + (Δx)^{3}$
- id: c
  content: |-
    $(2x^{3} - x^{2})Δx + x(Δx)^{2} + (Δx)^{3}$
- id: d
  content: |-
    $(x^{3} - 2x^{2})Δx + 3x(Δx)^{2} + (Δx)^{3}$
- id: e
  content: |-
    $(x^{2} - x^{3})Δx + 2x(Δx)^{2} + x(Δx)^{3}$
```

---

<a id="global-truncation-error"></a>
## Global Truncation Error

In practice, numerical methods apply many steps in sequence, using previously computed values to advance the solution. As a result, errors from earlier steps can propagate to later steps, leading to a growing discrepancy between the numerical and exact solutions over time.

The **global truncation error** (**GTE**) of a numerical method is the total error accumulated over several steps. More formally, the GTE of a numerical method after step $n$ from $x_{n-1}$ to

$$
x_n = x_{n-1} + \Delta x
$$

is

$$
e_n = y(x_n) - y_n
$$

where $y(x_n)$ is the exact value at $x_n$, and $y_n$ is the numerical value produced after $n$ steps.

GTE is the accumulated discrepancy between the numerical and true solutions after many steps.

![](<../Source/7.5.2. Error in Numerical Methods/Images/3a694aeb2de6c719e7f3137e155c6d32.png>)

Notice that, for step $1$ and initial value $y(x_0)=y_0$, the GTE and LTE are identical:

$$
\begin{aligned}
e_{1} &= y(x_{1}) - y_{1} \\
&= y(x_{1}) - (y_{0} + Δy_{1}) \\
&= y(x_{1}) - (y(x_{0}) + Δy_{1}) \\
&= τ_{1}
\end{aligned}
$$

The plot below shows the numerical solution and the exact solution for a method implementation, along with the vertical gaps $e_n$.

![](<../Source/7.5.2. Error in Numerical Methods/Images/ac80cd8f3b0d4bf329b39326130c6c6f.png>)

---

<a id="computing-global-truncation-error-given-numerical-solutions-to-an-initial-value-problem"></a>
## Computing Global Truncation Error Given Numerical Solutions to an Initial Value Problem

**Example:** A numerical method is used to approximate a solution to an initial value problem, resulting in the table below. If the exact solution is $y(x) = \sqrt{x + 2}$, find, rounded to three decimal places, the global truncation error at each step.

| $n$ | $x_n$ | $y_n$ | $e_n$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $1.414$ | $0$ |
| $1$ | $0.3$ | $1.480$ |  |
| $2$ | $0.6$ | $1.570$ |  |
| $3$ | $0.9$ | $1.650$ |  |

**Explanation**

The global truncation error (GTE) is the total error accumulated after several steps of a numerical method. The GTE of a numerical method at step $n$ from $x_{n-1}$ to

$$
x_n = x_{n-1} + \Delta x
$$

is

$$
e_n = y(x_n) - y_n
$$

where $y(x_n)$ is the exact value at $x_n$, and $y_n$ is the numerical value produced after $n$ steps.

We are given the numerical values $y_n$ produced at each step in the table. Now, we compute the exact value $y(x_n)$ at each value of $x_n{:}$

- The exact value at $x_0=0$ is $y(0) = \sqrt{0 + 2} = \sqrt{2}$.
- The exact value at $x_1=0.3$ is $y(0.3) = \sqrt{0.3 + 2}$.
- The exact value at $x_2=0.6$ is $y(0.6) = \sqrt{0.6 + 2}$.
- The exact value at $x_3=0.9$ is $y(0.9) = \sqrt{0.9 + 2}$.

Let's add these values to the table.

| $n$ | $x_n$ | $y_n$ | $y(x_n)$ | $e_n$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $0$ | $1.414$ | $\sqrt{2}$ | $0$ |
| $1$ | $0.3$ | $1.480$ | $\sqrt{2.3}$ |  |
| $2$ | $0.6$ | $1.570$ | $\sqrt{2.6}$ |  |
| $3$ | $0.9$ | $1.650$ | $\sqrt{2.9}$ |  |

Finally, we find the GTE at each step, rounding to three decimal places in each case:

- The GTE of the first step is
$e_{1}|= y(x_{1}) - y_{1}; = \sqrt{2.3} - 1.480; \approx 0.037$.
- The GTE of the second step is
$e_{2}|= y(x_{2}) - y_{2}; = \sqrt{2.6} - 1.570; \approx 0.042$.
- The GTE of the third step is
$e_{3}|= y(x_{3}) - y_{3}; = \sqrt{2.9} - 1.650; \approx 0.053$.

Therefore, the completed table is as given below.

| $n$ | $x_n$ | $y_n$ | $y(x_n)$ | $e_n$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $0$ | $1.414$ | $\sqrt{2}$ | $0$ |
| $1$ | $0.3$ | $1.480$ | $\sqrt{2.3}$ | $\bbox[4pt,border: 1px solid gainsboro]{0.037}$ |
| $2$ | $0.6$ | $1.570$ | $\sqrt{2.6}$ | $\bbox[4pt,border: 1px solid gainsboro]{0.042}$ |
| $3$ | $0.9$ | $1.650$ | $\sqrt{2.9}$ | $\bbox[4pt,border: 1px solid gainsboro]{0.053}$ |

---

**Question 7**

> A scientific calculator is required to answer this question.

A numerical method is used to approximate a solution to an initial value problem, resulting in the table below. If the exact solution is $y(x) = \cos x$, find, rounded to three decimal places, the global truncation error at each step.

| $n$ | $x_{n}$ | $y_{n}$ | $e_{n}$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $1$ | $0$ |
| $1$ | $0.3$ | $0.943$ | $\underline{\hspace{1.5cm}}$ |
| $2$ | $0.6$ | $0.793$ | $\underline{\hspace{1.5cm}}$ |
| $3$ | $0.9$ | $0.595$ | $\underline{\hspace{1.5cm}}$ |

---

**Question 8**

> A scientific calculator is required to answer this question.

A numerical method is used to approximate a solution to an initial value problem, resulting in the table below. If the exact solution is $y(x) = e^{-x}$, find, rounded to three decimal places, the global truncation error at each step.

| $n$ | $x_{n}$ | $y_{n}$ | $e_{n}$ |
| --- | ---: | ---: | ---: |
| $0$ | $0$ | $1$ | $0$ |
| $1$ | $0.2$ | $0.762$ | $\underline{\hspace{1.5cm}}$ |
| $2$ | $0.4$ | $0.558$ | $\underline{\hspace{1.5cm}}$ |
| $3$ | $0.6$ | $0.353$ | $\underline{\hspace{1.5cm}}$ |

---

<a id="relationship-between-local-and-global-truncation-errors"></a>
## Relationship Between Local and Global Truncation Errors

How do local errors build to grow into global errors?

Again, suppose a numerical method advances the solution using the increment formula

$$
\Delta y = \Phi(x, y, \Delta x) \cdot \Delta x
$$

for some function $\Phi$.

- LTE $\tau_n$ measures the error made in *one step*, where the step begins from the exact value $y(x_{n-1})$.
- GTE $e_n$ measures the accumulated error after *many steps*, starting from $x_0$ and using the numerical method recursively.

So, at each step, we can think of the GTE as a combination of

- the total accumulated error from the previous steps ($e_{n-1}$),
- the way the method propagates these errors forward, and
- a new error introduced in the current step ($\tau_n$).

In particular, the global truncation error satisfies the following:

$$
e_n = e_{n-1} + \left[ \Phi(x_{n-1}, y(x_{n-1}), \Delta x) - \Phi(x_{n-1}, y_{n-1}, \Delta x)\right] \cdot \Delta x + \tau_n
$$

Indeed, using the iterative notation, at step $n$, the local truncation error is

$$
\begin{aligned}
τ_{n} &= y(x_{n}) - (y(x_{n - 1}) + Φ(x_{n - 1}, y(x_{n - 1}), Δx) \cdot Δx)
\end{aligned}
$$

Rearranging, we get an expression for the exact value $y(x_n){:}$

$$
y(x_n) = y(x_{n-1}) + \Phi(x_{n-1}, y(x_{n-1}), \Delta x) \cdot \Delta x + \tau_n
$$

Then, substituting this expression into the equation for GTE, we obtain our recursion:

$$
\begin{aligned}
e_{n} &= y(x_{n}) - y_{n} \\
&= y(x_{n}) - (y_{n - 1} + Δy_{n - 1}) \\
&= [y(x_{n - 1}) + Φ(x_{n - 1}, y(x_{n - 1}), Δx) \cdot Δx + τ_{n}] - [y_{n - 1} + Φ(x_{n - 1}, y_{n - 1}, Δx) \cdot Δx] \\
&= (y(x_{n - 1}) - y_{n - 1}) + (Φ(x_{n - 1}, y(x_{n - 1}), Δx) - Φ(x_{n - 1}, y_{n - 1}, Δx)) \cdot Δx + τ_{n} \\
&= e_{n - 1}_(⏟)_(GTE at step [math]n-1[/math]) + [Φ(x_{n - 1}, y(x_{n - 1}), Δx) - Φ(x_{n - 1}, y_{n - 1}, Δx)] \cdot Δx_(⏟)_(propagation ofe_{n - 1}) + τ_{n}_(⏟)_(LTE at step [math]n[/math])
\end{aligned}
$$

The diagram below illustrates this propagation, where $\Delta y_n$ is the increment produced by the numerical method at step $n$ starting from the previous numerical value $y_{n-1}$, while $\Delta y$ is the increment produced by the same formula but starting from the exact value $y(x_{n-1})$.

![](<../Source/7.5.2. Error in Numerical Methods/Images/b3721b80dea8abe3ecb22adc0c08e0b7.png>)

```update-progress
```

[[MA/Mathematical-Analysis-&-Modeling/DEQ/Home|Home]]
[[MA/Mathematical-Analysis-&-Modeling/DEQ/0. Table of Contents/TOC|Table of Contents]]
