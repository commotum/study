# Further Logarithmic Differentiation

<!--
lesson-id: 344
topic-code: CA1.5.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Differentiating a Product](#differentiating-a-product)
- [Using Logarithms to Differentiate a Quotient](#using-logarithms-to-differentiate-a-quotient)
- [Differentiating a Quotient](#differentiating-a-quotient)
- [Differentiating a Radical Expression](#differentiating-a-radical-expression)

## Prerequisites

- [Logarithmic Differentiation](<5.3.1. Logarithmic Differentiation.md>)
- [The Power Rule for Exponents With Algebraic Expressions](<../../../../AG1/8. Exponential Functions/8.1. Rules of Exponents/Lessons/8.1.4. The Power Rule for Exponents With Algebraic Expressions.md>)
- [Simplifying Square Root Expressions Using the Product Rule](<../../../../AG1/4. Radical Expressions/4.1. Radical Expressions/Lessons/4.1.4. Simplifying Square Root Expressions Using the Product Rule.md>)
- [Simplifying Square Root Expressions Using the Quotient Rule](<../../../../AG1/4. Radical Expressions/4.1. Radical Expressions/Lessons/4.1.6. Simplifying Square Root Expressions Using the Quotient Rule.md>)
- [The Quotient Rule for Logarithms](<../../../../AG2/4. Exponentials & Logarithms/4.2. The Laws of Logarithms/Lessons/4.2.2. The Quotient Rule for Logarithms.md>)

---

<a id="introduction"></a>
## Introduction

When a function $y=y(x)$ contains complex products, quotients, or radicals, we can use logarithmic differentiation to find the derivative $y'(x)$.

For example, consider the following function:

$$
y = x^2 \cdot 2^x
$$

Our goal is to find $y'(x)$. To do this, we will use the product rule for logarithms, namely

$$
\ln(a b)= \ln a + \ln b
$$

We differentiate our function by following four steps.

**Step 1.** We take the natural logarithm of both sides and apply the product and power rules:

$$
\begin{aligned}
\ln y &= \ln (x^{2} \cdot 2^{x}) \\
\ln y &= \ln (x^{2}) + \ln (2^{x}) \\
\ln y &= 2\ln x + x\ln 2
\end{aligned}
$$

**Step 2.** Next, we differentiate both sides of our equation using implicit differentiation:

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(2\ln x + x\ln 2) \\
\frac{1}{y} \cdot y^{′} &= 2 \cdot (\ln x)^{′} + (x)^{′} \cdot \ln (2) \\
\frac{1}{y} \cdot y^{′} &= 2 \cdot \frac{1}{x} + 1 \cdot \ln (2) \\
\frac{y^{′}}{y} &= \frac{2}{x} + \ln 2
\end{aligned}
$$

**Step 3.** Then, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(\frac{2}{x} + \ln 2)
\end{aligned}
$$

**Step 4.** Finally, we substitute back our original expression for

$$
y =x^2\cdot 2^x{:}
$$

$$
\begin{aligned}
y^{′} &= x^{2} \cdot 2^{x}(\frac{2}{x} + \ln 2) \\
&= x \cdot 2^{x + 1} + x^{2} \cdot 2^{x}\ln 2
\end{aligned}
$$

---

<a id="differentiating-a-product"></a>
## Differentiating a Product

**Example:** Consider the function $y = x^5 (\cos x)^x$. Its derivative is given by

$y'(x) = x^4 (\cos x)^x \left(5 + g(x) \right)$.

Find the function $g(x)$.

**Explanation**

We have the function

$$
y = x^5 (\cos x)^x
$$

First, we find the natural logarithm of both sides and use the product rule for logarithms:

$$
\begin{aligned}
\ln y &= \ln (x^{5}(\cos x)^{x}) \\
\ln y &= \ln (x^{5}) + \ln ((\cos x)^{x}) \\
\ln y &= 5\ln x + x\ln (\cos x)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(5\ln x + x\ln (\cos x)) \\
\frac{1}{y} \cdot y^{′} &= \frac{5}{x} + (x)^{′} \cdot \ln (\cos x) + x \cdot (\ln (\cos x))^{′} \\
\frac{y^{′}}{y} &= \frac{5}{x} + \ln (\cos x) + x \cdot \frac{1}{\cos x} \cdot (-\sin x) \\
\frac{y^{′}}{y} &= \frac{5}{x} + \ln (\cos x) - x\tan x
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(\frac{5}{x} + \ln (\cos x) - x\tan x)
\end{aligned}
$$

Finally, we substitute

$$
y = x^5 (\cos x)^x {:}
$$

$$
\begin{aligned}
y^{′} &= x^{5}(\cos x)^{x}(\frac{5}{x} + \ln (\cos x) - x\tan x) \\
&= x^{5}(\cos x)^{x}(\frac{5}{x} + (x\ln (\cos x))/(x) - \frac{x^{2}\tan x}{x}) \\
&= x^{4}(\cos x)^{x}(5 + x ln ⁡ (cos ⁡ x) - x 2 tan ⁡ x)
\end{aligned}
$$

Therefore,

$$
g(x) = x \ln (\cos x) - x^2 \tan x
$$

---

**Question 1**

Consider the function $y = x(x^{2} + 8)e^{x^{2} + x}$. The derivative of this function is given by
$y^{′} = e^{x^{2} + x} \cdot (ax^{4} + bx^{3} + cx^{2} + 8x + 8)$.

Find the value of $a + b + c$.

- [ ] A. $14$
- [ ] B. $20$
- [ ] C. $22$
- [ ] D. $18$
- [ ] E. $16$

---

**Question 2**

Consider the function $y = x^{2}(\sin x)^{x}$. Its derivative is given by

$y^{′}(x) = x(\sin x)^{x} \cdot (2 + g(x))$.

Find the function $g(x)$.

- [ ] A. $x\ln (\sin x) + x^{2}\cot x$
- [ ] B. $x\ln (\cos x) + x^{2}\cot x$
- [ ] C. $x\ln (\sin x) - 2x^{2}\tan x$
- [ ] D. $x\ln (\cos x) + x^{2}\tan x$
- [ ] E. $x\ln (\sin x) + 2x\cot x$

---

<a id="using-logarithms-to-differentiate-a-quotient"></a>
## Using Logarithms to Differentiate a Quotient

In cases where we wish to differentiate a quotient of functions

$$
y = f(x) = \dfrac{g(x)}{h(x)}
$$

we can apply the quotient rule for logarithms:

$$
\ln \left(\dfrac{a}{b}\right) = \ln a - \ln b
$$

For example, let's compute $y'$, where the function $y$ is given by

$$
y = \dfrac {e^{x^2}}{(1-2x)^2(x-1)^3}
$$

First, we find the natural logarithm of both sides and use the quotient and power rules for logarithms:

$$
\begin{aligned}
\ln y &= \ln ((e^{x^{2}})/((1 - 2x)^{2}(x - 1)^{3})) \\
\ln y &= \ln (e^{x^{2}}) - \ln ((1 - 2x)^{2}) - \ln ((x - 1)^{3}) \\
\ln y &= x^{2} - 2\ln (1 - 2x) - 3\ln (x - 1)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(x^{2} - 2\ln (1 - 2x) - 3\ln (x - 1)) \\
\frac{1}{y} \cdot y^{′} &= 2x - 2 \cdot \frac{1}{1 - 2x} \cdot (-2) - 3 \cdot \frac{1}{x - 1} \\
\frac{y^{′}}{y} &= 2x + \frac{4}{1 - 2x} - \frac{3}{x - 1}
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(2x + \frac{4}{1 - 2x} - \frac{3}{x - 1})
\end{aligned}
$$

Finally, we substitute our expression for $y$, and simplify:

$$
\begin{aligned}
y^{′} &= (e^{x^{2}})/((1 - 2x)^{2}(x - 1)^{3})(2x + \frac{4}{1 - 2x} - \frac{3}{x - 1}) \\
&= (e^{x^{2}})/((1 - 2x)^{2}(x - 1)^{3})((2x(1 - 2x)(x - 1) + 4(x - 1) - 3(1 - 2x))/((1 - 2x)(x - 1))) \\
&= (e^{x^{2}})/((1 - 2x)^{3}(x - 1)^{4})(2x(1 - 2x)(x - 1) + 4(x - 1) - 3(1 - 2x)) \\
&= (e^{x^{2}})/((1 - 2x)^{3}(x - 1)^{4})(-4x^{3} + 6x^{2} + 8x - 7) \\
&= - (e^{x^{2}})/((1 - 2x)^{3}(x - 1)^{4})(4x^{3} - 6x^{2} - 8x + 7) \\
&= - (e^{x^{2}}(4x^{3} - 6x^{2} - 8x + 7))/((1 - 2x)^{3}(x - 1)^{4})
\end{aligned}
$$

---

<a id="differentiating-a-quotient"></a>
## Differentiating a Quotient

**Example:** Consider the function $y = \dfrac{(x+3)^3}{(x+5)^6}$. The derivative of this function is given by
$y' = -\dfrac{g(x)(x+3)^2}{(x+5)^7}$.

Find the function $g(x)$.

**Explanation**

We have the function

$$
y = \dfrac{(x+3)^3}{(x+5)^6}
$$

First, we take the natural logarithm of both sides and use the quotient and power rules for logarithms:

$$
\begin{aligned}
\ln y &= \ln (((x + 3)^{3})/((x + 5)^{6})) \\
\ln y &= \ln ((x + 3)^{3}) - \ln ((x + 5)^{6}) \\
\ln y &= 3\ln (x + 3) - 6\ln (x + 5)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(3\ln (x + 3) - 6\ln (x + 5)) \\
\frac{1}{y} \cdot y^{′} &= 3 \cdot \frac{1}{x + 3} - 6 \cdot \frac{1}{x + 5} \\
\frac{y^{′}}{y} &= \frac{3}{x + 3} - \frac{6}{x + 5}
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(\frac{3}{x + 3} - \frac{6}{x + 5})
\end{aligned}
$$

Finally, we substitute

$$
y = \dfrac{(x+3)^3}{(x+5)^6}{:}
$$

$$
\begin{aligned}
y^{′} &= ((x + 3)^{3})/((x + 5)^{6})(\frac{3}{x + 3} - \frac{6}{x + 5}) \\
&= ((x + 3)^{3})/((x + 5)^{6})((3(x + 5) - 6(x + 3))/((x + 3)(x + 5))) \\
&= ((x + 3)^{3})/((x + 5)^{6})((-3x - 3)/((x + 3)(x + 5))) \\
&= - (3 (x + 1)(x + 3)^{2})/((x + 5)^{7})
\end{aligned}
$$

Therefore,

$$
g(x) = \bbox[2pt,Gainsboro]{3(x+1)}
$$

---

**Question 3**

Consider the function $y = ((x + 1)^{4}(x - 5)^{3})/((x - 3)^{8})$. The derivative of this function is given by
$y^{′} = ((x - 5)^{2}(x + 1)^{3}(ax^{2} + bx + c))/((x - 3)^{9})$.

Find the value of $a + b + c$.

- [ ] A. $-96$
- [ ] B. $-104$
- [ ] C. $92$
- [ ] D. $102$
- [ ] E. $84$

---

**Question 4**

Consider the function $y = (e^{\cos x})/((x - 3)^{2}(1 - x)^{2})$. The derivative of this function is given by
$y^{′} = (e^{\cos x})/((x - 3)^{3}(1 - x)^{3})(ax - 8 + (x^{2} + bx + c)\sin x)$.

Find the value of $a + b + c$.

- [ ] A. $11$
- [ ] B. $3$
- [ ] C. $-1$
- [ ] D. $-5$
- [ ] E. $5$

---

<a id="differentiating-a-radical-expression"></a>
## Differentiating a Radical Expression

**Example:** Consider the function $y = \sqrt[3]{\dfrac{x^2}{x^5+ 1}}$. The derivative of this function is given by
$y' = \dfrac{g(x)}{3\sqrt[3]{x(x^5+1)^4}}$.

Find the function $g(x)$.

**Explanation**

Our function can be written as

$$
\begin{aligned}
y &= \sqrt[3]{\frac{x^{2}}{x^{5} + 1}} \\
&= \frac{\sqrt[3]{x^{2}}}{\sqrt[3]{x^{5} + 1}} \\
&= (x^{2/3})/((x^{5} + 1)^{1/3})
\end{aligned}
$$

First, we take the natural logarithm of both sides and use the quotient and power rules for logarithms:

$$
\begin{aligned}
\ln y &= \ln ((x^{2/3})/((x^{5} + 1)^{1/3})) \\
\ln y &= \ln (x^{2/3}) - \ln ((x^{5} + 1)^{1/3}) \\
\ln y &= \frac{2}{3}\ln x - \frac{1}{3}\ln (x^{5} + 1)
\end{aligned}
$$

Next, we differentiate both sides with respect to $x{:}$

$$
\begin{aligned}
\frac{d}{dx}(\ln y) &= \frac{d}{dx}(\frac{2}{3}\ln x - \frac{1}{3}\ln (x^{5} + 1)) \\
\frac{1}{y} \cdot y^{′} &= \frac{2}{3} \cdot \frac{1}{x} - \frac{1}{3} \cdot \frac{1}{x^{5} + 1} \cdot (5x^{4}) \\
\frac{y^{′}}{y} &= \frac{1}{3}(\frac{2}{x} - \frac{5x^{4}}{x^{5} + 1})
\end{aligned}
$$

Now, we solve for $y'{:}$

$$
\begin{aligned}
y^{′} &= y(\frac{1}{3}(\frac{2}{x} - \frac{5x^{4}}{x^{5} + 1}))
\end{aligned}
$$

Finally, we substitute

$$
y = \sqrt[3]{\dfrac{x^2}{x^5+ 1}}{:}
$$

$$
\begin{aligned}
y^{′} &= \frac{1}{3}\sqrt[3]{\frac{x^{2}}{x^{5} + 1}}(\frac{2}{x} - \frac{5x^{4}}{x^{5} + 1}) \\
&= \frac{1}{3}\sqrt[3]{\frac{x^{2}}{x^{5} + 1}}((2(x^{5} + 1) - 5x^{5})/(x(x^{5} + 1))) \\
&= \frac{1}{3}\sqrt[3]{\frac{x^{2}}{x^{5} + 1}}((-3x^{5} + 2)/(x(x^{5} + 1))) \\
&= \frac{1}{3}\sqrt[3]{\frac{x^{2}}{x^{5} + 1}}((-3x^{5} + 2)/(\sqrt[3]{x^{3}(x^{5} + 1)^{3}})) \\
&= (-3 x 5 + 2)/(3\sqrt[3]{x(x^{5} + 1)^{4}})
\end{aligned}
$$

Therefore,

$$
g(x) = \bbox[2pt,Gainsboro]{-3x^5+2}
$$

---

**Question 5**

Consider the function $y = \sqrt{(x^{2} + 1)(x - 1)^{2}}$. The derivative of this function for $x > 1$ is given by
$y^{′} = \frac{ax^{2} + bx + c}{\sqrt{x^{2} + 1}}$.

Find the value of $a + b + c$.

- [ ] A. $4$
- [ ] B. $1$
- [ ] C. $2$
- [ ] D. $-2$
- [ ] E. $-4$

---

**Question 6**

Consider the function $y = \sqrt{\frac{x^{2} + 1}{x + 1}}$. The derivative of this function is given by
$y^{′} = (g(x))/(2(x + 1)^{3/2}\sqrt{x^{2} + 1})$.

Find the function $g(x)$.

- [ ] A. $x^{2} + 2x + 2$
- [ ] B. $x^{2} + 2x - 1$
- [ ] C. $-x^{2} + x - 1$
- [ ] D. $x^{2} + x - 2$
- [ ] E. $-x^{2} + 2x + 2$

```update-progress
```

[[MA/Single-Variable-Calculus/CA1/Home|Home]]
[[MA/Single-Variable-Calculus/CA1/0. Table of Contents/TOC|Table of Contents]]
