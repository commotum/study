# L'Hopital's Rule Applied to Tables

<!--
lesson-id: 1210
topic-code: CA1.6.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining the Limit of a Function Given as a Table of Values Using L'Hopital's Rule](#determining-the-limit-of-a-function-given-as-a-table-of-values-using-lhopitals-rule)
- [Determining the Limit of a Function Given as a Table of Values Using L'Hopital's Rule Twice](#determining-the-limit-of-a-function-given-as-a-table-of-values-using-lhopitals-rule-twice)

## Prerequisites

- [L'Hopital's Rule](<6.4.1. L'Hopital's Rule.md>)
- [Calculating Derivatives From Graphs Using the Chain Rule](<../../../5. Advanced Differentiation/5.1. Differentiating Composite Functions/Lessons/5.1.6. Calculating Derivatives From Graphs Using the Chain Rule.md>)

---

<a id="introduction"></a>
## Introduction

The function $f(x)$ is differentiable on an open interval containing $x=2$. The values of $f$ and $f'$ are given in the following table:

| $x$ | $f(x)$ | $f'(x)$ |
| --- | ---: | ---: |
| $2$ | $0$ | $4$ |

Let's find

$$
\lim\limits_{x \to 2} \dfrac{f(x)}{x^3-8}
$$

Notice that if we try to substitute $x=2$ directly into the limit, we reach the indeterminate form

$$
\dfrac{0}{0}
$$

$$
\begin{aligned}
lim_(x → 2)(f(x))/(x^{3} - 8) &= (f(x))/(x^{3} - 8) \mid _{x = 2} \\
&= (f(2))/(2^{3} - 8) \\
&= \frac{0}{8 - 8} \\
&= \frac{0}{0}
\end{aligned}
$$

So, we use L'Hopital's rule:

$$
\begin{aligned}
lim_(x → 2)(f(x))/(x^{3} - 8) &= lim_(x → 2)((f(x))^{′})/((x^{3} - 8)^{′}) \\
&= lim_(x → 2)(f^{′}(x))/(3x^{2})
\end{aligned}
$$

We substitute $x=2$ again, and this time, we are able to determine the result:

$$
\begin{aligned}
lim_(x → 2)(f^{′}(x))/(3x^{2}) &= (f^{′}(x))/(3x^{2}) \mid _{x = 2} \\
&= (f^{′}(2))/(3 \cdot 2^{2}) \\
&= \frac{4}{3 \cdot 2^{2} - 0} \\
&= \frac{1}{3}
\end{aligned}
$$

Therefore,

$$
\lim\limits_{x \to 2} \dfrac{f(x)}{x^3-8}=\dfrac{1}{3}
$$

---

<a id="determining-the-limit-of-a-function-given-as-a-table-of-values-using-lhopitals-rule"></a>
## Determining the Limit of a Function Given as a Table of Values Using L'Hopital's Rule

**Example:** The functions $f(x)$ and $g(x)$ are differentiable on an open interval containing $x=1$. The values of $f$, $g$, and their respective derivatives are shown in the following table:

| $x$ | $f(x)$ | $f'(x)$ | $g(x)$ | $g'(x)$ |
| --- | ---: | ---: | ---: | ---: |
| $1$ | $-2$ | $-5$ | $1$ | $0$ |

Find $\lim\limits_{x \to 1} \dfrac{f(x)\ln x}{x - g(x)}$.

**Explanation**

Notice that if we try to substitute $x=1$ directly into the limit, we reach the indeterminate form

$$
\dfrac{0}{0}
$$

$$
\begin{aligned}
lim_(x → 1)(f(x)\ln x)/(x - g(x)) &= (f(x)\ln x)/(x - g(x)) \mid _{x = 1} \\
&= (f(1)\ln 1)/(1 - g(1)) \\
&= ((-2) \cdot 0)/(1 - 1) \\
&= \frac{0}{0}
\end{aligned}
$$

So, we use L'Hopital's rule:

$$
\begin{aligned}
lim_(x → 1)(f(x)\ln x)/(x - g(x)) &= lim_(x → 1)((f(x)\ln x)^{′})/((x - g(x))^{′}) \\
&= lim_(x → 1)(f^{′}(x)\ln x + (f(x))/(x))/(1 - g^{′}(x))
\end{aligned}
$$

We substitute $x=1$ again, and this time, we are able to determine the result:

$$
\begin{aligned}
lim_(x → 1)(f^{′}(x)\ln x + (f(x))/(x))/(1 - g^{′}(x)) &= (f^{′}(x)\ln x + (f(x))/(x))/(1 - g^{′}(x)) \mid _{x = 1} \\
&= (f^{′}(1)\ln 1 + (f(1))/(1))/(1 - g^{′}(1)) \\
&= ((-5) \cdot 0 + (-2))/(1 - 0) \\
&=-2
\end{aligned}
$$

Therefore,

$$
\lim\limits_{x \to 1} \dfrac{f(x)\ln x}{x-g(x)}=-2
$$

---

**Question 1**

```quiz
type: radio
id: ma-16172
content: |-
  The functions $f(x)$ and $g(x)$ are differentiable on an open interval containing $x = 7$. The values of $f$, $g$, and their respective derivatives are shown in the following table:

  | $x$ | $f(x)$ | $f^{′}(x)$ | $g(x)$ | $g^{′}(x)$ |
  | --- | ---: | ---: | ---: | ---: |
  | $7$ | $0$ | $1$ | $0$ | $2$ |

  Find $lim_(x → 7)(f(x))/(x^{2}g(x))$.
options:
- id: a
  content: |-
    $\frac{1}{7}$
- id: b
  content: |-
    $49$
- id: c
  content: |-
    $-49$
- id: d
  content: |-
    $\frac{1}{98}$
  correct: true
- id: e
  content: |-
    $-7$
```

---

**Question 2**

```quiz
type: radio
id: ma-16150
content: |-
  The functions $f(x)$ and $g(x)$ are differentiable on an open interval containing $x =-3$. The values of $f$, $g$, and their respective derivatives are shown in the following table:

  | $x$ | $f(x)$ | $f^{′}(x)$ | $g(x)$ | $g^{′}(x)$ |
  | --- | ---: | ---: | ---: | ---: |
  | $-3$ | $0$ | $7$ | $-3$ | $0$ |

  Find $lim_(x → - 3)(f(x))/(x - g(x))$.
options:
- id: a
  content: |-
    $2$
- id: b
  content: |-
    $4$
- id: c
  content: |-
    $0$
- id: d
  content: |-
    $7$
  correct: true
- id: e
  content: |-
    $+ ∞$
```

---

<a id="determining-the-limit-of-a-function-given-as-a-table-of-values-using-lhopitals-rule-twice"></a>
## Determining the Limit of a Function Given as a Table of Values Using L'Hopital's Rule Twice

**Example:** The functions $f(x)$ and $g(x)$ are twice-differentiable on an open interval containing $x=-3$. The values of $f$, $g$, and their respective derivatives are given below:

| $x$ | $f(x)$ | $f'(x)$ | $f''(x)$ | $g(x)$ | $g'(x)$ | $g''(x)$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $-3$ | $1$ | $0$ | $4$ | $-3$ | $1$ | $1$ |

Find $\lim\limits_{x \to -3} \dfrac{f(x)-1}{x-g(x)}$.

**Explanation**

Notice that if we try to substitute $x=-3$ directly into the limit, we reach the indeterminate form

$$
\dfrac{0}{0}
$$

$$
\begin{aligned}
lim_(x → - 3)(f(x) - 1)/(x - g(x)) &= (f(x) - 1)/(x - g(x)) \mid _{x =-3} \\
&= (f(-3) - 1)/((-3) - g(-3)) \\
&= (1 - 1)/((-3) - (-3)) \\
&= \frac{0}{0}
\end{aligned}
$$

So, we use L'Hopital's rule:

$$
\begin{aligned}
lim_(x → - 3)(f(x) - 1)/(x - g(x)) &= lim_(x → - 3)((f(x) - 1)^{′})/((x - g(x))^{′}) \\
&= lim_(x → - 3)(f^{′}(x))/(1 - g^{′}(x))
\end{aligned}
$$

We substitute $x=-3$ again, but once more, we reach the indeterminate form

$$
\dfrac{0}{0}
$$

$$
\begin{aligned}
lim_(x → - 3)(f^{′}(x))/(1 - g^{′}(x)) &= (f^{′}(x))/(1 - g^{′}(x)) \mid _{x =-3} \\
&= (f^{′}(-3))/(1 - g^{′}(-3)) \\
&= \frac{0}{1 - 1} \\
&= \frac{0}{0}
\end{aligned}
$$

But that's okay, because we can just apply L'Hopital's rule again! We get

$$
\begin{aligned}
lim_(x → - 3)(f^{′}(x))/(1 - g^{′}(x)) &= lim_(x → - 3)((f^{′}(x))^{′})/((1 - g^{′}(x))^{′}) \\
&=-lim_(x → - 3)(f^{″}(x))/(g^{″}(x))
\end{aligned}
$$

and this time, when we substitute $x=-3$, we are actually able to determine the result:

$$
\begin{aligned}
-lim_(x → - 3)(f^{″}(x))/(g^{″}(x)) &= - (f^{″}(x))/(g^{″}(x)) \mid _{x =-3} \\
&= - (f^{″}(-3))/(g^{″}(-3)) \\
&=-\frac{4}{1} \\
&=-4
\end{aligned}
$$

Therefore,

$$
\lim\limits_{x \to -3} \dfrac{f(x)-1}{x-g(x)}=-4
$$

---

**Question 3**

```quiz
type: radio
id: ma-16175
content: |-
  The functions $f(x)$ and $g(x)$ are twice-differentiable on an open interval containing $x = 0$. The values of $f$, $g$, and their respective derivatives are given below:

  | $x$ | $f(x)$ | $f^{′}(x)$ | $f^{″}(x)$ | $g(x)$ | $g^{′}(x)$ | $g^{″}(x)$ |
  | --- | ---: | ---: | ---: | ---: | ---: | ---: |
  | $0$ | $1$ | $1$ | $0$ | $0$ | $0$ | $-1$ |

  Find $lim_(x → 0)(e^{x} - f(x))/(x^{2} + g(x))$.
options:
- id: a
  content: |-
    $-\frac{1}{3}$
- id: b
  content: |-
    $-\frac{1}{2}$
- id: c
  content: |-
    $-2$
- id: d
  content: |-
    $∞$
- id: e
  content: |-
    $1$
  correct: true
```

---

**Question 4**

```quiz
type: radio
id: ma-16174
content: |-
  The functions $f(x)$ and $g(x)$ are twice-differentiable on an open interval containing $x = 10$. The values of $f$, $g$, and their respective derivatives are given below:

  | $x$ | $f(x)$ | $f^{′}(x)$ | $f^{″}(x)$ | $g(x)$ | $g^{′}(x)$ | $g^{″}(x)$ |
  | --- | ---: | ---: | ---: | ---: | ---: | ---: |
  | $10$ | $-5$ | $0$ | $3$ | $-20$ | $-2$ | $-6$ |

  Find $lim_(x → 10)(f(x) + 5)/(2x + g(x))$.
options:
- id: a
  content: |-
    $-\frac{1}{2}$
  correct: true
- id: b
  content: |-
    $-2$
- id: c
  content: |-
    $2$
- id: d
  content: |-
    $∞$
- id: e
  content: |-
    $\frac{1}{3}$
```

```update-progress
```

[[MA/Single-Variable-Calculus/CA1/Home|Home]]
[[MA/Single-Variable-Calculus/CA1/0. Table of Contents/TOC|Table of Contents]]
