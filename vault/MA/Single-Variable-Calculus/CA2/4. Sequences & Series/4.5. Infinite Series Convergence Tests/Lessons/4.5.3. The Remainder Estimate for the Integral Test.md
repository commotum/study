# The Remainder Estimate for the Integral Test

<!--
lesson-id: 1173
topic-code: CA2.4.5.3
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Using the Remainder Estimate to Determine an Upper Bound](#using-the-remainder-estimate-to-determine-an-upper-bound)
- [Using the Remainder Estimate to Determine a Lower Bound](#using-the-remainder-estimate-to-determine-a-lower-bound)
- [Bounding the Sum of a Convergent Series](#bounding-the-sum-of-a-convergent-series)
- [Bounding a Series](#bounding-a-series)
- [The Intuition Behind the Bounds for the Remainder Estimate](#the-intuition-behind-the-bounds-for-the-remainder-estimate)

## Prerequisites

- [The Integral Test](<4.5.2. The Integral Test.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we have a convergent series whose sum equals the finite number $S{:}$

$$
\displaystyle{S = \sum_{n=1}^\infty a_n}
$$

Let $s_N$ be the $N$th partial sum of this series. Then, we can write the sum $S$ as

$$
S = s_N + R_N
$$

where $R_N$ is called the **remainder** (or **error**) of $s_N$.

The remainder $R_N$ is simply the difference between the "true" value of the series and its $N$th partial sum:

$$
R_N = S - s_N
$$

Since finding the exact sum of an infinite series is often impossible, we often wish to approximate $S$ by $s_N$ and determine upper and lower bounds for the error $R_N$.

Now, suppose we have a function $f(x)$ such that

$$
a_n = f(n),\qquad n=1,2,3,\ldots
$$

If $f(x)$ satisfies the conditions for the integral test (i.e., it is positive, continuous, decreasing), then we have the following lower and upper bounds for the remainder:

$$
\underbrace{\int_{N+1}^\infty f(x)\,\textrm d x}_{\textrm{lower bound}} \leq R_N \leq \underbrace{\int_{N}^\infty f(x)\,\textrm d x}_{\textrm{upper bound}}
$$

We call this condition the **remainder estimate for the integral test**.

We'll build some intuition behind the remainder estimate at the end of the lesson. But for now, let's consider a concrete example.

---

<a id="a-worked-example"></a>
## A Worked Example

Let's consider the following series:

$$
S = \sum_{n = 1}^\infty \dfrac 1 {n^2}
$$

Suppose we approximate $S$ by its $5$th partial sum $s_5$. What are the lower and upper bounds of the remainder $R_5$ in this case?

First, notice that the function $f(x)$, defined as

$$
f(x)= \dfrac{1}{x^2}
$$

is positive, continuous, and decreasing for

$$
x \geq 1
$$

Therefore, $f(x)$ satisfies the conditions for the integral test.

Now, the remainder estimate for the integral test states that

$$
\underbrace{\int_{6}^\infty \dfrac{1}{x^2} \,\textrm d x}_{\textrm{lower bound}} \leq R_5 \leq \underbrace{\int_{5}^\infty \dfrac{1}{x^2} \,\textrm d x}_{\textrm{upper bound}}
$$

Computing the two improper integrals, we obtain the following bounds on the remainder:

$$
\dfrac{1}{6} \leq R_5 \leq \dfrac{1}{5}
$$

Let's check this result by computing the exact value of the remainder in this case.

It can be shown that

$$
S = \sum_{n = 1}^\infty \dfrac 1 {n^2} = \dfrac{\pi^2}{6} \approx 1.644\,934
$$

rounded to six decimal places.

Moreover, the $5$th partial sum is

$$
s_5 = \sum_{n = 1}^5 \dfrac 1 {n^2} \approx 1.463\,611
$$

Therefore, the true value of the remainder $R_5$ in this case is

$$
\begin{aligned}
R_{5} &= S - s_{5} \\
&\approx 1.644934 - 1.463611 \\
&\approx 0.181
\end{aligned}
$$

rounded to three decimal places. So, we see that $R_5$ indeed lies between

$$
\dfrac16\approx 0.16
$$

and

$$
\dfrac15 = 0.25
$$

---

<a id="using-the-remainder-estimate-to-determine-an-upper-bound"></a>
## Using the Remainder Estimate to Determine an Upper Bound

**Example:** Consider the following series:
$S = \sum\limits_{n = 1}^\infty \dfrac{1}{5^n}$
Suppose that $S$ is approximated by its $4$th partial sum. Use the remainder estimate for the integral test to determine an upper bound of the remainder $R_{4}$.

**Explanation**

For any convergent series

$$
S = \displaystyle \sum_{n=1}^\infty a_n
$$

the remainder (or error) $R_N$ obtained when $S$ is approximated using its $N$th partial sum $s_N$ is defined as

$$
R_N = S - s_N
$$

If $f(x)$ satisfies the conditions for the integral test, then the remainder is bounded by

$$
\underbrace{\int_{N+1}^\infty f(x)\,\textrm d x}_{\textrm{lower bound}} \leq R_N \leq \underbrace{\int_{N}^\infty f(x)\,\textrm d x}_{\textrm{upper bound}}
$$

where $a_n = f(n)$ for

$$
n = 1, 2, 3, \ldots
$$

Notice that

$$
f(x) = \dfrac{1}{5^x}
$$

is positive, continuous, and decreasing for

$$
x \geq 1
$$

Therefore, $f(x)$ satisfies the conditions for the integral test.

We wish to compute an *upper* bound for the remainder when $S$ is approximated by $s_{4}$. Therefore, $N = 4$, and we have

$$
R_{4} \leq \int_{4}^\infty f(x) \,\textrm d x
$$

Let's now calculate the upper bound:

$$
R_{4}\begin{vmatrix}\le ∫_{4}^{∞}\frac{1}{5^{x}}dx \\ = ∫_{4}^{∞}5^{-x}dx \\ =-\frac{1}{5^{x}\ln 5}\end{vmatrix}_{4}^{∞}; =-\frac{1}{\ln 5}lim_(b → ∞) [\frac{1}{5^{x}}]_{4}^{b}; =-\frac{1}{\ln 5}lim_(b → ∞) (\frac{1}{5^{b}} - \frac{1}{5^{4}}); =-\frac{1}{\ln 5}(0 - \frac{1}{625}); =-\frac{1}{\ln 5}(-\frac{1}{625}); = \frac{1}{625\ln 5}; \approx 0.000994
$$

rounded to six decimal places.

---

**Question 1**

```quiz
type: radio
id: ma-14254
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:

  $S = ∑_(n = 1)^(∞)\frac{1}{n^{6}}$
  Suppose that $S$ is approximated by its $5$th partial sum. Use the remainder estimate for the integral test to determine an upper bound of the remainder $R_{5}$.
options:
- id: a
  content: |-
    $R_{5} \le \frac{1}{12725}$
- id: b
  content: |-
    $R_{5} \le \frac{1}{12625}$
- id: c
  content: |-
    $R_{5} \le \frac{1}{17325}$
- id: d
  content: |-
    $R_{5} \le \frac{1}{15125}$
- id: e
  content: |-
    $R_{5} \le \frac{1}{15625}$
```

---

**Question 2**

```quiz
type: radio
id: ma-14255
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:
  $S = ∑_(n = 1)^(∞)\frac{1}{2^{n}}$
  Suppose that $S$ is approximated by its $5$th partial sum. Use the remainder estimate for the integral test to determine an upper bound of the remainder $R_{5}$. Round your answer to three decimal places.
options:
- id: a
  content: |-
    $R_{5} \le 0.018$
- id: b
  content: |-
    $R_{5} \le 0.058$
- id: c
  content: |-
    $R_{5} \le 0.064$
- id: d
  content: |-
    $R_{5} \le 0.045$
- id: e
  content: |-
    $R_{5} \le 0.078$
```

---

<a id="using-the-remainder-estimate-to-determine-a-lower-bound"></a>
## Using the Remainder Estimate to Determine a Lower Bound

**Example:** Consider the following series:
$\sum\limits_{n = 1}^\infty \dfrac{1}{(2n+1)^3}$
Suppose that $S$ is approximated by its $8$th partial sum. Use the remainder estimate for the integral test to determine a lower bound of the remainder $R_{8}$.

**Explanation**

For any convergent series

$$
S=\displaystyle \sum_{n=1}^\infty a_n
$$

the remainder (or error) $R_N$ obtained when $S$ is approximated using its $N$th partial sum $s_N$ is defined as

$$
R_N = S - s_N
$$

If $f(x)$ satisfies the conditions for the integral test, then the remainder is bounded by

$$
\underbrace{\int_{N+1}^\infty f(x)\,\textrm d x}_{\textrm{lower bound}} \leq R_N \leq \underbrace{\int_{N}^\infty f(x)\,\textrm d x}_{\textrm{upper bound}}
$$

where $a_n = f(n)$ for

$$
n =1, 2, 3, \ldots
$$

Notice that

$$
f(x) = \dfrac{1}{(2x+1)^3}
$$

is positive, continuous, and decreasing for

$$
x \geq 1
$$

Therefore, $f(x)$ satisfies the conditions for the integral test.

We wish to compute a *lower* bound for the remainder when $S$ is approximated by $s_{8}$. Therefore, $N = 8$, and we have

$$
\int_{8 + 1}^\infty f(x)\,\textrm d x = \int_{9}^\infty f(x)\,\textrm d x \leq R_{8}
$$

Let's now calculate the lower bound:

$$
R_{8}\begin{vmatrix}\ge ∫_{9}^{∞}(1)/((2x + 1)^{3})dx \\ = ∫_{9}^{∞}(2x + 1)^{-3}dx \\ = - (1)/(4(2x + 1)^{2})\end{vmatrix}_{9}^{∞}; =-\frac{1}{4}lim_(b → ∞) [(1)/((2x + 1)^{2})]_{9}^{b}; =-\frac{1}{4}lim_(b → ∞) ((1)/((2b + 1)^{2}) - (1)/((2 \cdot 9 + 1)^{2})); =-\frac{1}{4}(0 - \frac{1}{361}); =-\frac{1}{4}(-\frac{1}{361}); = \frac{1}{1444}
$$

---

**Question 3**

```quiz
type: radio
id: ma-14261
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:
  $∑_(n = 1)^(∞)\frac{1}{n^{6}}$
  Suppose that $S$ is approximated by its $5$th partial sum. Use the remainder estimate for the integral test to determine a lower bound of the remainder $R_{5}$.
options:
- id: a
  content: |-
    $R_{5} \ge \frac{1}{37520}$
- id: b
  content: |-
    $R_{5} \ge \frac{1}{42250}$
- id: c
  content: |-
    $R_{5} \ge \frac{1}{28270}$
- id: d
  content: |-
    $R_{5} \ge \frac{1}{26550}$
- id: e
  content: |-
    $R_{5} \ge \frac{1}{38880}$
```

---

**Question 4**

```quiz
type: radio
id: ma-165997
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:
  $∑_(n = 1)^(∞)\frac{1}{4^{n}}$
  Suppose that $S$ is approximated by its $4$th partial sum. Use the remainder estimate for the integral test to determine a lower bound of the remainder $R_{4}$. Round your answer to six decimal places.
options:
- id: a
  content: |-
    $0.000632$
- id: b
  content: |-
    $0.000704$
- id: c
  content: |-
    $0.000945$
- id: d
  content: |-
    $0.000278$
- id: e
  content: |-
    $0.000354$
```

---

<a id="bounding-the-sum-of-a-convergent-series"></a>
## Bounding the Sum of a Convergent Series

Suppose that a convergent series

$$
S = \sum_{n = 1}^\infty a_n
$$

has the following lower and upper bounds for the remainder $R_N{:}$

$$
L \leq R_N \leq U
$$

Let's add the corresponding partial sum $s_N$ to all sides of this inequality:

$$
s_N + L \leq s_N + R_N \leq s_N + U
$$

Now, since

$$
S = s_N + R_N
$$

we obtain the following bounds on the sum $S$ of the series:

$$
s_N + L \leq S \leq s_N + U
$$

For example, we previously determined that the series

$$
S = \sum_{n = 1}^\infty \dfrac{1}{n^2}
$$

has the following lower and upper bounds for the remainder $R_5{:}$

$$
\dfrac{1}{6} \leq R_5 \leq \dfrac{1}{5}
$$

By adding the corresponding partial sum $s_5$, we obtain the following bounds for the sum:

$$
s_{5} + \frac{1}{6} \le S \le s_{5} + \frac{1}{5}
$$

---

<a id="bounding-a-series"></a>
## Bounding a Series

**Example:** Consider the following series:
$S = \sum\limits_{n = 1}^\infty \dfrac{1}{e^n}$
Suppose that $S$ is approximated by its $11$th partial sum $s_{11}$. Find the upper bounds on $S$ in terms of $s_{11}$ and the corresponding integral test remainder estimate.

**Explanation**

For any convergent series

$$
S=\displaystyle \sum_{n=1}^\infty a_n
$$

the remainder (or error) $R_N$ obtained when $S$ is approximated using its $N$th partial sum $s_N$ is defined as

$$
R_N = S - s_N
$$

If $f(x)$ satisfies the conditions for the integral test, then the remainder is bounded by

$$
\underbrace{\int_{N+1}^\infty f(x)\,\textrm d x}_{\textrm{lower bound}} \leq R_N \leq \underbrace{\int_{N}^\infty f(x)\,\textrm d x}_{\textrm{upper bound}}
$$

where $a_n = f(n)$ for

$$
n=1,2,3\ldots
$$

Notice that

$$
f(x)= \dfrac{1}{e^x}
$$

is positive, continuous, and decreasing for

$$
x\geq 1
$$

Therefore, $f(x)$ satisfies the conditions for the integral test.

We wish to compute a *upper* bound for the remainder when $S$ is approximated by $s_{11}$. Therefore, $N=11$, and we have

$$
R_{11} \leq \int_{11}^\infty f(x)\,\textrm d x
$$

Let's now calculate the upper bound:

$$
R_{11}\begin{vmatrix}\le ∫_{11}^{∞}\frac{1}{e^{x}}dx \\ = ∫_{11}^{∞}e^{-x}dx \\ =-\frac{1}{e^{x}}\end{vmatrix}_{11}^{∞}; =-lim_(b → ∞) [\frac{1}{e^{x}}]_{11}^{b}; =-lim_(b → ∞) (\frac{1}{e^{b}} - \frac{1}{e^{11}}); = - (0 - \frac{1}{e^{11}}); = - (-\frac{1}{e^{11}}); = \frac{1}{e^{11}}
$$

Finally, we can form an upper bound for $S$ by adding $s_{11}$ to both sides of our inequality, as follows:

$$
\begin{bmatrix}R_{11} & \le \frac{1}{e^{11}} \\ s_{11} + R_{11} & \le s_{11} + \frac{1}{e^{11}} \\ S & \le s_{11} + \frac{1}{e^{11}}\end{bmatrix}
$$

---

**Question 5**

```quiz
type: radio
id: ma-14250
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:
  $S = ∑_(n = 1)^(∞)(1)/((n + 3)^{2})$

  Suppose that $S$ is approximated by its $10$th partial sum $s_{10}$. Which of the following is an upper bounds on $S$ in terms of $s_{10}$ and the integral test reminder estimate?
options:
- id: a
  content: |-
    $S \le s_{10} + \frac{1}{13}$
- id: b
  content: |-
    $S \le s_{10} + \frac{1}{14}$
- id: c
  content: |-
    $S \le s_{10} + \frac{1}{11}$
- id: d
  content: |-
    $S \le s_{10} + \frac{1}{15}$
- id: e
  content: |-
    $S \le s_{10} + \frac{1}{12}$
```

---

**Question 6**

```quiz
type: radio
id: ma-138974
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  > A scientific calculator is required to answer this question.

  Consider the following series:
  $S = ∑_(n = 1)^(∞)\frac{1}{n^{6}}$
  Suppose that $S$ is approximated by its $6$th partial sum $s_{6}$. Which of the following is an upper bounds on $S$ in terms of $s_{6}$ and the integral test reminder estimate?
options:
- id: a
  content: |-
    $S \ge s_{6} + \frac{1}{78175}$
- id: b
  content: |-
    $S \ge s_{6} + \frac{1}{74225}$
- id: c
  content: |-
    $S \ge s_{6} + \frac{1}{87775}$
- id: d
  content: |-
    $S \ge s_{6} + \frac{1}{84035}$
- id: e
  content: |-
    $S \ge s_{6} + \frac{1}{74275}$
```

---

<a id="the-intuition-behind-the-bounds-for-the-remainder-estimate"></a>
## The Intuition Behind the Bounds for the Remainder Estimate

Earlier, we determined that the remainder $R_5$ for the series

$$
S = \sum_{n = 1}^\infty \dfrac 1 {n^2}
$$

should satisfy the following inequality:

$$
\underbrace{\int_{6}^\infty \dfrac{1}{x^2} \,\textrm d x}_{\textrm{lower bound}} \leq R_5 \leq \underbrace{\int_{5}^\infty \dfrac{1}{x^2} \,\textrm d x}_{\textrm{upper bound}}
$$

To see why this is true, first recall that the function

$$
f(x)=\dfrac{1}{x^2}, \qquad x\geq 1
$$

satisfies the conditions for the integral test.

Let's visualize $f(x)$ and the corresponding terms

$$
a_n = \dfrac{1}{n^2}
$$

for

$$
n\geq 1
$$

using a diagram.

![](<../Source/4.5.3. The Remainder Estimate for the Integral Test/Images/f89ffc5c11d947d740d7837e73f150c2.png>)

We can represent the sum of our series using the areas of vertical bars with a width of $1$ in the following two ways:

![](<../Source/4.5.3. The Remainder Estimate for the Integral Test/Images/48df192239a02b9f9bcfae451cf09487.png>)

Note the following:

- On the left diagram, the *top-left* corner of the $n$th bar coincides with $a_n$.
- On the right diagram, the *top-right* corner of the $n$th bar coincides with $a_n$.

In both cases, the partial sum $s_5$ is represented by the combined area of the first $5$ bars, and the remainder $R_5$ is represented by the combined area of the remaining bars.

Finally, consider the following two diagrams.

![](<../Source/4.5.3. The Remainder Estimate for the Integral Test/Images/b396c2657ce1cf3faf984a223924c5ea.png>)

Notice that:

- On the left diagram, the bars corresponding to $R_5$ contain the area below the curve for $x\geq 6$. This means that
$\int_6^\infty \dfrac{1}{x^2} \, \textrm{d}x \leq R_5$.
- On the right diagram, the area below the curve contains the bars corresponding to $R_5$ for $x\geq 5$. This means that
$R_5 \leq \int_5^\infty \dfrac{1}{x^2} \, \textrm{d}x$.

Combining these two inequalities, we arrive at

$$
\int_{6}^\infty \dfrac{1}{x^2} \,\textrm d x \leq R_5 \leq \int_{5}^\infty \dfrac{1}{x^2} \,\textrm d x
$$

```update-progress
```

[[MA/Single-Variable-Calculus/CA2/Home|Home]]
[[MA/Single-Variable-Calculus/CA2/0. Table of Contents/TOC|Table of Contents]]
