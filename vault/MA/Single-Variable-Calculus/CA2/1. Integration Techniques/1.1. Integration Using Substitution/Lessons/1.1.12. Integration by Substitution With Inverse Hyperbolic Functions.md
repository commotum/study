# Integration by Substitution With Inverse Hyperbolic Functions

<!--
lesson-id: 3258
topic-code: CA2.1.1.12
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Sine](#calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-sine)
- [Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Cosine](#calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-cosine)
- [Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Tangent](#calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-tangent)
- [Calculating Definite Integrals Using Substitution With Inverse Hyperbolic Functions](#calculating-definite-integrals-using-substitution-with-inverse-hyperbolic-functions)

## Prerequisites

- [Integration by Substitution With Inverse Trigonometric Functions](<1.1.10. Integration by Substitution With Inverse Trigonometric Functions.md>)
- [Integration Using Inverse Hyperbolic Functions](<../../../../CA1/8. Integration/8.1. Indefinite Integrals/Lessons/8.1.9. Integration Using Inverse Hyperbolic Functions.md>)

---

<a id="introduction"></a>
## Introduction

Consider the integral

$$
\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}
$$

$$
\displaystyle \int \dfrac{2}{\sqrt{4x^2-1}} \, \textrm{d}x
$$

As it is, this integral doesn't resemble any of the basic integrals we have encountered so far. However, we can rewrite our integral as

$$
\int \dfrac{2}{\sqrt{4x^2-1}}\,\textrm{d}x = 2 \int \dfrac{1}{\sqrt{\left(2x\right)^2-1}}\textrm{d}x
$$

which becomes very similar to the basic integral for the inverse hyperbolic cosine,

$$
\int \frac{1}{\sqrt{u^2-1}}\,\textrm{d}u = \arcosh u+C
$$

The only difference is that instead of $u$ as the variable, we have $2x$.

So, let $u=2x$. Differentiating, we have

$$
\dfrac{\textrm{d}u}{\textrm{d}x}=2 \quad\Longrightarrow\quad \textrm{d}x = \dfrac{1}{2} \,\textrm{d}u
$$

Using the above, we can write the integral in terms of $u$ and compute it, as follows:

$$
\begin{aligned}
∫\frac{2}{\sqrt{4x^{2} - 1}}dx &= 2∫(1)/(\sqrt{(2x)^{2} - 1})dx \\
&= 2∫\frac{1}{\sqrt{u^{2} - 1}} \cdot \frac{1}{2}du \\
&= ∫\frac{1}{\sqrt{u^{2} - 1}}du \\
&= arcosh u + C \\
&= arcosh (2x) + C
\end{aligned}
$$

---

<a id="calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-sine"></a>
## Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Sine

**Example:** $\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits} \displaystyle \int \dfrac{\pi}{\sqrt{2x^2+1}} \, \textrm{d}x=$

**Explanation**

$$
\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}
$$

This integral resembles the basic integral for the inverse hyperbolic sine,

$$
\int \frac{1}{\sqrt{u^2+1}}\,\textrm{d}u = \arsinh u+C
$$

The only difference is that instead of $u^2$ as the variable, we have

$$
2x^2 = \left(\sqrt{2}\,x\right)^2
$$

So, let's substitute

$$
u =\sqrt{2}\,x
$$

Differentiating, we get

$$
\dfrac{\textrm{d}u}{\textrm{d}x}=\sqrt{2} \quad\Longrightarrow\quad \textrm{d}x = \dfrac{1}{\sqrt{2}} \,\textrm{d}u
$$

Using the above, we can write the integral in terms of $u$ and compute it, as follows:

$$
\begin{aligned}
∫\frac{π}{\sqrt{2x^{2} + 1}}dx &= ∫(π)/(\sqrt{(\sqrt{2}x)^{2} + 1})dx \\
&= π∫\frac{1}{\sqrt{u^{2} + 1}} \cdot (\frac{1}{\sqrt{2}}du) \\
&= \frac{π}{\sqrt{2}}∫\frac{1}{\sqrt{u^{2} + 1}}du \\
&= \frac{π}{\sqrt{2}}arsinh u + C \\
&= \frac{π}{\sqrt{2}}arsinh (\sqrt{2}x) + C
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-95431
content: |-
  What is $∫\frac{5}{\sqrt{x^{2} + 4}}dx$?
options:
- id: a
  content: |-
    $5arsinh (\frac{x}{2}) + C$
  correct: true
- id: b
  content: |-
    $\frac{5}{2}arsinh (\frac{x}{2}) + C$
- id: c
  content: |-
    $\frac{5}{2}arcosh (\frac{x}{2}) + C$
- id: d
  content: |-
    $5arcosh (\frac{x}{2}) + C$
- id: e
  content: |-
    $\frac{1}{5}arcosh (\frac{x}{2}) + C$
```

---

**Question 2:**

```quiz
type: radio
id: ma-95430
content: |-
  What is $∫\frac{1}{\sqrt{9x^{2} + 1}}dx$?
options:
- id: a
  content: |-
    $\frac{1}{3}arcosh 3x + C$
- id: b
  content: |-
    $\frac{1}{9}arsinh 3x + C$
- id: c
  content: |-
    $\frac{1}{3}arsinh 3x + C$
  correct: true
- id: d
  content: |-
    $\frac{1}{3}arcosh 3x + C$
- id: e
  content: |-
    $9arsinh 3x + C$
```

---

<a id="calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-cosine"></a>
## Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Cosine

**Example:** $\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}$

$\displaystyle \int \dfrac{3}{\sqrt{x^2-4}} \,\textrm{d}x =$

**Explanation**

$$
\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}
$$

Let's rewrite our integral as

$$
\int \dfrac{3}{\sqrt{x^2-4}}\,\textrm{d}x = \dfrac{3}{2} \int \dfrac{1}{\sqrt{\left(\frac{x}{2}\right)^2-1}} \, \textrm{d}x
$$

This integral resembles the basic integral for the inverse hyperbolic cosine,

$$
\int \frac{1}{\sqrt{u^2-1}}\,\textrm{d}u = \arcosh u+C
$$

The only difference is that instead of $u^2$ as the variable, we have

$$
\left(\dfrac{x}{2}\right)^2
$$

So, let's substitute

$$
u = \dfrac x 2
$$

Differentiating, we get

$$
\dfrac{\textrm{d}u}{\textrm{d}x}= \dfrac{1}{2} \quad\Longrightarrow\quad \textrm{d}x =2\,\textrm{d}u
$$

Using the above, we can write the integral in terms of $u$ and evaluate it, as follows:

$$
\begin{aligned}
∫\frac{3}{\sqrt{x^{2} - 4}}dx &= \frac{3}{2}∫(1)/(\sqrt{(\frac{x}{2})^{2} - 1})dx \\
&= \frac{3}{2}∫\frac{1}{\sqrt{u^{2} - 1}} \cdot 2du \\
&= 3∫\frac{du}{\sqrt{u^{2} - 1}} \\
&= 3arcosh u + C \\
&= 3arcosh (\frac{x}{2}) + C
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-95433
content: |-
  What is $∫\frac{2}{\sqrt{x^{2} - 9}}dx$?
options:
- id: a
  content: |-
    $3arcosh (\frac{x}{2}) + C$
- id: b
  content: |-
    $9arsinh (\frac{x}{3}) + C$
- id: c
  content: |-
    $2arcosh (\frac{x}{3}) + C$
  correct: true
- id: d
  content: |-
    $9arcosh (\frac{x}{2}) + C$
- id: e
  content: |-
    $3arsinh (\frac{x}{9}) + C$
```

---

**Question 4:**

```quiz
type: radio
id: ma-95432
content: |-
  What is $∫\frac{1}{\sqrt{16x^{2} - 1}}dx$?
options:
- id: a
  content: |-
    $\frac{1}{4}arcosh 4x + C$
  correct: true
- id: b
  content: |-
    $4arsinh (\frac{x}{4}) + C$
- id: c
  content: |-
    $\frac{1}{4}arsinh 4x + C$
- id: d
  content: |-
    $\frac{1}{16}arcosh 4x + C$
- id: e
  content: |-
    $16arcosh (\frac{x}{4}) + C$
```

---

<a id="calculating-indefinite-integrals-using-substitution-with-inverse-hyperbolic-tangent"></a>
## Calculating Indefinite Integrals Using Substitution With Inverse Hyperbolic Tangent

**Example:** $\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}$

$\displaystyle \int \dfrac{10}{1 - 4x^2} \, \textrm{d}x =$

**Explanation**

$$
\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}
$$

This integral resembles the basic integral for the inverse hyperbolic tangent,

$$
\int \frac{1}{1-u^2} \, \textrm{d}u =\artanh u +C
$$

The only difference is that instead of $u^2$ as the variable, we have

$$
4x^2 = \left(2x\right)^2
$$

So, let's substitute $u=2x$. Differentiating, we get

$$
\dfrac{\textrm{d}u}{\textrm{d}x}=2\quad\Longrightarrow\quad \dfrac 1 2\,\textrm d u = \textrm d x
$$

Using the above, we can write the integral in terms of $u$ and evaluate it, as follows:

$$
\begin{aligned}
∫\frac{10}{1 - 4x^{2}}dx &= 10∫(1)/(1 - (2x)^{2})dx \\
&= 10∫\frac{1}{1 - u^{2}} \cdot \frac{1}{2}du \\
&= 5∫\frac{1}{1 - u^{2}}du \\
&= 5artanh u + C \\
&= 5artanh 2x + C
\end{aligned}
$$

Note that this result is subject to the condition

$$
\left|2x \right| < 1
$$

---

**Question 5:**

```quiz
type: radio
id: ma-95435
content: |-
  What is $∫\frac{7}{25 - x^{2}}dx$?
options:
- id: a
  content: |-
    $\frac{7}{2}artanh (\frac{x}{7}) + C$
- id: b
  content: |-
    $\frac{7}{5}artanh (\frac{x}{3}) + C$
- id: c
  content: |-
    $\frac{7}{5}artanh (\frac{x}{5}) + C$
  correct: true
- id: d
  content: |-
    $\frac{1}{7}artanh (\frac{x}{5}) + C$
- id: e
  content: |-
    $\frac{1}{5}artanh (\frac{x}{7}) + C$
```

---

**Question 6:**

```quiz
type: radio
id: ma-95434
content: |-
  What is $∫\frac{3}{1 - 36x^{2}}dx$?
options:
- id: a
  content: |-
    $\frac{1}{2}artanh (\frac{x}{3}) + C$
- id: b
  content: |-
    $\frac{1}{2}artanh 36x + C$
- id: c
  content: |-
    $\frac{1}{2}artanh (\frac{x}{2}) + C$
- id: d
  content: |-
    $\frac{1}{2}artanh 6x + C$
  correct: true
- id: e
  content: |-
    $\frac{1}{6}artanh 6x + C$
```

---

<a id="calculating-definite-integrals-using-substitution-with-inverse-hyperbolic-functions"></a>
## Calculating Definite Integrals Using Substitution With Inverse Hyperbolic Functions

**Example:** $\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}$

Evaluate the integral $\displaystyle{\int_{4}^{2\sqrt{5}} \dfrac{1}{\sqrt{x^2 - 4}}\, \textrm{d}x}$.

**Explanation**

$$
\newcommand{\arsinh}{\mathop{\rm arsinh}\nolimits} \newcommand{\arcosh}{\mathop{\rm arcosh}\nolimits} \newcommand{\artanh}{\mathop{\rm artanh}\nolimits} \newcommand{\sech}{\mathop{\rm sech}\nolimits} \newcommand{\csch}{\mathop{\rm csch}\nolimits} \newcommand{\coth}{\mathop{\rm coth}\nolimits} \newcommand{\arsech}{\mathop{\rm arsech}\nolimits} \newcommand{\arcsch}{\mathop{\rm arcsch}\nolimits} \newcommand{\arcoth}{\mathop{\rm arcoth}\nolimits}
$$

Note that we can rewrite this integral as

$$
\int_{4}^{2\sqrt{5}} \dfrac{1}{\sqrt{x^2 - 4}}\, \textrm{d}x = \dfrac{1}{2}\int_{4}^{2\sqrt{5}} \dfrac{1}{\sqrt{\left(\frac {x}{2} \right)^2 - 1}} \, \textrm{d}x
$$

This integral resembles the basic integral for inverse hyperbolic cosine,

$$
\int \frac{1}{\sqrt{u^2-1}}\, \textrm{d}u = \arcosh{u}+C
$$

The only difference is that instead of $u^2$ as the variable, we have

$$
\left(\dfrac {x}{2} \right)^2
$$

So, let's substitute

$$
u = \dfrac {x}{2}
$$

Differentiating, we get

$$
\dfrac{\textrm{d}u}{\textrm{d}x} = \dfrac{1}{2} \quad\Longrightarrow\quad \textrm{d}x = 2\,\textrm{d}u
$$

Before substituting, we use the table below to change the limits from $x$ to $u$.
$\begin{bmatrix}x & 4 & 2\sqrt{5} \\ u & 2 & \sqrt{5}\end{bmatrix}$
Using the above, we can now write the integral in terms of $u$ and evaluate:

$$
\begin{aligned} \int_{4}^{2\sqrt{5}} \dfrac{1}{\sqrt{x^2 - 4}}\, \textrm{d}x &= \dfrac{1}{2}\int_{4}^{2\sqrt{5}} \dfrac{1}{\sqrt{\left(\frac {x}{2} \right)^2 - 1}} \, \textrm{d}x \\[5pt] &= \dfrac{1}{2} \int_{2}^{\sqrt 5} \dfrac{1}{\sqrt{u^2 - 1}} \cdot (2 \, \textrm{d}u) \\[5pt] &= \int_{2}^{\sqrt 5} \dfrac{\textrm{d}u}{\sqrt{u^2 - 1}} \\[5pt] &= \arcosh u \Big \mid _{2}^{\sqrt 5} \\[5pt] &= \arcosh{\sqrt 5} - \arcosh{2} \\[5pt] &= \ln\left(\sqrt 5 + \sqrt{\left(\sqrt 5\right)^2 - 1}\right) - \ln\left(2 + \sqrt{\left(2\right)^2 - 1}\right) \\[5pt] &= \ln\left(2 + \sqrt{5}\right) - \ln\left(2 + \sqrt{3}\right) \\[5pt] & = \ln\left(\dfrac{2 + \sqrt{5}}{2 + \sqrt{3}} \right) \end{aligned}
$$

---

**Question 7:**

```quiz
type: radio
id: ma-95437
content: |-
  Evaluate the integral $∫_{0}^{3}\frac{6}{\sqrt{x^{2} + 3}}dx$.
options:
- id: a
  content: |-
    $6\sqrt{3}\ln (\sqrt{3} + 1)$
- id: b
  content: |-
    $\sqrt{3}\ln (\sqrt{3} + 2)$
- id: c
  content: |-
    $3\sqrt{3}\ln (\sqrt{2} + 2)$
- id: d
  content: |-
    $3\ln (\sqrt{6} + 1)$
- id: e
  content: |-
    $6\ln (\sqrt{3} + 2)$
  correct: true
```

---

**Question 8:**

```quiz
type: radio
id: ma-95436
content: |-
  Evaluate the integral

  $∫_{0}^{1/4}\frac{2}{3 - 12x^{2}}dx$.
options:
- id: a
  content: |-
    $\frac{1}{3}artanh (\frac{1}{2})$
  correct: true
- id: b
  content: |-
    $\frac{1}{3}artanh (\frac{1}{4})$
- id: c
  content: |-
    $\frac{1}{6}artanh (\frac{1}{2})$
- id: d
  content: |-
    $\frac{2}{3}artanh 1$
- id: e
  content: |-
    $\frac{2}{3}artanh (\frac{1}{4})$
```

```update-progress
```

[[MA/Single-Variable-Calculus/CA2/Home|Home]]
[[MA/Single-Variable-Calculus/CA2/0. Table of Contents/TOC|Table of Contents]]
