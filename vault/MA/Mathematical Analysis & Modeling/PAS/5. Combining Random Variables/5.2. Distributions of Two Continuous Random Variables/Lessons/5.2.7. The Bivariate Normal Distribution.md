# The Bivariate Normal Distribution

<!--
lesson-id: 918
topic-code: PAS.5.2.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Normal Distribution vs. Bivariate Normal Distribution](#normal-distribution-vs-bivariate-normal-distribution)
- [Interpretation of Parameters in a Bivariate Normal Distribution](#interpretation-of-parameters-in-a-bivariate-normal-distribution)
- [The Joint PDF of a Bivariate Normal Random Variable](#the-joint-pdf-of-a-bivariate-normal-random-variable)
- [Linear Combinations of Normal Random Variables From Bivariate Distributions](#linear-combinations-of-normal-random-variables-from-bivariate-distributions)
- [Linear Combinations of Elements From a Bivariate Normal Random Variable](#linear-combinations-of-elements-from-a-bivariate-normal-random-variable)
- [The Multivariate Normal Distribution](#the-multivariate-normal-distribution)
- [Linear Combinations of Elements From a Multivariate Normal Random Variable](#linear-combinations-of-elements-from-a-multivariate-normal-random-variable)

## Prerequisites

- [Level Curves](<../../../../MVC/2. Multivariable Functions/2.1. Introduction to Multivariable Functions/Lessons/2.1.5. Level Curves.md>)
- [Joint Distributions for Continuous Random Variables](<5.2.1. Joint Distributions for Continuous Random Variables.md>)
- [Combining Multiple Normally Distributed Random Variables](<../../5.3. Linear Combinations of Random Variables/Lessons/5.3.4. Combining Multiple Normally Distributed Random Variables.md>)
- [The Covariance Matrix](<../../5.5. Covariance of Random Variables/Lessons/5.5.3. The Covariance Matrix.md>)
- [Variance of Sums of Random Variables](<../../5.5. Covariance of Random Variables/Lessons/5.5.2. Variance of Sums of Random Variables.md>)

---

<a id="introduction"></a>
## Introduction

Let

$$
\mathbf{X} = [X_1, X_2]^T
$$

be two (dependent) random variables. We say that $\mathbf{X}$ is a **bivariate normal vector** with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$ if the joint probability density function of $\mathbf{X}$ is given by

$$
f(x_1, x_2) = \dfrac{1}{2\pi\sqrt{\mid \Sigma \mid}} \mathrm{exp}\left(-\dfrac 12(\mathbf X - \boldsymbol \mu)^T \,\Sigma^{-1} \,(\mathbf X - \boldsymbol \mu)\right)
$$

where

$$
μ = \begin{bmatrix}μ_{1} \\ μ_{2}\end{bmatrix}, Σ = \begin{bmatrix}σ_{1}^{2} & σ_{12} \\ σ_{21} & σ_{2}^{2}\end{bmatrix} = \begin{bmatrix}σ_{1}^{2} & ρσ_{1}σ_{2} \\ ρσ_{1}σ_{2} & σ_{2}^{2}\end{bmatrix}
$$

Here,

- $\sigma^2_1 = \textrm{Var}[X_1]$ and $\sigma^2_2 = \textrm{Var}[X_2]$ are the variances of $X_1$ and $X_2$,
- $\sigma_{12} = \sigma_{21} = \textrm{Cov}[X_1,X_2]$ is the covariance of $X_1$ and $X_2$,
- $\rho$ is the linear correlation coefficient of $X_1$ and $X_2$, and
- $\mid \Sigma \mid$ denotes the determinant of $\Sigma$, the covariance matrix.

A graph of a typical density function of a bivariate normal vector is depicted below.

![](<../Source/5.2.7. The Bivariate Normal Distribution/Images/becddc06fb28610a93d1f37c60dd4f1d.png>)

This surface is often referred to as having a "bell shape."

---

<a id="normal-distribution-vs-bivariate-normal-distribution"></a>
## Normal Distribution vs. Bivariate Normal Distribution

Recall that a normally distributed random variable $X\sim N(\mu,\sigma^2)$ has the probability density function

$$
f(x) = \dfrac{1}{\sigma \sqrt{2\pi}} e^{-\frac{1}{2} \left(\frac{x-\mu}{\sigma} \right)^2}
$$

This probability density function can be written as

$$
f(x) = (2\pi)^{-{\color{red}1}/2} ({\color{blue}\sigma^2})^{-1/2} \: \mathrm{exp}\left(-\frac{1}{2} (x-\mu) ({\color{blue}\sigma^2})^{-1} (x-\mu) \right)
$$

Let's compare this to the density function of a bivariate normal vector. First, we rewrite the joint density $f(x_1,x_2)$ of the bivariate normal distribution as follows:

$$
\begin{aligned}
f(x_{1}, x_{2}) &= \frac{1}{2π\sqrt{\mid Σ \mid}}\exp(-\frac{1}{2}(X - μ)^{T}Σ^{-1}(X - μ)) \\
&= (2π)^{-1}\begin{vmatrix}Σ & ^{-1/2}\exp(-\frac{1}{2}(X - μ)^{T}Σ^{-1}(X - μ)) \\ = (2π)^{-2/2} & Σ\end{vmatrix}^{-1/2}\exp(-\frac{1}{2}(X - μ)^{T}Σ^{-1}(X - μ))
\end{aligned}
$$

By comparing the expressions for $f(x)$ and $f(x_1,x_2)$, we see that the *bivariate* normal distribution can be considered as a generalization of the *normal distribution* from the ${\color{red}1}$-dimensional case (when we have only one variable, $x$) to the ${\color{red}2}$-dimensional case (when we have two variables, $x_1$ and $x_2$).

This analogy will be particularly helpful when extending the bivariate normal distribution to more than two random variables.

---

<a id="interpretation-of-parameters-in-a-bivariate-normal-distribution"></a>
## Interpretation of Parameters in a Bivariate Normal Distribution

Let's consider a bivariate normal vector

$$
\mathbf{X} = [X_1, X_2]^T
$$

with the joint probability density function

$$
f(x_1, x_2) = \dfrac{1}{2\pi\sqrt{\mid \Sigma \mid}} \mathrm{exp}\left(-\dfrac 12(\mathbf X - \boldsymbol \mu)^T \,\Sigma^{-1} \,(\mathbf X - \boldsymbol \mu)\right)
$$

where the mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$ are given by

$$
μ = \begin{bmatrix}μ_{1} \\ μ_{2}\end{bmatrix}, Σ = \begin{bmatrix}σ_{1}^{2} & σ_{12} \\ σ_{21} & σ_{2}^{2}\end{bmatrix} = \begin{bmatrix}σ_{1}^{2} & ρσ_{1}σ_{2} \\ ρσ_{1}σ_{2} & σ_{2}^{2}\end{bmatrix}
$$

Most of the parameters of the distribution can be interpreted geometrically on a corresponding graph of the probability density function:

- The mean vector $\boldsymbol{\mu}$ gives the $xy$-coordinates of the peak.
- The correlation coefficient $\rho$ defines how much the bell is squeezed towards the plane $y=x{:}$ when $\rho \to 0$, the horizontal cross-sections are almost circular, whilewhen $\rho \to 1$, the shape becomes more and more squeezed towards the plane $y=x$.

![](<../Source/5.2.7. The Bivariate Normal Distribution/Images/783789f61ba85060e43242de6ea0eeed.png>)

- The variances $\sigma_1^2$ and $\sigma_2^2$ represent how much the curve is spread out along the principal axes $x_1=\mu_1$ and $x_2=\mu_2{:}$when $\sigma_1^2$ and $\sigma_2^2$ are small, the bell shape is more "pointy," whilefor larger $\sigma_1^2$ and $\sigma_2^2$, the bell shape is more "flat."

The level curves of the surface have equations of the form

$$
(\mathbf X - \boldsymbol \mu)^T \,\Sigma^{-1} \,(\mathbf X - \boldsymbol \mu) = \textrm{const}
$$

which represent ellipses centered at $(\mu_1,\mu_2)$.

![](<../Source/5.2.7. The Bivariate Normal Distribution/Images/dcc9210c43f7ddc33f5e3e99a2d40a0a.png>)

---

<a id="the-joint-pdf-of-a-bivariate-normal-random-variable"></a>
## The Joint PDF of a Bivariate Normal Random Variable

**Example:** Let $\mathbf{X} = [X_1, X_2]^T$ be a bivariate normal random vector. The variances of $X_1$ and $X_2$ and their correlation coefficient are given by

$\sigma_1^2=9,\qquad \sigma_2^2 = 1, \qquad \rho = \dfrac1{3}$.

Given that the joint probability density function of $\mathbf X$ is
$f(\mathbf x) = \dfrac1k\,\mathrm{exp}\left(-\dfrac 12(\mathbf x - \boldsymbol \mu)^T \,\Sigma^{-1} \,(\mathbf x - \boldsymbol \mu)\right)$,

where $\boldsymbol{\mu}$ is the mean vector and $\Sigma$ is the corresponding covariance matrix, find the value of $k$.

**Explanation**

Recall that if

$$
\mathbf{X} = [X_1, X_2]^T
$$

is a bivariate normal vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$, given by

$$
μ = (μ_{1}; μ_{2}), Σ = \begin{bmatrix}σ_{1}^{2} & σ_{12} \\ σ_{21} & σ_{2}^{2}\end{bmatrix} = \begin{bmatrix}σ_{1}^{2} & ρσ_{1}σ_{2} \\ ρσ_{1}σ_{2} & σ_{2}^{2}\end{bmatrix}
$$

where

$$
\sigma^2_i = \textrm{Var}[X_i]
$$

$$
\sigma_{12} = \textrm{Cov}[X_1,X_2]
$$

and $\rho$ is the correlation, then the joint probability density function of $\mathbf X$ is

$$
f(\mathbf x) = \dfrac{1}{2\pi\sqrt{\mid \Sigma \mid}} \mathrm{exp}\left(-\dfrac 12(\mathbf x - \boldsymbol \mu)^T \,\Sigma^{-1} \,(\mathbf x - \boldsymbol \mu)\right)
$$

and $\mid \Sigma \mid$ denotes the determinant of $\Sigma$.

Using the given values of $\sigma^2_1, \sigma^2_2$, and $\rho$, our covariance matrix is

$$
\begin{bmatrix}9 & \frac{1}{3} \cdot \sqrt{9} \cdot \sqrt{1} \\ \frac{1}{3} \cdot \sqrt{9} \cdot \sqrt{1} & 1\end{bmatrix}
$$

Therefore,

$$
\mid \Sigma \mid = 9\cdot 1 - 1\cdot 1 = 8
$$

Substituting this value into our expression for the joint PDF, we obtain

$$
\begin{aligned}
f(x) &= \frac{1}{2π\sqrt{8}}\exp(-\frac{1}{2}(x - μ)^{T}Σ^{-1}(x - μ)) \\
&= \frac{1}{4 π 2}\exp(-\frac{1}{2}(x - μ)^{T}Σ^{-1}(x - μ))
\end{aligned}
$$

Finally,

$$
k=4\pi\sqrt 2
$$

---

**Question 1**

Let $X = [X_{1}, X_{2}]^{T}$ be a bivariate normal random vector. The variances of $X_{1}$ and $X_{2}$ and their covariance are given by

$σ_{1}^{2} = 10, σ_{2}^{2} = 5, σ_{12} = 5$.

Given that the joint probability density function of $X$ is
$f(x) = \frac{1}{k}\exp(-\frac{1}{2}(x - μ)^{T}Σ^{-1}(x - μ))$,

where $μ$ is the mean vector and $Σ$ is the corresponding covariance matrix, find the value of $k$.

- [ ] A. $2π\sqrt{5}$
- [ ] B. $10π$
- [ ] C. $5π$
- [ ] D. $π\sqrt{5}$
- [ ] E. $2π\sqrt{3}$

---

**Question 2**

Let $X = [X_{1}, X_{2}]^{T}$ be a bivariate normal random vector. The variances of $X_{1}$ and $X_{2}$ and their correlation coefficient are given by

$σ_{1}^{2} = 1, σ_{2}^{2} = 4, ρ = \frac{1}{2}$.

Given that the joint probability density function of $X$ is
$f(x) = \frac{1}{k}\exp(-\frac{1}{2}(x - μ)^{T}Σ^{-1}(x - μ))$,

where $μ$ is the mean vector and $Σ$ is the corresponding covariance matrix, find the value of $k$.

- [ ] A. $2π\sqrt{3}$
- [ ] B. $2π\sqrt{5}$
- [ ] C. $π\sqrt{5}$
- [ ] D. $π\sqrt{3}$
- [ ] E. $π\sqrt{2}$

---

<a id="linear-combinations-of-normal-random-variables-from-bivariate-distributions"></a>
## Linear Combinations of Normal Random Variables From Bivariate Distributions

A linear combination of coordinates of a bivariate normal random vector generates a (one-dimensional) normal distribution:

> *If $\mathbf{X} = [X_1, X_2]^T$ is a bivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$, given by*
> $μ = \begin{bmatrix}μ_{1} \\ μ_{2}\end{bmatrix}, Σ = \begin{bmatrix}σ_{1}^{2} & σ_{12} \\ σ_{21} & σ_{2}^{2}\end{bmatrix}$,
> *then the random variable $Y$, defined as*
> $Y= aX_1 + bX_2$
> *is a normally distributed random variable for all values of $a$ and $b$. Furthermore, we have that*
> $E[Y]|= aμ_{1} + bμ_{2},; Var[Y]|= a^{2}σ_{1}^{2} + b^{2}σ_{2}^{2} + 2abσ_{12}$.

---

<a id="linear-combinations-of-elements-from-a-bivariate-normal-random-variable"></a>
## Linear Combinations of Elements From a Bivariate Normal Random Variable

**Example:** Let $\mathbf{X} = [X_1,X_2]^T$ be a bivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$ given by
$μ = \begin{bmatrix}2 \\ -1\end{bmatrix}, Σ = \begin{bmatrix}4 & -3 \\ -3 & 7\end{bmatrix}$.
Find the distribution of $Y = 3X_1 + X_2$.

**Explanation**

Recall that if

$$
\mathbf{X} = [X_1, X_2]^T
$$

is a bivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$, given by

$$
μ = \begin{bmatrix}μ_{1} \\ μ_{2}\end{bmatrix}, Σ = \begin{bmatrix}σ_{1}^{2} & σ_{12} \\ σ_{21} & σ_{2}^{2}\end{bmatrix}
$$

then the random variable $Y$, defined as

$$
Y= aX_1 + bX_2
$$

is a normally distributed random variable for all values of $a$ and $b$, where

$$
\begin{aligned}
E[Y] &= aμ_{1} + bμ_{2}, \\
Var[Y] &= a^{2}σ_{1}^{2} + b^{2}σ_{2}^{2} + 2abσ_{12}
\end{aligned}
$$

In our case, we have $\mu_1 = 2$ and $\mu_2 = -1$. Therefore,

$$
\begin{aligned}
E[Y] &= E[3X_{1} + X_{2}] \\
&= 3E[X_{1}] + E[X_{2}] \\
&= 3 \cdot μ_{1} + μ_{2} \\
&= 3 \cdot 2 + (-1) \\
&= 5
\end{aligned}
$$

Also, we have

$$
\sigma_1^2 = 4, \sigma_2^2 = 7
$$

and

$$
\sigma_{12} = \sigma_{21} = -3
$$

Therefore,

$$
\begin{aligned}
Var[Y] &= Var[3X_{1} + X_{2}] \\
&= 3^{2} \cdot 4 + 1^{2} \cdot 7 + 2 \cdot 3 \cdot 1 \cdot (-3) \\
&= 36 + 7 - 18 \\
&= 25
\end{aligned}
$$

Therefore, we conclude that
$Y \sim N(5,25)$.

---

**Question 3**

> A scientific calculator is required to answer this question.

Let $X = [X_{1}, X_{2}]^{T}$ be a bivariate normal random vector with mean vector $μ$ and covariance matrix $Σ$ given by
$μ = \begin{bmatrix}0 \\ 1\end{bmatrix}, Σ = \begin{bmatrix}1 & 1 \\ 1 & 4\end{bmatrix}$.
Find the distribution of $Y = X_{1} - 2X_{2}$.

- [ ] A. $Y∼N(-2, \sqrt{15})$
- [ ] B. $Y∼N(-2, 13)$
- [ ] C. $Y∼N(-2, 17)$
- [ ] D. $Y∼N(-2, \sqrt{17})$
- [ ] E. $Y∼N(-2, \sqrt{13})$

---

**Question 4**

> A scientific calculator is required to answer this question.

Let $X = [X_{1}, X_{2}]^{T}$ be a bivariate normal random vector with mean vector $μ$ and covariance matrix $Σ$ given by
$μ = \begin{bmatrix}0 \\ 1\end{bmatrix}, Σ = \begin{bmatrix}1 & 1 \\ 1 & 4\end{bmatrix}$.

Let $Y = X_{1} - 2X_{2}$. Calculate $P(Y \ge 1)$.

*Hint: A table of values of $Φ(z)$ for different values of $z$ is given below, where $Φ(z)$ is the cumulative distribution function for the standard normal distribution.*

| $z$ | $0.03$ | $0.04$ | $0.05$ |
| --- | ---: | ---: | ---: |
| $0.8$ | $0.7967$ | $0.7995$ | $0.8023$ |
| $0.9$ | $0.8238$ | $0.8264$ | $0.8289$ |
| $1.0$ | $0.8485$ | $0.8508$ | $0.8531$ |

- [ ] A. $0.2033$
- [ ] B. $0.1977$
- [ ] C. $0.1762$
- [ ] D. $0.1711$
- [ ] E. $0.2005$

---

<a id="the-multivariate-normal-distribution"></a>
## The Multivariate Normal Distribution

Earlier, we compared the PDF of $X\sim N(\mu,\sigma^2)$, given by

$$
f(x) = (2\pi)^{-{\color{red}1}/2} ({\color{blue}\sigma^2})^{-1/2} \: \mathrm{exp}\left(-\frac{1}{2} (x-\mu) ({\color{blue}\sigma^2})^{-1} (x-\mu) \right)
$$

and the joint PDF of a bivariate normal distribution

$$
\mathbf X = [X_1,X_2]^T
$$

with mean vector $\boldsymbol\mu$ and covariance matrix $\Sigma$, given by

$$
f(x_1, x_2) = (2\pi)^{-{\color{red}2}/2} {\color{blue} \mid \Sigma \mid}^{-1/2} \: \mathrm{exp}\left(-\dfrac 12(\mathbf X - \boldsymbol \mu)^T \,{\color{blue}\Sigma}^{-1} \,(\mathbf X - \boldsymbol \mu)\right)
$$

We can generalize this for an arbitrary number of variables ${\color{red}p}$, as follows:

$$
f(x_1, x_2, \ldots, x_p) = (2\pi)^{-{\color{red}p}/2} {\color{blue} \mid \Sigma \mid}^{-1/2} \: \mathrm{exp}\left(-\dfrac 12(\mathbf X - \boldsymbol \mu)^T \,{\color{blue}\Sigma}^{-1} \,(\mathbf X - \boldsymbol \mu)\right)
$$

The function $f(x_1, x_2, \ldots, x_p)$ is called the **multivariate normal** density function. A random vector

$$
\mathbf X = [X_1,X_2,\ldots,X_p]^T
$$

with this joint density function is said to follow a **multivariate normal distribution**.

Similar to the bivariate case, we can consider linear combinations of coordinates of multivariate normal random vectors:

> *If $\mathbf{X} = [X_1, X_2, \ldots, X_k]^T$ is a multivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$, then the random variable $Y$, defined as $Y= \mathbf c^T \mathbf X = \sum_{i=1}^k c_i X_i$ is a normally distributed random variable, where $\mathbf c$ is any $k\times 1$ column vector, and*

$$
\begin{aligned}
E[Y] &= c^{T}μ, \\
Var[Y] &= c^{T}Σc
\end{aligned}
$$

Notice that the expression for the variance is a quadratic form.

---

<a id="linear-combinations-of-elements-from-a-multivariate-normal-random-variable"></a>
## Linear Combinations of Elements From a Multivariate Normal Random Variable

**Example:** Let $\mathbf{X} = [X_1, X_2,X_3]^T$ be a multivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$ given by
$μ = \begin{bmatrix}1 \\ 2 \\ 1\end{bmatrix}, Σ = \begin{bmatrix}6 & 1 & 2 \\ 1 & 3 & 2 \\ 2 & 2 & 4\end{bmatrix}$.

Find the distribution of $Y = X_1 -2X_2 +4 X_3$.

**Explanation**

Recall that if

$$
\mathbf{X} = [X_1, X_2, \ldots, X_k]^T
$$

is a multivariate normal random vector with mean vector $\boldsymbol{\mu}$ and covariance matrix $\Sigma$, then the random variable $Y$, defined as

$$
Y= \mathbf c^T \mathbf X = \sum_{i=1}^k c_i X_i
$$

is a normally distributed random variable, where $\mathbf c$ is any $k\times 1$ column vector, and

$$
\begin{aligned}
E[Y] &= c^{T}μ, \\
Var[Y] &= c^{T}Σc
\end{aligned}
$$

In our case, since we wish to know the distribution of

$$
Y = X_1 -2X_2 +4 X_3
$$

we have

$$
\begin{bmatrix}1 \\ -2 \\ 4\end{bmatrix}
$$

Therefore, for the mean of $Y$, we have

$$
\begin{aligned}
E[Y] &= c^{T}μ \\
&= \begin{bmatrix}1 & -2 & 4\end{bmatrix}\begin{bmatrix}1 \\ 2 \\ 1\end{bmatrix} \\
&= 1 - 4 + 4 \\
&= 1
\end{aligned}
$$

and for the variance of $Y$, we have

$$
\begin{aligned}
Var[Y] &= c^{T}Σc \\
&= \begin{bmatrix}1 & -2 & 4\end{bmatrix}\begin{bmatrix}6 & 1 & 2 \\ 1 & 3 & 2 \\ 2 & 2 & 4\end{bmatrix}\begin{bmatrix}1 \\ -2 \\ 4\end{bmatrix} \\
&= \begin{bmatrix}1 & -2 & 4\end{bmatrix}\begin{bmatrix}12 \\ 3 \\ 14\end{bmatrix} \\
&= 12 - 6 + 56 \\
&= 62
\end{aligned}
$$

Therefore, we conclude that

$$
Y\sim N(1,62)
$$

---

**Question 5**

> A scientific calculator is required to answer this question.

Let $X = [X_{1}, X_{2}, X_{3}]^{T}$ be a multivariate normal random vector with mean vector $μ$ and covariance matrix $Σ$ given by
$μ = \begin{bmatrix}3 \\ 1 \\ 2\end{bmatrix}, Σ = \begin{bmatrix}2 & 1 & 1 \\ 1 & 3 & 2 \\ 1 & 2 & 4\end{bmatrix}$.
Find the distribution of $Y = X_{1} + 2X_{2} - X_{3}$.

- [ ] A. $N(-1, 10)$
- [ ] B. $N(1, 3)$
- [ ] C. $N(3, 12)$
- [ ] D. $N(3, 10)$
- [ ] E. $N(1, 12)$

---

**Question 6**

> A scientific calculator is required to answer this question.

Let $X = [X_{1}, X_{2}, X_{3}]^{T}$ be a multivariate normal random vector with mean vector $μ$ and covariance matrix $Σ$ given by
$μ = \begin{bmatrix}3 \\ 1 \\ 2\end{bmatrix}, Σ = \begin{bmatrix}2 & 1 & 1 \\ 1 & 3 & 2 \\ 1 & 2 & 4\end{bmatrix}$.

Let $Y = X_{1} + 2X_{2} - X_{3}$. Find $P(Y < 1)$.

*Hint: A table of values of $Φ(z)$ for different values of $z$ is given below, where $Φ(z)$ is the cumulative distribution function for the standard normal distribution.*

| $z$ | $0.05$ | $0.06$ | $0.07$ | $0.08$ |
| --- | ---: | ---: | ---: | ---: |
| $0.5$ | $0.7088$ | $0.7123$ | $0.7157$ | $0.7190$ |
| $0.6$ | $0.7422$ | $0.7454$ | $0.7486$ | $0.7517$ |
| $0.7$ | $0.7734$ | $0.7764$ | $0.7794$ | $0.7823$ |
| $0.8$ | $0.8023$ | $0.8051$ | $0.8078$ | $0.8106$ |

- [ ] A. $0.2810$
- [ ] B. $0.3783$
- [ ] C. $0.3062$
- [ ] D. $0.3300$
- [ ] E. $0.2401$

```update-progress
```

[[MA/PAS/Home|Home]]
[[MA/PAS/0. Table of Contents/TOC|Table of Contents]]
