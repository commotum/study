# Introduction to Order Statistics

<!--
lesson-id: 3326
topic-code: PAS.10.1.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding an Order Statistic](#finding-an-order-statistic)
- [Calculating Probabilities Associated With Maximums](#calculating-probabilities-associated-with-maximums)
- [Calculating the Probability That the Maximum Is Smaller Than Some Value](#calculating-the-probability-that-the-maximum-is-smaller-than-some-value)
- [Calculating Probabilities Associated With Minimums](#calculating-probabilities-associated-with-minimums)
- [Calculating the Probability That the Minimum Is Smaller Than Some Value](#calculating-the-probability-that-the-minimum-is-smaller-than-some-value)

## Prerequisites

- [The Continuous Uniform Distribution](<../../../4. Continuous Probability Distributions/4.2. The Continuous Uniform Distribution/Lessons/4.2.1. The Continuous Uniform Distribution.md>)
- [The Normal Distribution](<../../../4. Continuous Probability Distributions/4.1. The Normal Distribution/Lessons/4.1.4. The Normal Distribution.md>)
- [The Exponential Distribution](<../../../4. Continuous Probability Distributions/4.3. The Exponential Distribution/Lessons/4.3.1. The Exponential Distribution.md>)
- [Independence of Continuous Random Variables](<../../../5. Combining Random Variables/5.2. Distributions of Two Continuous Random Variables/Lessons/5.2.3. Independence of Continuous Random Variables.md>)
- [Sampling Distributions](<../../../6. Parametric Inference/6.1. Mean, Variance, and Proportion/Lessons/6.1.2. Sampling Distributions.md>)

---

<a id="introduction"></a>
## Introduction

Given an I.I.D random sample from a probability distribution, suppose we needed to know the probability that the third largest value was less than $10$, or the probability that the second smallest value was greater than $5$.

To find such probabilities, we first need to put the values in the sample in ascending order. This is where we use so-called **order statistics.**

For an I.I.D random sample $X_1, X_2, \ldots X_n$, the **order statistics** are obtained by arranging the values in ascending order. The **$r$th order statistic**, denoted $X_{(r)}$, is the $r$th smallest value in the ordered sample. Specifically,

- the first order statistic is $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$,
- the second order statistic is $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$,
- the third order statistic: $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$,
- $\vdots$
- the $r$th order statistic is $X_{(r)} =$ the $r$th smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$,
- $\vdots$
- the $n$th order statistic is $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$.

To demonstrate, consider the following sample taken from the I.I.D random sample $X_1, X_2, \ldots, X_5$.

$$
9,\quad 5,\quad 12,\quad 4,\quad 16
$$

What is the third order statistic $x_{(3)}$ for this sample?

To find the third order statistic for this sample, we first sort the numbers in increasing order:

$$
x_{(1)} = 4,\quad x_{(2)} = 5,\quad x_{(3)} = 9,\quad x_{(4)} = 12,\quad x_{(5)} = 16
$$

Therefore, the third order statistic for this sample is

$$
x_{(3)} = 9
$$

---

<a id="finding-an-order-statistic"></a>
## Finding an Order Statistic

**Example:** Consider the following sample taken from the I.I.D random sample $X_1, X_2, \ldots, X_6$.

$7,\quad 9,\quad 5,\quad 1,\quad 5,\quad 3$

What is the third order statistic $x_{(3)}$ for this sample?

**Explanation**

Suppose that $X_1, X_2, \ldots X_n$ is an I.I.D random sample from a probability distribution. The order statistics, denoted $X_{(r)}$, are defined by ranking the values from smallest to largest. Specifically:

- $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $\vdots$
- $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$

To find the third order statistic for this sample, we first sort the numbers in increasing order:

$$
x_{(1)} = 1,\quad x_{(2)} = 3,\quad x_{(3)} = 5,\quad x_{(4)} = 5,\quad x_{(5)} = 7,\quad x_{(6)} = 9
$$

Therefore, the third order statistic for this sample is

$$
x_{(3)} = 5
$$

---

**Question 1**

Consider the following sample taken from the I.I.D random sample $X_{1}, X_{2}, …, X_{5}$.

$3, 7, 2, 9, 5$

What is the third order statistic $x_{(3)}$ for this sample?

- [ ] A. $2$
- [ ] B. $5$
- [ ] C. $7$
- [ ] D. $9$
- [ ] E. $3$

---

**Question 2**

Consider the following sample taken from the I.I.D random sample $X_{1}, X_{2}, …, X_{6}$.

$17, 9, 15, 41, 15, 38$

What is the third order statistic $x_{(3)}$ for this sample?

- [ ] A. $15$
- [ ] B. $16$
- [ ] C. $17$
- [ ] D. $41$
- [ ] E. $38$

---

<a id="calculating-probabilities-associated-with-maximums"></a>
## Calculating Probabilities Associated With Maximums

Let $X_1$, $X_2$, $X_3$ be three numbers chosen independently and uniformly at random from the interval $[0,5]$. What is the probability that the *maximum* of the three variables is less than $2$?

Suppose that $X_1, X_2, \ldots X_n$ is an I.I.D random sample from a probability distribution. The order statistics, denoted $X_{(r)}$, are defined by ranking the values from smallest to largest. Specifically:

- $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $\quad\vdots$
- $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$

Now, for *continuous* probability distributions, we may assume that

$X_{(1)} < X_{(2)} < X_{(3)} < \cdots < X_{(n)}$
since the probability that any order statistics are the same equals zero.

We wish to find

$$
P\left(X_{(3)} < 2\right)
$$

This can only happen if *all* $X_i$'s in our sample are smaller than $2$. One such configuration is shown below.

![](<../Source/10.1.7. Introduction to Order Statistics/Images/d051447266a8a499e88c601b30698c1d.png>)

First, we note that since $X_i\sim U[0,5]$, we have

$$
\begin{aligned}
P(X_{i} < 2) &= P(0 < X_{i} < 2) \\
&= ∫_{0}^{2}f(x)dx \\
&= ∫_{0}^{2}\frac{1}{5}dx \\
&= \frac{2}{5} - 0 \\
&= \frac{2}{5}
\end{aligned}
$$

Therefore, since the $X_i$'s are independent, we have

$$
\begin{aligned}
P(X_{(3)} < 2) &= P(X_{1} < 2, X_{2} < 2, X_{3} < 2) \\
&= P(X_{1} < 2) \cdot P(X_{2} < 2) \cdot P(X_{3} < 2) \\
&= \frac{2}{5} \cdot \frac{2}{5} \cdot \frac{2}{5} \\
&= \frac{8}{125}
\end{aligned}
$$

Let's see another example.

---

<a id="calculating-the-probability-that-the-maximum-is-smaller-than-some-value"></a>
## Calculating the Probability That the Maximum Is Smaller Than Some Value

**Example:** Let $X_1, X_2, X_3$ be three independent random variables, each chosen from the distribution with the following PDF:

${\frac{x}{2},\begin{vmatrix}0 \le x \le 2, \\ 0,\end{vmatrix}otherwise$.

What is the probability that the third order statistic $X_{(3)}$ is less than $1$?

**Explanation**

Suppose that $X_1, X_2, \ldots X_n$ is an I.I.D random sample from a probability distribution. The order statistics, denoted $X_{(r)}$, are defined by ranking the values from smallest to largest. Specifically:

- $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $\quad\vdots$
- $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$

For continuous probability distributions, we may assume that

$X_{(1)} < X_{(2)} < X_{(3)} < \cdots < X_{(n)}$
since the probability that any order statistics are the same equals zero.

We wish to find

$$
P\left(X_{(3)} < 1\right)
$$

This can only happen if *all* $X_i$'s in our sample are smaller than $1$. One such configuration is shown below.

![](<../Source/10.1.7. Introduction to Order Statistics/Images/d8cbc1acb2eba52d0c4629ccdfb8965a.png>)

The probability that any one $X_i$ is less than $1$ is

$$
\begin{aligned}
P(X_{i} < 1) &= P(0 < X < 1) \\
&= ∫_{0}^{1}f(x)dx \\
&= ∫_{0}^{1}\frac{x}{2}dx \\
&= [\frac{x^{2}}{4}]_{0}^{1} \\
&= \frac{1}{4}
\end{aligned}
$$

Therefore, since the $X_i$'s are independent, we have

$$
\begin{aligned}
P(X_{(3)} < 1) &= P(X_{1} < 1, X_{2} < 1, X_{3} < 1) \\
&= P(X_{1} < 1) \cdot P(X_{2} < 1) \cdot P(X_{3} < 1) \\
&= \frac{1}{4} \cdot \frac{1}{4} \cdot \frac{1}{4} \\
&= \frac{1}{64}
\end{aligned}
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

Let $X_{1}$, $X_{2}$, $X_{3}$ be three numbers chosen independently and uniformly at random from the interval $[0, 1]$. What is the probability that the third order statistic $X_{(3)}$ is less than $\frac{1}{2}$?

Hint: The probability density function for a uniformly distributed continuous random variable $X$ on an interval $[0, 1]$ is given by

$f(x) = {1,\begin{vmatrix}0 \le x \le 1, \\ 0,\end{vmatrix}otherwise$.

- [ ] A. $\frac{1}{8}$
- [ ] B. $\frac{1}{12}$
- [ ] C. $\frac{1}{16}$
- [ ] D. $\frac{1}{4}$
- [ ] E. $\frac{1}{3}$

---

**Question 4**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, X_{3}, X_{4}$ be four independent random variables, each chosen from $N(1, 3^{2})$ What is the approximate probability that the maximum of these variables is less than or equal to $5$?

*Hint: The data below is taken from the table of values of the cumulative distribution function $Φ(z)$ for the standard normal distribution.*

| $z$ | $0.00$ | $0.01$ | $0.02$ | $0.03$ | $0.04$ |
| --- | ---: | ---: | ---: | ---: | ---: |
| $1.2$ | $0.8849$ | $0.8869$ | $0.8888$ | $0.8907$ | $0.8925$ |
| $1.3$ | $0.9032$ | $0.9049$ | $0.9066$ | $0.9082$ | $0.9099$ |
| $1.4$ | $0.9192$ | $0.9207$ | $0.9222$ | $0.9236$ | $0.9251$ |

- [ ] A. $0.6803$
- [ ] B. $0.6714$
- [ ] C. $0.8804$
- [ ] D. $0.5819$
- [ ] E. $0.7024$

---

<a id="calculating-probabilities-associated-with-minimums"></a>
## Calculating Probabilities Associated With Minimums

Let $X_1$, $X_2$, and $X_3$ be three numbers independently chosen from $N(1,2^2)$. What is the approximate probability that the *minimum* of the three variables is at most $0$?

Suppose that $X_1, X_2, \ldots X_n$ is an I.I.D random sample from a probability distribution. The order statistics, denoted $X_{(r)}$, are defined by ranking the values from smallest to largest. Specifically:

- $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $\quad\vdots$
- $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$

For continuous probability distributions, we may assume that

$X_{(1)} < X_{(2)} < X_{(3)} < \cdots < X_{(n)}$
since the probability that any order statistics are the same equals zero.

We wish to find

$$
P\left(X_{(1)} \le 0\right)
$$

To do this, we will first compute the complementary probability and then subtract it from $1{:}$

$$
P\left(X_{(1)} \leq 0\right) = 1 - P\left(X_{(1)} > 0\right)
$$

The event $X_{(1)} > 0$ means that all three numbers $X_1, X_2, X_3$ must be greater than $0$. One such configuration is shown below.

![](<../Source/10.1.7. Introduction to Order Statistics/Images/e9e5976cf5445997f576f7b0601f346b.png>)

Since the numbers are chosen independently, we have

$$
\begin{aligned}
P(X_{(1)} > 0) &= P(X_{1} > 0, X_{2} > 0, X_{3} > 0) \\
&= P(X_{1} > 0) \cdot P(X_{2} > 0) \cdot P(X_{3} > 0)
\end{aligned}
$$

Each $X_i \sim N(1,2^2)$, so, we transform $X$ into a standard normal random variable $Z$ by $z$-scoring:

$$
\begin{aligned}
P(X_{i} > 0) &= P(Z > \frac{0 - 1}{2}) \\
&= P(Z > - 0.5)
\end{aligned}
$$

Hence, the probability that $X_i > 0$ is

$$
\begin{aligned}
P(X_{i} > 0) &= P(Z > - 0.5) \\
&= 1 - P(Z \le - 0.5) \\
&= 1 - Φ(-0.5) \\
&= 1 - 0.3085 \\
&= 0.6915
\end{aligned}
$$

Therefore, the probability that all three numbers are greater than $0$ is

$$
P\left(X_{(1)} > 0 \right) = 0.6915 \cdot 0.6915 \cdot 0.6915 \approx 0.3307
$$

Finally, the desired probability is

$$
P\left(X_{(1)} \leq 0 \right) = 1 - P\left(X_{(1)} > 0\right) \approx 1 - 0.3307 = 0.6693
$$

Thus, the probability that the minimum of $(X_1, X_2, X_3)$ is at most $0$ is $0.6693$.

---

<a id="calculating-the-probability-that-the-minimum-is-smaller-than-some-value"></a>
## Calculating the Probability That the Minimum Is Smaller Than Some Value

**Example:** Let $X_1$, $X_2$, and $X_3$ be three independent random variables, each chosen from an exponential distribution with rate parameter $\lambda = 1$. What is the approximate probability that the minimum of the three variables is at most $\dfrac{1}{3}$?

*Hint: The probability density function (PDF) for an exponential distribution with rate parameter $\lambda = 1$ is given by*
${e^{-x},\begin{vmatrix}x \ge 0, \\ 0,\end{vmatrix}otherwise$.

**Explanation**

Suppose that $X_1, X_2, \ldots X_n$ is an I.I.D random sample from a probability distribution. The order statistics, denoted $X_{(r)}$, are defined by ranking the values from smallest to largest. Specifically:

- $X_{(1)} = \min\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(2)} =$ the second smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $X_{(3)} =$ the third smallest of $\left\{X_1, X_2, X_3, \ldots, X_n\right\}$
- $\quad\vdots$
- $X_{(n)} = \max\left\{X_1, X_2, X_3, \ldots, X_n\right\}$

For continuous probability distributions, we may assume that

$X_{(1)} < X_{(2)} < X_{(3)} < \cdots < X_{(n)}$
since the probability that any order statistics are the same equals zero.

We wish to find

$$
P\left(X_{(1)} \le \dfrac{1}{3}\right)
$$

To do this, we will first compute the complementary probability and then subtract it from $1{:}$

$$
P\left(X_{(1)} \leq \dfrac{1}{3}\right) = 1 - P\left(X_{(1)} > \dfrac{1}{3}\right)
$$

The event

$$
X_{(1)} > \dfrac{1}{3}
$$

means that all three numbers $X_1, X_2, X_3$ must be greater than

$$
\dfrac{1}{3}
$$

One such configuration is shown below.

![](<../Source/10.1.7. Introduction to Order Statistics/Images/ab745803d0d7d90a76d96771485f12cd.png>)

Since the numbers are chosen independently, we have

$$
\begin{aligned}
P(X_{(1)} > \frac{1}{3}) &= P(X_{1} > \frac{1}{3}, X_{2} > \frac{1}{3}, X_{3} > \frac{1}{3}) \\
&= P(X_{1} > \frac{1}{3}) \cdot P(X_{2} > \frac{1}{3}) \cdot P(X_{3} > \frac{1}{3})
\end{aligned}
$$

The cumulative distribution function (CDF) of the exponential distribution for

$$
x\geq 0
$$

is

$$
\begin{aligned}
F_{X}(x) &= P(X_{i} \le x) \\
&= ∫_{0}^{x}f_{X}(t)dt \\
&= ∫_{0}^{x}e^{-t}dt \\
&= 1 - e^{-x}
\end{aligned}
$$

Thus, the probability that any one variable

$$
X_i > \dfrac{1}{3}
$$

is

$$
P\left(X_i > \dfrac{1}{3}\right) = 1 - F_X\left(\dfrac{1}{3}\right) = 1- (1 - e^{-1/3}) = e^{-1/3}
$$

Therefore, the probability that all three numbers are greater than

$$
\dfrac{1}{3}
$$

is

$$
P\left(X_{(1)} > \dfrac{1}{3}\right) = e^{-1/3} \cdot e^{-1/3} \cdot e^{-1/3} = e^{-1}
$$

Finally, the desired probability is

$$
P\left(X_{(1)} \leq \dfrac{1}{3}\right) = 1 - P\left(X_{(1)} > \dfrac{1}{3}\right) = 1 - e^{-1} \approx 0.632
$$

Thus, the probability that the minimum of $(X_1, X_2, X_3)$ is at most

$$
\dfrac{1}{3}
$$

is $0.632$.

---

**Question 5**

> A scientific calculator is required to answer this question.

Let $X_{1}$, $X_{2}$ and $X_{3}$ be three numbers independently chosen from a uniform distribution on $[0, 1]$. What is the probability that the minimum is at most $\frac{1}{3}$?

- [ ] A. $\frac{11}{27}$
- [ ] B. $\frac{8}{27}$
- [ ] C. $\frac{13}{27}$
- [ ] D. $\frac{19}{27}$
- [ ] E. $\frac{10}{27}$

---

**Question 6**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, X_{3}$ be three independent random variables, each chosen from the distribution with the following PDF:

$f_{X}(x) = {x - 1,\begin{vmatrix}1 \le x \le 2, \\ 3 - x\end{vmatrix}2 < x \le 3,; 0 \mid otherwise$.

What is the probability that the minimum of the three variables is at most $2$?

- [ ] A. $\frac{7}{8}$
- [ ] B. $\frac{1}{2}$
- [ ] C. $\frac{3}{4}$
- [ ] D. $\frac{1}{3}$
- [ ] E. $\frac{5}{8}$

```update-progress
```

[[MA/PAS/Home|Home]]
[[MA/PAS/0. Table of Contents/TOC|Table of Contents]]
