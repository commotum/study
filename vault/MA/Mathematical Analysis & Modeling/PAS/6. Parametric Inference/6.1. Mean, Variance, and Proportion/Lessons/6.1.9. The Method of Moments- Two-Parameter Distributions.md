# The Method of Moments: Two-Parameter Distributions

<!--
lesson-id: 5363
topic-code: PAS.6.1.9
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding MM Estimators for Continuous Uniform Random Variables](#finding-mm-estimators-for-continuous-uniform-random-variables)
- [Finding MM Estimators for Binomial Random Variables](#finding-mm-estimators-for-binomial-random-variables)
- [Finding MM Estimators for Normal Random Variables](#finding-mm-estimators-for-normal-random-variables)

## Prerequisites

- [The Normal Distribution](<../../../4. Continuous Probability Distributions/4.1. The Normal Distribution/Lessons/4.1.4. The Normal Distribution.md>)
- [Mean and Variance of the Binomial Distribution](<../../../3. Discrete Probability Distributions/3.3. The Binomial Distribution/Lessons/3.3.3. Mean and Variance of the Binomial Distribution.md>)
- [Mean and Variance of the Continuous Uniform Distribution](<../../../4. Continuous Probability Distributions/4.2. The Continuous Uniform Distribution/Lessons/4.2.2. Mean and Variance of the Continuous Uniform Distribution.md>)
- [The Method of Moments](<6.1.8. The Method of Moments.md>)

---

<a id="introduction"></a>
## Introduction

In this lesson, we'll learn how to apply the method of moments to two-parameter probability distributions, such as $U(a,b)$ (continuous uniform distribution), $B(m,p)$ (binomial distribution), and $N(\mu, \sigma)$ (normal distribution).

First, let's remind ourselves of the method of moments.

Suppose we have an I.I.D. random sample of size $n{:}$
$X_1,\quad X_2,\quad \ldots, \quad X_n$

The $k$th theoretical moment is defined as

$$
\mu_k = \textrm{E}[X_i^k] = \sum_{x\in S} x^k \cdot p(x)
$$

The $k$th sample moment is defined as

$$
M_k = \dfrac1n \sum_{i=1}^n X_i^k
$$

To find the method of moments estimator for some population parameter, we equate the corresponding theoretical and sample moments.

$$
\textrm{E}[X_i] = M_1, \qquad \textrm{E}[X_i^2] = M_2, \qquad \textrm{E}[X_i^3] = M_3, \qquad \ldots
$$

Let's see some examples.

---

<a id="finding-mm-estimators-for-continuous-uniform-random-variables"></a>
## Finding MM Estimators for Continuous Uniform Random Variables

**Example:** Suppose $X_1,X_2,\ldots,X_{5}$ is an I.I.D. random sample with $X_i\sim U(\theta, \theta+6)$, where $\theta$ is an unknown parameter and $\theta>0$. For a particular sample $x_1, x_2, x_3\ldots,x_{5}$ you're given that
$\displaystyle\sum_{i=1}^{5} x_i^2 = 140$.

Find the method of moments estimate $\widehat{\theta}_{MM}$ for $\theta$.

**Explanation**

Recall that the $k$th sample moment $M_k$ of a sample $X_1, X_2, \ldots, X_n$ is given by

$$
M_k = \dfrac1n \sum_{i=1}^n X_i^k
$$

In particular, the first and second sample moments are given by

$$
M_1 = \dfrac1n \sum_{i=1}^n X_i \,, \qquad M_2 = \dfrac1n \sum_{i=1}^n X_i^2
$$

To find the method of moments estimator for a parameter, we calculate a sample moment and the corresponding theoretical moment and equate them.

We are given the sum of the squared values in the sample. So, we can calculate the second sample moment:

$$
m_2 = \dfrac{1}{n} \sum_{i=1}^{n} x_i^2 = \dfrac{140}{5}=28
$$

We are given that the variables follow a uniform distribution $U(a,b)$. Therefore,

$$
\textrm{E}[X_i] = \dfrac{a+b}{2}, \qquad \textrm{Var}[X_i] = \dfrac{(b-a)^2}{12}
$$

We also have

$$
\textrm{Var}[X_i] = \textrm{E}[X^2_i] - \left(\textrm{E}[X_i]\right)^2
$$

Therefore, we can calculate the second theoretical moment as follows:

$$
\begin{aligned}
E[X_{i}^{2}] &= Var[X_{i}] + (E[X_{i}])^{2} \\
&= (((θ + 6) - θ)^{2})/(12) + ((θ + (θ + 6))/(2))^{2} \\
&= \frac{36}{12} + (θ + 3)^{2} \\
&= 3 + θ^{2} + 6θ + 9 \\
&= θ^{2} + 6θ + 12
\end{aligned}
$$

Now, equating the sample and theoretical second moments and using the estimator $\widehat\theta_{MM}$ in place of $\theta$, we get

$$
\begin{aligned}
θ^(^)_{MM}^{2} + 6θ^(^)_{MM} + 12 &= 28
\end{aligned}
$$

Then, we solve for $\widehat\theta_{MM}{:}$

$$
\begin{aligned}
θ^(^)_{MM}^{2} + 6θ^(^)_{MM} + 12 &= 28 \\
θ^(^)_{MM}^{2} + 6θ^(^)_{MM} - 16 &= 0 \\
(θ^(^)_{MM} + 8)(θ^(^)_{MM} - 2) &= 0
\end{aligned}
$$

This gives the solutions

$$
\widehat{\theta}_{MM}= -8, \qquad \widehat{\theta}_{MM}= 2
$$

However, we discard the negative solution since we are given that $\theta > 0$. Therefore, our method of moments estimate for $\theta$ is

$$
\widehat{\theta}_{MM}= \bbox[3pt,Gainsboro]{\color{blue}2}
$$

---

**Question 1**

Suppose $X_{1}, X_{2}, …, X_{7}$ is an I.I.D. random sample with $X_{i}∼U[θ, θ + 10]$, where $θ$ is an unknown parameter. For a particular sample $x_{1}, x_{2}, …, x_{7}$, you're given that
$∑_(i = 1)^(7)x_{i} = 8.4$.

Find the method of moments estimate $θ^(^)_{MM}$ for $θ$.

$θ^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$

---

**Question 2**

Suppose $X_{1}, X_{2}, …, X_{10}$ is an I.I.D. random sample with $X_{i}∼U(θ, θ + 4)$, where $θ$ is an unknown parameter. For a particular sample $x_{1}, x_{2}, …, x_{10}$ you're given that
$∑_(i = 1)^(10)x_{i}^{2} = \frac{40}{3}$.

Find the method of moments estimate $θ^(^)_{MM}$ for $θ$.

$θ^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$

---

<a id="finding-mm-estimators-for-binomial-random-variables"></a>
## Finding MM Estimators for Binomial Random Variables

**Example:** Suppose $X_1,X_2,\ldots,X_6$ is an I.I.D. random sample with $X_i\sim B(3, \theta)$ and unknown probability of success $\theta$. For a particular sample $x_1, x_2,\ldots, x_{6}$, you're given that
$\displaystyle\sum_{i=1}^{6} x_i^2 = 10$.

Find the method of moments estimate $\widehat{\theta}_{MM}$ for $\theta$.

**Explanation**

Recall that the $k$th sample moment $M_k$ of a sample $X_1, X_2, \ldots, X_n$ is given by

$$
M_k = \dfrac1n \sum_{i=1}^n X_i^k
$$

In particular, the first and second sample moments are given by

$$
M_1 = \dfrac1n \sum_{i=1}^n X_i \,, \qquad M_2 = \dfrac1n \sum_{i=1}^n X_i^2
$$

To find the method of moments estimator for a parameter, we calculate a sample moment and the corresponding theoretical moment and equate them.

We are given the sum of the squared values in the sample. So, we can calculate the second sample moment:

$$
m_2 = \dfrac{1}{n} \sum_{i=1}^{n} x_i^2 = \dfrac{10}{6}=\dfrac53
$$

We are given that the variables follow a binomial distribution $B(m,\theta)$ Therefore,

$$
\textrm{E}[X_i] = m\theta, \qquad \textrm{Var}[X_i] = m\theta (1-\theta)
$$

We also have

$$
\textrm{Var}[X_i] = \textrm{E}[X^2_i] - \left(\textrm{E}[X_i]\right)^2
$$

Therefore, we can calculate the second theoretical moment as follows:

$$
\begin{aligned}
E[X_{i}^{2}] &= Var[X_{i}] + (E[X_{i}])^{2} \\
&= mθ(1 - θ) + m^{2}θ^{2} \\
&= 3θ(1 - θ) + 3^{2}θ^{2} \\
&= 3θ - 3θ^{2} + 9θ^{2} \\
&= 6θ^{2} + 3θ
\end{aligned}
$$

Now, equating the sample and theoretical second moments and using the estimator $\widehat\theta_{MM}$ in place of $\theta$, we get

$$
\begin{aligned}
6θ^(^)_{MM}^{2} + 3θ^(^)_{MM} = \frac{5}{3} \\
18θ^(^)_{MM}^{2} + 9θ^(^)_{MM} - 5 &= 0
\end{aligned}
$$

Then, we solve for $\widehat{\theta}_{MM}{:}$

$$
\begin{aligned}
θ^(^)_{MM} &= (-9 ± \sqrt{9^{2} - 4(18)(-5)})/(2(18)) \\
θ^(^)_{MM} &= \frac{-3 ± 7}{12}
\end{aligned}
$$

This gives the solutions

$$
\widehat{\theta}_{MM}= -\dfrac{5}{6}, \qquad \widehat{\theta}_{MM}= \dfrac{1}{3}
$$

However, we discard the negative solution since $\theta > 0$. Therefore, our method of moments estimate for $\theta$ is

$$
\widehat{\theta}_{MM} = \dfrac 13
$$

---

**Question 3**

Suppose $X_{1}, X_{2}, …, X_{5}$ is an I.I.D. random sample with $X_{i}∼B(3, θ)$ and unknown probability of success $θ$. For a particular sample $x_{1}, x_{2}, …, x_{5}$, you're given that
$∑_(i = 1)^(5)x_{i} = 12$.

Find the method of moments estimate $θ^(^)_{MM}$ for $θ$.

$θ^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$

---

**Question 4**

Suppose $X_{1}, X_{2}, X_{3}, X_{4}$ is an I.I.D. random sample with $X_{i}∼B(4, θ)$ and unknown probability of success $θ$. For a particular sample $x_{1}, x_{2}, x_{3}, x_{4}$, you're given that
$∑_(i = 1)^(4)x_{i}^{2} = 7$.

Find the method of moments estimate $θ^(^)_{MM}$ for $θ$.

- [ ] A. $θ^(^)_{MM} = \frac{7}{16}$
- [ ] B. $θ^(^)_{MM} = \frac{5}{16}$
- [ ] C. $θ^(^)_{MM} = \frac{7}{12}$
- [ ] D. $θ^(^)_{MM} = \frac{1}{2}$
- [ ] E. $θ^(^)_{MM} = \frac{1}{4}$

---

<a id="finding-mm-estimators-for-normal-random-variables"></a>
## Finding MM Estimators for Normal Random Variables

**Example:** Suppose $X_1, X_2, \ldots, X_{10}$ is an I.I.D. random sample with $X_i \sim N(\mu, \, \sigma^2)$, where $\mu$ and $\sigma^2$ are unknown parameters. For a particular sample $x_1, x_2, \ldots, x_{10}$, you're given that
$\displaystyle\sum_{i=1}^{10} x_i = 70, \qquad \sum_{i=1}^{10} x_i^2 = 550$.

Find the method of moments estimates $\widehat{\mu}_{MM}$ and $\widehat{\sigma^2}_{MM}$ for $\mu$ and $\sigma^2$, respectively.

**Explanation**

Recall that the $k$th sample moment $M_k$ of a sample $X_1, X_2, \ldots, X_n$ is given by

$$
M_k = \dfrac1n \sum_{i=1}^n X_i^k
$$

In particular, the first and second sample moments are given by

$$
M_1 = \dfrac1n \sum_{i=1}^n X_i \,, \qquad M_2 = \dfrac1n \sum_{i=1}^n X_i^2
$$

To find the method of moments estimator for a parameter, we calculate a sample moment and the corresponding theoretical moment and equate them.

We are given the sum of the values in the sample and the sum of squares. So, we can calculate the first and second sample moments:

$$
\begin{aligned}
m_{1} &= \frac{1}{n}∑_(i = 1)^(n)x_{i} = \frac{70}{10} = 7 \\
m_{2} &= \frac{1}{n}∑_(i = 1)^(n)x_{i}^{2} = \frac{550}{10} = 55
\end{aligned}
$$

We are given that the variables $X_i$ follow a normal distribution. Therefore:

$$
\begin{aligned}
E[X_{i}] &= μ \\
E[X_{i}^{2}] &= Var[X_{i}] + (E[X_{i}])^{2} = σ^{2} + μ^{2}
\end{aligned}
$$

Now, equating the sample and theoretical first and second moments, using the estimators $\widehat\mu_{MM}$ and $\widehat{\sigma^2}_{MM}$ in place of $\mu$ and $\sigma^2$, we get

$$
\begin{aligned}
μ^(^)_{MM} &= 7, \\
σ^{2}^(^)_{MM} + μ^(^)_{MM}^{2} &= 55 \\
σ^{2}^(^)_{MM} + 7^{2} &= 55 \\
σ^{2}^(^)_{MM} &= 6
\end{aligned}
$$

---

**Question 5**

Suppose $X_{1}, X_{2}, …, X_{15}$ is an I.I.D. random sample with $X_{i}∼N(μ, σ^{2})$, where $μ$ and $σ^{2}$ are unknown parameters. For a particular sample $x_{1}, x_{2}, …, x_{15}$, you're given that
$∑_(i = 1)^(15)x_{i} = 120, ∑_(i = 1)^(15)x_{i}^{2} = 963$.

Find the method of moments estimates $μ^(^)_{MM}$ and $σ^{2}^(^)_{MM}$ for $μ$ and $σ^{2}$, respectively.

$μ^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$
$, σ^{2}^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$

---

**Question 6**

Suppose $X_{1}, X_{2}, …, X_{12}$ is an I.I.D. random sample with $X_{i}∼N(μ, σ^{2})$, where $μ$ and $σ^{2}$ are unknown parameters. For a particular sample $x_{1}, x_{2}, …, x_{12}$, you're given that
$∑_(i = 1)^(12)x_{i} = 90, ∑_(i = 1)^(12)x_{i}^{2} = 708$.

Find the method of moments estimates $μ^(^)_{MM}$ and $σ^{2}^(^)_{MM}$ for $μ$ and $σ^{2}$, respectively.

$μ^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$
$, σ^{2}^(^)_{MM} =$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/PAS/Home|Home]]
[[MA/PAS/0. Table of Contents/TOC|Table of Contents]]
