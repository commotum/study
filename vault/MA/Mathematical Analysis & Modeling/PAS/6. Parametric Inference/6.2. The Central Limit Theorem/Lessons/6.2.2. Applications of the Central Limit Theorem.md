# Applications of the Central Limit Theorem

<!--
lesson-id: 3862
topic-code: PAS.6.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Applying the CLT to Continuous Uniform Random Variables](#applying-the-clt-to-continuous-uniform-random-variables)
- [Applying the CLT to Binomial Random Variables](#applying-the-clt-to-binomial-random-variables)
- [Applying the CLT to Geometric Random Variables](#applying-the-clt-to-geometric-random-variables)
- [Applying the CLT to Poisson Random Variables](#applying-the-clt-to-poisson-random-variables)

## Prerequisites

- [The Central Limit Theorem](<6.2.1. The Central Limit Theorem.md>)
- [Mean and Variance of the Binomial Distribution](<../../../3. Discrete Probability Distributions/3.3. The Binomial Distribution/Lessons/3.3.3. Mean and Variance of the Binomial Distribution.md>)
- [Mean and Variance of the Poisson Distribution](<../../../3. Discrete Probability Distributions/3.4. The Poisson Distribution/Lessons/3.4.3. Mean and Variance of the Poisson Distribution.md>)
- [Mean and Variance of the Geometric Distribution](<../../../3. Discrete Probability Distributions/3.5. The Geometric Distribution/Lessons/3.5.3. Mean and Variance of the Geometric Distribution.md>)
- [Mean and Variance of the Continuous Uniform Distribution](<../../../4. Continuous Probability Distributions/4.2. The Continuous Uniform Distribution/Lessons/4.2.2. Mean and Variance of the Continuous Uniform Distribution.md>)

---

<a id="introduction"></a>
## Introduction

Suppose $X_1, X_2, \ldots, X_n$ is a random sample of size $n$ from a distribution with population mean $\mu$ and population variance $\sigma^2$. Let's define a new random variable as the sum of all $X_i$'s as follows:

$$
X = \sum_{i=1}^n X_i
$$

The sample mean $\overline{X}$ is given by

$$
\overline{X} = \dfrac1n \sum_{i=1}^n X_i
$$

which is equivalent to

$$
n\cdot \overline{X} =\sum_{i=1}^n X_i
$$

Therefore, our random variable $X$ is simply the product of $n$ with the sample mean:

$$
X = n\cdot \overline{X}
$$

Now, the central limit theorem (CLT) states that for sufficiently large $n$, the distribution of $\overline{X}$ can be approximated as

$$
\overline{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right)
$$

Therefore, since

$$
\textrm{E}[\overline X] = \mu, \qquad \textrm{Var}[\overline X] = \dfrac{\sigma^2}{n}
$$

we can calculate $\textrm E[X]$ and $\textrm{Var}[X]$ using the properties of mean and variance as follows:

$$
\begin{aligned}
E[X] &= E[nX^(―)] \\
&= n \cdot E[X^(―)] \\
&= nμ \\
Var[X] &= Var[nX^(―)] \\
&= n^{2} \cdot Var[X^(―)] \\
&= n^{2} \cdot \frac{σ^{2}}{n} \\
&= nσ^{2}
\end{aligned}
$$

Finally, since $\overline{X}$ is (approximately) normally distributed, we have that $X$ is also approximately normal. Therefore,

$$
X\sim N(n\mu, n\sigma^2)
$$

This is an important result. It tells us that provided that we have a sufficiently large I.I.D. sample, the sum of the elements from that sample is approximately normally distributed with mean $n\mu$ and variance $n\sigma^2$. This result does not depend on the distribution of the $X_i$'s!

---

<a id="applying-the-clt-to-continuous-uniform-random-variables"></a>
## Applying the CLT to Continuous Uniform Random Variables

**Example:** Let $X_1,X_2,\ldots,X_{110} \sim U[1,5]$ be an I.I.D. sample of size $110$ from the continuous uniform distribution on the interval $[1,5]$. Suppose we define the random variable $X$ as
$X = \sum_{i=1}^{110} X_i$.

What is the approximate distribution of $X$?

**Explanation**

Recall that the central limit theorem states the following:

> *Suppose that $X_1, X_2, \ldots, X_n$ is a random sample of size $n$ from a population with a population mean $\mu$ and population standard deviation $\sigma$. The central limit theorem (or CLT) states that for sufficiently large $n$, the distribution of the sample mean $\overline{X}$ can be approximated as*
> $\overline{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right)$.

Moreover, if we define

$$
X = \sum_{i=1}^n X_i
$$

then, by the properties of mean and variance, we have

$$
\textrm{E}[X] = n\mu, \qquad \textrm{Var}[X] = n\sigma^2
$$

and consequently,

$$
X\sim N(n\mu, n\sigma^2)
$$

The expected value and the variance of a uniform random variable $X_i \sim U[a,b]$ can be found as

$$
\textrm{E}[X_i] = \dfrac{a+b}{2}, \qquad \textrm{Var}[X_i] = \dfrac{(b-a)^2}{12}
$$

So, in our case, we have

$$
\begin{aligned}
n &= 110, \\
μ &= E[X_{i}] \\
&= \frac{a + b}{2} \\
&= \frac{1 + 5}{2} \\
&= \frac{6}{2} \\
&= 3, \\
σ^{2} &= Var[X_{i}] \\
&= ((b - a)^{2})/(12) \\
&= ((5 - 1)^{2})/(12) \\
&= \frac{16}{12} \\
&= \frac{4}{3}
\end{aligned}
$$

Therefore, for the approximate distribution of $X$, we obtain

$$
X \mid ∼N(nμ, nσ^{2}); ∼N(110 \cdot 3, 110 \cdot \frac{4}{3}); ∼N(330, \frac{440}{3})
$$

---

**Question 1**

Given that $X_{1}, X_{2}, …, X_{75}∼U[1, 7]$ is an I.I.D. sample of size $75$ from the continuous uniform distribution on the interval $[1, 7]$, what is the approximate distribution of the sample mean $X^(―)$?The approximate distribution is $X^(―)∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

**Question 2**

Let $X_{1}, X_{2}, …, X_{57}∼U[2, 4]$ be an I.I.D. sample of size $57$ from the continuous uniform distribution on the interval $[2, 4]$. Suppose we define the random variable $X$ as
$X = ∑_(i = 1)^(57)X_{i}$.

What is the approximate distribution of $X$?

The approximate distribution is $X∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

<a id="applying-the-clt-to-binomial-random-variables"></a>
## Applying the CLT to Binomial Random Variables

**Example:** Given that $X_i\sim B\left(25,\dfrac{9}{20}\right)$ for $i=1,2,\ldots,66$ are independent binomial random variables, what is the approximate distribution of the sample mean $\overline{X}$?

**Explanation**

Recall that the central limit theorem states the following:

> *Suppose that $X_1, X_2, \ldots, X_n$ is a random sample of size $n$ from a population with a population mean $\mu$ and population standard deviation $\sigma$. The central limit theorem (or CLT) states that for sufficiently large $n$, the distribution of the sample mean $\overline{X}$ can be approximated as*
> $\overline{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right)$.

The expected value and the variance of a binomial random variable $X_i \sim B(m,p)$ can be found as

$$
\textrm{E}[X_i] = mp, \qquad \textrm{Var}[X_i] = mp(1-p)
$$

So, in our case, we have

$$
\begin{aligned}
n &= 66, \\
μ &= E[X_{i}] \\
&= mp \\
&= 25 \cdot \frac{9}{20} \\
&= \frac{45}{4}, \\
σ^{2} &= Var[X_{i}] \\
&= mp(1 - p) \\
&= 25 \cdot \frac{9}{20} \cdot \frac{11}{20} \\
&= \frac{99}{16}
\end{aligned}
$$

Therefore, for the approximate distribution of the sample mean $\overline{X}$, we obtain

$$
X^(―) \mid ∼N(μ, \frac{σ^{2}}{n}); ∼N(\frac{45}{4}, \frac{99}{16} \cdot \frac{1}{66}); ∼N(\frac{45}{4}, \frac{3}{32})
$$

---

**Question 3**

Given that $X_{i}∼B(30, 0.25)$ for $i = 1, 2, …, 100$ are independent binomial random variables, what is the approximate distribution of the sample mean $X^(―)$?The approximate distribution is $X^(―)∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

**Question 4**

Let $X_{i}∼B(15, 0.6)$ for $i = 1, 2, …, 60$ be independent binomial random variables. Suppose we define the random variable $X$ as
$X = ∑_(i = 1)^(60)X_{i}$.
What is the approximate distribution of $X$?The approximate distribution is $X∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

<a id="applying-the-clt-to-geometric-random-variables"></a>
## Applying the CLT to Geometric Random Variables

**Example:** Let $X_i \sim \textrm{Geom}(0.4)$ for $i=1,2,\ldots,80$ be independent geometric random variables. Suppose we define the random variable $X$ as
$X = \sum_{i=1}^{80} X_i$.

What is the approximate distribution of $X$?

**Explanation**

Recall that the central limit theorem states the following:

> *Suppose that $X_1, X_2, \ldots, X_n$ is a random sample of size $n$ from a population with a population mean $\mu$ and population standard deviation $\sigma$. The central limit theorem (or CLT) states that for sufficiently large $n$, the distribution of the sample mean $\overline{X}$ can be approximated as*
> $\overline{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right)$.

Moreover, if we define

$$
X = \sum_{i=1}^n X_i
$$

then by the properties of mean and variance, we have

$$
\textrm{E}[X] = n\mu, \qquad \textrm{Var}[X] = n\sigma^2
$$

and consequently,

$$
X\sim N(n\mu, n\sigma^2)
$$

The expected value and the variance of a geometric random variable $X_i \sim \textrm{Geom}(p)$ can be found as

$$
\textrm{E}[X_i] = \dfrac{1}{p}, \qquad \textrm{Var}[X_i] = \dfrac{1-p}{p^2}
$$

So, in our case, we have

$$
\begin{aligned}
n &= 80, \\
μ &= E[X_{i}] \\
&= \frac{1}{p} \\
&= \frac{1}{0.4} \\
&= \frac{5}{2}, \\
σ^{2} &= Var[X_{i}] \\
&= \frac{1 - p}{p^{2}} \\
&= \frac{1 - 0.4}{0.4^{2}} \\
&= \frac{0.6}{0.16} \\
&= \frac{15}{4}
\end{aligned}
$$

Therefore, for the approximate distribution of $X$, we obtain

$$
X \mid ∼N(nμ, nσ^{2}); ∼N(80 \cdot \frac{5}{2}, 80 \cdot \frac{15}{4}); ∼N(200, 300)
$$

---

**Question 5**

Given that $X_{i}∼Geom(0.2)$ for $i = 1, 2, …, 80$ are independent geometric random variables, what is the approximate distribution of the sample mean $X^(―)$?The approximate distribution is $X^(―)∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

**Question 6**

Let $X_{i}∼Geom(\frac{1}{3})$ for $i = 1, 2, …, 60$ be independent geometric random variables. Suppose we define the random variable $X$ as
$X = ∑_(i = 1)^(60)X_{i}$.
What is the approximate distribution of $X$?The approximate distribution is $X∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

<a id="applying-the-clt-to-poisson-random-variables"></a>
## Applying the CLT to Poisson Random Variables

**Example:** Given that $X_i \sim \textrm{Po}(12)$ for $i=1,2,\ldots,60$ are independent Poisson random variables, what is the approximate distribution of the sample mean $\overline{X}$?

**Explanation**

Recall that the central limit theorem states the following:

> *Suppose that $X_1, X_2, \ldots, X_n$ is a random sample of size $n$ from a population with a population mean $\mu$ and population standard deviation $\sigma$. The central limit theorem (or CLT) states that for sufficiently large $n$, the distribution of the sample mean $\overline{X}$ can be approximated as*
> $\overline{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right)$.

The expected value and the variance of a Poisson random variable $X_i \sim \textrm{Po}(\lambda)$ can be found as

$$
\textrm{E}[X_i] = \textrm{Var}[X_i] = \lambda
$$

So, in our case, we have

$$
\begin{aligned}
n &= 60, \\
μ &= E[X_{i}] = 12, \\
σ^{2} &= Var[X_{i}] = 12
\end{aligned}
$$

Therefore, for the approximate distribution of the sample mean $\overline{X}$, we obtain

$$
X^(―) \mid ∼N(μ, \frac{σ^{2}}{n}); ∼N(12, \frac{12}{60}); ∼N(12, 1 5)
$$

---

**Question 7**

Given that $X_{i}∼Po(2)$ for $i = 1, 2, …, 50$ are independent Poisson random variables, what is the approximate distribution of the sample mean $X^(―)$?The approximate distribution is $X^(―)∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

---

**Question 8**

Let $X_{i}∼Po(3)$ for $i = 1, 2, …, 120$ be independent Poisson random variables. Suppose we define the random variable $X$ as
$X = ∑_(i = 1)^(120)X_{i}$.
What is the approximate distribution of $X$?The approximate distribution is $X∼N($
$\underline{\hspace{1.5cm}}$ $$,
$\underline{\hspace{1.5cm}}$
$)$.

```update-progress
```

[[MA/PAS/Home|Home]]
[[MA/PAS/0. Table of Contents/TOC|Table of Contents]]
