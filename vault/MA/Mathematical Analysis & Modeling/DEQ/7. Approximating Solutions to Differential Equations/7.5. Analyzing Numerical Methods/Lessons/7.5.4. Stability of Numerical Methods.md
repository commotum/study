# Stability of Numerical Methods

<!--
lesson-id: 6685
topic-code: DEQ.7.5.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Stability of Euler's Method](#stability-of-eulers-method)
- [Identifying True Statements About the Stability of Euler's Method](#identifying-true-statements-about-the-stability-of-eulers-method)
- [Determining Stability in Euler's Method](#determining-stability-in-eulers-method)
- [Stability Functions, Stability Regions, and Absolute Stability](#stability-functions-stability-regions-and-absolute-stability)
- [A Worked Example](#a-worked-example)
- [Identifying True Statements About the Stability of Other Methods](#identifying-true-statements-about-the-stability-of-other-methods)
- [Table of Results](#table-of-results)
- [Determining Stability in Other Methods](#determining-stability-in-other-methods)

## Prerequisites

- [Convergence of Geometric Sequences](<../../../../CBC/14. Sequences & Series/14.1. Sequences/Lessons/14.1.2. Convergence of Geometric Sequences.md>)
- [Error in Numerical Methods](<7.5.2. Error in Numerical Methods.md>)
- [The Modified Euler Method](<../../7.2. Euler's Method/Lessons/7.2.3. The Modified Euler Method.md>)
- [The RK4 Method](<../../7.3. Higher-Order Numerical Methods/Lessons/7.3.1. The RK4 Method.md>)
- [The Trapezoidal Method](<../../7.4. Implicit Numerical Methods/Lessons/7.4.2. The Trapezoidal Method.md>)

---

<a id="introduction"></a>
## Introduction

When solving ODEs numerically:

- The solution is advanced in many steps.
- Each step introduces a small error (LTE).
- These errors propagate forward (contributing to GTE).

Stability describes how a numerical method responds to these small perturbations.

A method is **stable** if small errors remain controlled as the computation proceeds.
A method is **unstable** if small errors can grow rapidly, even when the true solution stays well-behaved.

Stability determines how local errors turn into global errors: a method may be accurate in a single step, but if it is unstable, the global error can grow without bound.

One way to study stability is to test a numerical method on the following **test equation**:

$$
y' = -\lambda y, \qquad y(0)=1, \quad x \geq 0, \quad \lambda > 0
$$

The exact solution of the test equation is

$$
y(x) = e^{-\lambda x}
$$

which satisfies $y(x) \to 0$ as $x \to \infty$.

We say that a numerical method with update formula

$$
y_{n+1} = y_n + \Delta y
$$

is **stable** for a given step size $\Delta x$ if, when applied to the test equation, the numerical solution also satisfies

$$
y_n \to 0 \:\text{as}\: n \to \infty
$$

To see how this works in practice, let's analyze the stability of Euler's method.

---

<a id="stability-of-eulers-method"></a>
## Stability of Euler's Method

We will now check the stability of Euler's method by applying it to the test equation

$$
y' = -\lambda y, \qquad y(0)=1, \quad x \geq 0, \quad \lambda > 0
$$

First, recall that, for an initial value problem

$$
y' = f(x,y), y(x_0) = y_0
$$

Euler's method is given by the update formula

$$
y_{n+1} = y_n + f(x_n, y_n) \cdot \Delta x
$$

Applying Euler's method to the test equation, we get the following:

$$
\begin{aligned}
y_{n + 1} &= y_{n} + (- λy_{n}) \cdot Δx \\
&= y_{n} - λy_{n} \cdot Δx \\
&= (1 - λΔx)y_{n}
\end{aligned}
$$

Now, for stability, we require $y_n \to 0$ as $n \to \infty$. Here, since $y_0=1$, we have

$$
\begin{aligned}
y_{n} &= (1 - λΔx)y_{n - 1} \\
&= (1 - λΔx)((1 - λΔx)y_{n - 2}) \\
&= (1 - λΔx)^{2}y_{n - 2} \\
&= ⋮⋮ \\
&= (1 - λΔx)^{n}y_{0} \\
&= (1 - λΔx)^{n}
\end{aligned}
$$

Let's define

$$
R(\lambda\Delta x) = 1 - \lambda\Delta x
$$

Then, $y_n$ converges to $0$ as $n \to \infty$ if and only if $\mid R(\lambda\Delta x) \mid < 1$. The function $R(\lambda\Delta x)$ is called the **stability function** of Euler's method.

Expanding this inequality, we get

$$
\mid R(λΔx) \mid \mid < 1; \mid 1 - λΔx \mid \mid < 1; - 1 < 1 - λΔx\begin{vmatrix}< 1 \\ -2 < - λΔx\end{vmatrix}< 0; 0 < λΔx \mid < 2
$$

This means that Euler's method is stable when applied to the test equation for values in the region $0 < \lambda\Delta x < 2$.

The diagram below shows the exact solution curve with the numerical steps of Euler's method for the cases $\lambda\Delta x < 2$ and

$$
\lambda\Delta x = 2
$$

with $\lambda > 0$ fixed.

![](<../Source/7.5.4. Stability of Numerical Methods/Images/f723b0574e30e665499be59c3e6e5136.png>)

Notice that, for $\lambda\Delta x < 2$, the numerical values converge to $0$ (stable), whereas for

$$
\lambda\Delta x = 2
$$

the values oscillate between $-1$ and $1$ and do not converge to $0$ (unstable).

For values $\lambda\Delta x > 2$, the numerical solution would grow in magnitude at each step, leading to rapidly diverging values.

---

<a id="identifying-true-statements-about-the-stability-of-eulers-method"></a>
## Identifying True Statements About the Stability of Euler's Method

**Example:** Which of the following statements are true when testing the stability of Euler's method on the test equation $y' = -\lambda y$ with $y = y(x), \, y(0) = 8, \ x \geq 0, \, \lambda = 6$, and step size $\Delta x$?

1. The update formula can be written as $y_{n+1} = \left(1 - 6\Delta x\right)^n y_n$.
2. The method is stable for $\mid 1 - 6\Delta x \mid < 1$.
3. The method is stable for step size $\Delta x = 0.1$.
4. The method is **unstable** for step size $\Delta x = 0.2$.

**Explanation**

A numerical method

$$
y_{n+1} = y_n + \Delta y_n
$$

is stable for a given step size $\Delta x$ if, when applied to the test equation

$$
y' = -\lambda y, \qquad y(0) = 8, \quad x \geq 0, \quad \lambda > 0
$$

the numerical solution satisfies $y_n \to 0$ as $n \to \infty$, just like the exact solution.

Now, let's check the stability of Euler's Method by applying it to the test equation

$$
y' = -6y, \qquad y(0) = 8
$$

First, for an initial value problem

$$
y' = f(x,y), y(x_0) = y_0
$$

Euler's method is given by the update formula

$$
y_{n+1} = y_n + f(x_n,y_n) \cdot \Delta x
$$

Now, let's check our statements:

- Applying Euler's method to the test equation, we get the following:
$y_{n + 1}|= y_{n} + (-6y_{n}) \cdot Δx; = y_{n} - 6y_{n} \cdot Δx; = (1 - 6Δx)y_{n}$.
Hence, statement I is false.
- For stability, we require $y_n \to 0$ as $n \to \infty$. Here, we have
$y_{n}|= (1 - 6Δx)y_{n - 1}; = (1 - 6Δx)((1 - 6Δx)y_{n - 2}); = (1 - 6Δx)^{2}y_{n - 2}; ⋮⋮; = (1 - 6Δx)^{n}y_{0}; = 8(1 - 6Δx)^{n}$.
Therefore, $y_n$ converges to $0$ as $n \to \infty$ if and only if $\left|1 - 6\Delta x\right| < 1$, as stated. Hence, statement II is true.
- Expanding the inequality $\left|1 - 6\Delta x\right| < 1$, we get
$\begin{bmatrix}-1 & < 1 - 6Δx < 1 \\ -2 & < - 6Δx < 0 \\ 0 & < 6Δx < 2 \\ 0 & < Δx < \frac{1}{3}\end{bmatrix}$.
Finally, we determine stability for the given step sizes:Since $\Delta x = 0.1$ satisfies the above inequality, Euler's method is stable for this value. Hence, statement III is true.Since $\Delta x = 0.2$ also satisfies the above inequality, Euler's method is stable for this value. Hence, statement IV is false.

In conclusion, the correct answer is "II and III only".

---

**Question 1**

Which of the following statements are true when testing the stability of Euler's method on the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ = 4$, and step size $Δx$?

1. The update formula can be written as $y_{n + 1} = (1 - 4Δx)^{n}y_{n}$.
2. The method is stable for $\mid 1 - 4Δx \mid < 1$.
3. The method is stable for step size $Δx = 0.2$.
4. The method is **unstable** for step size $Δx = 0.3$.

- [ ] A. II and III only
- [ ] B. II, III, and IV only
- [ ] C. I, II, and III only
- [ ] D. III and IV only
- [ ] E. I, II, and IV only

---

**Question 2**

Which of the following statements are true when testing the stability of Euler's method on the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ = 5$, and step size $Δx$?

1. The update formula can be written as $y_{n + 1} = (1 - 5Δx)^{n}y_{n}$.
2. The method is stable for $\mid 1 - 5Δx \mid < 1$.
3. The method is stable for step size $Δx = 0.2$.
4. The method is **unstable** for step size $Δx = 0.25$.

- [ ] A. II, III, and IV only
- [ ] B. III and IV only
- [ ] C. I, II, and III only
- [ ] D. II and III only
- [ ] E. I, II, and IV only

---

<a id="determining-stability-in-eulers-method"></a>
## Determining Stability in Euler's Method

**Example:** For which of the following step sizes is Euler's Method stable, given that $\lambda = 1$ is the parameter of the test equation $y' = -\lambda y$ with $y = y(x), y(0) = 1, x \geq 0$, and step size $\Delta x$?

1. $\Delta x = 0.5$
2. $\Delta x = 0.9$
3. $\Delta x = 1.2$
4. $\Delta x = 1.8$

*Hint: The stability function for Euler's method is given by* $R(\lambda\Delta x) = 1 - \lambda \Delta x$.

**Explanation**

A numerical method

$$
y_{n+1} = y_n + \Delta y_n
$$

is stable for a given step size $\Delta x$ if, when applied to the test equation

$$
y' = -\lambda y, \qquad y(0) = 1, \quad x \geq 0, \quad \lambda > 0
$$

the numerical solution satisfies $y_n \to 0$ as $n \to \infty$, just like the exact solution.

For stability, we require that the following condition holds:

$$
\mid R(λΔx) \mid \mid < 1; \mid 1 - λΔx \mid \mid < 1
$$

Solving this gives
$0 < \lambda \Delta x < 2$.

This means that Euler's method is stable when applied to the test equation for values in the region $0 < \lambda\Delta x < 2$.

In our case, where $\lambda=1$, this region simplifies to
$0 \mid < Δx < 2$.

Now, of the given step sizes:

- $\Delta x = 0.5$ satisfies the inequality,
- $\Delta x = 0.9$ satisfies the inequality,
- $\Delta x = 1.2$ satisfies the inequality, and
- $\Delta x = 1.8$ satisfies the inequality.

Therefore, Euler's method is stable for all given step sizes.

Thus, the correct answer is "I, II, III, and IV".

---

**Question 3**

For which of the following values is Euler's method stable, given that $λ$ is the parameter of the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ > 0$, and step size $Δx$?

1. $λΔx = 0.67$
2. $λΔx = 1.20$
3. $λΔx = 1.80$
4. $λΔx = 2.25$

*Hint: The stability function for Euler's method is given by* $R(λΔx) = 1 - λΔx$.

- [ ] A. None
- [ ] B. I, II, and III only
- [ ] C. I only
- [ ] D. I, II, III, and IV
- [ ] E. I and II only

---

**Question 4**

For which of the following step sizes is Euler's Method **unstable**, given that $λ = 20$ is the parameter of the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ > 0$, and step size $Δx$?

1. $Δx = 0.05$
2. $Δx = 0.12$
3. $Δx = 0.2$
4. $Δx = 0.25$

*Hint: The stability function for Euler's method is given by* $R(λΔx) = 1 - λΔx$.

- [ ] A. III and IV only
- [ ] B. I, II, III, and IV
- [ ] C. IV only
- [ ] D. None
- [ ] E. II, III, and IV only

---

<a id="stability-functions-stability-regions-and-absolute-stability"></a>
## Stability Functions, Stability Regions, and Absolute Stability

In general, when a numerical method

$$
y_{n+1} = y_n + \Delta y_n
$$

is applied to the test equation

$$
y' = -\lambda y, \qquad y(0) = 1, \quad x \geq 0, \quad \lambda > 0
$$

we can write the update in the form

$$
y_{n+1} = R(\lambda\Delta x)y_n
$$

where $R(\lambda\Delta x)$ is the **stability function** of the method.

For stability, we require $y_n \to 0$ as $n \to \infty$. Here, since $y_0=1$,

$$
\begin{aligned}
y_{n} &= R(λΔx)y_{n - 1} \\
&= (R(λΔx))^{2}y_{n - 2} \\
&= ⋮⋮ \\
&= (R(λΔx))^{n}y_{0} \\
&= (R(λΔx))^{n}
\end{aligned}
$$

As a geometric sequence, this decays to $0$ as $n\to\infty$ if and only if $\mid R(\lambda\Delta x) \mid < 1$.

The set of values for which the method is stable, given by $\mid R(\lambda\Delta x) \mid < 1$, is called the **stability region** of the method.

A method that is stable for all possible values $\lambda\Delta x > 0$ is called **absolutely stable**.

We've seen that Euler's method is *not* absolutely stable, since its stability region is $0 < \lambda\Delta x < 2$. But what about the implicit Euler method? Let's find out.

---

<a id="a-worked-example"></a>
## A Worked Example

Now, let's check the stability of the implicit Euler method by applying it to the test equation

$$
y' = -\lambda y, \qquad y(0) = 1, \quad x \geq 0, \quad \lambda > 0
$$

First, recall that, for an initial value problem

$$
y' = f(x,y), y(x_0) = y_0
$$

the implicit Euler method is given by the update formula

$$
y_{n+1} = y_n + f(x_{n+1},y_{n+1}) \cdot \Delta x
$$

Applying the implicit Euler method to the test equation, we get the following:

$$
\begin{aligned}
y_{n + 1} &= y_{n} + (- λy_{n + 1}) \cdot Δx \\
y_{n + 1} + λy_{n + 1} \cdot Δx &= y_{n} \\
y_{n + 1}(1 + λΔx) &= y_{n} \\
y_{n + 1} &= \frac{1}{1 + λΔx} \cdot y_{n}
\end{aligned}
$$

So, the stability function is

$$
R(\lambda\Delta x) = \dfrac{1}{1 + \lambda\Delta x}
$$

The method is stable when

$$
\mid R(λΔx) \mid \mid < 1; \mid \frac{1}{1 + λΔx} \mid \mid < 1
$$

Therefore, $y_n$ converges to $0$ as $n \to \infty$ if and only if

$$
\left|\dfrac{1}{1 + \lambda\Delta x}\right| < 1
$$

Since $1 + \lambda\Delta x > 1$ for any positive step size $\Delta x$, we have

$$
\left|\dfrac{1}{1 + \lambda\Delta x}\right| < 1
$$

for all $\Delta x > 0$. So, the implicit Euler method is absolutely stable!

The diagram below shows the exact solution curve with the numerical steps of both the explicit and the implicit Euler methods for the value

$$
\lambda\Delta x = 2
$$

We saw before that the explicit method oscillates between the values $-1$ and $1$ and does not converge to $0$.

Conversely, the numerical steps of the implicit Euler method converge to $0$ quite quickly for the same step size!

![](<../Source/7.5.4. Stability of Numerical Methods/Images/a3f116666d9e9be9489eec23582bcc71.png>)

Implicit methods tend to be more numerically stable than explicit methods.

---

<a id="identifying-true-statements-about-the-stability-of-other-methods"></a>
## Identifying True Statements About the Stability of Other Methods

**Example:** For the initial value problem $y' = f(x,y), y(x_0) = y_0$, the modified Euler method can be expressed as the update formula

$Δy_{n}^{(p)}|= f(x_{n}, y_{n}) \cdot Δx,; y_{n + 1}|= y_{n} + \frac{1}{2}(f(x_{n}, y_{n}) + f(x_{n + 1}, y_{n} + Δy_{n}^{(p)})) \cdot Δx$.

Which of the following statements are true when testing the stability of the modified Euler method on the test equation $y'=-\lambda y$ with $y = y(x), y(0) = 1, x \geq 0, \lambda = 12$, and step size $\Delta x$?

1. The stability function is $R(12\Delta x) = 1 - 12\Delta x + 72(\Delta x)^2$.
2. The stability region is $\left|72\left(\Delta x - \dfrac16\right)^2\right| < 1$.
3. The method is absolutely stable.
4. The method is stable for step size $\Delta x = 0.15$.

*Hint:* The stability function $R$ is given by $y_{n+1} = R(\lambda\Delta x)y_n$.

**Explanation**

A numerical method

$$
y_{n+1} = y_n + \Delta y_n
$$

is stable for a given step size $\Delta x$ if, when applied to the test equation

$$
y' = -\lambda y, \qquad y(0) = 1, \quad x \geq 0, \quad \lambda > 0
$$

the numerical solution satisfies $y_n \to 0$ as $n \to \infty$, just like the exact solution.

When a method is applied to the test equation, the update takes the form

$$
y_{n+1} = R(\lambda\Delta x)y_n
$$

where $R(\lambda\Delta x)$ is the stability function of the method. The set of values for which the method is stable, given by $\mid R(\lambda\Delta x) \mid < 1$, is called the stability region of the method.

A method that is stable for all possible values $\lambda\Delta x > 0$ is called absolutely stable.

Now, let's check the stability of the modified Euler Method by applying it to the test equation

$$
y' = -12y, \qquad y(0) = 1
$$

We now examine our statements:

- Applying the modified Euler method to this test equation, we get the following:
$y_{n} + Δy_{n}^{(p)}|= y_{n} + f(x_{n}, y_{n}) \cdot Δx; = y_{n} - 12y_{n} \cdot Δx$
Thus,
$y_{n + 1}|= y_{n} + \frac{1}{2}(f(x_{n}, y_{n}) + f(x_{n + 1}, y_{n} - 12y_{n} \cdot Δx)) \cdot Δx; = y_{n} + \frac{1}{2}(-12y_{n} - 12(y_{n} - 12y_{n} \cdot Δx)) \cdot Δx; = y_{n} + \frac{1}{2}(-24y_{n} + 144y_{n} \cdot Δx) \cdot Δx; = y_{n} - 12y_{n} \cdot Δx + 72y_{n} \cdot (Δx)^{2}; = [1 - 12Δx + 72(Δx)^{2}]y_{n}$.
So, the stability function is $R(12\Delta x) = 1 - 12\Delta x + 72(\Delta x)^2$. Hence, statement I is true.
- The method is stable when
$\mid R(12Δx) \mid \mid < 1; \mid 1 - 12Δx + 72(Δx)^{2} \mid \mid < 1; \mid 72(Δx - \frac{1}{12})^{2} + \frac{1}{2} \mid \mid < 1$.
Therefore, $y_n$ converges to $0$ as $n \to \infty$ if and only if
$\left|72\left(\Delta x - \dfrac1{12}\right)^2 + \dfrac12\right| < 1$.
Hence, statement II is false.
- Now, since the argument of the absolute value is always non-negative, we can remove the absolute value. So, we have
$\begin{bmatrix}72(Δx - \frac{1}{12})^{2} + \frac{1}{2} & < 1 \\ 72(Δx - \frac{1}{12})^{2} & < \frac{1}{2} \\ (Δx - \frac{1}{12})^{2} & < \frac{1}{144} \\ -\frac{1}{12} < Δx - \frac{1}{12} & < \frac{1}{12} \\ 0 < Δx & < \frac{1}{6}\end{bmatrix}$
Thus, the modified Euler method is not absolutely stable, and statement III is false.
- Finally, since it satisfies this inequality, the modified Euler method is stable for step size $\Delta x = 0.15$, and hence statement IV is true.

In conclusion, the correct answer is "I and IV only".

---

**Question 5**

Which of the following statements are true when testing the stability of the implicit Euler method on the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ = 3$, and step size $Δx$?

1. The stability function is $R(3Δx) = \frac{1}{1 + 3Δx}$.
2. The stability region is $\mid \frac{1}{1 - 3Δx} \mid < 1$.
3. The method is absolutely stable.
4. The method is **unstable** for step size $Δx = 0.25$.

*Hint:* The stability function $R$ is given by $y_{n + 1} = R(λΔx)y_{n}$.

- [ ] A. II and IV only
- [ ] B. II only
- [ ] C. I and IV only
- [ ] D. II and III only
- [ ] E. I and III only

---

**Question 6**

Which of the following statements are true when testing the stability of the trapezoidal method on the test equation $y^{′} = - λy$ with $y = y(x), y(0) = 1, x \ge 0, λ = 3$, and step size $Δx$?

1. The stability function is $R(3Δx) = \frac{1 - 3Δx/2}{1 + 3Δx/2}$.
2. The stability region is $\mid \frac{9}{2}(Δx - \frac{2}{3})^{2} + \frac{1}{2} \mid < 1$.
3. The method is absolutely stable.
4. The method is **unstable** for step size $Δx = 2.5$.

*Hint:* The stability function $R$ is given by $y_{n + 1} = R(λΔx)y_{n}$.

- [ ] A. I and III only
- [ ] B. I and II only
- [ ] C. II and III only
- [ ] D. I and IV only
- [ ] E. II and IV only

---

<a id="table-of-results"></a>
## Table of Results

A table of some numerical methods, their stability functions, and stability regions is shown below. These results assume the test equation

$$
y' = -\lambda y
$$

(where $\lambda > 0$) with step size $\Delta x$.

| Numerical Method | Stability Function $R(\lambda \Delta x)$ | Stability Region $\mid R(\lambda \Delta x) \mid < 1$ | Absolutely Stable? |
| --- | ---: | ---: | ---: |
| Euler's | $1 - \lambda \Delta x$ | $0 < \lambda \Delta x < 2$ | $\color{red}\times$ |
| Implicit Euler | $\dfrac{1}{1 + \lambda \Delta x}$ | All values $\lambda \Delta x > 0$ | $\color{green}\checkmark$ |
| Trapezoidal | $\dfrac{1 - \lambda \Delta x/2}{1 + \lambda \Delta x/2}$ | All values $\lambda \Delta x > 0$ | $\color{green}\checkmark$ |
| Modified Euler | $1 - \lambda\Delta x + \dfrac{(\lambda\Delta x)^2}{2}$ | $0 < \lambda\Delta x < 2$ | $\color{red}\times$ |
| Runge-Kutta (RK4) | $1 - \lambda\Delta x + \dfrac{(\lambda\Delta x)^2}{2} - \dfrac{(\lambda\Delta x)^3}{6} + \dfrac{(\lambda\Delta x)^4}{24}$ | $0 < \lambda\Delta x < 2.785\ldots$ | $\color{red}\times$ |

---

<a id="determining-stability-in-other-methods"></a>
## Determining Stability in Other Methods

**Example:** For which of the following values is the Runge–Kutta Method (RK4) stable, given that $\lambda$ is the parameter of the test equation?

1. $\lambda\Delta x = 0.6$
2. $\lambda\Delta x = 1.4$
3. $\lambda\Delta x = 2.8$
4. $\lambda\Delta x = 3.3$

*Hint: The stability function $R$ of the RK4 method is given by*
$R(\lambda\Delta x) = 1 - \lambda\Delta x + \dfrac{(\lambda\Delta x)^2}{2} - \dfrac{(\lambda\Delta x)^3}{6} + \dfrac{(\lambda\Delta x)^4}{24}$.

**Explanation**

A numerical method

$$
y_{n+1} = y_n + \Delta y_n
$$

is stable for a given step size $\Delta x$ if, when applied to the test equation

$$
y' = -\lambda y, \qquad y(0) = 1, \quad x \geq 0, \quad \lambda > 0
$$

the numerical solution satisfies $y_n \to 0$ as $n \to \infty$, just like the exact solution.

When a method is applied to the test equation, the update takes the form

$$
y_{n+1} = R(\lambda\Delta x)y_n
$$

where $R(\lambda\Delta x)$ is referred to as the stability function of the method. The set of values for which the method is stable, given by $\mid R(\lambda\Delta x) \mid < 1$, is called the stability region of the method.

The stability function of the RK4 Method is

$$
R(\lambda\Delta x) = 1 - \lambda\Delta x + \dfrac{(\lambda\Delta x)^2}{2} - \dfrac{(\lambda\Delta x)^3}{6} + \dfrac{(\lambda\Delta x)^4}{24}
$$

Let's check the stability of the RK4 Method for each of the given values in turn.

- For $\lambda\Delta x = 0.6$, we have
$\begin{vmatrix}R(0.6) & |= & 1 - 0.6 + ((0.6)^{2})/(2) - ((0.6)^{3})/(6) + ((0.6)^{4})/(24)\end{vmatrix}; = \mid \frac{2747}{5000} \mid; = 0.5494 < 1.✓$
Since this value falls *within* the stability region, the RK4 Method is stable.
- For $\lambda\Delta x = 1.4$, we have
$\begin{vmatrix}R(1.4) & |= & 1 - 1.4 + ((1.4)^{2})/(2) - ((1.4)^{3})/(6) + ((1.4)^{4})/(24)\end{vmatrix}; = \mid \frac{4241}{15000} \mid; = 0.282… < 1.✓$
Since this value falls *within* the stability region, the RK4 Method is stable.
- For $\lambda\Delta x = 2.8$, we have
$\begin{vmatrix}R(2.8) & |= & 1 - 2.8 + ((2.8)^{2})/(2) - ((2.8)^{3})/(6) + ((2.8)^{4})/(24)\end{vmatrix}; = \mid \frac{639}{625} \mid; = 1.0224 > 1.\cdot$
Since this value falls *outside* the stability region, the RK4 Method is unstable.
- For $\lambda\Delta x = 3.3$, we have
$\begin{vmatrix}R(3.3) & |= & 1 - 3.3 + ((3.3)^{2})/(2) - ((3.3)^{3})/(6) + ((3.3)^{4})/(24)\end{vmatrix}; = \mid \frac{167747}{80000} \mid; = 2.096… > 1.\cdot$
Since this value falls *outside* the stability region, the RK4 Method is unstable.

Therefore, the correct answer is "I and II only".

Note that the stability region of the RK4 Method is $0 < \lambda\Delta x < 2.785\ldots$, so it is not absolutely stable.

---

**Question 7**

> A scientific calculator is required to answer this question.

For which of the following values is the implicit Euler method stable, given that $λ$ is the parameter of the test equation?

1. $λΔx = 0.25$
2. $λΔx = 1$
3. $λΔx = 1.5$
4. $λΔx = 9$

*Hint: The stability function $R$ of the implicit Euler method is given by*
$R(λΔ) = \frac{1}{1 + λΔx}$.

- [ ] A. I, II, III, and IV
- [ ] B. I, II, and III only
- [ ] C. None
- [ ] D. I and II only
- [ ] E. I only

---

**Question 8**

> A scientific calculator is required to answer this question.

For which of the following values is the Runge-Kutta Method (RK4) stable, given that $λ$ is the parameter of the test equation?

1. $λΔx = 0.4$
2. $λΔx = 1$
3. $λΔx = 2$
4. $λΔx = 3$

*Hint: The stability function $R$ of the RK4 method is given by*
$R(λΔ) = 1 - λΔx + ((λΔx)^{2})/(2) - ((λΔx)^{3})/(6) + ((λΔx)^{4})/(24)$.

- [ ] A. I only
- [ ] B. None
- [ ] C. I, II, III, and IV
- [ ] D. I, II, and III only
- [ ] E. I and II only

```update-progress
```

[[MA/DEQ/Home|Home]]
[[MA/DEQ/0. Table of Contents/TOC|Table of Contents]]
