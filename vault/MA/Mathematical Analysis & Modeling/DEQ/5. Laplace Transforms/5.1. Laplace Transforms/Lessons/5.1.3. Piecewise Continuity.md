# Piecewise Continuity

<!--
lesson-id: 6669
topic-code: DEQ.5.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Piecewise-Continuous Functions Over Closed Intervals](#identifying-piecewise-continuous-functions-over-closed-intervals)
- [Piecewise Continuity](#piecewise-continuity)
- [Identifying Piecewise-Continuous Functions Over Infinite Intervals](#identifying-piecewise-continuous-functions-over-infinite-intervals)

## Prerequisites

- [The Floor and Ceiling Functions](<5.1.1. The Floor and Ceiling Functions.md>)
- [Continuity Over an Interval](<../../../../CAB/1. Limits and Continuity/1.6. Continuity/Lessons/1.6.8. Continuity Over an Interval.md>)
- [Point Discontinuities](<../../../../CAB/1. Limits and Continuity/1.6. Continuity/Lessons/1.6.5. Point Discontinuities.md>)
- [Jump Discontinuities](<../../../../CAB/1. Limits and Continuity/1.6. Continuity/Lessons/1.6.6. Jump Discontinuities.md>)
- [The Unit Step Function](<5.1.2. The Unit Step Function.md>)
- [Sets and Functions](<../../../../LAL/1. Preliminaries/1.2. Set Operations/Lessons/1.2.6. Sets and Functions.md>)
- [Further Continuity of Piecewise Functions](<../../../../CAB/1. Limits and Continuity/1.6. Continuity/Lessons/1.6.4. Further Continuity of Piecewise Functions.md>)
- [Infinite Sets](<../../../../LAL/1. Preliminaries/1.1. Introduction to Set Theory/Lessons/1.1.9. Infinite Sets.md>)

---

<a id="introduction"></a>
## Introduction

We often want to deal with functions that behave "nicely" on a closed interval:

- Often, they should be continuous most of the time, and
- any discontinuities should be simple and well-behaved.

A function $f(x)$ is **piecewise continuous** on $x\in [a,b]$ if

- it is continuous on $[a,b]$ except at *finitely many points*, and
- at each point of discontinuity, *the one-sided limits exist and are finite*.

In other words, there can only be finitely many discontinuities, and each of them must be either

- a point discontinuity (also called a removable discontinuity), or
- a jump discontinuity.

**Note:** Every *continuous* function is *piecewise continuous* since it has no discontinuities at all.

Let's consider two examples on the interval $x \in [0,\pi]$.

- The function
${x + 2,\begin{vmatrix}x < 1 \\ 5 - x,\end{vmatrix}x \ge 1$
is piecewise continuous on $x\in[0,\pi]$ since it is continuous on $x\in[0,\pi]$ except at the point $x=1$, where there is a jump discontinuity and the one-sided limits exist and are finite:
$\lim\limits_{x\to1^-}h(x)=3, \qquad \lim\limits_{x\to1^+}h(x)=4$

![](<../Source/5.1.3. Piecewise Continuity/Images/f58d4029fd7d06a567a8d67d0479577b.png>)

- The function
$f(x)=2\tan x$
is not piecewise continuous on $x\in[0,\pi]$ since the one-sided limits at $x=\dfrac{\pi}{2}$ are infinite:
$\lim\limits_{x\to\pi/2^-}f(x)=+\infty, \qquad \lim\limits_{x\to\pi/2^+}f(x)=-\infty$

![](<../Source/5.1.3. Piecewise Continuity/Images/fe30c8eeafd5569ded8f64074167caba.png>)

---

<a id="identifying-piecewise-continuous-functions-over-closed-intervals"></a>
## Identifying Piecewise-Continuous Functions Over Closed Intervals

**Example:** Which of the following functions are piecewise continuous on $x\in [0,\pi]$.

${x + 2,\begin{vmatrix}x < 1 \\ 5 - x,\end{vmatrix}x \ge 1$.

**Explanation**

A function $f(x)$ is piecewise continuous on $x\in [a,b]$ if

- it is continuous on $[a,b]$ except at finitely many points, and
- at each point of discontinuity, the one-sided limits exist and are finite.

In other words, there can only be finitely many discontinuities, and each of them must be either a point or a jump discontinuity.

With that in mind, let's examine each function.

- The function $f(x)$ is not piecewise continuous over $x\in [0,\pi]$ since the one-sided limits at $x=\dfrac{\pi}{2}$ are infinite:
$\lim\limits_{x\to\pi/2^-}f(x) = +\infty, \qquad \lim\limits_{x\to\pi/2^+}f(x) = -\infty$
- The function $g(x)$ is piecewise continuous over $x\in [0,\pi]$ since it is continuous over $x\in [0,\pi]$.
- The function $h(x)$ is piecewise continuous over $x\in [0,\pi]$ since it is continuous over $x\in [0,\pi]$ except at the point $x=1$, where the one-sided limits exist and are finite:
$\lim\limits_{x\to1^-}h(x) = 3, \qquad \lim\limits_{x\to1^+}h(x) = 4$

Therefore, the correct answer is "$g$ and $h$ only."

---

**Question 1:**

```quiz
type: radio
id: ma-340920
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following functions are piecewise continuous on $x ∈ [0, π]$.
  
  $f(x) = \frac{1}{x + 1}, g(x) = \tan x, h(x) = {0,\begin{vmatrix}x \ne 1 \\ 1,\end{vmatrix}x = 1$.
options:
- id: a
  content: |-
    $f, g$ and $h$
- id: b
  content: |-
    $h$ only
- id: c
  content: |-
    $f$ only
- id: d
  content: |-
    $f$ and $h$ only
- id: e
  content: |-
    $f$ and $g$ only
```

---

**Question 2:**

```quiz
type: radio
id: ma-340908
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following functions are piecewise continuous on $x ∈ [- 1, 1]$.
  
  $f(x) = \frac{1}{x}, g(x) = {0,\begin{vmatrix}x < 0 \\ 1,\end{vmatrix}x \ge 0,h(x) = \cos x$.
options:
- id: a
  content: |-
    $f$ and $h$ only
- id: b
  content: |-
    $g$ and $h$ only
- id: c
  content: |-
    $f, g$, and $h$
- id: d
  content: |-
    $h$ only
- id: e
  content: |-
    $g$ only
```

---

<a id="piecewise-continuity"></a>
## Piecewise Continuity

So far, we have talked about what it means for a function to be piecewise continuous on a closed interval $[a,b]$.

To define piecewise continuity on a more general interval, we check what happens on every closed and bounded subinterval.

Suppose $I \subseteq \mathbb{R}$. A function $f: I \to \mathbb{R}$ is **piecewise continuous on $I$** if it is piecewise continuous on every closed and bounded subinterval $[a,b] \subset I$.

For example, consider the floor function

$$
f(x) = \lfloor x \rfloor
$$

on the interval $[MATH: [0, \infty).]$

- It has infinitely many jump discontinuities at the integers $x = 1, 2, 3, \dots$
- However, any closed and bounded subinterval $[MATH: [a,b] \subset [0, \infty)]$ contains only finitely many integers.
- Thus, $f$ is piecewise continuous on every such $[a,b]$, so it is piecewise continuous on $[MATH: [0, \infty).]$

**Note:** If a function has infinitely many discontinuities on some closed and bounded interval $[a,b]$, then it is *not* piecewise continuous on any interval that contains $[a,b]$.

---

<a id="identifying-piecewise-continuous-functions-over-infinite-intervals"></a>
## Identifying Piecewise-Continuous Functions Over Infinite Intervals

**Example:** Which of the following functions are piecewise continuous on $[MATH: x\in [0,\infty).]$

$f(x)|= {1,\begin{vmatrix}⌈x⌉is even \\ -1,\end{vmatrix}⌈x⌉is odd; g(x)|= u(x - 1) + u(x - \frac{1}{2}) + u(x - \frac{1}{3}) + ⋯; h(x)|= ⌈\frac{x}{2}⌉$
Note that $u(x)$ is the unit step function, and $\lceil x \rceil$ is the ceiling function.

**Explanation**

Suppose $I\subseteq \mathbb R$. A function $f: I \to \mathbb R$ is piecewise continuous on $I$ if it is piecewise continuous on every closed and bounded subinterval $[a,b]\subset I$.

With that in mind, let's examine each function.

- The function $f(x)$ is piecewise continuous over $[MATH: x\in [0,\infty).]$ The function has jump discontinuities at $x=1,2,3,\ldots$, and any closed interval $[MATH: [a,b]\subset [0,\infty)]$ will contain finitely many discontinuities.

![](<../Source/5.1.3. Piecewise Continuity/Images/d382ac4af0a40eb467736e4be9da14d2.png>)

- The function $g(x)$ is not piecewise continuous over $[MATH: x\in [0,\infty).]$ Consider the closed subinterval $x\in [0,1]$. Notice that the function has jump discontinuities at $x=1,\dfrac12,\dfrac13,\dfrac14,\ldots$, so it has infinitely many discontinuities on this closed interval.
- The function $h(x)$ is piecewise continuous over $[MATH: x\in [0,\infty).]$ The function has jump discontinuities at $x=0,2,4,\ldots$, and any closed interval $[MATH: [a,b]\subset [0,\infty)]$ will contain finitely many discontinuities.

![](<../Source/5.1.3. Piecewise Continuity/Images/a5b2b21c0b5d491bbb537537aec2e88e.png>)

Therefore, the correct answer is "$f$ and $h$ only."

---

**Question 3:**

```quiz
type: radio
id: ma-340909
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following functions are piecewise continuous on $[MATH: x ∈ [0, ∞).]$
  
  $f(x) = \sec x, g(x) = u(x - 1), h(x) = {1,\begin{vmatrix}⌊x⌋is even \\ -1,\end{vmatrix}⌊x⌋is odd$
  Note that $u(x)$ is the unit step function, and $⌊x⌋$ is the floor function.
options:
- id: a
  content: |-
    None
- id: b
  content: |-
    $h$ only
- id: c
  content: |-
    $g$ only
- id: d
  content: |-
    $g$ and $h$ only
- id: e
  content: |-
    $f, g$ and $h$
```

---

**Question 4:**

```quiz
type: radio
id: ma-341612
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Which of the following functions are piecewise continuous on $[MATH: x ∈ [0, ∞).]$
  
  $f(x) = {-1,\begin{vmatrix}⌊x⌋is even \\ 1,\end{vmatrix}⌊x⌋is odd,g(x) = u(x - 2), h(x) = \csc x$.
  Note that $u(x)$ is the unit step function, and $⌊x⌋$ is the floor function.
options:
- id: a
  content: |-
    $g$ and $h$ only
- id: b
  content: |-
    $f$ and $h$ only
- id: c
  content: |-
    $f$ and $g$ only
- id: d
  content: |-
    $h$ only
- id: e
  content: |-
    $g$ only
```

```update-progress
```

[[MA/DEQ/Home|Home]]
[[MA/DEQ/0. Table of Contents/TOC|Table of Contents]]
