# Fundamental Matrices

<!--
lesson-id: 6376
topic-code: DEQ.4.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Constructing a Fundamental Matrix](#constructing-a-fundamental-matrix)
- [Properties of the Fundamental Matrix](#properties-of-the-fundamental-matrix)
- [Proving Properties of a Fundamental Matrix](#proving-properties-of-a-fundamental-matrix)
- [Fundamental Matrices and Matrix Exponentials](#fundamental-matrices-and-matrix-exponentials)
- [Computing Fundamental Matrices Using Matrix Exponentials](#computing-fundamental-matrices-using-matrix-exponentials)

## Prerequisites

- [Solving Homogeneous Systems of ODEs With Distinct Eigenvalues and Initial Conditions](<../../4.2. Solving Systems of Linear ODEs/Lessons/4.2.3. Solving Homogeneous Systems of ODEs With Distinct Eigenvalues and Initial Conditions.md>)
- [Matrix Exponentials](<4.4.1. Matrix Exponentials.md>)

---

<a id="introduction"></a>
## Introduction

Consider the homogeneous linear system

$$
\mathbf{x}' = A\mathbf{x}
$$

where $A$ is an $n\times n$ matrix.

A **fundamental matrix** for this system is an $n\times n$ matrix $\Phi(t)$ whose columns are $n$ linearly independent solutions of the system.

If $\mathbf{x}_1(t),\dots,\mathbf{x}_n(t)$ are linearly independent solutions, then one fundamental matrix is

$$
\Phi(t)=\big[\mathbf{x}_1(t),\dots,\mathbf{x}_n(t)\big]
$$

**Note:** The columns of $\Phi(t)$ can be written in any order.

Let's see an example.

---

<a id="constructing-a-fundamental-matrix"></a>
## Constructing a Fundamental Matrix

**Example:** $\begin{bmatrix}5 & 1 \\ 0 & 5\end{bmatrix}$
Consider the system of linear ODEs and two of its linearly independent solutions $\mathbf x_1$ and $\mathbf x_2$, shown above. Find the fundamental matrix for the system.

**Explanation**

Given a linear system

$$
\mathbf{x}' = A \mathbf{x}
$$

where $A$ is an $n \times n$ matrix, a *fundamental matrix* $\Phi(t)$ of the system is an $n \times n$ matrix whose columns are linearly independent solutions of the system.

In our cases, we have a $2 \times 2$ system. We are also given that the two linearly independent solutions of the system are the following:

$$
\begin{aligned}
x_{1}(t) &= \begin{bmatrix}4 \\ 0\end{bmatrix}e^{5t} = \begin{bmatrix}4e^{5t} \\ 0\end{bmatrix} \\
x_{2}(t) &= \begin{bmatrix}4t \\ 4\end{bmatrix}e^{5t} = \begin{bmatrix}4te^{5t} \\ 4e^{5t}\end{bmatrix}
\end{aligned}
$$

Therefore, our fundamental could be written as
$\begin{bmatrix}4e^{5t} & 4te^{5t} \\ 0 & 4e^{5t}\end{bmatrix}$

**Note:** The columns of $\Phi$ can be written in any order.

---

**Question 1:** $x^{′} = \begin{bmatrix}1 & 3 \\ 1 & -1\end{bmatrix}x, x_{1}(t) = \begin{bmatrix}-1 \\ 1\end{bmatrix}e^{-2t}, x_{2}(t) = \begin{bmatrix}3 \\ 1\end{bmatrix}e^{2t}$
Consider the system of linear ODEs and two of its linearly independent solutions $x_{1}$ and $x_{2}$, shown above. Which of the following is a fundamental matrix for the system?

- [ ] A. $\begin{bmatrix}3e^{-2} & -e^{2} \\ e^{-2} & e^{2}\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-1 & 3 \\ 1 & 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 3 \\ 1 & -1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-e^{-2t} & 3e^{2t} \\ e^{-2t} & e^{2t}\end{bmatrix}$
- [ ] E. $\begin{bmatrix}3e^{-2t} & -e^{2t} \\ e^{-2t} & e^{2t}\end{bmatrix}$

---

**Question 2:** $x^{′} = \begin{bmatrix}1 & -2 \\ 2 & 1\end{bmatrix}x, x_{1}(t) = \begin{bmatrix}\cos 2t \\ \sin 2t\end{bmatrix}e^{t}, x_{2}(t) = \begin{bmatrix}-\sin 2t \\ \cos 2t\end{bmatrix}e^{t}$
Consider the system of linear ODEs and two of its linearly independent solutions $x_{1}$ and $x_{2}$, shown above. Which of the following is a fundamental matrix for the system?

- [ ] A. $\begin{bmatrix}1 & -2 \\ 2 & 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}e^{t}\cos 2t & -e^{t}\sin 2t \\ e^{t}\sin 2t & e^{t}\cos 2t\end{bmatrix}$
- [ ] C. $\begin{bmatrix}e^{t}\cos t & e^{t}\sin t \\ -e^{t}\sin t & e^{t}\cos t\end{bmatrix}$
- [ ] D. $\begin{bmatrix}\cos 2t & -\sin 2t \\ \sin 2t & \cos 2t\end{bmatrix}$
- [ ] E. $\begin{bmatrix}e\cos 2 & -e\sin 2 \\ e\sin 2 & e\cos 2\end{bmatrix}$

---

<a id="properties-of-the-fundamental-matrix"></a>
## Properties of the Fundamental Matrix

Again, let $\Phi(t)$ be a fundamental matrix for the linear system

$$
\mathbf{x}' = A\mathbf{x}
$$

where $A$ is an $n\times n$ matrix. The fundamental matrices have the following important properties.

1. *Satisfies the underlying system*. Indeed, since each column of $\Phi(t)$ is a solution of the system, we have
$\Phi'(t) = A\,\Phi(t)$.
2. *Invertibility*. The columns of $\Phi(t)$ are linearly independent for all $t$, so
$\det\Phi(t)\neq 0$
for all $t$. In particular, $\Phi(t)$ is invertible for all $t$.
3. *Representation of solutions*. Every solution $\mathbf{x}(t)$ of the system can be written in the form
$\mathbf{x}(t)=\Phi(t)\,\mathbf{c}$,
where $\mathbf{c}$ is a constant vector.
4. *Non-uniqueness*. If $C$ is any invertible constant matrix, then
$\Psi(t)=\Phi(t)\,C$
is also a fundamental matrix for the same system.

We'll now get some practice.

---

<a id="proving-properties-of-a-fundamental-matrix"></a>
## Proving Properties of a Fundamental Matrix

**Example:** Consider the system $\mathbf{x}'(t) = A \mathbf{x}(t)$, where $A$ is a constant $2 \times 2$ matrix. Let $\Phi(t)$ be a fundamental matrix for this system. A derivation showing that $\Phi(t)$ satisfies the matrix differential equation $\Phi'(t) = A \, \Phi(t)$ is given below.

*Hint: $\big[\mathbf{a}, \: \mathbf{b} \big]$ denotes a matrix whose columns are vectors $\mathbf{a}$ and $\mathbf{b}$.*

> $\textrm{L1}{:}\;$ Let $\Phi(t) = \big[\mathbf{x}_1(t), \: \mathbf{x}_2(t) \big]$
> $\textrm{L2}{:}\;$ $\mathbf{x}_1'(t) = A \mathbf{x}_1(t)$
> $\textrm{L3}{:}\;$ $\mathbf{x}_2'(t) = A \mathbf{x}_2(t)$
> $\textrm{L4}{:}\;$ $\Phi'(t) = \big[\mathbf{x}_1'(t), \: \mathbf{x}_2'(t) \big]$
> $\textrm{L5}{:}\;$ $\Phi'(t) = \big[A \mathbf{x}_1(t), \: A \mathbf{x}_2(t) \big]$
> $\textrm{L6}{:}\;$ $\Phi'(t) = A \big[\mathbf{x}_1(t), \: \mathbf{x}_2(t) \big]$
> $\textrm{L7}{:}\;$ $\Phi'(t) = A \Phi(t)$

Select the correct options in the following reasoning.

$\quad$ Line $\textrm{L2}$ follows from the fact that $AAAAAAAAAAAAAAA$.

$\quad$ Line $\textrm{L6}$ follows from line $\textrm{L5}$ by $AAAAAAAAAAAAAAA$.

$\quad$ Line $\textrm{L7}$ follows from line $\textrm{L6}$ and substitution from line $AAAAAAAAAAAAAAA$.

**Explanation**

Let's justify each of the selected steps.

- We first consider line $\textrm{L2}$. Since $\Phi(t)$ is a fundamental matrix, its columns $\mathbf{x}_1(t)$ and $\mathbf{x}_2(t)$ are linearly independent solutions of the homogeneous system $\mathbf{x}'(t) = A \mathbf{x}(t)$. This means that each column satisfies the differential equation. In particular,
$\mathbf{x}_1'(t) = A \mathbf{x}_1(t)$.
Therefore, $\textrm{L2}$ follows from the fact that $\mathbf{x}_1$ is a solution of the system.
- Next, we consider lines $\textrm{L5}$ and $\textrm{L6}$. We now factor the matrix $A$ on the left:
$\Phi'(t)=\big[A\mathbf{x}_1(t),\:A\mathbf{x}_2(t)\big] = A\big[\mathbf{x}_1(t),\:\mathbf{x}_2(t)\big]$.
Therefore, $\textrm{L6}$ follows from $\textrm{L5}$ by factoring out $A$ on the left.
- Finally, we consider lines $\textrm{L6}$ and $\textrm{L7}$. From $\textrm{L6}$ we have $\Phi'(t)=A\big[\mathbf{x}_1(t),\:\mathbf{x}_2(t)\big]$. Using $\Phi(t)=\big[\mathbf{x}_1(t),\:\mathbf{x}_2(t)\big]$ from $\textrm{L1}$, we substitute to obtain $\Phi'(t)=A\Phi(t)$. Thus, $\textrm{L7}$ follows from $\textrm{L6}$ by substitution from $L1$.

---

**Question 3**

Consider the system $x^{′}(t) = Ax(t)$, where $A$ is a constant $2 \cdot 2$ matrix. Let $Φ(t)$ be a fundamental matrix for this system. A derivation showing that $Φ(t)$ is invertible for all $t$ is given below.

> $L1$: Let $x_{1}(t)$ and $x_{2}(t)$ be the columns of $Φ(t)$
> $L2$: $x_{1}(t)$ and $x_{2}(t)$ are linearly independent for all $t$
> $L3$: $det(Φ(t)) \ne 0$ for all $t$
> $L4$: $Φ(t)$ is invertible for all $t$

Select the correct options in the following reasoning.

Line $L2$ follows from $L1$ by the $\underline{\hspace{2cm}}$.

Line $L3$ follows from line $L2$ by $\underline{\hspace{2cm}}$.

Line $L4$ follows from line $\underline{\hspace{2cm}}$ by the properties of invertible matrices.

#### Select 1

- [ ] definition of a fundamental matrix
- [ ] properties of diagonalizable matrices
- [ ] properties of invertible matrices
- [ ] definition of matrix multiplication

#### Select 2

- [ ] \Phi$
- [ ] the zero factor theorem
- [ ] integrating the given system
- [ ] substituting $\Phi' = A \
- [ ] the properties of determinants

#### Select 3

- [ ] [MATH: L3]
- [ ] [MATH: L2]
- [ ] [MATH: L4]
- [ ] [MATH: L1]

---

**Question 4**

Consider the system $x^{′}(t) = Ax(t)$, where $A$ is a constant $2 \cdot 2$ matrix. Let $Φ(t)$ be a fundamental matrix for this system. A derivation showing that every solution $x(t)$ can be written in the form $x(t) = Φ(t)c$ for some constant vector $c = [c_{1}, c_{2}]^{T}$ is given below.

> $L1$: Let $x_{1}(t)$ and $x_{2}(t)$ be the columns of $Φ(t)$
> $L2$: $x_{1}(t)$ and $x_{2}(t)$ form a basis of solutions
> $L3$: $x(t)$ can be written as $x(t) = c_{1}x_{1}(t) + c_{2}x_{2}(t)$
> $L4$: $Φ(t)c = c_{1}x_{1}(t) + c_{2}x_{2}(t)$
> $L5$: $x(t) = Φ(t)c$

Select the correct options in the following reasoning.

Line $L2$ follows from $L1$ by the $\underline{\hspace{2cm}}$.

Line $L3$ follows from line $L2$ by $\underline{\hspace{2cm}}$.

Line $L4$ follows from line $L1$ by the $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] definition of a fundamental matrix
- [ ] properties of diagonalizable matrices
- [ ] properties of invertible matrices
- [ ] definition of matrix multiplication

#### Select 2

- [ ] the definition of a basis
- [ ] adding the vectors [MATH: x_{1}(t)] and [MATH: x_{2}(t)]
- [ ] integrating the given system
- [ ] multiplying [MATH: Φ(t)] with it's inverse

#### Select 3

- [ ] definition of matrix multiplication
- [ ] properties of invertible matrices
- [ ] definition of a fundamental matrix
- [ ] fact that [MATH: Φ] is nonsingular

---

<a id="fundamental-matrices-and-matrix-exponentials"></a>
## Fundamental Matrices and Matrix Exponentials

For a linear system

$$
\mathbf{x}' = A\mathbf{x}
$$

with constant $n\times n$ matrix $A$, the **standard fundamental matrix** is the unique fundamental matrix $\Phi_0(t)$ that satisfies the initial condition

$$
\Phi_0(0)=I_n
$$

where $I_n$ is the $n \times n$ identity matrix.

For constant matrices $A$, the standard fundamental matrix is given by

$$
\Phi_0(t)=e^{At}
$$

This matrix satisfies

$$
\Phi_0'(t)=A\,\Phi_0(t) \qquad\text{and}\qquad \Phi_0(0)=I_n
$$

**Note:** Any other fundamental matrix $\Phi(t)$ for the same system can be written in the form

$$
\Phi(t)=\Phi_0(t)\,C
$$

where $C$ is an invertible constant matrix.

In the next slide, we will compute the standard fundamental matrix for a specific example.

---

<a id="computing-fundamental-matrices-using-matrix-exponentials"></a>
## Computing Fundamental Matrices Using Matrix Exponentials

**Example:** $\begin{bmatrix}0 & -7 \\ 7 & 0\end{bmatrix}$
Consider the system of linear ODEs $\mathbf{x}' = A\mathbf{x}$ with the matrix $A$ shown above. Compute the **standard** fundamental matrix $\Phi(t)$ for this system.

**Explanation**

Suppose $A$ is a constant $n\times n$ matrix and $\mathbf x(t)$ is an $n\times 1$ column vector. The standard fundamental matrix of the linear system

$$
\mathbf x'(t) = A\mathbf x(t)
$$

denoted $\Phi_0(t)$, is the unique matrix solution of the initial value problem

$$
\Phi_0'(t) = A\Phi_0(t), \quad \Phi_0(0) = I_n
$$

where $I_n$ is the identity matrix.

Furthermore, it can be shown that

$$
\Phi_0(t) = e^{At}
$$

We have a square matrix of dimension $n=2$. Thus,

$$
\begin{aligned}
e^{At} &= α_{1}At + α_{0}I \\
&= α_{1}\begin{bmatrix}0 & -7 \\ 7 & 0\end{bmatrix}t + α_{0}\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \\
&= \begin{bmatrix}α_{0} & -7α_{1}t \\ 7α_{1}t & α_{0}\end{bmatrix}
\end{aligned}
$$

where $\alpha_1$ and $\alpha_0$ are constants to be determined.

Consider the polynomial

$$
r(\lambda) = \alpha_1 \lambda + \alpha_0
$$

The eigenvalues of $At$ are

$$
\lambda_1 = 7\textrm{i}t, \qquad \lambda_2 = -7\textrm{i}t
$$

which are complex conjugates. Thus, we have the following:

$$
\begin{aligned}
r((7\textrm{i})t) &= e^{(7\textrm{i})t} \\
α_{1}(7\textrm{i})t + α_{0} &= \cos (7t) + \textrm{i}\sin (7t) \\
α_{0} + 7\textrm{i}α_{1}t &= \cos (7t) + \textrm{i}\sin (7t)
\end{aligned}
$$

Now, we equate the real and imaginary parts, and solve for $\alpha_1$ and $\alpha_0{:}$

$$
{α_{0} = \cos (7t); 7α_{1}t = \sin (7t)⇒{α_{0} = \cos (7t); α_{1} = \frac{1}{7t}\sin (7t)
$$

Substituting $\alpha_1$ and $\alpha_0$ into the expressions for the required entries of $e^{At}$, we get the following:

$$
\begin{aligned}
α_{0} &= \cos (7t) \\
7α_{1}t &= 7 \cdot \frac{1}{7t}\sin (7t) \cdot t \\
&= \sin (7t)
\end{aligned}
$$

Therefore,
$\begin{bmatrix}\cos (7t) & -\sin (7t) \\ \sin (7t) & \cos (7t)\end{bmatrix}$

---

**Question 5**

$A = \begin{bmatrix}2 & 1 \\ 0 & 2\end{bmatrix}$
Consider the system of linear ODEs $x^{′} = Ax$ with the matrix $A$ shown above. Compute the **standard** fundamental matrix $Φ_{0}(t)$ for this system.

| $Φ_{0}(t) =$ |  | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |  |
| --- | ---: | ---: | ---: | ---: |
|  | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |  |  |

---

**Question 6**

$A = \begin{bmatrix}0 & 2 \\ -2 & 0\end{bmatrix}$
Consider the system of linear ODEs $x^{′} = Ax$ with the matrix $A$ shown above. Compute the **standard** fundamental matrix $Φ_{0}(t)$ for this system.

| $Φ_{0}(t) =$ |  | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |  |
| --- | ---: | ---: | ---: | ---: |
|  | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |  |  |

```update-progress
```

[[MA/DEQ/Home|Home]]
[[MA/DEQ/0. Table of Contents/TOC|Table of Contents]]
