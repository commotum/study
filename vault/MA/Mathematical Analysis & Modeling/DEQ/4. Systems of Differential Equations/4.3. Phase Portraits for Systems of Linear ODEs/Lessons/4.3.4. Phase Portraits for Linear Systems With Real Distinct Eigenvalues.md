# Phase Portraits for Linear Systems With Real Distinct Eigenvalues

<!--
lesson-id: 3239
topic-code: DEQ.4.3.4
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example: One Positive and One Negative Eigenvalue](#a-worked-example-one-positive-and-one-negative-eigenvalue)
- [A Worked Example: Both Positive Eigenvalues](#a-worked-example-both-positive-eigenvalues)
- [Sketching the Phase Portrait of a System](#sketching-the-phase-portrait-of-a-system)
- [Identifying the Parameters of a System Given Its Phase Portrait](#identifying-the-parameters-of-a-system-given-its-phase-portrait)
- [Classification of Equilibria](#classification-of-equilibria)
- [Classifying Systems of Linear Differential Equations With Distinct Real Eigenvalues](#classifying-systems-of-linear-differential-equations-with-distinct-real-eigenvalues)

## Prerequisites

- [Solving Homogeneous Systems of ODEs With Distinct Eigenvalues](<../../4.2. Solving Systems of Linear ODEs/Lessons/4.2.2. Solving Homogeneous Systems of ODEs With Distinct Eigenvalues.md>)
- [Phase Portraits for Decoupled Linear Systems](<4.3.3. Phase Portraits for Decoupled Linear Systems.md>)

---

<a id="introduction"></a>
## Introduction

In this topic, we'll study *phase portraits* of linear systems
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$
in the case where the $2 \times 2$ matrix $A$ has two *real, distinct* eigenvalues $\lambda_1$ and $\lambda_2$ with corresponding eigenvectors $\mathbf{v}_1$ and $\mathbf{v}_2$.

If $\mathbf{v}$ is an eigenvector corresponding to an eigenvalue $\lambda$, then

$$
\mathbf{x}(t)=c\,e^{\lambda t}\mathbf{v}
$$

gives us a solution that represents a line through the origin parallel to $\mathbf{v}$. So, when $\lambda_1$ and $\lambda_2$ are real and distinct, we always have two special straight-line solutions:

$$
\mathbf{x}(t)=c_1 e^{\lambda_1 t}\mathbf{v}_1 \qquad\text{and}\qquad \mathbf{x}(t)=c_2 e^{\lambda_2 t}\mathbf{v}_2
$$

Along the line parallel to the eigenvector $\mathbf{v}$:

- If $\lambda<0$, then $e^{\lambda t}\to 0$ as $t\to\infty$, so trajectories move *toward* the origin (*stable line*).
- If $\lambda>0$, then $e^{\lambda t}\to\infty$ as $t\to\infty$, so trajectories move *away* from the origin (*unstable line*).

Usually, to sketch the phase portrait, we proceed as follows:

1. Draw the two eigenvector lines through the origin (one line parallel to the eigenvector $\mathbf{v}_1$ and another parallel to the eigenvector $\mathbf{v}_2$).
2. Add arrows on each eigenvector line:toward the origin if its eigenvalue is negative, oraway from the origin if its eigenvalue is positive.
3. Add a few curved trajectories that are generally "pulled" toward the stable direction(s) and "pushed" along unstable direction(s).

Let's see some concrete examples.

---

<a id="a-worked-example-one-positive-and-one-negative-eigenvalue"></a>
## A Worked Example: One Positive and One Negative Eigenvalue

Sketch the phase portrait of the system of linear differential equations

$$
{x^{′}(t) =-2x(t) + 4y(t); y^{′}(t) = 2x(t)
$$

given that the eigenvalues of the system's matrix are

$$
\lambda_1=-4
$$

and

$$
\lambda_2=2
$$

for which the corresponding eigenvectors are $\begin{bmatrix}2 \\ -1\end{bmatrix}$ and $\begin{bmatrix}1 \\ 1\end{bmatrix}$ respectively.

Recall that the general solution of the system is
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$

The line that passes through the origin and is parallel to $\begin{bmatrix}2 \\ -1\end{bmatrix}$ has the equation

$$
y =-\dfrac{1}{2}x
$$

and the straight-line solutions of the form
$\begin{bmatrix}2 \\ -1\end{bmatrix}$
lie on that line.

Since $e^{-4t} \to 0$ as $t \to \infty$, these solutions approach the origin as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/58a406c11ea4b94b91de95f72b7a5c36.png>)

The line that passes through the origin and is parallel to $\begin{bmatrix}1 \\ 1\end{bmatrix}$ has the equation
$y= x$
and the straight-line solutions of the form
$\begin{bmatrix}1 \\ 1\end{bmatrix}$
lie on that line.

Since $e^{2t} \to \infty$ as $t \to \infty$, these solutions approach infinity as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/3c2b108dd9d62b1bef4f2114ff323e68.png>)

The phase portrait of our system looks as follows:

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/37095c1730913cedea015bcf8f0cf711.png>)

---

<a id="a-worked-example-both-positive-eigenvalues"></a>
## A Worked Example: Both Positive Eigenvalues

Let's sketch the phase portrait of the system of linear differential equations

$$
{x^{′}(t) = 3x(t) + y(t); y^{′}(t) = 2x(t) + 4y(t)
$$

given that the eigenvalues of the system's matrix are $\lambda_1=5$ and

$$
\lambda_2=2
$$

for which the corresponding eigenvectors are $\begin{bmatrix}1 \\ 2\end{bmatrix}$ and $\begin{bmatrix}1 \\ -1\end{bmatrix}$ respectively.

Recall that the general solution of the system is
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$

The line that passes through the origin and is parallel to $\begin{bmatrix}1 \\ 2\end{bmatrix}$ has the equation
$y= 2x$
and the straight-line solutions of the form
$\begin{bmatrix}1 \\ 2\end{bmatrix}$
lie on that line.

Since $e^{5t} \to \infty$ as $t \to \infty$, these solutions approach infinity as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/b6f0daffda33a636dbd326a3c2f66154.png>)

The line that passes through the origin and is parallel to $\begin{bmatrix}1 \\ -1\end{bmatrix}$ has the equation
$y= -x$
and the straight-line solutions of the form
$\begin{bmatrix}1 \\ -1\end{bmatrix}$
lie on that line.

Since $e^{2t} \to \infty$ as $t \to \infty$, these solutions approach infinity as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/75951c370cf253fa708cab4c45ff929e.png>)

For this particular system, we have
$0 < \lambda_2 < \lambda_1$
thus,

$$
\lambda_1 = 5
$$

is the dominant eigenvalue.

Since the dominant eigenvalue is

$$
\lambda_1=5
$$

solutions become parallel to the straight-line solution containing the vector $\mathbf{v}_1$ as $t \to \infty$.

The phase portrait of our system looks as follows:

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/7f708edfe4214a34bb8864af0e698ab4.png>)

---

<a id="sketching-the-phase-portrait-of-a-system"></a>
## Sketching the Phase Portrait of a System

**Example:** ${x^{′}(t) =-2x(t) + 2y(t); y^{′}(t) = x(t) - 3y(t)$
Sketch the phase portrait of the system of linear differential equations above given that the eigenvalues of the system's matrix are $\lambda_1=-1$ and $\lambda_2=-4$, for which the corresponding eigenvectors are $\mathbf{v}_1 = [2, \ 1]^T$ and $\mathbf{v}_2 = [-1, \ 1]^T$, respectively.

**Explanation**

The general solution of the system is
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$

The line that passes through the origin and is parallel to $\begin{bmatrix}2 \\ 1\end{bmatrix}$ has the equation

$$
y= \dfrac{1}{2}x
$$

and the straight-line solutions of the form
$\begin{bmatrix}2 \\ 1\end{bmatrix}$
lie on that line.

Since $e^{-t} \to 0$ as $t \to \infty$, these solutions approach the origin as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/a3ea75c2afeb553be2bd71c6717e1fa0.png>)

The line that passes through the origin and is parallel to $\begin{bmatrix}-1 \\ 1\end{bmatrix}$ has the equation
$y=-x$
and the straight-line solutions of the form
$\begin{bmatrix}-1 \\ 1\end{bmatrix}$
lie on that line.

Since $e^{-4t} \to 0$ as $t \to \infty$, these solutions approach the origin as $t \to \infty$.

Let's add the solutions to the phase space.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/cb8af2968caaacb5b676cea6316430ee.png>)

For this particular system, we have
$\lambda_2 < \lambda_1 < 0$
thus,

$$
\lambda_2 = -4
$$

is the dominant eigenvalue.

Since the dominant eigenvalue is

$$
\lambda_2 = -4
$$

solutions become parallel to the straight-line solution containing the vector $\mathbf{v}_2$ as they move away from the origin.

The phase portrait of our system looks as follows:

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/ccc3466bc4de9ad91d2f893096827841.png>)

---

**Question 1**

${x^{′}(t) = 2x(t) - 2y(t); y^{′}(t) = 2x(t) - 3y(t)$
Consider the system of linear differential equations above. The eigenvalues of the system's matrix are $λ_{1} =-2$ and $λ_{2} = 1$, for which the corresponding eigenvectors are $v_{1} = [1, 2]^{T}$ and $v_{2} = [2, 1]^{T}$, respectively. Select the correct options in the reasoning below.The general solution of the system is
$e^{-t}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: e^{− t}]
- [ ] [MATH: e^{t}]
- [ ] [MATH: e^{2t}]
- [ ] [MATH: e^{− 2t}]

#### Select 2

- [ ] [MATH: e^{− 2t}]
- [ ] [MATH: e^{− t}]
- [ ] [MATH: e^{2t}]
- [ ] [MATH: e^{t}]

---

**Question 2**

${x^{′}(t) = 3x(t) + 2y(t); y^{′}(t) = x(t) + 2y(t)$
Consider the system of linear differential equations above. The eigenvalues of the system's matrix are $λ_{1} = 4$ and $λ_{2} = 1$, for which the corresponding eigenvectors are $v_{1} = [2, 1]^{T}$ and $v_{2} = [- 1, 1]^{T}$, respectively. Select the correct options in the reasoning below.The general solution of the system is
$e^{-t}$ $\underline{\hspace{2cm}}$ $\underline{\hspace{2cm}}$

#### Select 1

- [ ] [MATH: e^{− t}]
- [ ] [MATH: e^{t}]
- [ ] [MATH: e^{− 4t}]
- [ ] [MATH: e^{4t}]

#### Select 2

- [ ] [MATH: e^{t}]
- [ ] [MATH: e^{− t}]
- [ ] [MATH: e^{4t}]
- [ ] [MATH: e^{− 4t}]

---

<a id="identifying-the-parameters-of-a-system-given-its-phase-portrait"></a>
## Identifying the Parameters of a System Given Its Phase Portrait

**Example:** The phase portrait for a system of linear differential equations whose matrix has two real, distinct eigenvalues is shown above.
The eigenvalues of the system's matrix are $\lambda_1$ and $\lambda_2$, for which the corresponding eigenvectors are $\mathbf{v}_1 = [-1, \ 1]^T$ and $\mathbf{v}_2 = [1, \ 0]^T$, respectively.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/7f0b26ad72c30f42ceeabc3bf1096678.png>)

Identify the relations between $\lambda_1$, $\lambda_2$, and $0$.

**Explanation**

The straight-line solutions of the system have the form
$\begin{bmatrix}-1 \\ 1\end{bmatrix}$

The line that passes through the origin and is parallel to $\begin{bmatrix}-1 \\ 1\end{bmatrix}$ has the equation
$y= -x$,
as shown below.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/3f9d87b005f31a0a928364a190e4d763.png>)

The tangents along this line are pointing away from the origin, meaning that the solutions of the form
$\begin{bmatrix}-1 \\ 1\end{bmatrix}$
must approach infinity as $t \to \infty$. So, we must have $\lambda_1 > 0$.

The line that passes through the origin and is parallel to $\begin{bmatrix}1 \\ 0\end{bmatrix}$ has the equation
$y= 0$,
as shown below.

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/a75b5bab1a5310e66dfd91a6e1bae33a.png>)

The tangents along this line are pointing away from the origin, meaning that the solutions of the form
$\begin{bmatrix}1 \\ 0\end{bmatrix}$
must approach infinity as $t \to \infty$. So, we must have $\lambda_2 > 0$.

So, for this particular system, both eigenvalues are positive. The dominant eigenvalue is the one with the greatest magnitude.

Now, since the solutions become parallel to the straight-line solution containing the vector $\mathbf{v}_2$ as they move away from the origin, the dominant eigenvalue is $\lambda_2$.

Therefore, $\lambda_2 > \lambda_1 > 0$.

---

**Question 3**

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/q-332479.png>)

The phase portrait for a system of linear differential equations whose matrix has two real, distinct eigenvalues is shown above.
The eigenvalues of the system's matrix are $λ_{1}$ and $λ_{2}$, for which the corresponding eigenvectors are $v_{1} = [1, 2]^{T}$ and $v_{2} = [2, 1]^{T}$, respectively.
Select the correct options in the reasoning below.The line that passes through the origin and is parallel to $v_{1} = \begin{bmatrix}1 \\ 2\end{bmatrix}$ has the equation
$-\frac{1}{2}x$ $\underline{\hspace{2cm}}$
The tangents along this line are pointing
$\underline{\hspace{2cm}}$,
meaning that the solutions of the form
$c_{1}e^{λ_{1}t}\begin{bmatrix}1 \\ 2\end{bmatrix}$
must approach
$\underline{\hspace{2cm}}$
as $t → ∞$. So, we must have
$\underline{\hspace{2cm}}$.

#### Select 1

- [ ] [MATH: − (1)/(2)x]
- [ ] [MATH: − x]
- [ ] [MATH: 2x]
- [ ] [MATH: (1)/(2)x]

#### Select 2

- [ ] toward the origin
- [ ] away from the origin

#### Select 3

- [ ] infinity
- [ ] the origin

#### Select 4

- [ ] [MATH: λ_{1} = 0]
- [ ] [MATH: λ_{1} < 0]
- [ ] [MATH: λ_{1} > 0]

---

**Question 4**

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/q-332506.png>)

The phase portrait for a system of linear differential equations whose matrix has two real, distinct eigenvalues is shown above.
The eigenvalues of the system's matrix are $λ_{1}$ and $λ_{2}$, for which the corresponding eigenvectors are $v_{1} = [2, 1]^{T}$ and $v_{2} = [- 1, 1]^{T}$, respectively.
Select the correct options in the reasoning below.The line that passes through the origin and is parallel to $v_{1} = \begin{bmatrix}2 \\ 1\end{bmatrix}$ has the equation
$-\frac{1}{2}x$ $\underline{\hspace{2cm}}$
The tangents along this line are pointing
$\underline{\hspace{2cm}}$,
meaning that the solutions of the form
$c_{1}e^{λ_{1}t}\begin{bmatrix}2 \\ 1\end{bmatrix}$
must approach
$\underline{\hspace{2cm}}$
as $t → ∞$. So, we must have
$\underline{\hspace{2cm}}$.

#### Select 1

- [ ] [MATH: − (1)/(2)x]
- [ ] [MATH: (1)/(2)x]
- [ ] [MATH: 2x]
- [ ] [MATH: − x]

#### Select 2

- [ ] away from the origin
- [ ] toward the origin

#### Select 3

- [ ] infinity
- [ ] the origin

#### Select 4

- [ ] [MATH: λ_{1} = 0]
- [ ] [MATH: λ_{1} < 0]
- [ ] [MATH: λ_{1} > 0]

---

<a id="classification-of-equilibria"></a>
## Classification of Equilibria

Finally, let’s summarize what we’ve learned so far.

Again, we are considering a linear system
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$
in the case where the $2 \times 2$ matrix $A$ has two *real, distinct* eigenvalues $\lambda_1$ and $\lambda_2$ with corresponding eigenvectors $\mathbf{v}_1$ and $\mathbf{v}_2$.

Recall that if

$$
\lambda_1, \lambda_2 \neq 0
$$

the system has a single equilibrium point at the origin $(0,0)$.
To classify this equilibrium, we look at the signs of $\lambda_1$ and $\lambda_2$.

First, we describe the straight-line solutions:

- Along the line parallel to $\mathbf{v}_1$, solutions have the form $\mathbf{x}(t)=c_1e^{\lambda_1 t}\mathbf{v}_1$ where $c_1 \in \mathbb{R}$.If $\lambda_1<0$, it's a *stable* line (direction).If $\lambda_1>0$, it's an *unstable* line (direction).
- Along the line parallel to $\mathbf{v}_2$, solutions have the form $\mathbf{x}(t)=c_2e^{\lambda_2 t}\mathbf{v}_2$ where $c_2 \in \mathbb{R}$.If $\lambda_2<0$, it's a *stable* line (direction).If $\lambda_2>0$, it's an *unstable* line (direction).

As for the classification of the equilibrium at the origin, we get the following:

- *Unstable node (source)* if $\lambda_1>0$ and $\lambda_2>0$.
- *Stable node (sink)* if $\lambda_1<0$ and $\lambda_2<0$. In fact, it's asymptotically stable, as the trajectories converge to the origin.
- *Saddle point* if $\lambda_1\lambda_2<0$ (one positive, one negative).

Let's apply this classification to concrete examples.

---

<a id="classifying-systems-of-linear-differential-equations-with-distinct-real-eigenvalues"></a>
## Classifying Systems of Linear Differential Equations With Distinct Real Eigenvalues

**Example:** ${x^{′}(t) = 9x(t) + 6y(t); y^{′}(t) =-6x(t) - 6y(t)$
Consider the system of linear differential equations above. The eigenvalues of the system's matrix are $\lambda_1=6$ and $\lambda_2=-3$, for which the corresponding eigenvectors are $\mathbf{v}_1 = [-2, \ 1]^T$ and $\mathbf{v}_2 = [-1, \ 2]^T$, respectively. Complete the following statements about the system by filling in the blanks.

Identify stable and unstable lines, and classify the equilibrium point (origin).

**Explanation**

The general solution of the system is
$\begin{bmatrix}x(t) \\ y(t)\end{bmatrix}$

- The line that passes through the origin and is parallel to $\begin{bmatrix}-2 \\ 1\end{bmatrix}$ has the equation
$y= -\dfrac12x$.
The straight-line solutions of the form
$\begin{bmatrix}-2 \\ 1\end{bmatrix}$
lie on that line and approach infinity as $t \to \infty$. So, $y=-\dfrac12x$ is an unstable line.
- The line that passes through the origin and is parallel to $\begin{bmatrix}-1 \\ 2\end{bmatrix}$ has the equation
$y=-2x$.
The straight-line solutions of the form
$\begin{bmatrix}-1 \\ 2\end{bmatrix}$
lie on that line and approach the origin as $t \to \infty$. So, $y=-2x$ is a stable line.

Therefore, the equilibrium point (origin) is a saddle point.

**Note:** The phase portrait of the system looks as follows:

![](<../Source/4.3.4. Phase Portraits for Linear Systems With Real Distinct Eigenvalues/Images/0e06d98a83065e6228e2e9238e34f00e.png>)

---

**Question 5**

${x^{′}(t) = 3x(t) - y(t); y^{′}(t) = 4x(t) - 2y(t)$
Consider the system of linear differential equations above. The eigenvalues of the system's matrix are $λ_{1} =-1$ and $λ_{2} = 2$, for which the corresponding eigenvectors are $v_{1} = [1, 4]^{T}$ and $v_{2} = [1, 1]^{T}$, respectively. Select the correct options below regarding the given system.
$y = 4x$ is $\underline{\hspace{2cm}}$ line.

$\underline{\hspace{2cm}}$ is $\underline{\hspace{2cm}}$ line.

The equilibrium point (origin) is $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] an unstable
- [ ] a stable

#### Select 2

- [ ] [MATH: y = x]
- [ ] [MATH: y = − 2x]
- [ ] [MATH: y = − x]
- [ ] [MATH: y = 2x]

#### Select 3

- [ ] an unstable
- [ ] a stable

#### Select 4

- [ ] an unstable node (source)
- [ ] a stable node (sink)
- [ ] a saddle point

---

**Question 6**

${x^{′}(t) =-3x(t) - 2y(t); y^{′}(t) =-x(t) - 2y(t)$
Consider the system of linear differential equations above. The eigenvalues of the system's matrix are $λ_{1} =-4$ and $λ_{2} =-1$, for which the corresponding eigenvectors are $v_{1} = [2, 1]^{T}$ and $v_{2} = [- 1, 1]^{T}$, respectively. Select the correct options below regarding the given system.
$y = \frac{1}{2}x$ is $\underline{\hspace{2cm}}$ line.

$\underline{\hspace{2cm}}$ is $\underline{\hspace{2cm}}$ line.

The equilibrium point (origin) is $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] an unstable
- [ ] a stable

#### Select 2

- [ ] [MATH: y = − x]
- [ ] [MATH: y = x]
- [ ] [MATH: x = 0]
- [ ] [MATH: y = 2x]

#### Select 3

- [ ] a stable
- [ ] an unstable

#### Select 4

- [ ] an unstable node (source)
- [ ] a saddle point
- [ ] a stable node (sink)

```update-progress
```

[[MA/DEQ/Home|Home]]
[[MA/DEQ/0. Table of Contents/TOC|Table of Contents]]
