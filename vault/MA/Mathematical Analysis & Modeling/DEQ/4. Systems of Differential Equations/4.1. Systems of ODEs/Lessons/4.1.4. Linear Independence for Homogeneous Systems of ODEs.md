# Linear Independence for Homogeneous Systems of ODEs

<!--
lesson-id: 6374
topic-code: DEQ.4.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Wronskian of Two Vector-Valued Functions](#calculating-the-wronskian-of-two-vector-valued-functions)
- [Identifying Linearly Independent Vector-Valued Functions](#identifying-linearly-independent-vector-valued-functions)
- [Fundamental Sets of Solutions for Systems of Linear ODEs](#fundamental-sets-of-solutions-for-systems-of-linear-odes)
- [Identifying Fundamental Sets of Solutions to Systems of Linear ODEs](#identifying-fundamental-sets-of-solutions-to-systems-of-linear-odes)

## Prerequisites

- [Introduction to Systems of Linear ODEs](<4.1.1. Introduction to Systems of Linear ODEs.md>)
- [Linear Independence of Solutions to Homogeneous ODEs](<../../../3. Linear Differential Equations/3.1. Theory of Solutions to Linear ODEs/Lessons/3.1.6. Linear Independence of Solutions to Homogeneous ODEs.md>)

---

<a id="introduction"></a>
## Introduction

The **Wronskian** $W(\mathbf{x}_1,\mathbf{x}_2)$ of two vector-valued functions
$\begin{bmatrix}x_{11}(t) \\ x_{21}(t)\end{bmatrix}$
is defined as
$\begin{vmatrix}x_{11}(t) & x_{12}(t) \\ x_{21}(t) & x_{22}(t)\end{vmatrix}$

Analogous to scalar ordinary differential equations (ODEs), the Wronskian of vector-valued functions can be used to determine linear dependency. In particular:

> - If the Wronskian is not identically zero, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are *linearly independent*.
> - If the Wronskian is identically zero, and it's known that $\mathbf{x}_1$ and $\mathbf{x}_2$ are solutions to the same system of linear ODEs, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are *linearly dependent*.

Let's see some concrete examples.

---

<a id="calculating-the-wronskian-of-two-vector-valued-functions"></a>
## Calculating the Wronskian of Two Vector-Valued Functions

**Example:** $\begin{bmatrix}t^{3} \\ 2\end{bmatrix}$
Find the Wronskian $W(\mathbf{x}_1, \mathbf{x}_2)$ of the above vector-valued functions.

**Explanation**

The Wronskian $W(\mathbf{x}_1,\mathbf{x}_2)$ of two vector-valued functions
$\begin{bmatrix}x_{11}(t) \\ x_{21}(t)\end{bmatrix}$
is defined as
$\begin{vmatrix}x_{11}(t) & x_{12}(t) \\ x_{21}(t) & x_{22}(t)\end{vmatrix}$

Therefore, our Wronskian is

$$
\begin{aligned}
W(x_{1}, x_{2}) &= \begin{vmatrix}t^{3} & t^{3} - 5 \\ 2 & 4\end{vmatrix} \\
&= t^{3} \cdot 4 - (t^{3} - 5) \cdot 2 \\
&= 4t^{3} - 2t^{3} + 10 \\
&= 2t^{3} + 10
\end{aligned}
$$

---

**Question 1**

$x_{1}(t) = \begin{bmatrix}e^{t} \\ 0\end{bmatrix}, x_{2}(t) = \begin{bmatrix}0 \\ e^{-t}\end{bmatrix}$
The Wronskian of the vector-valued functions given above is $W(x_{1}, x_{2}) =$ $\underline{\hspace{1.5cm}}$.

---

**Question 2**

$x_{1}(t) = \begin{bmatrix}\cos (3t) \\ -\sin (3t)\end{bmatrix}, x_{2}(t) = \begin{bmatrix}\sin (3t) \\ \cos (3t)\end{bmatrix}$
The Wronskian of the vector-valued functions given above is $W(x_{1}, x_{2}) =$ $\underline{\hspace{1.5cm}}$.

---

<a id="identifying-linearly-independent-vector-valued-functions"></a>
## Identifying Linearly Independent Vector-Valued Functions

**Example:** $\begin{bmatrix}-4e^{2t} \\ e^{2t}\end{bmatrix}$
Consider the vector-valued functions $\mathbf{x}_1(t)$ and $\mathbf{x}_2(t)$ above. Given that $\mathbf x_1$ and $\mathbf x_2$ are solutions of the same system of linear ODEs, compute the Wronskian is $W(\mathbf{x}_1, \mathbf{x}_2)$ and determine if the set $\left\{\mathbf{x}_1(t), \mathbf{x}_2(t) \right\}$ is linearly dependent on $\mathbb R$.

**Explanation**

The Wronskian $W(\mathbf{x}_1,\mathbf{x}_2)$ of two vector-valued functions
$\begin{bmatrix}x_{11}(t) \\ x_{21}(t)\end{bmatrix}$
is defined as
$\begin{vmatrix}x_{11}(t) & x_{12}(t) \\ x_{21}(t) & x_{22}(t)\end{vmatrix}$

Note the following:

- If the Wronskian is not identically zero, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are linearly independent.
- If the Wronskian is identically zero, and it's known that $\mathbf{x}_1$ and $\mathbf{x}_2$ are solutions to the same system of linear ODEs, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are linearly dependent.

In our case, our Wronskian is given by

$$
\begin{aligned}
W(x_{1}, x_{2}) &= \begin{vmatrix}-4e^{2t} & e^{t} \\ e^{2t} & 2e^{t}\end{vmatrix} \\
&= (-4e^{2t}) \cdot (2e^{t}) - e^{t} \cdot (e^{2t}) \\
&=-8e^{3t} - e^{3t} \\
&=-9e^{3t}
\end{aligned}
$$

which is nonzero for at least one $t\in \mathbb R$.

Therefore, the set

$$
\left\{\mathbf{x}_1(t), \mathbf{x}_2(t) \right\}
$$

is linearly independent on $\mathbb R$.

---

**Question 3**

$x_{1}(t) = \begin{bmatrix}3e^{t} \\ -6e^{t}\end{bmatrix}, x_{2}(t) = \begin{bmatrix}e^{t} \\ -2e^{t}\end{bmatrix}, t ∈ R$
Consider the vector-valued functions $x_{1}(t)$ and $x_{2}(t)$ above. Given that $x_{1}$ and $x_{2}$ are solutions of the same system of linear ODEs, select the correct options in the reasoning below. The Wronskian is $W(x_{1}, x_{2}) =$
$\underline{\hspace{2cm}}$,
which is
$\underline{\hspace{2cm}}$.

Therefore, the set ${x_{1}(t), x_{2}(t)}$ is linearly
$\underline{\hspace{2cm}}$
on $R$.

#### Select 1

- [ ] [MATH: e^{2t}]
- [ ] [MATH: 6e^{2t}]
- [ ] [MATH: 0]
- [ ] [MATH: − 6e^{2t}]

#### Select 2

- [ ] zero for all [MATH: t ∈ R]
- [ ] nonzero for at least one [MATH: t ∈ R]

#### Select 3

- [ ] independent
- [ ] dependent

---

**Question 4**

$x_{1}(t) = \begin{bmatrix}\cos t \\ \sin t\end{bmatrix}, x_{2}(t) = \begin{bmatrix}-\sin t \\ \cos t\end{bmatrix}, t ∈ R$
Consider the vector-valued functions $x_{1}(t)$ and $x_{2}(t)$ above. Given that $x_{1}$ and $x_{2}$ are solutions of the same system of linear ODEs, select the correct options in the reasoning below. The Wronskian is $W(x_{1}, x_{2}) =$
$\underline{\hspace{2cm}}$,

which is
$\underline{\hspace{2cm}}$.

Therefore, the set ${x_{1}(t), x_{2}(t)}$ is linearly
$\underline{\hspace{2cm}}$
on $R$.

#### Select 1

- [ ] [MATH: 0]
- [ ] [MATH: − 1]
- [ ] [MATH: \cos (2t)]
- [ ] [MATH: 1]

#### Select 2

- [ ] nonzero for at least one [MATH: t ∈ R]
- [ ] zero for all [MATH: t ∈ R]

#### Select 3

- [ ] independent
- [ ] dependent

---

<a id="fundamental-sets-of-solutions-for-systems-of-linear-odes"></a>
## Fundamental Sets of Solutions for Systems of Linear ODEs

A pair of solutions $\mathbf{x}_1(t)$ and $\mathbf{x}_2(t)$ to a $2\times 2$ homogeneous linear system

$$
\mathbf{x}'(t)=A\mathbf{x}(t)
$$

is called a **fundamental set of solutions** on an interval if the solutions are linearly independent on that interval.

A practical test for determining whether a pair of functions forms a fundamental set of solutions is to use the Wronskian:

> If $\mathbf{x}_1$ and $\mathbf{x}_2$ are solutions to the system and
> $W(\mathbf{x}_1,\mathbf{x}_2) \neq 0$
> for at least one $t_0$ in the interval, then they form a fundamental set of solutions on that interval.

This definition generalizes to any $n\times n$ homogeneous linear system

$$
\mathbf{x}'(t)=A\mathbf{x}(t)
$$

A set of solutions
$\mathbf{x}_1(t),\ \mathbf{x}_2(t),\ \ldots,\ \mathbf{x}_n(t)$
is a *fundamental set of solutions* on an interval if the solutions are linearly independent on that interval.

The same Wronskian idea applies: compute the determinant of the $n\times n$ matrix whose columns are $\mathbf{x}_1(t),\ldots,\mathbf{x}_n(t)$. If it is nonzero at some $t_0$ in the interval, then the solutions form a fundamental set on that interval.

Next, we will look at a concrete example applying this test.

---

<a id="identifying-fundamental-sets-of-solutions-to-systems-of-linear-odes"></a>
## Identifying Fundamental Sets of Solutions to Systems of Linear ODEs

**Example:** ${x_{1}^{′}(t) = 2x_{1}(t) + x_{2}(t); x_{2}^{′}(t) =-4x_{1}(t) - 2x_{2}(t)$.
Consider the system of linear ODEs above, where $t\in\mathbb R$. Which of the following statements are true?

1. The function $\begin{bmatrix}t^{2} + 1 \\ -4t\end{bmatrix}$ is a solution of the system.
2. The function $\begin{bmatrix}2t + 1 \\ -4t\end{bmatrix}$ is a solution of the system.
3. The function $\begin{bmatrix}t \\ 1 - 2t\end{bmatrix}$ is a solution of the system.

Also, determine which pair of solutions forms a set of fundamental solutions to the system.

**Explanation**

The Wronskian $W(\mathbf{x}_1,\mathbf{x}_2)$ of two vector-valued functions
$\begin{bmatrix}x_{11}(t) \\ x_{21}(t)\end{bmatrix}$
is defined as
$\begin{vmatrix}x_{11}(t) & x_{12}(t) \\ x_{21}(t) & x_{22}(t)\end{vmatrix}$

Note the following:

- If the Wronskian is not identically zero, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are linearly independent.
- If the Wronskian is identically zero, and it's known that $\mathbf{x}_1$ and $\mathbf{x}_2$ are solutions to the same system of linear ODEs, then $\mathbf{x}_1$ and $\mathbf{x}_2$ are linearly dependent.

First, we write our system in matrix form:

$$
\begin{bmatrix}2 & 1 \\ -4 & -2\end{bmatrix}
$$

Now, let's substitute the given vectors into the matrix differential equation:

- $\begin{bmatrix}t^{2} + 1 \\ -4t\end{bmatrix}$ $\text{isn't}$ a solution of the system. Indeed:
$\begin{bmatrix}(t^{2} + 1)^{′} \\ (-4t)^{′}\end{bmatrix}|= \begin{bmatrix}2 & 1 \\ -4 & -2\end{bmatrix}\begin{bmatrix}t^{2} + 1 \\ -4t\end{bmatrix}; \begin{bmatrix}2t \\ -4\end{bmatrix}|= \begin{bmatrix}2 \cdot (t^{2} + 1) + 1 \cdot (-4t) \\ -4 \cdot (t^{2} + 1) + (-2) \cdot (-4t)\end{bmatrix}; \begin{bmatrix}2t \\ -4\end{bmatrix}|= \begin{bmatrix}2t^{2} - 4t + 2 \\ -4t^{2} + 8t - 4\end{bmatrix}\cdot$

- $\begin{bmatrix}2t + 1 \\ -4t\end{bmatrix}$ $\text{is}$ a solution of the system. Indeed:
$\begin{bmatrix}(2t + 1)^{′} \\ (-4t)^{′}\end{bmatrix}|= \begin{bmatrix}2 & 1 \\ -4 & -2\end{bmatrix}\begin{bmatrix}2t + 1 \\ -4t\end{bmatrix}; \begin{bmatrix}2 \\ -4\end{bmatrix}|= \begin{bmatrix}2 \cdot (2t + 1) + 1 \cdot (-4t) \\ -4 \cdot (2t + 1) + (-2) \cdot (-4t)\end{bmatrix}; \begin{bmatrix}2 \\ -4\end{bmatrix}|= \begin{bmatrix}2 \\ -4\end{bmatrix}✓$
- $\begin{bmatrix}t \\ 1 - 2t\end{bmatrix}$ $\text{is}$ a solution of the system. Indeed:
$\begin{bmatrix}(t)^{′} \\ (1 - 2t)^{′}\end{bmatrix}|= \begin{bmatrix}2 & 1 \\ -4 & -2\end{bmatrix}\begin{bmatrix}t \\ 1 - 2t\end{bmatrix}; \begin{bmatrix}1 \\ -2\end{bmatrix}|= \begin{bmatrix}2 \cdot t + 1 \cdot (1 - 2t) \\ -4 \cdot t + (-2) \cdot (1 - 2t)\end{bmatrix}; \begin{bmatrix}1 \\ -2\end{bmatrix}|= \begin{bmatrix}1 \\ -2\end{bmatrix}✓$

Now, consider the set of solutions $\big\{\mathbf{b},\mathbf{c} \big\}$. Its Wronskian is

$$
\begin{aligned}
W(b, c) &= \begin{vmatrix}2t + 1 & t \\ -4t & 1 - 2t\end{vmatrix} \\
&= (2t + 1) \cdot (1 - 2t) - t \cdot (-4t) \\
&= (2t + 1)(1 - 2t) + 4t^{2} \\
&= 1
\end{aligned}
$$

which is nonzero for at least one $t \in \mathbb R$. Thus, since $\mathbf b$ and $\mathbf c$ are both solutions of the same linear system, and their Wronskian is nonzero for at least one $t\in\mathbb R$, $\big\{\mathbf{b},\mathbf{c} \big\}$ is a set of fundamental solutions of the system.

---

**Question 5**

${x_{1}^{′}(t) =-6x_{1}(t) + 7x_{2}(t); x_{2}^{′}(t) =-2x_{1}(t) + 3x_{2}(t)$.
Consider the system of linear ODEs above, where $t ∈ R$. Select the correct options in the reasoning below. The function $a(t) = \begin{bmatrix}7e^{-4t} \\ 2e^{-4t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

The function $b(t) = \begin{bmatrix}e^{t} \\ -2e^{t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

The function $c(t) = \begin{bmatrix}e^{t} \\ e^{t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

#### Select 1

- [ ] isn't
- [ ] is

#### Select 2

- [ ] is
- [ ] isn't

#### Select 3

- [ ] is
- [ ] isn't

---

**Question 6**

${x_{1}^{′}(t) = 2x_{1}(t) + x_{2}(t); x_{2}^{′}(t) = x_{1}(t) + 2x_{2}(t)$.
Consider the system of linear ODEs above, where $t ∈ R$. Select the correct options in the reasoning below. The function $a(t) = \begin{bmatrix}e^{3t} \\ e^{3t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

The function $b(t) = \begin{bmatrix}e^{t} \\ e^{t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

The function $c(t) = \begin{bmatrix}5e^{3t} \\ 5e^{3t}\end{bmatrix}$
$\underline{\hspace{2cm}}$
a solution of the system.

#### Select 1

- [ ] isn't
- [ ] is

#### Select 2

- [ ] is
- [ ] isn't

#### Select 3

- [ ] isn't
- [ ] is

```update-progress
```

[[MA/DEQ/Home|Home]]
[[MA/DEQ/0. Table of Contents/TOC|Table of Contents]]
