# The Image of a Linear Transformation in the Cartesian Plane

<!--
lesson-id: 3645
topic-code: LAL.4.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Identitying the Type of the Image of a Linear Transformation](#identitying-the-type-of-the-image-of-a-linear-transformation)
- [Determining the One-Dimensional Image of a Linear Transformation in the Cartesian Plane](#determining-the-one-dimensional-image-of-a-linear-transformation-in-the-cartesian-plane)
- [Identifying True Statements Regarding Linear Transformations in the Cartesian Plane](#identifying-true-statements-regarding-linear-transformations-in-the-cartesian-plane)

## Prerequisites

- [Singular Linear Transformations in the Plane](<../../../../PCL/6. Matrices/6.5. Linear Transformations/Lessons/6.5.12. Singular Linear Transformations in the Plane.md>)
- [The Kernel of a Linear Transformation](<MA/LAL/4. Linear Transformations/4.1. Linear Transformations/Lessons/4.1.2. The Kernel of a Linear Transformation.md>)
- [The Image and Rank of a Linear Transformation](<MA/LAL/4. Linear Transformations/4.1. Linear Transformations/Lessons/4.1.3. The Image and Rank of a Linear Transformation.md>)
- [The Rank-Nullity Theorem](<../../../3. Vector Spaces/3.4. Dimension and Rank in N-Dimensional Space/Lessons/3.4.5. The Rank-Nullity Theorem.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we have the following linear transformation:

$$
\mathbf{T}: \mathbb{R}^2 \to \mathbb{R}^2
$$

The image of $\mathbf{T}$ is a subspace of $\mathbb{R}^2$. Therefore, it can only be one of the following:

- the whole of $\mathbb{R}^2$,
- a line through the origin, or
- a single point (vector), namely the origin $(0,0)$.

Let's denote the standard matrix of $\mathbf{T}$ by $T$. It turns out that the dimension of the image of $\mathbf{T}$ depends on $\text{rank}(T)$ (or equivalently on $\text{nullity}(T)$ by the rank-nullity theorem). Indeed, we have the following:

- If $\textrm{rank}(T)=2$, then the two columns of $T$ are linearly independent. This means $T$ is invertible and therefore, $\textrm{Im}(\mathbf{T}) = \mathbb{R}^2$.For example, the following transformation maps $\mathbb{R}^2$ onto $\mathbb{R}^2$:
$\begin{bmatrix}2 & 1 \\ 1 & 2\end{bmatrix}$

![](<../Source/4.1.4. The Image of a Linear Transformation in the Cartesian Plane/Images/205e06869b3bb6040fef45b4d4862146.png>)

- If $\textrm{rank}(T)=1$, then there is a nonzero vector that generates the image of $\mathbf{T}$. In this case, the image of $\mathbf{T}$ is a one-dimensional subspace of $\Bbb R^2$. So, it is a line passing through the origin.For example, the following transformation maps $\mathbb{R}^2$ onto the line $y=2x$:
$\begin{bmatrix}2 & 1 \\ 4 & 2\end{bmatrix}$
Notice that the points $(2,4)$ and $(1,2)$, defined by the columns of $T$, lie on the line $y=2x$.

![](<../Source/4.1.4. The Image of a Linear Transformation in the Cartesian Plane/Images/b1465cde945eb2c94d34c18153d58781.png>)

- If $\textrm{rank}(T)=0$ then, according to the rank-nullity theorem,
$nullity(T)|= nullity(T); = 2 - rank(T); = 2 - 0; = 2$.
This means that $\mathbf{T}(\mathbf{v})=\mathbf{0}$ for all $\mathbf{v}\in \Bbb R^2$. So, in this case, we have that
$\begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}$
which maps $\mathbb{R}^2$ onto $\{(0,0)\}$, i.e., the set containing only the zero vector $\mathbf{0}$.

![](<../Source/4.1.4. The Image of a Linear Transformation in the Cartesian Plane/Images/8cdb6cd559083cbedb355df112874128.png>)

---

<a id="identitying-the-type-of-the-image-of-a-linear-transformation"></a>
## Identitying the Type of the Image of a Linear Transformation

**Example:** Consider the standard matrices of three linear transformations shown below. Which of these transformations map $\mathbb{R}^2$ onto a line?

$\begin{bmatrix}-2 & 2 \\ 1 & -1\end{bmatrix}$

**Explanation**

Given a linear transformation over $\mathbb{R}^2$ with the standard matrix $T$, we have the following:

- $T$ maps $\mathbb{R}^2$ onto $\mathbb{R}^2$ if and only if $\text{rank}(T)=2$, i.e., $T$ is nonsingular.
- $T$ maps $\mathbb{R}^2$ onto a line if and only if $\text{rank}(T)=1$.
- $T$ maps $\mathbb{R}^2$ onto $(0,0)$ if and only if $\text{rank}(T)=0$, i.e., $T$ is the zero matrix.

With that in mind, let's examine our matrices.

- $T_1$ maps $\mathbb{R}^2$ onto a line. Notice that the columns of $T_1$ are linearly dependent but it's not the zero matrix. So, $\text{rank}(T_1)=1$.
- $T_2$ maps $\mathbb{R}^2$ onto $\mathbb{R}^2$. Indeed, $T_2$ is a nonsingular matrix, i.e. $\text{rank}(T_2)=2$.
- $T_3$ maps $\mathbb{R}^2$ onto $(0,0)$ since $T_3$ is the zero matrix, i.e. $\text{rank}(T_3)=0$.

Therefore, the correct answer is "$T_1$ only."

---

**Question 1**

Consider the standard matrices of three linear transformations shown below. Which of the transformations map $R^{2}$ onto $(0, 0)$?

$T_{1} = \begin{bmatrix}1 & 0 \\ 1 & 0\end{bmatrix}, T_{2} = \begin{bmatrix}1 & -2 \\ -2 & 4\end{bmatrix}, T_{3} = \begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}$

- [ ] A. $T_{1}$, $T_{2}$, and $T_{3}$
- [ ] B. $T_{2}$ only
- [ ] C. $T_{1}$ and $T_{2}$ only
- [ ] D. $T_{1}$ only
- [ ] E. $T_{3}$ only

---

**Question 2**

Consider the standard matrices of three linear transformations shown below. Which of the transformations map $R^{2}$ onto the whole of $R^{2}$?

$T_{1} = \begin{bmatrix}1 & 0 \\ 2 & -1\end{bmatrix}, T_{2} = \begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}, T_{3} = \begin{bmatrix}1 & -1 \\ 1 & -1\end{bmatrix}$

- [ ] A. $T_{3}$ only
- [ ] B. $T_{2}$ only
- [ ] C. $T_{1}$ and $T_{3}$ only
- [ ] D. $T_{2}$ and $T_{3}$ only
- [ ] E. $T_{1}$ only

---

<a id="determining-the-one-dimensional-image-of-a-linear-transformation-in-the-cartesian-plane"></a>
## Determining the One-Dimensional Image of a Linear Transformation in the Cartesian Plane

**Example:** Consider the standard matrix $T$ of the linear transformation $\mathbf{T}$, given by
$\begin{bmatrix}6 & -9 \\ 2 & -3\end{bmatrix}$
Find the equation of the line that the transformation $\mathbf{T}$ maps $\mathbb{R}^2$ onto.

**Explanation**

Notice that $T$ has proportional rows, yet $T$ is not the zero matrix.

Therefore,

$$
\textrm{rank}(\mathbf{T})=1
$$

and $\mathbf{T}$ maps $\mathbb{R}^2$ onto a line that passes through the origin.

Moreover, the line must pass through the origin $(0,0)$ and the points $(6,2)$ and $(-9,-3)$, which are defined by the columns of $T$.

Computing the slope $m$ of this line, we get

$$
m = \dfrac{-3-2}{-9-6} = \dfrac13
$$

Therefore, $\mathbf{T}$ maps $\mathbb{R}^2$ onto the line

$$
y=\dfrac13x
$$

---

**Question 3:** Let
$T = \begin{bmatrix}1 & 2 \\ 3 & 6\end{bmatrix}$
be the standard matrix of the linear transformation $T$. Then $T$ maps $R^{2}$ onto

- [ ] A. $y = 3x$
- [ ] B. $(0, 0)$
- [ ] C. $R^{2}$
- [ ] D. $y = x$
- [ ] E. $y = \frac{1}{3}x$

---

**Question 4:** Let
$T = \begin{bmatrix}2 & 2 \\ 2 & 2\end{bmatrix}$
be the standard matrix of the linear transformation $T$. Then $T$ maps $R^{2}$ onto

- [ ] A. $y = \frac{1}{2}x$
- [ ] B. $R^{2}$
- [ ] C. $(0, 0)$
- [ ] D. $y =-2x$
- [ ] E. $y = x$

---

<a id="identifying-true-statements-regarding-linear-transformations-in-the-cartesian-plane"></a>
## Identifying True Statements Regarding Linear Transformations in the Cartesian Plane

**Example:** Consider a linear transformation $\mathbf{T}$ that maps $\mathbb{R}^2$ onto the line $y=-5x$. Which of the following statements are true?

1. $\langle -1, 5 \rangle \in \text{Im}(\mathbf{T})$
2. $\textrm{Ker}(\mathbf{T})=\{0\}$
3. $\textrm{Im}(\mathbf{T})=\Bbb R^2$

**Explanation**

Recall that when the image of a linear transformation $\mathbf{T}$ in $\mathbb{R}^2$ with the standard matrix $T$ is a one-dimensional line, then

$$
\textrm{rank}(T) = \textrm{rank}(\mathbf{T}) = 1
$$

and, according to the rank-nullity theorem,

$$
\begin{aligned}
nullity(T) = nullity(T) &= 2 - rank(T) \\
&= 2 - 1 \\
&= 1
\end{aligned}
$$

With that in mind, let's examine our statements in turn.

- Statement I is true. Indeed, the point $(-1,5)$ satisfies the equation of the line $y=-5x$ since $5=-5\cdot (-1)$. Hence,
$\langle -1, 5 \rangle \in \textrm{Im}(\mathbf{T})$.
- Statement II is false. Since $\textrm{nullity}(T)=1$, we get
$\textrm{Ker}(\mathbf{T}) \neq \{\mathbf0\}$.
- Statement III is false. Since $\textrm{rank}(\mathbf{T})=1$, we get
$\textrm{Im}(\mathbf{T}) \neq \Bbb R^2$.

Therefore, the correct answer is "I only."

---

**Question 5**

Consider a linear transformation $T$ that maps $R^{2}$ to the origin. Which of the following statements are true?

1. $rank(T) = 1$, where $T$ is the standard matrix of $T$
2. $Im(T)$ contains no vectors of $R^{2}$
3. $Ker(T) = R^{2}$

- [ ] A. II and III only
- [ ] B. I and II only
- [ ] C. III only
- [ ] D. II only
- [ ] E. I only

---

**Question 6**

Consider a linear transformation $T$ that maps $R^{2}$ onto $R^{2}$. Which of the following statements are true?

1. $nullity(T) = 0$, where $T$ is the standard matrix of $T$
2. $rank(T) = 0$
3. $Ker(T) = {0}$

- [ ] A. II and III only
- [ ] B. I and III only
- [ ] C. I and II only
- [ ] D. III only
- [ ] E. II only

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
