# Gaussian Elimination For NxM Systems of Equations

<!--
lesson-id: 1733
topic-code: LAL.2.2.12
-->

## Table of Contents

- [Introduction](#introduction)
- [Solving a Consistent System of Equations Using Gauss-Jordan Elimination](#solving-a-consistent-system-of-equations-using-gauss-jordan-elimination)
- [Consistent and Inconsistent Systems of Linear Equations](#consistent-and-inconsistent-systems-of-linear-equations)
- [Classifying a System as Consistent Dependent, Consistent Independent, or Inconsistent](#classifying-a-system-as-consistent-dependent-consistent-independent-or-inconsistent)
- [Determining the Value of a Constant for Which a Given Linear System Is Consistent or Inconsistent](#determining-the-value-of-a-constant-for-which-a-given-linear-system-is-consistent-or-inconsistent)

## Prerequisites

- [Reduced Row Echelon Form](<2.2.11. Reduced Row Echelon Form.md>)

---

<a id="introduction"></a>
## Introduction

We can use Gaussian elimination to find the general solutions of systems with any number of equations and variables. For instance, consider the system of linear equations

$$
{x - y + z = 1,; y + 3z = 0
$$

This system has the augmented matrix
$\begin{bmatrix}1 & -1 & 1 & 1 \\ 0 & 1 & 3 & 0\end{bmatrix}$

Using the standard Gauss-Jordan method, we can reduce this matrix to reduced row echelon form as follows:

$$
\begin{aligned}
M &= \begin{bmatrix}1 & -1 & 1 & 1 \\ 0 & 1 & 3 & 0\end{bmatrix} \mid R_{1} \mid:=R_{1} + R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 4 & 1 \\ 0 & 1 & 3 & 0\end{bmatrix}
\end{aligned}
$$

The corresponding system is now

$$
{x + 4z = 1; y + 3z = 0
$$

We see that the basic variables are $x$ and $y$, and we have one free variable $z$. From the second equation, we get $y=-3z$, and from the first equation, we obtain $x=1-4z$. Therefore, the general solution is

$$
{x = 1 - 4z,; y =-3z,; z = z
$$

In vector form, the solution can be written as

$$
\begin{bmatrix}x \\ y \\ z\end{bmatrix} = \begin{bmatrix}1 - 4z \\ -3z \\ z\end{bmatrix} = \begin{bmatrix}1 \\ 0 \\ 0\end{bmatrix} + z\begin{bmatrix}-4 \\ -3 \\ 1\end{bmatrix}, z ∈ (- ∞, ∞)
$$

---

<a id="solving-a-consistent-system-of-equations-using-gauss-jordan-elimination"></a>
## Solving a Consistent System of Equations Using Gauss-Jordan Elimination

**Example:** Consider the system of linear equations
$x + 5y + z = 2; x + 5y + 2z = 5$.
Use the standard Gauss-Jordan method to reduce the augmented matrix $M$ of the system to reduced row echelon form, and then solve the linear system.

**Explanation**

The augmented matrix for this system is
$\begin{bmatrix}1 & 5 & 1 & 2 \\ 1 & 5 & 2 & 5\end{bmatrix}$

Using the standard Gauss-Jordan method, we can reduce this matrix to reduced row echelon form as follows:

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & 5 & 1 & 2 \\ 1 & 5 & 2 & 5\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + (-1)R_{1}; ∼\begin{bmatrix}1 & 5 & 1 & 2 \\ 0 & 0 & 1 & 3\end{bmatrix} \mid R_{1} \mid:=R_{1} + (-1)R_{2}; ∼\begin{bmatrix}1 & 5 & 0 & -1 \\ 0 & 0 & 1 & 3\end{bmatrix}
$$

The corresponding system is now

$$
x + 5y =-1; z = 3
$$

The basic variables are the variables whose coefficients are pivots. In this case, the basic variables are $x$ and $z$, and we have one free variable, $y$. That means we need to find $x$ and $z$ in terms of $y$.

Therefore, the general solution is

$$
{x =-1 - 5y; y = y; z = 3
$$

where $y$ (the free variable) is an arbitrary number.

In vector form, the solution can be written as

$$
\begin{bmatrix}x \\ y \\ z\end{bmatrix} = \begin{bmatrix}-1 - 5y \\ y \\ 3\end{bmatrix}, y ∈ (- ∞, ∞)
$$

---

**Question 1:** Consider the system of linear equations
${x + y + 2z = 19; 3x + y + 2z = 31$.
Use the standard Gauss-Jordan method to reduce the augmented matrix $M$ of the system to reduced row echelon form, and then solve the linear system.

- [ ] A. $\begin{bmatrix}6 - z \\ 13 - 2z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] B. $\begin{bmatrix}z \\ 13 - 2z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] C. $\begin{bmatrix}6 \\ 13 - 2z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] D. $\begin{bmatrix}z \\ 13 - 2z \\ 6\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] E. No solution

---

**Question 2:** Consider the system of linear equations
${x + 3z =-2; x + y = 0$.
Use the standard Gauss-Jordan method to reduce the augmented matrix $M$ of the system to reduced row echelon form, and then solve the linear system.

- [ ] A. $\begin{bmatrix}-2 - 3z \\ 3z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] B. $\begin{bmatrix}-2 - 3z \\ 2 + 3z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] C. $\begin{bmatrix}-2 + 3z \\ 2 + 3z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] D. $\begin{bmatrix}2 - 3z \\ 2 - 3z \\ z\end{bmatrix}, z ∈ (- ∞, ∞)$
- [ ] E. No solution

---

<a id="consistent-and-inconsistent-systems-of-linear-equations"></a>
## Consistent and Inconsistent Systems of Linear Equations

Recall that a system of linear equations is called **consistent** if it has at least one solution. Otherwise, if there are no solutions, the system is called **inconsistent**.

For a consistent system,

- the system is called **consistent independent** if the solution is unique, while
- the system is called **consistent dependent** if there are infinitely many solutions.

![](<../Source/2.2.12. Gaussian Elimination For NxM Systems of Equations/Images/ef44dc9b559ee21ae3dd64ec80d86222.png>)

---

<a id="classifying-a-system-as-consistent-dependent-consistent-independent-or-inconsistent"></a>
## Classifying a System as Consistent Dependent, Consistent Independent, or Inconsistent

**Example:** Given the system
$2x - 3y = 4; - 4x + 6y =-8$,
which of the following statements are true?

1. The system is consistent dependent
2. The system has no solution
3. The system is consistent independent

**Explanation**

To classify the system, we first need to find its solution. The augmented matrix for this system is
$\begin{bmatrix}2 & -3 & 4 \\ -4 & 6 & -8\end{bmatrix}$

Using Gaussian elimination, we can reduce this matrix to row echelon form as follows:

$$
\begin{aligned}
M &= \begin{bmatrix}2 & -3 & 4 \\ -4 & 6 & -8\end{bmatrix} \mid R_{2} \mid:=R_{2} + 2R_{1} \\
&= ∼\begin{bmatrix}2 & -3 & 4 \\ 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

The system is now

$$
2x - 3y = 4; 0x + 0y = 0
$$

Since any numbers $x$ and $y$ form a solution of the final equation

$$
0x+0y=0
$$

we can remove this equation from the system. So, our system now consists of just a single equation:

$$
2x -3y= 4
$$

The basic variable is $x$, and we have a free variable $y$. That means we need to find $x$ in terms of $y$.

Therefore, since there are infinitely many solutions, we conclude that the system is *consistent dependent*.

As a result, only statement I is true.

---

**Question 3**

Consider the following system of equations.
${-x + 2y = 6; 2x - 3y = 13; 5x - y = 45$
Which of the following statements are true?

1. The system is consistent dependent
2. The system is inconsistent
3. The system has a unique solution

- [ ] A. I only
- [ ] B. II only
- [ ] C. III only
- [ ] D. I and III only
- [ ] E. None

---

**Question 4**

Consider the following system of equations.
${x - 3y + z = 0; - 4x + 6y = 2$
Which of the following statements are true?

1. The system is consistent dependent
2. The system has no solution
3. The system is consistent independent

- [ ] A. I and II only
- [ ] B. I only
- [ ] C. III only
- [ ] D. II only
- [ ] E. None

---

<a id="determining-the-value-of-a-constant-for-which-a-given-linear-system-is-consistent-or-inconsistent"></a>
## Determining the Value of a Constant for Which a Given Linear System Is Consistent or Inconsistent

**Example:** Determine the value of $k$ for which the following system of linear equations is inconsistent:
${2x - 2y = 1,; 4x + ky =-3$

**Explanation**

The augmented matrix for this system is

$$
\begin{bmatrix}2 & -2 & 1 \\ 4 & k & -3\end{bmatrix}
$$

Using the method of Gaussian elimination, we can reduce this matrix to row echelon form as follows:

$$
\begin{aligned}
M &= \begin{bmatrix}2 & -2 & 1 \\ 4 & k & -3\end{bmatrix} \mid R_{2} \mid:=R_{2} - 2R_{1} \\
&= ∼\begin{bmatrix}2 & -2 & 1 \\ 0 & k + 4 & -5\end{bmatrix}
\end{aligned}
$$

The corresponding system is now

$$
2x - 2y = 1; (k + 4)y =-5
$$

The system is inconsistent if it has no solutions. For the system to have no solutions, one of the equations above must be false.

If $k=-4$, then the second equation is $0=-5$, which is false, and the system will have no solution.

Therefore, our system is inconsistent when $k=-4$.

---

**Question 5**

Determine the value of $k$ for which the following system of linear equations is consistent dependent:

${3x + 4y = 12; x + ky = 4$

- [ ] A. $k =-\frac{4}{3}$
- [ ] B. $k =-4$
- [ ] C. $k = \frac{4}{3}$
- [ ] D. $k = \frac{3}{4}$
- [ ] E. $k = 0$

---

**Question 6**

Determine the value of $k$ for which the following system of linear equations is inconsistent:

${2x + 3y = 1; 6x + (k + 3)y =-5$

- [ ] A. $k =-2$
- [ ] B. $k = 7$
- [ ] C. $k = 9$
- [ ] D. $k = 6$
- [ ] E. $k = 4$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
