# Elementary 2x2 Matrices

<!--
lesson-id: 1726
topic-code: LAL.2.3.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying an Elementary 2x2 Matrix](#identifying-an-elementary-2x2-matrix)
- [The Inverse of an Elementary Matrix](#the-inverse-of-an-elementary-matrix)
- [Finding the Inverse of an Elementary 2x2 Matrix](#finding-the-inverse-of-an-elementary-2x2-matrix)
- [Computing a Product Involving the Inverse of an Elementary 2x2 Matrix](#computing-a-product-involving-the-inverse-of-an-elementary-2x2-matrix)

## Prerequisites

- [Introduction to the Inverse of a Matrix](<../../../../PCL/6. Matrices/6.4. The Inverse of a Matrix/Lessons/6.4.1. Introduction to the Inverse of a Matrix.md>)
- [Creating Rows or Columns Containing Zeros Using Gaussian Elimination](<../../2.2. Gaussian Elimination/Lessons/2.2.5. Creating Rows or Columns Containing Zeros Using Gaussian Elimination.md>)

---

<a id="introduction"></a>
## Introduction

An **elementary matrix** is a matrix that can be obtained from the identity matrix by applying *only one* elementary row operation.

For example, starting with the $2 \times 2$ identity matrix $\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix}$ we can apply a single elementary row operation to get any of the matrices below:

$$
\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}
$$

- $E_1$ is obtained from $I_2$ by applying the row operation $R_1 \leftrightarrow R_2$.
- $E_2$ is obtained from $I_2$ by applying the row operation $R_1:=2R_1$.
- $E_3$ is obtained from $I_2$ by applying the row operation $R_2:= R_2 + (-7)R_1$.

---

<a id="identifying-an-elementary-2x2-matrix"></a>
## Identifying an Elementary 2x2 Matrix

**Example:** Is
$E = \begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix}$
an elementary matrix?

**Explanation**

An elementary matrix is a matrix obtained from the identity matrix by applying exactly one elementary row operation.

The given matrix $E$ is an elementary matrix because it can be obtained from $I_2$ by applying the elementary row operation

$$
R_2:= R_2+(-5) R_1
$$

as follows:

$$
\begin{aligned}
I_{2} &= \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \mid R_{2} \mid:=R_{2} + (-5)R_{1} \\
&= ∼\begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix} \\
&= E
\end{aligned}
$$

---

**Question 1**

Which of the following are elementary matrices?

$P = \begin{bmatrix}1 & -2 \\ 0 & 1\end{bmatrix}, Q = \begin{bmatrix}1 & 1 \\ -3 & 1\end{bmatrix}, S = \begin{bmatrix}1 & -1 \\ 0 & 2\end{bmatrix}$

- [ ] A. $P$ and $Q$ only
- [ ] B. $P$ only
- [ ] C. $Q$ and $S$ only
- [ ] D. $Q$ only
- [ ] E. None

---

**Question 2**

Which of the following are elementary matrices?

$A = \begin{bmatrix}1 & 0 \\ 2 & 4\end{bmatrix}, B = \begin{bmatrix}1 & 0 \\ 0 & 4\end{bmatrix}, C = \begin{bmatrix}1 & 0 \\ 2 & 1\end{bmatrix}$

- [ ] A. $A$ only
- [ ] B. $B$ and $C$ only
- [ ] C. $C$ only
- [ ] D. $B$ only
- [ ] E. $A$ and $B$ only

---

<a id="the-inverse-of-an-elementary-matrix"></a>
## The Inverse of an Elementary Matrix

Each elementary matrix $E$ is invertible, and the corresponding inverse is always an elementary matrix of the same type as $E$.
To illustrate, let's consider the matrix

$\begin{bmatrix}1 & 0 \\ 4 & 1\end{bmatrix}$
To find the inverse of $E$, we proceed as follows:

**Step 1:** Write down $\color{blue}E$ and the identity matrix $I_2$ side-by-side, as shown below:
$\begin{bmatrix}1 & 0 & 1 & 0 \\ 4 & 1 & 0 & 1\end{bmatrix}$

**Step 2:** Apply an elementary row operation that transforms the left matrix into the identity matrix. In our case, we need to apply the transformation

$$
R_2:= R_2 + (-4)R_1
$$

We apply the operation to the entire row (not only the left part):

$$
\begin{aligned}
\begin{bmatrix}E & I_{2}\end{bmatrix} &= \begin{bmatrix}1 & 0 & 1 & 0 \\ 4 & 1 & 0 & 1\end{bmatrix} \mid R_{2}:=R_{2} + (-4)R_{1} \\
&= ∼\begin{bmatrix}1 & 0 & 1 & 0 \\ 0 & 1 & -4 & 1\end{bmatrix} \\
&= \begin{bmatrix}I_{2} & E^{-1}\end{bmatrix}
\end{aligned}
$$

The matrix $E^{-1}$ is obtained on the right-hand side of our big matrix after the transformation. Therefore,
$\begin{bmatrix}1 & 0 \\ -4 & 1\end{bmatrix}$

We can work out the inverse of all elementary $2 \times 2$ matrices in a similar way.

---

<a id="finding-the-inverse-of-an-elementary-2x2-matrix"></a>
## Finding the Inverse of an Elementary 2x2 Matrix

**Example:** Find the inverse of the elementary matrix
$\begin{bmatrix}1 & 0 \\ 0 & \frac{2}{5}\end{bmatrix}$

**Explanation**

We write down $E$ and the identity matrix $I$ side-by-side (as shown below) and apply an elementary row operation that transforms the left matrix into the identity matrix:

$$
\begin{aligned}
\begin{bmatrix}E & I\end{bmatrix} &= \begin{bmatrix}1 & 0 & 1 & 0 \\ 0 & \frac{2}{5} & 0 & 1\end{bmatrix} \mid R_{2}:=\frac{5}{2}R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 1 & 0 \\ 0 & 1 & 0 & \frac{5}{2}\end{bmatrix}
\end{aligned}
$$

The inverse is obtained on the right-hand side of our big matrix after the transformation. Therefore,

$$
\begin{bmatrix}1 & 0 \\ 0 & \frac{5}{2}\end{bmatrix}
$$

---

**Question 3:** Find the inverse of the elementary matrix
$A = \begin{bmatrix}1 & 0 \\ -7 & 1\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}1 & 0 \\ -7 & 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 0 \\ 7 & 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 7 \\ 0 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & -7 \\ 0 & 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix}$

---

**Question 4:** Find the inverse of the elementary matrix
$E = \begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}0 & 1 \\ 1 & 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 1 \\ 1 & 0\end{bmatrix}$
- [ ] C. $\begin{bmatrix}0 & -1 \\ -1 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}0 & -1 \\ -1 & 0\end{bmatrix}$
- [ ] E. $\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}$

---

<a id="computing-a-product-involving-the-inverse-of-an-elementary-2x2-matrix"></a>
## Computing a Product Involving the Inverse of an Elementary 2x2 Matrix

**Example:** Find the product $E_1 E_{2}^{-1}$, where $\begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix}$ and $\begin{bmatrix}1 & -2 \\ 0 & 1\end{bmatrix}$

**Explanation**

Notice that $E_1$ and $E_2$ are both elementary matrices.

First, we need to find $E_2^{-1}$. We write down $E_2$ and the identity matrix $I$ side-by-side (as shown below) and apply an elementary row operation that transforms the left matrix into the identity matrix:

$$
\begin{aligned}
\begin{bmatrix}E_{2} & I\end{bmatrix} &= \begin{bmatrix}1 & -2 & 1 & 0 \\ 0 & 1 & 0 & 1\end{bmatrix} \mid R_{1}:=R_{1} + 2R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 1 & 2 \\ 0 & 1 & 0 & 1\end{bmatrix}
\end{aligned}
$$

Therefore,
$\begin{bmatrix}1 & 2 \\ 0 & 1\end{bmatrix}$

Now, we can find the product $E_1 E_{2}^{-1}$:
$\begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix}$

---

**Question 5:** Find the product $E_{1}^{-1}E_{2}$, where
$E_{1}|= \begin{bmatrix}1 & -7 \\ 0 & 1\end{bmatrix}, E_{2} = \begin{bmatrix}1 & 0 \\ 2 & 1\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}1 & 2 \\ 0 & 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 0 \\ 2 & 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}15 & 7 \\ 2 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}15 & -7 \\ 2 & 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}15 & 2 \\ 7 & 1\end{bmatrix}$

---

**Question 6:** Find the product $E_{1}E_{2}^{-1}$, where
$E_{1}|= \begin{bmatrix}1 & 4 \\ 0 & 1\end{bmatrix}, E_{2} = \begin{bmatrix}1 & 0 \\ 3 & 1\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}-11 & -3 \\ 4 & 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 4 \\ -3 & -11\end{bmatrix}$
- [ ] C. $\begin{bmatrix}3 & 4 \\ -11 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-11 & 4 \\ -3 & 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-11 & 4 \\ 1 & -3\end{bmatrix}$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
