# Classifying Quadratic Curves

<!--
lesson-id: 3336
topic-code: LAL.7.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Non-Degenerate Quadratic Curves](#non-degenerate-quadratic-curves)
- [Degenerate (Real) Quadratic Curves](#degenerate-real-quadratic-curves)
- [Empty Sets](#empty-sets)
- [Identifying a Quadratic Curve Type From Its Reduced Equation: No Cross-Product Term](#identifying-a-quadratic-curve-type-from-its-reduced-equation-no-cross-product-term)
- [Classifying a Quadratic Curve That Does Not Contain a Cross-Product Term](#classifying-a-quadratic-curve-that-does-not-contain-a-cross-product-term)
- [Identifying a Quadratic Curve Type: No Cross-Product Term](#identifying-a-quadratic-curve-type-no-cross-product-term)
- [Classifying a General Quadratic Curve](#classifying-a-general-quadratic-curve)
- [Identifying a Quadratic Curve Type](#identifying-a-quadratic-curve-type)

## Prerequisites

- [Upward and Downward Opening Parabolas](<../../../../AG2/7. Conic Sections/7.1. Parabolas as Conic Sections/Lessons/7.1.1. Upward and Downward Opening Parabolas.md>)
- [Reducing a Quadratic Curve to Its Principal Axes](<7.3.1. Reducing a Quadratic Curve to Its Principal Axes.md>)
- [Completing the Square With Leading Coefficients](<../../../../AG1/10. Quadratic Equations & Functions/10.1. Quadratic Equations/Lessons/10.1.11. Completing the Square With Leading Coefficients.md>)

---

<a id="introduction"></a>
## Introduction

The general equation of a second order (quadratic) curve is an expression of the form

$$
\alpha_{11}x_1^2+2\alpha_{12}x_1x_2 + \alpha_{22}x_2^2 + 2\alpha_{13}x_1+ 2\alpha_{23}x_2 +\alpha_{33}=0
$$

where $x_1$ and $x_2$ are variables, and $\alpha_{ij} \in \Bbb R$ for all

$$
1\leq i,j \leq 3
$$

For example,

$$
3x_1^2-x_1x_2-5x_2^2+5x_2 - 1 = 0
$$

is a general quadratic curve.

---

<a id="non-degenerate-quadratic-curves"></a>
## Non-Degenerate Quadratic Curves

There are three basic so-called *non-degenerate quadratic curves*:

| Standard Equation | Description |
| --- | ---: |
| $\dfrac{x_1^2}{a^2} + \dfrac{x_2^2}{b^2}=1$ | Ellipse |
| $\dfrac{x_1^2}{a^2} - \dfrac{x_2^2}{b^2}=1$ | Hyperbola |
| $x_2^2= 2px_1$ | Parabola |

---

<a id="degenerate-real-quadratic-curves"></a>
## Degenerate (Real) Quadratic Curves

There are also a few so-called *degenerate (real) quadratic curves:*

| Standard Equation | Description |
| --- | ---: |
| $\dfrac{x_1^2}{a^2} - \dfrac{x_2^2}{b^2}=0$ | Pair of<br>intersecting lines |
| ${x_i^2}=a^2$ | Pair of<br>parallel lines |
| ${x_i^2}=0$ | Pair of<br>coincident lines |
| $\dfrac{x_1^2}{a^2} + \dfrac{x_2^2}{b^2}=0$ | Single point |

Let's see why these equations define the described curves:

- $\dfrac{x_1^2}{a^2} - \dfrac{x_2^2}{b^2}=0$ is equivalent to $(bx_1 - ax_2)(bx_1 + ax_2)=0$ which in turn gives equations of two lines $bx_1 - ax_2=0$ and $bx_1 + ax_2=0$ intersecting at the origin.
- ${x_i^2}=a^2$ is equivalent to $(x_i - a)(x_i + a)=0$ which in turn gives equations of two parallel lines $x_i =a$ and $x_i=-a$.
- ${x_i^2}=0$ gives equations of two coincident lines $x_i =0$ and $-x_i=0$.
- $\dfrac{x_1^2}{a^2} + \dfrac{x_2^2}{b^2}=0$ is equivalent to $b^2x_1^2+a^2x_2^2=0$ which in turn gives a single point, the origin, since the sum of two squares is $0$ if and only if both squares are $0$.

---

<a id="empty-sets"></a>
## Empty Sets

Finally, some quadratic equations might define an *empty set* (or so-called *imaginary curve*):

| Standard Equation | Description |
| --- | ---: |
| $\dfrac{x_1^2}{a^2} + \dfrac{x_2^2}{b^2}=-1$ | Imaginary ellipse |
| $x_i^2=-a^2$ | Pair of imaginary<br>parallel lines |

Let's see why these equations define the described curves:

- Although there are no real solutions, the equation $\dfrac{x_1^2}{a^2} + \dfrac{x_2^2}{b^2} = -1$, which is equivalent to $\dfrac{x_1^2}{(a\textrm{i})^2} + \dfrac{x_2^2}{(b\textrm{i})^2} = 1$, has imaginary solutions, so we could interpret this as an imaginary ellipse.
- Although there are no real solutions, the equation ${x_i^2}=-a^2$ is equivalent to $(x_i - a\textrm{i})(x_i + a\textrm{i})=0$ which in turn gives equations of two imaginary parallel lines $x_i =a\textrm{i}$ and $x_i=-a\textrm{i}$.

---

<a id="identifying-a-quadratic-curve-type-from-its-reduced-equation-no-cross-product-term"></a>
## Identifying a Quadratic Curve Type From Its Reduced Equation: No Cross-Product Term

**Example:** Which of the following equations define a non-empty set of points in the $x_1x_2$-plane?

1. $x_1^2 +y_2^2= 0$
2. $x_1^2+\dfrac{x_2^2}{2} = -1$
3. $2x_2^2 -x_1^2= 0$

**Explanation**

Let's examine each of the equations in turn.

- The equation $x_1^2 +y_2^2= 0$ defines a non-empty set. It represents the point $(0,0)$ in the $x_1x_2$-plane.
- The equation $x_1^2+\dfrac{x_2^2}{2} = -1$ defines an empty set. Notice that the left-hand side is non-negative for all $x_1$ and $x_2$, while the right-hand side is negative.
- The equation $2x_2^2 -x_1^2= 0$ defines a non-empty set. It represents a pair of intersecting lines, $x_1 =\sqrt{2}x_2$ and $x_1 =-\sqrt{2}x_2$.

Therefore, the correct answer is "I and III only."

---

**Question 1:** Which of the following equations defines two parallel lines in the $x_{1}x_{2}$-plane?

- [ ] A. $C_{4}: x_{2}^{2} = 7$
- [ ] B. $C_{5}: x_{1}^{2} + \frac{x_{2}^{2}}{4} =-1$
- [ ] C. $C_{3}: x_{2}^{2} = 2x_{1}$
- [ ] D. $C_{2}: \frac{x_{1}^{2}}{4} - \frac{x_{2}^{2}}{9} = 1$
- [ ] E. $C_{1}: x_{1}^{2} + x_{2}^{2} = 0$

---

**Question 2**

Which of the following equations define a non-empty set of points in the $x_{1}x_{2}$-plane?

1. $x_{1}^{2} = 6$
2. $x_{1}^{2} + \frac{x_{2}^{2}}{5} =-1$
3. $x_{2}^{2} =-7$

- [ ] A. I only
- [ ] B. III only
- [ ] C. I, II, and III
- [ ] D. I and III only
- [ ] E. II only

---

<a id="classifying-a-quadratic-curve-that-does-not-contain-a-cross-product-term"></a>
## Classifying a Quadratic Curve That Does Not Contain a Cross-Product Term

A quadratic equation with no cross-terms can be rewritten in standard form by completing the square. Let's see how this is done in the following example.

Suppose we are given the equation

$$
2x_1^2+x_2^2 - 4x_1 + 4x_2 = 2
$$

what type of curve does it represent?

First, we complete the squares for the variables $x_1$ and $x_2$:

$$
\begin{aligned}
2x_{1}^{2} + x_{2}^{2} - 4x_{1} + 4x_{2} &= 2 \\
2(x_{1}^{2} - 2x_{1}) + (x_{2}^{2} + 4x_{2}) &= 2 \\
2(x_{1}^{2} - 2x_{1} + 1 - 1) + (x_{2}^{2} + 4x_{2} + 4 - 4) &= 2 \\
2(x_{1} - 1)^{2} - 2 + (x_{2} + 2)^{2} - 4 &= 2 \\
2(x_{1} - 1)^{2} + (x_{2} + 2)^{2} &= 8
\end{aligned}
$$

Now, performing the change of variables

$$
{y_{1} = x_{1} - 1,; y_{2} = x_{2} + 2
$$

we get the following equation in standard form:

$$
\begin{aligned}
2y_{1}^{2} + y_{2}^{2} &= 8 \\
\frac{y_{1}^{2}}{4} + \frac{y_{2}^{2}}{8} &= 1 \\
\frac{y_{1}^{2}}{2^{2}} + (y_{2}^{2})/((\sqrt{8})^{2}) &= 1
\end{aligned}
$$

This represents an ellipse with semiaxes $2$ and $\sqrt{8}$.

---

<a id="identifying-a-quadratic-curve-type-no-cross-product-term"></a>
## Identifying a Quadratic Curve Type: No Cross-Product Term

**Example:** What type of a quadratic curve is defined by the following equation?
$3x_1^2 + 2x_1 - x_2 =\dfrac{5}{3}$

**Explanation**

First, we complete the squares for the variables $x_1$:

$$
\begin{aligned}
3x_{1}^{2} + 2x_{1} - x_{2} &= \frac{5}{3} \\
3(x_{1}^{2} + \frac{2}{3}x_{1}) - x_{2} &= \frac{5}{3} \\
3(x_{1}^{2} + \frac{2}{3}x_{1} + (\frac{1}{3})^{2} - (\frac{1}{3})^{2}) - x_{2} &= \frac{5}{3} \\
3(x_{1} + \frac{1}{3})^{2} - 3(\frac{1}{3})^{2} - x_{2} &= \frac{5}{3} \\
3(x_{1} + \frac{1}{3})^{2} - \frac{1}{3} - x_{2} &= \frac{5}{3} \\
3(x_{1} + \frac{1}{3})^{2} &= x_{2} + 2
\end{aligned}
$$

Now, performing the change of variables

$$
{y_{1} = x_{1} + \frac{1}{3},; y_{2} = x_{2} + 2
$$

we get the following:

$$
\begin{aligned}
3y_{1}^{2} &= y_{2} \\
y_{1}^{2} &= \frac{1}{3}y_{2}
\end{aligned}
$$

This represents a parabola that opens in the direction of the positive $y_2$-axis.

---

**Question 3:** What type of a quadratic curve is defined by the following equation?
$5x_{1}^{2} - 4x_{1} + 3x_{2} = \frac{11}{5}$

- [ ] A. An ellipse with semiaxes $\sqrt{5}$ and $\sqrt{2}$
- [ ] B. A parabola
- [ ] C. A hyperbola with semiaxes $5$ and $3$
- [ ] D. An empty set
- [ ] E. Two parallel lines

---

**Question 4:** What type of a quadratic curve is defined by the following equation?
$2x_{1}^{2} + 3x_{2}^{2} - 4x_{1} + 12x_{2} =-8$

- [ ] A. A pair of parallel lines
- [ ] B. An ellipse with semiaxes $\sqrt{3}$ and $\sqrt{2}$
- [ ] C. A hyperbola with semiaxes $3$ and $4$
- [ ] D. An ellipse with semiaxes $9$ and $4$
- [ ] E. A parabola

---

<a id="classifying-a-general-quadratic-curve"></a>
## Classifying a General Quadratic Curve

In general, the cross-term of a quadratic equation can be eliminated by an orthogonal change of variables. That is, one of the kind

$$
\mathbf{x} =P \mathbf{y}
$$

where the columns of $P$ form an orthonormal eigenbasis for the corresponding quadratic form's matrix.

Let $\mathcal C$ be a plane curve defined as

$$
\mathcal C:\: 2x_1x_2 -2\sqrt2x_1+2\sqrt2x_2 =8
$$

How do we determine the type of our curve?

Notice that the quadratic form $2x_1x_2$ can be reduced to canonical form $y_1^2-y_2^2$ using the change of variables
$\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$

First, let's write the given change of variables explicitly:

$$
\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}
$$

We know that this change of variables reduces the corresponding quadratic form

$$
Q(\mathbf x) = 2x_1x_2
$$

to its canonical form. Indeed, performing the change of variables on $\mathcal C$, gives

$$
2x_{1}x_{2} - 2\sqrt{2}x_{1} + 2\sqrt{2}x_{2} = 8⟹y_{1}^{2} - y_{2}^{2} + 4y_{2} = 8
$$

and, by completing the square for the variable $y_2$, we get the equation

$$
\begin{aligned}
y_{1}^{2} - (y_{2} - 2)^{2} &= 4
\end{aligned}
$$

The graph of the resulting equation in the $y_1,y_2$ variables is the following.

![](<../Source/7.3.2. Classifying Quadratic Curves/Images/7572b5977885f8aaefaf92f79e8e0ed9.png>)

Notice that we can write down the change-of-variables matrix as

$$
\begin{bmatrix}\frac{1}{\sqrt{2}} & -\frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}}\end{bmatrix}
$$

which is a rotation matrix. Looking at the diagram above, we see that going from one coordinate system to another can be viewed as a rotation of our axes by $45^\circ$.

**Watch out!** In general, this action is an orthogonal transformation (it can be a rotation, a reflection, or a combination of such transformations).

Finally, performing the change of variables

$$
{z_{1} = y_{1},; z_{2} = y_{2} - 2
$$

we get the following:

$$
\begin{aligned}
y_{1}^{2} - (y_{2} - 2)^{2} &= 4 \\
\frac{z_{1}^{2}}{4} - \frac{z_{2}^{2}}{4} &= 1 \\
\frac{z_{1}^{2}}{2^{2}} - \frac{z_{2}^{2}}{2^{2}} &= 1
\end{aligned}
$$

This (standard) equation represents a hyperbola in which both semiaxes are equal to $2$.

In the diagram below we represent the curve in the $z_1,z_2$ variables.

![](<../Source/7.3.2. Classifying Quadratic Curves/Images/ed4b7db3aa440b5ea5d03b0f17ff8047.png>)

Looking at the diagram above, we see that in the final step we simply translated our system by $(0,2)$ in $y$-coordinates.

**Note:** Finding the simplest equation of a quadratic curve can be viewed as the following series of steps:

- **Step 1**. We eliminate the cross-term by reducing the quadratic part to canonical form. Geometrically, this is equivalent to an orthogonal transformation of our coordinate system.
- **Step 2**. We eliminate the linear terms by completing the squares. Geometrically, this is equivalent to a translation of our coordinate system.

---

<a id="identifying-a-quadratic-curve-type"></a>
## Identifying a Quadratic Curve Type

**Example:** Consider the change of variables given by the matrix equation
$\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$
and the curve $\mathcal C$, defined as
$\mathcal C:\: 2x_1^2+6x_1x_2+ 2x_2^2 -2x_1+2x_2 =7$.

Given that the change of variables above reduces $Q(\mathbf{x})=2x_1^2+6x_1x_2+2x_2^2$ to its canonical (diagonal) form $5y_1^2-y_2^2$, what type of a quadratic curve is defined by $\mathcal C$?

**Explanation**

First, we note the following:

$$
C:\: \underbrace{2x_1^2+6x_1x_2+2x_2^2}_{Q(\mathbf{x})} -2x_1+2x_2 =7
$$

Let's write the given change of variables explicitly:

$$
\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}
$$

We know that the given change of variables reduces $Q(\mathbf x)$ to its canonical form. Therefore, performing the change of variables on $\mathcal C$, we obtain

$$
\begin{aligned}
2x_{1}^{2} + 6x_{1}x_{2} + 2x_{2}^{2} - 2x_{1} + 2x_{2} &= 7 \\
5y_{1}^{2} - y_{2}^{2} - 2(\frac{1}{\sqrt{2}}y_{1} - \frac{1}{\sqrt{2}}y_{2}) + 2(\frac{1}{\sqrt{2}}y_{1} + \frac{1}{\sqrt{2}}y_{2}) &= 7 \\
5y_{1}^{2} - y_{2}^{2} + \frac{4}{\sqrt{2}}y_{2} &= 7 \\
5y_{1}^{2} - y_{2}^{2} + 2\sqrt{2}y_{2} &= 7
\end{aligned}
$$

Now, we complete the square for the variable $y_2$:

$$
\begin{aligned}
5y_{1}^{2} - y_{2}^{2} + 2\sqrt{2}y_{2} &= 7 \\
5y_{1}^{2} - (y_{2}^{2} - 2\sqrt{2}y_{2}) &= 7 \\
5y_{1}^{2} - (y_{2}^{2} - 2\sqrt{2}y_{2} + (\sqrt{2})^{2}) + 2 &= 7 \\
5y_{1}^{2} - (y_{2}^{2} - 2\sqrt{2}y_{2} + (\sqrt{2})^{2}) + (\sqrt{2})^{2} &= 7 \\
5y_{1}^{2} - (y_{2} - \sqrt{2})^{2} + 2 &= 7 \\
5y_{1}^{2} - (y_{2} - \sqrt{2})^{2} &= 5
\end{aligned}
$$

Finally, performing the change of variables

$$
{z_{1} = y_{1},; z_{2} = y_{2} - \sqrt{2}
$$

we get the following:

$$
\begin{aligned}
5y_{1}^{2} - (y_{2} - \sqrt{2})^{2} &= 5 \\
5z_{1}^{2} - z_{2}^{2} &= 5 \\
\frac{z_{1}^{2}}{1^{2}} - (z_{2}^{2})/((\sqrt{5})^{2}) &= 1
\end{aligned}
$$

This represents a hyperbola with semiaxes $1$ and $\sqrt5$.

---

**Question 5**

Consider the change of variables given by the matrix equation
$\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix} = \begin{bmatrix}\frac{1}{\sqrt{5}} & -\frac{2}{\sqrt{5}} \\ \frac{2}{\sqrt{5}} & \frac{1}{\sqrt{5}}\end{bmatrix}\begin{bmatrix}y_{1} \\ y_{2}\end{bmatrix}$,
and the curve $C$, defined as
$C: 2x_{1}^{2} + 4x_{1}x_{2} + 5x_{2}^{2} + 4\sqrt{5}x_{1} - 2\sqrt{5}x_{2} =-26$.

Given that the change of variables above reduces $Q(x) = 2x_{1}^{2} + 4x_{1}x_{2} + 5x_{2}^{2}$ to its canonical (diagonal) form $6y_{1}^{2} + y_{2}^{2}$, what type of a quadratic curve is defined by $C$?

- [ ] A. An ellipse with semiaxes $1$ and $6$
- [ ] B. A circle of radius $\sqrt{6}$
- [ ] C. A parabola
- [ ] D. An hyperbola with semiaxes $1$ and $6$
- [ ] E. An empty set

---

**Question 6**

Consider the change of variables given by the matrix equation
$\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix} = \begin{bmatrix}-\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}}\end{bmatrix}\begin{bmatrix}y_{1} \\ y_{2}\end{bmatrix}$,
and the curve $C$, defined as
$C: 5x_{1}^{2} - 4x_{1}x_{2} + 5x_{2}^{2} - 2x_{1} + 2x_{2} = \frac{145}{7}$.

Given that the change of variables above reduces $Q(x) = 5x_{1}^{2} - 4x_{1}x_{2} + 5x_{2}^{2}$ to its canonical (diagonal) form $7y_{1}^{2} + 3y_{2}^{2}$, what type of a quadratic curve is defined by $C$?

- [ ] A. A pair of intersecting lines
- [ ] B. A hyperbola with semiaxes $\sqrt{7}$ and $\sqrt{3}$
- [ ] C. A parabola
- [ ] D. An ellipse with semiaxes $\sqrt{3}$ and $\sqrt{7}$
- [ ] E. A hyperbola with semiaxes $7$ and $3$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
