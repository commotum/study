# The Coordinates of a Vector Relative to a Basis in Abstract Vector Spaces

<!--
lesson-id: 3212
topic-code: LAL.3.5.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating a Vector Given Its Coordinates Relative to a Basis](#calculating-a-vector-given-its-coordinates-relative-to-a-basis)
- [Finding the Coordinates of a Vector With Respect to a Given Basis in a Matrix Vector Space](#finding-the-coordinates-of-a-vector-with-respect-to-a-given-basis-in-a-matrix-vector-space)
- [Finding the Coordinates of a Vector With Respect to a Given Basis in a Polynomial Vector Space](#finding-the-coordinates-of-a-vector-with-respect-to-a-given-basis-in-a-polynomial-vector-space)
- [Isomorphisms of Vector Spaces](#isomorphisms-of-vector-spaces)

## Prerequisites

- [Writing Vectors in Different Bases](<../../3.3. Bases of N-Dimensional Space/Lessons/3.3.5. Writing Vectors in Different Bases.md>)
- [Bases in Abstract Vector Spaces](<MA/LAL/3. Vector Spaces/3.5. Abstract Vector Spaces/Lessons/3.5.5. Bases in Abstract Vector Spaces.md>)

---

<a id="introduction"></a>
## Introduction

We've already seen how to write the coordinates of a vector $\mathbf v\in \mathbb R^n$ relative some general basis $\mathcal B$ of $\mathbb R^n$. In this lesson, we will generalize this concept to abstract vector spaces.

As an example, consider the matrices $A_1$ and $A_2$, given by

$$
\begin{bmatrix}3 & -1 \\ 0 & 5\end{bmatrix}
$$

The set

$$
\mathcal{B}=\big\{A_1, A_2 \big\}
$$

is a basis of the vector space

$$
V = \textrm{Span}\big\{A_1, A_2 \big\}
$$

Now, suppose that the coordinates of the matrix $C$ relative to $\mathcal{B}$ are

$$
\begin{bmatrix}2 \\ 4\end{bmatrix}
$$

Let's find the matrix $C$ using this information.

As usual, any vector in $\textrm{Span}\big\{A_1, A_2 \big\}$ can be written as

$$
x_1 A_1 + x_2 A_2
$$

where $x_1,x_2$ are the coordinates of the vector relative to the basis.

In our case, we have

$$
\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}
$$

Therefore,

$$
\begin{aligned}
C &= 2 \cdot \begin{bmatrix}3 & -1 \\ 0 & 5\end{bmatrix} + 4 \cdot \begin{bmatrix}0 & 3 \\ 2 & -2\end{bmatrix} \\
&= \begin{bmatrix}6 & -2 \\ 0 & 10\end{bmatrix} + \begin{bmatrix}0 & 12 \\ 8 & -8\end{bmatrix} \\
&= \begin{bmatrix}6 & 10 \\ 8 & 2\end{bmatrix}
\end{aligned}
$$

---

<a id="calculating-a-vector-given-its-coordinates-relative-to-a-basis"></a>
## Calculating a Vector Given Its Coordinates Relative to a Basis

**Example:** Consider the polynomials $p_1(t)$ and $p_2(t)$, given by
$p_1(t)=3+2t, \qquad p_2(t)=-t+t^2$.
Find $p(t)$ given that $\mathcal{B}=\big\{p_1(t), p_2(t) \big\}$ is a basis of $\textrm{Span}\big\{p_1, p_2 \big\}$, and the coordinates of $p(t)$ relative to $\mathcal{B}$ are $\begin{bmatrix}4 \\ -3\end{bmatrix}$

**Explanation**

Any vector in $\textrm{Span}\big\{p_1, p_2 \big\}$ can be written as

$$
x_{1}p_{1}(t) + x_{2}p_{2}(t)
$$

In our case, we have

$$
\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}
$$

Therefore,

$$
\begin{aligned}
p(t) &= 4 \cdot (3 + 2t) + (-3) \cdot (-t + t^{2}) \\
&= 12 + 8t + 3t - 3t^{2} \\
&= 12 + 11t - 3t^{2}
\end{aligned}
$$

---

**Question 1:** Consider the polynomials $p_{1}(t)$ and $p_{2}(t)$, given by
$p_{1}(t) = 1 + t, p_{2}(t) = 1 - t^{2}$.
Find $p(t)$ given that $B = {p_{1}(t), p_{2}(t)}$ is a basis of $Span{p_{1}, p_{2}}$, and the coordinates of $p(t)$ relative to $B$ are $[p(t)]_{B} = \begin{bmatrix}1 \\ -1\end{bmatrix}$.

- [ ] A. $p(t) = 1 - t^{2}$
- [ ] B. $p(t) = t^{2} - t$
- [ ] C. $p(t) = t + t^{2}$
- [ ] D. $p(t) =-2t + t^{2}$
- [ ] E. $p(t) = 1 + t - t^{2}$

---

**Question 2**

$A_{1} = \begin{bmatrix}1 & 0 \\ 2 & -3\end{bmatrix}, A_{2} = \begin{bmatrix}0 & 1 \\ 5 & 0\end{bmatrix}$

Consider the matrices shown above. Find the matrix $C$ given that $B = {A_{1}, A_{2}}$ is a basis of $Span{A_{1}, A_{2}}$, and the coordinates of the matrix $C$ relative to $B$ are $[C]_{B} = \begin{bmatrix}2 \\ -3\end{bmatrix}$.

- [ ] A. $C = \begin{bmatrix}-5 & -6 \\ -11 & -6\end{bmatrix}$
- [ ] B. $C = \begin{bmatrix}-3 & 2 \\ 4 & 9\end{bmatrix}$
- [ ] C. $C = \begin{bmatrix}2 & -3 \\ 6 & -11\end{bmatrix}$
- [ ] D. $C = \begin{bmatrix}2 & -3 \\ -11 & -6\end{bmatrix}$
- [ ] E. $C = \begin{bmatrix}-3 & -4 \\ -9 & 2\end{bmatrix}$

---

<a id="finding-the-coordinates-of-a-vector-with-respect-to-a-given-basis-in-a-matrix-vector-space"></a>
## Finding the Coordinates of a Vector With Respect to a Given Basis in a Matrix Vector Space

**Example:** Consider the following matrices:
$\begin{bmatrix}2 & 0 \\ 8 & -4\end{bmatrix}$
If $\mathcal B = \{B_1,B_2\}$ is a basis of $\textrm{Span}\{B_1, B_2 \}$, calculate $\begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$

**Explanation**

We need to find $x_1$ and $x_2$ such that

$$
x_1 B_1 +x_2 B_2 = A
$$

Substituting the given information into the above equation, we get

$$
\begin{aligned}
x_{1}\begin{bmatrix}2 & 0 \\ 8 & -4\end{bmatrix} + x_{2}\begin{bmatrix}-1 & 0 \\ -2 & 1\end{bmatrix} &= \begin{bmatrix}-5 & 0 \\ -2 & 1\end{bmatrix} \\
\begin{bmatrix}2x_{1} - x_{2} & 0 \\ 8x_{1} - 2x_{2} & -4x_{1} + x_{2}\end{bmatrix} &= \begin{bmatrix}-5 & 0 \\ -2 & 1\end{bmatrix}
\end{aligned}
$$

Equating the corresponding entries, we obtain

$$
{2x_{1} - x_{2} =-5; 8x_{1} - 2x_{2} =-2; - 4x_{1} + x_{2} = 1
$$

The equations above give a linear system with the augmented matrix

$$
\begin{bmatrix}2 & -1 & -5 \\ 8 & -2 & -2 \\ -4 & 1 & 1\end{bmatrix}
$$

Row-reducing the matrix using Gaussian elimination, we obtain the following:

$$
M = \mid \begin{bmatrix}2 & -1 & -5 \\ 8 & -2 & -2 \\ -4 & 1 & 1\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} + (-4)R_{1} \\ R_{3}:=R_{3} + 2R_{1} \\ ∼\end{vmatrix}\begin{bmatrix}2 & -1 & -5 \\ 0 & 2 & 18 \\ 0 & -1 & -9\end{bmatrix}
$$

The system is now

$$
{2x_{1} - x_{2} =-5; 2x_{2} = 18; - x_{2} =-9
$$

From the second and third equations, we get $x_2 =9$. From the first equation, we get

$$
\begin{aligned}
2x_{1} - 9 &= -5 \\
2x_{1} &= 4 \\
x_{1} &= 2
\end{aligned}
$$

Therefore, $\begin{bmatrix}2 \\ 9\end{bmatrix}$

---

**Question 3:** Consider the following matrices:
$B_{1} = \begin{bmatrix}4 & 5 \\ 4 & 0\end{bmatrix}, B_{2} = \begin{bmatrix}0 & -3 \\ 2 & 0\end{bmatrix}, A = \begin{bmatrix}2 & 1 \\ 3 & 0\end{bmatrix}$
If $B = {B_{1}, B_{2}}$ is a basis of $Span{B_{1}, B_{2}}$, and $[A]_{B} = \begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$, then $x_{1} + x_{2} =$

- [ ] A. $\frac{4}{3}$
- [ ] B. $1$
- [ ] C. $-2$
- [ ] D. $-\frac{3}{2}$
- [ ] E. $-3$

---

**Question 4:** Consider the following matrices:
$B_{1} = \begin{bmatrix}2 & 7 \\ -5 & 0\end{bmatrix}, B_{2} = \begin{bmatrix}0 & 3 \\ -1 & 0\end{bmatrix}, A = \begin{bmatrix}-2 & -1 \\ 3 & 0\end{bmatrix}$
If $B = {B_{1}, B_{2}}$ is a basis of $Span{B_{1}, B_{2}}$, and $[A]_{B} = \begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$, then $x_{1} + x_{2} =$

- [ ] A. $1$
- [ ] B. $2$
- [ ] C. $0$
- [ ] D. $-2$
- [ ] E. $-1$

---

<a id="finding-the-coordinates-of-a-vector-with-respect-to-a-given-basis-in-a-polynomial-vector-space"></a>
## Finding the Coordinates of a Vector With Respect to a Given Basis in a Polynomial Vector Space

**Example:** Consider the following polynomials:

$p_1(t)=5+t^2, \quad p_2(t)=3t-2t^2, \quad q(t)=5-3t+3t^2$.

If $\mathcal{B}=\{p_1(t), p_2(t) \}$ is a basis of $\textrm{Span}\big\{p_1(t), p_2(t) \big\}$, find $[q(t)]_{\mathcal{B}}$.

**Explanation**

We need to find $x_1$ and $x_2$ such that

$$
x_1 p_1(t)+x_2 p_2(t) = q(t)
$$

Substituting the given information into the above equation, we get

$$
\begin{aligned}
x_{1}(5 + t^{2}) + x_{2}(3t - 2t^{2}) &= 5 - 3t + 3t^{2} \\
5x_{1} + 3x_{2}t + (x_{1} - 2x_{2})t^{2} &= 5 - 3t + 3t^{2}
\end{aligned}
$$

Equating the coefficients of $t$, we obtain

$$
{5x_{1} = 5; 3x_{2} =-3; x_{1} - 2x_{2} = 3
$$

The equations above give a linear system with the augmented matrix

$$
\begin{bmatrix}5 & 0 & 5 \\ 0 & 3 & -3 \\ 1 & -2 & 3\end{bmatrix}
$$

Row-reducing the matrix using Gaussian elimination, we obtain the following:

$$
M =\begin{vmatrix}\begin{bmatrix}5 & 0 & 5 \\ 0 & 3 & -3 \\ 1 & -2 & 3\end{bmatrix} & R_{3} &:=R_{3} + (-\frac{1}{5})R_{1} \\ ∼ & \begin{bmatrix}5 & 0 & 5 \\ 0 & 3 & -3 \\ 0 & -2 & 2\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + \frac{2}{3}R_{2}; ∼ \mid \begin{bmatrix}5 & 0 & 5 \\ 0 & 3 & -3 \\ 0 & 0 & 0\end{bmatrix}
$$

The system is now

$$
{5x_{1} = 5; 3x_{2} =-3
$$

From the first and second equations, we get $x_1=1$ and $x_2 =-1$. Therefore, $\begin{bmatrix}1 \\ -1\end{bmatrix}$

---

**Question 5**

Consider the following polynomials:
$p_{1}(t) = t^{3} + t + 1, p_{2}(t) = t^{3} + t^{2} - 2t + 1, p_{3}(t) = 2t^{2} - 4t + 2, q(t) = t^{3} + t^{2} - 3t$.

If $B = {p_{1}(t), p_{2}(t), p_{3}(t)}$ is a basis of $Span{p_{1}(t), p_{2}(t), p_{3}(t)}$, and $[q(t)]_{B} = \begin{bmatrix}x_{1} \\ x_{2} \\ x_{3}\end{bmatrix}$, then $x_{1} =$

- [ ] A. $-7$
- [ ] B. $-1$
- [ ] C. $p(t) ∉ Span{p_{1}, p_{2}, p_{3}}$
- [ ] D. $6$
- [ ] E. $-\frac{5}{2}$

---

**Question 6**

Consider the following polynomials:

$p_{1}(t) = 1 + t^{2}, p_{2}(t) = 1 - t + 2t^{2}, q(t) = 1 - 3t + 4t^{2}$.

If $B = {p_{1}(t), p_{2}(t)}$ is a basis of $Span{p_{1}(t), p_{2}(t)}$, and $[q(t)]_{B} = \begin{bmatrix}x_{1} \\ x_{2}\end{bmatrix}$, then $x_{1} + x_{2} =$

- [ ] A. $1$
- [ ] B. $4$
- [ ] C. $-3$
- [ ] D. $-\frac{1}{2}$
- [ ] E. $-2$

---

<a id="isomorphisms-of-vector-spaces"></a>
## Isomorphisms of Vector Spaces

Suppose we fix a basis

$$
\mathcal{B}=\{\mathbf{b}_1, \mathbf{b}_2, \ldots, \mathbf{b}_n \}
$$

of a finite-dimensional vector space $V$ with scalars from $\mathbb{R}$ (note that "finite-dimensional" means that the basis contains a finite number of elements). Then, we can represent any element $\mathbf{v} \in V$ using the coordinates of $\mathbf{v}$ relative to the basis $\mathcal{B}$:

$$
\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
$$

This defines a bijective mapping $\phi: V\to \mathbb{R}^n$. Moreover, this particular mapping has some nice properties:

- $\phi(\mathbf{u}+\mathbf{v}) = \phi(\mathbf{u}) + \phi(\mathbf{v})$ for any $\mathbf{u}, \mathbf{v} \in V$, and
- $\phi(\alpha \mathbf{v}) = \alpha \phi(\mathbf{v})$ for any $\mathbf{v} \in V$ and any $\alpha \in \mathbb{R}$.

Because of this, we say that $V$ is **isomorphic** to $\mathbb{R}^n$. Intuitively, this means that $V$ and $\mathbb R^n$ have identical structures.

So, any finite-dimensional vector space is isomorphic to $\mathbb{R}^n$ for some particular $n$. This means that instead of studying all possible vector spaces, we can simply study only one family of vector spaces, namely $\mathbb{R}^n$ for

$$
n\geq 1
$$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
