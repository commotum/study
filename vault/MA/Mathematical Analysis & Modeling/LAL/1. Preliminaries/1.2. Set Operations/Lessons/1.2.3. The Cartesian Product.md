# The Cartesian Product

<!--
lesson-id: 49
topic-code: LAL.1.2.3
-->

## Table of Contents

- [Introduction](#introduction)
- [The Cartesian Product](#the-cartesian-product)
- [Creating a Schematic for a Cartesian Product](#creating-a-schematic-for-a-cartesian-product)
- [Computing a Cartesian Product](#computing-a-cartesian-product)
- [The Cardinality of a Cartesian Product](#the-cardinality-of-a-cartesian-product)
- [Computing the Cardinality of a Cartesian Product](#computing-the-cardinality-of-a-cartesian-product)
- [Generalizing the Cartesian Product](#generalizing-the-cartesian-product)
- [Using Parentheses in Cartesian Products](#using-parentheses-in-cartesian-products)
- [Cartesian Products With Three Sets](#cartesian-products-with-three-sets)

## Prerequisites

- [Infinite Sets](<../../1.1. Introduction to Set Theory/Lessons/1.1.9. Infinite Sets.md>)

---

<a id="introduction"></a>
## Introduction

An **ordered pair** $(x, y)$ is a pair of mathematical objects $x$ and $y$ written in parentheses and separated by a comma.

You are already familiar with an ordered pair $(x,y)$ representing a point in the Cartesian plane. In this context, $x$ and $y$ are real numbers. However, more generally, the coordinates $x$ and $y$ of an ordered pair can be any mathematical objects we like! Moreover, they do not need to be the same type of object.

Here are some examples of ordered pairs.

$$
\begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix}
$$

Note the following:

- We say that an ordered pair $(x,y)$ has **coordinates** $x$ and $y$.
- Given two ordered pairs $(a, b)$ and $(c, d)$, we say they're **equal** if $a = c$ and $b = d$, and we write
$(a, b) = (c, d)$.
For example, $(2, 3) = (2, 3)$ but $(2, 3) \ne (3, 2)$.

We use ordered pairs to define the so-called **Cartesian product** of two sets. Let's discuss this in more detail.

---

<a id="the-cartesian-product"></a>
## The Cartesian Product

The **Cartesian product** of two sets $A$ and $B$, denoted $A \times B$, is a set containing all ordered pairs whose first coordinate belongs to $A$ and whose second coordinate belongs to $B$.

We define the Cartesian product using the following constructive definition:

$$
A \times B = \big\{(a, b) \:: \: a \in A,\, b \in B \big\}
$$

Consider the following finite sets:

$$
A = \{x, y\}, \qquad B = \{1, 2, 3\}
$$

To help visualize the Cartesian product $A\times B$, we'll use a visual schematic.

First, we create a table and list the elements of $A$ in a row across the top and the elements of $B$ in a column down the left side.

![](<../Source/1.2.3. The Cartesian Product/Images/eb9f361b470429af06506b8b9d2fe0ea.png>)

Then, at each location marked $\ast$, we write an ordered pair whose first coordinate is the corresponding element of $A$ and whose second coordinate is the corresponding element of $B$.

![](<../Source/1.2.3. The Cartesian Product/Images/c13c97f44f910322b7909a75905aaf5d.png>)

To write down the Cartesian product $A\times B$, we construct a set containing all ordered pairs in our schematic. This gives

$$
A \times B = \big\{(x, 1), (x, 2), (x, 3), (y, 1), (y, 2), (y, 3)\big\}
$$

Let's now consider $B\times A$. The schematic for this set is shown below.

![](<../Source/1.2.3. The Cartesian Product/Images/329e00a581c99c0da3ada2c2774b2f08.png>)

Therefore,

$$
B\times A = \big\{(1, x), (1, y), (2, x), (2, y), (3, x), (3, y)\big\}
$$

Since

$$
(a,b)\neq (b,a)
$$

we have that

$$
A\times B \neq B\times A
$$

in general.

We can also construct the Cartesian product

$$
A\times A = A^2
$$

The schematic for this set is given below.

![](<../Source/1.2.3. The Cartesian Product/Images/372a4ec5c70bdff91a6751e0d235e639.png>)

Therefore,

$$
A\times A = A^2 = \big\{(x, x), (x, y), (y, x), (y, y)\big\}
$$

Finally, note that the Cartesian product of *any* set with the empty set equals the empty set:

$$
A \times \emptyset = \emptyset, \qquad \emptyset \times A = \emptyset
$$

---

<a id="creating-a-schematic-for-a-cartesian-product"></a>
## Creating a Schematic for a Cartesian Product

**Example:** Construct a schematic of the set $A^2$ for $A = \{0,3 \}$.

**Explanation**

The set $A^2$ is equivalent to the Cartesian product

$$
A^2 = A \times A
$$

The Cartesian product $A \times B$ consists of all ordered pairs whose first coordinate belongs to $A$ and whose second coordinate belongs to $B$.

To create a schematic of the Cartesian product $A\times A$, we create a table and list the elements of the first set $A$ in a row across the top and the elements of the second set $A$ in a column down the left side.

![](<../Source/1.2.3. The Cartesian Product/Images/144ff19e358a073bcea04a9719793455.png>)

Then, at each intersection, we write an ordered pair whose first coordinate is the corresponding element of the first set and the second coordinate is the corresponding element of the second set.

Our completed $A \times A$ schematic is shown below.

![](<../Source/1.2.3. The Cartesian Product/Images/4f01eda6f1609be0d129be13ac2d565c.png>)

---

**Question 1**

Which of the following is a schematic of the Cartesian product $A \cdot B$ for $A = {1, 10, 100}$ and $B = {a, b}$?

- [ ] A. ![](<../Source/1.2.3. The Cartesian Product/Images/q-181287-a-3.png>)
- [ ] B. ![](<../Source/1.2.3. The Cartesian Product/Images/q-181287-a-4.png>)
- [ ] C. ![](<../Source/1.2.3. The Cartesian Product/Images/q-181287-a-1.png>)
- [ ] D. ![](<../Source/1.2.3. The Cartesian Product/Images/q-181287-a-2.png>)
- [ ] E. ![](<../Source/1.2.3. The Cartesian Product/Images/q-181287-a-5.png>)

---

**Question 2**

![](<../Source/1.2.3. The Cartesian Product/Images/q-181293.png>)

Using the visual schematic above for the Cartesian product $A^{2}$ for $A = {-1, 1}$, determine which of the following statements are true.

1. $(1,-1) ∈ A^{2}$
2. $1 ∈ A^{2}$
3. $(-1, 1) ∈ A^{2}$

- [ ] A. I and III only
- [ ] B. III only
- [ ] C. I only
- [ ] D. I and II only
- [ ] E. II and III only

---

<a id="computing-a-cartesian-product"></a>
## Computing a Cartesian Product

**Example:** For the sets $A = \{3,4,7\}$, $B = \{f,g\}$, and $C = A \times B$, determine which of the following statements are true.

1. $(f,4) \in C$
2. $(7,g) \in C$
3. $(3,f) \in C$

**Explanation**

The Cartesian product $A \times B$ consists of all ordered pairs whose first coordinate belongs to $A$ and whose second coordinate belongs to $B$.

In this case, the schematic for $A \times B$ is shown below.

![](<../Source/1.2.3. The Cartesian Product/Images/488cc12c3ff8d8b797a5ecd57846bd45.png>)

Hence, the set

$$
C = A \times B
$$

is given by

$$
C = \big\{\bbox[2pt, Gainsboro]{\color{blue}(3,f)}, (3, g), (4, f), (4, g), (7, f), \bbox[2pt, Gainsboro]{\color{blue}(7,g)}\big\}
$$

Therefore, the correct answer is "II and III only."

---

**Question 3**

For the sets $A = {2, 4, 6}$, $B = {x, y}$, and $C = A \cdot B$, determine which of the following statements are true.

1. $(2, 4) ∈ C$
2. $(2, x) ∈ C$
3. $(3, y) ∈ C$

- [ ] A. II and III only
- [ ] B. III only
- [ ] C. II only
- [ ] D. I only
- [ ] E. I and II only

---

**Question 4**

For the sets $Z = {-1, 0, 1}$ and $Y = Z \cdot Z$, determine which of the following statements are true.

1. $(-1, 0, 1) ∈ Y$
2. $(-1, 1) ∈ Y$
3. $(0, 0) ∈ Y$

- [ ] A. II only
- [ ] B. I and II only
- [ ] C. III only
- [ ] D. II and III only
- [ ] E. I and III only

---

<a id="the-cardinality-of-a-cartesian-product"></a>
## The Cardinality of a Cartesian Product

Consider the following two sets:

$$
A = \{x,y\}, \qquad B = \{1,2,3\}
$$

What is the cardinality of $A\times B$? In other words, how many elements are contained in this set?

Let's start by visualizing our $A\times B$ schematic:

![](<../Source/1.2.3. The Cartesian Product/Images/75f28fc1c031c2d1a3781857ce28e5a2.png>)

Notice that our schematic has

- $2$ columns, corresponding to the number of elements in $A$ (i.e., $\mid A|=2$), and
- $3$ rows, corresponding to the number of elements in $B$ (i.e., $\mid B|=3$).

Therefore,

$$
\begin{vmatrix}A\times B & = & A & \cdot & B\end{vmatrix} = 2\cdot 3 = 6
$$

In general, if $A$ and $B$ are *finite* sets, then

$$
\begin{vmatrix}A \times B & = & A & \cdot & B\end{vmatrix}
$$

---

<a id="computing-the-cardinality-of-a-cartesian-product"></a>
## Computing the Cardinality of a Cartesian Product

**Example:** If $C = \{3, 4, 7, 9, 11 \}$ and $D =\{1, 2, 3, \ldots, 10 \}$, what is the cardinality of $C \times D$?

**Explanation**

For any two finite sets $C$ and $D$, the cardinality of $C \times D$ is

$$
\begin{vmatrix}C \times D & = & C & \cdot & D\end{vmatrix}
$$

In this case, we have

$$
\begin{aligned}
\begin{vmatrix}C \cdot D & &= & C & \cdot & D\end{vmatrix} \\
&= 5 \cdot 10 \\
&= 50
\end{aligned}
$$

---

**Question 5:** What is the cardinality of $G \cdot H$ for $G = {1, 2, 3,..., 10}$ and $H = {1, 2, 3, 4, 5}$?

- [ ] A. $50$
- [ ] B. $45$
- [ ] C. $15$
- [ ] D. $25$
- [ ] E. $100$

---

**Question 6:** What is the cardinality of $Z \cdot Y$ for $Z = {1, 4, 9, 16, 25, 36}$ and $Y = {2, 3, 7, 9}$?

- [ ] A. $4$
- [ ] B. $24$
- [ ] C. $14$
- [ ] D. $10$
- [ ] E. $6$

---

<a id="generalizing-the-cartesian-product"></a>
## Generalizing the Cartesian Product

We can extend the definition of the Cartesian product to include three or more sets.

For example, if we have three sets $A,B$, and $C$, then the Cartesian product $A\times B\times C$ is defined as

$$
A\times B\times C = \big\{(a, b, c) \:: \: a \in A,\, b \in B, c\in C \big\}
$$

In other words, $A\times B\times C$ contains all **ordered triples** $(a,b,c)$ such that $a$ belongs to $A, b$ belongs to $B$, and $c$ belongs to $C$.

For example, if

$$
A=\{{\color{blue}{1}},{\color{blue}{2}}\},\quad B=\{{\color{red}{x}}, {\color{red}{y}}\},\quad \quad C=\{z\}
$$

then

$$
A\times B\times C = \Big\{({\color{blue}{1}},{\color{red}{x}},z),\, ({\color{blue}{1}},{\color{red}{y}},z),\, ({\color{blue}{2}},{\color{red}{x}},z),\, ({\color{blue}{2}},{\color{red}{y}},z)\Big\}
$$

We can extend the cardinality rule as follows:

$$
\begin{vmatrix}A\times B\times C & = & A & \cdot & B & \cdot & C\end{vmatrix}
$$

So, in the case above,

$$
\begin{vmatrix}A\times B\times C & = & A & \cdot & B & \cdot & C\end{vmatrix} = 2\cdot2\cdot 1 = 4
$$

Nothing stops us from defining the Cartesian product of any finite number of sets! So, if $A_1, A_2, \ldots, A_n$ denotes a collection of $n$ sets, then

$$
A_1\times A_2\times\cdots\times A_n = \Big\{(a_1, a_2, \ldots, a_n) \:: \: a_i \in A_i, 1\leq i\leq n \Big\}
$$

where $(a_1,a_2,\ldots,a_n)$ is an **ordered tuple**.

Once again, the cardinality rule extends intuitively:

$$
\begin{vmatrix}A_1\times A_2\times \cdots \times A_n & = & A_1 & \cdot & A_2 & \cdots & A_n\end{vmatrix}
$$

The **Cartesian power** $A^n$ of a set $A$ for natural $n$ is defined as

$$
A^n = \underbrace{A\times A\times\cdots\times A}_{n \textrm{times}} = \Big\{(a_1, a_2, \ldots, a_n) \:: \: a_i \in A, 1\leq i\leq n \Big\}
$$

For example,

$$
\begin{aligned}
A^{3} &= A \cdot A \cdot A \\
&= {1, 2} \cdot {1, 2} \cdot {1, 2} \\
&= {(1, 1, 1)(1, 1, 2)(1, 2, 1)(1, 2, 2)(2, 1, 1)(2, 1, 2)(2, 2, 1)(2, 2, 2)}
\end{aligned}
$$

---

<a id="using-parentheses-in-cartesian-products"></a>
## Using Parentheses in Cartesian Products

We must pay special attention whenever parentheses are used in Cartesian products.

In general,

$$
A \cdot B \cdot C \mid \ne A \cdot (B \cdot C), andA \cdot B \cdot C \ne (A \cdot B) \cdot C
$$

and we also have that

$$
A \cdot (B \cdot C) \ne (A \cdot B) \cdot C
$$

To see why, note that $A\times (B\times C)$ denotes the Cartesian product of $A$ with $B\times C$. Expressing it formally using a constructive definition, we have

$$
A\times (B\times C) = \Big\{\left(a, (b, c)\right) \:: \: a \in A,\, (b,c) \in B\times C \Big\}
$$

In other words, $A\times (B\times C)$ contains all ordered *pairs* (not triples) such that the first coordinate $a$ belongs to $A$, and the second coordinate is an ordered pair $(b,c)$ belonging to $B\times C$.

Similarly,

$$
(A\times B)\times C = \Big\{\left((a, b), c\right) \:: \: (a, b) \in A\times B,\, c\in C \Big\}
$$

To understand this concretely, let's recall the sets $A,B$, and $C$ defined earlier

$$
A=\{{\color{blue}{1}},{\color{blue}{2}}\},\quad B=\{{\color{red}{x}}, {\color{red}{y}}\},\quad \quad C=\{z\}
$$

where we found that

$$
A\times B\times C = \Big\{({\color{blue}{1}},{\color{red}{x}},z),\, ({\color{blue}{1}},{\color{red}{y}},z),\, ({\color{blue}{2}},{\color{red}{x}},z),\, ({\color{blue}{2}},{\color{red}{y}},z)\Big\}
$$

Let's calculate $A\times (B\times C)$ and $(A\times B)\times C$ for these sets:

- To calculate $A\times (B\times C)$, we first note that
$B\times C = \big\{({\color{red}{x}},z), ({\color{red}{y}},z)\big\}$
and so the product $A\times (B\times C)$ is
$A \cdot (B \cdot C)|= {1, 2} \cdot {(x, z), (y, z)}; = {(1, (x, z)), (1, (y, z)), (2, (x, z)), (2, (y, z))}; \ne A \cdot B \cdot C$.
- Similarly, to calculate $(A\times B)\times C$, we first note that
$A\times B = \big\{({\color{blue}{1}},{\color{red}{x}}),\, ({\color{blue}{1}},{\color{red}{y}}),\, ({\color{blue}{2}},{\color{red}{x}}),\, ({\color{blue}{2}},{\color{red}{y}}) \big\}$
and so the product $A\times (B\times C)$ is
$(A \cdot B) \cdot C|= {(1, x), (1, y), (2, x), (2, y)} \cdot {z}; = {((1, x), z), ((1, y), z), ((2, x), z), ((2, y), z)}; \ne A \cdot B \cdot C$.

Finally, note that the cardinalities of all three sets are equal:

$$
\begin{vmatrix}A\times B\times C & = & A\times (B\times C) & = & (A\times B)\times C\end{vmatrix}
$$

---

<a id="cartesian-products-with-three-sets"></a>
## Cartesian Products With Three Sets

**Example:** If $A = \{p,q \}$ and $B = \{4,9 \}$, which of the following elements belong to $(A \times B) \times B$?

1. $((p,4),9)$
2. $((q,9),4)$
3. $(p,4,9)$

**Explanation**

The Cartesian product $A \times B$ consists of all ordered pairs whose first coordinate belongs to $A$ and whose second coordinate belongs to $B$.

In this case, $A \times B$ consists of every combination of $\{p,q \}$ with $\{4,9 \}$, as shown in the schematic below.

![](<../Source/1.2.3. The Cartesian Product/Images/41f76e800a83fdbf548fb4a281d338f4.png>)

Hence, the set $A \times B$ is given by

$$
A \times B = \{(p,4), (p,9), (q,4), (q,9)\}
$$

Note that

$$
\begin{vmatrix}A \times B & = & A & \cdot & B\end{vmatrix} = 2 \cdot 2 = 4
$$

Now, the Cartesian product
$(A \times B) \times B$
consists of every combination of $A \times B$ and $B$, as shown in the schematic below.

![](<../Source/1.2.3. The Cartesian Product/Images/12cd1413549bdde65ed735cf3e17b99f.png>)

Note that

$$
\begin{vmatrix}(A \times B) \times B & = & A \times B & \cdot & B\end{vmatrix} = 4 \cdot 2 = 8
$$

From the given options, we see that both $\bbox[2pt,Gainsboro]{\color{blue}((p, 4), 9)}$ and $\bbox[2pt,Gainsboro]{\color{blue}((q,9),4)}$ are in $(A \times B) \times B$.

Therefore, the correct answer is "I and II only."

---

**Question 7**

If $A = {-1, 1}$ and $B = {u, v}$, which of the following elements belong to $B \cdot B \cdot A$?

1. $(v, v, 1)$
2. $((v, v), 1)$
3. $(1, v, u)$

- [ ] A. II only
- [ ] B. I and II only
- [ ] C. I and III only
- [ ] D. III only
- [ ] E. I only

---

**Question 8**

If $A = {0, 1, 2}$ and $B = {x, y}$, which of the following elements belong to $(A \cdot B) \cdot A$?

1. $(1, y, 2)$
2. $((1, y), 2)$
3. $((y, 1), y)$

- [ ] A. I and II only
- [ ] B. I only
- [ ] C. III only
- [ ] D. II only
- [ ] E. II and III only

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
