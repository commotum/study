# Markov Chains

<!--
lesson-id: 1911
topic-code: LAL.8.2.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Stochastic Vectors and Stochastic Matrices](#identifying-stochastic-vectors-and-stochastic-matrices)
- [Constructing a Stochastic Matrix Given Some Data](#constructing-a-stochastic-matrix-given-some-data)
- [Markov Chains](#markov-chains)
- [Calculating a State Vector](#calculating-a-state-vector)
- [Constructing a Stochastic Matrix and Making a Prediction](#constructing-a-stochastic-matrix-and-making-a-prediction)

## Prerequisites

- [The Complement of an Event](<../../../../GEO/6. Probability & Combinatorics/6.1. Introduction to Probability/Lessons/6.1.5. The Complement of an Event.md>)
- [Multiplying a Matrix by a Column Vector](<../../../../PCL/6. Matrices/6.2. Matrix Multiplication/Lessons/6.2.1. Multiplying a Matrix by a Column Vector.md>)
- [Recursive Sequences](<../../../../AG1/11. Sequences/11.1. Introduction to Sequences/Lessons/11.1.2. Recursive Sequences.md>)

---

<a id="introduction"></a>
## Introduction

We can use repeated matrix multiplication to determine the state of a system given its initial state and some information about how one state transitions to the next state.

To explain this concretely, suppose we have a population where people live either in the city or the countryside. A study carried out by the local authority shows that, each year,

- $6\%$ of the city population relocates to the countryside,
- $3\%$ of the countryside population relocates to the city, and
- everyone else remains where they are.

This information can be represented using a diagram, like the one below.

![](<../Source/8.2.1. Markov Chains/Images/607fa544e0da73963380a042ae01d18e.png>)

We can also present this information as a table:

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **City** | **Countryside** |
| To: | **City** | $0.94$ | $0.03$ |
| **Countryside** | $0.06$ | $0.97$ |  |

We want to use this information to determine how the population will be distributed $n$ years after the study was carried out.

First, let's turn this table into a matrix $P$:

$$
\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}
$$

Notice that this matrix has the following properties:

- it is a square matrix
- the sum of the entries in each column equals $1$
- all entries in the matrix are non-negative

A square matrix with these properties is called a **stochastic matrix**.

Now suppose that, at the time of the study, $70\%$ of the population lived in the city and $30\%$ in the countryside. We can represent this **initial state** using a so-called **stochastic vector** $\mathbf x_0$, given by

$$
\begin{bmatrix}0.70 \\ 0.30\end{bmatrix}
$$

Stochastic vectors contain non-negative entries only, and the sum of the entries equals $1$. Note that each column of $P$ is a stochastic vector.

Now that we have our stochastic matrix and initial state vector, we can use matrix multiplication to calculate the population distribution sometime after the initial study took place:

- To calculate the population distribution after one year, we calculate the product $P\mathbf x_0$:
$\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}$
The resulting vector
$\begin{bmatrix}0.667 \\ 0.333\end{bmatrix}$
tells us that one year after the study, $66.7$% of the population will live in the city, and $33.3$% will live in the countryside.
- To model the distribution after $2$ years, we compute $P\mathbf x_1$:
$\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}$
The resulting vector
$\begin{bmatrix}0.637 \\ 0.363\end{bmatrix}$
tells us that two years after the study, $63.7$% of the population will live in the city, and $36.3$% will live in the countryside.

---

<a id="identifying-stochastic-vectors-and-stochastic-matrices"></a>
## Identifying Stochastic Vectors and Stochastic Matrices

**Example:** Which of the following are stochastic matrices?

$\begin{bmatrix}-0.2 & 0 \\ 1.2 & 1\end{bmatrix}$

**Explanation**

Recall that a stochastic vector is a vector with nonnegative entries that add up to $1$. A matrix is stochastic if all its columns are stochastic vectors.

With that in mind, let's examine our matrices in turn.

- Matrix $\bf{U}$ is *not* a stochastic matrix since its first column is not a stochastic vector (it contains negative entries):
$\begin{bmatrix}-0.2 & 0 \\ 1.2 & 1\end{bmatrix}$
- Matrix $\bf{V}$ is a stochastic matrix. Indeed, each column is a stochastic vector (the corresponding entries are nonnegative and add up to one):
$\begin{bmatrix}0.35 & 0.95 \\ 0.65 & 0.05\end{bmatrix}0.35 + 0.65 = 1; 0.95 + 0.05 = 1$
- Matrix $\bf{W}$ is *not* a stochastic matrix since its third column is not a stochastic vector (the corresponding entries don't add up to one):
$\begin{bmatrix}0 & 1 & 0 \\ 0.5 & 0 & 0.4 \\ 0.5 & 0 & 0.7\end{bmatrix}$

Therefore, the correct answer is "$\bf{V}$ only."

---

**Question 1**

Which of the following are stochastic vectors?

$u = \begin{bmatrix}0.5 \\ 0 \\ 0.5\end{bmatrix}, v = \begin{bmatrix}-0.45 \\ 1.55\end{bmatrix}, w = \begin{bmatrix}0 \\ 0\end{bmatrix}$

- [ ] A. $u$ and $w$ only
- [ ] B. $u$, $v$, and $w$
- [ ] C. $u$ and $v$ only
- [ ] D. $v$ only
- [ ] E. $u$ only

---

**Question 2**

Which of the following are stochastic matrices?

$U = \begin{bmatrix}0.2 & 0 \\ 0.5 & 1\end{bmatrix}, V = \begin{bmatrix}0.45 & 0.25 \\ 0.55 & 0.75\end{bmatrix}, W = \begin{bmatrix}0 & 1 & 0 \\ 0.5 & 0.2 & 0.3 \\ 0.5 & 0 & 0.7\end{bmatrix}$

- [ ] A. $V$ and $W$ only
- [ ] B. $W$ only
- [ ] C. $V$ only
- [ ] D. $U$, $V$, and $W$
- [ ] E. $U$ and $V$ only

---

<a id="constructing-a-stochastic-matrix-given-some-data"></a>
## Constructing a Stochastic Matrix Given Some Data

**Example:** There are two groups of people in a particular city: those who use their own vehicles and those who use public transport. A study has shown that, each year

- $35\%$ of people who used their own vehicles swap to public transport, and
- $25\%$ of people who used public transport swap to their own vehicles.

Give the stochastic matrix that describes how people change their means of transportation each year.

**Explanation**

We are given the following situation for each year:

- $35\%$ of people who used their own vehicles swap to public transport. This implies that $65\%$ of them remain using their own vehicles.
- $25\%$ of people who used public transport swap to their own vehicles. This implies that $75\%$ of them remain using public transport.

Writing this information in tabular form, we obtain the following:

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Vehicles** | **Public Transport** |
| To: | **Vehicles** | $0.65$ | $0.25$ |
| **Public Transport** | $0.35$ | $0.75$ |  |

Note that we can write a corresponding stochastic matrix as follows:
$\begin{bmatrix}0.65 & 0.25 \\ 0.35 & 0.75\end{bmatrix}$

---

**Question 3**

In a particular country, there are two groups of people: those who use trains to travel and those who use cars. A study has shown that, each year

- $42%$ of people who used trains swap to cars, and
- $33%$ of people who used cars swap to trains.

Which of the following tables describes how these people change their preferences each year?

- [ ] A.

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Trains** | **Cars** |
| To: | **Trains** | $0.58$ | $0.33$ |
| **Cars** | $0.42$ | $0.67$ |  |

- [ ] B.

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Trains** | **Cars** |
| To: | **Trains** | $0.42$ | $0.67$ |
| **Cars** | $0.58$ | $0.33$ |  |

- [ ] C.

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Trains** | **Cars** |
| To: | **Trains** | $0.42$ | $0.33$ |
| **Cars** | $0.58$ | $0.67$ |  |

- [ ] D.

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Trains** | **Cars** |
| To: | **Trains** | $0.58$ | $0.67$ |
| **Cars** | $0.42$ | $0.33$ |  |

- [ ] E.

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Trains** | **Cars** |
| To: | **Trains** | $0.67$ | $0.58$ |
| **Cars** | $0.33$ | $0.42$ |  |

---

**Question 4**

There are three groups of fans in a certain town: those who prefer basketball, those who prefer baseball, and those who prefer football. A study has shown that, each year

- from the people that prefer basketball, $32%$ change to baseball and $28%$ change to football,
- from the people that prefer baseball, $45%$ change to football and $15%$ change to basketball,
- from the people that prefer football, $25%$ change to basketball and $65%$ change to baseball.

The stochastic matrix that describes this situation is given below. What is the value of $a + b + c$?

$P = \begin{bmatrix}0.4 & 0.15 & a \\ 0.32 & b & 0.65 \\ c & 0.45 & 0.1\end{bmatrix}$

- [ ] A. $1.07$
- [ ] B. $0.93$
- [ ] C. $0.99$
- [ ] D. $0.87$
- [ ] E. $1$

---

<a id="markov-chains"></a>
## Markov Chains

Let's reconsider the population we looked at earlier, where the stochastic vector that represents the initial distribution of people between the city and the countryside is

$\begin{bmatrix}0.70 \\ 0.30\end{bmatrix}$
and the yearly change in this distribution is represented by the stochastic matrix

$$
\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}
$$

Earlier, we found that the population distribution after $1$ and $2$ years is given by the vectors $\mathbf x_1$ and $\mathbf x_2$,
respectively, where

$$
\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}
$$

and

$$
\begin{bmatrix}0.94 & 0.03 \\ 0.06 & 0.97\end{bmatrix}
$$

The vectors $\mathbf{x}_0, \mathbf{x}_1, \mathbf{x}_2, \ldots$ are called **state vectors**, and $\mathbf x_0$ is the **initial state**.

To find the state vector corresponding to the $(k+1)$th year, we left-multiply the state vector corresponding to the $k$th year by $P$. This gives rise to the following sequence:

$$
\begin{aligned}
x_{1} &= Px_{0}, \\
x_{2} &= Px_{1}, \\
x_{3} &= Px_{2}, \\
⋮\begin{vmatrix}⋮ \\ x_{k + 1} &= Px_{k} \\ ⋮\end{vmatrix}⋮
\end{aligned}
$$

A sequence of stochastic vectors $\mathbf{x}_0, \mathbf{x}_1, \mathbf{x}_2, \ldots$ related to a stochastic matrix $P$ in this way is called a **Markov chain**.

---

<a id="calculating-a-state-vector"></a>
## Calculating a State Vector

**Example:** $\begin{bmatrix}0.5 & 0.1 \\ 0.5 & 0.9\end{bmatrix}$

Consider a Markov chain with the stochastic matrix $P$ and the initial state vector $\mathbf{x}_0$ given above. Calculate the state vector $\mathbf{x}_2$.

**Explanation**

To find the state vector $\mathbf{x}_2$, we apply two steps of the Markov chain:

$$
\mathbf{x_1}=P\mathbf{x}_0, \qquad \mathbf{x}_2=P\mathbf{x}_1
$$

After the first step, we have

$$
\begin{aligned}
x_{1} &= Px_{0} \\
&= \begin{bmatrix}0.5 & 0.1 \\ 0.5 & 0.9\end{bmatrix}\begin{bmatrix}0.2 \\ 0.8\end{bmatrix} \\
&= \begin{bmatrix}0.18 \\ 0.82\end{bmatrix}
\end{aligned}
$$

Finally, after the second step, we obtain

$$
\begin{aligned}
x_{2} &= Px_{1} \\
&= \begin{bmatrix}0.5 & 0.1 \\ 0.5 & 0.9\end{bmatrix}\begin{bmatrix}0.18 \\ 0.82\end{bmatrix} \\
&= \begin{bmatrix}0.172 \\ 0.828\end{bmatrix}
\end{aligned}
$$

---

**Question 5**

> A calculator is required to answer this question.

$P = \begin{bmatrix}0.4 & 0.2 & 0.2 \\ 0.3 & 0.4 & 0.5 \\ 0.3 & 0.4 & 0.3\end{bmatrix}, x_{0} = \begin{bmatrix}0.2 \\ 0.6 \\ 0.2\end{bmatrix}$

Consider a Markov chain with the stochastic matrix $P$ and the initial state vector $x_{0}$ given above. Calculate the state vector $x_{1}$.

- [ ] A. $\begin{bmatrix}0.24 \\ 0.4 \\ 0.36\end{bmatrix}$
- [ ] B. $\begin{bmatrix}0.12 \\ 0.52 \\ 0.36\end{bmatrix}$
- [ ] C. $\begin{bmatrix}0.28 \\ 0.38 \\ 0.34\end{bmatrix}$
- [ ] D. $\begin{bmatrix}0.2 \\ 0.42 \\ 0.56\end{bmatrix}$
- [ ] E. $\begin{bmatrix}0.24 \\ 0.32 \\ 0.44\end{bmatrix}$

---

**Question 6**

> A calculator is required to answer this question.

$P = \begin{bmatrix}0.2 & 0.5 \\ 0.8 & 0.5\end{bmatrix}, x_{0} = \begin{bmatrix}0.7 \\ 0.3\end{bmatrix}$

Consider a Markov chain with the stochastic matrix $P$ and the initial state vector $x_{0}$ given above. Calculate the state vector $x_{2}$.

- [ ] A. $\begin{bmatrix}0.393 \\ 0.607\end{bmatrix}$
- [ ] B. $\begin{bmatrix}0.611 \\ 0.389\end{bmatrix}$
- [ ] C. $\begin{bmatrix}0.442 \\ 0.558\end{bmatrix}$
- [ ] D. $\begin{bmatrix}0.413 \\ 0.587\end{bmatrix}$
- [ ] E. $\begin{bmatrix}0.526 \\ 0.474\end{bmatrix}$

---

<a id="constructing-a-stochastic-matrix-and-making-a-prediction"></a>
## Constructing a Stochastic Matrix and Making a Prediction

**Example:** A certain industry produces $2$ types of sugar, namely sugar $A$ and sugar $B$. The following table describes how the number of consumers changes between both types of sugar in a month:

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Sugar $A$** | **Sugar $B$** |
| To: | **Sugar $A$** | $0.3$ | $0.45$ |
| **Sugar $B$** | $0.7$ | $0.55$ |  |

That is, $70\%$ of sugar $A$ customers switch to sugar $B$ each month, and $45\%$ of sugar $B$ customers switch to sugar $A$.

Currently, $600$ customers use sugar $A$, while $400$ customers use sugar $B$. How many customers will use sugar $A$ in one month? You may assume that the total number of customers doesn't change.

**Explanation**

From the table, we can build the corresponding stochastic matrix:

$$
\begin{bmatrix}0.3 & 0.45 \\ 0.7 & 0.55\end{bmatrix}
$$

The vector describing the number of customers for each type of sugar is
$\begin{bmatrix}600 \\ 400\end{bmatrix}$
Dividing the vector above by the total number of customers $(1\,000)$, we get the stochastic vector of the initial state:

$$
\begin{bmatrix}600 \\ 400\end{bmatrix}
$$

To find the state vector $\mathbf{x}_1$ after one month, we apply one step of the Markov chain:

$$
\mathbf{x}_1=P\mathbf{x}_0
$$

Therefore, we have

$$
\begin{aligned}
x_{1} &= Px_{0} \\
&= \begin{bmatrix}0.3 & 0.45 \\ 0.7 & 0.55\end{bmatrix}\begin{bmatrix}0.6 \\ 0.4\end{bmatrix} \\
&= \begin{bmatrix}0.36 \\ 0.64\end{bmatrix}
\end{aligned}
$$

So, after one month, $\color{blue}36\%$ of customers will use sugar $A$. Computing $36\%$ of $1\,000$, we obtain

$$
1\,000 \cdot 0.36 = 360
$$

Therefore, $360$ customers will use sugar $A$ in one month.

---

**Question 7**

> A calculator is required to answer this question.

A certain company produces two types of smartphones: Phone $A$ and Phone $B$. The following table describes how the number of customers changes between both types of smartphones each year:

|  |  | From: |  |
| --- | ---: | ---: | ---: |
|  |  | **Phone $A$** | **Phone $B$** |
| To: | **Phone $A$** | $0.6$ | $0.3$ |
| **Phone $B$** | $0.4$ | $0.7$ |  |

That is, $40%$ of Phone $A$ customers switch to Phone $B$ each year, and $30%$ of Phone $B$ customers switch to Phone $A$.

Currently, $600000$ customers use Phone $A$, while $400000$ customers use Phone $B$. How many customers will use Phone $A$ in one year? You may assume that the total number of customers doesn't change.

- [ ] A. $400000$
- [ ] B. $480000$
- [ ] C. $520000$
- [ ] D. $430000$
- [ ] E. $640000$

---

**Question 8**

> A calculator is required to answer this question.

A certain company produces three types of cars: Standard, Sports, and Deluxe. The following table describes how the number of customers changes between the three types of cars each year:

|  |  | From: |  |  |
| --- | ---: | ---: | ---: | ---: |
|  |  | **Standard** | **Sports** | **Deluxe** |
| To: | **Standard** | $0.6$ | $0.1$ | $0.1$ |
| **Sports** | $0.2$ | $0.6$ | $0.2$ |  |
| **Deluxe** | $0.2$ | $0.3$ | $0.7$ |  |

For example, $20%$ of Standard customers switch to Sports each year, and $30%$ of Sports customers switch to Deluxe.

Currently, $500000$ customers use Standard, $400000$ customers use Sports and $100000$ customers use Deluxe. How many customers will use Sports in one year? You may assume that the total number of customers doesn't change.

- [ ] A. $320000$
- [ ] B. $280000$
- [ ] C. $290000$
- [ ] D. $360000$
- [ ] E. $350000$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
