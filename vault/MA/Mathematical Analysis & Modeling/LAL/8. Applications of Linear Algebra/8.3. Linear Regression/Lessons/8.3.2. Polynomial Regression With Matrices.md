# Polynomial Regression With Matrices

<!--
lesson-id: 2170
topic-code: LAL.8.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding an Expression for a Least-Squares Regression Curve](#finding-an-expression-for-a-least-squares-regression-curve)
- [Identifying the Elements of a Polynomial Regression Model](#identifying-the-elements-of-a-polynomial-regression-model)
- [Regression Curves Containing Zero Coefficients](#regression-curves-containing-zero-coefficients)
- [Identifying the Design Matrix When the Regression Curve Contains Zero Coefficients](#identifying-the-design-matrix-when-the-regression-curve-contains-zero-coefficients)

## Prerequisites

- [Selecting a Regression Model](<../../../../AG1/12. Bivariate Statistics/12.1. Correlation & Regression/Lessons/12.1.6. Selecting a Regression Model.md>)
- [Linear Regression With Matrices](<8.3.1. Linear Regression With Matrices.md>)

---

<a id="introduction"></a>
## Introduction

We've seen how to construct a simple linear regression model given some bivariate data $(x,y)$. We can generalize this technique to fit data to an arbitrary polynomial. We call this process **polynomial regression**.

The following table gives the number of units $x$ (in hundreds) produced by a small company and the respective profits $y$ (in thousands of dollars) earned for producing $x$ units:

| Goods $(x)$ | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: |
| Profit $(y)$ | $16$ | $11$ | $15$ | $18$ |

Suppose we want to find the quadratic function

$$
y = \beta_0 + \beta_1 x + \beta_2 x^2
$$

that best fits the given data points.

![](<../Source/8.3.2. Polynomial Regression With Matrices/Images/151c1d8d5104005ab862cb6ace27196d.png>)

Substituting the values of $x$ into the regression equation and equating the results to the corresponding observed values of $y$, we obtain the following system:

$$
{β_{0} + 1β_{1} + 1^{2}β_{2} = 16; β_{0} + 2β_{1} + 2^{2}β_{2} = 11; β_{0} + 3β_{1} + 3^{2}β_{2} = 15; β_{0} + 4β_{1} + 4^{2}β_{2} = 18
$$

This system can be written using matrix notation as

$$
\begin{bmatrix}1 & 1 & 1^{2} \\ 1 & 2 & 2^{2} \\ 1 & 3 & 3^{2} \\ 1 & 4 & 4^{2}\end{bmatrix}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

Since our points do not lie on a quadratic curve, there will be no parameters $\beta_0, \beta_1$, and $\beta_2$ such that this equation is true. So instead, we seek parameters $\beta_0, \beta_1$, and $\beta_2$ that best approximate the solution.

In other words, we seek to find a column vector $\boldsymbol\beta$ such that

$$
\| \mathbf{y} - X\boldsymbol{\beta}\|
$$

is as small as possible.

Notice that this is exactly the least-squares problem for the system

$$
X \boldsymbol{\beta} = \mathbf{y}
$$

Therefore, the coefficients of the curve that best fits the data can be found as the solution to the corresponding least-squares problem!

Pre-multiplying the equation

$$
X \boldsymbol{\beta} = \mathbf{y}
$$

by $X^T$, we obtain the corresponding normal equation

$$
X^T \! X {\hat{\boldsymbol{\beta}}} = X^T \mathbf{y}
$$

Since the columns of $X$ are linearly independent, the matrix ${X^T}X$ is invertible, and

$$
{\hat{\boldsymbol{\beta}}} = (X^T \! X)^{-1} X^T \mathbf{y}
$$

Substituting $X$ and $\mathbf{y}$ into the formula above and simplifying, we get

$$
β^(^) = \begin{bmatrix}22.5 \\ -9 \\ 2\end{bmatrix}
$$

Therefore, the quadratic curve of best fit (regression curve) is

$$
y = 22.5 - 9x + 2x^2
$$

---

<a id="finding-an-expression-for-a-least-squares-regression-curve"></a>
## Finding an Expression for a Least-Squares Regression Curve

**Example:** Consider the data above and the matrices below. Find an expression (in terms of $X$ and $\mathbf{y}$) for the vector of coefficients $\boldsymbol{\beta}$ of the curve $y=\beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3$ that best fits the data.

| $x$ | **$0$** | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $1$ | $2$ | $3$ | $5$ | $2$ |

$\begin{bmatrix}1 & 0 & 0^{2} & 0^{3} \\ 1 & 1 & 1^{2} & 1^{3} \\ 1 & 2 & 2^{2} & 2^{3} \\ 1 & 3 & 3^{2} & 3^{3} \\ 1 & 4 & 4^{2} & 4^{3}\end{bmatrix}$

**Explanation**

The coefficients of the curve

$$
y=\beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3
$$

that best fits the data can be found as the least-squares solution ${\hat{\boldsymbol{\beta}}}$ to the system

$$
X \boldsymbol{\beta} = \mathbf{y}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

Pre-multiplying this equation by $X^T$, we obtain the corresponding normal equation

$$
X^T \! X {\hat{\boldsymbol{\beta}}} = X^T \mathbf{y}
$$

Since the columns of $X$ are linearly independent, the matrix ${X^T}X$ is invertible and

$$
{\hat{\boldsymbol{\beta}}} = (X^T \! X)^{-1} X^T \mathbf{y}
$$

---

**Question 1**

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $3$ | $-2$ | $-1$ | $1$ | $-1$ |

Consider the data above and the matrices below. Find an expression for the vector of coefficients of the curve $y = β_{0} + β_{1}x + β_{2}x^{2}$ that best fits the data.

$X = \begin{bmatrix}1 & 1 & 1^{2} \\ 1 & 2 & 2^{2} \\ 1 & 3 & 3^{2} \\ 1 & 4 & 4^{2} \\ 1 & 5 & 5^{2}\end{bmatrix}, β = \begin{bmatrix}β_{0} \\ β_{1} \\ β_{2}\end{bmatrix}, y = \begin{bmatrix}3 \\ -2 \\ -1 \\ 1 \\ -1\end{bmatrix}$

- [ ] A. $(X^{T}X)^{-1}X^{T}y$
- [ ] B. $X^{T}(XX^{T})^{-1}y$
- [ ] C. $(XX^{T})^{-1}Xy$
- [ ] D. $X^{T}(XX^{T})^{-1}Xy$
- [ ] E. $X(X^{T}X)^{-1}X^{T}y$

---

**Question 2**

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $2$ | $-3$ | $-2$ | $0$ | $1$ |

Consider the data above and the matrices below. Find an expression for the vector of coefficients of the curve $y = β_{0} + β_{1}x + β_{2}x^{2} + β_{3}x^{3}$ that best fits the data.

$X = \begin{bmatrix}1 & 1 & 1^{2} & 1^{3} \\ 1 & 2 & 2^{2} & 2^{3} \\ 1 & 3 & 3^{2} & 3^{3} \\ 1 & 4 & 4^{2} & 4^{3} \\ 1 & 5 & 5^{2} & 5^{3}\end{bmatrix}, β = \begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix}, y = \begin{bmatrix}2 \\ -3 \\ -2 \\ 0 \\ 1\end{bmatrix}$

- [ ] A. $(X^{T}X)^{-1}X^{T}y$
- [ ] B. $X^{T}(XX^{T})^{-1}y$
- [ ] C. $X(X^{T}X)^{-1}y$
- [ ] D. $X(X^{T}X)^{-1}X^{T}y$
- [ ] E. $(XX^{T})^{-1}Xy$

---

<a id="identifying-the-elements-of-a-polynomial-regression-model"></a>
## Identifying the Elements of a Polynomial Regression Model

**Example:** The vector of coefficients of the curve $y = \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3$ that best fits the data above can be found using polynomial regression as

| $x$ | **$2$** | **$3$** | **$4$** | **$5$** | **$6$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $40$ | $70$ | $120$ | $100$ | $80$ |

$\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix}$

where $X$ is the corresponding design matrix and $\mathbf{y}$ is the observation vector. Find $X$ and $\mathbf{y}$ in this context.

**Explanation**

| $x$ | **$\color{blue}2$** | **$\color{blue}3$** | **$\color{blue}4$** | **$\color{blue}5$** | **$\color{blue}6$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $\color{red}40$ | $\color{red}70$ | $\color{red}120$ | $\color{red}100$ | $\color{red}80$ |

The coefficients of the curve

$$
y = \beta_0 + \beta_1 x + \beta_2 x^2 + \beta_3 x^3
$$

that best fits the data can be found as the least-squares solution ${\hat{\boldsymbol{\beta}}}$ to the system

$$
\begin{aligned}
\begin{bmatrix}1 & 2 & 2^{2} & 2^{3} \\ 1 & 3 & 3^{2} & 3^{3} \\ 1 & 4 & 4^{2} & 4^{3} \\ 1 & 5 & 5^{2} & 5^{3} \\ 1 & 6 & 6^{2} & 6^{3}\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}40 \\ 70 \\ 120 \\ 100 \\ 80\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

where $X$ is the design matrix, $\beta$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

---

**Question 3**

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $3$ | $-2$ | $-1$ | $1$ | $-1$ |

The vector of coefficients of the curve $y = β_{0} + β_{1}x + β_{2}x^{2} + β_{3}x^{3}$ that best fits the data above can be found using polynomial regression as

$\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?

- [ ] A. $\begin{bmatrix}1 & 3 \\ 1 & -2 \\ 1 & -1 \\ 1 & 1 \\ 1 & -1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}3 \\ -2 \\ -1 \\ 1 \\ -1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 1 \\ 1 & 2 \\ 1 & 3 \\ 1 & 4 \\ 1 & 5\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 \\ 2 \\ 3 \\ 4 \\ 5\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 1 & 1^{2} & 1^{3} \\ 1 & 2 & 2^{2} & 2^{3} \\ 1 & 3 & 3^{2} & 3^{3} \\ 1 & 4 & 4^{2} & 4^{3} \\ 1 & 5 & 5^{2} & 5^{3}\end{bmatrix}$

---

**Question 4**

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $3$ | $-2$ | $-1$ | $1$ | $-1$ |

The vector of coefficients of the curve $y = β_{0} + β_{1}x + β_{2}x^{2} + β_{3}x^{3}$ that best fits the data above can be found using polynomial regression as

$\begin{bmatrix}β_{0} \\ β_{1} \\ β_{2} \\ β_{3}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the observation vector $y$ in this context?

- [ ] A. $\begin{bmatrix}3 \\ -2 \\ -1 \\ 1 \\ -1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 3 \\ 1 & -2 \\ 1 & -1 \\ 1 & 1 \\ 1 & -1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 1 \\ 1 & 2 \\ 1 & 3 \\ 1 & 4 \\ 1 & 5\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 \\ 2 \\ 3 \\ 4 \\ 5\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 1 & 1^{2} & 1^{3} \\ 1 & 2 & 2^{2} & 2^{3} \\ 1 & 3 & 3^{2} & 3^{3} \\ 1 & 4 & 4^{2} & 4^{3} \\ 1 & 5 & 5^{2} & 5^{3}\end{bmatrix}$

---

<a id="regression-curves-containing-zero-coefficients"></a>
## Regression Curves Containing Zero Coefficients

In some cases, we might need to constrain the regression curve so that some of its coefficients equal zero.

Consider the table below, which gives the number of units $x$ (in hundreds) produced by a small company and the respective profits $y$ (in thousands of dollars) earned for producing $x$ units.

| Goods $(x)$ | **$1$** | **$2$** | **$3$** | **$4$** |
| --- | ---: | ---: | ---: | ---: |
| Profit $(y)$ | $2$ | $5$ | $8$ | $18$ |

It's known that the profit equals zero when the company does not produce any goods, so $y=0$ when $x=0$. The company wishes to find a *quadratic* curve that best fits the data *subject to* the constraint that it should pass through the point $(0,0)$.

So, our regression curve must pass through the point $(0,0)$. In other words, the $y$-intercept of the quadratic curve must be $0$. Therefore, we want to find the quadratic function

$$
y = \beta_1 x + \beta_2 x^2
$$

that best fits the given data points.

![](<../Source/8.3.2. Polynomial Regression With Matrices/Images/b11123809898045f232dff4ca6c14f1b.png>)

Substituting the values of $x$ into the regression equation and equating the results to the corresponding observed values of $y$, we obtain the following system:

$$
{β_{1} + 1^{2}β_{2} = 2; 2β_{1} + 2^{2}β_{2} = 5; 3β_{1} + 3^{2}β_{2} = 8; 4β_{1} + 4^{2}β_{2} = 18
$$

This system can be written using matrix notation as

$$
\begin{bmatrix}1 & 1^{2} \\ 2 & 2^{2} \\ 3 & 3^{2} \\ 4 & 4^{2}\end{bmatrix}
$$

where $X$ is the design matrix, $\boldsymbol{\beta}$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

The rest of the arguments are the same as before. Since our points do not lie on a quadratic curve, there will be no parameters $\beta_1$ and $\beta_2$ such that this equation is true. So instead, we seek parameters $\beta_1$ and $\beta_2$ that best approximate the solution.

---

<a id="identifying-the-design-matrix-when-the-regression-curve-contains-zero-coefficients"></a>
## Identifying the Design Matrix When the Regression Curve Contains Zero Coefficients

**Example:** The vector of coefficients of the curve $y = \beta_0 + \beta_2 x^2 + \beta_3 x^3$ that best fits the data above can be found using polynomial regression as

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $6$ | $3$ | $2$ | $4$ | $6$ |

$\begin{bmatrix}β_{0} \\ β_{2} \\ β_{3}\end{bmatrix}$

where $X$ is the corresponding design matrix and $\mathbf{y}$ is the observation vector. Find $X$ and $\mathbf{y}$ in this context.

**Explanation**

| $x$ | **$\color{blue}1$** | **$\color{blue}2$** | **$\color{blue}3$** | **$\color{blue}4$** | **$\color{blue}5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $\color{red}6$ | $\color{red}3$ | $\color{red}2$ | $\color{red}4$ | $\color{red}6$ |

The coefficients of the curve

$$
y = \beta_0 + \beta_2 x^2 + \beta_3 x^3
$$

that best fits the data can be found as the least-squares solution ${\hat{\boldsymbol{\beta}}}$ to the system

$$
\begin{aligned}
\begin{bmatrix}1 & 1^{2} & 1^{3} \\ 1 & 2^{2} & 2^{3} \\ 1 & 3^{2} & 3^{3} \\ 1 & 4^{2} & 4^{3} \\ 1 & 5^{2} & 5^{3}\end{bmatrix}^(⏞)^(X)\begin{bmatrix}β_{0} \\ β_{2} \\ β_{3}\end{bmatrix}^(⏞)^(β) &= \begin{bmatrix}6 \\ 3 \\ 2 \\ 4 \\ 6\end{bmatrix}^(⏞)^(y)
\end{aligned}
$$

where $X$ is the design matrix, $\beta$ is the parameter vector, and $\mathbf{y}$ is the observation vector.

---

**Question 5**

| $x$ | **$1$** | **$2$** | **$3$** | **$4$** | **$5$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $3$ | $-2$ | $-1$ | $1$ | $-1$ |

The vector of coefficients of the curve $y = β_{0} + β_{2}x^{2}$ that best fits the data above can be found using polynomial regression as

$\begin{bmatrix}β_{0} \\ β_{2}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?

- [ ] A. $\begin{bmatrix}1 & 1^{2} & 3 \\ 1 & 2^{2} & -2 \\ 1 & 3^{2} & -1 \\ 1 & 4^{2} & 1 \\ 1 & 5^{2} & -1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}1 & 1^{2} \\ 1 & 2^{2} \\ 1 & 3^{2} \\ 1 & 4^{2} \\ 1 & 5^{2}\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & 1 & 1^{2} \\ 1 & 2 & 2^{2} \\ 1 & 3 & 3^{2} \\ 1 & 4 & 4^{2} \\ 1 & 5 & 5^{2}\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & 3 \\ 1 & -2 \\ 1 & -1 \\ 1 & 1 \\ 1 & -1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 1 \\ 1 & 2 \\ 1 & 3 \\ 1 & 4 \\ 1 & 5\end{bmatrix}$

---

**Question 6**

| $x$ | **$-1$** | **$0$** | **$1$** | **$2$** | **$3$** |
| --- | ---: | ---: | ---: | ---: | ---: |
| $y$ | $2$ | $9$ | $0$ | $-9$ | $-12$ |

The vector of coefficients of the curve $y = β_{0} + β_{2}x^{2} + β_{3}x^{3}$ that best fits the data above can be found using polynomial regression as

$\begin{bmatrix}β_{0} \\ β_{2} \\ β_{3}\end{bmatrix} = (X^{T}X)^{-1}X^{T}y$,

where $X$ is the corresponding design matrix and $y$ is the observation vector. Which of the following is the design matrix $X$ in this context?

- [ ] A. $\begin{bmatrix}1 & -1 & (-1)^{2} \\ 1 & 0 & 0^{2} \\ 1 & 1 & 1^{2} \\ 1 & 2 & 2^{2} \\ 1 & 3 & 3^{2}\end{bmatrix}$
- [ ] B. $\begin{bmatrix}2 & -1 & (-1)^{2} \\ 9 & 0 & 0^{2} \\ 0 & 1 & 1^{2} \\ -9 & 2 & 2^{2} \\ -12 & 3 & 3^{2}\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 & (-1)^{2} & (-1)^{3} \\ 1 & 0^{2} & 0^{3} \\ 1 & 1^{2} & 1^{3} \\ 1 & 2^{2} & 2^{3} \\ 1 & 3^{2} & 3^{3}\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & -1 & (-1)^{3} \\ 1 & 0 & 0^{3} \\ 1 & 1 & 1^{3} \\ 1 & 2 & 2^{3} \\ 1 & 3 & 3^{3}\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-1 & (-1)^{2} & (-1)^{3} \\ 0 & 0^{2} & 0^{3} \\ 1 & 1^{2} & 1^{3} \\ 2 & 2^{2} & 2^{3} \\ 3 & 3^{2} & 3^{3}\end{bmatrix}$

```update-progress
```

[[MA/LAL/Home|Home]]
[[MA/LAL/0. Table of Contents/TOC|Table of Contents]]
