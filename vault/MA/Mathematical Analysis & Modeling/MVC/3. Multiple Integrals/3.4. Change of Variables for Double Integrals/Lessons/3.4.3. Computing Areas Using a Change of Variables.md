# Computing Areas Using a Change of Variables

<!--
lesson-id: 1998
topic-code: MVC.3.4.3
-->

## Table of Contents

- [Introduction](#introduction)
- [The Change of Variables Formula](#the-change-of-variables-formula)
- [A Worked Example](#a-worked-example)
- [Mapping an Area to a Rectangular Region: Transformations Given](#mapping-an-area-to-a-rectangular-region-transformations-given)
- [Mapping an Area to a Rectangular Region: Transformations Not Given](#mapping-an-area-to-a-rectangular-region-transformations-not-given)
- [Finding an Area by Transforming to a Type I Region](#finding-an-area-by-transforming-to-a-type-i-region)

## Prerequisites

- [Double Integrals Over Type I Regions](<../../3.2. Double Integrals/Lessons/3.2.5. Double Integrals Over Type I Regions.md>)
- [Transformations of Regions Between Curves](<../../../2. Multivariable Functions/2.6. Plane Transformations/Lessons/2.6.6. Transformations of Regions Between Curves.md>)

---

<a id="introduction"></a>
## Introduction

Given a real interval

$$
D = (a,b)\subset \mathbb R
$$

the length of this interval can be represented by the integral

$$
\textrm{Length}(D) = \int_a^b \,\textrm d x
$$

which we can also write as

$$
\textrm{Length}(D) = \int\limits_D \,\textrm d x
$$

Suppose we define an invertible function $u = u(x)$. By the change of variables formula, the length of our interval can also be expressed as

$$
\textrm{Length}(D) = \int\limits_{\Delta} \dfrac{\textrm d x}{\textrm d u}\, \textrm d u
$$

Note the following:

- Whenever we perform a change of variables, the domain of integration changes, too. This new integration domain is denoted $\Delta$. More technically, $\Delta$ is the *image* of the $D$ under the action of $u(x)$.
- If we think of $\dfrac{\textrm d x}{\textrm d u}$ as a fraction, the $\textrm d u$'s in our length formula appear to "cancel out," leaving only the original $\textrm d x$. This makes the change of variables formula easy to remember.

So, to summarize,

$$
\textrm{Length}(D) = \int\limits_D \,\textrm d x = \int\limits_{\Delta} \dfrac{\textrm d x}{\textrm d u}\, \textrm d u
$$

Since the length of our interval is simply $b-a$, it's doubtful that we'll ever use this formula in practice! However, it does indicate how we might use a change of variables to find the *area* of a complex region in two-dimensional space. Let's explore this idea in more detail.

---

<a id="the-change-of-variables-formula"></a>
## The Change of Variables Formula

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/8267812ac0a59ca3430c5ebaeef0f80b.png>)

Consider the region $D$ in the $xy$-plane shown above. The area of this region is given by

$$
\iint\limits_{D} \mathrm d x\textrm d y
$$

where we'll use the convention

$$
\textrm d A = \mathrm d x\textrm d y
$$

when referring to integration with respect to the Cartesian coordinates $x$ and $y$.

This region looks pretty complicated. In such cases, it's often easier to find an invertible transformation $\mathbf T$ (i.e., change of variables) whose inverse maps the region $D$ to simpler, preferably rectangular, region $\Delta$ in the $uv$-plane.

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/33c69e4c15b46f222ec5d5226a8bbf3d.png>)

So, rather than computing the required area by integrating with respect to $x$ and $y$, we can instead perform the transformation and integrate with respect to $u$ and $v$.

To carry out this process, we apply the **change of variables formula**

$$
\textrm{Area}(D) = \iint\limits_{D} \mathrm d x \mathrm d y = \iint\limits_{\Delta} \left| \dfrac{\partial (x, y)}{\partial (u, v)} \right| \ \mathrm d u \mathrm d v
$$

where

$$
\dfrac{\partial (x, y)}{\partial (u, v)}
$$

is the Jacobian determinant corresponding to $\mathbf T$.

Notice that this formula is analogous to the one we saw earlier for computing the length of an interval. In particular:

- If we consider the Jacobian as a kind of fraction, the $\partial(u,v)$ and $\textrm d u\textrm d v$ parts appear to "cancel out," similar to the previous formula.
- Note that $\mathbf T$ could be orientation-reversing. We take the absolute value of the Jacobian to ensure that $D$ and $\Delta$ have the same orientation.

Finally, note that the change of variables formula assumes that $\mathbf T$ is a $C^1$ transformation and has no critical points in the interior of $\Delta$.

Often, the most challenging part is finding a suitable change of variables. The main idea is to get a nice rectangular shape with sides parallel to the axes in the $uv$-plane. In other words, we require a change of variables that reduces the equations of our curves to something like the following:

$$
u = \textrm{const}_1, \quad u = \textrm{const}_2, \quad v = \textrm{const}_3, \quad v = \textrm{const}_4
$$

Let's look at a concrete example of computing an area using the change of variables formula.

---

<a id="a-worked-example"></a>
## A Worked Example

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/19c29b32cce18d8d29708037505220c5.png>)

Consider the finite region $D$ in the first quadrant (shown above) bounded by the lines

$$
{\color{blue}{3y-x}} = 3, \qquad {\color{blue}{3y-x}} = 6, \qquad {\color{red}{2x+y}}=4, \qquad {\color{red}{2x+y}}=6
$$

The area of the required region is given by

$$
\iint\limits_{D} \mathrm d x \mathrm d y
$$

Let's define a transformation $\mathbf T$ as follows:

$$
\begin{bmatrix}u \\ v\end{bmatrix}
$$

This transformation maps some region $\Delta$ in the $uv$-plane to our region $D$ in the $xy$-plane.

To compute the required area, we will use the change of variables formula

$$
\iint\limits_{D} \mathrm d x \mathrm d y = \iint\limits_{\Delta} \left| \dfrac{\partial (x, y)}{\partial (u, v)} \right| \ \mathrm d u \mathrm d v
$$

where

$$
\dfrac{\partial (x, y)}{\partial (u, v)}
$$

is the Jacobian determinant corresponding to $\mathbf T$.

Looking at the lines that bound our region, we consider the following change of variables:

$$
u = {\color{blue}{3y-x}}, \qquad v = {\color{red}{2x+y}}
$$

Note that this change of variables gives us the *inverse* function $\mathbf T^{-1}$, that is

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

To compute the required area, we proceed in four steps:

**Step 1**: Find $\Delta$, which is the image of $D$ under the action of $\mathbf{T}^{-1}$.

We substitute $u=3y-x$ into the equations of the first and second lines:

$$
\begin{bmatrix}3y - x = 3 & ⟹ & u = 3 \\ 3y - x = 6 & ⟹ & u = 6\end{bmatrix}
$$

And we substitute

$$
v = 2x+y
$$

into the equations of the third and fourth lines:

$$
\begin{bmatrix}2x + y = 4 & ⟹ & v = 4 \\ 2x + y = 6 & ⟹ & v = 6\end{bmatrix}
$$

Therefore, our domain in the $uv$-plane is the following rectangle:

$$
\Delta = \big\{(u,v) \:: \: 3 \leq u \leq 6, \quad 4 \leq v \leq 6\big\}
$$

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/d4670b752177cbf1a02797c8ad5be4db.png>)

**Step 2**: Compute the Jacobian determinant corresponding to $\mathbf T^{-1}$.

Our transformation $\mathbf T^{-1}$ is

$$
u = 3y-x, \qquad v = 2x+y
$$

The Jacobian determinant corresponding to $\mathbf T^{-1}$ is

$$
\begin{aligned}
(∂(u, v))/(∂(x, y)) &= \begin{vmatrix}\frac{∂u}{∂x} & \frac{∂u}{∂y} \\ \frac{∂v}{∂x} & \frac{∂v}{∂y}\end{vmatrix} \\
&=\begin{vmatrix}-1 & 3 \\ 2 & 1\end{vmatrix} \\
&=-7
\end{aligned}
$$

**Step 3**: Compute the Jacobian determinant corresponding to $\mathbf T$.

The Jacobian determinant corresponding to $\mathbf T$ is

$$
\dfrac{\partial (x, y)}{\partial (u, v)} = \left(\dfrac{\partial (u, v)}{\partial (x, y)} \right)^{-1} = -\dfrac17
$$

Note that

$$
\dfrac{\partial (x, y)}{\partial (u, v)} \neq 0
$$

everywhere inside $\Delta$. In other words, $\mathbf T$ has no critical points inside $\Delta$.

**Step 4**: Compute the required area.

We calculate the required area by performing the change of variables:

$$
\begin{aligned}
∬_(D)dxdy &= ∬_(Δ)\begin{vmatrix}(∂(x, y))/(∂(u, v)) & dudv \\ = ∬_(Δ) & -\frac{1}{7}\end{vmatrix}dudv \\
&= \frac{1}{7}∬_(Δ)dudv \\
&= \frac{1}{7} \cdot Area(Δ) \\
&= \frac{1}{7} \cdot (6 - 3) \cdot (6 - 4) \\
&= \frac{1}{7} \cdot 3 \cdot 2 \\
&= \frac{6}{7}
\end{aligned}
$$

---

<a id="mapping-an-area-to-a-rectangular-region-transformations-given"></a>
## Mapping an Area to a Rectangular Region: Transformations Given

**Example:** Using the change of variables

$u = \dfrac{y^2}{x}, \qquad v= xy$,

calculate the area of the finite region $D$ in the first quadrant bounded by the curves
$x = {y^2}, \qquad x=\dfrac{y^2}3, \qquad y=\dfrac2x, \qquad y=\dfrac4x$.

**Explanation**

The area of the required region is given by

$$
\iint\limits_{D} \mathrm d x \mathrm d y
$$

Let's define a transformation $\mathbf T$ as follows:

$$
\begin{bmatrix}u \\ v\end{bmatrix}
$$

This transformation maps some region $\Delta$ in the $uv$-plane to our region $D$ in the $xy$-plane.

To compute the required area, we will use the change of variables formula

$$
\iint\limits_{D} \mathrm d x \mathrm d y = \iint\limits_{\Delta} \left| \dfrac{\partial (x, y)}{\partial (u, v)} \right| \ \mathrm d u \mathrm d v
$$

where

$$
\dfrac{\partial (x, y)}{\partial (u, v)}
$$

is the Jacobian determinant corresponding to $\mathbf T$.

Note that the changes of variables

$$
u = \dfrac{y^2}{x}, \qquad v= xy
$$

gives us the *inverse* function $\mathbf T^{-1}$, that is

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

To compute the required area, we proceed in four steps:

**Step 1**: Find $\Delta$, which is the image of $D$ under the action of $\mathbf{T}^{-1}$.

We substitute

$$
u= \dfrac{y^2}{x}
$$

into the equations of the first and second curves:

$$
\begin{bmatrix}x = y^{2} & ⟹ & \frac{y^{2}}{x} = 1 & ⟹ & u = 1 \\ x = \frac{y^{2}}{3} & ⟹ & \frac{y^{2}}{x} = 3 & ⟹ & u = 3\end{bmatrix}
$$

And we substitute $v = xy$ into the equations of the third and fourth curves:

$$
\begin{bmatrix}y = \frac{2}{x} & ⟹ & xy = 2 & ⟹ & v = 2 \\ y = \frac{4}{x} & ⟹ & xy = 4 & ⟹ & v = 4\end{bmatrix}
$$

Therefore, our domain in the $uv$-plane is the following rectangle:

$$
\Delta = \big\{(u,v) \:: \: 1 \leq u \leq 3, \quad 2 \leq v \leq 4\big\}
$$

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/e27de2eefceebf65321988f3aec19324.png>)

**Step 2**: Compute the Jacobian determinant corresponding to $\mathbf T^{-1}$.

The Jacobian determinant corresponding to $\mathbf T^{-1}$ is

$$
\begin{aligned}
(∂(u, v))/(∂(x, y)) &= \begin{vmatrix}\frac{∂u}{∂x} & \frac{∂u}{∂y} \\ \frac{∂v}{∂x} & \frac{∂v}{∂y}\end{vmatrix} \\
&=\begin{vmatrix}-\frac{y^{2}}{x^{2}} & \frac{2y}{x} \\ y & x\end{vmatrix} \\
&=-\frac{3y^{2}}{x}
\end{aligned}
$$

**Step 3**: Compute the Jacobian determinant corresponding to $\mathbf T$.

The Jacobian determinant corresponding to $\mathbf T$ is

$$
\dfrac{\partial (x, y)}{\partial (u, v)} = \left(\dfrac{\partial (u, v)}{\partial (x, y)} \right)^{-1} = -\dfrac{x}{3y^2} = -\dfrac{1}{3u}
$$

Note that

$$
\dfrac{\partial (x, y)}{\partial (u, v)} \neq 0
$$

everywhere inside $\Delta$. In other words, $\mathbf T$ has no critical points inside $\Delta$.

**Step 4**: Compute the required area.

We calculate the required area by performing the change of variables:

$$
\begin{aligned}
∬_(D)dxdy &= ∬_(Δ)\begin{vmatrix}(∂(x, y))/(∂(u, v)) & dudv \\ = ∬_(Δ) & -\frac{1}{3u}\end{vmatrix}dudv \\
&= \frac{1}{3}∫_{1}^{3}∫_{2}^{4}\frac{1}{u}dudv \\
&= \frac{1}{3}∫_{1}^{3}\frac{1}{u}du∫_{2}^{4}dv \\
&= \frac{1}{3} \cdot (\ln 3 - \ln 1) \cdot (4 - 2) \\
&= \frac{2}{3}\ln 3
\end{aligned}
$$

---

**Question 1**

Using the change of variables

$u = y - 2x, v = y + x$,
calculate the area of the finite region $D$ in the first quadrant bounded by the lines
$y = 2x - 1, y = 2x + 1, y = 1 - x, y = 2 - x$.

- [ ] A. $6$
- [ ] B. $\frac{1}{3}$
- [ ] C. $2$
- [ ] D. $\frac{2}{3}$
- [ ] E. $4$

---

**Question 2**

Using the change of variables

$u = \frac{y}{x^{2}}, v = xy$,

calculate the area of the finite region $D$ in the first quadrant bounded by the curves
$y = x^{2}, y = 2x^{2}, y = \frac{1}{x}, y = \frac{4}{x}$.

- [ ] A. $\frac{1}{2}\ln 2$
- [ ] B. $3\ln 2$
- [ ] C. $\ln 4$
- [ ] D. $4\ln 2$
- [ ] E. $\ln 2$

---

<a id="mapping-an-area-to-a-rectangular-region-transformations-not-given"></a>
## Mapping an Area to a Rectangular Region: Transformations Not Given

**Example:** Use a change of variables to calculate the area of the finite region $D$ in the first quadrant bounded by the following curves.
$e^y = \dfrac{5}{e^x}, \qquad e^y=\dfrac{10}{e^x}, \qquad e^x=e^y, \qquad e^x=2e^y$

**Explanation**

The area of the required region is given by

$$
\iint\limits_{D} \mathrm d x \mathrm d y
$$

Let's define a transformation $\mathbf T$ as follows:

$$
\begin{bmatrix}u \\ v\end{bmatrix}
$$

We'll assume this transformation maps some region $\Delta$ in the $uv$-plane to our region $D$ in the $xy$-plane.

To compute the required area, we will use the change of variables formula

$$
\iint\limits_{D} \mathrm d x \mathrm d y = \iint\limits_{\Delta} \left| \dfrac{\partial (x, y)}{\partial (u, v)} \right| \ \mathrm d u \mathrm d v
$$

where

$$
\dfrac{\partial (x, y)}{\partial (u, v)}
$$

is the Jacobian determinant corresponding to $\mathbf T$.

To compute the required area, we proceed in five steps:

**Step 1**: Find a suitable change of variables.

Notice that we can rewrite the equations of our curves as follows:

$$
e^{x+y} = 5, \qquad e^{x+y}=10, \qquad e^{x-y} = 1, \qquad e^{x-y} = 2
$$

This suggests the following change of variables:

$$
u = e^{x+y}, \qquad v = e^{x-y}
$$

This change of variables gives us the *inverse* function $\mathbf T^{-1}$, that is

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

**Step 2**: Find $\Delta$, which is the image of $D$ under the action of $\mathbf{T}^{-1}$.

We substitute

$$
u=e^{x+y}
$$

into the equations of the first and second curves:

$$
e^{x + y} = 5⟹u = 5; e^{x + y} = 10⟹u = 10
$$

And we substitute $v =e^{x-y}$ into the equations of the third and fourth curves:

$$
e^{x - y} = 1⟹v = 1; e^{x - y} = 2⟹v = 2
$$

Therefore, our domain in the $uv$-plane is the following rectangle:

$$
\Delta = \big\{(u,v) \:: \: 5 \leq u \leq 10, \quad 1 \leq v \leq 2\big\}
$$

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/2e3a7747d13b0b7a3d4fc51bdbc38e12.png>)

**Step 3**: Compute the Jacobian determinant corresponding to $\mathbf T^{-1}$.

The Jacobian determinant corresponding to $\mathbf T^{-1}$ is

$$
\begin{aligned}
(∂(u, v))/(∂(x, y)) &= \begin{vmatrix}\frac{∂u}{∂x} & \frac{∂u}{∂y} \\ \frac{∂v}{∂x} & \frac{∂v}{∂y}\end{vmatrix} \\
&=\begin{vmatrix}e^{x + y} & e^{x + y} \\ e^{x - y} & -e^{x - y}\end{vmatrix} \\
&=-e^{2x} - e^{2x} \\
&=-2e^{2x}
\end{aligned}
$$

**Step 4**: Compute the Jacobian determinant corresponding to $\mathbf T$.

The Jacobian determinant corresponding to $\mathbf T$ is

$$
\dfrac{\partial (x, y)}{\partial (u, v)} = \left(\dfrac{\partial (u, v)}{\partial (x, y)} \right)^{-1} = -\dfrac{1}{2}e^{-2x}=-\dfrac{1}{2uv}
$$

Note that

$$
\dfrac{\partial (u, v)}{\partial (x, y)}\neq 0
$$

everywhere inside $\Delta$. In other words, $\mathbf T$ has no critical points inside $\Delta$.

**Step 5**: Compute the required area.

We calculate the required area by performing the change of variables:

$$
\begin{aligned}
∬_(D)dxdy &= ∬_(Δ)\begin{vmatrix}(∂(x, y))/(∂(u, v)) & dudv \\ = ∬_(Δ) & -\frac{1}{2uv}\end{vmatrix}dudv \\
&= \frac{1}{2}∬_(Δ)\frac{1}{uv}dudv \\
&= \frac{1}{2}∫_{5}^{10}\frac{1}{u}du∫_{1}^{2}\frac{1}{v}dv \\
&= \frac{1}{2} \cdot (\ln 10 - \ln 5) \cdot (\ln 2 - \ln 1) \\
&= \frac{1}{2}(\ln 2)^{2}
\end{aligned}
$$

---

**Question 3:** Use a change of variables to calculate the area of the finite region $D$ bounded by the following lines.
$3y = 5x + 2, 3y = 5x - 2, 2x = y, 4x = 2y + 3$

- [ ] A. $6$
- [ ] B. $2$
- [ ] C. $-2$
- [ ] D. $4$
- [ ] E. $-4$

---

**Question 4:** Use a change of variables to calculate the area of the finite region $D$ in the first quadrant bounded by the following curves and lines.
$y = \frac{1}{x}, y = \frac{2}{x}, y = x, y = 2x$

- [ ] A. $2\ln 3$
- [ ] B. $\ln 2$
- [ ] C. $\frac{1}{2}\ln 3$
- [ ] D. $\frac{1}{2}\ln 2$
- [ ] E. $3\ln 2$

---

<a id="finding-an-area-by-transforming-to-a-type-i-region"></a>
## Finding an Area by Transforming to a Type I Region

**Example:** Using the change of variables

$u =3x-2y, \quad v=x+y$,
calculate the area of the finite region $D$ bounded by the curve $(3x-2y)^2 - y - x = 0$ and the line $x+y=4$.

**Explanation**

The area of the required region is given by

$$
\iint\limits_{D} \mathrm d x \mathrm d y
$$

Let's define a transformation $\mathbf T$ as follows:

$$
\begin{bmatrix}u \\ v\end{bmatrix}
$$

This transformation maps some region $\Delta$ in the $uv$-plane to our region $D$ in the $xy$-plane.

To compute the required area, we will use the change of variables formula

$$
\iint\limits_{D} \mathrm d x \mathrm d y = \iint\limits_{\Delta} \left| \dfrac{\partial (x, y)}{\partial (u, v)} \right| \ \mathrm d u \mathrm d v
$$

where

$$
\dfrac{\partial (x, y)}{\partial (u, v)}
$$

is the Jacobian determinant corresponding to $\mathbf T$.

Note that the changes of variables

$$
u = 3x-2y, \qquad v=x+y
$$

gives us the *inverse* function $\mathbf T^{-1}$, that is

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

To compute the required area, we proceed in four steps:

**Step 1**: Find $\Delta$, which is the image of $D$ under the action of $\mathbf{T}^{-1}$.

Substituting $3x-2y = u$ and

$$
x+y = v
$$

into the equation of the first curve, we get

$$
(3x - 2y)^{2} - y - x = 0⟹u^{2} - v = 0⟹v = u^{2}
$$

while substituting

$$
x + y = v
$$

into the equation of the second curve, we obtain

$$
y + x = 4⟹v = 4
$$

To find the intersections of the curves in the $uv$-plane, we solve the following system:

$$
{v = u^{2}; v = 4⟹{u = ± 2; v = 4
$$

So, the intersection points are $(\pm 2, 4)$.

Therefore, our domain in the $uv$-plane has the type I representation

$$
\Delta = \big\{(u,v) \:: \: -2 \leq u \leq 2, \, u^2 \leq v \leq 4\big\}
$$

as shown below.

![](<../Source/3.4.3. Computing Areas Using a Change of Variables/Images/05063fb1a4b34617ec582fe90f4c0f48.png>)

**Step 2**: Compute the Jacobian determinant corresponding to $\mathbf T^{-1}{:}$

The Jacobian determinant corresponding to $\mathbf T^{-1}$ is

$$
\begin{aligned}
(∂(u, v))/(∂(x, y)) &= \begin{vmatrix}\frac{∂u}{∂x} & \frac{∂u}{∂y} \\ \frac{∂v}{∂x} & \frac{∂v}{∂y}\end{vmatrix} \\
&=\begin{vmatrix}3 & -2 \\ 1 & 1\end{vmatrix} \\
&= 5
\end{aligned}
$$

**Step 3**: Compute the Jacobian determinant corresponding to $\mathbf T{:}$

The Jacobian determinant corresponding to $\mathbf T$ is

$$
\dfrac{\partial (x, y)}{\partial (u, v)} = \left(\dfrac{\partial (u, v)}{\partial (x, y)} \right)^{-1} = (5)^{-1} = \dfrac 15
$$

Note that

$$
\dfrac{\partial (x, y)}{\partial (u, v)} \neq 0
$$

everywhere inside $\Delta$. In other words, $\mathbf T$ has no critical points inside $\Delta$.

**Step 4**: Compute the required area.

We calculate the required area by performing the change of variables:

$$
\begin{aligned}
∬_(D)dxdy &= ∬_(Δ) \mid (∂(x, y))/(∂(u, v)) \mid dudv \\
&= ∬_(Δ)\frac{1}{5}dudv \\
&= \frac{1}{5}∫_{-2}^{2}[∫_{u^{2}}^{4}dv]du \\
&= \frac{1}{5}∫_{-2}^{2}[v]_{u^{2}}^{4}du \\
&= \frac{1}{5}∫_{-2}^{2}(4 - u^{2})du \\
&= \frac{1}{5} \cdot 2∫_{0}^{2}(4 - u^{2})du \\
&= \frac{2}{5}[4u - \frac{u^{3}}{3}]_{0}^{2} \\
&= \frac{2}{5}(8 - \frac{8}{3}) \\
&= \frac{2}{5}(\frac{16}{3}) \\
&= \frac{32}{15}
\end{aligned}
$$

---

**Question 5**

Using the change of variables

$u = x - y, v = y$,
calculate the area of the finite region $D$ bounded by the curve $(x - y)^{2} - y = 0$ and the line $y = 1$.

- [ ] A. $2$
- [ ] B. $\frac{4}{3}$
- [ ] C. $1$
- [ ] D. $\sqrt{2}$
- [ ] E. $\frac{2}{3}$

---

**Question 6**

Using the change of variables

$u = x + y, v = x - y$,
calculate the area of the finite region $D$ bounded by the curve $(x + y)^{4} - x + y = 0$ and the line $y = 0$.

- [ ] A. $\frac{3}{20}$
- [ ] B. $\frac{1}{2}$
- [ ] C. $\frac{2}{3}$
- [ ] D. $\frac{1}{10}$
- [ ] E. $\frac{1}{3}$

```update-progress
```

[[MA/MVC/Home|Home]]
[[MA/MVC/0. Table of Contents/TOC|Table of Contents]]
