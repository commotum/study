# Directional Derivatives

<!--
lesson-id: 1938
topic-code: MVC.2.4.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating a Directional Derivative](#calculating-a-directional-derivative)
- [The Gradient as the Direction of Maximum Increase of a Function](#the-gradient-as-the-direction-of-maximum-increase-of-a-function)
- [Finding the Direction of Maximum Increase or Maximum Decrease of a Function](#finding-the-direction-of-maximum-increase-or-maximum-decrease-of-a-function)
- [The Directional Derivative as a Generalization of the Partial Derivative](#the-directional-derivative-as-a-generalization-of-the-partial-derivative)
- [The Rate of Change in the Direction of the Gradient](#the-rate-of-change-in-the-direction-of-the-gradient)
- [Finding the Rate of Change of a Function in a Given Direction](#finding-the-rate-of-change-of-a-function-in-a-given-direction)
- [Deriving the Formula For the Directional Derivative](#deriving-the-formula-for-the-directional-derivative)

## Prerequisites

- [The Gradient Vector](<2.4.1. The Gradient Vector.md>)

---

<a id="introduction"></a>
## Introduction

Let's consider a surface $z=f(x,y)$ and a unit vector

$$
\mathbf{u} = \left\langle u_1, u_2 \right\rangle
$$

The **directional derivative of $f$** at the point $(x_0,y_0)$ in the direction of $\mathbf{u}$ is

$$
f'_{\mathbf u}(x_0,y_0) = \lim\limits_{t \to 0}\dfrac{f(\mathbf x_0 + t \mathbf u) - f(\mathbf x_0)}{t}
$$

The directional derivative of $f$ is the rate of change of the function $f$ in the direction of $\mathbf{u}$. We'll discuss this idea more later in the lesson.

It turns out that if $f$ is differentiable, we can write $f'_{\mathbf u}$ in terms of partial derivatives $f_x$ and $f_y$, as follows:

$$
f'_{\mathbf u}(x_0,y_0) = \nabla f (x_0,y_0) \cdot \mathbf u
$$

At the end of the lesson, we'll derive this formula. But for now, let's get some practice using it.

For example, let's find the directional derivative of the function

$$
f(x, y) = x^2 + y^2
$$

at the point

$$
(x_0,y_0)=(1,2)
$$

in the direction of the vector

$$
\mathbf{v} = \mathbf{i} + \mathbf{j}
$$

First, we find the unit vector $\mathbf{u}$ in the direction of $\mathbf{v}$:

$$
\begin{aligned}
‖v‖ &= \sqrt{1^{2} + 1^{2}} = \sqrt{2} \\
u &= \frac{v}{‖v‖} = \frac{1}{\sqrt{2}}(i + j)
\end{aligned}
$$

Now, we compute the gradient $\nabla f(x,y)$ and evaluate it at the point $(1,2)$:

$$
\begin{aligned}
∇f(x, y) &= \frac{∂}{∂x}(x^{2} + y^{2})i + \frac{∂}{∂y}(x^{2} + y^{2})j \\
&= 2xi + 2yj \\
∇f(1, 2) &= (2 \cdot 1)i + (2 \cdot 2)j \\
&= 2i + 4j
\end{aligned}
$$

Therefore, the directional derivative is

$$
\begin{aligned}
f_{u}^{′}(1, 2) &= ∇f(1, 2) \cdot u \\
&= (2i + 4j) \cdot (\frac{1}{\sqrt{2}}i + \frac{1}{\sqrt{2}}j) \\
&= \frac{2 \cdot 1 + 4 \cdot 1}{\sqrt{2}} \\
&= \frac{6}{\sqrt{2}} \\
&= 3\sqrt{2}
\end{aligned}
$$

---

<a id="calculating-a-directional-derivative"></a>
## Calculating a Directional Derivative

**Example:** Find the directional derivative of the function $g(x, y) =3x^2+4xy$ at the point $A(0,1)$ in the direction towards the point $B(1,4)$.

**Explanation**

The directional derivative $g'_{\mathbf u}(a,b)$ of a function $g(x,y)$ at the point $(a,b)$ in the direction of the vector $\mathbf v$ is given by

$$
g'_{\mathbf u}(a,b) = \nabla g(a,b)\cdot\mathbf u
$$

where $\mathbf u$ is a unit vector in the direction of $\mathbf v$.

First, we find the direction vector and the corresponding unit vector:

$$
\begin{aligned}
AB^(→) &= ⟨1, 4⟩ - ⟨0, 1⟩ \\
&= ⟨1, 3⟩ \\
u &= (AB^(→))/(‖AB^(→)‖) \\
&= \frac{1}{\sqrt{1^{2} + 3^{2}}}⟨1, 3⟩ \\
&= \frac{1}{\sqrt{10}}⟨1, 3⟩
\end{aligned}
$$

Now, we calculate the gradient $\nabla g(x,y)$ and evaluate it at the point $A(0,1)$:

$$
\begin{aligned}
∇g(x, y) &= ⟨\frac{∂}{∂x}(3x^{2} + 4xy), \frac{∂}{∂y}(3x^{2} + 4xy)⟩ \\
&= ⟨6x + 4y, 4x⟩ \\
∇g(0, 1) &= ⟨6 \cdot 0 + 4 \cdot 1, 4 \cdot 0⟩ \\
&= ⟨4, 0⟩
\end{aligned}
$$

Therefore, the directional derivative is

$$
\begin{aligned}
g_{u}^{′}(0, 1) &= ∇g(0, 1) \cdot u \\
&= ⟨4, 0⟩ \cdot ⟨\frac{1}{\sqrt{10}}, \frac{3}{\sqrt{10}}⟩ \\
&= \frac{4}{\sqrt{10}} \\
&= \frac{4\sqrt{10}}{10} \\
&= \frac{2\sqrt{10}}{5}
\end{aligned}
$$

---

**Question 1:** Find the directional derivative of the function $f(x, y) = (x + y^{2})^{2}$ at the point $A(-1,-2)$ in the direction towards the point $B(2, 2)$.

- [ ] A. $\frac{18}{5}$
- [ ] B. $-\frac{18}{5}$
- [ ] C. $-\frac{21}{5}$
- [ ] D. $\frac{78}{5}$
- [ ] E. $-\frac{78}{5}$

---

**Question 2:** Find the directional derivative of the function $f(x, y) = 2x + y^{2}$ at the point $(0, 1)$ in the direction of the vector $v = 5i + 2j$.

- [ ] A. $\frac{4\sqrt{21}}{21}$
- [ ] B. $\frac{14\sqrt{21}}{21}$
- [ ] C. $\frac{14\sqrt{29}}{29}$
- [ ] D. $14\sqrt{29}$
- [ ] E. $\frac{10\sqrt{29}}{29}$

---

<a id="the-gradient-as-the-direction-of-maximum-increase-of-a-function"></a>
## The Gradient as the Direction of Maximum Increase of a Function

From the formula for the directional derivative, we get that if

$$
z = f (x, y)
$$

is a differentiable function and $\mathbf u$ is a unit vector, then

$$
\begin{aligned} f'_{\mathbf u} &= \nabla f \cdot \mathbf u \\[3pt] &= \mid \nabla f \mid \begin{vmatrix}\mathbf u & \cos\theta \\[3pt] & = & \nabla f\end{vmatrix} \cos\theta, \end{aligned}
$$

where $\theta$ is the angle between the vectors $\nabla f$ and $\mathbf u$, as depicted below.

![](<../Source/2.4.5. Directional Derivatives/Images/da54c3218bf555e2aa0507ba67a016ca.png>)

It follows that $f'_{\mathbf u}$ is maximized when $\theta =0$. In other words, $f'_{\mathbf u}$ is maximized when $\mathbf u$ and $\nabla f$ point in the same direction. Thus, the maximum value of $f'_{\mathbf u}$ is $\mid \nabla f \mid$.

A similar argument shows that $f'_{\mathbf u}$ reaches its minimum value $-\mid \nabla f \mid$ when

$$
\theta = \pi
$$

that is, when $\nabla f$ and $\mathbf u$ point in opposite directions.

As a result, we get the following theorem:

> *A differentiable function $f$ increases most rapidly in the direction of the gradient and decreases most rapidly in the opposite direction to that of its gradient.*

For functions of more than two variables, the same theorem holds.

---

<a id="finding-the-direction-of-maximum-increase-or-maximum-decrease-of-a-function"></a>
## Finding the Direction of Maximum Increase or Maximum Decrease of a Function

**Example:** In what direction does the function $f (x, y) = \ln (x^2y)$ decrease most rapidly at the point $(2, 1)$?

**Explanation**

At a given point, a function $f(x,y)$ decreases most rapidly in the direction opposite to that of its gradient.

So, we compute $-\nabla f(x,y)$ and evaluate it at the point $(2,1)$:

$$
\begin{aligned}
- ∇f(x, y) &= -\frac{∂}{∂x}(\ln (x^{2}y))i - \frac{∂}{∂y}(\ln (x^{2}y))j \\
&=-\frac{1}{x^{2}y} \cdot 2xyi - \frac{1}{x^{2}y} \cdot x^{2}j \\
&=-\frac{2}{x}i - \frac{1}{y}j \\
- ∇f(2, 1) &= -\frac{2}{2}i - \frac{1}{1}j \\
&=-i - j
\end{aligned}
$$

Therefore, the function decreases most rapidly in the direction of $-\, \mathbf i - \mathbf j$.

---

**Question 3:** In what direction does the function $g(x, y, z) = y^{2} + \sin (2πx + πz)$ increase most rapidly at the point $(1, 1, 2)$?

- [ ] A. $⟨π, 2, π⟩$
- [ ] B. $⟨ - π,-1, π⟩$
- [ ] C. $⟨ - 2π,-2, - π⟩$
- [ ] D. $⟨2π, 2, π⟩$
- [ ] E. $⟨2, 2, 1⟩$

---

**Question 4:** In what direction does the function $f(x, y) = \cos x + x^{2}y$ increase most rapidly at the point $(1, 1)$?

- [ ] A. $(\sin (1) - 2)i - j$
- [ ] B. $-\sin (1)i + 2j$
- [ ] C. $2i + j$
- [ ] D. $-2i - j$
- [ ] E. $(2 - \sin (1))i + j$

---

<a id="the-directional-derivative-as-a-generalization-of-the-partial-derivative"></a>
## The Directional Derivative as a Generalization of the Partial Derivative

For a surface $z = f(x,y)$, the partial derivative $f_x(x_0,y_0)$ gives us the rate of change of $f$ in the direction of the vector

$$
\mathbf{i} = \langle 1,0\rangle
$$

But this is just a special case of the directional derivative, because

$$
\begin{aligned}
f_{i}^{′}(x_{0}, y_{0}) &= ∇(x_{0}, y_{0}) \cdot ⟨1, 0⟩ \\
&= ⟨f_{x}(x_{0}, y_{0}), f_{y}(x_{0}, y_{0})⟩ \cdot ⟨1, 0⟩ \\
&= f_{x}(x_{0}, y_{0})
\end{aligned}
$$

Similarly,

$$
\begin{aligned}
f_{j}^{′}(x_{0}, y_{0}) &= ∇(x_{0}, y_{0}) \cdot ⟨0, 1⟩ \\
&= ⟨f_{x}(x_{0}, y_{0}), f_{y}(x_{0}, y_{0})⟩ \cdot ⟨0, 1⟩ \\
&= f_{y}(x_{0}, y_{0})
\end{aligned}
$$

So, the directional derivative generalizes the notion of a partial derivative.

Now, recall that the partial derivatives $f_x$ and $f_y$ represent the rate of change of $f$ in the direction of vectors $\mathbf i$ and $\mathbf j$, respectively. As for a general directional derivative, we obtain the following property:

> *The directional derivative $f'_\mathbf{u}(x_0,y_0)$ represents the rate of change of the function $f(x,y)$ at the point $(x_0,y_0)$ in the direction of $\mathbf{u}$.*

---

<a id="the-rate-of-change-in-the-direction-of-the-gradient"></a>
## The Rate of Change in the Direction of the Gradient

What is the directional derivative of the function $\mathbf{f}$ at $(x_0,y_0)$ in the direction of the gradient?

Let's apply our formula. First, we find the unit vector $\mathbf{u}$ in the direction of $\nabla\! f$:

$$
\begin{aligned}
u &= (∇f(x_{0}, y_{0}))/(‖∇f(x_{0}, y_{0})‖)
\end{aligned}
$$

Therefore, the directional derivative is

$$
\begin{aligned}
f_{u}^{′}(x_{0}, y_{0}) &= ∇f(x_{0}, y_{0}) \cdot (∇f(x_{0}, y_{0}))/(‖∇f(x_{0}, y_{0})‖) \\
&= (∇f(x_{0}, y_{0}) \cdot ∇f(x_{0}, y_{0}))/(‖∇f(x_{0}, y_{0})‖) \\
&= (‖∇f(x_{0}, y_{0})‖^{2})/(‖∇f(x_{0}, y_{0})‖) \\
&= ‖∇f(x_{0}, y_{0})‖
\end{aligned}
$$

So, we conclude the following:

> *The directional derivative in the direction of the gradient equals the norm of the gradient. Furthermore, the norm of the gradient can be interpreted as the rate of change of the function in the direction of the gradient.*

---

<a id="finding-the-rate-of-change-of-a-function-in-a-given-direction"></a>
## Finding the Rate of Change of a Function in a Given Direction

**Example:** Consider the function $g (x, y) = \ln \left(\sqrt {xy^3}\right)$. What is the rate of change of $g$ at the point $\left(\dfrac 12, \dfrac 32 \right)$ in the direction in which the function increases most rapidly?

**Explanation**

At a given point, a function $g(x,y)$ increases most rapidly in the direction of its gradient at that point.

In our case, we compute $\nabla g(x,y)$ and evaluate it at the point

$$
\left(\dfrac 12, \dfrac 32 \right)\!
$$

$$
\begin{aligned}
∇g(x, y) &= ⟨\frac{∂}{∂x}(\ln (\sqrt{xy^{3}})), \frac{∂}{∂y}(\ln (\sqrt{xy^{3}}))⟩ \\
&= ⟨\frac{1}{\sqrt{xy^{3}}} \cdot \frac{y^{3}}{2\sqrt{xy^{3}}}, \frac{1}{\sqrt{xy^{3}}} \cdot \frac{3xy^{2}}{2\sqrt{xy^{3}}}⟩ \\
&= ⟨\frac{1}{2x}, \frac{3}{2y}⟩ \\
∇g(\frac{1}{2}, \frac{3}{2}) &= ⟨(1)/(2 \cdot (\frac{1}{2})), (3)/(2 \cdot (\frac{3}{2}))⟩ \\
&= ⟨1, 1⟩
\end{aligned}
$$

Finally, the rate of change of $g(x,y)$ at the point

$$
\left(\dfrac 12, \dfrac 32 \right)
$$

in the direction of

$$
\nabla g\left(\dfrac 12, \dfrac 32 \right)
$$

is given by

$$
\begin{aligned}
‖∇g(\frac{1}{2}, \frac{3}{2})‖ &= ‖⟨1, 1⟩‖ \\
&= \sqrt{1^{2} + 1^{2}} \\
&= \sqrt{2}
\end{aligned}
$$

---

**Question 5:** Consider the function $g(x, y) = \sin (y\ln x)$. What is the rate of change of $g(x, y)$ at the point $(e, π)$ in the direction in which the function increases most rapidly?

- [ ] A. $\frac{\sqrt{π^{2} + e^{2}}}{π}$
- [ ] B. $\frac{\sqrt{π + e}}{e}$
- [ ] C. $\frac{\sqrt{π^{4} + e^{2}}}{π}$
- [ ] D. $\frac{\sqrt{π^{2} + e^{2}}}{e}$
- [ ] E. $\sqrt{\frac{π + e}{e}}$

---

**Question 6:** Consider the function $g(x, y) = \frac{x^{2}}{y}$. What is the rate of change of $g(x, y)$ at the point $(2, 1)$ in the direction of the vector $v = ⟨ - 1, 3⟩$?

- [ ] A. $\frac{2\sqrt{5}}{5}$
- [ ] B. $-\frac{8\sqrt{10}}{5}$
- [ ] C. $1$
- [ ] D. $17$
- [ ] E. $-\frac{\sqrt{10}}{10}$

---

<a id="deriving-the-formula-for-the-directional-derivative"></a>
## Deriving the Formula For the Directional Derivative

Consider a surface $S$ defined by $z=f(x,y)$, and suppose that the point $P$ with coordinates $(x_0,y_0, f(x_0,y_0))$ lies on $S$. We wish to derive a formula for the directional derivative $f'_\mathbf{u}(x_0,y_0)$, where

$$
\mathbf u = \langle u_1, u_2 \rangle
$$

is a unit vector.

Geometrically, $f'_{\mathbf u}(x_0,y_0)$ can be constructed in the following way:

- Define a vertical plane $\Pi$ that's parallel to $\mathbf u$ and passes through $P$.
- Define the curve $C$ as the intersection of the surface $S$ and the plane $\Pi$.
- Then, the directional derivative $f'_{\mathbf u}$ is the slope of the tangent to $C$ at $P$.

The geometric interpretation of $f'_{\mathbf u}$ is similar to that of the partial derivatives $f_x$ and $f_y$. The only difference here is that we are not restricting the plane $\Pi$ to be parallel to the coordinate axes.

Now, since $f'_{\mathbf u}$ is tangent to $C$ and is in the direction of $\mathbf u$ at $P$, the vector

$$
\mathbf v = \langle u_1, u_2, f'_{\mathbf u}\rangle
$$

is tangent to our surface $S$ at $P$.

![](<../Source/2.4.5. Directional Derivatives/Images/f5cac8101a8b5c74c9644a747e1b2488.png>)

Recall that $\langle f_x (x_0,y_0), f_y(x_0,y_0), -1\rangle$ is known to be a normal vector to the surface at $P$. Since this vector must be perpendicular to $\mathbf v$, the dot product of the two vectors must be zero, which means that

$$
\begin{aligned} \langle u_1, u_2, f'_{\mathbf u}\rangle \cdot \langle f_x (x_0,y_0), f_y(x_0,y_0), -1\rangle &=0 \\[5pt] u_1 f_x (x_0,y_0) + u_2 f_y (x_0,y_0) - f'_{\mathbf u} &=0 \\[5pt] f'_{\mathbf u} = \nabla f (x_0,y_0) \cdot \mathbf u. \end{aligned}
$$

We can interpret this formula as the projection of $\nabla f (x_0,y_0)$ onto the direction of $\mathbf u$, as shown below.

![](<../Source/2.4.5. Directional Derivatives/Images/df30bed0139eba2df02b6c7ba680dc54.png>)

```update-progress
```

[[MA/MVC/Home|Home]]
[[MA/MVC/0. Table of Contents/TOC|Table of Contents]]
