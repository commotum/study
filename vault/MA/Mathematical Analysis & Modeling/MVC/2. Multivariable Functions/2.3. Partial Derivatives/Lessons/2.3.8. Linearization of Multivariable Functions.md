# Linearization of Multivariable Functions

<!--
lesson-id: 1937
topic-code: MVC.2.3.8
-->

## Table of Contents

- [Introduction](#introduction)
- [Linearizing a Quadratic Function](#linearizing-a-quadratic-function)
- [Estimating the Value of a Two-Variable Function](#estimating-the-value-of-a-two-variable-function)
- [Linearization of Multivariable Functions in General](#linearization-of-multivariable-functions-in-general)
- [Estimating the Value of a Three-Variable Function](#estimating-the-value-of-a-three-variable-function)
- [Finding the Best Linear Approximation of a Multivariable Function](#finding-the-best-linear-approximation-of-a-multivariable-function)

## Prerequisites

- [Approximating Functions Using Local Linearity and Linearization](<../../../../CAB/5. Analytical Applications of Differentiation/5.4. Approximating Values of a Function/Lessons/5.4.1. Approximating Functions Using Local Linearity and Linearization.md>)
- [The Gradient Vector](<../../2.4. The Gradient Vector/Lessons/2.4.1. The Gradient Vector.md>)
- [Tangent Planes to Surfaces](<2.3.7. Tangent Planes to Surfaces.md>)

---

<a id="introduction"></a>
## Introduction

We've already seen that if the function $f(x,y)$ is differentiable at the point $(a,b)$, then the equation of the tangent plane to the surface $z=f(x,y)$ at the point $(a,b)$ is given by

$$
z - f(a,b) = (x-a)\dfrac{\partial f}{\partial x}(a,b) + (y-b)\dfrac{\partial f}{\partial y}(a,b)
$$

which we can rewrite as

$$
z = f(a,b) + (x-a)\dfrac{\partial f}{\partial x}(a,b) + (y-b)\dfrac{\partial f}{\partial y}(a,b)
$$

Since $f$ is differentiable at $(a,b)$, it can be approximated by its tangent plane at $(a,b)$ *provided that* $(x,y)$ is "close" to $(a,b)$. Thus, by replacing $z$ with $f(x,y)$ in the above equation, we get the approximation

$$
f(x,y) \approx f(a,b) + (x-a)\dfrac{\partial f}{\partial x}(a,b) + (y-b)\dfrac{\partial f}{\partial y}(a,b)
$$

This equation gives the best linear approximation of the function $f(x,y)$ in an open neighborhood of $(a,b)$.

Finally, we can write the right-hand side of the above equation using the gradient vector $\nabla f(a,b)$, as follows:

$$
f(x,y) \approx f(a,b) + \nabla f(a,b) \cdot \langle x-a, y-b\rangle
$$

---

<a id="linearizing-a-quadratic-function"></a>
## Linearizing a Quadratic Function

Consider the function

$$
f(x,y)=4-x^2-y^2
$$

at the point $P(1,1)$. We want to find the best linear approximation to this function, valid in a neighborhood of $P$.

The partial derivatives of $f$ are given by

$$
\begin{aligned}
\begin{bmatrix}f_{x}(x, y) &= -2x & ⟹ & f_{x}(1, 1) =-2 \\ f_{y}(x, y)|=-2y & ⟹ & f_{y}(1, 1) =-2\end{bmatrix}
\end{aligned}
$$

The partial derivatives are continuous functions at every point, which means that $f$ is differentiable everywhere, and

$$
\nabla f (1,1) = \langle -2, -2\rangle
$$

Therefore, the best linear approximation of $f$ in a neighborhood of $(1,1)$ is given by

$$
f(x, y) \mid \approx f(1, 1) + ∇f(1, 1) \cdot ⟨x - 1, y - 1⟩; = 2 + ⟨ - 2,-2⟩ \cdot ⟨x - 1, y - 1⟩; = 2 + (-2) \cdot (x - 1) + (-2) \cdot (y - 1); = 2 - 2x + 2 - 2y + 2; = 6 - 2x - 2y
$$

Now, let's take a look at the diagram below. Notice that in the neighborhood of $(x,y)=(1,1)$, the surface and its tangent plane approximation are very close.

![](<../Source/2.3.8. Linearization of Multivariable Functions/Images/3590807e45200979bdd728ca5bbed318.png>)

Therefore, we can use this to approximate values of $f(x,y)$ that are close to $(1,1)$. For example, using our approximation, we have

$$
f(1.01, 1.01) \mid \approx 6 - 2(1.01) - 2(1.01); = 1.96
$$

The actual value of $f$ at this point is $1.9598$, so our approximation is pretty good!

---

<a id="estimating-the-value-of-a-two-variable-function"></a>
## Estimating the Value of a Two-Variable Function

**Example:** Let $f(x,y)$ be a differentiable function such that
$f(1,1) = 1, \quad f_x(x,y) = 2xy, \quad f_y(x,y) = x^2$.

Using a linear approximation of $f$ at the point $(1,1)$, estimate the value of $f(1.1, 0.9)$.

**Explanation**

The linear approximation of the function $f$ at the point $(a,b)$ is given by

$$
f(x,y) \approx f(a,b) + \nabla f(a,b)\cdot \langle x-a, y-b \rangle
$$

So, the linear approximation of $f$ at the point $(1,1)$ is

$$
f(x, y) \mid \approx f(1, 1) + ∇f(1, 1) \cdot ⟨x - 1, y - 1⟩; = f(1, 1) + ⟨f_{x}(1, 1), f_{y}(1, 1)⟩ \cdot ⟨x - 1, y - 1⟩; = 1 + ⟨f_{x}(1, 1), f_{y}(1, 1)⟩ \cdot ⟨x - 1, y - 1⟩
$$

Now, we find $f_x(1,1)$ and $f_y(1,1)$ using the given partial derivatives:

$$
\begin{aligned}
f_{x}(1, 1) &= 2(1)(1) = 2 \\
f_{y}(1, 1) &= (1)^{2} = 1
\end{aligned}
$$

Therefore, we have

$$
f(x,y) \approx 1 + \langle 2, 1 \rangle \cdot \langle x-1, y-1 \rangle
$$

Finally, we can estimate $f(1.1, 0.9)$ by evaluating the linear approximation at $x= 1.1$ and $y=0.9$, as follows:

$$
f(1.1, 0.9) \mid \approx 1 + ⟨2, 1⟩ \cdot ⟨1.1 - 1, 0.9 - 1⟩; = 1 + ⟨2, 1⟩ \cdot ⟨0.1,-0.1⟩; = 1 + 0.2 - 0.1; = 1.1
$$

---

**Question 1**

> A scientific calculator is required to answer this question.

Suppose that $f(x, y)$ is a differentiable function such that
$f(2, 5) = 6, f_{x}(2, 5) = 1, f_{y}(2, 5) =-1$.

Using a linear approximation of $f$ at the point $(2, 5)$, estimate the value of $f(2.06, 4.92)$

- [ ] A. $6.04$
- [ ] B. $5.91$
- [ ] C. $5.96$
- [ ] D. $6.14$
- [ ] E. $6.51$

---

**Question 2**

> A scientific calculator is required to answer this question.

Let $f(x, y)$ be a differentiable function such that
$f(1, 1) = 1, f_{x}(x, y) = 3x^{2}y^{4}, f_{y}(x, y) = 4x^{3}y^{3}$.

Using a linear approximation of $f$ at the point $(1, 1)$, estimate the value of $f(1.02, 0.97)$.

- [ ] A. $1.02$
- [ ] B. $1.12$
- [ ] C. $0.98$
- [ ] D. $0.94$
- [ ] E. $1.06$

---

<a id="linearization-of-multivariable-functions-in-general"></a>
## Linearization of Multivariable Functions in General

So far, we've been using the fact that if $f(x,y)$ is differentiable at the point $(a,b)$, then

$$
f(x,y) \approx f(a,b) + \nabla f(a,b)\cdot \langle x-a, y-b \rangle
$$

We can extend this result to higher dimensions.

For example, if $f(x,y, z)$ is differentiable at the point $(a,b, c)$, then

$$
f(x,y,z) \approx f(a,b,c) + \nabla f(a,b,c)\cdot \langle x-a, y-b,z-c \rangle
$$

for all $(x,y,z)$ that lie in some open neighborhood of $(a,b,c)$.

More generally, if a function $f(\mathbf x)$ in the variable

$$
\mathbf x = (x_1, x_2. \dots, x_n)
$$

is differentiable at the point $\mathbf x_0\in \Bbb R^n$, then

$$
f(\mathbf x) \approx f(\mathbf x_0) + \nabla f(\mathbf x_0) \cdot (\mathbf x-\mathbf x_0)
$$

for all $\mathbf x$ that lie in some open neighborhood of $\mathbf x_0$.

---

<a id="estimating-the-value-of-a-three-variable-function"></a>
## Estimating the Value of a Three-Variable Function

**Example:** Suppose that $f(x,y, z)$ is a differentiable function such that
$f(1,2,5)=2, \quad f_x(1,2,5)=4, \quad f_y(1,2,5)=1, \quad f_z(1,2,5)=-2$.
Using a linear approximation of $f$ at the point $(1,2,5)$ estimate the value of $f(1.01,2.02,4.95)$.

**Explanation**

The linear approximation of the function $f$ at the point $(a,b, c)$ is given by

$$
f(x,y,z) \approx f(a,b,c) + \nabla f(a,b,c)\cdot \langle x-a, y-b,z-c \rangle
$$

So, the linear approximation of $f$ at the point $(1,2,5)$ is

$$
f(x, y, z) \mid \approx f(1, 2, 5) + ∇f(1, 2, 5) \cdot ⟨x - 1, y - 2, z - 5⟩; = f(1, 2, 5) + ⟨f_{x}(1, 2, 5), f_{y}(1, 2, 5), f_{z}(1, 2, 5)⟩ \cdot ⟨x - 1, y - 2, z - 5⟩; = 2 + ⟨4, 1,-2⟩ \cdot ⟨x - 1, y - 2, z - 5⟩
$$

We can approximate $f(1.01,2.02,4.95)$ by evaluating the linear approximation at $x= 1.01$, $y=2.02$, and $z=4.95$ as follows:

$$
f(1.01, 2.02, 4.95) \mid \approx 2 + ⟨4, 1,-2⟩ \cdot ⟨1.01 - 1, 2.02 - 2, 4.95 - 5⟩; = 2 + ⟨4, 1,-2⟩ \cdot ⟨0.01, 0.02,-0.05⟩; = 2 + 0.04 + 0.02 + 0.1; = 2.16
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

Suppose that $f(x, y, z)$ is a differentiable function such that
$f(2,-2,-1) = 4, f_{x}(2,-2,-1) = 2, f_{y}(2,-2,-1) =-2, f_{z}(2,-2,-1) =-4$.
Using a linear approximation of $f$ at the point $(2,-2,-1)$ estimate the value of $f(2.02,-1.98,-1.05)$

- [ ] A. $3.9$
- [ ] B. $3.85$
- [ ] C. $4.2$
- [ ] D. $4.06$
- [ ] E. $3.91$

---

**Question 4**

> A scientific calculator is required to answer this question.

Let $f(x, y, z)$ be a differentiable function such that
$f(3, 3, 1) = 3, f_{x}(x, y, z) = \frac{yz}{2\sqrt{xyz}}, f_{y}(x, y, z) = \frac{xz}{2\sqrt{xyz}}, f_{z}(x, y, z) = \frac{xy}{2\sqrt{xyz}}$.

Using a linear approximation of $f$ at the point $(3, 3, 1)$, estimate the value of $f(3.01, 3.02, 0.98)$.

- [ ] A. $2.985$
- [ ] B. $2.825$
- [ ] C. $3.125$
- [ ] D. $3.070$
- [ ] E. $2.905$

---

<a id="finding-the-best-linear-approximation-of-a-multivariable-function"></a>
## Finding the Best Linear Approximation of a Multivariable Function

**Example:** Let $f(x,y, z)= xyz+y^2$. Find the best linear approximation of $f$ at the point $(3, -1, 1)$.

**Explanation**

The linear approximation of the function $f$ at the point $(a,b)$ is given by

$$
f(x,y,z) \approx f(a,b,c) + \nabla f(a,b,c)\cdot \langle x-a, y-b, z-c \rangle
$$

So, the linear approximation of $f$ at the point $(3, -1, 1)$ is

$$
f(x, y, z) \mid \approx f(3,-1, 1) + ∇f(3,-1, 1) \cdot ⟨x - 3, y + 1, z - 1⟩
$$

From the definition of $f$, we have that

$$
f(3, -1, 1)= -2
$$

Computing the partial derivatives, we get the following:

$$
\begin{aligned}
\begin{bmatrix}f_{x} &= yz & ⇒ & f_{x}(3,-1, 1) =-1, \\ f_{y}|= xz + 2y & ⇒ & f_{y}(3,-1, 1) = 1 \\ f_{z}|= xy & ⇒ & f_{z}(3,-1, 1) =-3\end{bmatrix}
\end{aligned}
$$

Therefore, the gradient of the function at the point $(3, -1)$ is given by

$$
\begin{aligned}
∇f(3,-1) &= ⟨ - 1, 1,-3⟩
\end{aligned}
$$

Finally, the linear approximation of the function $f$ is

$$
f(x, y, z) \mid \approx f(3,-1, 1) + ∇f(3,-1, 1) \cdot ⟨x - 3, y + 1, z - 1⟩; =-2 + ⟨ - 1, 1,-3⟩ \cdot ⟨x - 3, y + 1, z - 1⟩; =-2 - (x - 3) + (y + 1) - 3(z - 1); =-2 - x + 3 + y + 1 - 3z + 3; = 5 - x + y - 3z
$$

---

**Question 5**

> A scientific calculator is required to answer this question.

Let $f(x, y) = \ln (1 + x + y)$. Find the linear approximation of $f$ at the point $(0, 1)$.

- [ ] A. $f(x, y) \approx \ln 2 - 1 + x + y$
- [ ] B. $f(x, y) \approx \ln 2 + \frac{x}{2} + \frac{y - 1}{2}$
- [ ] C. $f(x, y) \approx \ln 2 - \frac{x}{2} + \frac{y - 1}{2}$
- [ ] D. $f(x, y) \approx \ln 2 + \frac{x}{2} - \frac{y - 1}{2}$
- [ ] E. $f(x, y) \approx \ln 2 + \frac{x}{3} + \frac{y - 1}{3}$

---

**Question 6**

> A scientific calculator is required to answer this question.

Let $f(x, y, z) = e^{xy}\cos (z)$. Find the linear approximation of $f$ at the point $(0, 1, π)$.

- [ ] A. $f(x, y, z) \approx - 2 - x + y$
- [ ] B. $f(x, y, z) \approx 1 - x$
- [ ] C. $f(x, y, z) \approx - 1 - x$
- [ ] D. $f(x, y, z) \approx - 1 + x$
- [ ] E. $f(x, y, z) \approx π - 1 - x - z$

```update-progress
```

[[MA/MVC/Home|Home]]
[[MA/MVC/0. Table of Contents/TOC|Table of Contents]]
