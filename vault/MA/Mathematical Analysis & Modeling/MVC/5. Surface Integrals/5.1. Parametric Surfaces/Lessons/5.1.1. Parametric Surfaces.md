# Parametric Surfaces

<!--
lesson-id: 1789
topic-code: MVC.5.1.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Parametrization of a Surface Defined by an Explicit Cartesian Equation](#finding-a-parametrization-of-a-surface-defined-by-an-explicit-cartesian-equation)
- [Finding a Parametrization of a Surface Defined by an Implicit Cartesian Equation](#finding-a-parametrization-of-a-surface-defined-by-an-implicit-cartesian-equation)
- [Finding a Parametrization of a Surface Defined in Cylindrical Polar Coordinates](#finding-a-parametrization-of-a-surface-defined-in-cylindrical-polar-coordinates)
- [Finding a Parametrization of a Surface Defined in Spherical Polar Coordinates](#finding-a-parametrization-of-a-surface-defined-in-spherical-polar-coordinates)

## Prerequisites

- [Graphing Curves Defined Parametrically](<../../../../PCL/8. Parametric Equations/8.1. Parametric Equations/Lessons/8.1.1. Graphing Curves Defined Parametrically.md>)
- [The Domain of a Multivariable Function](<../../../2. Multivariable Functions/2.1. Introduction to Multivariable Functions/Lessons/2.1.4. The Domain of a Multivariable Function.md>)
- [Cylindrical Polar Coordinates](<../../../3. Multiple Integrals/3.5. Change of Variables for Triple Integrals/Lessons/3.5.1. Cylindrical Polar Coordinates.md>)
- [Spherical Polar Coordinates](<../../../3. Multiple Integrals/3.5. Change of Variables for Triple Integrals/Lessons/3.5.3. Spherical Polar Coordinates.md>)

---

<a id="introduction"></a>
## Introduction

Just as a curve in three-dimensional space can be described using a vector-valued function $\mathbf r(t)$ of a single parameter $t$, we can also describe a surface in three-dimensional space using a vector-valued function $\mathbf{r}(u,v)$ of two parameters $u$ and $v$.

For example, consider the vector-valued function given by

$$
\mathbf{r}(u,v) =\langle \cos u, \: v, \: \sin u + 1 \rangle, \qquad 0\leq u \leq \dfrac{\pi}2, \quad -2\leq v \leq 2
$$

The domain of the parameter space $D$ and surface $S$ described by $\mathbf r(u,v)$ are both shown below.

![](<../Source/5.1.1. Parametric Surfaces/Images/c2991f33514c09b97ece2de694199bb4.png>)

As we vary $(u, v)$, the tip of the vector $\mathbf r(u,v)$ touches different points on the surface $S$. Each choice of point $(u,v)$ in $D$ gives a unique point on $S$, and varying $(u,v)$ throughout its entire domain traces out the entire surface $S$.

---

<a id="finding-a-parametrization-of-a-surface-defined-by-an-explicit-cartesian-equation"></a>
## Finding a Parametrization of a Surface Defined by an Explicit Cartesian Equation

**Example:** Find a vector-valued function $\mathbf{r}(u,v)$ that parametrizes the surface defined in Cartesian coordinates as $z=y^5-x^2$.

**Explanation**

A point $(x,y,z)$ on our surface has the corresponding position vector given by

$$
\mathbf{r} = x \, \mathbf{i} + y \, \mathbf{j} + z \, \mathbf{k} = x \, \mathbf{i} + y \, \mathbf{j} + \big(y^5-x^2\big) \, \mathbf{k}
$$

Now, setting $x=u$ and $y=v$, we obtain the vector function that parametrizes the surface:

$$
\mathbf{r}(u,v) = u \, \mathbf{i} + v \, \mathbf{j} + \big(v^5-u^2\big) \, \mathbf{k}
$$

---

**Question 1:** Find a vector-valued function $r(u, v)$ that parametrizes the surface defined in Cartesian coordinates as $z = x\sqrt{1 - y}$.

- [ ] A. $r(u, v) = ⟨u, v, u\sqrt{1 - v}⟩$
- [ ] B. $r(u, v) = ⟨u, v, \sqrt{1 - v}⟩$
- [ ] C. $r(u, v) = ⟨u, u\sqrt{1 - v}, v⟩$
- [ ] D. $r(u, v) = ⟨u, v, u(1 - v)⟩$
- [ ] E. $r(u, v) = ⟨u, v^{2}, \sqrt{1 - u}⟩$

---

**Question 2:** Find a vector-valued function $r(u, v)$ that parametrizes the surface defined in Cartesian coordinates as $z = x^{3} - xy$.

- [ ] A. $r(u, v) = ui + vj + (u^{3} - uv)k$
- [ ] B. $r(u, v) = vi + uj + (u^{3} - uv)k$
- [ ] C. $r(u, v) = uvi + uj + vk$
- [ ] D. $r(u, v) = ui + (u^{3} - uv)j + vk$
- [ ] E. $r(u, v) = uvi + vj + uk$

---

<a id="finding-a-parametrization-of-a-surface-defined-by-an-implicit-cartesian-equation"></a>
## Finding a Parametrization of a Surface Defined by an Implicit Cartesian Equation

**Example:** $\mathbf{r}(u,v) =\big\langle u, \: v, \: \text{_____} \big\rangle$

The vector-valued function $\mathbf{r}(u,v)$ parametrizes the surface defined in Cartesian coordinates as
$x^2z-2y+z=6$.
What is the third component of $\mathbf{r}$?

**Explanation**

First, we write the equation of the surface in the form $z=f(x,y)$:

$$
\begin{aligned}
x^{2}z - 2y + z &= 6 \\
(x^{2} + 1)z &= 6 + 2y \\
z &= (2(y + 3))/(x^{2} + 1)
\end{aligned}
$$

Now, a point $(x,y,z)$ on our surface has the corresponding position vector given by

$$
\mathbf{r} = \big\langle x, \: y, \: z \big\rangle = \left\langle x, \: y, \: \dfrac{2(y+3)}{x^2+1} \right\rangle
$$

Finally, setting $x=u$ and $y=v$, we obtain the vector function that parametrizes the surface:

$$
\mathbf{r}(u,v) =\left\langle u, \: v, \: {\color{black}\dfrac{2(v+3)}{u^2+1}} \right\rangle
$$

Therefore, the third component of $\mathbf{r}$ is

$$
{\color{black}\dfrac{2(v+3)}{u^2+1}}
$$

---

**Question 3**

$r(u, v) = ui + vj + _____k$

The vector-valued function $r(u, v)$ parametrizes the surface defined in Cartesian coordinates as
$xy - z^{2} = 1$ for $z > 0$.
What is the third component of $r$?

- [ ] A. $uv + 1$
- [ ] B. $\sqrt{uv - 1}$
- [ ] C. $\sqrt{u + v + 1}$
- [ ] D. $\sqrt{uv + 1}$
- [ ] E. $uv - 1$

---

**Question 4**

$r(u, v) = ⟨u, v, _____⟩$

The vector-valued function $r(u, v)$ parametrizes the surface defined in Cartesian coordinates as
$x^{3} + y^{2} - z^{2} = 2$
for $z > 0$. What is the third component of $r$?

- [ ] A. $\sqrt{u^{3} + v^{2}}$
- [ ] B. $\sqrt{u^{3} + v^{2} - 2}$
- [ ] C. $2 - u^{3} - v^{2}$
- [ ] D. $u^{3} + v^{2}$
- [ ] E. $u^{3} + v^{2} - 2$

---

<a id="finding-a-parametrization-of-a-surface-defined-in-cylindrical-polar-coordinates"></a>
## Finding a Parametrization of a Surface Defined in Cylindrical Polar Coordinates

**Example:** $\mathbf{f}(r,\theta) =\big\langle r\cos{\theta}, \: r\sin{\theta}, \: \text{_____} \big\rangle$

The vector-valued function $\mathbf{f}(r,\theta)$ parametrizes the surface defined in cylindrical polar coordinates as
${r^2}-4z^2=0$
for $z \geq 0$. What is the third component of $\mathbf{f}$?

**Explanation**

First, we write the equation of the surface in the form

$$
z=f(r,\theta)
$$

$$
\begin{aligned}
r^{2} - 4z^{2} &= 0 \\
4z^{2} &= r^{2} \\
z^{2} &= \frac{r^{2}}{4} \\
z &= \frac{r}{2}
\end{aligned}
$$

We disregard the negative solution since we are given that

$$
z\geq 0
$$

If $(r, \theta, z)$ are the cylindrical coordinates of a point and $(x,y,z)$ are the corresponding Cartesian coordinates of that point, then we have

$$
x = r\cos\theta, \qquad y = r\sin\theta, \qquad z = z
$$

Now, a point $(x,y,z)$ on our surface has the corresponding position vector given by

$$
\mathbf{f} = \big\langle x, \: y, \: z \big\rangle = \big\langle r\cos\theta, \: r\sin\theta, \: z \big\rangle
$$

Finally, substituting

$$
z= \dfrac r2
$$

we obtain

$$
\mathbf{f}(r,\theta) =\left\langle r\cos{\theta}, \: r\sin{\theta}, \: {\color{black} \dfrac r2} \right\rangle
$$

Therefore, the third component of $\mathbf{f}$ is

$$
{\color{black} \dfrac r2}
$$

---

**Question 5**

$f(r, θ) = ⟨r\cos θ, _____, 2r^{2}\cos θ⟩$

The vector-valued function $f(r, θ)$ parametrizes the surface defined in cylindrical polar coordinates as
$z = 2r^{2}\cos θ$.
What is the second component of $f$?

- [ ] A. $r\sin θ$
- [ ] B. $θ\cos r$
- [ ] C. $r\cos θ$
- [ ] D. $r\tan θ$
- [ ] E. $θ\sin r$

---

**Question 6**

$f(r, θ) = ⟨r\cos θ, r\sin θ, _____⟩$

The vector-valued function $f(r, θ)$ parametrizes the surface defined in cylindrical polar coordinates as
$\frac{z^{2}}{r^{2}} = θ$
for $z \ge 0$. What is the third component of $f$?

- [ ] A. $\frac{r}{\sqrt{θ}}$
- [ ] B. $\frac{r}{θ}$
- [ ] C. $r^{2}v$
- [ ] D. $r\sqrt{θ}$
- [ ] E. $θ\sqrt{r}$

---

<a id="finding-a-parametrization-of-a-surface-defined-in-spherical-polar-coordinates"></a>
## Finding a Parametrization of a Surface Defined in Spherical Polar Coordinates

**Example:** $\mathbf{f}(\theta,\phi) = \big\langle \text{_____}, \: \theta \sin\theta \sin^2 \phi, \: \theta \sin\phi \cos\phi \big\rangle$

The vector-valued function $\mathbf{f}(\theta,\phi)$ parametrizes the surface defined in spherical polar coordinates as
$\rho=\theta \sin{\phi}$.
What is the first component of $\mathbf{f}$?

**Explanation**

If $(\rho, \theta, \phi)$ are the spherical coordinates of a point, then its Cartesian coordinates $(x,y,z)$ can be found by using the following formulas:

$$
x = \rho\cos\theta\sin\phi, \qquad y = \rho\sin\theta\sin\phi, \qquad z = \rho\cos\phi
$$

Now, a point $(x,y,z)$ on our surface has the corresponding position vector given by

$$
\mathbf{f} = \big\langle x, \: y, \: z \big\rangle = \big\langle \rho\cos\theta\sin\phi, \: \rho\sin\theta\sin\phi, \: \rho\cos\phi \big\rangle
$$

Finally, substituting

$$
\rho=\theta \sin{\phi}
$$

we obtain

$$
\begin{aligned}
f(θ, ϕ) &= ⟨θ\sin ϕ \cdot \cos θ\sin ϕ, θ\sin ϕ \cdot \sin θ\sin ϕ, θ\sin ϕ \cdot \cos ϕ⟩ \\
&= ⟨θ\cos θ\sin^{2} ϕ, θ\sin θ\sin^{2} ϕ, θ\sin ϕ\cos ϕ⟩
\end{aligned}
$$

Therefore, the first component of $\mathbf{f}$ is ${\color{black}\theta \cos\theta \sin^2 \phi}$.

---

**Question 7**

$f(θ, ϕ) = ⟨_____, ϕ\cos θ\sin θ\sin ϕ, ϕ\cos θ\cos ϕ⟩$

The vector-valued function $f(θ, ϕ)$ parametrizes the surface defined in spherical polar coordinates as
$ρ = ϕ\cos θ$.
What is the first component of $f$?

- [ ] A. $ϕ\cos^{2} θ\sin ϕ$
- [ ] B. $ϕ\cos θ\sin ϕ$
- [ ] C. $ϕ\cos θ\sin^{2} ϕ$
- [ ] D. $ϕ\cos^{2} θ\sin^{2} ϕ$
- [ ] E. $ϕ\sin θ\cos ϕ$

---

**Question 8**

$f(θ, ϕ) = ⟨ϕ\cot θ\sin ϕ, ϕ\sin ϕ, _____⟩$

The vector-valued function $f(θ, ϕ)$ parametrizes the surface defined in spherical polar coordinates as
$ϕ = ρ\sin θ$.
What is the third component of $f$?

- [ ] A. $ϕ\csc θ\sin ϕ$
- [ ] B. $ϕ\cot θ\sin ϕ$
- [ ] C. $ϕ\tan θ\sin ϕ$
- [ ] D. $ϕ\csc θ\cos ϕ$
- [ ] E. $ϕ\sec θ\cos ϕ$

```update-progress
```

[[MA/MVC/Home|Home]]
[[MA/MVC/0. Table of Contents/TOC|Table of Contents]]
