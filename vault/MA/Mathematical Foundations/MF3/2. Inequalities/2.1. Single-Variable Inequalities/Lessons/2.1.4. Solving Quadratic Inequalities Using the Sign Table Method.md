# Solving Quadratic Inequalities Using the Sign Table Method

<!--
lesson-id: 118
topic-code: MF3.2.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Completing a Table of Signs](#completing-a-table-of-signs)
- [Solving a Quadratic Inequality Using the Sign Table Method](#solving-a-quadratic-inequality-using-the-sign-table-method)
- [Solving a Quadratic Inequality With No Constant Term Using the Sign Table Method](#solving-a-quadratic-inequality-with-no-constant-term-using-the-sign-table-method)
- [Solving a Quadratic Inequality Using the Sign Table Method](#solving-a-quadratic-inequality-using-the-sign-table-method)
- [Solving an Inequality by Rearranging Terms and Using the Sign Table Method](#solving-an-inequality-by-rearranging-terms-and-using-the-sign-table-method)

## Prerequisites

- [Solving Elementary Quadratic Inequalities](<2.1.1. Solving Elementary Quadratic Inequalities.md>)

---

<a id="introduction"></a>
## Introduction

A **quadratic inequality** is an inequality that involves the square of a variable. For example, the inequality

$$
x^2 +x-2 \geq 0
$$

is a quadratic inequality.

One way of solving a quadratic inequality is to use a **sign table.** A sign table is just a way of keeping track of the sign (positive or negative) of various expressions for various values of $x$.

For example, suppose we want to keep track of the sign of the expression $x+2$. First, we identify where the expression is zero:

$$
\begin{aligned}
x + 2 &= 0 \\
x &= -2
\end{aligned}
$$

Now, we split up the number line across the zero of the expression. So we have two intervals, $(-\infty, -2)$ and $(-2,\infty)$.

On each of these intervals, the expression $x+2$ is either always positive or always negative. To keep track of this, we can set up a sign table as follows:

|  | $(-\infty,-2)$ | $(-2,\infty)$ |
| --- | ---: | ---: |
| $x+2$ |  |  |

To fill out the sign table, we have to figure out the sign of $x+2$ on each of the intervals provided. We can do this by choosing test points:

- In the interval $(-\infty, -2)$, we can choose a test point of $x_1 = -3$. Substituting this test point into the expression, we get
$x+2 = -3 + 2 = -1 < 0$,
which means that the expression $x+2$ is negative $({\color{red}-})$ for all values of $x$ in the interval $(-\infty, -2)$.
- In the interval $(-2, \infty)$, we can choose a test point of $x_2 = 0$. Substituting this test point into the expression, we get
$x+2 = 0 + 2 = 2 > 0$,
which means that the expression is positive $({\color{blue}+})$ for all values of $x$ in the interval $(-2, \infty)$.

Filling out our sign table, then, we have

|  | $(-\infty,-2)$ | $(-2,\infty)$ |
| --- | ---: | ---: |
| $x+2$ | $\color{red}-$ | $\color{blue}+$ |

For now, let's focus on learning how to set up and fill out a sign table. Then, we'll learn how to use them to solve quadratic inequalities.

---

<a id="completing-a-table-of-signs"></a>
## Completing a Table of Signs

**Example:** Complete the following sign table:

|  | $(-\infty,-1)$ | $(-1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-2$ |  |  |  |
| $x+1$ |  |  |  |
| $(x-2)(x+1)$ |  |  |  |

**Explanation**

We need to fill in each box with the sign of the corresponding expression on the corresponding interval.

Let's start down the first column. In the interval $(-\infty, -1)$, we choose a test point $x_1=-2$. Then, we have

$$
\begin{aligned}
x - 2 &= -2 - 2 =-4 < 0, \\
x + 1 &= -2 + 1 =-1 < 0
\end{aligned}
$$

so both the expressions $x-2$ and $x+1$ are negative $({\color{red}-})$ on the interval $(-\infty,-1)$.

|  | $(-\infty,-1)$ | $(-1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-2$ | $\color{red}-$ |  |  |
| $x+1$ | $\color{red}-$ |  |  |
| $(x-2)(x+1)$ |  |  |  |

To find the sign of $(x-2)(x+1)$ on this interval, we can just multiply the corresponding signs of $x-2$ and $x+1$:

$$
({\color{red}-}) \times ({\color{red}-}) = ({\color{blue}+})
$$

So $(x-2)(x+1)$ is positive $({\color{blue}+})$ on this interval, and we can complete the first column of the table:

|  | $(-\infty,-1)$ | $(-1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-2$ | $\color{red}-$ |  |  |
| $x+1$ | $\color{red}-$ |  |  |
| $(x-2)(x+1)$ | $\color{blue}+$ |  |  |

To find the entries in the other columns, we use the same procedure.

- In the interval $(-1,2)$, we can choose a test point $x_2=0$. Then we have
$x - 2|= 0 - 2 = (-),; x + 1|= 0 + 1 = (+),; (x - 2)(x + 1)|= (-) \cdot (+) = (-)$,
and we fill in the table as follows:

|  | $(-\infty,-1)$ | $(-1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-2$ | $\color{red}-$ | $\color{red}-$ |  |
| $x+1$ | $\color{red}-$ | $\color{blue}+$ |  |
| $(x-2)(x+1)$ | $\color{blue}+$ | $\color{red}-$ |  |

- In the interval $(2,\infty)$, we can choose a test point $x_3=3$. Then we have
$x - 2|= 3 - 2 = (+),; x + 1|= 3 + 1 = (+),; (x - 2)(x + 1)|= (+) \cdot (+) = (+)$,
and we fill in the table as follows:

|  | $(-\infty,-1)$ | $(-1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-2$ | $\color{red}-$ | $\color{red}-$ | $\color{blue}+$ |
| $x+1$ | $\color{red}-$ | $\color{blue}+$ | $\color{blue}+$ |
| $(x-2)(x+1)$ | $\color{blue}+$ | $\color{red}-$ | $\color{blue}+$ |

---

**Question 1**

```quiz
type: radio
id: q-1
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Choose the appropriate signs for the sign table shown below.
  
  |  | $(- ∞,-11)$ | $(-11, 0)$ | $(0, ∞)$ |
  | --- | ---: | ---: | ---: |
  | $x + 11$ | $?$ | $?$ | $?$ |
  
  Which signs complete the row from left to right?
options:
- id: a
  content: |-
    $-, -, -$
- id: b
  content: |-
    $-, +, +$
- id: c
  content: |-
    $+, -, +$
- id: d
  content: |-
    $+, +, -$
- id: e
  content: |-
    $+, +, +$
```

---

**Question 2**

```quiz
type: radio
id: q-2
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What are the missing entries in the sign table below?
  
  |  | $(- ∞,-8)$ | $(-8, 6)$ | $(6, ∞)$ |
  | --- | ---: | ---: | ---: |
  | $x - 6$ | $-$ | $-$ | $?$ |
  | $x + 8$ | $-$ | $+$ | $?$ |
  | $(x - 6)(x + 8)$ | $+$ | $-$ | $?$ |
  
  Which signs complete the blanks from top to bottom?
options:
- id: a
  content: |-
    $-, +, -$
- id: b
  content: |-
    $+, -, -$
- id: c
  content: |-
    $+, +, -$
- id: d
  content: |-
    $+, +, +$
- id: e
  content: |-
    $-, -, +$
```

---

<a id="solving-a-quadratic-inequality-using-the-sign-table-method"></a>
## Solving a Quadratic Inequality Using the Sign Table Method

Given a quadratic inequality like $(x+1)(x-1) > 0$, let's see how to find the solution set using the **sign table method**.

First, we find the roots of the corresponding equation

$$
(x+1)(x-1)=0
$$

The two roots are $x=-1$ and $x=1$, and they split the number line into the $3$ subintervals $(-\infty, -1)$, $(-1,1)$, and $(1,\infty)$.

Now, we construct a sign table with the list of the intervals on the top and the list of factors on the left.

|  | $(-\infty,-1)$ | $(-1,1)$ | $(1,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-1$ |  |  |  |
| $x+1$ |  |  |  |
| $(x+1)(x-1)$ |  |  |  |

Filling in the sign table using the usual method, we get the following result:

|  | $(-\infty,-1)$ | $(-1,1)$ | $(1,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-1$ | $\color{red}-$ | $\color{red}-$ | $\color{blue}+$ |
| $x+1$ | $\color{red}-$ | $\color{blue}+$ | $\color{blue}+$ |
| $(x+1)(x-1)$ | $({\color{red}-})\cdot({\color{red}-})={\color{blue}+}$ | $({\color{red}-})\cdot({\color{blue}+})={\color{red}-}$ | $({\color{blue}+})\cdot({\color{blue}+})={\color{blue}+}$ |

Finally, to solve the quadratic inequality $(x+1)(x-1) > 0$, we look at the bottom row to find out where the expression $(x+1)(x-1)$ is positive $({\color{blue}+})$.

We see that the expression is positive on the intervals $(-\infty,-1)$ and $(1,\infty)$.

Therefore, we conclude that $(x+1)(x-1) > 0$ for all $x \in (-\infty, -1) \cup (1, \infty)$.

---

<a id="solving-a-quadratic-inequality-with-no-constant-term-using-the-sign-table-method"></a>
## Solving a Quadratic Inequality With No Constant Term Using the Sign Table Method

**Example:** Find the solution of $x^2-3x \lt 0$.

**Explanation**

First, let's factor the left-hand side of the inequality:
$\begin{bmatrix}x^{2} - 3x & < 0 \\ x(x - 3) & < 0\end{bmatrix}$

Next, we find the roots of the corresponding equation $x(x-3)=0$. The roots are $x=0$ and $x=3$, and they split the number line into the $3$ intervals $(-\infty,0)$, $(0,3)$, and $(3,\infty)$.

Now, we construct a sign table with the list of the intervals on the top and the list of factors on the left. Filling in the sign table using the usual method, we get the following result:

|  | $(-\infty,0)$ | $(0,3)$ | $(3,\infty)$ |
| --- | ---: | ---: | ---: |
| $x$ | ${\color{red}-}$ | ${\color{blue}+}$ | ${\color{blue}+}$ |
| $x-3$ | ${\color{red}-}$ | ${\color{red}-}$ | ${\color{blue}+}$ |
| $x(x-3)$ | $({\color{red}-})\cdot({\color{red}-})={\color{blue}+}$ | $({\color{blue}+})\cdot({\color{red}-})={\color{red}-}$ | $({\color{blue}+})\cdot({\color{blue}+})={\color{blue}+}$ |

Finally, to solve the quadratic inequality $x(x-3) \lt 0$, we look at the bottom row to find out where the expression $x(x-3)$ is negative.

We see that the expression is negative on the interval $(0, 3)$.

Therefore, we conclude that $x^2-3x \lt 0$ for all $x\in (0,3)$.

---

**Question 3:**

```quiz
type: radio
id: q-3
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $x^{2} - 5x > 0$?
options:
- id: a
  content: |-
    $x ∈ (- ∞, 0)∪(5, ∞)$
- id: b
  content: |-
    $x ∈ (0, 5)$
- id: c
  content: |-
    $x ∈ (- ∞,-5)∪(5, ∞)$
- id: d
  content: |-
    $x ∈ (-5, 0)$
- id: e
  content: |-
    $x ∈ (- ∞,-5)∪(0, ∞)$
```

---

**Question 4:**

```quiz
type: radio
id: q-4
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $x^{2} - 2x < 0$?
options:
- id: a
  content: |-
    $x ∈ (0, 2)$
- id: b
  content: |-
    $x ∈ (- ∞,-2)∪(0, ∞)$
- id: c
  content: |-
    $x ∈ (- ∞,-2)∪(2, ∞)$
- id: d
  content: |-
    $x ∈ (- ∞, 0)∪(2, ∞)$
- id: e
  content: |-
    $x ∈ (-2, 0)$
```

---

**Question 5:**

```quiz
type: radio
id: q-5
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $x^{2} + 6x \le 0$?
options:
- id: a
  content: |-
    $[MATH: x ∈ [0, ∞)]$
- id: b
  content: |-
    $[MATH: x ∈ [- 6, ∞)]$
- id: c
  content: |-
    $[MATH: x ∈ (- ∞,-6]∪[0, ∞)]$
- id: d
  content: |-
    $x ∈ [- 6, 0]$
- id: e
  content: |-
    $[MATH: x ∈ (- ∞, 0]]$
```

---

<a id="solving-a-quadratic-inequality-using-the-sign-table-method"></a>
## Solving a Quadratic Inequality Using the Sign Table Method

**Example:** Find the solution of $x^2-3x +2\geq 0$.

**Explanation**

First, let's factor the left-hand side of the inequality:

$$
\begin{bmatrix}x^{2} - 3x + 2 & \ge 0 \\ (x - 1)(x - 2) & \ge 0\end{bmatrix}
$$

Next, we find the roots of the corresponding equation

$$
(x-1)(x-2)=0
$$

The roots are $x = 1$ and $x=2$, and they split the number line into the $3$ subintervals $(-\infty,1)$, $(1,2)$, and $(2,\infty)$.

Now, we construct a sign table with the list of the intervals on the top and the list of factors on the left. Filling in the sign table using the usual method, we get the following result:

|  | $(-\infty,1)$ | $(1,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x-1$ | ${\color{red}-}$ | ${\color{blue}+}$ | ${\color{blue}+}$ |
| $x-2$ | ${\color{red}-}$ | ${\color{red}-}$ | ${\color{blue}+}$ |
| $(x-1)(x-2)$ | $({\color{red}-})\cdot({\color{red}-})={\color{blue}+}$ | $({\color{blue}+})\cdot({\color{red}-})={\color{red}-}$ | $({\color{blue}+})\cdot({\color{blue}+})={\color{blue}+}$ |

Finally, to solve the quadratic inequality

$$
(x-1)(x-2) \geq 0
$$

we look at the bottom row to find out where the expression $(x-1)(x-2)$ is zero or positive.

We see that the expression is positive on the intervals $(-\infty, 1)$ and $(2,\infty)$. Also, we know that the expression equals $0$ at the points $x=1$ and $x=2$.

Therefore, we conclude that

$$
(x-1)(x-2) \geq 0
$$

for all $[MATH: x\in (-\infty, 1] \cup [2, \infty).]$

---

**Question 6:**

```quiz
type: radio
id: q-6
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $x^{2} - 3x - 10 > 0$?
options:
- id: a
  content: |-
    $x ∈ (-5, 2)$
- id: b
  content: |-
    $x ∈ (-2, 5)$
- id: c
  content: |-
    $x ∈ (- ∞,-5)∪(2, ∞)$
- id: d
  content: |-
    $x ∈ (- ∞, 2)∪(5, ∞)$
- id: e
  content: |-
    $x ∈ (- ∞,-2)∪(5, ∞)$
```

---

**Question 7:**

```quiz
type: radio
id: q-7
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the solution of $x^{2} - x - 42 < 0$.
options:
- id: a
  content: |-
    $x ∈ (-7, 6)$
- id: b
  content: |-
    $x ∈ (-6, 7)$
- id: c
  content: |-
    $x ∈ (- ∞,-7)∪(6, ∞)$
- id: d
  content: |-
    $x ∈ (- ∞,-6)∪(7, ∞)$
- id: e
  content: |-
    $x ∈ (- ∞, 6)∪(7, ∞)$
```

---

**Question 8:**

```quiz
type: radio
id: q-8
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $3x^{2} - 3x - 36 \le 0$?
options:
- id: a
  content: |-
    $[MATH: x ∈ (- ∞,-3]∪[4, ∞)]$
- id: b
  content: |-
    $x ∈ [3, 4]$
- id: c
  content: |-
    $[MATH: x ∈ (- ∞, 3]∪[4, ∞)]$
- id: d
  content: |-
    $x ∈ [- 3, 4]$
- id: e
  content: |-
    $x ∈ [- 4, 3]$
```

---

<a id="solving-an-inequality-by-rearranging-terms-and-using-the-sign-table-method"></a>
## Solving an Inequality by Rearranging Terms and Using the Sign Table Method

**Example:** Find the solution of $x^2+x \leq 6$.

**Explanation**

First, we rewrite the given inequality so that the right-hand side equals zero. Then, we factor the left-hand side:

$$
\begin{bmatrix}x^{2} + x & \le 6 \\ x^{2} + x - 6 & \le 0 \\ (x - 2)(x + 3) & \le 0\end{bmatrix}
$$

Next, we find the roots of the corresponding equation

$$
(x-2)(x+3) = 0
$$

The roots are $x=2$ and $x=-3$, and they split the number line into $3$ subintervals $(-\infty, -3)$, $(-3,2)$, and $(2,\infty)$.

Now, we construct a sign table with the list of the intervals on the top and the list of factors on the left. Filling in the sign table using the usual method, we get the following result:

|  | $(-\infty,-3)$ | $(-3,2)$ | $(2,\infty)$ |
| --- | ---: | ---: | ---: |
| $x+3$ | ${\color{red}-}$ | ${\color{blue}+}$ | ${\color{blue}+}$ |
| $x-2$ | ${\color{red}-}$ | ${\color{red}-}$ | ${\color{blue}+}$ |
| $(x+3)(x-2)$ | ${\color{blue}+}$ | ${\color{red}-}$ | ${\color{blue}+}$ |

Finally, to solve the quadratic inequality

$$
(x-2)(x+3) \leq 0
$$

we look at the bottom row to find out where the expression $(x-2)(x+3)$ is zero or negative.

We see that the expression is negative on the interval $(-3,2)$. Also, we know that the expression equals zero at the points $x=-3$ and $x=2$.

Therefore, we conclude that

$$
(x-2)(x+3) \leq 0
$$

for all $x\in[-3,2]$.

---

**Question 9:**

```quiz
type: radio
id: q-9
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  For which values of $x$ is $x^{2} - x \ge 2x + 10$?
options:
- id: a
  content: |-
    $[MATH: x ∈ [- 5, ∞)]$
- id: b
  content: |-
    $x ∈ [- 5, 2]$
- id: c
  content: |-
    $[MATH: x ∈ (- ∞,-5]∪[2, ∞)]$
- id: d
  content: |-
    $x ∈ [- 2, 5]$
- id: e
  content: |-
    $[MATH: x ∈ (- ∞,-2]∪[5, ∞)]$
```

---

**Question 10:**

```quiz
type: radio
id: q-10
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find the solution of $\frac{1}{2}x^{2} - \frac{1}{2}x \le 3$.
options:
- id: a
  content: |-
    $[MATH: x ∈ (- ∞,-2]∪[3, ∞)]$
- id: b
  content: |-
    $[MATH: x ∈ (- ∞, 2]∪[3, ∞)]$
- id: c
  content: |-
    $x ∈ [2, 3]$
- id: d
  content: |-
    $x ∈ [- 2, 3]$
- id: e
  content: |-
    $x ∈ [- 3,-2]$
```

```update-progress
```

[[MA/MF3/Home|Home]]
[[MA/MF3/0. Table of Contents/TOC|Table of Contents]]
