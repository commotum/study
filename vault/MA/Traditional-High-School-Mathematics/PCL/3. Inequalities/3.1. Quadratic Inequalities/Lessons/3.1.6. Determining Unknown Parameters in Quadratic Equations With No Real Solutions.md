# Determining Unknown Parameters in Quadratic Equations With No Real Solutions

<!--
lesson-id: 6289
topic-code: PCL.3.1.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining Ranges of Parameter Values in Quadratic Equations With No Real Solutions](#determining-ranges-of-parameter-values-in-quadratic-equations-with-no-real-solutions)
- [Determining Extreme Parameter Values in Quadratic Equations With No Real Solutions](#determining-extreme-parameter-values-in-quadratic-equations-with-no-real-solutions)

## Prerequisites

- [Determining Unknown Parameters in Quadratic Equations With Real Solutions](<3.1.5. Determining Unknown Parameters in Quadratic Equations With Real Solutions.md>)

---

<a id="introduction"></a>
## Introduction

Recall that for a quadratic equation

$$
ax^2 + bx + c = 0
$$

with

$$
a\neq 0
$$

the discriminant is defined as

$$
\mathcal{D} = b^2 - 4ac
$$

A quadratic equation has no real solutions if the discriminant is negative: $\mathcal{D} < 0$.

If a quadratic equation contains an unknown parameter and we know the equation has no real solutions, we can use the condition $\mathcal D < 0$ to place conditions on the unknown parameter.

To demonstrate, let's consider the equation

$$
2x^2-5kx+10 = 0
$$

where $k$ is a real parameter. We wish to find *all* the values of $k$ for which the quadratic equation has no real solutions.

To compute the discriminant, we first note that the coefficients are

$$
a=2,\quad b=-5k,\quad c=10
$$

So, we require

$$
\begin{bmatrix}D & < 0 \\ b^{2} - 4ac & < 0 \\ (-5k)^{2} - 4(2)(10) & < 0 \\ 25k^{2} - 80 & < 0\end{bmatrix}
$$

This is a quadratic inequality in the variable $k$. Solving this equation for $k$, we get the following:

$$
25k^{2} - 80\begin{vmatrix}< 0 \\ 25k^{2}\end{vmatrix}< 80; k^{2}\begin{vmatrix}< \frac{16}{5} \\ \sqrt{k^{2}}\end{vmatrix}< \sqrt{\frac{16}{5}}; \mid k \mid \mid < \frac{4}{\sqrt{5}}; \mid k \mid \mid < \frac{4\sqrt{5}}{5}
$$

Therefore, our solution is

$$
-\dfrac{4\sqrt{5}}{5} < k < \dfrac{4\sqrt{5}}{5}
$$

We can also express this solution using interval notation:

$$
k \in \left(-\dfrac{4\sqrt{5}}{5},\dfrac{4\sqrt{5}}{5}\right)
$$

Any value of $k$ in this interval will give a quadratic equation with no real solutions.

---

<a id="determining-ranges-of-parameter-values-in-quadratic-equations-with-no-real-solutions"></a>
## Determining Ranges of Parameter Values in Quadratic Equations With No Real Solutions

**Example:** Given that $kx^2 + 6x + k=0$, where $k$ is a non-zero real number, find the values of $k$ for which the equation has no real solutions.

**Explanation**

A quadratic equation has no real solutions if the discriminant is negative: $\mathcal{D} < 0$.

To compute the discriminant, note the following coefficients: $a = k$, $b = 6$ and $c = k$.

So, we require

$$
D\begin{vmatrix}< 0 \\ b^{2} - 4ac\end{vmatrix}< 0; (6)^{2} - 4(k)(k)\begin{vmatrix}< 0 \\ 36 - 4k^{2}\end{vmatrix}< 0; 36\begin{vmatrix}< 4k^{2} \\ k^{2}\end{vmatrix}> 9; \sqrt{k^{2}} \mid > \sqrt{9}; \mid k \mid \mid > 3
$$

If $\mid k \mid > 3$, then $k \lt -3$ or $k \gt 3$. Using interval notation, then, our solution is $k \in (-\infty, -3) \cup (3, \infty)$.

---

**Question 1:** Given that $x^{2} + 12kx + 3 = 0$, find the values of $k$ for which the equation
has no real solutions.

- [ ] A. $k < - \frac{\sqrt{3}}{6}$ or $k > \frac{\sqrt{3}}{6}$
- [ ] B. $k > \frac{\sqrt{3}}{6}$
- [ ] C. $k < - \frac{\sqrt{3}}{6}$
- [ ] D. $-\frac{\sqrt{3}}{6} < k < \frac{\sqrt{3}}{6}$
- [ ] E. $k < \frac{\sqrt{3}}{6}$

---

**Question 2:** Given that $kx^{2} + 2x + 3k = 0$, where $k$ is a non-zero real number, find the values of $k$ for which the equation has no real solutions.

- [ ] A. $k ∈ (-\frac{\sqrt{3}}{3}, 0)∪(0, \frac{\sqrt{3}}{3})$
- [ ] B. $k ∈ (- ∞,-\frac{\sqrt{3}}{3})∪(\frac{\sqrt{3}}{3}, ∞)$
- [ ] C. $k ∈ (- ∞,-\frac{\sqrt{3}}{3})$
- [ ] D. $k ∈ (\frac{\sqrt{3}}{3}, ∞)$
- [ ] E. $k ∈ (-\sqrt{3}, 0)∪(0, \sqrt{3})$

---

<a id="determining-extreme-parameter-values-in-quadratic-equations-with-no-real-solutions"></a>
## Determining Extreme Parameter Values in Quadratic Equations With No Real Solutions

**Example:** Given that $x(px - 24) = -9$, where $p$ is an **integer** constant, has no real solutions, what is the least possible value of $p$?

**Explanation**

A quadratic equation has no real solutions if the discriminant is negative: $\mathcal{D} < 0$.

First, rewrite the equation in standard quadratic form:

$$
\begin{aligned}
x(px - 24) &= -9 \\
px^{2} - 24x &= -9 \\
px^{2} - 24x + 9 &= 0
\end{aligned}
$$

The coefficients of our quadratic equation are the following:

$$
a = p, \qquad b = -24, \qquad c = 9
$$

So, computing the discriminant, we get

$$
\begin{aligned}
D &= b^{2} - 4ac \\
&= (-24)^{2} - 4(p)(9) \\
&= 576 - 36p
\end{aligned}
$$

To have no real solutions, we need $\mathcal{D} < 0{:}$

$$
\begin{bmatrix}576 - 36p & < 0 \\ -36p & < - 576 \\ p & > \frac{-576}{-36} \\ p & > 16\end{bmatrix}
$$

Since $p$ must be an integer, we conclude that the least possible value is $17$.

---

**Question 3**

Given that $12x^{2} - 20x + 5k = 0$, where $k$ is an **integer** constant, has no real solutions, what is the least possible value of $k$?The least possible value of $k$ is
$\underline{\hspace{1.5cm}}$.

---

**Question 4**

Given that $4x^{2} + 3kx + 9 = 0$, where $k$ is an **integer** constant, has no real solutions, what is the least possible value of $k$?The least possible value of $k$ is
$\underline{\hspace{1.5cm}}$.

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Traditional-High-School-Mathematics/PCL/Home|Home]]
[[MA/Traditional-High-School-Mathematics/PCL/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
