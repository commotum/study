# Products of Complex Numbers Expressed in Polar Form

<!--
lesson-id: 1231
topic-code: PCL.10.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Product of Two Complex Numbers Expressed in Polar Form](#calculating-the-product-of-two-complex-numbers-expressed-in-polar-form)
- [Calculating the Product of Two Complex Numbers With Negative Arguments](#calculating-the-product-of-two-complex-numbers-with-negative-arguments)
- [Justification of the Formula](#justification-of-the-formula)

## Prerequisites

- [The Sum and Difference Formulas for Sine](<../../../4. Trigonometry/4.3. The Sum and Difference Formulas/Lessons/4.3.1. The Sum and Difference Formulas for Sine.md>)
- [The Sum and Difference Formulas for Cosine](<../../../4. Trigonometry/4.3. The Sum and Difference Formulas/Lessons/4.3.2. The Sum and Difference Formulas for Cosine.md>)
- [The Polar Form of a Complex Number](<10.3.1. The Polar Form of a Complex Number.md>)

---

<a id="introduction"></a>
## Introduction

Consider the following complex numbers $z_1$ and $z_2$ expressed in polar form:

$$
\begin{aligned}
z_{1} &= r_{1}(\cos θ_{1} + \textrm{i}\sin θ_{1}) \\
z_{2} &= r_{2}(\cos θ_{2} + \textrm{i}\sin θ_{2})
\end{aligned}
$$

To compute the product $z_1\cdot z_2$, we can use the following formula:

$$
z_1z_2 = r_1 r_2\left(\cos(\theta_1+\theta_2) + \textrm{i} \sin(\theta_1+\theta_2)\right)
$$

For example, let's use this formula to compute the product of the following two complex numbers:

$$
\begin{aligned}
z_{1} &= 2[\cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})] \\
z_{2} &= 3[\cos (\frac{2π}{3}) + \textrm{i}\sin (\frac{2π}{3})]
\end{aligned}
$$

Here, we have

$$
{\color{black}r_1}={\color{black}2}
$$

$$
{\color{black}r_2}={\color{black}3}
$$

$$
{\color{black}\theta_1}={\color{black}\dfrac{\pi}{3}}
$$

and

$$
{\color{black}\theta_2}={\color{black}\dfrac{2\pi}{3}}
$$

Applying the formula, we get

$$
\begin{aligned}
z_{1}z_{2} &= 2 \cdot 3[\cos (\frac{π}{3} + \frac{2π}{3}) + \textrm{i}\sin (\frac{π}{3} + \frac{2π}{3})] \\
&= 6(\cos π + \textrm{i}\sin π)
\end{aligned}
$$

This is the final answer expressed on polar form. However, by evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
z_{1}z_{2} &= 6(\cos π + \textrm{i}\sin π) \\
&= 6((-1) + \textrm{i}(0)) \\
&=-6
\end{aligned}
$$

So, we conclude that

$$
z_1\cdot z_2 = -6
$$

At the end of this lesson, we will show where the formula comes from. But for now, let's focus on using it.

---

<a id="calculating-the-product-of-two-complex-numbers-expressed-in-polar-form"></a>
## Calculating the Product of Two Complex Numbers Expressed in Polar Form

**Example:** If $z_1=2\left[\cos \left(\dfrac{\pi}{2} \right) +\textrm{i}\sin \left(\dfrac{\pi}{2}\right) \right]$ and $z_2=4\left[\cos \left(\dfrac{5\pi}{6} \right) +\textrm{i}\sin \left(\dfrac{5\pi}{6}\right) \right]$, calculate the product $z_1z_2$.

**Explanation**

To compute the product of two complex numbers, we use the formula

$$
\begin{aligned} z_1z_2=r_1r_2[\cos(\theta_1+\theta_2)+\textrm{i}\sin(\theta_1+\theta_2)]. \end{aligned}
$$

Substituting in $r_1=2$, $r_2 =4$,

$$
\theta_1=\dfrac{\pi}{2}
$$

and

$$
\theta_2=\dfrac{5\pi}{6}
$$

we get

$$
\begin{aligned} z_1z_2&=2\cdot 4\left[\cos\left(\dfrac{\pi}{2}+\dfrac{5\pi}{6}\right)+\textrm{i}\sin\left(\dfrac{\pi}{2}+\dfrac{5\pi}{6}\right)\right] \\[5pt] &=8\left[ \cos \left(\dfrac{4\pi}{3} \right) + \textrm{i} \sin \left(\dfrac{4\pi}{3} \right) \right]. \end{aligned}
$$

By evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
z_{1}z_{2} &= 8[(-\frac{1}{2}) + \textrm{i}(-\frac{\sqrt{3}}{2})] \\
&=-4 - 4\sqrt{3}\textrm{i}
\end{aligned}
$$

---

**Question 1:** If $z_{1} = 5[\cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})]$ and $z_{2} = 4[\cos (\frac{π}{6}) + \textrm{i}\sin (\frac{π}{6})]$, calculate the product $z_{1}z_{2}$.

- [ ] A. $20\textrm{i}$
- [ ] B. $-20\textrm{i}$
- [ ] C. $1 - 20\textrm{i}$
- [ ] D. $-1 - 20\textrm{i}$
- [ ] E. $-1 + 20\textrm{i}$

---

**Question 2:** If $z_{1} = 3[\cos (\frac{π}{4}) + \textrm{i}\sin (\frac{π}{4})]$ and $z_{2} = [\cos (\frac{π}{6}) + \textrm{i}\sin (\frac{π}{6})]$, calculate the product $z_{1}z_{2}$.

- [ ] A. $3[\cos (\frac{5π}{12}) + \textrm{i}\sin (\frac{5π}{12})]$
- [ ] B. $3[\cos (\frac{7π}{12}) + \textrm{i}\sin (\frac{7π}{12})]$
- [ ] C. $3[\cos (\frac{5π}{9}) + \textrm{i}\sin (\frac{5π}{9})]$
- [ ] D. $3[\cos (\frac{4π}{3}) + \textrm{i}\sin (\frac{4π}{3})]$
- [ ] E. $3[\cos (\frac{3π}{8}) + \textrm{i}\sin (\frac{3π}{8})]$

---

<a id="calculating-the-product-of-two-complex-numbers-with-negative-arguments"></a>
## Calculating the Product of Two Complex Numbers With Negative Arguments

**Example:** If $z_1=2\left[\cos\left(-\dfrac{\pi}{6}\right)+\textrm{i}\sin\left(-\dfrac{\pi}{6}\right)\right]$ and $z_2=6\left[\cos \left(\dfrac{2\pi}{3} \right) +\textrm{i}\sin \left(\dfrac{2\pi}{3}\right) \right]$, find the the product $z_1z_2$.

**Explanation**

To compute the product of two complex numbers, we use the formula

$$
\begin{aligned} z_1z_2=r_1r_2[\cos(\theta_1+\theta_2)+\textrm{i}\sin(\theta_1+\theta_2)]. \end{aligned}
$$

Substituting in $r_1=2$, $r_2 =6$,

$$
\theta_1=-\dfrac{\pi}{6}
$$

and

$$
\theta_2=\dfrac{2\pi}{3}
$$

we get

$$
\begin{aligned} z_1z_2&=12\left[\cos\left(-\dfrac{\pi}{6}+\dfrac{2\pi}{3}\right)+\textrm{i}\sin\left(-\dfrac{\pi}{6}+\dfrac{2\pi}{3}\right)\right] \\[5pt] &=12\left[\cos\left(\dfrac{\pi}{2}\right)+\textrm{i}\sin\left(\dfrac{\pi}{2}\right)\right]. \end{aligned}
$$

By evaluating the sine and cosine, we can get the expression in Cartesian form:

$$
\begin{aligned}
z_{1}z_{2} &= 12[(0) + \textrm{i}(1)] \\
&= 12\textrm{i}
\end{aligned}
$$

---

**Question 3:** If $z_{1} = 2[\cos (-\frac{2π}{3}) + \textrm{i}\sin (-\frac{2π}{3})]$ and $z_{2} = \cos (\frac{π}{3}) + \textrm{i}\sin (\frac{π}{3})$, find the product $z_{1}z_{2}$.

- [ ] A. $2 + \sqrt{3}\textrm{i}$
- [ ] B. $1 - \sqrt{2}\textrm{i}$
- [ ] C. $2 - \sqrt{2}\textrm{i}$
- [ ] D. $1 - \sqrt{3}\textrm{i}$
- [ ] E. $1 + \sqrt{2}\textrm{i}$

---

**Question 4:** If $z_{1} = \cos (π) + \textrm{i}\sin (π)$ and $z_{2} = 3[\cos (-\frac{5π}{2}) + \textrm{i}\sin (-\frac{5π}{2})]$, find the product $z_{1}z_{2}$.

- [ ] A. $3 + 3\textrm{i}$
- [ ] B. $3 + \textrm{i}$
- [ ] C. $1 + 3\textrm{i}$
- [ ] D. $\textrm{i}$
- [ ] E. $3\textrm{i}$

---

<a id="justification-of-the-formula"></a>
## Justification of the Formula

To compute the product of the complex numbers

$$
\begin{aligned}
z_{1} &= r_{1}(\cos θ_{1} + \textrm{i}\sin θ_{1}) \\
z_{2} &= r_{2}(\cos θ_{2} + \textrm{i}\sin θ_{2})
\end{aligned}
$$

we have been using the formula

$$
z_1z_2 = r_1 r_2\left(\cos(\theta_1+\theta_2) + \textrm{i} \sin(\theta_1+\theta_2)\right)
$$

To see why the formula works, let's compute the product manually:

$$
\begin{aligned}
z_{1}z_{2} &= r_{1}(\cos θ_{1} + \textrm{i}\sin θ_{1}) \cdot r_{2}(\cos θ_{2} + \textrm{i}\sin θ_{2}) \\
&= r_{1}r_{2}(\cos θ_{1} + \textrm{i}\sin θ_{1})(\cos θ_{2} + \textrm{i}\sin θ_{2}) \\
&= r_{1}r_{2}[(\cos θ_{1}\cos θ_{2} - \sin θ_{1}\sin θ_{2}) + \textrm{i}(\sin θ_{1}\cos θ_{2} + \cos θ_{1}\sin θ_{2})]
\end{aligned}
$$

We now recall the addition formulas for cosine and sine:

$$
\begin{aligned}
\cos (θ_{1} + θ_{2}) &= \cos θ_{1}\cos θ_{2} - \sin θ_{1}\sin θ_{2} \\
\sin (θ_{1} + θ_{2}) &= \sin θ_{1}\cos θ_{2} + \cos θ_{1}\sin θ_{2}
\end{aligned}
$$

Substituting the above into our expression for $z_1z_2$, we finally arrive at

$$
z_1z_2 = r_1 r_2\left(\cos(\theta_1+\theta_2) + \textrm{i} \sin(\theta_1+\theta_2)\right)
$$

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Traditional-High-School-Mathematics/PCL/Home|Home]]
[[MA/Traditional-High-School-Mathematics/PCL/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
