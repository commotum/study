# Special Properties of the Complex Conjugate

<!--
lesson-id: 905
topic-code: PCL.10.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Sum of a Complex Number and Its Conjugate](#calculating-the-sum-of-a-complex-number-and-its-conjugate)
- [Calculating the Product of a Complex Number and its Conjugate](#calculating-the-product-of-a-complex-number-and-its-conjugate)
- [Calculating the Product of a Complex Number and Its Conjugate](#calculating-the-product-of-a-complex-number-and-its-conjugate)
- [Some Other Properties of the Complex Conjugate](#some-other-properties-of-the-complex-conjugate)

## Prerequisites

- [Multiplying Complex Numbers](<../../../../AG2/1. Number Systems/1.2. Introduction to Complex Numbers/Lessons/1.2.5. Multiplying Complex Numbers.md>)
- [The Complex Conjugate](<10.2.1. The Complex Conjugate.md>)

---

<a id="introduction"></a>
## Introduction

When we add a complex number and its conjugate, the result is always twice the real part of the complex number. That is to say,

$$
z + \overline{z} = 2\operatorname{Re}(z)
$$

where $\operatorname{Re}(z)$ is the real part of $z$.

For example, suppose we have a complex number

$$
z = 3+4\textrm{i}
$$

To obtain the conjugate, we just flip the sign of the imaginary part and get

$$
\overline{z} = 3 - 4 \textrm{i}
$$

Now, adding $z$ and its conjugate $\overline{z}$, we find that the real part $(3)$ is doubled while the imaginary part $(4)$ is canceled out:

$$
\begin{aligned}
z + z^(―) &= (3 + 4\textrm{i}) + (3 - 4\textrm{i}) \\
&= (3 + 3) + (4\textrm{i} - 4\textrm{i}) \\
&= 6 + 0\textrm{i} \\
&= 6
\end{aligned}
$$

This result holds in general. For any complex number

$$
z=x+\textrm{i}y
$$

the conjugate is

$$
\overline{z} = x-\textrm{i}y
$$

and when we add, we find that the real part $(x)$ is doubled while the imaginary part $(y)$ is canceled out:

$$
\begin{aligned}
z + z^(―) &= (x + \textrm{i}y) + (x - \textrm{i}y) \\
&= (x + x) + (\textrm{i}y - \textrm{i}y) \\
&= 2x + \textrm{i}0 \\
&= 2x \\
&= 2Re(z)
\end{aligned}
$$

---

<a id="calculating-the-sum-of-a-complex-number-and-its-conjugate"></a>
## Calculating the Sum of a Complex Number and Its Conjugate

**Example:** If $z=7-3\textrm{i}$, calculate $z+\overline{z}$.

**Explanation**

*Method 1*

When we add a complex number and its conjugate, the result is always twice the real part of the complex number.

For the given complex number

$$
z=7-3\textrm{i}
$$

the real part is

$$
\operatorname{Re}(z) = 7
$$

so we have

$$
\begin{aligned}
z + z^(―) &= 2Re(z) \\
&= 2(7) \\
&= 14
\end{aligned}
$$

*Method 2*

Alternatively, we can actually carry out the process of adding the complex number and its conjugate.

To obtain the conjugate of the complex number

$$
z=7-3\textrm{i}
$$

we flip the sign of the imaginary part and get

$$
\overline{z} = 7 + 3 \textrm{i}
$$

Adding, we get

$$
\begin{aligned}
z + z^(―) &= (7 - 3\textrm{i}) + (7 + 3\textrm{i}) \\
&= (7 + 7) + (-3\textrm{i} + 3\textrm{i}) \\
&= 14 + \textrm{i}0 \\
&= 14
\end{aligned}
$$

---

**Question 1:** If $z =-2 + 4\textrm{i}$, calculate $z + z^(―)$.

- [ ] A. $-4$
- [ ] B. $-4\textrm{i}$
- [ ] C. $-4 + 4\textrm{i}$
- [ ] D. $4\textrm{i}$
- [ ] E. $4$

---

**Question 2:** If $z = 1 - 4\textrm{i}$, calculate $z + z^(―)$.

- [ ] A. $-2\textrm{i}$
- [ ] B. $-2$
- [ ] C. $1 + 2\textrm{i}$
- [ ] D. $2\textrm{i}$
- [ ] E. $2$

---

<a id="calculating-the-product-of-a-complex-number-and-its-conjugate"></a>
## Calculating the Product of a Complex Number and its Conjugate

When we multiply a complex number and its conjugate, the result is always the sum of squares of the real part and the imaginary part. That is to say,

$$
z\overline{z} = x^2 + y^2 = (\operatorname{Re}(z))^2 + (\operatorname{Im}(z))^2
$$

For example, suppose we have a complex number

$$
z=3+4\textrm{i}
$$

To obtain the conjugate, we just flip the sign of the imaginary part and get

$$
\overline{z} = 3 - 4 \textrm{i}
$$

Now, multiplying $z$ and its conjugate $\overline{z}$, we find that the result is the sum of squares of the real part $(3^2=9)$ and the imaginary part $(4^2=16)$:

$$
\begin{aligned}
zz^(―) &= (3 + 4\textrm{i})(3 - 4\textrm{i}) \\
&= 3^{2} - 3(4\textrm{i}) + 3(4\textrm{i}) - 4^{2}\textrm{i}^{2} \\
&= 9 - 12\textrm{i} + 12\textrm{i} - 16(-1) \\
&= 9 + 16 \\
&= 25
\end{aligned}
$$

This result holds in general. For any complex number

$$
z=x+\textrm{i}y
$$

the conjugate is

$$
\overline{z} = x-\textrm{i}y
$$

and when we multiply, we find that the result is the sum of squares of the real part $(x^2)$ and the imaginary part $(y^2)$:

$$
\begin{aligned}
zz^(―) &= (x + \textrm{i}y)(x - \textrm{i}y) \\
&= x^{2} - \textrm{i}xy + \textrm{i}xy - \textrm{i}^{2}y^{2} \\
&= x^{2} - (-1)y^{2} \\
&= x^{2} + y^{2} \\
&= (Re(z))^{2} + (Im(z))^{2}
\end{aligned}
$$

---

<a id="calculating-the-product-of-a-complex-number-and-its-conjugate"></a>
## Calculating the Product of a Complex Number and Its Conjugate

**Example:** Given that $z=7-4\textrm{i}$, calculate $z\overline{z}$.

**Explanation**

*Method 1*

When we multiply a complex number and its conjugate, the result is always the sum of squares of the real part and the imaginary part.

For the given complex number

$$
z=7-4\textrm{i}
$$

the real part is

$$
\textrm{Re}(z)=7
$$

and the imaginary part is

$$
\textrm{Im}(z)=-4
$$

so we have

$$
\begin{aligned}
zz^(―) &= (Re(z))^{2} + (Im(z))^{2} \\
&= 7^{2} + (-4)^{2} \\
&= 49 + 16 \\
&= 65
\end{aligned}
$$

*Method 2*

Alternatively, we can actually carry out the process of multiplying the complex number and its conjugate.

To obtain the conjugate of the complex number

$$
z=7-4\textrm{i}
$$

we flip the sign of the imaginary part and get

$$
\overline{z} = 7 + 4 \textrm{i}
$$

Multiplying, we get

$$
\begin{aligned}
zz^(―) &= (7 - 4\textrm{i})(7 + 4\textrm{i}) \\
&= 7^{2} + (7)(4)\textrm{i} + (-4)(7)\textrm{i} - (4)^{2}\textrm{i}^{2} \\
&= 49 + 28\textrm{i} - 28\textrm{i} - (16)(-1) \\
&= 49 + 16 \\
&= 65
\end{aligned}
$$

---

**Question 3:** Given that $z = 2 + 5\textrm{i}$, calculate $zz^(―)$.

- [ ] A. $34\textrm{i}$
- [ ] B. $29$
- [ ] C. $7$
- [ ] D. $34 + \textrm{i}$
- [ ] E. $4$

---

**Question 4:** Given that $z = 3 - 5\textrm{i}$, calculate $zz^(―)$.

- [ ] A. $28$
- [ ] B. $32$
- [ ] C. $15\textrm{i}$
- [ ] D. $32 + \textrm{i}$
- [ ] E. $34$

---

<a id="some-other-properties-of-the-complex-conjugate"></a>
## Some Other Properties of the Complex Conjugate

The complex conjugate has a few other useful properties. These are used less often, but it's worth being aware of them.

- We can distribute the complex conjugate over individual terms.
$\overline{z_1 + z_2} = \overline{z_1} + \overline{z_2}$
- The conjugate of a square is equal to the square of a conjugate:
$\overline{z^2} = (\overline{z})^2$
- If $z$ is a complex number and $a$ is a real number, then
$\overline{az} = a\overline{z}$.

<!-- lesson-nav:start -->

---

```update-progress
```

[[MA/Traditional-High-School-Mathematics/PCL/Home|Home]]
[[MA/Traditional-High-School-Mathematics/PCL/0. Table of Contents/TOC|Table of Contents]]

---

<!-- lesson-nav:end -->
