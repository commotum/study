# Properties of Fourier Series

<!--
lesson-id: 6709
topic-code: PS2.10.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Using Linearity of Fourier Series](#using-linearity-of-fourier-series)
- [Fourier Series Properties: Vertical Shifting](#fourier-series-properties-vertical-shifting)
- [Fourier Series Properties: Horizontal Reflection](#fourier-series-properties-horizontal-reflection)
- [Identifying Effects of Vertical Shifting and Horizontal Reflection on Coefficients](#identifying-effects-of-vertical-shifting-and-horizontal-reflection-on-coefficients)
- [Fourier Series of Odd and Even Functions](#fourier-series-of-odd-and-even-functions)
- [Parity Recognition and Series Type](#parity-recognition-and-series-type)

## Prerequisites

- [Products of Even and Odd Functions](<../../../../AG2/3. Functions/3.1. Functions/Lessons/3.1.11. Products of Even and Odd Functions.md>)
- [Introduction to Fourier Series](<10.2.1. Introduction to Fourier Series.md>)

---

<a id="introduction"></a>
## Introduction

In applications, we often build new periodic signals by adding old ones together or scaling their amplitudes.
The Fourier series behaves well under these transformations, i.e., scaling and adding functions simply scales and adds their Fourier coefficients.

More precisely, let $f(x)$ and $g(x)$ be $2\pi$-periodic functions with Fourier series

$$
\begin{bmatrix}f(x) & ∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)(a_{n}\cos (nx) + b_{n}\sin (nx)), \\ g(x) & ∼\frac{c_{0}}{2} + ∑_(n = 1)^(∞)(c_{n}\cos (nx) + d_{n}\sin (nx))\end{bmatrix}
$$

For constants $\alpha$ and $\beta$, define

$$
h(x)=\alpha f(x)+\beta g(x)
$$

Then $h$ is also $2\pi$-periodic, and its Fourier series has the form

$$
h(x)\sim \dfrac{A_0}{2}+\sum_{n=1}^{\infty}\big(A_n\cos(nx)+B_n\sin(nx)\big)
$$

where the coefficients are obtained by combining the coefficients term-by-term:

- $A_0=\alpha a_0+\beta c_0$
- $A_n=\alpha a_n+\beta c_n$ for $n\ge 1$
- $B_n=\alpha b_n+\beta d_n$ for $n\ge 1$

Equivalently,

$$
\alpha f(x)+\beta g(x) \sim \dfrac{\alpha a_0+\beta c_0}{2} +\sum_{n=1}^{\infty}\Big((\alpha a_n+\beta c_n)\cos(nx)+(\alpha b_n+\beta d_n)\sin(nx)\Big)
$$

**Watch out!** In this notation,

- the *constant Fourier coefficient* is $A_0$, while
- the *term* in the series is $\dfrac{A_0}{2}$.

Let's see a concrete example below.

---

<a id="using-linearity-of-fourier-series"></a>
## Using Linearity of Fourier Series

**Example:** $\begin{bmatrix}f(x) & ∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)a_{n}\cos (nx) + b_{n}\sin (nx) \\ g(x) & ∼\frac{c_{0}}{2} + ∑_(n = 1)^(∞)c_{n}\cos (nx) + d_{n}\sin (nx)\end{bmatrix}$

Let $f(x)$ and $g(x)$ be $2\pi$-periodic extensions of some functions from $[MATH: x \in [-\pi,\pi)]$ to $x \in \mathbb{R}$ with the Fourier series shown above. What are the Fourier constant coefficient, the $3\textrm{rd}$ Fourier sine coefficient, and the $8\textrm{th}$ Fourier cosine coefficient of $-3f(x)+4g(x)$?

**Explanation**

Let $f(x)$ and $g(x)$ be $2\pi$-periodic functions with Fourier series

$$
\begin{bmatrix}f(x) & ∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)(a_{n}\cos (nx) + b_{n}\sin (nx)), \\ g(x) & ∼\frac{c_{0}}{2} + ∑_(n = 1)^(∞)(c_{n}\cos (nx) + d_{n}\sin (nx))\end{bmatrix}
$$

Then for any constants $\alpha$ and $\beta$,

$$
\alpha f(x) + \beta g(x) \sim \frac{\alpha a_0 + \beta c_0}{2} + \sum_{n=1}^{\infty} \Big((\alpha a_n + \beta c_n)\cos(nx) + (\alpha b_n + \beta d_n)\sin(nx) \Big)
$$

Using the linearity property of Fourier series, the corresponding Fourier coefficients of $-3f(x)+4g(x)$ are

- $-3a_0+4c_0$ for the constant coefficient,
- $-3a_n+4c_n$, $n \ge 1$ for the cosine coefficients, and
- $-3b_n+4d_n$, $n \ge 1$ for the sine coefficients.

Therefore,

- the Fourier constant coefficient of $-3f(x)+4g(x)$ is $-3a_0+4c_0$,
- the $3$rd Fourier sine coefficient of $-3f(x)+4g(x)$ is $-3b_3+4d_3$, and
- the $8$th Fourier cosine coefficient of $-3f(x)+4g(x)$ is $-3a_8+4c_8$.

---

**Question 1:**

```quiz
type: radio
id: ma-340758
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $\begin{bmatrix}f(x) & ∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)a_{n}\cos (nx) + b_{n}\sin (nx) \\ g(x) & ∼\frac{c_{0}}{2} + ∑_(n = 1)^(∞)c_{n}\cos (nx) + d_{n}\sin (nx)\end{bmatrix}$
  Let $f(x)$ and $g(x)$ be $2π$-periodic extensions of some functions from $[MATH: x ∈ [- π, π)]$ to $x ∈ R$ with the Fourier series shown above. What is the constant Fourier coefficient of $-4f(x) + g(x)$?
options:
- id: a
  content: |-
    $-4a_{1} + c_{1}$
- id: b
  content: |-
    $-4a_{0} + b_{0}$
- id: c
  content: |-
    $-4a_{0} + c_{0}$
- id: d
  content: |-
    $-4b_{1} + d_{1}$
- id: e
  content: |-
    $-4b_{0} + d_{0}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-340765
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $\begin{bmatrix}f(x) & ∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)a_{n}\cos (nx) + b_{n}\sin (nx) \\ g(x) & ∼\frac{c_{0}}{2} + ∑_(n = 1)^(∞)c_{n}\cos (nx) + d_{n}\sin (nx)\end{bmatrix}$
  Let $f(x)$ and $g(x)$ be $2π$-periodic extensions of some functions from $[MATH: x ∈ [- π, π)]$ to $x ∈ R$ with the Fourier series shown above. What is the $3rd$ Fourier sine coefficient of $-2f(x) + 5g(x)$?
options:
- id: a
  content: |-
    $-2a_{0} + 5c_{0}$
- id: b
  content: |-
    $-2b_{3} + 5c_{3}$
- id: c
  content: |-
    $-2a_{3} + 5c_{3}$
- id: d
  content: |-
    $-2b_{3} + 5d_{3}$
- id: e
  content: |-
    $-2b_{0} + 5d_{0}$
```

---

<a id="fourier-series-properties-vertical-shifting"></a>
## Fourier Series Properties: Vertical Shifting

Some simple transformations of a periodic function have very predictable effects on its Fourier coefficients. First, we consider the effect of *adding a constant* (a *vertical shift*).

Assume $f$ has the Fourier series

$$
f(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}\big(a_n\cos(nx)+b_n\sin(nx)\big)
$$

If

$$
h(x)=f(x)+C
$$

for a constant $C$, then only the constant part of the Fourier series changes:

$$
h(x)\sim \dfrac{a_0+2C}{2}+\sum_{n=1}^{\infty}\big(a_n\cos(nx)+b_n\sin(nx)\big)
$$

So, the coefficients of the transformed series are

$$
a_0^{(h)}=a_0+2C, \qquad a_n^{(h)}=a_n, \qquad b_n^{(h)}=b_n
$$

In the next slides, we will apply this rule to a concrete example.

---

<a id="fourier-series-properties-horizontal-reflection"></a>
## Fourier Series Properties: Horizontal Reflection

Another simple transformation is *reflecting a function across the $y$-axis*.

Again, assume $f$ has the Fourier series

$$
f(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}\big(a_n\cos(nx)+b_n\sin(nx)\big)
$$

If $h(x)=f(-x)$, then cosine terms stay the same while sine terms change sign (because $\cos(nx)$ is even and $\sin(nx)$ is odd):

$$
h(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}\big(a_n\cos(nx)-b_n\sin(nx)\big)
$$

So, the coefficients of the transformed series are

$$
a_0^{(h)}=a_0, \qquad a_n^{(h)}=a_n, \qquad b_n^{(h)}=-b_n
$$

Let's see how to apply this rule in practice on the next slides.

---

<a id="identifying-effects-of-vertical-shifting-and-horizontal-reflection-on-coefficients"></a>
## Identifying Effects of Vertical Shifting and Horizontal Reflection on Coefficients

**Example:** $f(x) \sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}a_n\cos(nx)+b_n\sin(nx)$
Let $f(x)$ be the $2\pi$-periodic extesion of a function from $[MATH: x \in [-\pi,\pi)]$ to $x \in \mathbb{R}$ with the Fourier series shown above. What is the $5$th sine Fourier coefficient of $f(-x)$?

**Explanation**

The Fourier series of $f(x)$ is

$$
f(x) \sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}a_n\cos(nx)+b_n\sin(nx)
$$

Recall that the corresponding Fourier coefficients of $f(-x)$ are

- $a_0$ for the constant coefficient,
- $a_n$ $n \ge 1$, for the cosine coefficients, and
- $-b_n$ $n \ge 1$, for the sine coefficients.

Therefore, the $5$th sine coefficient of $f(-x)$ is $-b_5$.

---

**Question 3:**

```quiz
type: radio
id: ma-338668
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $f(x)∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)a_{n}\cos (nx) + b_{n}\sin (nx)$
  Let $f(x)$ be the $2π$-periodic extension of a function from $[MATH: x ∈ [- π, π)]$ to $x ∈ R$ with the Fourier series shown above. What is the $4$th cosine Fourier coefficient of $f(x) - 3$?
options:
- id: a
  content: |-
    $a_{4} - 6$
- id: b
  content: |-
    $a_{4} + 6$
- id: c
  content: |-
    $a_{4} + 3$
- id: d
  content: |-
    $a_{4}$
- id: e
  content: |-
    $a_{4} - 3$
```

---

**Question 4:**

```quiz
type: radio
id: ma-338681
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  $f(x)∼\frac{a_{0}}{2} + ∑_(n = 1)^(∞)a_{n}\cos (nx) + b_{n}\sin (nx)$
  Let $f(x)$ be the $2π$-periodic extesion of a function from $[MATH: x ∈ [- π, π)]$ to $x ∈ R$ with the Fourier series shown above. What is the $6$th sine Fourier coefficient of $f(-x)$?
options:
- id: a
  content: |-
    $b_{6} - 1$
- id: b
  content: |-
    $-a_{6}$
- id: c
  content: |-
    $a_{6}$
- id: d
  content: |-
    $-b_{6}$
- id: e
  content: |-
    $b_{6}$
```

---

<a id="fourier-series-of-odd-and-even-functions"></a>
## Fourier Series of Odd and Even Functions

When a $2\pi$-periodic function has symmetry, many of its Fourier coefficients must be zero. This can simplify Fourier series computations dramatically.

Assume $f(x)$ is $2\pi$-periodic and piecewise continuous on $[MATH: [-\pi,\pi),]$ with Fourier series

$$
f(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}\big(a_n\cos(nx)+b_n\sin(nx)\big)
$$

We will use the property that the integral of an odd function over a symmetric interval like $[-\pi, \pi]$ is always zero.

- If $f$ is an *even* function, meaning $f(-x)=f(x)$, then all sine coefficients $b_n$ must be zero.
This is because each $b_n$ is calculated from an integral of an *odd* function:
$b_n = \dfrac{1}{\pi}\displaystyle \int_{-\pi}^{\pi} \underbrace{f(x)}_{\text{even}} \, \underbrace{\sin(nx)}_{\text{odd}} \, \text{d}x = \dfrac{1}{\pi}\displaystyle \int_{-\pi}^{\pi} (\text{odd function})\,\text{d}x = 0$.
The Fourier series of an even function has *cosine terms only*:
$f(x)\sim \dfrac{a_0}{2}+\sum_{n=1}^{\infty}a_n\cos(nx)$
- If $f$ is an *odd* function, meaning $f(-x)=-f(x)$, then the constant and all cosine coefficients (both $a_0$ and $a_n$) must be zero.
This is because these coefficients are calculated from integrals of *odd* functions:
$a_n = \dfrac{1}{\pi}\displaystyle \int_{-\pi}^{\pi} \underbrace{f(x)}_{\text{odd}} \, \underbrace{\cos(nx)}_{\text{even}} \,\text{d}x = \dfrac{1}{\pi}\displaystyle \int_{-\pi}^{\pi} (\text{odd function})\,\text{d}x = 0 \quad (\text{for} n \ge 0)$.
The Fourier series of an odd function has *sine terms only*:
$f(x)\sim \sum_{n=1}^{\infty}b_n\sin(nx)$

**Note:** We may use either $[MATH: [-\pi,\pi)]$ or $[-\pi,\pi]$ as the base interval. Including or excluding the endpoints does not affect the conclusions.

Let's see how this works on concrete examples below.

---

<a id="parity-recognition-and-series-type"></a>
## Parity Recognition and Series Type

**Example:** Consider the function $f(x)=x\cos(2x)$ on $x \in [-\pi,\pi]$. Complete the statements below by filling in the blanks.
Determine if the function $f(x)$ is odd or even, and which terms of the corresponding Fourier series of $f(x)$ must be zero.

**Explanation**

To determine whether $f$ is even or odd, we compare $f(-x)$ and $f(x){:}$

$$
\begin{aligned}
f(-x) &= (-x)\cos (2(-x)) \\
&= (-x)\cos (-2x) \\
&= (-x)\cos (2x) \\
&=-f(x)
\end{aligned}
$$

Since

$$
f(-x) = -f(x)
$$

we have that $f$ is odd.

For Fourier series on $[-\pi,\pi]$, the cosine coefficients are given by

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi} f(x)\cos(nx)\,\text{d}x
$$

and the constant coefficient is

$$
a_0=\frac{1}{\pi}\int_{-\pi}^{\pi} f(x)\,\text{d}x
$$

Here, $f(x)$ is odd and $\cos(nx)$ is even, so $f(x)\cos(nx)$ is odd. Also, $f(x)$ itself is odd.

Now, the integral of an odd function over $[-\pi,\pi]$ is $0$. Thus, $a_n=0$ for all

$$
n\ge1
$$

and $a_0=0$.

Therefore, the function $f(x)$ is $\bbox[4pt, border: solid 1pt gainsboro]{\text{odd}}$ and the $\bbox[4pt, border: solid 1pt gainsboro]{\text{cosine}}$ terms in its Fourier series must be zero.

---

**Question 5:**

```quiz
type: select
id: ma-338656-select-1
# MA_ANSWER_MISSING: set questions[0].correct_option to one of this block's options[].id values
content: |-
  Consider the function $f(x) =-5x^{4} + 2x^{2}$ on $x ∈ [- π, π]$. Select the correct options below.
  The function $f(x)$ is
  $\underline{\hspace{2cm}}$.
  
  Consequently, the $\underline{\hspace{2cm}}$
  in the corresponding Fourier series of $f(x)$ must be zero.
options:
- id: select-1-option-1
  content: |-
    even
- id: select-1-option-2
  content: |-
    odd
questions:
- id: select-1
  content: |-
    Select 1
```

```quiz
type: select
id: ma-338656-select-2
# MA_ANSWER_MISSING: set questions[0].correct_option to one of this block's options[].id values
content: |-
  Consider the function $f(x) =-5x^{4} + 2x^{2}$ on $x ∈ [- π, π]$. Select the correct options below.
  The function $f(x)$ is
  $\underline{\hspace{2cm}}$.
  
  Consequently, the $\underline{\hspace{2cm}}$
  in the corresponding Fourier series of $f(x)$ must be zero.
options:
- id: select-2-option-1
  content: |-
    cosine terms
- id: select-2-option-2
  content: |-
    sine terms
questions:
- id: select-2
  content: |-
    Select 2
```

---

**Question 6:**

```quiz
type: select
id: ma-338682-select-1
# MA_ANSWER_MISSING: set questions[0].correct_option to one of this block's options[].id values
content: |-
  Consider the function $f(x) = x\cos (x)$ on $x ∈ [- π, π]$. Select the correct options below.
  The function $f(x)$ is
  $\underline{\hspace{2cm}}$.
  
  Consequently, the
  $\underline{\hspace{2cm}}$
  in the corresponding Fourier series of $f(x)$ must be zero.
options:
- id: select-1-option-1
  content: |-
    even
- id: select-1-option-2
  content: |-
    odd
questions:
- id: select-1
  content: |-
    Select 1
```

```quiz
type: select
id: ma-338682-select-2
# MA_ANSWER_MISSING: set questions[0].correct_option to one of this block's options[].id values
content: |-
  Consider the function $f(x) = x\cos (x)$ on $x ∈ [- π, π]$. Select the correct options below.
  The function $f(x)$ is
  $\underline{\hspace{2cm}}$.
  
  Consequently, the
  $\underline{\hspace{2cm}}$
  in the corresponding Fourier series of $f(x)$ must be zero.
options:
- id: select-2-option-1
  content: |-
    cosine terms
- id: select-2-option-2
  content: |-
    sine terms
questions:
- id: select-2
  content: |-
    Select 2
```

```update-progress
```

[[MA/PS2/Home|Home]]
[[MA/PS2/0. Table of Contents/TOC|Table of Contents]]
