# Finding a Least-Squares Solution Using QR Factorization

<!--
lesson-id: 2168
topic-code: PS2.3.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying an Expression for a Least-Squares Solution in Terms of the Matrices Q and R](#identifying-an-expression-for-a-least-squares-solution-in-terms-of-the-matrices-q-and-r)
- [Using QR Factorization to Solve a Least-Squares Problem](#using-qr-factorization-to-solve-a-least-squares-problem)
- [Finding the Least-Squares Solution of a Linear System Given a QR Factorization](#finding-the-least-squares-solution-of-a-linear-system-given-a-qr-factorization)

## Prerequisites

- [QR Factorization](<../../../1. Projections/1.4. Orthogonalization Processes/Lessons/1.4.3. QR Factorization.md>)
- [The Least-Squares Solution of a Linear System (With Collinearity)](<3.1.2. The Least-Squares Solution of a Linear System (With Collinearity).md>)

---

<a id="introduction"></a>
## Introduction

Let's consider the least-squares problem for the system

$$
A\mathbf{x} = \mathbf{b}
$$

We can use QR factorization to construct the least-squares solution of this system. Recall that finding the QR factorization of $A$ means expressing $A$ in the form

$$
A = QR
$$

where

- $Q$ has orthonormal columns, and
- $R$ is an upper triangular matrix with positive entries on its main diagonal.

To see how QR factorization can be used to solve a least-squares problem, we first recall that the least-squares solution satisfies the normal equation

$$
A^T A \mathbf{x} = A^T \mathbf{b}
$$

Substituting $A = QR$, we obtain

$$
\begin{aligned}
A^{T}Ax &= A^{T}b \\
(QR)^{T}QRx &= (QR)^{T}b \\
R^{T}(Q^{T}Q)Rx &= R^{T}Q^{T}b
\end{aligned}
$$

Since $Q$ has orthonormal columns, we have $Q^T Q = I$. Therefore, the above equation becomes

$$
R^TR\mathbf{x} = R^TQ^T\mathbf{b}
$$

Since $R^T$ is invertible, pre-multiplying our equation by $(R^T)^{-1}$ gives

$$
\begin{aligned}
(R^{T})^{-1} \cdot R^{T}Rx &= (R^{T})^{-1} \cdot R^{T}Q^{T}b
\end{aligned}
$$

which simplifies as

$$
R \mathbf{x} = Q^T \mathbf{b}. \qquad\qquad (\ast)
$$

The least-squares solution $\hat{\mathbf x}$ can now be found by solving this equation using back-substitution or row operations. This is usually the fastest way.

If we want an explicit formula for the least-squares solution, then we note that since $R$ is invertible, pre-multiplying $(\ast)$ by $R^{-1}$ gives

$$
\begin{aligned}
R^{-1} \cdot Rx &= R^{-1} \cdot Q^{T}b
\end{aligned}
$$

which simplifies as

$$
\mathbf{x} = R^{-1} Q^T \mathbf{b}
$$

The least-squares solution is unique if $A$ has linearly independent columns.

---

<a id="identifying-an-expression-for-a-least-squares-solution-in-terms-of-the-matrices-q-and-r"></a>
## Identifying an Expression for a Least-Squares Solution in Terms of the Matrices Q and R

**Example:** $\bbox[5px, border: 1px solid black]{\phantom{A}} \, \mathbf{x} = \bbox[5px, border: 1px solid black]{\phantom{A}} \, \mathbf{b}$

Let $A=QR$ be a QR decomposition of an $m \times n$ matrix $A$ with linearly independent columns, and the matrix equation above gives the least-squares solution of $A\mathbf{x}=\mathbf{b}$. From left to right, what are the missing parts of the equation?

**Explanation**

The least-squares solution of

$$
A\mathbf{x}=\mathbf{b}
$$

satisfies the normal equation

$$
A^T A\mathbf{x} = A^T\mathbf{b}
$$

Substituting $A=QR$, we obtain

$$
\begin{aligned}
A^{T}Ax &= A^{T}b \\
(QR)^{T}QRx &= (QR)^{T}b \\
R^{T}(Q^{T}Q)Rx &= R^{T}Q^{T}b
\end{aligned}
$$

Since $Q$ has orthonormal columns, it satisfies $Q^T Q = I$, and we get

$$
R^T R \mathbf{x} = R^T \! Q^T \mathbf{b}
$$

Finally, since $R^T$ is invertible, pre-multiplying by $(R^T)^{-1}$ gives

$$
\begin{aligned}
(R^{T})^{-1} \cdot R^{T}Rx &= (R^{T})^{-1} \cdot R^{T}Q^{T}b \\
R \mid x &= Q Tb
\end{aligned}
$$

Therefore, the missing parts of the equation are $R$ and $Q^T$.

---

**Question 1**

$RA = Ab$

Let $A = QR$ be a QR decomposition of an $m \cdot n$ matrix $A$ with linearly independent columns, and the matrix equation above gives the least-squares solution of $Ax = b$. From left to right, what are the missing parts of the equation?

- [ ] A. $x$ and $m \cdot I$
- [ ] B. $Q^{T}$ and $x^{T}$
- [ ] C. $Q$ and $x^{T}$
- [ ] D. $x$ and $Q^{T}$
- [ ] E. $x$ and $Q$

---

**Question 2**

$x = AQ^{T}A$

Let $A = QR$ be a QR decomposition of an $m \cdot n$ matrix $A$ with linearly independent columns, and the expression above gives the least-squares solution of $Ax = b$. From left to right, what are the missing parts of the equation?

- [ ] A. $R$ and $b^{T}$
- [ ] B. $R^{-1}$ and $A$
- [ ] C. $R^{-1}$ and $b$
- [ ] D. $R^{T}$ and $b$
- [ ] E. $R$ and $b$

---

<a id="using-qr-factorization-to-solve-a-least-squares-problem"></a>
## Using QR Factorization to Solve a Least-Squares Problem

Let's find the least-squares solution to

$$
A\mathbf{x} = \mathbf{b}
$$

where $A = QR$, and
$\begin{bmatrix}-1 & 1 \\ 1 & 1\end{bmatrix}$

Previously, we showed that the normal equation corresponding to

$$
A\mathbf{x}=\mathbf{b}
$$

where $A=QR$, can be reduced to

$$
R \mathbf{x} = Q^T \mathbf{b}
$$

Substituting our matrices into this equation, we have

$$
\begin{aligned}
\frac{1}{\sqrt{2}}\begin{bmatrix}2 & -4 \\ 0 & 3\end{bmatrix}^(⏞)^(R)x &= \frac{1}{\sqrt{2}}\begin{bmatrix}-1 & 1 \\ 1 & 1\end{bmatrix}^(⏞)^(Q^{T})\begin{bmatrix}2 \\ 4\end{bmatrix}^(⏞)^(b) \\
\begin{bmatrix}2 & -4 \\ 0 & 3\end{bmatrix}x &= \begin{bmatrix}2 \\ 6\end{bmatrix}
\end{aligned}
$$

which gives the system

$$
{2x_{1} - 4x_{2} = 2; 3x_{2} = 6
$$

From the second equation, we get $x_2=2$. Substituting this into the first equation, we obtain

$$
\begin{aligned}
2x_{1} - 4(2) &= 2 \\
2x_{1} &= 10 \\
x_{1} &= 5
\end{aligned}
$$

Therefore, the least-squares solution to

$$
A\mathbf{x} = \mathbf{b}
$$

is $\begin{bmatrix}5 \\ 2\end{bmatrix}$

---

<a id="finding-the-least-squares-solution-of-a-linear-system-given-a-qr-factorization"></a>
## Finding the Least-Squares Solution of a Linear System Given a QR Factorization

**Example:** Given the QR decomposition $A=QR$, find the least-squares solution to $A\mathbf{x}=\mathbf{b}$, where
$\begin{bmatrix}3 & -6 \\ 6 & 2 \\ 2 & 3\end{bmatrix}$

**Explanation**

The least-squares solution of

$$
A\mathbf{x}=\mathbf{b}
$$

satisfies the corresponding normal equation:

$$
A^T A\mathbf{x} = A^T\mathbf{b}
$$

Substituting $A=QR$, we obtain

$$
\begin{aligned}
A^{T}Ax &= A^{T}b \\
(QR)^{T}QRx &= (QR)^{T}b \\
R^{T}(Q^{T}Q)Rx &= R^{T}Q^{T}b
\end{aligned}
$$

Since $Q$ has orthonormal columns, it satisfies $Q^T Q = I$, and we get

$$
R^T R \mathbf{x} = R^T \! Q^T \mathbf{b}
$$

Finally, since $R^T$ is invertible, pre-multiplying by $(R^T)^{-1}$ gives

$$
\begin{aligned}
(R^{T})^{-1} \cdot R^{T}Rx &= (R^{T})^{-1} \cdot R^{T}Q^{T}b \\
Rx &= Q^{T}b
\end{aligned}
$$

Now, substituting our matrices, we have

$$
\begin{aligned}
\frac{1}{7}\begin{bmatrix}7 & -3 \\ 0 & 2\end{bmatrix}^(⏞)^(R)x &= \frac{1}{7}\begin{bmatrix}3 & 6 & 2 \\ -6 & 2 & 3\end{bmatrix}^(⏞)^(Q^{T})\begin{bmatrix}0 \\ 4 \\ 2\end{bmatrix}^(⏞)^(b) \\
\begin{bmatrix}7 & -3 \\ 0 & 2\end{bmatrix}x &= \begin{bmatrix}28 \\ 14\end{bmatrix}
\end{aligned}
$$

which gives the system

$$
{7x_{1} - 3x_{2} = 28; 2x_{2} = 14
$$

From the second equation, we get $x_2=7$. Substituting this into the first equation, we obtain

$$
\begin{aligned}
7x_{1} - 3(7) &= 28 \\
7x_{1} &= 49 \\
x_{1} &= 7
\end{aligned}
$$

Therefore, the least-squares solution to

$$
A\mathbf{x}=\mathbf{b}
$$

is $\begin{bmatrix}7 \\ 7\end{bmatrix}$

---

**Question 3:** Given the QR decomposition $A = QR$, find the least-squares solution to $Ax = b$, where
$Q = \frac{1}{\sqrt{5}}\begin{bmatrix}-1 & 2 \\ 2 & 1\end{bmatrix}, R = \frac{1}{\sqrt{5}}\begin{bmatrix}1 & 7 \\ 0 & 5\end{bmatrix}, b = \begin{bmatrix}1 \\ 3\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}3 \\ 1\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-2 \\ 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}-1 \\ 2\end{bmatrix}$
- [ ] D. $\begin{bmatrix}2 \\ 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}-2 \\ 2\end{bmatrix}$

---

**Question 4:** Given the QR decomposition $A = QR$, find the least-squares solution to $Ax = b$, where
$Q = \frac{1}{\sqrt{30}}\begin{bmatrix}\sqrt{5} & -5 \\ -2\sqrt{5} & -2 \\ \sqrt{5} & 1\end{bmatrix}, R = \frac{1}{\sqrt{30}}\begin{bmatrix}6\sqrt{5} & -4\sqrt{5} \\ 0 & 2\end{bmatrix}, b = \begin{bmatrix}1 \\ 3 \\ 1\end{bmatrix}$.

- [ ] A. $\begin{bmatrix}-2 \\ -\frac{3}{2}\end{bmatrix}$
- [ ] B. $\begin{bmatrix}-2 \\ 1\end{bmatrix}$
- [ ] C. $\begin{bmatrix}1 \\ \frac{1}{2}\end{bmatrix}$
- [ ] D. $\begin{bmatrix}-4 \\ -5\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 \\ -2\end{bmatrix}$

```update-progress
```

[[MA/PS2/Home|Home]]
[[MA/PS2/0. Table of Contents/TOC|Table of Contents]]
