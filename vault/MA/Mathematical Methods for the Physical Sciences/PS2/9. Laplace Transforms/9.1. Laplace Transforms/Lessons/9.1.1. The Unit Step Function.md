# The Unit Step Function

<!--
lesson-id: 2760
topic-code: PS2.9.1.1
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Evaluating the Step Function](#evaluating-the-step-function)
- [Graphs of Transformed Step Functions](#graphs-of-transformed-step-functions)
- [The Function y = u(-t)](#the-function-y-u-t)
- [Graphs of Reflected Step Functions](#graphs-of-reflected-step-functions)

## Prerequisites

- [Piecewise Functions](<../../../../AG1/6. Functions/6.1. Functions/Lessons/6.1.12. Piecewise Functions.md>)
- [Combining Reflections With Other Graph Transformations](<../../../../AG2/3. Functions/3.2. Graph Transformations of Functions/Lessons/3.2.10. Combining Reflections With Other Graph Transformations.md>)

---

<a id="introduction"></a>
## Introduction

The **unit step function**, $u(t)$, is used to model quantities that change suddenly at a specific time, such as the application of a force, the switching of a circuit, or the start of a signal.

The unit step function is defined as

$$
{1,\begin{vmatrix}t \ge 0, \\ 0,\end{vmatrix}t < 0
$$

In other words, the function is *off* before $t=0$ and *on* afterward. The graph of the unit step function is shown below.

![](<../Source/9.1.1. The Unit Step Function/Images/d3ee8bf0e4eb8d777c90c4b088c11062.png>)

There are two primary ways to modify the unit step function:

- **Scaling**: Multiplying $u(t)$ by a constant changes the magnitude (height) of the step.
- **Shifting**: Replacing $t$ with $t-a$ changes the turn-on time from $t=0$ to $t=a$. Applying only the shift results in the function $u(t - a)$, defined by
${0,\begin{vmatrix}t < a, \\ 1,\end{vmatrix}t \ge a$.
This function remains at $0$ until $t=a$ and then immediately jumps to $1$.

Combining both modifications gives the general form of a **scaled and shifted** step function:

$$
f(t) = c \cdot u(t-a)
$$

This function remains at $0$ for $t < a$, and then jumps to the constant value $c$ at $t=a$. In this way, the unit step function allows us to model both the *timing* and the *magnitude* of a sudden change.

Next, we will look at a concrete example of graphing a specific scaled and shifted step function.

---

<a id="a-worked-example"></a>
## A Worked Example

Consider an example of a scaled and shifted step function $c\cdot u(t-a)$.

Let

$$
f(t) = -2u(t-3)
$$

where $u$ is the unit step function. We evaluate this function at $t=2$.

Substituting $t = 2$, we have

$$
\begin{aligned}
f(2) &= -2u(2 - 3) \\
&=-2u(-1) \\
&= (-2) \cdot 0 \\
&= 0
\end{aligned}
$$

Therefore,

$$
f(2)=0
$$

In the graph, $f(2)$ lies on the flat blue segment before the step activates, so its value is $0$.

![](<../Source/9.1.1. The Unit Step Function/Images/9303af8a4969ad2fd10632ab4d9612fb.png>)

Now, let’s get some practice working through a few examples.

---

<a id="evaluating-the-step-function"></a>
## Evaluating the Step Function

**Example:** Evaluate $f(t) = 9u(t-9)$ at $t=9$, where $u$ is the unit step function.

**Explanation**

The unit step function $u(t)$ is defined as

$$
{1, t \ge 0,; 0, t < 0
$$

Evaluating $f(t)$ at $t=9$, we have

$$
\begin{aligned}
f(9) &= 9u(9 - 9) \\
&= 9u(0) \\
&= 9 \cdot 1 \\
&= 9
\end{aligned}
$$

Therefore,

$$
f(9)=9
$$

---

**Question 1:**

```quiz
type: free
id: ma-331183
# MA_ANSWER_MISSING: set correct to an optional reference answer
content: |-
  Evaluate $f(t) = 2u(t - 4)$ at $t = 3$, where $u$ is the unit step function.$f(t) =$
  $\underline{\hspace{1.5cm}}$
```

---

**Question 2:**

```quiz
type: free
id: ma-331233
# MA_ANSWER_MISSING: set correct to an optional reference answer
content: |-
  Evaluate $f(t) = 3u(t - 5)$ at $t = 9$, where $u$ is the unit step function.$f(t) =$
  $\underline{\hspace{1.5cm}}$
```

---

<a id="graphs-of-transformed-step-functions"></a>
## Graphs of Transformed Step Functions

**Example:** The graph of $y=f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?

![](<../Source/9.1.1. The Unit Step Function/Images/b25aad85ae1374bd6c1bd86713ad24ca.png>)

**Explanation**

The unit step function $u(t)$ is defined as

$$
{1, t \ge 0,; 0, t < 0
$$

The graph of $y=u(t)$ is shown below.

![](<../Source/9.1.1. The Unit Step Function/Images/cea5e5cfbce8e6708425a867337b8411.png>)

Notice that to transform $y=u(t)$ to the given function $y=f(t)$, we perform the following transformations:

- First, we shift $y=u(t)$ by $2$ units to the left, giving $y=u(t+2)$.
- Then, we scale $y=u(t+2)$ by a factor of $3$ parallel to the $y$-axis, giving $y=3u(t+2)$.

Therefore, we conclude that

$$
f(t)=3u(t+2)
$$

---

**Question 3:**

```quiz
type: radio
id: ma-331245
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/9.1.1. The Unit Step Function/Images/q-331245.png>)
  
  The graph of $y = f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?
options:
- id: a
  content: |-
    $2u(t - 2)$
- id: b
  content: |-
    $u(t + 2)$
- id: c
  content: |-
    $u(t - 2)$
- id: d
  content: |-
    $u(t - 2) + 1$
- id: e
  content: |-
    $u(t + 2) + 1$
```

---

**Question 4:**

```quiz
type: radio
id: ma-331249
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/9.1.1. The Unit Step Function/Images/q-331249.png>)
  
  The graph of $y = f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?
options:
- id: a
  content: |-
    $-2u(t)$
- id: b
  content: |-
    $2u(t)$
- id: c
  content: |-
    $u(t)$
- id: d
  content: |-
    $2u(t - 1)$
- id: e
  content: |-
    $2u(t) + 1$
```

---

<a id="the-function-y-u-t"></a>
## The Function y = u(-t)

So far, we have seen how scaling and shifting affect the unit step function.
We now examine how the unit step function behaves under **reflection**.

The graph of $y = u(-t)$ represents the reflection of the unit step function $u(t)$ across the $y$-axis.

![](<../Source/9.1.1. The Unit Step Function/Images/9fca07235de5c475d697ebd5ba183400.png>)

Under this transformation, the step still occurs at $t = 0$, but the direction of the step is reversed. In particular,

- $u(t)$ *turns on* at $t=0$ and remains on for $t>0$,
- $u(-t)$ *turns off* at $t=0$ and remains off for $t>0$.

Now, let’s look at a few examples to see how reflection works in practice.

---

<a id="graphs-of-reflected-step-functions"></a>
## Graphs of Reflected Step Functions

**Example:** The graph of $y = f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?

![](<../Source/9.1.1. The Unit Step Function/Images/6058d85fd3a0bb42589d8c84a6f4f946.png>)

**Explanation**

The unit step function $u(t)$ is defined as

$$
{1, t \ge 0,; 0, t < 0
$$

The graph of $y = u(t)$ is shown below.

![](<../Source/9.1.1. The Unit Step Function/Images/63ce6d8bac7002ace80730a3619b1370.png>)

Notice that to transform the function $y = u(t)$ to the given function $y = f(t)$, we perform the following transformations:

- First, we reflect $y = u(t)$ in the $y$-axis, which gives $y = u(-t)$.
- Next, we shift by one unit to the left, giving $y = u(-(t+1)) = u(-1 - t)$.
- Finally, we scale by a scale factor of $2$ parallel to the $y$-axis, giving $y = 2u(-1 - t)$.

Therefore, we conclude that

$$
f(t) = 2u(-1 - t)
$$

---

**Question 5:**

```quiz
type: radio
id: ma-331266
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/9.1.1. The Unit Step Function/Images/q-331266.png>)
  
  The graph of $y = f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?
options:
- id: a
  content: |-
    $u(2 - t)$
- id: b
  content: |-
    $2u(2 - t)$
- id: c
  content: |-
    $u(-2 - t)$
- id: d
  content: |-
    $u(2 - t) + 1$
- id: e
  content: |-
    $u(t - 2)$
```

---

**Question 6:**

```quiz
type: radio
id: ma-331270
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  ![](<../Source/9.1.1. The Unit Step Function/Images/q-331270.png>)
  
  The graph of $y = f(t)$ is shown above. Which of the following could be $f(t)$, where $u$ denotes the unit step function?
options:
- id: a
  content: |-
    $u(-t)$
- id: b
  content: |-
    $2u(-t)$
- id: c
  content: |-
    $2u(-t) + 1$
- id: d
  content: |-
    $2u(t)$
- id: e
  content: |-
    $-2u(-t)$
```

```update-progress
```

[[MA/PS2/Home|Home]]
[[MA/PS2/0. Table of Contents/TOC|Table of Contents]]
