# The Modified Euler Method

<!--
lesson-id: 3247
topic-code: PS2.11.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [The Predictor Equation of the Modified Euler Method](#the-predictor-equation-of-the-modified-euler-method)
- [Completing a Table Using the Predictor Equation of the Modified Euler Method](#completing-a-table-using-the-predictor-equation-of-the-modified-euler-method)
- [The Modified Euler Method](#the-modified-euler-method)
- [A Worked Example](#a-worked-example)
- [Calculating the Corrector Term](#calculating-the-corrector-term)
- [Completing the First Row of a Table Using the Modified Euler Method](#completing-the-first-row-of-a-table-using-the-modified-euler-method)
- [Approximating the Solution to an Initial Value Problem Using The Modified Euler Method With One Step](#approximating-the-solution-to-an-initial-value-problem-using-the-modified-euler-method-with-one-step)

## Prerequisites

- [Euler's Method: Calculating Multiple Steps](<11.1.2. Euler's Method- Calculating Multiple Steps.md>)

---

<a id="introduction"></a>
## Introduction

**Predictor-Corrector methods** are numerical methods for approximating solutions to differential equations that use two equations at each step:

- The **predictor** obtains an initial approximation (a *prediction*) for the change $\Delta y$.
- The **corrector** refines the predictor's estimate, adjusting for changes in the slope between successive $x$-values, to obtain a *corrected* approximation for $\Delta y$.

Some common predictor-corrector methods include:

- Modified Euler Method (Heun's Method)
- $2$-Step Adams-Bashforth-Moulton Method
- $4$-Step Adams-Bashforth-Moulton Method
- Milne's Method

In the next slide, we'll introduce the predictor of the simplest predictor-corrector method, the Modified Euler Method, and apply it to a specific numerical example.

---

<a id="the-predictor-equation-of-the-modified-euler-method"></a>
## The Predictor Equation of the Modified Euler Method

The **Modified Euler Method** (**Heun's Method**) is a predictor-corrector numerical method that uses Euler's method for its predictor step:

$$
\text{Predictor}: \qquad \Delta y_p = y' \cdot \Delta x
$$

The initially predicted new $y$-value is then given by

$$
y_p = y + \Delta y_p
$$

Note the following:

- The step size $\Delta x$ represents the change in $x$.
- The quantity $y'$ represents the slope at the current point.
- The predicted $y$-increment is denoted $\Delta y_p$ for notational convenience. It represents the initial change in $y$ made by the predictor step when using the modified Euler method.

For example, consider the following initial value problem:

$$
y' = x^2 + y, \qquad y(0) = 1
$$

Let's begin the process of using the modified Euler method to approximate the value of $y(1)$.

Since we know $y(0)$ and wish to estimate $y(1)$, we will use a step size of

$$
\Delta x = 1 - 0 = 1
$$

We'll use a table when applying the modified Euler method. First, we add the initial condition data:

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $\,y_p\,$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ |  |  |  |

Next, we compute the value of $y'$ according to the given rule:

$$
\begin{aligned}
y^{′} &= x^{2} + y \\
&= 0^{2} + 1 \\
&= 1
\end{aligned}
$$

So, we add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $\,y_p\,$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ |  |  |

Next, we compute $\Delta y_p$ using the predictor equation:

$$
\begin{aligned}
Δy_{p} &= y^{′} \cdot Δx \\
&= 1 \cdot 1 \\
&= 1
\end{aligned}
$$

We add this value to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $\,y_p\,$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ |  |

Hence, according to the predictor equation, the initially predicted new $y$-value is

$$
\begin{aligned}
y_{p} &= y + Δy_{p} \\
&= 1 + 1 \\
&= 2
\end{aligned}
$$

Therefore, the completed table is as given below.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $\,y_p\,$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ |

We can view this initial Euler-based approximation produced by the predictor equation in the coordinate plane as follows.

![](<../Source/11.1.3. The Modified Euler Method/Images/4475b2e416944d3fe38aef3ab98c1cd1.png>)

---

<a id="completing-a-table-using-the-predictor-equation-of-the-modified-euler-method"></a>
## Completing a Table Using the Predictor Equation of the Modified Euler Method

**Example:** Consider the following initial value problem:
$y' = 2xy + y^2, \qquad y(0)=1$
We wish to approximate the solution using the modified Euler method. Given that the step size is $\Delta x = 1$, fill the missing entries in the table below.

| $\,x\,$ | $\,y\,$ | $y'$ | $\Delta y_p$ | $y_p$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ |  |  |  |

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the predictor equation of the modified Euler method with step size $\Delta x$ is given by

$$
Δy_{p} = y^{′} \cdot Δx
$$

The initially predicted new $y$-value is then given by

$$
y_p = y + \Delta y_p
$$

First, we compute the value of $y'$ according to the given rule:

$$
\begin{aligned}
y^{′} &= 2xy + y^{2} \\
&= 2 \cdot 0 \cdot 1 + 1^{2} \\
&= 1
\end{aligned}
$$

So, we add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ |  |  |

Next, we compute $\Delta y_p$ using the predictor equation:

$$
\begin{aligned}
Δy_{p} &= y^{′} \cdot Δx \\
&= 1 \cdot 1 \\
&= 1
\end{aligned}
$$

We add this value to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ |  |

Hence, according to the predictor equation, the initially predicted new $y$-value is

$$
\begin{aligned}
y_{p} &= y + Δy_{p} \\
&= 1 + 1 \\
&= 2
\end{aligned}
$$

Therefore, the completed table is as given below.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ |

---

**Question 1**

Consider the following initial value problem:
$y^{′} = x - 2y, y(1) = 0$
We wish to approximate the solution using the modified Euler method. Given that the step size is $Δx = \frac{1}{2}$, fill the missing entries in the table below.

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ |
| --- | ---: | ---: | ---: | ---: |
| $1$ | $0$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |

---

**Question 2**

Consider the following initial value problem:
$y^{′} = 2x - y, y(0) = 3$
We wish to approximate the solution using the modified Euler method. Given that the step size is $Δx = \frac{1}{4}$, fill the missing entries in the table below.

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ |
| --- | ---: | ---: | ---: | ---: |
| $0$ | $3$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |

---

<a id="the-modified-euler-method"></a>
## The Modified Euler Method

The modified Euler method uses Euler's method for the predictor step. We saw that the predictor's approximating line segment has left endpoint $(x,y)$ and right endpoint

$$
(x_\text{new}, y_p) = (x + \Delta x, y + \Delta y_p)
$$

Now, we apply the corrector step. In general, the corrector takes the predictor's initial approximation and "corrects" for some errors, moving it closer to the true solution.

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the **Modified Euler Method** with step size $\Delta x$ is given by

$$
\begin{bmatrix}Predictor: & Δy_{p} = y^{′} \cdot Δx, \\ Corrector: & Δy = \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx\end{bmatrix}
$$

where

$$
y'_p = f(x_\text{new},y_p)
$$

is the initially predicted slope at the right endpoint

$$
(x_\text{new}, y_p) = (x + \Delta x, y + \Delta y_p)
$$

In other words, the corrector of the modified Euler method uses the average slope over the interval $[x,x_\text{new}]$, based on the slopes at the current point $(x,y)$ and at the initially predicted new point $(x_\text{new}, y_p)$.

Let's demonstrate how to apply the corrector equation in our previous example.

---

<a id="a-worked-example"></a>
## A Worked Example

Let's return to the following initial value problem:

$$
y' = x^2 + y, \qquad y(0)=1
$$

We will now complete our approximation of the value of $y(1)$ using the modified Euler method.

First, recall that we previously completed the predictor step, resulting in the following values:

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ |  |  |

The initially predicted new point produced by the predictor equation is

$$
(x_\text{new}, y_p) = (0+1,2) =(1,2)
$$

So, the predicted slope at this point is

$$
\begin{aligned}
y_{p}^{′} &= x_{new}^{2} + y_{p} \\
&= 1^{2} + 2 \\
&= 3
\end{aligned}
$$

We can add this to our table.

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ | $3$ |  |

Finally, we compute $\Delta y$ using the corrector equation:

$$
\begin{aligned}
Δy &= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx \\
&= \frac{1}{2}(1 + 3) \cdot 1 \\
&= \frac{1}{2} \cdot 4 \\
&= 2
\end{aligned}
$$

We add this to our table.

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ | $3$ | $2$ |

The value of $x$ in the next row is

$$
x_\text{new} = 1
$$

To get the value of $y$ in the next row, we add $\Delta y$ to $y{:}$

$$
y_\text{new} = y + \Delta y = 1 + 2 = 3
$$

Adding these results to our table gives the following:

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ | $3$ | $2$ |
| $1$ | $3$ |  |  |  |  |  |

Therefore, we conclude that

$$
y(1) \approx 3
$$

We can view our approximation made by the modified Euler method in the coordinate plane as follows:

![](<../Source/11.1.3. The Modified Euler Method/Images/41f71a7676b32eeced1c321116817028.png>)

Notice how the correction made by the corrector significantly improves the initial approximation produced by the predictor, at the cost of additional computation.

---

<a id="calculating-the-corrector-term"></a>
## Calculating the Corrector Term

**Example:** Consider the following initial value problem:
$y' = xy^2 - x^2, \qquad y(0)=2$
We wish to approximate the solution using the modified Euler method. Complete the first row of the table below, given that the step size is $\Delta x = \dfrac12$.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $2$ | $0$ | $0$ | $2$ | $\dfrac{7}{4}$ |  |

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the modified Euler method with step size $\Delta x$ is given by

$$
\begin{aligned}
\begin{bmatrix}Predictor &: & Δy_{p} &= y^{′} \cdot Δx, \\ Corrector &: & Δy|= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx\end{bmatrix}
\end{aligned}
$$

where

$$
y'_p = f(x_\text{new},y_p)
$$

is the initially predicted slope at the predicted new point

$$
(x_\text{new}, y_p) = (x + \Delta x, y + \Delta y_p)
$$

We compute $\Delta y$ using the corrector equation:

$$
\begin{aligned}
Δy &= \frac{1}{2}(y^{′} + y_{p}^{′})Δx \\
&= \frac{1}{2}(0 + \frac{7}{4}) \cdot \frac{1}{2} \\
&= \frac{1}{4}(\frac{7}{4}) \\
&= \frac{7}{16}
\end{aligned}
$$

Therefore, the completed first row is as given below.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $2$ | $0$ | $0$ | $2$ | $\dfrac{7}{4}$ | $\dfrac{7}{16}$ |

---

**Question 3**

Consider the following initial value problem:
$y^{′} = x - y, y(0) = 2$
We wish to approximate the solution using the modified Euler method. Complete the first row of the table below, given that the step size is $Δx = \frac{1}{2}$.

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ | $y_{p}^{′}$ | $Δy$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $2$ | $-2$ | $-1$ | $1$ | $-\frac{1}{2}$ | $\underline{\hspace{1.5cm}}$ |

---

**Question 4**

Consider the following initial value problem:
$y^{′} = x^{2} - y^{2}, y(0) = 1$
We wish to approximate the solution using the modified Euler method. Complete the first row of the table below, given that the step size is $Δx = \frac{1}{4}$.

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ | $y_{p}^{′}$ | $Δy$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $-1$ | $-\frac{1}{4}$ | $\frac{3}{4}$ | $-\frac{1}{2}$ | $\underline{\hspace{1.5cm}}$ |

---

<a id="completing-the-first-row-of-a-table-using-the-modified-euler-method"></a>
## Completing the First Row of a Table Using the Modified Euler Method

**Example:** Consider the following initial value problem:
$y' = xy - y^2 + 2, \qquad y(0)=1$
We wish to approximate the solution using the modified Euler method. Complete the first row of the table below, given that the step size is $\Delta x = 1$.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ |  |  |  |  |  |

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the modified Euler method with step size $\Delta x$ is given by

$$
\begin{aligned}
\begin{bmatrix}Predictor &: & Δy_{p} &= y^{′} \cdot Δx, \\ Corrector &: & Δy|= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx\end{bmatrix}
\end{aligned}
$$

where

$$
y'_p = f(x_\text{new},y_p)
$$

is the initially predicted slope at the predicted new point

$$
(x_\text{new}, y_p) = (x + \Delta x, y + \Delta y_p)
$$

First, we compute the value of $y'$ according to the given rule:

$$
\begin{aligned}
y^{′} &= xy - y^{2} + 2 \\
&= 0 \cdot 1 - 1^{2} + 2 \\
&= 1
\end{aligned}
$$

So, we add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ |  |  |  |  |

Next, we compute $\Delta y_p$ using the predictor equation:

$$
\begin{aligned}
Δy_{p} &= y^{′} \cdot Δx \\
&= 1 \cdot 1 \\
&= 1
\end{aligned}
$$

We add this value to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ |  |  |  |

The coordinates of the initially predicted new point given by the predictor equation are

$$
\begin{aligned}
x_{new} &= x + Δx = 0 + 1 = 1, \\
y_{p} &= y + Δy_{p} = 1 + 1 = 2
\end{aligned}
$$

Let's add the second value to the table.

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ |  |  |

So, the predicted slope at this point is

$$
\begin{aligned}
y_{p}^{′} &= f(x_{new}, y_{p}) \\
&= x_{new}y_{p} - y_{p}^{2} + 2 \\
&= (1)(2) - (2)^{2} + 2 \\
&= 0
\end{aligned}
$$

We can add this to our table.

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ | $0$ |  |

Finally, we compute $\Delta y$ using the corrector equation:

$$
\begin{aligned}
Δy &= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx \\
&= \frac{1}{2}(1 + 0) \cdot 1 \\
&= \frac{1}{2}
\end{aligned}
$$

Therefore, the completed first row is as given below.

| $\,x\,$ | $\,y\,$ | $\, y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $1$ | $2$ | $0$ | $\dfrac{1}{2}$ |

---

**Question 5**

Consider the following initial value problem:
$y^{′} = xy + 1, y(0) = 1$
We wish to approximate the solution using the modified Euler method. Complete the first row of the table below, given that the step size is $Δx = \frac{1}{2}$.

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ | $y_{p}^{′}$ | $Δy$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $0$ | $1$ | $1$ | $\frac{1}{2}$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ | $\underline{\hspace{1.5cm}}$ |

---

**Question 6**

Consider the following initial value problem:
$y^{′} = 2xy, y(1) = 1$
We wish to approximate the solution using the modified Euler method. What entry should be placed in the cell indicated in the table below, if the step size is $Δx = 1$?

| $x$ | $y$ | $y^{′}$ | $Δy_{p}$ | $y_{p}$ | $y_{p}^{′}$ | $Δy$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ |  |  |  |  |  |
| $2$ | $$? |  |  |  |  |  |

- [ ] A. $5$
- [ ] B. $6$
- [ ] C. $7$
- [ ] D. $4$
- [ ] E. $8$

---

<a id="approximating-the-solution-to-an-initial-value-problem-using-the-modified-euler-method-with-one-step"></a>
## Approximating the Solution to an Initial Value Problem Using The Modified Euler Method With One Step

**Example:** Consider the following initial value problem:
$y' = x^2y^2+xy^2, \qquad y(1)=1$
Use the modified Euler method with one step to approximate $y(2)$.

**Explanation**

For an initial value problem

$$
y' = f(x,y), \qquad y_0 = y(x_0)
$$

the modified Euler method with step size $\Delta x$ is given by

$$
\begin{aligned}
\begin{bmatrix}Predictor &: & Δy_{p} &= y^{′} \cdot Δx, \\ Corrector &: & Δy|= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx\end{bmatrix}
\end{aligned}
$$

where

$$
y'_p = f(x_\text{new},y_p)
$$

is the initially predicted slope at the predicted new point

$$
(x_\text{new}, y_p) = (x + \Delta x, y + \Delta y_p)
$$

Since we want to find the value of $y$ at $x=2$, we will use a step size of

$$
\Delta x = 2 - 1 = 1
$$

First, we add the initial condition data to a table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ |  |  |  |  |  |

Now, we compute the value of $y'$ according to the given rule:

$$
\begin{aligned}
y^{′} &= x^{2}y^{2} + xy^{2} \\
&= 1^{2} \cdot 1^{2} + 1 \cdot 1^{2} \\
&= 2
\end{aligned}
$$

So, we add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ |  |  |  |  |

Next, we compute $\Delta y_p$ using the predictor equation:

$$
\begin{aligned}
Δy_{p} &= y^{′} \cdot Δx \\
&= 2 \cdot 1 \\
&= 2
\end{aligned}
$$

We add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ | $2$ |  |  |  |

The coordinates of the initially predicted new point given by the predictor equation are

$$
\begin{aligned}
x_{new} &= x + Δx = 1 + 1 = 2, \\
y_{p} &= y + Δy_{p} = 1 + 2 = 3
\end{aligned}
$$

Let's add the second value to the table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ | $2$ | $3$ |  |  |

So, the predicted slope at this point is

$$
\begin{aligned}
y_{p}^{′} &= x_{new}^{2}y_{p}^{2} + x_{new}y_{p}^{2} \\
&= 2^{2} \cdot 3^{2} + 2 \cdot 3^{2} \\
&= 54
\end{aligned}
$$

We can add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ | $2$ | $3$ | $54$ |  |

Finally, we compute $\Delta y$ using the corrector equation:

$$
\begin{aligned}
Δy &= \frac{1}{2}(y^{′} + y_{p}^{′}) \cdot Δx \\
&= \frac{1}{2}(2 + 54) \cdot 1 \\
&= \frac{1}{2} \cdot 56 \\
&= 28
\end{aligned}
$$

We add this to our table.

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ | $2$ | $3$ | $54$ | $28$ |

The value of $x$ in the next row is

$$
x_\text{new} = 2
$$

To get the value of $y$ in the next row, we add $\Delta y$ to $y{:}$

$$
y_\text{new} = y + \Delta y = 1 + 28 = 29
$$

Adding these results to our table gives the following:

| $\,x\,$ | $\,y\,$ | $\,y'\,$ | $\Delta y_p$ | $y_p$ | $y'_p$ | $\Delta y$ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| $1$ | $1$ | $2$ | $2$ | $3$ | $54$ | $28$ |
| $2$ | $29$ |  |  |  |  |  |

Therefore, we conclude that

$$
y(2) \approx \bbox[4pt,border: 1px solid gainsboro]{29}
$$

---

**Question 7**

Consider the following initial value problem:
$y^{′} = 3x + 2y, y(0) = 1$
Use the modified Euler method with one step to approximate $y(1)$.$y(1) \approx$
$\underline{\hspace{1.5cm}}$

---

**Question 8**

Consider the following initial value problem:
$y^{′} = x^{2} + 2xy + y^{2}, y(0) = 1$
Use the modified Euler method with one step to approximate $y(1)$.$y(1) \approx$
$\underline{\hspace{1.5cm}}$

```update-progress
```

[[MA/PS2/Home|Home]]
[[MA/PS2/0. Table of Contents/TOC|Table of Contents]]
