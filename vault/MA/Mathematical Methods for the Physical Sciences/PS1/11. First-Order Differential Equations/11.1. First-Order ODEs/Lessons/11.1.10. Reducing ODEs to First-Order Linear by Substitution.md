# Reducing ODEs to First-Order Linear by Substitution

<!--
lesson-id: 1164
topic-code: PS1.11.1.10
-->

## Table of Contents

- [Introduction](#introduction)
- [Reducing a First-Order ODE to a First-Order Linear ODE Using a Substitution](#reducing-a-first-order-ode-to-a-first-order-linear-ode-using-a-substitution)
- [Solving a First-Order ODE Using a Substitution](#solving-a-first-order-ode-using-a-substitution)
- [Solving a First-Order Initial Value Problem Using a Substitution](#solving-a-first-order-initial-value-problem-using-a-substitution)

## Prerequisites

- [Solving First-Order Linear ODEs Using Integrating Factors](<11.1.7. Solving First-Order Linear ODEs Using Integrating Factors.md>)

---

<a id="introduction"></a>
## Introduction

Let's consider the first-order differential equation

$$
2y\dfrac {\textrm{d}y} {\textrm{d}x}+y^2=x
$$

Notice that it is *not* a first-order linear equation because it cannot be written in the form

$$
y' + P(x) y = Q(x)
$$

In fact, this equation is an example of a *nonlinear* first-order differential equation.

We can sometimes transform a nonlinear differential equation into a linear differential equation using a substitution. If we can do this, then we can apply the method of integrating factors to solve it.

To transform the above equation into a linear differential equation, we can use the substitution

$$
z=y^2
$$

Differentiating $z(x)$ using the chain rule, we get

$$
\dfrac {\textrm{d}z} {\textrm{d}x}=2y\dfrac {\textrm{d}y} {\textrm{d}x}
$$

Consequently, the original differential equation can be written in terms of $z$ as follows:

$$
\dfrac {\textrm{d}z} {\textrm{d}x}+z=x
$$

This differential equation is linear! So, we can use the method of integrating factors. The given equation is in the standard form

$$
\frac{\textrm{d}z}{\textrm{d}x} + P(x)z = Q(x)
$$

with

$$
P(x)=1
$$

and $Q(x)=x$. We then compute an integrating factor:

$$
\begin{aligned}
I(x) &= e^{∫P(x)dx} \\
&= e^{∫1dx} \\
&= e^{x}
\end{aligned}
$$

Multiplying both sides of the standard form equation by $I(x)=e^x$ yields

$$
\begin{aligned}
e^{x}(\frac{dz}{dx} + z) &= e^{x} \cdot x \\
\frac{dz}{dx}e^{x} + ze^{x} &= xe^{x} \\
\frac{d}{dx}(ze^{x}) &= xe^{x}
\end{aligned}
$$

We now integrate with respect to $x$, using integration by parts to integrate the right-hand side:

$$
\begin{aligned}
∫\frac{d}{dx}(ze^{x})dx &= ∫xe^{x}dx \\
ze^{x} &= (x - 1)e^{x} + C \\
z &= x - 1 + Ce^{-x}
\end{aligned}
$$

Finally, we determine $y(x)$ by substituting the solution into $z=y^2$. This results in

$$
\begin{aligned}
y^{2} &= z \\
y^{2} &= x - 1 + Ce^{-x} \\
y &= ± \sqrt{x - 1 + Ce^{-x}}
\end{aligned}
$$

---

<a id="reducing-a-first-order-ode-to-a-first-order-linear-ode-using-a-substitution"></a>
## Reducing a First-Order ODE to a First-Order Linear ODE Using a Substitution

**Example:** The substitution $z= y^2$ can be used to transform the equation
$\dfrac{{{\rm{d}}y}}{{{\rm{d}}x}} = \dfrac{x+2}{2y} + 2x^2y$
into an equation of the form $z'(x) + P(x)z(x) = Q(x)$. What is the function $P(x)$?

**Explanation**

First, we make the substitution
$z= y^{2}$.
Differentiating $z(x)$ using the chain rule, we get

$$
\dfrac{\textrm{d}z}{\textrm{d}x} = 2y \,\dfrac{\textrm{d}y}{\textrm{d}x} \quad\Longrightarrow\quad \dfrac{\textrm{d}y}{\textrm{d}x} = \dfrac{1}{2z^{1/2}}\,\dfrac{\textrm{d}z}{\textrm{d}x}
$$

Consequently, the original differential equation can be written in terms of $z$ as follows:

$$
\begin{aligned}
\frac{1}{2z^{1/2}}\frac{dz}{dx} &= \frac{x + 2}{2z^{1/2}} + 2x^{2}z^{1/2}
\end{aligned}
$$

We now write the above equation in standard form

$$
\frac{\textrm{d}z}{\textrm{d}x} + P(x)z = Q(x)
$$

In doing so, we obtain

$$
\begin{aligned}
\frac{dz}{dx} &= x + 2 + 4x^{2}z \\
\frac{dz}{dx} - 4x^{2}z &= x + 2
\end{aligned}
$$

Therefore,

$$
P(x) =-4x^2
$$

---

**Question 1:** The substitution $z = y^{-1}$ can be used to transform the equation
$x^{2}\frac{dy}{dx} = y + 2y^{2}$
into an equation of the form $z^{′}(x) + P(x)z(x) = Q(x)$. What is the function $P(x)$?

- [ ] A. $-\frac{2}{x^{2}}$
- [ ] B. $x^{2}$
- [ ] C. $-2x^{2}$
- [ ] D. $-\frac{1}{x^{2}}$
- [ ] E. $\frac{1}{x^{2}}$

---

**Question 2:** The substitution $z = y^{2}$ can be used to transform the equation
$2x^{2}\frac{dy}{dx} = \frac{1 - y^{2}}{y}$
into an equation of the form $z^{′}(x) + P(x)z(x) = Q(x)$. What is the function $Q(x)$?

- [ ] A. $-\frac{2}{x^{3}}$
- [ ] B. $-x^{2}$
- [ ] C. $2x^{3}$
- [ ] D. $\frac{1}{x}$
- [ ] E. $\frac{1}{x^{2}}$

---

<a id="solving-a-first-order-ode-using-a-substitution"></a>
## Solving a First-Order ODE Using a Substitution

**Example:** Find the general solution to the following first-order ODE using the substitution $z=y^3$.
$xy^2\dfrac {\textrm{d}y} {\textrm{d}x} = x^2+y^3$

**Explanation**

Let's make a substitution
$z=y^3\,$.

Differentiating $z(x)$ using the chain rule, we get

$$
\dfrac{\textrm{d}z}{\textrm{d}x}=3y^2\,\dfrac{\textrm{d}y}{\textrm{d}x} \quad\Longrightarrow\quad y^2\dfrac{\textrm{d}y}{\textrm{d}x}=\dfrac{1}{3}\,\dfrac{\textrm{d}z}{\textrm{d}x}
$$

Consequently, the original differential equation can be written in terms of $z$ as follows:

$$
\frac{x}{3}\left(\dfrac{\textrm{d}z}{\textrm{d}x}\right)=x^2+z
$$

This differential equation is linear, so we can use the method of integrating factors. First, we write the above equation in standard form:

$$
\begin{aligned}
\frac{dz}{dx} &= \frac{3}{x}(x^{2} + z) \\
\frac{dz}{dx} &= 3x + \frac{3}{x}z \\
\frac{dz}{dx} - \frac{3}{x}z &= 3x
\end{aligned}
$$

The above equation is in the standard form

$$
\frac{\textrm{d}z}{\textrm{d}x} + P(x)y = Q(x)
$$

with

$$
P(x) =-\frac{3}{x}, Q(x) = 3x
$$

We then compute an integrating factor:

$$
\begin{aligned}
I(x) &= e^{∫P(x)dx} \\
&= e^{∫(-3/x)dx} \\
&= e^{-3\ln x} \\
&= \frac{1}{x^{3}}
\end{aligned}
$$

Multiplying both sides of the standard form equation by

$$
I(x)=\dfrac{1}{x^3}
$$

yields

$$
\begin{aligned}
\frac{1}{x^{3}}(\frac{dz}{dx} - \frac{3}{x}z) &= \frac{1}{x^{3}} \cdot 3x \\
\frac{1}{x^{3}} \cdot \frac{dz}{dx} - \frac{3}{x^{4}}z &= \frac{3}{x^{2}} \\
\frac{d}{dx}(\frac{z}{x^{3}}) &= \frac{3}{x^{2}}
\end{aligned}
$$

Now, we integrate with respect to $x$:

$$
\begin{aligned}
∫\frac{d}{dx}(\frac{z}{x^{3}})dx &= ∫\frac{3}{x^{2}}dx \\
\frac{z}{x^{3}} &= -\frac{3}{x} + C \\
z &= -3x^{2} + Cx^{3}
\end{aligned}
$$

Finally, we determine $y(x)$ by substituting the solution into $z=y^3$. This results in

$$
\begin{aligned}
y^{3} &= z \\
y^{3} &= -3x^{2} + Cx^{3} \\
y &= \sqrt[3]{-3x^{2} + Cx^{3}}
\end{aligned}
$$

---

**Question 3**

Find the general solution to the following equation.
$2(1 + x^{2})\frac{dy}{dx} = \frac{1}{y} - 2xy, y > 0$

*Hint: You may wish to make use of the substitution $z = y^{2}$.*

- [ ] A. $y = \sqrt{\frac{x + C}{1 + x^{2}}}$
- [ ] B. $y = \sqrt{\frac{x + C}{1 + x}}$
- [ ] C. $y = \frac{x + C}{1 + x^{2}}$
- [ ] D. $y = \frac{2x}{C - x^{2}}$
- [ ] E. $y = \sqrt{\frac{2x + C}{2 + x^{2}}}$

---

**Question 4**

Find the general solution to the following equation.
$\frac{dy}{dx} = \frac{y + 2x^{2}y^{2}}{x}$

*Hint: You may wish to make use of the substitution $z = y^{-1}$.*

- [ ] A. $y = \frac{2x}{C - 3x^{2}}$
- [ ] B. $y = \frac{3x}{C - 2x^{3}}$
- [ ] C. $y = \frac{3x^{2}}{C - x^{3}}$
- [ ] D. $y = \frac{2x^{2}}{C - 3x}$
- [ ] E. $y = \frac{2x}{C - x}$

---

<a id="solving-a-first-order-initial-value-problem-using-a-substitution"></a>
## Solving a First-Order Initial Value Problem Using a Substitution

**Example:** Find the specific solution of the first-order ODE
$xe^y\dfrac {\textrm{d}y} {\textrm{d}x}+e^y = e^{x} \,$,
given that $y(1)=1$ and $x>0$.

**Explanation**

Let's make a substitution
$z=e^y\,$.
Differentiating $z(x)$ with the chain rule, we get

$$
\dfrac{\textrm{d}z}{\textrm{d}x}=e^y\,\dfrac{\textrm{d}y}{\textrm{d}x}
$$

Consequently, the original differential equation can be written in terms of $z$ as follows:

$$
x\dfrac{\textrm{d}z}{\textrm{d}x}+z=e^x\,
$$

with the initial condition

$$
z(1)=e^{y(1)}=e^1=e\,
$$

Notice that the left-hand side of the differential equation is now an exact derivative:

$$
\begin{aligned}
x\frac{dz}{dx} + z &= \frac{d}{dx}(xz)
\end{aligned}
$$

So, we have

$$
\begin{aligned}
\frac{d}{dx}(xz) &= e^{x} \\
∫\frac{d}{dx}(xz)dx &= ∫e^{x}dx \\
xz &= e^{x} + C \\
z &= \frac{e^{x} + C}{x}
\end{aligned}
$$

Now, we determine the value of $C$ that satisfies the initial condition $z(1)=e$:

$$
\begin{aligned}
z(1) &= e \\
\frac{e^{1} + C}{1} &= e \\
e + C &= e \\
C &= 0
\end{aligned}
$$

So we have

$$
z = \frac{e^x}{x}\,
$$

Finally, we determine $y(x)$ by substituting the solution into $z=e^y$. This results in

$$
\begin{aligned}
e^{y} &= z \\
e^{y} &= \frac{e^{x}}{x} \\
y &= \ln (\frac{e^{x}}{x}) \\
&= \ln (e^{x}) - \ln (x) \\
&= x - \ln (x)
\end{aligned}
$$

under the condition that $x>0$. So, the final solution is

$$
y = x - \ln (x), \quad x > 0
$$

---

**Question 5**

Solve the initial value problem
$2(1 + x^{2})\frac{dy}{dx} = \frac{1}{y} - 2xy, y(0) = 2$.

*Hint: You may wish to make use of the fact that the given differential equation can be transformed into the equation
$\frac{dz}{dx} + \frac{2x}{1 + x^{2}}z = \frac{1}{1 + x^{2}}$
using the substitution $z = y^{2}$.*

- [ ] A. $y = \frac{x + 2}{1 - x^{2}}$
- [ ] B. $y = \sqrt{\frac{x + 1}{x^{2} + 1}}$
- [ ] C. $y = 2\sqrt{\frac{x^{2} + 1}{x + 1}}$
- [ ] D. $y = \frac{x + 2}{\sqrt{x^{2} + 1}}$
- [ ] E. $y = \sqrt{\frac{x + 4}{x^{2} + 1}}$

---

**Question 6**

Solve the initial value problem
$\frac{dy}{dx} + (y(y - 1))/(x) = 0, y(1) = 5, x > 0$.

*Hint: You may wish to make use of the fact that the given differential equation can be transformed into the equation
$\frac{dz}{dx} + \frac{1}{x}z = \frac{1}{x}$
using the substitution $z = y^{-1}$.*

- [ ] A. $y = \frac{x - 6}{x - 2}$
- [ ] B. $y = \frac{3x + 7}{1 + x}$
- [ ] C. $y = \frac{5x}{5x - 4}$
- [ ] D. $y = \frac{x}{x + 1}$
- [ ] E. $y = \frac{x + 4}{4x - 3}$

```update-progress
```

[[MA/Mathematical Methods for the Physical Sciences/PS1/Home|Home]]
[[MA/Mathematical Methods for the Physical Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
