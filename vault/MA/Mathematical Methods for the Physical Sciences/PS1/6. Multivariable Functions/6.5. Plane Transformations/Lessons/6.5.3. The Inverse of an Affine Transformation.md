# The Inverse of an Affine Transformation

<!--
lesson-id: 3707
topic-code: PS1.6.5.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying Invertible Affine Transformations](#identifying-invertible-affine-transformations)
- [Finding the Inverse of an Affine Transformation](#finding-the-inverse-of-an-affine-transformation)
- [Inverting Affine Transformations Given in Matrix Form](#inverting-affine-transformations-given-in-matrix-form)
- [Inverting Affine Transformations Given in Parametric Form](#inverting-affine-transformations-given-in-parametric-form)
- [Image Types Formed by Affine Transformations](#image-types-formed-by-affine-transformations)
- [A Summary of Image Types](#a-summary-of-image-types)
- [Identifying the Types of Images Formed by an Affine Transformation](#identifying-the-types-of-images-formed-by-an-affine-transformation)

## Prerequisites

- [Solving 2x2 Systems of Equations Using Inverse Matrices](<../../../../PCL/6. Matrices/6.4. The Inverse of a Matrix/Lessons/6.4.4. Solving 2x2 Systems of Equations Using Inverse Matrices.md>)
- [Singular Linear Transformations in the Plane](<../../../../PCL/6. Matrices/6.5. Linear Transformations/Lessons/6.5.12. Singular Linear Transformations in the Plane.md>)
- [Affine Transformations](<6.5.1. Affine Transformations.md>)

---

<a id="introduction"></a>
## Introduction

Suppose that $\mathbf T:\mathbb R^2\rightarrow\mathbb R^2$ is an affine transformation. The **inverse** of $\mathbf T$ (if it exists) is an affine transformation $\mathbf T^{-1}$ such that

$$
\mathbf T^{-1} (\mathbf T(\mathbf u)) = \mathbf T (\mathbf T^{-1}(\mathbf u)) = \mathbf u
$$

for every vector $\mathbf u\in\mathbb R^2$. In other words, the transformation $\mathbf T^{-1}$ reverses the action of $\mathbf T$.

Not all affine transformations are invertible. So how can we determine whether a given affine transformation is invertible?

To answer this, recall that an affine transformation $\mathbf T$ can be expressed in matrix form as

$$
\mathbf x = A \mathbf u + \mathbf b
$$

where $\mathbf x, \mathbf u$, and $\mathbf b$ are column vectors in $\mathbb R^2$, and $A$ is a $2\times 2$ matrix. We can reverse the action of $\mathbf T$ by making $\mathbf u$ the subject of this equation, as follows:

$$
\begin{aligned}
Au + b &= x \\
Au &= x - b \\
A^{-1} \cdot Au &= A^{-1} \cdot (x - b) \\
I_{2} \cdot u &= A^{-1}(x - b) \\
u &= A^{-1}(x - b)
\end{aligned}
$$

This equation gives us a method of computing the inverse of an affine transformation. Additionally, we have the following:

> *An affine transformation $\mathbf T$ is invertible if and only if the matrix $A$ that represents the linear transformation associated with $\mathbf T$ is invertible, that is, $\det(A) \neq 0$.*

---

<a id="identifying-invertible-affine-transformations"></a>
## Identifying Invertible Affine Transformations

**Example:** Consider the affine transformation $\mathbf T$ defined as

$x = u - v - 2, \qquad y = v - u + 5$.

Suppose that $A$ is the matrix representing the linear transformation associated with $\mathbf T$. Which of the following statements are true?

1. $\begin{bmatrix}1 & -1 \\ -1 & 1\end{bmatrix}$
2. $\det(A) = 0$
3. The transformation is invertible

**Explanation**

An affine transformation $\mathbf T:\mathbb R^2\rightarrow\mathbb R^2$ is a function of the form

$$
\mathbf x = A \mathbf u + \mathbf b
$$

where $\mathbf x, \mathbf u$, and $\mathbf b$ are column vectors in $\mathbb R^2$, and $A$ is a $2\times 2$ matrix.

Let's look at each statement in turn:

- Statement I is true. The given transformation can be expressed as
$\begin{bmatrix}x \\ y\end{bmatrix}|= \begin{bmatrix}u - v - 2 \\ v - u + 5\end{bmatrix}; = \begin{bmatrix}u - v \\ -u + v\end{bmatrix} + \begin{bmatrix}-2 \\ 5\end{bmatrix}; = \begin{bmatrix}1 & -1 \\ -1 & 1\end{bmatrix}_(⏟)_(A) \cdot \begin{bmatrix}u \\ v\end{bmatrix}_(⏟)_(u) + \begin{bmatrix}-2 \\ 5\end{bmatrix}_(⏟)_(b)$.
Therefore,
$\begin{bmatrix}1 & -1 \\ -1 & 1\end{bmatrix}$
- Statement II is true. Computing the determinant of $A$, we get
$\begin{bmatrix}1 & -1 \\ -1 & 1\end{bmatrix}$
- Statement III is false. Since $\det(A) = 0$, the affine transformation is *not* invertible.

Therefore, the correct answer is "I and II only."

---

**Question 1**

Consider the affine transformation $T$ defined as

$x = u - v, y = u + 2v + 1$.

Suppose that $A$ is the matrix that represents the linear transformation associated with $T$. Which of the following statements are true?

1. $A = \begin{bmatrix}1 & 1 \\ 1 & 2\end{bmatrix}$
2. $det(A) = 0$
3. The transformation is invertible

- [ ] A. I and III only
- [ ] B. I and II only
- [ ] C. II only
- [ ] D. III only
- [ ] E. I only

---

**Question 2**

Which of the following affine transformations are invertible?

1. $T_{1}: \begin{bmatrix}u \\ v\end{bmatrix}⟶\begin{bmatrix}v - u \\ u + v\end{bmatrix}$
2. $T_{2}: \begin{bmatrix}u \\ v\end{bmatrix}⟶\begin{bmatrix}u - 2 \\ u - v + 1\end{bmatrix}$
3. $T_{3}: \begin{bmatrix}u \\ v\end{bmatrix}⟶\begin{bmatrix}3u + 6v \\ u + 2v - 1\end{bmatrix}$

- [ ] A. $T_{3}$ only
- [ ] B. $T_{1}$ and $T_{2}$ only
- [ ] C. $T_{1}$ and $T_{3}$ only
- [ ] D. $T_{2}$ only
- [ ] E. $T_{1}$ only

---

<a id="finding-the-inverse-of-an-affine-transformation"></a>
## Finding the Inverse of an Affine Transformation

Consider the affine transformation $\mathbf T$, expressed in matrix form as

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

We wish to determine whether $\mathbf T^{-1}$ exists and find its matrix form if it does exist.

Computing the determinant of $A$, we get
$\begin{bmatrix}1 & -3 \\ 1 & -2\end{bmatrix}$

Since

$$
\det(A) \neq 0
$$

the affine transformation is invertible.

To compute the inverse of $\mathbf T$, we will use the formula we derived earlier:

$$
\mathbf{u} = A^{-1}(\mathbf{x} - \mathbf{b})
$$

We start by calculating the inverse of $A{:}$

$$
\begin{aligned}
A^{-1} &= \frac{1}{1}\begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix} \\
&= \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}
\end{aligned}
$$

Therefore, the matrix form of the inverse affine transformation $\mathbf{T}^{-1}$ is

$$
\begin{aligned}
\begin{bmatrix}u \\ v\end{bmatrix}_(⏟)_(u) &= \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}_(⏟)_(A^{-1}) \cdot (\begin{bmatrix}x \\ y\end{bmatrix}_(⏟)_(x) - \begin{bmatrix}2 \\ 3\end{bmatrix}_(⏟)_(b)) \\
&= \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}\begin{bmatrix}2 \\ 3\end{bmatrix} \\
&= \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}5 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}-2 & 3 \\ -1 & 1\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} + \begin{bmatrix}-5 \\ -1\end{bmatrix}
\end{aligned}
$$

---

<a id="inverting-affine-transformations-given-in-matrix-form"></a>
## Inverting Affine Transformations Given in Matrix Form

**Example:** The matrix form of the affine transformation $\mathbf T$ is given by

$\begin{bmatrix}x \\ y\end{bmatrix}$

Find $a+b+c$, given that the matrix form of the inverse transformation $\mathbf T^{-1}$ is

$\begin{bmatrix}u \\ v\end{bmatrix}$

**Explanation**

An affine transformation $\mathbf T:\mathbb R^2\rightarrow\mathbb R^2$ is a function of the form

$$
\mathbf x = A \mathbf u + \mathbf b
$$

where $\mathbf x, \mathbf u$, and $\mathbf b$ are column vectors in $\mathbb R^2$, and $A$ is a $2\times 2$ matrix.

Reversing the action of $\mathbf{T}$, we get

$$
\begin{aligned}
Au + b &= x \\
Au &= x - b \\
u &= A^{-1}(x - b)
\end{aligned}
$$

The matrix form of $\mathbf T$ gives us the following information:

$$
\begin{bmatrix}-4 & -9 \\ 3 & 7\end{bmatrix}
$$

Next, we calculate the inverse of $A{:}$

$$
\begin{aligned}
A^{-1} &= (1)/((-4) \cdot 7 - (-9) \cdot 3)\begin{bmatrix}7 & 9 \\ -3 & -4\end{bmatrix} \\
&= \frac{1}{-1}\begin{bmatrix}7 & 9 \\ -3 & -4\end{bmatrix} \\
&= \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}
\end{aligned}
$$

Therefore, the inverse transformation $\mathbf{T}^{-1}$ is

$$
\begin{aligned}
\begin{bmatrix}u \\ v\end{bmatrix}_(⏟)_(u) &= \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}_(⏟)_(A^{-1}) \cdot (\begin{bmatrix}x \\ y\end{bmatrix}_(⏟)_(x) - \begin{bmatrix}5 \\ -3\end{bmatrix}_(⏟)_(b)) \\
&= \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}\begin{bmatrix}5 \\ -3\end{bmatrix} \\
&= \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} - \begin{bmatrix}-8 \\ 3\end{bmatrix} \\
&= \begin{bmatrix}-7 & -9 \\ 3 & 4\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} + \begin{bmatrix}8 \\ -3\end{bmatrix}
\end{aligned}
$$

Therefore, we have

$$
a+b+c = -7 + 3 + 8 = 4
$$

---

**Question 3**

The matrix form of the affine transformation $T$ is given by

$\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}2 & 1 \\ 5 & 2\end{bmatrix} \cdot \begin{bmatrix}u \\ v\end{bmatrix} + \begin{bmatrix}2 \\ 1\end{bmatrix}$.

Find $a + b + c$, given that the matrix form of the inverse transformation $T^{-1}$ is given by

$\begin{bmatrix}u \\ v\end{bmatrix} = \begin{bmatrix}a & b \\ 5 & -2\end{bmatrix} \cdot \begin{bmatrix}x \\ y\end{bmatrix} + \begin{bmatrix}3 \\ c\end{bmatrix}$.

- [ ] A. $-11$
- [ ] B. $7$
- [ ] C. $11$
- [ ] D. $-8$
- [ ] E. $-9$

---

**Question 4**

The matrix form of the affine transformation $T$ is given by

$\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}7 & -5 \\ -4 & 3\end{bmatrix} \cdot \begin{bmatrix}u \\ v\end{bmatrix} + \begin{bmatrix}4 \\ -3\end{bmatrix}$.

Find $a + b + c$, given that the matrix form of the inverse transformation $T^{-1}$ is given by

$\begin{bmatrix}u \\ v\end{bmatrix} = \begin{bmatrix}a & 5 \\ b & 7\end{bmatrix}\begin{bmatrix}x \\ y\end{bmatrix} + \begin{bmatrix}c \\ 5\end{bmatrix}$.

- [ ] A. $-4$
- [ ] B. $10$
- [ ] C. $4$
- [ ] D. $6$
- [ ] E. $0$

---

<a id="inverting-affine-transformations-given-in-parametric-form"></a>
## Inverting Affine Transformations Given in Parametric Form

**Example:** The affine transformation $\mathbf T$ is defined as

$x = u - v + 2, \qquad y = 4u - 5v + 9$.

Given that the inverse transformation $\mathbf T^{-1}$ of $\mathbf T$ is
$u = \bbox[Gainsboro,2pt]{a}x - y + \bbox[Gainsboro,2pt]{b}, \qquad v = \bbox[Gainsboro,2pt]{c}x - y + 1$,
what is the value of $a+b+c$?

**Explanation**

An affine transformation $\mathbf T:\mathbb R^2\rightarrow\mathbb R^2$ is a function of the form

$$
\mathbf x = A \mathbf u + \mathbf b
$$

where $\mathbf x, \mathbf u$, and $\mathbf b$ are column vectors in $\mathbb R^2$, and $A$ is a $2\times 2$ matrix.

Reversing the action of $\mathbf{T}$, we get

$$
\begin{aligned}
Au + b &= x \\
Au &= x - b \\
u &= A^{-1}(x - b)
\end{aligned}
$$

So, writing our transformation in matrix form, we have

$$
\begin{aligned}
\begin{bmatrix}x \\ y\end{bmatrix} &= \begin{bmatrix}u - v + 2 \\ 4u - 5v + 9\end{bmatrix} \\
&= \begin{bmatrix}u - v \\ 4u - 5v\end{bmatrix} + \begin{bmatrix}2 \\ 9\end{bmatrix} \\
&= \begin{bmatrix}1 & -1 \\ 4 & -5\end{bmatrix}_(⏟)_(A) \cdot \begin{bmatrix}u \\ v\end{bmatrix}_(⏟)_(u) + \begin{bmatrix}2 \\ 9\end{bmatrix}_(⏟)_(b)
\end{aligned}
$$

Next, we calculate the inverse of $A{:}$

$$
\begin{aligned}
A^{-1} &= (1)/(1 \cdot (-5) - (-1) \cdot 4)\begin{bmatrix}-5 & 1 \\ -4 & 1\end{bmatrix} \\
&= \frac{1}{-1}\begin{bmatrix}-5 & 1 \\ -4 & 1\end{bmatrix} \\
&= \begin{bmatrix}5 & -1 \\ 4 & -1\end{bmatrix}
\end{aligned}
$$

Therefore, the inverse transformation $\mathbf{T}^{-1}$ is

$$
\begin{aligned}
\begin{bmatrix}u \\ v\end{bmatrix}_(⏟)_(u) &= \begin{bmatrix}5 & -1 \\ 4 & -1\end{bmatrix}_(⏟)_(A^{-1}) \cdot (\begin{bmatrix}x \\ y\end{bmatrix}_(⏟)_(x) - \begin{bmatrix}2 \\ 9\end{bmatrix}_(⏟)_(b)) \\
&= \begin{bmatrix}5 & -1 \\ 4 & -1\end{bmatrix}\begin{bmatrix}x - 2 \\ y - 9\end{bmatrix} \\
&= \begin{bmatrix}5 \cdot (x - 2) + (-1) \cdot (y - 9) \\ 4 \cdot (x - 2) + (-1) \cdot (y - 9)\end{bmatrix} \\
&= \begin{bmatrix}5x - y - 1 \\ 4x - y + 1\end{bmatrix}
\end{aligned}
$$

Equating the corresponding components, the inverse transformation $\mathbf{T}^{-1}$ in coordinate form is

$$
u = \bbox[Gainsboro,2pt]{5}x - y + \bbox[Gainsboro,2pt]{(-1)}, \qquad v = \bbox[Gainsboro,2pt]{4}x - y + 1
$$

Therefore, we have

$$
a+b+c = 5 + (-1) + 4 = 8
$$

---

**Question 5**

The affine transformation $T$ is defined as

$x = u + v, y = u + 2v + 1$.

Given that the inverse transformation $T^{-1}$ of $T$ is
$u = ax - y + 1, v = by - x + c$,
what is the value of $a + b + c$?

- [ ] A. $5$
- [ ] B. $2$
- [ ] C. $0$
- [ ] D. $-1$
- [ ] E. $1$

---

**Question 6**

The affine transformation $T$ is defined as

$x = 5v - 7u + 3, y = 7v - 10u + 5$.

Given that the inverse transformation $T^{-1}$ of $T$ is
$u = ax - 5y + 4, v = bx + cy + 5$,
what is the value of $a + b + c$?

- [ ] A. $5$
- [ ] B. $-3$
- [ ] C. $10$
- [ ] D. $1$
- [ ] E. $7$

---

<a id="image-types-formed-by-affine-transformations"></a>
## Image Types Formed by Affine Transformations

Consider the square in the $uv$-plane shown below.

![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/43240713453a0b851b7d353cad656456.png>)

We wish to explore the image of this square under an affine transformation
$\mathbf{T}: \mathbf{u} \longrightarrow A \mathbf{u} + \mathbf{b}$,
where $A$ is a $2\times 2$ matrix, and $\mathbf u$ and $\mathbf b$ are $2\times 1$ column vectors.

Similar to linear transformations, there are three possibilities depending on the value of $\det(A)$.

- $\mathbf{T}$ is non-singular ($\det(A)\neq 0$). In this case, our square is mapped to a parallelogram.For example, let the transformation $\mathbf T$ be given by
$\begin{bmatrix}x \\ y\end{bmatrix}$
Mapping our square to the $xy$-plane, we get the following parallelogram:![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/26bd6d4edcef4844a9574969d914ff34.png>)
- $\mathbf{T}$ is singular ($\det(A)=0$) and $A$ has at least one nonzero entry. In this case, our square is mapped to a line segment.For example, let the transformation $\mathbf T$ be given by
$\begin{bmatrix}x \\ y\end{bmatrix}$
Mapping our square to the $xy$-plane gives the following line segment:![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/fe612bcff9f6b1e098849b409d59578a.png>)
- $\mathbf{T}$ is singular ($\det(A)=0$) and $A$ is the zero matrix. In this case, our square is mapped to a point on the plane.For example, let the transformation $\mathbf T$ be given by
$\begin{bmatrix}x \\ y\end{bmatrix}$
Mapping our square to the $xy$-plane gives the following point.![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/f90cee3ea3bc0bb2875e5a165d9901a1.png>)

---

<a id="a-summary-of-image-types"></a>
## A Summary of Image Types

In general, if we consider the affine transformation $\mathbf T:\Bbb R^2 \to \Bbb R^2$, defined by the mapping

$\mathbf{T}: \mathbf{u} \longrightarrow A \mathbf{u} + \mathbf{b}$,
where $A$ is a $2\times 2$ matrix and $\mathbf u$ and $\mathbf b$ are $2\times 1$ column vectors, then we have one of the following three possibilities:

- If $\mathbf{T}$ is non-singular, i.e., when $\det(A) \neq 0$, then the image of a two-dimensional object is a two-dimensional object. In particular, the image of a polygon with $n$ vertices is a polygon with $n$ vertices.
- If $\mathbf{T}$ is singular, i.e., when $\det(A) = 0$, we have two additional subcases: If $A$ has at least one nonzero entry, then the image of a two-dimensional object is a one-dimensional line segment.If $A$ is the zero matrix, then the image of a two-dimensional object is a point on the plane (namely, the point with position vector $\mathbf b$).

---

<a id="identifying-the-types-of-images-formed-by-an-affine-transformation"></a>
## Identifying the Types of Images Formed by an Affine Transformation

**Example:** Consider the affine transformation $\mathbf T$, given by
$\begin{bmatrix}x \\ y\end{bmatrix}$
Describe the image of the triangle shown above under the action of $\mathbf{T}$.

![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/4252f221506b6465a4575a94777c7b53.png>)

**Explanation**

Recall that for any affine transformation
$\mathbf{T}: \mathbf{u} \longrightarrow A \mathbf{u} + \mathbf{b}$,
where $A$ is a $2\times 2$ matrix and $\mathbf u$ and $\mathbf b$ are $2\times 1$ column vectors, we have one of the following three possibilities:

- If $\mathbf{T}$ is non-singular, i.e., when $\det(A) \neq 0$, then the image of a two-dimensional object is a two-dimensional object. In particular, the image of a polygon with $n$ vertices is a polygon with $n$ vertices.
- If $\mathbf{T}$ is singular, i.e., when $\det(A) = 0$, we have two additional subcases:If $A$ has at least one nonzero entry, then the image of a two-dimensional object is a one-dimensional line segment.If $A$ is the zero matrix, then the image of a two-dimensional object is a point on the plane.

With that in mind, let's examine our transformation.

Notice that the matrix representation of the linear transformation associated with $\mathbf{T}$ is
$\begin{bmatrix}2 & 1 \\ 4 & 2\end{bmatrix}$

Computing the determinant of $A$, we obtain

$$
\begin{aligned}
det(T) &= \begin{vmatrix}2 & 1 \\ 4 & 2\end{vmatrix} \\
&= 2 \cdot 2 - 4 \cdot 1 \\
&= 0
\end{aligned}
$$

So, we have a singular nonzero matrix.

Therefore, $\mathbf{T}$ is non-invertible and the image of the triangle under the action of $\mathbf{T}$ is a line segment.

---

**Question 7**

![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/q-162152.png>)

Consider the affine transformation $T$, given by
$x = 2u - v, y = u + 2v + 1$.
What would be the image of the triangle shown above under the action of $T$?

- [ ] A. A line segment
- [ ] B. A point
- [ ] C. A tetrahedron
- [ ] D. A parallelogram
- [ ] E. A triangle

---

**Question 8**

![](<MA/Mathematical Methods for the Physical Sciences/PS1/6. Multivariable Functions/6.5. Plane Transformations/Source/6.5.3. The Inverse of an Affine Transformation/Images/q-162151.png>)

Consider the affine transformation $T$, given by
$\begin{bmatrix}x \\ y\end{bmatrix} = \begin{bmatrix}-2 & 6 \\ 1 & -3\end{bmatrix} \cdot \begin{bmatrix}u \\ v\end{bmatrix} + \begin{bmatrix}-1 \\ 1\end{bmatrix}$.
What would be the image of the square shown above under the action of $T$?

- [ ] A. A line segment
- [ ] B. A triangle
- [ ] C. A point on the plane
- [ ] D. A parallelogram
- [ ] E. A parallelepiped

```update-progress
```

[[MA/Mathematical Methods for the Physical Sciences/PS1/Home|Home]]
[[MA/Mathematical Methods for the Physical Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
