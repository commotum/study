# Creating Rows or Columns Containing Zeros Using Gaussian Elimination

<!--
lesson-id: 3029
topic-code: PS1.2.1.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Using Row Operations to Create a Column of Zeros in a 3x3 Matrix](#using-row-operations-to-create-a-column-of-zeros-in-a-3x3-matrix)
- [Using Row Operations to Create a Column of Zeros in a 4x4 Matrix](#using-row-operations-to-create-a-column-of-zeros-in-a-4x4-matrix)
- [Column Operations](#column-operations)
- [Using Column Operations to Create a Row of Zeros in a 3x3 Matrix](#using-column-operations-to-create-a-row-of-zeros-in-a-3x3-matrix)
- [Using Column Operations to Create a Row of Zeros in a 4x4 Matrix](#using-column-operations-to-create-a-row-of-zeros-in-a-4x4-matrix)

## Prerequisites

- [Elementary Row Operations](<2.1.4. Elementary Row Operations.md>)

---

<a id="introduction"></a>
## Introduction

When studying linear algebra, it is common to use elementary row operations to reduce a matrix $A$ to another matrix $B$ such that $B$ contains many zeros.

To illustrate, let's consider the matrix $A$, given by

$$
\begin{bmatrix}1 & 1 & -4 \\ 3 & 2 & -2 \\ -5 & -3 & 6\end{bmatrix}
$$

Consider the entry $a_{11}$ located in the top left-hand corner. We can reduce *all* of the entries *below* it using elementary row operations.

First, we subtract $3$ times the first row from the second row.

$$
A = \mid \begin{bmatrix}1 & 1 & -4 \\ 3 & 2 & -2 \\ -5 & -3 & 6\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} - 3R_{1} \\ ∼\end{vmatrix}\begin{bmatrix}1 & 1 & -4 \\ 0 & -1 & 10 \\ -5 & -3 & 6\end{bmatrix}
$$

Then, we add $5$ times the first row to the third row.

$$
\begin{bmatrix}\begin{bmatrix}1 & 1 & -4 \\ 0 & -1 & 10 \\ -5 & -3 & 6\end{bmatrix} & R_{3}:=R_{3} + 5R_{1} \\ ∼ & \begin{bmatrix}1 & 1 & -4 \\ 0 & -1 & 10 \\ 0 & 2 & -14\end{bmatrix}\end{bmatrix}
$$

And we're done! We have successfully performed elementary row operations to reduce all of the entries below $a_{11}$ to zero.

---

<a id="using-row-operations-to-create-a-column-of-zeros-in-a-3x3-matrix"></a>
## Using Row Operations to Create a Column of Zeros in a 3x3 Matrix

**Example:** $\begin{bmatrix}1 & 4 & -7 \\ 2 & 2 & -8 \\ -3 & -2 & 1\end{bmatrix}$

By applying elementary row operations of the form $R_i:=R_i+kR_1$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary row operations?

**Explanation**

To transform $A$ into $B$, we need to make the entries below

$$
a_{11}={\color{blue}\bbox[lightgray,2pt]{1}}
$$

equal to ${\color{red}0}$.

To do this, we can apply the following elementary row operations:

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 4 & -7 \\ 2 & 2 & -8 \\ -3 & -2 & 1\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} - 2R_{1} \\ ∼\begin{bmatrix}1 & 4 & -7 \\ 0 & -6 & 6 \\ -3 & -2 & 1\end{bmatrix}\end{vmatrix}R_{3}:=R_{3} + 3R_{1} \\
&= ∼\begin{bmatrix}1 & 4 & -7 \\ 0 & -6 & 6 \\ 0 & 10 & -20\end{bmatrix} \\
&= B
\end{aligned}
$$

Therefore, the elementary row operations are

$\qquad$

$$
R_2:=R_2-2R_1\quad
$$

and

$$
\quad R_3:=R_3+3R_1
$$

---

**Question 1**

$A = \begin{bmatrix}-1 & 1 & 6 \\ 2 & -5 & -3 \\ -3 & -6 & -9\end{bmatrix}, B = \begin{bmatrix}3 & -9 & 0 \\ 2 & -5 & -3 \\ -9 & 9 & 0\end{bmatrix}$

By applying elementary row operations of the form $R_{i}:=R_{i} + kR_{2}$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary row operations?

- [ ] A. $R_{1}:=R_{1} + 3R_{2}$ and $R_{3}:=R_{3} - 2R_{2}$
- [ ] B. $R_{1}:=R_{1} + 2R_{2}$ and $R_{3}:=R_{3} + 3R_{2}$
- [ ] C. $R_{1}:=R_{1} + 2R_{2}$ and $R_{3}:=R_{3} - 3R_{2}$
- [ ] D. $R_{1}:=R_{1} - 2R_{2}$ and $R_{3}:=R_{3} + 3R_{2}$
- [ ] E. $R_{1}:=R_{1} - 2R_{2}$ and $R_{3}:=R_{3} - 3R_{2}$

---

**Question 2**

$A = \begin{bmatrix}-3 & 2 & -1 \\ 1 & 2 & 1 \\ 0 & 1 & 2\end{bmatrix}, B = \begin{bmatrix}-3 & 0 & -5 \\ 1 & 0 & -3 \\ 0 & 1 & 2\end{bmatrix}$

By applying elementary row operations of the form $R_{i}:=R_{i} + kR_{3}$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary row operations?

- [ ] A. $R_{1}:=R_{1} + 2R_{3}$ and $R_{2}:=R_{2} + 2R_{3}$
- [ ] B. $R_{1}:=R_{1} - 2R_{3}$ and $R_{2}:=R_{2} - 2R_{3}$
- [ ] C. $R_{1}:=R_{1} - 2R_{3}$ and $R_{2}:=R_{2} + R_{3}$
- [ ] D. $R_{1}:=R_{1} + 2R_{3}$ and $R_{2}:=R_{2} - 2R_{3}$
- [ ] E. $R_{1}:=R_{1} - 2R_{3}$ and $R_{2}:=R_{2} + 2R_{3}$

---

<a id="using-row-operations-to-create-a-column-of-zeros-in-a-4x4-matrix"></a>
## Using Row Operations to Create a Column of Zeros in a 4x4 Matrix

**Example:** $\begin{bmatrix}-1 & 2 & 1 & 5 \\ 1 & 1 & 2 & -1 \\ -1 & 3 & 1 & 1 \\ 0 & -2 & 1 & 5\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary row operations

1. $R_1:= \ldots$
2. $R_3:=R_3-3R_2$
3. $R_4:= \ldots$

each of the form $R_i:=R_i+kR_2$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary row operations?

**Explanation**

To transform $A$ into $B$, we need to make entries above and below

$$
a_{22}={\color{blue}\bbox[lightgray,2pt]{1}}
$$

equal to ${\color{red}0}$.

To do this, we can apply the following elementary row operations:

$$
\begin{aligned}
A &= \begin{bmatrix}-1 & 2 & 1 & 5 \\ 1 & 1 & 2 & -1 \\ -1 & 3 & 1 & 1 \\ 0 & -2 & 1 & 5\end{bmatrix}\begin{vmatrix}R_{1}:=R_{1} - 2R_{2} \\ ∼\begin{bmatrix}-3 & 0 & -3 & 7 \\ 1 & 1 & 2 & -1 \\ -1 & 3 & 1 & 1 \\ 0 & -2 & 1 & 5\end{bmatrix}\end{vmatrix}R_{3}:=R_{3} - 3R_{2} \\
&= ∼\begin{bmatrix}-3 & 0 & -3 & 7 \\ 1 & 1 & 2 & -1 \\ -4 & 0 & -5 & 4 \\ 0 & -2 & 1 & 5\end{bmatrix} \mid R_{4}:=R_{4} + 2R_{2} \\
&= ∼\begin{bmatrix}-3 & 0 & -3 & 7 \\ 1 & 1 & 2 & -1 \\ -4 & 0 & -5 & 4 \\ 2 & 0 & 5 & 3\end{bmatrix} \\
&= B
\end{aligned}
$$

Therefore, the missing elementary row operations are

$\qquad$

$$
R_1:= R_1 - 2R_2\quad
$$

and

$$
\quad R_4:=R_4+2R_2
$$

---

**Question 3**

$A = \begin{bmatrix}-2 & 3 & -4 & 5 \\ 5 & -1 & 3 & 0 \\ 0 & 4 & 7 & 2 \\ 0 & 5 & 4 & -3\end{bmatrix}, B = \begin{bmatrix}13 & 0 & 5 & 5 \\ 5 & -1 & 3 & 0 \\ 20 & 0 & 19 & 2 \\ 25 & 0 & 19 & -3\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary row operations

1. $R_{1}:=…$
2. $R_{3}:=R_{3} + 4R_{2}$
3. $R_{4}:=…$

each of the form $R_{i}:=R_{i} + kR_{2}$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary row operations?

- [ ] A. $R_{1}:=R_{1} - 4R_{2}$ and $R_{4}:=R_{4} - 5R_{2}$
- [ ] B. $R_{1}:=R_{1} - 3R_{2}$ and $R_{4}:=R_{4} - 5R_{2}$
- [ ] C. $R_{1}:=R_{1} + 3R_{2}$ and $R_{4}:=R_{4} + 5R_{2}$
- [ ] D. $R_{1}:=R_{1} + 3R_{2}$ and $R_{4}:=R_{4} + 4R_{2}$
- [ ] E. $R_{1}:=R_{1} + 3R_{2}$ and $R_{4}:=R_{4} - 4R_{2}$

---

**Question 4**

$A = \begin{bmatrix}1 & -3 & -7 & -5 \\ 1 & 10 & 7 & 2 \\ -1 & 0 & 8 & 7 \\ -2 & 1 & 0 & 9\end{bmatrix}, B = \begin{bmatrix}1 & -3 & -7 & -5 \\ 0 & 13 & 14 & 7 \\ 0 & -3 & 1 & 2 \\ 0 & -5 & -14 & -1\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary row operations

1. $R_{2}:=…$
2. $R_{3}:=R_{3} + R_{1}$
3. $R_{4}:=…$

each of the form $R_{i}:=R_{i} + kR_{1}$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary row operations?

- [ ] A. $R_{2}:=R_{2} + R_{1}$ and $R_{4}:=R_{4} - 2R_{1}$
- [ ] B. $R_{2}:=R_{2} - R_{1}$ and $R_{4}:=R_{4} + 2R_{1}$
- [ ] C. $R_{2}:=R_{2} - R_{1}$ and $R_{4}:=R_{4} - 2R_{1}$
- [ ] D. $R_{2}:=R_{2} + R_{1}$ and $R_{4}:=R_{4} + 2R_{1}$
- [ ] E. $R_{2}:=R_{2} - R_{1}$ and $R_{4}:=R_{4} + R_{1}$

---

<a id="column-operations"></a>
## Column Operations

We can do the same trick using the **elementary column operations** instead of row operations.

There are three types of elementary *column* operations, which are entirely analogous to the corresponding row operations:

- Switching any two columns:
$C_i \leftrightarrow C_j$
- Multiplying (each element of) a column by a non-zero number:
$C_i:= k C_i,\qquad k \neq 0$
- Adding a multiple of one column to another column:
$C_j:= C_j+k C_i,\qquad j \neq i$

**Watch out!**

- Unlike elementary row operations, elementary *column* operations change the solutions of a system of linear equations. This is because column operations alter the coefficients associated with each variable across the system, changing the equations.
- The exception is swapping two columns - this doesn't alter the solutions *provided that* we swap the corresponding variables in the solution vector. For example, if we swap the first and second columns, we must treat the second variable as the first and vice versa. The solution set remains unchanged because the relationships between the variables and their coefficients are preserved.

---

<a id="using-column-operations-to-create-a-row-of-zeros-in-a-3x3-matrix"></a>
## Using Column Operations to Create a Row of Zeros in a 3x3 Matrix

**Example:** $\begin{bmatrix}-6 & -5 & 1 \\ 8 & 3 & -4 \\ 9 & 7 & -2\end{bmatrix}$

By applying elementary column operations of the form $C_i:=C_i+kC_3$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary column operations?

**Explanation**

To transform $A$ into $B$, we need to make entries to the left of

$$
a_{13}={\color{blue}\bbox[lightgray,2pt]{1}}
$$

equal to ${\color{red}0}$.

To do this, we can apply the following elementary column operations:

$$
\begin{aligned}
A &= \begin{bmatrix}-6 & -5 & 1 \\ 8 & 3 & -4 \\ 9 & 7 & -2\end{bmatrix}\begin{vmatrix}C_{1}:=C_{1} + 6C_{3} \\ ∼\begin{bmatrix}0 & -5 & 1 \\ -16 & 3 & -4 \\ -3 & 7 & -2\end{bmatrix}\end{vmatrix}C_{2}:=C_{2} + 5C_{3} \\
&= ∼\begin{bmatrix}0 & 0 & 1 \\ -16 & -17 & -4 \\ -3 & -3 & -2\end{bmatrix} \\
&= B
\end{aligned}
$$

Therefore, the elementary column operations are

$\qquad$

$$
C_1:=C_1+6C_3\quad
$$

and

$$
\quad C_2:=C_2+5C_3
$$

---

**Question 5**

$A = \begin{bmatrix}10 & 5 & -5 \\ -1 & 3 & -4 \\ 8 & 8 & 0\end{bmatrix}, B = \begin{bmatrix}0 & 5 & 0 \\ -7 & 3 & -1 \\ -8 & 8 & 8\end{bmatrix}$

By applying elementary column operations of the form $C_{i}:=C_{i} + kC_{2}$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary column operations?

- [ ] A. $C_{1}:=C_{1} - 2C_{2}$ and $C_{3}:=C_{3} + C_{2}$
- [ ] B. $C_{1}:=C_{1} - 2C_{2}$ and $C_{3}:=C_{3} - C_{2}$
- [ ] C. $C_{1}:=C_{1} - C_{2}$ and $C_{3}:=C_{3} + 2C_{2}$
- [ ] D. $C_{1}:=C_{1} + 2C_{2}$ and $C_{3}:=C_{3} + C_{2}$
- [ ] E. $C_{1}:=C_{1} - C_{2}$ and $C_{3}:=C_{3} + C_{2}$

---

**Question 6**

$A = \begin{bmatrix}-1 & 2 & 2 \\ 1 & 3 & 1 \\ 3 & 4 & 1\end{bmatrix}, B = \begin{bmatrix}-7 & -6 & 2 \\ -2 & -1 & 1 \\ 0 & 0 & 1\end{bmatrix}$

By applying elementary column operations of the form $C_{i}:=C_{i} + kC_{3}$, the matrix $A$ can be transformed into the matrix $B$. What are the elementary column operations?

- [ ] A. $C_{1}:=C_{1} - 4C_{3}$ and $C_{2}:=C_{2} - 3C_{3}$
- [ ] B. $C_{1}:=C_{1} - 3C_{3}$ and $C_{2}:=C_{2} + 4C_{3}$
- [ ] C. $C_{1}:=C_{1} - 3C_{3}$ and $C_{2}:=C_{2} + C_{3}$
- [ ] D. $C_{1}:=C_{1} + 4C_{3}$ and $C_{2}:=C_{2} - 3C_{3}$
- [ ] E. $C_{1}:=C_{1} - 3C_{3}$ and $C_{2}:=C_{2} - 4C_{3}$

---

<a id="using-column-operations-to-create-a-row-of-zeros-in-a-4x4-matrix"></a>
## Using Column Operations to Create a Row of Zeros in a 4x4 Matrix

**Example:** $\begin{bmatrix}7 & -3 & -1 & -6 \\ 7 & -2 & -2 & -4 \\ -5 & 1 & 4 & 2 \\ -8 & -3 & -6 & -4\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary column operations

1. $C_1:=\ldots$
2. $C_3:= C_3 - 4C_2$
3. $C_4:= \ldots$

each of the form $C_i:=C_i+kC_2$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary column operations?

**Explanation**

To transform $A$ into $B$, we need to make entries to the left and to the right of

$$
a_{32}={\color{blue}\bbox[lightgray,2pt]{1}}
$$

equal to ${\color{red}0}$.

To do this, we can apply the following elementary column operations:

$$
\begin{aligned}
A &= \begin{bmatrix}7 & -3 & -1 & -6 \\ 7 & -2 & -2 & -4 \\ -5 & 1 & 4 & 2 \\ -8 & -3 & -6 & -4\end{bmatrix}\begin{vmatrix}C_{1}:=C_{1} + 5C_{2} \\ ∼\begin{bmatrix}-8 & -3 & -1 & -6 \\ -3 & -2 & -2 & -4 \\ 0 & 1 & 4 & 2 \\ -23 & -3 & -6 & -4\end{bmatrix}\end{vmatrix}C_{3}:=C_{3} - 4C_{2} \\
&= ∼\begin{bmatrix}-8 & -3 & 11 & -6 \\ -3 & -2 & 6 & -4 \\ 0 & 1 & 0 & 2 \\ -23 & -3 & 6 & -4\end{bmatrix} \mid C_{4}:=C_{4} - 2C_{2} \\
&= ∼\begin{bmatrix}-8 & -3 & 11 & 0 \\ -3 & -2 & 6 & 0 \\ 0 & 1 & 0 & 0 \\ -23 & -3 & 6 & 2\end{bmatrix} \\
&= B
\end{aligned}
$$

Therefore, the corresponding elementary column operations are

$\qquad$

$$
C_1:=C_1+5C_2\quad
$$

and

$$
\quad C_4:=C_4-2C_2
$$

---

**Question 7**

$A = \begin{bmatrix}5 & 2 & -3 & -1 \\ 1 & 4 & 3 & 3 \\ 4 & -2 & -6 & -4 \\ 1 & 0 & 4 & -2\end{bmatrix}, B = \begin{bmatrix}0 & 0 & 0 & -1 \\ 16 & 10 & -6 & 3 \\ -16 & -10 & 6 & -4 \\ -9 & -4 & 10 & -2\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary column operations

1. $C_{1}:=…$
2. $C_{2}:=C_{2} + 2C_{4}$
3. $C_{3}:=…$

each of the form $C_{i}:=C_{i} + kC_{4}$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary column operations?

- [ ] A. $C_{1}:=C_{1} - 5C_{4}$ and $C_{3}:=C_{3} + 3C_{4}$
- [ ] B. $C_{1}:=C_{1} + 5C_{4}$ and $C_{3}:=C_{3} - 2C_{4}$
- [ ] C. $C_{1}:=C_{1} + 5C_{4}$ and $C_{3}:=C_{3} - 3C_{4}$
- [ ] D. $C_{1}:=C_{1} + 3C_{4}$ and $C_{3}:=C_{3} - 5C_{4}$
- [ ] E. $C_{1}:=C_{1} + 5C_{4}$ and $C_{3}:=C_{3} + 3C_{4}$

---

**Question 8**

$A = \begin{bmatrix}5 & 0 & 2 & 7 \\ 1 & -7 & -4 & -6 \\ -1 & 3 & 1 & 2 \\ 2 & 7 & 2 & 3\end{bmatrix}, B = \begin{bmatrix}7 & -6 & 2 & 3 \\ -3 & 5 & -4 & 2 \\ 0 & 0 & 1 & 0 \\ 4 & 1 & 2 & -1\end{bmatrix}$

Consider the matrices $A$ and $B$ above. By applying three matrix elementary column operations

1. $C_{1}:=C_{1} + C_{3}$
2. $C_{2}:=…$
3. $C_{4}:=…$

each of the form $C_{i}:=C_{i} + kC_{3}$, the matrix $A$ can be transformed into the matrix $B$. What are the missing elementary column operations?

- [ ] A. $C_{2}:=C_{2} + 2C_{3}$ and $C_{4}:=C_{4} - C_{3}$
- [ ] B. $C_{2}:=C_{2} - 3C_{3}$ and $C_{4}:=C_{4} - 2C_{3}$
- [ ] C. $C_{2}:=C_{2} - C_{3}$ and $C_{4}:=C_{4} - 2C_{3}$
- [ ] D. $C_{2}:=C_{2} + 3C_{3}$ and $C_{4}:=C_{4} - 2C_{3}$
- [ ] E. $C_{2}:=C_{2} + 3C_{3}$ and $C_{4}:=C_{4} + 2C_{3}$

```update-progress
```

[[MA/Mathematical Methods for the Physical Sciences/PS1/Home|Home]]
[[MA/Mathematical Methods for the Physical Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
