# Solving Inequalities Involving Logarithmic Functions

<!--
lesson-id: 2858
topic-code: MF3.2.1.13
-->

## Table of Contents

- [Introduction](#introduction)
- [The Reason Why We Can Use Each Side as an Exponent](#the-reason-why-we-can-use-each-side-as-an-exponent)
- [Solving an Inequality Involving One Logarithmic Function](#solving-an-inequality-involving-one-logarithmic-function)
- [Inequalities with Two Logarithms](#inequalities-with-two-logarithms)
- [Solving an Inequality Involving Two Logarithmic Functions](#solving-an-inequality-involving-two-logarithmic-functions)

## Prerequisites

- [Compound AND Inequalities](<../../../../AG1/1. Equations & Inequalities/1.3. Linear Inequalities/Lessons/1.3.7. Compound AND Inequalities.md>)
- [Properties of Transformed Logarithmic Functions](<../../../../AG2/4. Exponentials & Logarithms/4.6. Graphs of Logarithmic Functions/Lessons/4.6.3. Properties of Transformed Logarithmic Functions.md>)
- [Solving Logarithmic Equations With Logarithms on Both Sides](<../../../../AG2/4. Exponentials & Logarithms/4.4. Logarithmic Equations/Lessons/4.4.5. Solving Logarithmic Equations With Logarithms on Both Sides.md>)

---

<a id="introduction"></a>
## Introduction

When we have a logarithm in an inequality with base $n > 1$, we can use each side of the inequality as an exponent of $n$.

However, there is a catch: when we solve the resulting inequality, we must remember to remove extraneous solutions. We can do this by keeping only the solutions that are in the domain of the original logarithm.

For example, to solve the inequality

$$
\log_2 (x-1) < 4
$$

we write each side of the inequality as an exponent of $2$, and we get

$$
\begin{bmatrix}2^{\log_{2} (x - 1)} & < 2^{4} \\ x - 1 & < 16 \\ x & < 17\end{bmatrix}
$$

Now, we must only keep the solutions that are in the domain of the logarithm in the original inequality. To find the domain of the logarithm, we recall that the argument of the logarithm must be positive:

$$
x-1 > 0 \quad \Rightarrow \quad x > 1
$$

Our solution is $x < 17$, which can be expressed as $(-\infty, 17)$. But the domain of the logarithm is $x > 1$, which can be expressed as $(1, \infty)$. To find the solutions that are in the domain of the logarithm, we take the intersection:

$$
(-\infty, 17) \cap (1, \infty) = (1, 17)
$$

Therefore, the solution is $x \in (1,17)$, which can also be written as $1 < x < 17$.

---

<a id="the-reason-why-we-can-use-each-side-as-an-exponent"></a>
## The Reason Why We Can Use Each Side as an Exponent

We're allowed to use each side of an inequality as the exponent of a number $n>1$. But why are we allowed to do this?

The reason is that this operation preserves the ordering of numbers. That is to say, if $a < b$ and $n>1$, then $n^a < n^b$.

To see this concretely, consider the following order of numbers:

$$
-2 < -1 < 0 < 0.5 < 1 < 2 < 3
$$

If we raise $2$ to the power of these numbers, the results stay in the same order:

$$
\underbrace{0.25}_{2^{-2}} < \underbrace{0.5}_{2^{-1}} < \underbrace{1}_{2^0} < \underbrace{\,\, 1.41\ldots \,\,}_{2^{0.5}} < \underbrace{2}_{2^1} < \underbrace{4}_{2^2} < \underbrace{8}_{2^3}
$$

---

<a id="solving-an-inequality-involving-one-logarithmic-function"></a>
## Solving an Inequality Involving One Logarithmic Function

**Example:** Solve the inequality $\dfrac{3}{5} \ln(x+2) +1 \leq 0$.

**Explanation**

First, we isolate the logarithm:

$$
\begin{bmatrix}\frac{3}{5}\ln (x + 2) + 1 & \le 0 \\ \frac{3}{5}\ln (x + 2) & \le - 1 \\ \ln (x + 2) & \le - \frac{5}{3}\end{bmatrix}
$$

Now, we use each side of the inequality as an exponent of $e$. We get the following:

$$
\begin{bmatrix}e^{\ln (x + 2)} & \le e^{-5/3} \\ x + 2 & \le e^{-5/3} \\ x & \le e^{-5/3} - 2\end{bmatrix}
$$

Now, we must only keep the solutions that are in the domain of the logarithm in the original inequality. To find the domain of the logarithm, we recall that the argument of the logarithm must be positive:

$$
x+2> 0 \quad \Rightarrow \quad x > -2
$$

Our solution is

$$
x \leq e^{-5/3}-2
$$

which can be expressed as $[MATH: \left(-\infty, e^{-5/3}-2\right].]$ Additionally, the domain of the logarithm is $x > -2$, which can be expressed as $(-2, \infty)$. To find the solutions that are in the domain of the logarithm, we take the intersection:

$[MATH: \left(-\infty, e^{-5/3}-2\right] \cap (-2, \infty) = \left(-2,e^{-5/3}-2 \right]]$

Therefore, the solution is $[MATH: x \in \left(-2, e^{-5/3}-2\right],]$ which can also be written as

$$
-2< x \leq e^{-5/3}-2
$$

---

**Question 1:** Solve the inequality $2\ln (3x) + 4 \ge 0$.

- [ ] A. $x \ge \frac{1}{3e^{2}}$
- [ ] B. $x \le \frac{1}{3e^{2}}$
- [ ] C. $-\frac{1}{3} \le x \le \frac{1}{3}$
- [ ] D. $0 < x \le \frac{1}{3e^{2}}$
- [ ] E. $x \ge \frac{1}{3}$

---

**Question 2:** Solve the inequality $10\log_{2} (x - 6) - 3 < 2$.

- [ ] A. $x > 6$
- [ ] B. $0 < x < 6 + \sqrt{2}$
- [ ] C. $6 < x < 6 + \sqrt{2}$
- [ ] D. $x < 6$
- [ ] E. $0 < x < 6$

---

<a id="inequalities-with-two-logarithms"></a>
## Inequalities with Two Logarithms

When we have two logarithms in an inequality, we can use the same solution procedure as before: we use each side of the inequality as an exponent of the base of the logarithm.

Just remember that we still need to remove extraneous solutions. We can do this by keeping only the solutions that are in the domains of *all* the logarithms in the original inequality.

---

<a id="solving-an-inequality-involving-two-logarithmic-functions"></a>
## Solving an Inequality Involving Two Logarithmic Functions

**Example:** Solve the inequality $\ln(x+2) - 6 < \ln(x-2)$.

**Explanation**

We use each side of the inequality as an exponent of $e$. This gives the following:

$$
\begin{bmatrix}e^{\ln (x + 2) - 6} & < e^{\ln (x - 2)} \\ e^{\ln (x + 2)} \cdot e^{-6} & < e^{\ln (x - 2)} \\ (x + 2)e^{-6} & < x - 2 \\ (x + 2) & < (x - 2)e^{6} \\ (x + 2) & < xe^{6} - 2e^{6} \\ 2 + 2e^{6} & < (e^{6} - 1)x \\ \frac{2 + 2e^{6}}{e^{6} - 1} & < x\end{bmatrix}
$$

Now, we must only keep the solutions that are in the domain of the logarithms in the original inequality. To find the domain of the logarithms, we recall that the argument of the logarithm must be positive:

$$
\begin{bmatrix}x + 2 & > 0 & ⇒ & x & > - 2 \\ x - 2 & > 0 & ⇒ & x & > 2\end{bmatrix}
$$

Our solution is

$$
x > \dfrac{2+2e^6}{e^6-1}
$$

which can be expressed as

$$
\left(\dfrac{2+2e^6}{e^6-1},\infty \right)
$$

Additionally, the domains of the logarithms are $x > -2$ and $x>2$, which can be expressed as $(-2,\infty)$ and $(2,\infty)$ respectively. To find the solutions that are in the domains of the logarithms, we take the intersection:

$$
\left(\dfrac{2+2e^6}{e^6-1},\infty \right) \cap (-2, \infty) \cap (2, \infty) = \left(\dfrac{2+2e^6}{e^6-1}, \infty \right)
$$

Therefore, the solution is

$$
x \in \left(\dfrac{2+2e^6}{e^6-1}, \infty \right)
$$

which can also be written as

$$
x > \dfrac{2+2e^6}{e^6-1}
$$

---

**Question 3:** Solve the inequality $\ln (x + 2) - 5 \ge \ln (3x)$.

- [ ] A. $0 < x \le \frac{2}{3e^{5} - 1}$
- [ ] B. $x \le \frac{2}{3e^{5} - 1}$
- [ ] C. $x \ge \frac{2}{3e^{5} - 1}$
- [ ] D. $-\frac{2}{3e^{5} - 1} \le x \le 0$
- [ ] E. $-\frac{2}{3e^{5} - 1} \le x \le \frac{2}{3e^{5} - 1}$

---

**Question 4:** Solve the inequality $\log_{3} (x + 4) + 2 < \log_{3} (5x)$.

- [ ] A. $-4 < x < 0$
- [ ] B. No solution
- [ ] C. $-9 < x < - 4$
- [ ] D. $x < - 9$
- [ ] E. $-9 < x < 0$

```update-progress
```

[[MA/MF3/Home|Home]]
[[MA/MF3/0. Table of Contents/TOC|Table of Contents]]
