# Right-Angle Rotations as Linear Transformations

<!--
lesson-id: 931
topic-code: MF3.14.5.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Using a Matrix That Represents a Rotation of 180 Degrees](#using-a-matrix-that-represents-a-rotation-of-180-degrees)
- [Rotations of 90 Degrees](#rotations-of-90-degrees)
- [Rotations of 270 Degrees](#rotations-of-270-degrees)
- [Identifying Rotation Matrices](#identifying-rotation-matrices)
- [Using a Matrix That Represents a Rotation of 90 Degrees](#using-a-matrix-that-represents-a-rotation-of-90-degrees)

## Prerequisites

- [Linear Transformations of Objects in the Plane](<14.5.4. Linear Transformations of Objects in the Plane.md>)
- [Rotating Objects in the Coordinate Plane Using Functions](<../../../../GEO/1. Proof, Congruence, & Similarity/1.2. Transformations/Lessons/1.2.3. Rotating Objects in the Coordinate Plane Using Functions.md>)

---

<a id="introduction"></a>
## Introduction

Consider the standard basis $\big\{\mathbf{i},\mathbf{j} \big\}$ of the Euclidean plane $\mathbb{R}^2$ and the unit square spanned by these two vectors.

Let's define a linear transformation $\mathbf R$ by its standard matrix $R$, given by

$$
\begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}
$$

We can find the images of $\{\mathbf{i},\mathbf{j} \}$ under $\mathbf{R}$ by computing the matrix products, as follows:

$$
\begin{aligned}
i^{′} &= R(i) = \begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}\begin{bmatrix}1 \\ 0\end{bmatrix} = \begin{bmatrix}-1 \\ 0\end{bmatrix} =-i \\
j^{′} &= R(j) = \begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}\begin{bmatrix}0 \\ 1\end{bmatrix} = \begin{bmatrix}0 \\ -1\end{bmatrix} =-j
\end{aligned}
$$

Plotting the vectors $\mathbf i$ and $\mathbf j$ and their images $\mathbf i'$ and $\mathbf j'$, we obtain the following:

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/f52e215a09fa52f80cfb007a00356b00.png>)

We see that the transformation $\mathbf R$ represented by the matrix $R$ defines a **counterclockwise rotation of $180^\circ$ (or $\pi$ radians) about the origin.**

Note that we typically refer to rotations in the counterclockwise sense. However, in this case, the transformation $\mathbf R$ can also be considered a rotation of $180^\circ$ (or $\pi$ radians) *clockwise* about the origin.

---

<a id="using-a-matrix-that-represents-a-rotation-of-180-degrees"></a>
## Using a Matrix That Represents a Rotation of 180 Degrees

**Example:** A triangle $S$ has vertices at $(2,-1), (-3,5)$, and $(4,-3)$. Which of the following are coordinates of the vertices of $S'$, the image of $S$ under the counterclockwise rotation $\mathbf{R}_{\pi}$ by an angle of $\pi$ radians about the origin?

1. $(-2,1)$
2. $(-4,3)$
3. $(-5,3)$

**Explanation**

The counterclockwise rotation $\mathbf{R}_{\pi}$ by an angle of $\pi$ radians is given by the matrix

$$
\begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}
$$

To find the image of $S$ under the action of $\mathbf{R}_{\pi}$, we first create a matrix $X$ containing all of the vertices of $S$ as columns:

$$
\begin{aligned}
X &= \begin{bmatrix}2 & -3 & 4 \\ -1 & 5 & -3\end{bmatrix}
\end{aligned}
$$

Now, we compute the image of $X$ under $\mathbf{R}_{\pi}$ by calculating the matrix product $RX$:

$$
\begin{aligned}
RX &= \begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}\begin{bmatrix}2 & -3 & 4 \\ -1 & 5 & -3\end{bmatrix} \\
&= \begin{bmatrix}-2 & 3 & -4 \\ 1 & -5 & 3\end{bmatrix}
\end{aligned}
$$

Therefore, the coordinates of the respective vertices of $S'$ are $(-2,1), (3,-5), (-4,3)$.

So, the correct answer is "I and II only."

---

**Question 1**

A triangle $S$ has vertices at $(-1,-2), (-1, 2), (1, 1)$. Which of the following are coordinates of the vertices of $S^{′}$, the image of $S$ under the **clockwise** rotation $R_{- π}$ by an angle of $π$ radians about the origin?

1. $(1, 2)$
2. $(1,-2)$
3. $(-1,-1)$

- [ ] A. II only
- [ ] B. I and II only
- [ ] C. III only
- [ ] D. I, II, and III
- [ ] E. I and III only

---

**Question 2**

A triangle $S$ has vertices at $(-1, 1), (2, 3), (5, 1)$. Which of the following are coordinates of the vertices of $S^{′}$, the image of $S$ under the counterclockwise rotation $R_{180^{∘}}$ by an angle of $180^{∘}$ about the origin?

1. $(-5,-1)$
2. $(3, 2)$
3. $(1, 1)$

- [ ] A. II only
- [ ] B. II and III only
- [ ] C. III only
- [ ] D. I only
- [ ] E. I and III only

---

<a id="rotations-of-90-degrees"></a>
## Rotations of 90 Degrees

Consider once again the standard basis $\big\{\mathbf{i},\mathbf{j} \big\}$ of the Euclidean plane $\mathbb{R}^2$ and the unit square that is spanned by these vectors.

Let's define a linear transformation $\mathbf R$ by its standard matrix $R$, given by

$$
\begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}
$$

As before, we can find the images of $\{\mathbf{i},\mathbf{j} \}$ under $\mathbf{R}$ as follows:

$$
\begin{aligned}
i^{′} &= R(i) = \begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}\begin{bmatrix}1 \\ 0\end{bmatrix} = \begin{bmatrix}0 \\ 1\end{bmatrix} = j \\
j^{′} &= R(j) = \begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}\begin{bmatrix}0 \\ 1\end{bmatrix} = \begin{bmatrix}-1 \\ 0\end{bmatrix} =-i
\end{aligned}
$$

Plotting the vectors $\mathbf i$ and $\mathbf j$ and their images $\mathbf i'$ and $\mathbf j'$, we obtain the following:

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/80a7f2f6e70a6340e889133836e152ac.png>)

We see that the transformation $\mathbf R$ represented by the matrix $R$ defines a **counterclockwise rotation of $90^\circ$, or $\dfrac \pi 2$ radians, about the origin.**

The transformation $\mathbf R$ can also be considered a rotation of $270^\circ$, or

$$
\dfrac{3\pi}{2}
$$

radians, *clockwise* about the origin.

---

<a id="rotations-of-270-degrees"></a>
## Rotations of 270 Degrees

Finally, let

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

be a matrix of a linear transformation $\mathbf R$. As usual, we can find the images of $\{\mathbf{i},\mathbf{j} \}$ under $\mathbf{R}$ as follows:

$$
\begin{aligned}
i^{′} &= R(i) = \begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}\begin{bmatrix}1 \\ 0\end{bmatrix} = \begin{bmatrix}0 \\ -1\end{bmatrix} =-j \\
j^{′} &= R(j) = \begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}\begin{bmatrix}0 \\ 1\end{bmatrix} = \begin{bmatrix}1 \\ 0\end{bmatrix} = i
\end{aligned}
$$

Plotting the vectors $\mathbf i$ and $\mathbf j$ and their images $\mathbf i'$ and $\mathbf j'$, we obtain the following:

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/86368b6b6d077107e510d6998949e38f.png>)

We see that the transformation $\mathbf R$ represented by the matrix $R$ defines a **counterclockwise rotation of $270^\circ$, or $\dfrac{3\pi}{2}$ radians, about the origin.**

The transformation $\mathbf R$ can also be considered a rotation of $90^\circ$, or

$$
\dfrac \pi 2
$$

radians *clockwise* about the origin.

---

<a id="identifying-rotation-matrices"></a>
## Identifying Rotation Matrices

**Example:** Consider the vector $\mathbf{v}$ shown above. Find the vector $A\mathbf{v}$ given that
$\begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}$

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/38d3b5868bf5043351514f3dff7dd033.png>)

**Explanation**

Consider the standard basis $\{\mathbf{i},\mathbf{j} \}$ of the Euclidean plane $\mathbb{R}^2$ and the unit square spanned by the vectors. Computing the images of $\{\mathbf{i},\mathbf{j} \}$ under $A$, we get the following:

$$
\begin{aligned}
i^{′} &= Ai = \begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}\begin{bmatrix}1 \\ 0\end{bmatrix} = \begin{bmatrix}0 \\ 1\end{bmatrix} = j \\
j^{′} &= Aj = \begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}\begin{bmatrix}0 \\ 1\end{bmatrix} = \begin{bmatrix}-1 \\ 0\end{bmatrix} =-i
\end{aligned}
$$

As a result, we obtain the following diagram:

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/2d169b5fa006402034a1f405a9c1cb45.png>)

So, the matrix represents a rotation of

$$
\dfrac{\pi}{2}
$$

radians counterclockwise about the origin.

Therefore, in order to find $A\mathbf{v}$, we need to rotate $\mathbf{v}$ counterclockwise by an angle of

$$
\dfrac{\pi}{2}
$$

radians, as shown below.

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/12a8c4243623a4dea1baaca392b2af92.png>)

---

**Question 3**

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450.png>)

Consider the vector $v$ shown above. Find the vector $Av$ given that
$A = \begin{bmatrix}0 & -1 \\ 1 & 0\end{bmatrix}$.

- [ ] A. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450-a-3.png>)
- [ ] B. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450-a-2.png>)
- [ ] C. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450-a-5.png>)
- [ ] D. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450-a-1.png>)
- [ ] E. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81450-a-4.png>)

---

**Question 4**

![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452.png>)

Consider the vector $v$ shown above. Find the vector $Av$ given that
$A = \begin{bmatrix}-1 & 0 \\ 0 & -1\end{bmatrix}$.

- [ ] A. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452-a-3.png>)
- [ ] B. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452-a-4.png>)
- [ ] C. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452-a-2.png>)
- [ ] D. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452-a-1.png>)
- [ ] E. ![](<../Source/14.5.7. Right-Angle Rotations as Linear Transformations/Images/q-81452-a-5.png>)

---

<a id="using-a-matrix-that-represents-a-rotation-of-90-degrees"></a>
## Using a Matrix That Represents a Rotation of 90 Degrees

**Example:** A triangle $S$ has vertices at $(-5,-1), (1,4), (3,-5)$. Which of the following are coordinates of the vertices of $S'$, the image of $S$ under the **clockwise** rotation $\mathbf{R}_{-90^\circ}$ by an angle of $90^\circ$ about the origin?

1. $(1,-5)$
2. $(4,-1)$
3. $(-5,-3)$

**Explanation**

The clockwise rotation $\mathbf{R}_{-90^\circ}$ by an angle of $90^\circ$ is the same as counterclockwise rotation by an angle of $270^\circ$. It's given by the matrix

$$
\begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}
$$

To find the image of $S$ under the action of $\mathbf{R}_{-90^\circ}$, we first create a matrix $X$ containing all of the vertices of $S$ as columns:

$$
\begin{aligned}
X &= \begin{bmatrix}-5 & 1 & 3 \\ -1 & 4 & -5\end{bmatrix}
\end{aligned}
$$

Now, we compute the image of $X$ under $\mathbf{R}_{-90^\circ}$ by calculating the matrix product $RX$:

$$
\begin{aligned}
RX &= \begin{bmatrix}0 & 1 \\ -1 & 0\end{bmatrix}\begin{bmatrix}-5 & 1 & 3 \\ -1 & 4 & -5\end{bmatrix} \\
&= \begin{bmatrix}-1 & 4 & -5 \\ 5 & -1 & -3\end{bmatrix}
\end{aligned}
$$

Therefore, the coordinates of the respective vertices of $S'$ are $(-1,5), (4,-1), (-5,-3)$.

So, the correct answer is "II and III only."

---

**Question 5**

A triangle $S$ has vertices at $(-2,-3), (-3, 2), (2, 4)$. Which of the following are coordinates of the vertices of $S^{′}$, the image of $S$ under the **clockwise** rotation $R_{-90^{∘}}$ by an angle of $90^{∘}$ about the origin?

1. $(-3, 2)$
2. $(4,-2)$
3. $(-4, 2)$

- [ ] A. I and II only
- [ ] B. II only
- [ ] C. I and III only
- [ ] D. I, II, and III
- [ ] E. II and III only

---

**Question 6**

A triangle $S$ has vertices at $(-2,-1), (1, 4), (1,-1)$. Which of the following are coordinates of the vertices of $S^{′}$, the image of $S$ under the counterclockwise rotation $R_{π/2}$ by an angle of $\frac{π}{2}$ radians about the origin?

1. $(4,-2)$
2. $(1,-2)$
3. $(1, 1)$

- [ ] A. I only
- [ ] B. III only
- [ ] C. I and II only
- [ ] D. I and III only
- [ ] E. II and III only
