# Euler's Formula

<!--
lesson-id: 898
topic-code: MF3.6.2.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Writing a Complex Number in Exponential Form](#writing-a-complex-number-in-exponential-form)
- [Writing a Complex Number Shown on an Argand Diagram in Exponential Form](#writing-a-complex-number-shown-on-an-argand-diagram-in-exponential-form)
- [Proving the Sum Formulas for Sine and Cosine](#proving-the-sum-formulas-for-sine-and-cosine)

## Prerequisites

- [The Polar Form of a Complex Number](<6.2.1. The Polar Form of a Complex Number.md>)

---

<a id="introduction"></a>
## Introduction

**Euler's formula** allows us to convert between complex exponential functions and complex numbers expressed in polar form. It states that

$$
e^{\textrm{i}\theta} = \cos\theta + \textrm{i}\sin\theta
$$

With Euler's formula, we can write any complex number $z$ in three equivalent ways. Letting

$$
r= \mid z \mid
$$

and

$$
\theta =\textrm{arg}(z)
$$

we have the following:

|  | **Cartesian** | **Polar** | **Exponential** |
| --- | ---: | ---: | ---: |
| $z$ | $x+\textrm{i}y$ | $r\left(\cos\theta + \textrm{i}\sin\theta\right)$ | $re^{\textrm{i}\theta}$ |

For instance, if

$$
z=1+\textrm{i}
$$

then we have

$$
\begin{aligned}
r &= \sqrt{1^{2} + 1^{2}} = \sqrt{2}, \\
θ &= arg(z) = \arctan (\frac{1}{1}) = \frac{π}{4}
\end{aligned}
$$

So, the complex number

$$
z=1+\textrm{i}
$$

can be written in exponential form as

$$
z=\sqrt{2}e^{\textrm{i}\pi/4}
$$

When $\theta=\pi$, we have a special case:

$$
e^{\textrm{i}\pi} = -1 \quad\Longrightarrow\quad e^{\textrm{i}\pi} +1=0
$$

The equation

$$
e^{\textrm{i}\pi} +1=0
$$

is called **Euler's identity**. It is often described as the most beautiful equation in all of mathematics because it combines five important constants $e, \textrm{i}, \pi, 1$, and $0$ into a single elegant form.

---

<a id="writing-a-complex-number-in-exponential-form"></a>
## Writing a Complex Number in Exponential Form

**Example:** Write the complex number $z=4+4\textrm{i}$ in exponential form.

**Explanation**

To write our complex number in the form

$$
z = r\textrm{e}^{\textrm{i}\theta}
$$

where

$$
r= \mid z \mid
$$

and

$$
\theta = \arg(z)
$$

we first calculate $r$:

$$
\begin{aligned}
r = \mid z \mid &= \sqrt{x^{2} + y^{2}} \\
&= \sqrt{4^{2} + 4^{2}} \\
&= 4\sqrt{2}
\end{aligned}
$$

Since $z$ lies in the first quadrant of the complex plane, we have

$$
\begin{aligned}
θ = \arg (z) &= \arctan (\frac{y}{x}) \\
&= \arctan (1) \\
&= \frac{π}{4}
\end{aligned}
$$

Finally, substituting

$$
r=4\sqrt{2}
$$

and

$$
\theta=\dfrac{\pi}{4}
$$

into

$$
z=re^{\textrm{i}\theta}
$$

we get

$$
z = 4\sqrt{2}\,\textrm{e}^{\pi\textrm{i}/4}
$$

---

**Question 1**

> A scientific calculator is required to answer this question.

Write the complex number $z =-1 + 2\textrm{i}$ in the form $re^{\textrm{i}θ}$, giving $θ$ in radians to $3$ decimal places.

- [ ] A. $z = \sqrt{3}e^{π\textrm{i}/3}$
- [ ] B. $z = \sqrt{5}e^{0.034\textrm{i}}$
- [ ] C. $z = \sqrt{5}e^{2.034\textrm{i}}$
- [ ] D. $z = \sqrt{3}e^{2.034\textrm{i}}$
- [ ] E. $z = \sqrt{5}e^{π\textrm{i}/3}$

---

**Question 2**

> A scientific calculator is required to answer this question.

Write the complex number $z =-1 + \textrm{i}$ in exponential form.

- [ ] A. $z = 2e^{5π\textrm{i}/6}$
- [ ] B. $z = \sqrt{2}e^{5π\textrm{i}/6}$
- [ ] C. $z = \sqrt{2}e^{2π\textrm{i}/3}$
- [ ] D. $z = 2e^{2π\textrm{i}/3}$
- [ ] E. $z = \sqrt{2}e^{3π\textrm{i}/4}$

---

<a id="writing-a-complex-number-shown-on-an-argand-diagram-in-exponential-form"></a>
## Writing a Complex Number Shown on an Argand Diagram in Exponential Form

**Example:** Write the complex number $z$, shown in the following Argand diagram, in exponential form.

![](<../Source/6.2.3. Euler's Formula/Images/a0fe46e109e3784d66675d033d5fdbd8.png>)

**Explanation**

The complex number shown in the Argand diagram can be written as

$$
\begin{aligned}
z &= x + \textrm{i}y \\
&=-3 + 2\textrm{i}
\end{aligned}
$$

To write it in the form

$$
z = r\textrm{e}^{\textrm{i}\theta}
$$

where

$$
r= \mid z \mid
$$

and

$$
\theta = \arg(z)
$$

we first calculate $r$:

$$
\begin{aligned}
r = \mid z \mid &= \sqrt{x^{2} + y^{2}} \\
&= \sqrt{(-3)^{2} + 2^{2}} \\
&= \sqrt{13}
\end{aligned}
$$

Now, since $z$ lies in the second quadrant, we can find the angle $\theta$ as

$$
\begin{aligned}
\arg (z) &= π - \mid \arctan (\frac{y}{x}) \mid \\
&= π - \mid \arctan (\frac{2}{3}) \mid \\
&= π - 0.588 \\
&= 2.554
\end{aligned}
$$

to three decimal places.

Finally, substituting

$$
r=\sqrt{13}
$$

and

$$
\theta=2.554
$$

into

$$
z=re^{\textrm{i} \theta}
$$

we get

$$
z = \sqrt{13}\,\textrm{e}^{2.554\,\textrm{i}}\,
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

![](<../Source/6.2.3. Euler's Formula/Images/q-17517.png>)

Write the complex number $z$, shown on the Argand diagram above, in the form $re^{\textrm{i}θ}$, giving $θ$ in radians to three decimal places.

- [ ] A. $z = 5e^{2π\textrm{i}/3}$
- [ ] B. $z = 5e^{0.464\textrm{i}}$
- [ ] C. $z = 5e^{0.644\textrm{i}}$
- [ ] D. $z = \sqrt{5}e^{0.644\textrm{i}}$
- [ ] E. $z = \sqrt{5}e^{1.644\textrm{i}}$

---

**Question 4**

> A scientific calculator is required to answer this question.

![](<../Source/6.2.3. Euler's Formula/Images/q-71978.png>)

Write the complex number $z$, shown on the Argand diagram above, in exponential form.

- [ ] A. $z = 2\sqrt{3}e^{π\textrm{i}/3}$
- [ ] B. $z = 2e^{5π\textrm{i}/6}$
- [ ] C. $z = 3e^{3π\textrm{i}/4}$
- [ ] D. $z = 2e^{2π\textrm{i}/3}$
- [ ] E. $z = 3\sqrt{2}e^{π\textrm{i}/6}$

---

<a id="proving-the-sum-formulas-for-sine-and-cosine"></a>
## Proving the Sum Formulas for Sine and Cosine

We can use Euler's formula to construct straightforward proofs of some trigonometric identities.

For example, let's prove the following:

$$
\cos(x+y) = \cos x\cos y - \sin x\sin y, \qquad \sin(x+y) = \sin x\cos y + \cos x\sin y
$$

By the rules of exponents, we have that

$$
e^{\textrm i (x+y)} = e^{\textrm i x} \cdot e^{\textrm iy}
$$

Using Euler's formula, this can be written as

$$
\underbrace{\cos(x+y) + \textrm i\sin(x+y)}_{e^{\textrm i (x+y)}} = \underbrace{\left(\cos x + \textrm i\sin x\right)}_{e^{\textrm i x}}\cdot \underbrace{\left(\cos y + \textrm i\sin y\right)}_{e^{\textrm iy}}
$$

If we expand the parentheses on the right-hand side and group real and imaginary parts, we get

$$
\begin{aligned}
\cos (x + y) + \textrm{i}\sin (x + y) &= (\cos x + \textrm{i}\sin x) \cdot (\cos y + \textrm{i}\sin y) \\
&= \cos x\cos y + \textrm{i}\cos x\sin y + \textrm{i}\sin x\cos y + \textrm{i}^{2}\sin x\sin y \\
&= \cos x\cos y + \textrm{i}\cos x\sin y + \textrm{i}\sin x\cos y - \sin x\sin y \\
&= \cos x\cos y - \sin x\sin y + \textrm{i}(\cos x\sin y + \sin x\cos y)
\end{aligned}
$$

So, we have

$$
\cos(x+y) + \textrm i\sin(x+y) = \cos x\cos y - \sin x\sin y + \textrm i\left(\cos x\sin y + \sin x\cos y\right)
$$

Equating the real parts of this equation gives

$$
\cos(x+y) = \cos x\cos y - \sin x\sin y
$$

and equating the imaginary parts gives

$$
\sin(x+y) = \sin x\cos y + \cos x\sin y
$$

as required.
