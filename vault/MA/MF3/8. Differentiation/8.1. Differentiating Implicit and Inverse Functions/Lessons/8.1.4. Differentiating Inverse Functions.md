# Differentiating Inverse Functions

<!--
lesson-id: 1785
topic-code: MF3.8.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating the Derivative of the Inverse of a Polynomial Function](#calculating-the-derivative-of-the-inverse-of-a-polynomial-function)
- [Calculating the Derivative of the Inverse of a Composite Function](#calculating-the-derivative-of-the-inverse-of-a-composite-function)

## Prerequisites

- [The Chain Rule With Trigonometric Functions](<../../../../CAB/3. Differentiation- Composite, Implicit, and Inverse Functions/3.1. Differentiating Composite Functions/Lessons/3.1.4. The Chain Rule With Trigonometric Functions.md>)
- [Calculating the Inverse of a Function](<../../../../AG2/3. Functions/3.1. Functions/Lessons/3.1.6. Calculating the Inverse of a Function.md>)
- [The Chain Rule With Exponential Functions](<../../../../CAB/3. Differentiation- Composite, Implicit, and Inverse Functions/3.1. Differentiating Composite Functions/Lessons/3.1.2. The Chain Rule With Exponential Functions.md>)
- [The Chain Rule With Logarithmic Functions](<../../../../CAB/3. Differentiation- Composite, Implicit, and Inverse Functions/3.1. Differentiating Composite Functions/Lessons/3.1.3. The Chain Rule With Logarithmic Functions.md>)
- [Calculating dy/dx Using dx/dy](<8.1.3. Calculating dy-dx Using dx-dy.md>)

---

<a id="introduction"></a>
## Introduction

Suppose that we have a function

$$
f(x) = x^2+2x
$$

and we want to find the derivative of the *inverse* function $(f^{-1})'(x)$. Can we do this without explicitly computing the inverse?

Indeed, we can! The trick is to write the inverse function *implicitly*, differentiate it, and then write the result in terms of $f^{-1}(x)$.

**Step 1:** First, we write the inverse function implicitly. The given function is

$$
y = x^2+2x
$$

and to write the inverse function implicitly, we swap $x$ and $y$ as follows:

$$
x=y^2+2y
$$

Note that in the equation above, we have

$$
y=f^{-1}(x)
$$

Also, remember that swapping $x$ and $y$ gives us the inverse function because the inverse function $f^{-1}(x)$ can be obtained by reflecting the graph of $f(x)$ over the line $y=x$.

![](<../Source/8.1.4. Differentiating Inverse Functions/Images/31732ab3274df93c172966e9e9e99ab6.png>)

**Step 2:** Next, we differentiate. The overall goal is to find

$$
\dfrac{\textrm dy}{\textrm dx}
$$

but the equation is already solved for $x$, so we start by computing

$$
\dfrac{\textrm dx}{\textrm dy}
$$

$$
\begin{aligned}
x &= y^{2} + 2y \\
\frac{dx}{dy} &= \frac{d}{dy}(y^{2} + 2y) \\
\frac{dx}{dy} &= 2y + 2
\end{aligned}
$$

To find

$$
\dfrac{\textrm{d}y}{\textrm{d}x}
$$

we take the reciprocal of

$$
\dfrac{\textrm{d}x}{\textrm{d}y}
$$

$$
\dfrac{\textrm{d}x}{\textrm{d}y} = 2y + 2 \quad \Rightarrow \quad \dfrac{\textrm{d}y}{\textrm{d}x}=\dfrac{1}{2y+2}
$$

**Step 3:** Lastly, we write the result in terms of $f^{-1}(x)$. Since

$$
y=f^{-1}(x)
$$

we have

$$
\begin{aligned}
\frac{dy}{dx} &= \frac{1}{2y + 2} \\
\frac{d}{dx}[f^{-1}(x)] &= (1)/(2f^{-1}(x) + 2) \\
(f^{-1})^{′}(x) &= (1)/(2f^{-1}(x) + 2)
\end{aligned}
$$

---

<a id="calculating-the-derivative-of-the-inverse-of-a-polynomial-function"></a>
## Calculating the Derivative of the Inverse of a Polynomial Function

**Example:** If $f(x)=3x^2+9$, find $(f^{-1})'(x)$ as a function of $f^{-1}(x)$.

**Explanation**

**Step 1:** First, we write the inverse function implicitly. The given function is

$$
y = 3x^2+9
$$

and to write the inverse function implicitly, we swap $x$ and $y$ as follows:

$$
x=3y^2+9
$$

Note that in the equation above, we have

$$
y=f^{-1}(x)
$$

**Step 2:** Next, we differentiate. The overall goal is to find

$$
\dfrac{\textrm dy}{\textrm dx}
$$

but the equation is already solved for $x$, so we compute

$$
\dfrac{\textrm dx}{\textrm dy}
$$

and then take the reciprocal:

$$
\begin{aligned}
\frac{dx}{dy} &= \frac{d}{dy}(3y^{2} + 9) \\
\frac{dx}{dy} &= 6y \\
\frac{dy}{dx} &= \frac{1}{6y}
\end{aligned}
$$

**Step 3:** Lastly, we write the result in terms of $f^{-1}(x)$. Since

$$
y=f^{-1}(x)
$$

we have

$$
\begin{aligned}
\frac{d}{dx}[f^{-1}(x)] &= (1)/(6f^{-1}(x)) \\
(f^{-1})^{′}(x) &= (1)/(6f^{-1}(x))
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-3303
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Find $(f^{-1})^{′}(x)$ as a function of $f^{-1}(x)$, if $f(x) = 6x^{3} + 5x - 11$.
options:
- id: a
  content: |-
    $(1)/(6(f^{-1}(x))^{3} + 5f^{-1}(x) - 11)$
- id: b
  content: |-
    $(1)/(18f^{-1}(x) + 5)$
- id: c
  content: |-
    $(1)/(18(f^{-1}(x))^{2} + 5)$
- id: d
  content: |-
    $(2)/(18(f^{-1}(x))^{2})$
- id: e
  content: |-
    $18(f^{-1}(x))^{2} + 5$
```

---

**Question 2:**

```quiz
type: radio
id: ma-32629
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If $g(x) = 2x^{2} + 6x - 2$, find $(g^{-1})^{′}(x)$ as a function of $g^{-1}(x)$.
options:
- id: a
  content: |-
    $(1)/(2g^{-1}(x) + 6)$
- id: b
  content: |-
    $4g^{-1}(x) + 6$
- id: c
  content: |-
    $(1)/(2(g^{-1}(x)) + 6g^{-1}(x) - 2)$
- id: d
  content: |-
    $- (1)/(4g^{-1}(x) + 6)$
- id: e
  content: |-
    $(1)/(4g^{-1}(x) + 6)$
```

---

<a id="calculating-the-derivative-of-the-inverse-of-a-composite-function"></a>
## Calculating the Derivative of the Inverse of a Composite Function

**Example:** Find $\left(f^{-1}\right)'(x)$ as a function of $f^{-1}(x)$ if $f(x) = \cos \left(x^2\right) +6$.

**Explanation**

**Step 1:** First, we write the inverse function implicitly. The given function is

$$
y = \cos \left(x^2\right) +6
$$

and to write the inverse function implicitly, we swap $x$ and $y$ as follows:

$$
x = \cos \left(y^2\right) + 6
$$

Note that in the equation above, we have

$$
y=f^{-1}(x)
$$

**Step 2:** Next, we differentiate. The overall goal is to find

$$
\dfrac{\textrm dy}{\textrm dx}
$$

but the equation is already solved for $x$, so we compute

$$
\dfrac{\textrm dx}{\textrm dy}
$$

and then take the reciprocal:

$$
\begin{aligned}
\frac{dx}{dy} &= \frac{d}{dy}[\cos (y^{2}) + 6] \\
\frac{dx}{dy} &= -2y\sin (y^{2}) \\
\frac{dy}{dx} &= - (1)/(2y\sin (y^{2})) \\
\frac{dy}{dx} &= -\frac{1}{2y}\csc (y^{2})
\end{aligned}
$$

**Step 3:** Lastly, we write the result in terms of $f^{-1}(x)$. Since

$$
y=f^{-1}(x)
$$

we have

$$
\begin{aligned}
\frac{d}{dx}[f^{-1}(x)] &= - (1)/(2(f^{-1}(x)))\csc ((f^{-1}(x))^{2}) \\
(f^{-1})^{′}(x) &= - (1)/(2(f^{-1}(x)))\csc ((f^{-1}(x))^{2})
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-32637
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If $h(x) = 4\cos^{2} (3x) + \cos x$, find $(h^{-1})^{′}(x)$ as a function of $h^{-1}(x)$.
options:
- id: a
  content: |-
    $- (1)/(12\sin (6h^{-1}(x)) + \sin (h^{-1}(x)))$
- id: b
  content: |-
    $- (1)/(48\sin (3h^{-1}(x)) + \sin (h^{-1}(x)))$
- id: c
  content: |-
    $- (1)/(12\sin (h^{-1}(x)))$
- id: d
  content: |-
    $(1)/(24\sin (6) + \sin (h^{-1}(x)))$
- id: e
  content: |-
    $- (1)/(12\sin (3h^{-1}(x)) + \sin (h^{-1}(x)))$
```

---

**Question 4:**

```quiz
type: radio
id: ma-32586
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  If $h(x) = \sqrt{2x + 3}$, find $(h^{-1})^{′}(x)$ as a function of $h^{-1}(x)$.
options:
- id: a
  content: |-
    $2\sqrt{h^{-1}(x)} + 3$
- id: b
  content: |-
    $2h^{-1}(x) + 3$
- id: c
  content: |-
    $(1)/(\sqrt{2h^{-1}(x) + 3})$
- id: d
  content: |-
    $(1)/(2\sqrt{2h^{-1}(x) + 3})$
- id: e
  content: |-
    $\sqrt{2h^{-1}(x) + 3}$
```
