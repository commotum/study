# Combining the Laws of Logarithms

<!--
lesson-id: 30
topic-code: MF2.5.2.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Rewriting an Expression Using the Product and Quotient Rules of Logarithms](#rewriting-an-expression-using-the-product-and-quotient-rules-of-logarithms)
- [Rewriting an Expression Using the Product and Power Rules of Logarithms](#rewriting-an-expression-using-the-product-and-power-rules-of-logarithms)
- [Rewriting an Expression Using the Quotient and Power Rules of Logarithms](#rewriting-an-expression-using-the-quotient-and-power-rules-of-logarithms)
- [Rewriting an Expression Using the Product, Quotient, and Power Rules of Logarithms](#rewriting-an-expression-using-the-product-quotient-and-power-rules-of-logarithms)

## Prerequisites

- [The Quotient Rule for Logarithms](<5.2.2. The Quotient Rule for Logarithms.md>)
- [The Power Rule for Logarithms](<5.2.3. The Power Rule for Logarithms.md>)

---

<a id="introduction"></a>
## Introduction

Recall that logarithms that share a common base $n$ obey the following three laws:

- **The Product Rule**
$\log_n(a) + \log_n(b) = \log_n(ab)$
- **The Quotient Rule**
$\log_n(a) - \log_n(b) = \log_n\left(\dfrac{a}{b} \right)$
- **The Power Rule**
$\log_n(c^m)= m\log_n\left(c \right)$

We can use these laws together to simplify a logarithmic expression.

For instance, we can write

$$
3\log_2(x) + \dfrac 1 2 \log_2(y)
$$

as an expression containing a single term.

First, using the power rule, we can write

$$
\eqalign {3\log_2(x) + \dfrac 1 2 \log_2(y) &= \log_2\left(x^3\right) + \log_2\left(y^{1/ 2}\right)\ &= \log_2\left(x^3\right) + \log_2 \left(\sqrt{y}\right).}
$$

Now, using the product rule, we get

$$
\log_2\left(x^3\right) + \log_2 \left(\sqrt{y}\right) = \log_2 \left(x^3\sqrt{y}\right)
$$

Therefore,

$$
3\log_2(x) + \dfrac 1 2 \log_2(y) = \log_2 \left(x^3\sqrt{y}\right)
$$

---

<a id="rewriting-an-expression-using-the-product-and-quotient-rules-of-logarithms"></a>
## Rewriting an Expression Using the Product and Quotient Rules of Logarithms

**Example:** Express $\ln(2)+\ln(x)-\ln(y)$ as a single logarithm.

**Explanation**

Using the product rule for natural logarithms, we can combine the first two terms of the given expression as follows:

$$
\ln (2) + \ln (x) - \ln (y) = \ln (2x) - \ln (y)
$$

Now, using the quotient rule for natural logarithms, we get

$$
\ln(2x)-\ln(y) = \ln \left(\dfrac{2x}{y} \right)
$$

Therefore,

$$
\ln(2)+\ln(x)-\ln(y) = \ln\left(\dfrac{2x}{y}\right)
$$

---

**Question 1:**

```quiz
type: radio
id: ma-47203
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $\log_{3} (\frac{9x}{2})$?
options:
- id: a
  content: |-
    $4 + 3\log_{3} (x) - 2\log_{3} (2)$
- id: b
  content: |-
    $\log_{3} (x) - 2\log_{3} (2)$
- id: c
  content: |-
    $3 + 2\log_{3} (x) - \log_{3} (2)$
- id: d
  content: |-
    $2 + \log_{3} (x) - \log_{3} (2)$
- id: e
  content: |-
    $9 + \log_{3} (x) - \log_{3} (2)$
```

---

**Question 2:**

```quiz
type: radio
id: ma-47200
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Express $\log (3) + \log (y) - \log (z)$ as a single logarithm.
options:
- id: a
  content: |-
    $\log (\frac{y}{3z})$
- id: b
  content: |-
    $\log (\frac{3z}{y})$
- id: c
  content: |-
    $\log (\frac{3y}{z})$
- id: d
  content: |-
    $\log (\frac{z}{3y})$
- id: e
  content: |-
    $\log (\frac{y}{z})$
```

---

<a id="rewriting-an-expression-using-the-product-and-power-rules-of-logarithms"></a>
## Rewriting an Expression Using the Product and Power Rules of Logarithms

**Example:** Express $\log(3)+4\log(y)$ as a single logarithm.

**Explanation**

Using the power rule for logarithms, we can rewrite the second term to obtain

$$
\log(3)+4\log(y)=\log(3)+\log(y^4)
$$

Now, using the product rule for logarithms, we can combine the two terms to get

$$
\log(3)+\log(y^4)=\log\left(3y^4\right)
$$

Therefore,

$$
\log(3)+4\log(y)=\log\left(3y^4\right)
$$

---

**Question 3:**

```quiz
type: radio
id: ma-47194
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $\log (5p^{3})$?
options:
- id: a
  content: |-
    $\log (5 + p^{3})$
- id: b
  content: |-
    $\log (5 + 3p)$
- id: c
  content: |-
    $\log (5) + 3\log (p)$
- id: d
  content: |-
    $\log (5) + p\log (3)$
- id: e
  content: |-
    $3\log (5)\log (p)$
```

---

**Question 4:**

```quiz
type: radio
id: ma-66907
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Express $3\log_{2} (x) + 2\log_{2} (y)$ as a single logarithm.
options:
- id: a
  content: |-
    $\log_{2} (x^{3} + y^{2})$
- id: b
  content: |-
    $5\log_{2} (xy)$
- id: c
  content: |-
    $\log_{2} (x^{3}y^{2})$
- id: d
  content: |-
    $\log_{2} (3x + 2y)$
- id: e
  content: |-
    $\log_{2} (6xy)$
```

---

<a id="rewriting-an-expression-using-the-quotient-and-power-rules-of-logarithms"></a>
## Rewriting an Expression Using the Quotient and Power Rules of Logarithms

**Example:** Express $3\log(x) - 2\log(5)$ as a single logarithm.

**Explanation**

Using the power rule for logarithms, we can rewrite both terms of the expression as follows:

$$
\begin{aligned}
3\log (x) - 2\log (5) &= \log (x^{3}) - \log (5^{2}) \\
&= \log (x^{3}) - \log (25)
\end{aligned}
$$

Now, using the quotient rule for logarithms, we can combine the two terms to get

$$
\log\left(x^3\right) - \log(25) =\log\left(\dfrac{x^3}{25} \right)
$$

Therefore,

$$
3\log(x) - 2\log(5) = \log\left(\dfrac{x^3}{25} \right)
$$

---

**Question 5:**

```quiz
type: radio
id: ma-1100
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $\log_{3} (\frac{x^{2}}{y})$?
options:
- id: a
  content: |-
    $2\log_{3} (x - y)$
- id: b
  content: |-
    $2\log_{3} (x) - \log_{3} (y)$
- id: c
  content: |-
    $\log_{3} (x) - \log_{3} (y)$
- id: d
  content: |-
    $\log_{3} (x - y)$
- id: e
  content: |-
    $\log_{3} (x) + \log_{3} (y)$
```

---

**Question 6:**

```quiz
type: radio
id: ma-1109
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Express $2\log_{2} (x) - 3\log_{2} (y)$ as a single logarithm.
options:
- id: a
  content: |-
    $\log_{2} (\frac{2x}{3y})$
- id: b
  content: |-
    $3\log_{2} (x) - 2\log_{2} (y)$
- id: c
  content: |-
    $\log_{2} (\frac{x^{2}}{y^{3}})$
- id: d
  content: |-
    $\log_{2} (\frac{3y}{2x})$
- id: e
  content: |-
    $\log_{2} (\frac{y^{3}}{x^{2}})$
```

---

<a id="rewriting-an-expression-using-the-product-quotient-and-power-rules-of-logarithms"></a>
## Rewriting an Expression Using the Product, Quotient, and Power Rules of Logarithms

**Example:** Express $3\log(2)+\log(x)-\log(4)$ as a single logarithm.

**Explanation**

Using the power rule for logarithms, we can rewrite the first term of the given expression as follows:

$$
\begin{aligned}
3\log (2) + \log (x) - \log (4) &= \log (2^{3}) + \log (x) - \log (4) \\
&= \log (8) + \log (x) - \log (4)
\end{aligned}
$$

Now, using the product rule for logarithms, we can combine the first two terms to obtain

$$
\log (8) + \log (x) - \log (4) = \log (8x) - \log (4)
$$

Finally, using the quotient rule for logarithms, we get

$$
\begin{aligned}
\log (8x) - \log (4) &= \log (\frac{8x}{4}) \\
&= \log (2x)
\end{aligned}
$$

Therefore,

$$
3\log(2)+\log(x)-\log(4) = \log\left(2x\right)
$$

---

**Question 7:**

```quiz
type: radio
id: ma-1102
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  What is $\log (\frac{xy^{3}}{z})$?
options:
- id: a
  content: |-
    $\log (x) - 3\log (z) - \log (y)$
- id: b
  content: |-
    $\log (x) + \log (z) - 3\log (y)$
- id: c
  content: |-
    $\log (x) + 3\log (y) - \log (z)$
- id: d
  content: |-
    $3\log (x) - \log (y) - \log (z)$
- id: e
  content: |-
    $3\log (x) + \log (y) - \log (z)$
```

---

**Question 8:**

```quiz
type: radio
id: ma-1111
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Express $3\ln (x) + 2\ln (y) - \frac{1}{2}\ln (z)$ as a single logarithm.
options:
- id: a
  content: |-
    $\ln (\frac{x^{3}}{\sqrt{y^{2}z}})$
- id: b
  content: |-
    $3\ln (\frac{xy^{2}}{\sqrt{z}})$
- id: c
  content: |-
    $\ln (\frac{x^{3}y^{2}}{\sqrt{z}})$
- id: d
  content: |-
    $\ln (\frac{6xy}{\sqrt{z}})$
- id: e
  content: |-
    $xy^{2} - \sqrt{z}$
```
