# The Factor Theorem

<!--
lesson-id: 846
topic-code: MF2.2.4.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Connection With Polynomial Division](#connection-with-polynomial-division)
- [Applying the Factor Theorem](#applying-the-factor-theorem)
- [The Factor Theorem for General Linear Factors](#the-factor-theorem-for-general-linear-factors)
- [Applying the Factor Theorem to Factors With Leading Coefficients](#applying-the-factor-theorem-to-factors-with-leading-coefficients)

## Prerequisites

- [Determining the Roots of Polynomials](<../../2.1. Polynomials/Lessons/2.1.4. Determining the Roots of Polynomials.md>)
- [Dividing Polynomials Using Synthetic Division](<../../2.3. Dividing Polynomials/Lessons/2.3.1. Dividing Polynomials Using Synthetic Division.md>)
- [Solving Systems of Linear Equations Using Elimination: Two Transformations](<../../../../AG1/2. Two-Variable Equations & Inequalities/2.3. Systems of Equations/Lessons/2.3.3. Solving Systems of Linear Equations Using Elimination- Two Transformations.md>)

---

<a id="introduction"></a>
## Introduction

If we know a *root* of a polynomial, we can immediately determine a *factor* of the polynomial using the **factor theorem**.

The factor theorem states that if $p(x)$ is a polynomial and

$$
x={\color{blue}{r}}
$$

is a *root* of $p(x)$, meaning that

$$
p({\color{blue}{r}})=0
$$

then $(x-{\color{blue}{r}})$ is a *factor* of $p(x)$.

The converse of the factor theorem is also true. In other words, if $(x-{\color{blue}{r}})$ is a factor of $p(x)$, then

$$
x={\color{blue}{r}}
$$

is a root of the polynomial.

For example, consider the polynomial $p(x)$, defined as

$$
p(x)=x^3-2x^2-x+2
$$

Note that

$$
x = {\color{blue}2}
$$

is a root of this polynomial:

$$
\begin{aligned}
p(2) &= 2^{3} - 2(2)^{2} - 2 + 2 \\
&= 8 - 8 - 2 + 2 \\
&= 0
\end{aligned}
$$

Therefore, according to the factor theorem, $(x-{\color{blue}2})$ is a *factor* of the polynomial. Indeed, it can be shown that this polynomial can be factored as follows:

$$
\begin{aligned}
x^{3} - 2x^{2} - x + 2 &= (x - 2)(x^{2} - 1)
\end{aligned}
$$

We'll learn more about factoring polynomials using the factor theorem in future lessons. But for now, we can check that this is correct by multiplying the parentheses.

---

<a id="connection-with-polynomial-division"></a>
## Connection With Polynomial Division

So, we know that the polynomial

$$
p(x) = x^3-2x^2-x+2
$$

can be factored as follows:

$$
\begin{aligned}
x^{3} - 2x^{2} - x + 2 &= (x - 2)(x^{2} - 1)
\end{aligned}
$$

By dividing both sides by $(x-2)$, we can rewrite this equation as

$$
\dfrac{x^3-2x^2-x+2}{x-2} = x^2-1
$$

Since the result of dividing the polynomial $p(x)$ by the factor $({x-{\color{black}2}})$ gives *another* polynomial, namely $x^2-1$, we say that $({x-{\color{black}2}})$ divides $p(x)$ *with no remainder*.

We can confirm that we get a remainder of zero when $p(x)$ is divided by $({x-{\color{blue}2}})$ using synthetic division:

|  | $x^3$ | $x^2$ | $x^1$ | $x^0$ |
| --- | ---: | ---: | ---: | ---: |
| $\color{blue}2$ | $1$ | $-2$ | $-1$ | $2$ |
|  | $\color{gray}\downarrow$ | $2$ | $0$ | $-2$ |
|  | $1$ | $0$ | $-1$ | $0$ |

---

<a id="applying-the-factor-theorem"></a>
## Applying the Factor Theorem

**Example:** Determine if $x+3$ is a factor of $p(x)=x^3+3x^2-3x-9$.

**Explanation**

According to the factor theorem, $(x-r)$ is a factor of $p(x)$ if the root $x = r$ is also a root of $p(x)$.

Here, we are considering $(x+3)$. The root of this expression can be found by setting the expression equal to $0$ and solving:

$$
\begin{aligned}
x + 3 &= 0 \\
x &= -3
\end{aligned}
$$

According to the factor theorem, then, $(x+3)$ is a factor of $p(x)$ if the root $x = -3$ is a root of $p(x)$.

So, we just need to check if $x=-3$ is a root of $p(x)$. In other words, we need to check if $p(-3)=0$:

$$
\begin{aligned}
p(-3) &= (-3)^{3} + 3(-3)^{2} - 3(-3) - 9 \\
&=-27 + 27 + 9 - 9 \\
&= 0
\end{aligned}
$$

Indeed, $p(-3)=0$, so $x = -3$ is a root of the polynomial. Consequently, by the factor theorem, we conclude that $(x+3)$ is indeed a factor of $p(x)$.

---

**Question 1**

Which of the following statements are true regarding the polynomial $f(x) = x^{3} - 1$?

1. $x = 1$ is a root of $f(x)$
2. $(x - 1)$ is a factor of $f(x)$
3. $(x - 1)$ divides $f(x)$ with zero remainder

- [ ] A. II only
- [ ] B. I and II only
- [ ] C. III only
- [ ] D. I and III only
- [ ] E. I, II and III

---

**Question 2**

For which of the following polynomials is $(x - 1)$ a factor?

1. $f(x) = x^{3} - x$
2. $g(x) = x^{3} + x$
3. $h(x) = 2x^{3} - x^{2} - x$

- [ ] A. None
- [ ] B. II only
- [ ] C. III only
- [ ] D. I and II only
- [ ] E. I and III only

---

<a id="the-factor-theorem-for-general-linear-factors"></a>
## The Factor Theorem for General Linear Factors

We can also apply the factor theorem to factors of the form $(ax-b)$.

For example, suppose that $(2x-3)$ is a factor of a polynomial $p(x)$. Then, we can immediately obtain a root of $p(x)$ by setting the factor equal to $0$ and solving for $x$, as follows:

$$
\begin{aligned}
2x - 3 &= 0 \\
2x &= 3 \\
x &= \frac{3}{2}
\end{aligned}
$$

Therefore, by the factor theorem, it follows that

$$
p\left(\dfrac32\right) = 0
$$

We can summarize this more general form of the factor theorem as follows:

- If $({\color{red}a}x-{\color{blue}b})$ is a factor of a polynomial $p(x)$, then $x=\dfrac{{\color{blue}b}}{{\color{red}a}}$ is a root of $p(x)$.
- Conversely, if $x=\dfrac{{\color{blue}b}}{{\color{red}a}}$ is a root of $p(x)$, then $({\color{red}a}x-{\color{blue}b})$ is a factor of $p(x)$.

---

<a id="applying-the-factor-theorem-to-factors-with-leading-coefficients"></a>
## Applying the Factor Theorem to Factors With Leading Coefficients

**Example:** Determine if $(4-3x)$ is a factor of $p(x)=-6x^2+11x-4$.

**Explanation**

According to the factor theorem, $(ax-b)$ is a factor of $p(x)$ if the root

$$
x = \dfrac{b}{a}
$$

is also a root of $p(x)$.

Here, we are considering $(4-3x)$. The root of this expression can be found by setting the expression equal to $0$ and solving:

$$
\begin{aligned}
4 - 3x &= 0 \\
4 &= 3x \\
x &= \frac{4}{3}
\end{aligned}
$$

According to the factor theorem, then, $(4-3x)$ is a factor of $p(x)$ if the root

$$
x=\dfrac{4}{3}
$$

is also a root of $p(x)$.

So, we just need to check if

$$
x=\dfrac{4}{3}
$$

is a root of $p(x)$. In other words, we need to check if

$$
p\left(\dfrac{4}{3} \right) =0
$$

$$
\begin{aligned}
p(\frac{4}{3}) &= -6(\frac{4}{3})^{2} + 11(\frac{4}{3}) - 4 \\
&=-6 \cdot \frac{16}{9} + \frac{44}{3} - 4 \\
&= \frac{-32 + 44 - 12}{3} \\
&= 0
\end{aligned}
$$

Indeed,

$$
p \left(\dfrac{4}{3} \right)=0
$$

so

$$
x=\dfrac{4}{3}
$$

is a root of the polynomial. Consequently, by the factor theorem, we conclude that $(4-3x)$ is indeed a factor of $p(x)$.

---

**Question 3**

Which of the following statements are true regarding the polynomial $f(x) = 4x^{2} - 1$?

1. $x = \frac{1}{4}$ is a root of $f(x)$
2. $(4x - 1)$ is a factor of $f(x)$
3. $(4x - 1)$ divides $f(x)$ with zero remainder

- [ ] A. None
- [ ] B. I only
- [ ] C. II and III only
- [ ] D. I and II only
- [ ] E. I, II and III

---

**Question 4**

For which of the following polynomials is $(2x + 3)$ a factor?

1. $f(x) = 3x^{3} - 7x^{2} + 2x$
2. $g(x) =-2x^{3} + 11x^{2} - 5x$
3. $h(x) = 4x^{2} - 9$

- [ ] A. I and III only
- [ ] B. II only
- [ ] C. III only
- [ ] D. II and III only
- [ ] E. I and II only

```update-progress
```

[[MA/MF2/Home|Home]]
[[MA/MF2/0. Table of Contents/TOC|Table of Contents]]
