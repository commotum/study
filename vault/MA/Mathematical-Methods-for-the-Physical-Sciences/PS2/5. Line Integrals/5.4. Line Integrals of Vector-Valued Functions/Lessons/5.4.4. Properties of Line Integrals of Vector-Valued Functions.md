# Properties of Line Integrals of Vector-Valued Functions

<!--
lesson-id: 3704
topic-code: PS2.5.4.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Establishing the Connection With Line Integrals of Scalar Fields](#establishing-the-connection-with-line-integrals-of-scalar-fields)
- [Reversing the Orientation of a Curve and Sums Over Unions of Paths](#reversing-the-orientation-of-a-curve-and-sums-over-unions-of-paths)
- [Using the Properties of Line Integrals of Vector-Valued Functions](#using-the-properties-of-line-integrals-of-vector-valued-functions)
- [Calculating the Line Integral of a Vector-Valued Function Along a Union of Curves](#calculating-the-line-integral-of-a-vector-valued-function-along-a-union-of-curves)

## Prerequisites

- [Interpreting Line Integrals of Vector-Valued Functions](<5.4.3. Interpreting Line Integrals of Vector-Valued Functions.md>)
- [Sums of Line Integrals With Respect to X and Y Over Parametric Curves](<../../5.3. Line Integrals With Respect to X and Y/Lessons/5.3.3. Sums of Line Integrals With Respect to X and Y Over Parametric Curves.md>)
- [Properties of Line Integrals With Respect to X and Y](<../../5.3. Line Integrals With Respect to X and Y/Lessons/5.3.2. Properties of Line Integrals With Respect to X and Y.md>)
- [Line Integrals of Vector-Valued Functions Over General Curves](<5.4.2. Line Integrals of Vector-Valued Functions Over General Curves.md>)

---

<a id="introduction"></a>
## Introduction

Line integrals of vector-valued functions share a fundamental connection with line integrals with respect to $x$ and $y$.

Consider a vector field $\mathbf F$ on $\mathbb R^2$ with components $P$ and $Q$:

$$
\mathbf F(x,y) = P(x,y)\,\mathbf i + Q(x,y)\,\mathbf j
$$

Now suppose that $C$ is a smooth curve on $\mathbb R^2$, given by

$$
\mathbf r (t) = x(t)\,\mathbf i + y(t)\,\mathbf j, \qquad t\in [a,b]
$$

Then, we can write the line integral of $\mathbf F$ along $C$ as

$$
\begin{aligned}
∫_(C)F \cdot dr &= ∫_{a}^{b}F(r(t)) \cdot r^{′}(t)dt \\
&= ∫_{a}^{b}(P(x(t), y(t))i + Q(x(t), y(t))j) \cdot (\frac{dx}{dt}i + \frac{dy}{dt}j)dt \\
&= ∫_{a}^{b}(P(x(t), y(t))\frac{dx}{dt} + Q(x(t), y(t))\frac{dy}{dt})dt \\
&= ∫_{a}^{b}P(x(t), y(t))\frac{dx}{dt}dt + Q(x(t), y(t))\frac{dy}{dt}dt \\
&= ∫_{a}^{b}P(x, y)dx + Q(x, y)dy
\end{aligned}
$$

So, we have the following formula:

$$
\int\limits_C \mathbf F\cdot\textrm d \mathbf r = \int_a^b P(x,y)\,\textrm d x + Q(x,y) \,\textrm d y
$$

An analogous result holds for vector fields on $\mathbb R^3$. That is,

$$
\int\limits_C \mathbf F\cdot\textrm d \mathbf r = \int_a^b P(x,y,z)\,\textrm d x + Q(x,y,z) \,\textrm d y + R(x,y,z) \,\textrm d z
$$

where

$$
\mathbf F(x,y,z) = P(x,y,z)\,\mathbf i + Q(x,y,z)\,\mathbf j + R(x,y,z)\,\mathbf k
$$

---

<a id="establishing-the-connection-with-line-integrals-of-scalar-fields"></a>
## Establishing the Connection With Line Integrals of Scalar Fields

**Example:** Let $\mathbf{F}(x,y) = (3x+2) \: \mathbf{i} - y^3 \: \mathbf{j}$ and let $C$ be a path in $\mathbb R^2$. Write $\displaystyle \int\limits_C \mathbf{F} \cdot \text{d}\mathbf{r}$ in the form $\displaystyle\int_a^b P(x,y)\,\textrm d x + Q(x,y)\,\textrm d y$.

**Explanation**

Given a piecewise-smooth path $C$ and a vector field

$$
\mathbf{F}(x,y) = P(x,y) \: \mathbf{i} + Q(x,y) \: \mathbf{j}
$$

defined on $C$, we have

$$
\int\limits_C \mathbf{F} \cdot \text{d}\mathbf{r} = \int\limits_C P(x,y) \: \text{d}x + Q(x,y) \: \text{d}y
$$

In our case,

$$
P = 3x+2
$$

and $Q = -y^3$. Therefore, we obtain

$$
\begin{aligned}
∫_(C)F \cdot dr &= ∫_(C)(3x + 2)dx - y^{3}dy
\end{aligned}
$$

---

**Question 1:** Let $F(x, y, z) = y^{2}i - x^{2}j + 5k$ and let $C$ be a path in $R^{3}$. Which of the following is equivalent to $∫_(C)F \cdot dr$?

- [ ] A. $∫_(C)(y^{2} - x^{2})dxdy + 5dz$
- [ ] B. $∫_(C)y^{2}dx - x^{2}dy + 5dz$
- [ ] C. $∫_(C)(y^{2} + x^{2} + 5)dxdydz$
- [ ] D. $∫_(C)x^{2}dx - y^{2}dy + 5dz$
- [ ] E. $∫_(C)y^{2}dx + x^{2}dy$

---

**Question 2:** If $∫_(C)zdx + xdy + ydz$ is equivalent to $∫_(C)F \cdot dr$ for some piecewise smooth curve $C$, then $F(x, y, z) =$

- [ ] A. $∇(x + y + z)$
- [ ] B. $\frac{1}{2}∇(x + y + z)^{2}$
- [ ] C. $\frac{1}{2}z^{2}i + \frac{1}{2}x^{2}j + \frac{1}{2}y^{2}k$
- [ ] D. $zi + xj + yk$
- [ ] E. $i + j + k$

---

<a id="reversing-the-orientation-of-a-curve-and-sums-over-unions-of-paths"></a>
## Reversing the Orientation of a Curve and Sums Over Unions of Paths

Since there is a fundamental link between line integrals of vector-valued functions and line integrals with respect to $x$ and $y$, it should be unsurprising that they share similar properties. We list two of them below:

- If $-C$ denotes the path obtained by reversing the orientation of $C$, then
$\int\limits_{-C} \mathbf F \cdot \mathrm d \mathbf r = -\int\limits_C \mathbf F \cdot \mathrm d \mathbf r$.
- If $C = C_1\cup C_2$ is the union of two paths $C_1$ and $C_2$, then
$\int\limits_{C} \mathbf F \cdot \mathrm d \mathbf r = \int\limits_{C_1} \mathbf F \cdot \mathrm d \mathbf r + \int\limits_{C_2} \mathbf F \cdot \mathrm d \mathbf r$.

The first property has an intuitive meaning if we think about it in terms of the work done by a force on a particle. To see this, consider the vector field $\mathbf F$ and the line segment $\overline{AB}$, shown below. Let $C$ be the path defined by traversing the line segment from $A$ to $B$.

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/94572df4ae212a603ac63748df659358.png>)

Let $W(C)$ be the work done in moving the particle along $C$. Since our particle moves in the same direction as the vector field, we have

$$
W(C) = \int\limits_{C} \mathbf F \cdot \mathrm d \mathbf r > 0
$$

However, if we move the particle from $B$ to $A$, we expect the line integral to be negative. This is because the particle is now moving against $\mathbf F$.

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/98d2a57d73d03a14fcd016d9b495fab5.png>)

Therefore,

$$
W(-C) = \int\limits_{-C} \mathbf F \cdot \mathrm d \mathbf r < 0
$$

We can show that the work done in moving the particle along $-C$ is the same as the work done in moving it along $C$ but with the opposite sign. That is

$$
W(-C) = -W(C)
$$

Therefore, we have

$$
\int\limits_{-C} \mathbf F \cdot \mathrm d \mathbf r = -\int\limits_{C} \mathbf F \cdot \mathrm d \mathbf r
$$

---

<a id="using-the-properties-of-line-integrals-of-vector-valued-functions"></a>
## Using the Properties of Line Integrals of Vector-Valued Functions

**Example:** Consider the integral $\displaystyle \int\limits_{C} \mathbf{F} \cdot \textrm{d}\mathbf{r}$, where $C$ is the closed polygonal path $FGHF$ shown above, traversed in the **clockwise** direction. Express this integral as a sum of line integrals along $C_1, C_2$, and $C_3$.

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/4c07015f012adfb9c576718541a44fe8.png>)

**Explanation**

First, notice that we can represent $C$, which is traversed in clockwise, as the union of $3$ paths:

$$
C = \{-C_1\} \cup \{-C_2\} \cup C_3
$$

where $-C_1$ and $-C_2$ are the same paths as $C_1$ and $C_2$ but traversed in the opposite direction.

Therefore, we obtain

$$
\begin{aligned}
∫_(C)F \cdot dr &= ∫_(-C_{1})F \cdot dr + ∫_(-C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr \\
&= - ∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr
\end{aligned}
$$

---

**Question 3**

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/q-76476.png>)

Which of the following is equivalent to the integral $∫_(C)F \cdot dr$, where $C$ is the closed polygonal path $ABCA$, shown in the diagram above, traversed in the **clockwise** direction?

- [ ] A. $- ∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr$
- [ ] B. $∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr$
- [ ] C. $∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr - ∫_(C_{3})F \cdot dr$
- [ ] D. $∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr$
- [ ] E. $- ∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr - ∫_(C_{3})F \cdot dr$

---

**Question 4**

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/q-76473.png>)

Which of the following is equivalent to the integral $∫_(C)F \cdot dr$, where $C$ is the closed polygonal path $OABO$, shown in the diagram above, traversed in the counterclockwise direction?

- [ ] A. $- ∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr - ∫_(C_{3})F \cdot dr$
- [ ] B. $∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr - ∫_(C_{3})F \cdot dr$
- [ ] C. $∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr$
- [ ] D. $- ∫_(C_{1})F \cdot dr - ∫_(C_{2})F \cdot dr - ∫_(C_{3})F \cdot dr$
- [ ] E. $∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr + ∫_(C_{3})F \cdot dr$

---

<a id="calculating-the-line-integral-of-a-vector-valued-function-along-a-union-of-curves"></a>
## Calculating the Line Integral of a Vector-Valued Function Along a Union of Curves

**Example:** The path $C_1$ consists of a line segment from the point $A(-2,0)$ to the point $O(0,0)$, and the path $C_2$ consists of a line segment from the point $O(0,0)$ to the point $B(1,1)$.

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/962f5b7322eaec11b61a09faa7e19175.png>)

Given that $\displaystyle \int\limits_{C_1} \mathbf{F} \cdot \textrm{d}\mathbf{r} = -4$, evaluate $\displaystyle \int\limits_{C} \mathbf{F} \cdot \textrm{d}\mathbf{r}$, where $\mathbf{F}(x, y) = (2x-3y) \,\mathbf{i} + (2y-5x)\,\mathbf {j}$ and $C = C_1\cup C_2$.

**Explanation**

We will use the fact that

$$
\int\limits_{C} \mathbf{F} \cdot \textrm{d}\mathbf{r} = \int\limits_{C_1} \mathbf{F} \cdot \textrm{d}\mathbf{r} + \int\limits_{C_2} \mathbf{F} \cdot \textrm{d}\mathbf{r}
$$

We're given the value of the first integral, so we only need to work out the second.

The path $C_2$ is the line segment from $O$ to $B$. It can be parametrized as

$$
\begin{aligned}
r_{2}(t) &= b + t(c - b) \\
&= ⟨0, 0⟩ + t(⟨1, 1⟩ - ⟨0, 0⟩) \\
&= ⟨t, t⟩ \\
&= ti + tj
\end{aligned}
$$

where $0 \lt t \lt 1$. Along this segment, we have $x = t$ and $y = t$. Therefore,

$$
\begin{aligned}
F(r_{2}(t)) &= (2x - 3y)i + (2y - 5x)j \\
&= (2t - 3t)i + (2t - 5t)j \\
&=-ti - 3tj
\end{aligned}
$$

Computing the derivative, we get the following:

$$
\begin{aligned}
r_{2}^{′}(t) &= \frac{d}{dt}(t)i + \frac{d}{dt}(t)j \\
&= i + j
\end{aligned}
$$

We can now compute the dot product:

$$
\begin{aligned}
F(r_{2}(t)) \cdot r_{2}^{′}(t) &= (-ti - 3tj) \cdot (i + j) \\
&=-t \cdot 1 + (-3t) \cdot 1 \\
&=-4t
\end{aligned}
$$

Finally, we can evaluate the integral, as follows:

$$
\begin{aligned}
∫_(C)F \cdot dr &= ∫_(C_{1})F \cdot dr + ∫_(C_{2})F \cdot dr \\
&=-4 + ∫_{0}^{1}F(r_{2}(t)) \cdot r_{2}^{′}(t)dt \\
&=-4 + ∫_{0}^{1}(-4t)dt \\
&=-4 + [- 2t^{2}]_{0}^{1} \\
&=-4 + [- 2 + 0] \\
&=-6
\end{aligned}
$$

---

**Question 5**

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/q-39342.png>)

The path $C_{1}$ consists of a line segment from the point $O(0, 0)$ to the point $A(2, 0)$, and the path $C_{2}$ consists of a line segment from the point $A(2, 0)$ to the point $B(1, 1)$.

Given that $∫_(C_{1})F \cdot dr = 2$, evaluate $∫_(C)F \cdot dr$, where $F(x, y) = (x + y)i + (x - y)j$, and $C = C_{1}∪C_{2}$.

- [ ] A. $1$
- [ ] B. $3$
- [ ] C. $2$
- [ ] D. $4$
- [ ] E. $6$

---

**Question 6**

![](<../Source/5.4.4. Properties of Line Integrals of Vector-Valued Functions/Images/q-39674.png>)

The path $C_{1}$ consists of a section of the parabola $y = x^{2}$ from the point $O(0, 0)$ to the point $A(2, 4)$, and the path $C_{2}$ consists of a line segment from the point $A(2, 4)$ to the point $B(0, 4)$.

Given that $∫_(C_{2})F \cdot dr =-2$, evaluate $∫_(C)F \cdot dr$, where $F(x, y) = xi + yj$ and $C = C_{1}∪C_{2}$.

- [ ] A. $-2$
- [ ] B. $8$
- [ ] C. $2$
- [ ] D. $10$
- [ ] E. $0$

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/0. Table of Contents/TOC|Table of Contents]]
