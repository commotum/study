# Path Independence of Line Integrals

<!--
lesson-id: 3360
topic-code: PS2.5.4.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Calculating a Line Integral Over an Arbitrary Path](#calculating-a-line-integral-over-an-arbitrary-path)
- [Calculating a Line Integral Over an Arbitrary Path When the Potential Function Is Not Given](#calculating-a-line-integral-over-an-arbitrary-path-when-the-potential-function-is-not-given)
- [Identifying Vector Fields That Are Independent of Path](#identifying-vector-fields-that-are-independent-of-path)

## Prerequisites

- [The Fundamental Theorem for Line Integrals](<5.4.5. The Fundamental Theorem for Line Integrals.md>)

---

<a id="introduction"></a>
## Introduction

The fundamental theorem of line integrals states that

$$
\int_{C} \nabla f \cdot \textrm{d}\mathbf r = f(\mathbf r(b)) - f(\mathbf r(a))
$$

where $C$ is a piecewise-smooth curve $\mathbf r(t)$ for $t\in [a,b]$, and $f$ is differentiable function such that $\nabla f$ is continuous on $C$.

This theorem tells us that the line integral of a conservative vector field

$$
\mathbf{F} = \nabla f
$$

is determined by the values of $f$ at the endpoints $A$ and $B$ of $C$ only. The integral does *not* depend on the path between $A$ and $B$.

In particular, if $\mathbf F$ is conservative, then we say that $\displaystyle\int_{C} \mathbf F \cdot \textrm{d}\mathbf r$ is **path-independent.**

---

<a id="calculating-a-line-integral-over-an-arbitrary-path"></a>
## Calculating a Line Integral Over an Arbitrary Path

**Example:** Consider the scalar field $f(x,y,z) = x^2 + y^2 + z^2$. Calculate $\displaystyle{\int_C \nabla f \cdot \textrm d\mathbf r}$, where $C$ is any smooth path from $(-2, 1, 0)$ to $(-1, 0, -1)$ in $\mathbb R^3$.

**Explanation**

Let $C$ be a piecewise-smooth curve given by the vector-valued function $\mathbf r(t)$ for $t \in [a,b]$. Let $f$ be a differentiable function of two or three variables such that $\nabla f$ is continuous on $C$. The fundamental theorem for line integrals states that

$$
\int_{C} \nabla f \cdot \textrm{d}\mathbf r = f(\mathbf r(b)) - f(\mathbf r(a))
$$

This theorem implies that the line integral of a conservative vector field depends only on the value of $f$ at the initial and terminal points $A$ and $B$, and is independent of the path between them.

In our case, the coordinates of the endpoints of the path $C$ are $A(-2, 1, 0)$ and $B(-1, 0, -1)$.

Applying the fundamental theorem for line integrals, we have

$$
\begin{aligned}
∫_{C}∇f \cdot dr &= f(-1, 0,-1) - f(-2, 1, 0) \\
&= ((-1)^{2} + 0^{2} + (-1)^{2}) - ((-2)^{2} + 1^{2} + 0^{2}) \\
&= 2 - 5 \\
&=-3
\end{aligned}
$$

---

**Question 1:** Consider the scalar field $f(x, y) = 4x - 3y$. Calculate $∫_{C}∇f \cdot dr$, where $C$ is any smooth path from $(0, 2)$ to $(1,-3)$ in $R^{2}$.

- [ ] A. $16$
- [ ] B. $15$
- [ ] C. $12$
- [ ] D. $22$
- [ ] E. $19$

---

**Question 2:** Consider the scalar field $f(x, y, z) = xyz + y^{2}$. Calculate $∫_{C}∇f \cdot dr$, where $C$ is any smooth path from $(1, 0,-1)$ to $(2, 1, 1)$ in $R^{3}$.

- [ ] A. $4$
- [ ] B. $0$
- [ ] C. $1$
- [ ] D. $3$
- [ ] E. $\frac{1}{2}$

---

<a id="calculating-a-line-integral-over-an-arbitrary-path-when-the-potential-function-is-not-given"></a>
## Calculating a Line Integral Over an Arbitrary Path When the Potential Function Is Not Given

**Example:** Consider the vector field $\mathbf F(x, y) = -2x\,\mathbf i + 6y^2\,\mathbf j$. Calculate $\displaystyle{\int_C \mathbf F \cdot \textrm d\mathbf r}$, where $C$ is any smooth path from $(-2, 0)$ to $(1, 2)$ in $\mathbb R^2$.

**Explanation**

Let $C$ be a piecewise-smooth curve given by the vector-valued function $\mathbf r(t)$ for $t \in [a,b]$. Let $f$ be a differentiable function of two or three variables such that $\nabla f$ is continuous on $C$. The fundamental theorem for line integrals states that

$$
\int_{C} \nabla f \cdot \textrm{d}\mathbf r = f(\mathbf r(b)) - f(\mathbf r(a))
$$

This theorem implies that the line integral of a conservative vector field depends only on the value of $f$ at the initial and terminal points $A$ and $B$, and is independent of the path between them.

In our case, the coordinates of the endpoints of the path $C$ are $A(-2,0)$ and $B(1, 2)$.

Notice that

$$
\mathbf F = P\,\mathbf i + Q\,\mathbf j
$$

is a vector field on $\mathbb R^2$ with domain

$$
D = \mathbb R^2
$$

Computing the partial derivatives, we get

$$
\dfrac{\partial P}{\partial y} = \dfrac{\partial Q}{\partial x} = 0
$$

Since the partial derivatives exist and are continuous and equal everywhere on $\mathbb R^2$, we conclude that $\mathbf F$ is conservative. Therefore, we know there exists a function $f$ such that

$$
\nabla f = \mathbf F
$$

Reconstructing $f$ from its gradient in the usual way, we get

$$
f = 2y^3 - x^2
$$

Finally, applying the fundamental theorem for line integrals, we have

$$
\begin{aligned}
∫_{C}F \cdot dr &= ∫_{C}∇f \cdot dr \\
&= f(1, 2) - f(-2, 0) \\
&= (2(2)^{3} - 1^{2}) - (2(0)^{3} - (-2)^{2}) \\
&= 16 - 1 + 4 \\
&= 19
\end{aligned}
$$

---

**Question 3:** Consider the vector field $F(x, y) = y^{2}e^{xy^{2}}i + 2xye^{xy^{2}}j$. Calculate $∫_{C}F \cdot dr$, where $C$ is any smooth path from $(0, 0)$ to $(\ln 2, 2)$ in $R^{2}$.

- [ ] A. $32$
- [ ] B. $16$
- [ ] C. $15$
- [ ] D. $3\ln 2$
- [ ] E. $\sqrt{e}$

---

**Question 4:** Consider the vector field $F(x, y) =-2xy\sin x^{2}i + \cos x^{2}j$. Calculate $∫_{C}F \cdot dr$, where $C$ is any smooth path from $(0, 0)$ to $(\sqrt{π}, 1)$ in $R^{2}$.

- [ ] A. $2$
- [ ] B. $1$
- [ ] C. $0$
- [ ] D. $-2$
- [ ] E. $-1$

---

<a id="identifying-vector-fields-that-are-independent-of-path"></a>
## Identifying Vector Fields That Are Independent of Path

**Example:** Consider the vector field $\mathbf F(x,y) = \left\langle ye^{xy},\, xe^{xy}\right\rangle$ and the points $A(1,1)$ and $B(2,2)$ in the $xy$-plane.

Which of the following statements are true?

1. $\mathbf F$ is conservative.
2. $\displaystyle\int\limits_{C_1} \mathbf F\cdot \textrm d \mathbf r = e^4-e$, where $C_1$ is the path along the curve $y=\sqrt{2x}$, traversed from $A$ to $B$.
3. $\displaystyle\int\limits_{C_2} \mathbf F\cdot \textrm d \mathbf r = e^4-e$, where $C_2$ is the path along along the line segment $\overline{AB}$, traversed from $A$ to $B$.

**Explanation**

Let $C$ be a piecewise-smooth curve given by the vector-valued function $\mathbf r(t)$ for $t\in [a,b]$. Let $f$ be a differentiable function of two or three variables such that $\nabla f$ is continuous on $C$. The fundamental theorem for line integrals states that

$$
\int_{C} \nabla f \cdot \textrm{d}\mathbf r = f(\mathbf r(b)) - f(\mathbf r(a))
$$

This theorem implies that the line integral of a conservative vector field depends only on the value of $f$ at the initial and terminal points $A$ and $B$, and is independent of the path between them.

With that in mind, we consider each statement in turn.

- Statement I is true. Notice that $\mathbf F = P\,\mathbf i + Q\,\mathbf j$ is a vector field on $\mathbb R^2$ with domain $D = \mathbb R^2$. Computing the partial derivatives, we get
$\dfrac{\partial P}{\partial y} = \dfrac{\partial Q}{\partial x} = (1+xy)e^{xy}$.
Since the partial derivatives exist and are continuous and equal everywhere on $\mathbb R^2$, we conclude that $\mathbf F$ is conservative. Therefore, we know there exists a function $f$ such that $\nabla f = \mathbf F$.
- Statement II is true. Since $\mathbf F$ is conservative, there exists a function $f$ such that $\nabla f = \mathbf F$. Reconstructing the function $f$ from its gradient in the usual way, we get
$f = e^{xy}$.
Applying the fundamental theorem for line integrals, we get
$∫_(C_{1})F \cdot dr|= ∫_(C_{1})∇f \cdot dr; = f(2, 2) - f(1, 1); = e^{2 \cdot 2} - e^{1 \cdot 1}; = e^{4} - e$.
- Statement III is true. Since $\mathbf F$ is conservative and $C_2$ has the same initial and terminal points as $C_1$, we have
$\int\limits_{C_2} \mathbf F\cdot \textrm d \mathbf r = \displaystyle\int\limits_{C_1} \mathbf F\cdot \textrm d \mathbf r = e^{4}-e$.

Therefore, the correct answer is "I, II, and III."

---

**Question 5**

Consider the vector field $F(x, y) = ⟨e^{x + y}, e^{x + y}⟩$ and the points $O(0, 0)$ and $A(1, \ln 2)$ in the $xy$-plane.

Which of the following statements are true?

1. $F$ is conservative.
2. $∫_(C_{1})F \cdot dr = 2e - 1$, where $C_{1}$ is the path along the line segment $OA^(―)$, traversed from $O$ to $A$.
3. $∫_(C_{2})F \cdot dr = 2e - 1$, where $C_{2}$ is the path along the curve $y = \ln (x + 1)$, traversed from $O$ to $A$.

- [ ] A. I and II only
- [ ] B. I only
- [ ] C. II only
- [ ] D. I and III only
- [ ] E. I, II, and III

---

**Question 6**

Consider the vector field $F(x, y) = ⟨y,-x⟩$ and the points $O(0, 0)$ and $A(1, 1)$ in the $xy$-plane.

Which of the following statements are true?

1. $∫_(C_{1})F \cdot dr = 0$, where $C_{1}$ is the path along the line segment $OA^(―)$, traversed from $O$ to $A$.
2. $∫_(C_{2})F \cdot dr = 0$, where $C_{2}$ is the path along the parabola $y = x^{2}$, traversed from $O$ to $A$.
3. $∫_(C)F \cdot dr$ is independent of path.
4. $F$ is conservative.

- [ ] A. All the statements are true.
- [ ] B. I and II only
- [ ] C. III and IV only
- [ ] D. I only
- [ ] E. II only

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/0. Table of Contents/TOC|Table of Contents]]
