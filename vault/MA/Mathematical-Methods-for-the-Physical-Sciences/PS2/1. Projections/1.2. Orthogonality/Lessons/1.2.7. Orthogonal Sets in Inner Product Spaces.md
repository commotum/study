# Orthogonal Sets in Inner Product Spaces

<!--
lesson-id: 2104
topic-code: PS2.1.2.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining Whether a Set of Vectors Is Orthogonal With Respect to a Given Inner Product](#determining-whether-a-set-of-vectors-is-orthogonal-with-respect-to-a-given-inner-product)
- [Determining Whether a Set of Polynomials is Orthogonal With Respect to a Given Inner Product](#determining-whether-a-set-of-polynomials-is-orthogonal-with-respect-to-a-given-inner-product)
- [Determining Whether a Set of Functions is Orthogonal With Respect to a Given Inner Product](#determining-whether-a-set-of-functions-is-orthogonal-with-respect-to-a-given-inner-product)

## Prerequisites

- [Orthogonal Vectors in Inner Product Spaces](<1.2.2. Orthogonal Vectors in Inner Product Spaces.md>)
- [Orthogonal Sets in Euclidean Spaces](<1.2.6. Orthogonal Sets in Euclidean Spaces.md>)

---

<a id="introduction"></a>
## Introduction

Let's consider an inner product space $V$ equipped with the inner product $\langle \cdot, \cdot \rangle$.
A set of vectors

$$
{U}=\{\mathbf{u}_1, \cdots, \mathbf{u}_p \}
$$

is called an **orthogonal set with respect to the given inner product** if all vectors in the set are mutually orthogonal.

For example, let's consider the set

$$
\begin{bmatrix}1 \\ 1 \\ 1\end{bmatrix}
$$

and the inner product on $\mathbb{R}^3$ defined as

$$
\langle \mathbf{x},\mathbf{y} \rangle = x_1 y_1 + 3x_2 y_2 + 2x_3 y_3
$$

If we denote the vectors in our set ${V}$ as $\{\mathbf{v}_1, \mathbf{v}_2, \mathbf{v}_3 \}$, we can check that ${V}$ is orthogonal by showing that

$$
{⟨v_{1}, v_{2}⟩ = 0; ⟨v_{1}, v_{3}⟩ = 0; ⟨v_{2}, v_{3}⟩ = 0
$$

Let's compute each inner product:

$$
\begin{aligned}
\begin{bmatrix}⟨v_{1}, v_{2}⟩ &= (1)(2) + 3(1)(2) + 2(1)(-4) = 2 + 6 - 8 = 0 & ✓ \\ ⟨v_{1}, v_{3}⟩|= (1)(-3) + 3(1)(1) + 2(1)(0) =-3 + 3 + 0 = 0 & ✓ \\ ⟨v_{2}, v_{3}⟩|= (2)(-3) + 3(2)(1) + 2(-4)(0) =-6 + 6 + 0 = 0 & ✓\end{bmatrix}
\end{aligned}
$$

So indeed, ${V}$ is an orthogonal set.

---

<a id="determining-whether-a-set-of-vectors-is-orthogonal-with-respect-to-a-given-inner-product"></a>
## Determining Whether a Set of Vectors Is Orthogonal With Respect to a Given Inner Product

**Example:** $\begin{bmatrix}2 \\ 2 \\ 1\end{bmatrix}$
Consider the vectors shown above. Given that the inner product in the vector space $\mathbb{R}^3$ is defined as
$\langle \mathbf{x},\mathbf{y} \rangle = 3x_1 y_1 + x_2 y_2 + 2x_3 y_3$,
which of the following statements are true?

1. $\mathbf{v}_1 \perp \mathbf{v}_2$ with respect to the given inner product
2. $\mathbf{v}_1 \perp \mathbf{v}_3$ with respect to the given inner product
3. $S=\{\mathbf{v}_1, \mathbf{v}_2, \mathbf{v}_3 \}$ is an orthogonal set with respect to the given inner product

**Explanation**

Recall that a set of vectors in an inner product space is orthogonal if all vectors in the set are mutually orthogonal with respect to the given inner product.

With that in mind, let's examine our statements in turn.

- Statement I is true. Indeed, we have
$⟨v_{1}, v_{2}⟩|= 3(2)(-1) + (2)(1) + 2(1)(2); =-6 + 2 + 4; = 0$,
which means that $\mathbf{v}_1 \perp \mathbf{v}_2$ with respect to the given inner product.
- Statement II is false since
$⟨v_{1}, v_{3}⟩|= 3(2)(0) + (2)(1) + 2(1)(1); = 0 + 2 + 2; = 4 \ne 0$,
which means that $\mathbf{v}_1 \not\perp \mathbf{v}_3$ with respect to the given inner product.
- Statement III is false. Since $\mathbf{v}_1 \not\perp \mathbf{v}_3$, the set $S$ is *not* orthogonal.

Therefore, the correct answer is "I only."

---

**Question 1**

$v_{1} = \begin{bmatrix}4 \\ -2 \\ 2\end{bmatrix}, v_{2} = \begin{bmatrix}-8 \\ 4 \\ 5\end{bmatrix}, v_{3} = \begin{bmatrix}1 \\ 2 \\ 0\end{bmatrix}$
Consider the vectors shown above. Given that the inner product in the vector space $R^{3}$ is defined as
$⟨x, y⟩ = x_{1}y_{1} + x_{2}y_{2} + 4x_{3}y_{3}$,
which of the following statements are true?

1. $v_{1}⊥v_{2}$ with respect to the given inner product
2. $v_{1}⊥v_{3}$ with respect to the given inner product
3. $S = {v_{1}, v_{2}, v_{3}}$ is an orthogonal set with respect to the given inner product

- [ ] A. I, II, and III
- [ ] B. II only
- [ ] C. I and II only
- [ ] D. None of the statements are true
- [ ] E. I only

---

**Question 2**

$v_{1} = \begin{bmatrix}6 \\ 0 \\ -2\end{bmatrix}, v_{2} = \begin{bmatrix}-1 \\ 1 \\ -1\end{bmatrix}, v_{3} = \begin{bmatrix}1 \\ 1 \\ 0\end{bmatrix}$
Consider the vectors shown above. Given that the inner product in the vector space $R^{3}$ is defined as
$⟨x, y⟩ = x_{1}y_{1} + 2x_{2}y_{2} + 3x_{3}y_{3}$,
which of the following statements are true?

1. $v_{1}⊥v_{2}$ with respect to the given inner product
2. $v_{1}⊥v_{3}$ with respect to the given inner product
3. $S = {v_{1}, v_{2}, v_{3}}$ is an orthogonal set with respect to the given inner product

- [ ] A. I, II, and III
- [ ] B. I and II only
- [ ] C. II only
- [ ] D. I only
- [ ] E. None of the statements is true

---

<a id="determining-whether-a-set-of-polynomials-is-orthogonal-with-respect-to-a-given-inner-product"></a>
## Determining Whether a Set of Polynomials is Orthogonal With Respect to a Given Inner Product

**Example:** Consider the following set of polynomials:
$p_1(t)=t+1, \qquad p_2(t)=t, \qquad p_3(t)=t-1$.
Given that the inner product in the vector space $\mathbb{R}_1[t]$ of polynomials of degree $1$ or less in the variable $t$ is defined as
$\langle p(t),q(t) \rangle =p(-1)q(-1)+p(1)q(1)$,
which of the following statements are true?

1. $p_1(t) \perp p_3(t)$ with respect to the given inner product
2. $p_2(t) \perp p_3(t)$ with respect to the given inner product
3. $S=\{p_1(t), p_2(t), p_3(t) \}$ is an orthogonal set with respect to the given inner product

**Explanation**

Recall that a set of vectors in an inner product space is orthogonal if all vectors in the set are mutually orthogonal with respect to the given inner product.

With that in mind, let's examine our statements in turn.

- Statement I is true. Indeed, we have
$⟨p_{1}(t), p_{3}(t)⟩|= ⟨t + 1, t - 1⟩; = (t + 1)\begin{vmatrix}_{-1} \cdot (t - 1) & _{-1} + (t + 1) & _{1} \cdot (t - 1)\end{vmatrix}_{1}; = (-1 + 1) \cdot (-1 - 1) + (1 + 1) \cdot (1 - 1); = 0$,
which means that $p_1(t) \perp p_3(t)$ with respect to the given inner product.
- Statement II is false since
$⟨p_{2}(t), p_{3}(t)⟩|= ⟨t, t - 1⟩; = (t)\begin{vmatrix}_{-1} \cdot (t - 1) & _{-1} + (t) & _{1} \cdot (t - 1)\end{vmatrix}_{1}; = (-1) \cdot (-1 - 1) + (1) \cdot (1 - 1); = 2 \ne 0$,
which means that $p_2(t) \not\perp p_3(t)$ with respect to the given inner product.
- Statement III is false. Since $p_2(t) \not\perp p_3(t)$, the set $S$ is *not* orthogonal.

Therefore, the correct answer is "I only."

---

**Question 3**

Consider the following set of polynomials:
$p_{1}(t) = t, p_{2}(t) = t - 1, p_{3}(t) = 2t - 2$.
Given that the inner product in the vector space $R_{1}[t]$ of polynomials of degree at most $1$ in the variable $t$ is defined as
$⟨p(t), q(t)⟩ = p(0)q(0) + p(1)q(1)$,
which of the following statements are true?

1. $p_{1}(t)⊥p_{2}(t)$ with respect to the given inner product
2. $p_{1}(t)⊥p_{3}(t)$ with respect to the given inner product
3. $S = {p_{1}(t), p_{2}(t), p_{3}(t)}$ is an orthogonal set with respect to the given inner product

- [ ] A. II only
- [ ] B. None of the statements are true
- [ ] C. I, II, and III
- [ ] D. I only
- [ ] E. I and II only

---

**Question 4**

Consider the following set of polynomials:
$p_{1}(t) = 2t, p_{2}(t) = t + 1, p_{3}(t) = 2t + 1$.
Given that the inner product in the vector space $R_{1}[t]$ of polynomials of degree at most $1$ in the variable $t$ is defined as
$⟨p(t), q(t)⟩ = p(0)q(0) + p(-1)q(-1)$,
which of the following statements are true?

1. $p_{1}(t)⊥p_{2}(t)$ with respect to the given inner product
2. $p_{1}(t)⊥̸p_{3}(t)$ with respect to the given inner product
3. $S = {p_{1}(t), p_{2}(t), p_{3}(t)}$ is an orthogonal set with respect to the given inner product

- [ ] A. None of the statements are true
- [ ] B. I only
- [ ] C. III only
- [ ] D. I and II only
- [ ] E. II only

---

<a id="determining-whether-a-set-of-functions-is-orthogonal-with-respect-to-a-given-inner-product"></a>
## Determining Whether a Set of Functions is Orthogonal With Respect to a Given Inner Product

**Example:** Consider the following set of functions:
$f_1(t)=1, \qquad f_2(t)=t, \qquad f_3(t)=2t+1$

Given that the inner product in the vector space $\mathcal{C}[-1,1]$ of continuous functions over $[-1,1]$ is defined as
$\langle f(t),g(t) \rangle = \int_{-1}^{1} f(t)g(t) \: \text{d}t$,
which of the following statements are true?

1. $f_1(t) \perp f_3(t)$ with respect to the given inner product
2. $f_2(t) \perp f_3(t)$ with respect to the given inner product
3. $S=\{f_1(t), f_2(t), f_3(t) \}$ is an orthogonal set with respect to the given inner product

**Explanation**

Recall that a set of vectors in an inner product space is called orthogonal if all vectors in the set are mutually orthogonal with respect to the given inner product.

With that in mind, let's examine our statements in turn.

- Statement I is false. We have
$⟨f_{1}(t), f_{3}(t)⟩|= ⟨1, 2t + 1⟩; = ∫_{-1}^{1}(2t + 1)dt; = (t^{2} + t) \mid _{-1}^{1}; = 1^{2} + 1 - ((-1)^{2} + (-1)); = 2 \ne 0$.
This means that $f_1(t) \not\perp f_2(t)$ with respect to the given inner product.
- Statement II is false. We have
$⟨f_{2}(t), f_{3}(t)⟩|= ⟨t, 2t + 1⟩; = ∫_{-1}^{1}t(2t + 1)dt; = ∫_{-1}^{1}(2t^{2} + t)dt; = \frac{2t^{3}}{3} + \frac{t^{2}}{2} \mid _{-1}^{1}; = (2(1)^{3})/(3) + \frac{1^{2}}{2} - ((2(-1)^{3})/(3) + ((-1)^{2})/(2)); = \frac{7}{6} + \frac{1}{6}; = \frac{4}{3} \ne 0$.
This means that $f_2(t) \not\perp f_3(t)$ with respect to the given inner product.
- Statement III is false. Since $f_1(t) \not\perp f_2(t)$, the set $S$ is *not* orthogonal.

Therefore, the correct answer is "None of the statements are true."

---

**Question 5**

Consider the following set of functions:
$f_{1}(t) = t^{2}, f_{2}(t) = t^{3}, f_{3}(t) = t$

Given that the inner product in the vector space $C[- 1, 1]$ of continuous functions over $[- 1, 1]$ is defined as
$⟨f(t), g(t)⟩ = ∫_{-1}^{1}f(t)g(t)dt$,
which of the following statements are true?

1. $f_{1}(t)⊥f_{2}(t)$ with respect to the given inner product
2. $f_{2}(t)⊥f_{3}(t)$ with respect to the given inner product
3. $S = {f_{1}(t), f_{2}(t), f_{3}(t)}$ is an orthogonal set with respect to the given inner product

*Hint: $⟨f_{1}, f_{3}⟩ = 0$.*

- [ ] A. I, II, and III
- [ ] B. I and II only
- [ ] C. None of the statements are true
- [ ] D. II only
- [ ] E. I only

---

**Question 6**

Consider the following set of functions:
$f_{1}(t) = \cos t, f_{2}(t) = \sin (2t), f_{3}(t) = t$

Given that the inner product in the vector space $C[0, π]$ of continuous functions over $[0, π]$ is defined as
$⟨f(t), g(t)⟩ = ∫_{0}^{π}f(t)g(t)dt$,
which of the following statements are true?

1. $f_{1}(t)⊥f_{2}(t)$ with respect to the given inner product
2. $f_{1}(t)⊥f_{3}(t)$ with respect to the given inner product
3. $S = {f_{1}(t), f_{2}(t), f_{3}(t)}$ is an orthogonal set with respect to the given inner product

- [ ] A. None of the statements are true
- [ ] B. I and II only
- [ ] C. I, II, and III
- [ ] D. II only
- [ ] E. I only

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/0. Table of Contents/TOC|Table of Contents]]
