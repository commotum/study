# Laplace Transforms of Piecewise Continuous Functions

<!--
lesson-id: 6717
topic-code: PS2.9.1.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Taking Laplace Transforms of Piecewise Functions](#taking-laplace-transforms-of-piecewise-functions)
- [Finding Laplace Transforms of Step Functions](#finding-laplace-transforms-of-step-functions)
- [The General Result for the Shifted Unit Step Function](#the-general-result-for-the-shifted-unit-step-function)
- [Finding Laplace Transforms of Other Piecewise Continuous Functions](#finding-laplace-transforms-of-other-piecewise-continuous-functions)
- [Uniqueness of Laplace Transforms](#uniqueness-of-laplace-transforms)

## Prerequisites

- [Definite Integrals of Piecewise Functions](<../../../../CAB/6. Integration/6.4. The Area Under a Curve/Lessons/6.4.7. Definite Integrals of Piecewise Functions.md>)
- [Introduction to Laplace Transforms](<9.1.4. Introduction to Laplace Transforms.md>)
- [Piecewise Continuity](<9.1.3. Piecewise Continuity.md>)

---

<a id="introduction"></a>
## Introduction

So far, we have focused on finding the Laplace transforms of functions such as $1$ and $e^{kt}$, which are defined by a single formula.

In many real-world applications, however, a function is defined by different rules on different time intervals.

Recall that a function is *piecewise continuous* on $[MATH: [0,\infty)]$ if it is continuous on each interval of a finite partition, with possible jump discontinuities at the partition points. A basic example is the **unit step function**:

$$
{0,\begin{vmatrix}t < 0 \\ 1,\end{vmatrix}t \ge 0
$$

Even though $u(t)$ is defined for all real $t$, notice that the Laplace transform only “sees” values for

$$
t\ge 0
$$

:

$$
\mathcal L\{f(t)\} = \int_0^\infty e^{-st}f(t)\,\textrm d t
$$

So, on $[MATH: [0,\infty)]$ we have $u(t)=1$, and the Laplace transform reduces to

$$
\mathcal L\{u(t)\} = \int_0^\infty e^{-st}\,\textrm d t
$$

This improper integral converges exactly when $s>0$, and in that case

$$
\bbox[4pt,border: 1px solid lightgray]{\mathcal L\{u(t)\} = \dfrac{1}{s},\qquad s>0.}
$$

**Note.** As far as Laplace transforms are concerned, $u(t)$ and the constant function $1$ are *indistinguishable*, because both equal $1$ for all

$$
t\ge 0
$$

.

---

<a id="taking-laplace-transforms-of-piecewise-functions"></a>
## Taking Laplace Transforms of Piecewise Functions

When $f(t)$ is defined by different formulas on different intervals, the Laplace transform is computed by **splitting the integral at the breakpoints**.

Suppose $f(t)$ has breakpoints at $t=a$ and $t=b$, where $0 < a < b$, so that

$$
{f_{1}(t),\begin{vmatrix}0 \le t < a, \\ f_{2}(t),\end{vmatrix}a \le t < b,; f_{3}(t), \mid t \ge b
$$

Then, we have

$$
\mathcal L\{f(t)\} = \int_0^a e^{-st}f_1(t)\,\textrm d t + \int_a^b e^{-st}f_2(t)\,\textrm d t + \int_b^\infty e^{-st}f_3(t)\,\textrm d t
$$

**Key Considerations:**

- **Computation:** Each piece is usually a standard definite or improper integral.
- **Domain:** The Laplace transform exists only for those $s$ where *every improper integral you create is finite*. The final domain is the *intersection* of the domains needed for each piece.

In this lesson, the only improper integrals we will encounter are of the form
$\int_c^\infty e^{(\alpha-s)t}\,\textrm d t$,
which converges exactly when $s>\alpha$.

Next, we will apply this method to a concrete example.

---

<a id="finding-laplace-transforms-of-step-functions"></a>
## Finding Laplace Transforms of Step Functions

**Example:** Use the definition of Laplace transforms to calculate $F(s) = \mathcal L\{f(t)\}$ for $f(t) = u(t - 2) + u(t-6)$, where $u$ is the unit step function.

**Explanation**

We apply the definition of the Laplace transform:

$$
\begin{aligned}
L{f(t)} &= F(s) = ∫_{0}^{∞}f(t)e^{-st}dt
\end{aligned}
$$

Recall that the unit step function is defined as

$$
u(t) = {0,\begin{vmatrix}t < 0 \\ 1,\end{vmatrix}t \ge 0
$$

So, for the piecewise functions $u(t-2)$ and $u(t-6)$, we have

$$
\begin{aligned}
u(t - 2) &= {0,\begin{vmatrix}t < 2 \\ 1,\end{vmatrix}t \ge 2 \\
u(t - 6) &= {0,\begin{vmatrix}t < 6 \\ 1,\end{vmatrix}t \ge 6
\end{aligned}
$$

Therefore,

$$
{0,\begin{vmatrix}t < 2 \\ 1,\end{vmatrix}2 \le t < 6; 2, \mid t \ge 6
$$

So, for our Laplace transform, we have

$$
\begin{aligned}
F(s) &= ∫_{0}^{∞}f(t)e^{-st}dt \\
&= ∫_{0}^{2}0 \cdot e^{-st}dt + ∫_{2}^{6}1 \cdot e^{-st}dt + ∫_{6}^{∞}2 \cdot e^{-st}dt \\
&= ∫_{2}^{6}e^{-st}dt + ∫_{6}^{∞}2e^{-st}dt
\end{aligned}
$$

Note that the second integral diverges for

$$
s \leq 0
$$

For $s > 0$, we calculate our Laplace transform as follows:

$$
\begin{aligned}
F(s) &= ∫_{2}^{6}e^{-st}dt + ∫_{6}^{∞}2e^{-st}dt \\
&= ∫_{2}^{6}e^{-st}dt + 2lim_(a → ∞)∫_{6}^{a}e^{-st}dt \\
&= [- \frac{e^{-st}}{s}]_{2}^{6} + 2lim_(a → ∞)(-\frac{e^{-st}}{s}) \mid _{6}^{a} \\
&=-\frac{e^{-6s}}{s} + \frac{e^{-2s}}{s} - \frac{2}{s} \cdot lim_(a → ∞)(e^{-sa} - e^{-6s}) \\
&= \frac{e^{-2s}}{s} - \frac{e^{-6s}}{s} - \frac{2}{s}(0 - e^{-6s}) \\
&= \frac{e^{-2s}}{s} - \frac{e^{-6s}}{s} + \frac{2e^{-6s}}{s} \\
&= \frac{e^{-2s}}{s} + \frac{e^{-6s}}{s}
\end{aligned}
$$

Therefore, the Laplace transform of $f(t)$ is

$$
\mathcal{L}\left\{f(t)\right\} =\dfrac{e^{-2s}}{s} +\dfrac{e^{-6s}}{s}, \qquad s > 0
$$

---

**Question 1**

Use the definition of Laplace transforms to calculate $F(s) = L{f(t)}$, where $f(t) = u(t - 5)$ and $u$ is the unit step function.$F(s) = L{f(t)} =$
$\underline{\hspace{1.5cm}}$,
valid for $s$
$\underline{\hspace{1.5cm}}$ $\underline{\hspace{1.5cm}}$.

---

**Question 2**

```quiz
type: radio
id: ma-64241
content: |-
  Find $L{f(t)}$ for the piecewise function defined by

  $f(t) = {0,\begin{vmatrix}t < 1 \\ 1,\end{vmatrix}1 \le t \le 2; 0, \mid t > 2$.
options:
- id: a
  content: |-
    $L{f(t)} = e^{-s} - e^{-2s}$
- id: b
  content: |-
    $L{f(t)} = \frac{1}{s}(e^{-s} - 1)$
- id: c
  content: |-
    $L{f(t)} = \frac{1}{s}(e^{-s} - e^{-2s})$
  correct: true
- id: d
  content: |-
    $L{f(t)} = \frac{1}{s}(1 - e^{-2s})$
- id: e
  content: |-
    $L{f(t)} = 1 - e^{-2s}$
```

---

<a id="the-general-result-for-the-shifted-unit-step-function"></a>
## The General Result for the Shifted Unit Step Function

As we've seen, the *unit step function* shifted by $a>0$ is defined by

$$
{0,\begin{vmatrix}t < a, \\ 1,\end{vmatrix}t \ge a
$$

To compute its Laplace transform, we apply the definition. Since $u(t-a) = 0$ for $t < a$, the integral vanishes on the interval $[MATH: [0,a){:}]$

$$
\mathcal L\{u(t-a)\} = \int_0^\infty e^{-st}u(t-a)\,\textrm d t = \int_a^\infty e^{-st}\,\textrm d t
$$

For $s>0$, this improper integral converges and evaluates to

$$
\int_a^\infty e^{-st}\,\textrm d t = \left[-\dfrac{e^{-st}}{s}\right]_a^\infty = \dfrac{e^{-as}}{s}
$$

Therefore, the **Laplace transform of the shifted unit step function** is

$$
\mathcal L\{u(t-a)\} = \dfrac{e^{-as}}{s}, \qquad s>0
$$

This result will be used repeatedly to simplify Laplace transforms of piecewise-defined functions involving step functions.

---

<a id="finding-laplace-transforms-of-other-piecewise-continuous-functions"></a>
## Finding Laplace Transforms of Other Piecewise Continuous Functions

**Example:** Use the definition of Laplace transforms to calculate $F(s) = \mathcal L\{f(t)\}$, where $f(t)$ is given by
${e^{2t},\begin{vmatrix}t \le 3 \\ 1\end{vmatrix}t > 3$.

**Explanation**

We apply the definition of the Laplace transform:

$$
\begin{aligned}
L{f(t)} &= F(s) = ∫_{0}^{∞}f(t)e^{-st}dt
\end{aligned}
$$

So, for the piecewise function

$$
{e^{2t},\begin{vmatrix}t \le 3 \\ 1\end{vmatrix}t > 3
$$

we have

$$
\begin{aligned}
F(s) &= ∫_{0}^{∞}f(t)e^{-st}dt \\
&= ∫_{0}^{3}e^{2t} \cdot e^{-st}dt + ∫_{3}^{∞}1 \cdot e^{-st}dt \\
&= ∫_{0}^{3}e^{2t - st}dt + ∫_{3}^{∞}e^{-st}dt \\
&= ∫_{0}^{3}e^{(2 - s)t}dt + ∫_{3}^{∞}e^{-st}dt
\end{aligned}
$$

Note that the improper integral diverges if

$$
s \leq 0
$$

For $s > 0$, we calculate our Laplace transform as follows:

$$
\begin{aligned}
F(s) &= ∫_{0}^{3}e^{(2 - s)t}dt + ∫_{3}^{∞}e^{-st}dt \\
&= \frac{1}{2 - s}[e^{(2 - s)t}]_{0}^{3} + ∫_{3}^{∞}e^{-st}dt \\
&= (e^{3(2 - s)} - 1)/(2 - s) + lim_(a → ∞)∫_{3}^{a}e^{-st}dt \\
&= (e^{3(2 - s)} - 1)/(2 - s) + lim_(a → ∞)(\frac{-e^{-st}}{s}) \mid _{3}^{a} \\
&= (e^{3(2 - s)} - 1)/(2 - s) - \frac{1}{s} \cdot lim_(a → ∞)(e^{-as} - e^{-3s}) \\
&= (e^{3(2 - s)} - 1)/(2 - s) - \frac{1}{s} \cdot (0 - e^{-3s}) \\
&= (e^{3(2 - s)} - 1)/(2 - s) + \frac{e^{-3s}}{s}
\end{aligned}
$$

valid for $s > 0$.

---

**Question 3**

Use the definition of Laplace transforms to calculate $F(s) = L{f(t)}$, where $f(t)$ is given by
$f(t) = {1,\begin{vmatrix}t \le 2 \\ e^{-t}\end{vmatrix}t > 2$.$F(s) = L{f(t)} =$ $\underline{\hspace{2cm}}$,
valid for $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] [MATH: (1)/(s) − (e^{− 2s})/(s) + (e^{− 2(1 − s)})/(s − 1)]
- [ ] [MATH: (1)/(s) + (e^{− 2s})/(s) + (e^{− 2(1 + s)})/(s + 1)]
- [ ] [MATH: (1)/(s) − (e^{− 2s})/(s) − (e^{− 2(1 + s)})/(s + 1)]
- [ ] [MATH: (1)/(s) − (e^{− 2s})/(s) + (e^{− 2(1 + s)})/(s + 1)]

#### Select 2

- [ ] [MATH: s > − 1]
- [ ] [MATH: s ≥ 1]
- [ ] [MATH: s ≥ − 1]
- [ ] [MATH: s > 1]
- [ ] [MATH: s ≥ 0]
- [ ] [MATH: s > 0]

---

**Question 4**

Use the definition of Laplace transforms to calculate $F(s) = L{f(t)}$, where $f(t)$ is given by
$f(t) = {e^{-2t},\begin{vmatrix}t \le 3 \\ 1\end{vmatrix}t > 3$.$F(s) = L{f(t)} =$ $\underline{\hspace{2cm}}$,
valid for $\underline{\hspace{2cm}}$.

#### Select 1

- [ ] [MATH: (1 − e^{− 3(2 + s)})/(2 + s) + (e^{− 3s})/(s)]
- [ ] [MATH: (e^{− 3(2 + s)})/(s + 2) + (e^{− 3s})/(s)]
- [ ] [MATH: (e^{3(2 + s)})/(s + 2) + (e^{− 3s})/(s)]
- [ ] [MATH: (e^{− 3(2 + s)} − 1)/(s + 2) − (e^{− 3s})/(s)]

#### Select 2

- [ ] [MATH: s > − 2]
- [ ] [MATH: s ≥ 3]
- [ ] [MATH: s ≥ − 2]
- [ ] [MATH: s > 0]
- [ ] [MATH: s > 3]
- [ ] [MATH: s ≥ 0]

---

<a id="uniqueness-of-laplace-transforms"></a>
## Uniqueness of Laplace Transforms

Along with existence, the Laplace transform also satisfies an important **uniqueness** property.

Suppose $f$ and $g$ are *piecewise continuous* on $[MATH: [0,\infty)]$ and of exponential order $s_0$, and that their Laplace transforms

$$
F(s)=\mathcal{L}\{f(t)\}, \qquad G(s)=\mathcal{L}\{g(t)\}, \qquad s>s_0
$$

satisfy

$$
F(s)=G(s) \quad \text{for all} s>s_0
$$

Then

$$
f(t)=g(t) \quad \text{for almost every} t\ge 0
$$

Moreover, if $f$ and $g$ are continuous, then

$$
f(t)=g(t) \quad \text{for all} t\ge 0
$$

To understand the meaning of *almost every*, consider the following piecewise continuous functions:

$$
{0,\begin{vmatrix}t < 1, \\ 1,\end{vmatrix}t \ge 1
$$

These functions differ only at the single point $t=1$. Nevertheless,

$$
\mathcal L\{f(t)\} = \mathcal L\{g(t)\} = \mathcal L\{h(t)\} = \dfrac{e^{-s}}{s}
$$

Changing the value of a piecewise continuous function at finitely or countably many isolated points does not affect its Laplace transform, since the transform is defined using an integral.

In short, for piecewise continuous functions, the Laplace transform determines the function completely, except for its values at isolated jump points.

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS2/0. Table of Contents/TOC|Table of Contents]]
