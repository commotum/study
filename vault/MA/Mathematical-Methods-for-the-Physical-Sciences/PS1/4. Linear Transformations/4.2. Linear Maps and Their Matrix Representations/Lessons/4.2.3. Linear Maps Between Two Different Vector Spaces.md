# Linear Maps Between Two Different Vector Spaces

<!--
lesson-id: 907
topic-code: PS1.4.2.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Standard Matrix of a Linear Transformation](#finding-the-standard-matrix-of-a-linear-transformation)
- [Determining the Components of a Linear Map](#determining-the-components-of-a-linear-map)
- [Finding the Image of a Vector Under a Linear Map](#finding-the-image-of-a-vector-under-a-linear-map)

## Prerequisites

- [The Standard Matrix of a Linear Transformation](<../../../../PCL/6. Matrices/6.5. Linear Transformations/Lessons/6.5.2. The Standard Matrix of a Linear Transformation.md>)

---

<a id="introduction"></a>
## Introduction

Similar to transformations in a vector space, we can define a transformation between two *different* vector spaces. For instance, consider the transformation that maps the vector

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

from $\mathbb{R}^2$ to the vector

$$
\begin{bmatrix}x + y \\ x - y \\ 2x + 2y\end{bmatrix}
$$

in $\mathbb{R}^3$, which can be written as

$$
\begin{bmatrix}x \\ y\end{bmatrix}
$$

The *standard matrix* of this transformation is

$$
\begin{bmatrix}1 & 1 \\ 1 & -1 \\ 2 & 2\end{bmatrix}
$$

Recall that a transformation $\mathbf{T}: V \to W$ between two vector spaces $V$ and $W$ is *linear* if for any vectors $\mathbf{v}_1, \mathbf{v}_2\in V$ and any scalar $k\in\mathbb{R}$, the following conditions are satisfied:

- $\mathbf{T}(k\mathbf{v}_1) = k\mathbf{T}(\mathbf{v}_1)$
- $\mathbf{T}(\mathbf{v}_1+\mathbf{v}_2) = \mathbf{T}(\mathbf{v}_1)+\mathbf{T}(\mathbf{v}_2)$

It is easy to check that if a transformation can be represented as a matrix, then it is a linear transformation. Therefore, the transformation $\mathbf{S}$ is a linear transformation.

In general, if $V$ and $W$ are $n$-dimensional and $m$-dimensional vector spaces respectively, then the standard matrix of a linear transformation $\mathbf{T}\!: V \!\rightarrow W$ is an $m\times n$ matrix $T$.

---

<a id="finding-the-standard-matrix-of-a-linear-transformation"></a>
## Finding the Standard Matrix of a Linear Transformation

**Example:** Find the standard matrix that represents the linear transformation $\mathbf{T}:\mathbb{R}^3 \to \mathbb{R}^2$, where
$\begin{bmatrix}x \\ y \\ z\end{bmatrix}$

**Explanation**

We require a $2\times 3$ matrix ${T}$ such that
$\begin{bmatrix}x \\ y \\ z\end{bmatrix}$

Notice that
$\begin{bmatrix}2 & -3 & 0 \\ 1 & 1 & -1\end{bmatrix}$
Therefore, the matrix which represents the linear transformation $\mathbf{T}$ is
$\begin{bmatrix}2 & -3 & 0 \\ 1 & 1 & -1\end{bmatrix}$

---

**Question 1:**

```quiz
type: radio
id: ma-8052
content: |-
  Find the standard matrix that represents the linear transformation $T: R^{3} → R^{2}$, where

  $T: \begin{bmatrix}x \\ y \\ z\end{bmatrix}⟶\begin{bmatrix}x - 3y + 8z \\ x + z + 2y\end{bmatrix}$.
options:
- id: a
  content: |-
    $\begin{bmatrix}1 & 1 & 1 \\ -3 & 1 & -3\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}1 & -3 & 8 \\ 1 & 1 & 2 \\ 0 & 0 & 0\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}1 & 1 & 1 \\ 0 & -3 & 0\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}1 & 1 \\ 1 & -3 \\ 2 & 8\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}1 & -3 & 8 \\ 1 & 2 & 1\end{bmatrix}$
  correct: true
```

---

**Question 2:**

```quiz
type: radio
id: ma-8030
content: |-
  Find the standard matrix that represents the linear transformation $T: R^{3} → R^{2}$, where

  $T: \begin{bmatrix}x \\ y \\ z\end{bmatrix}⟶\begin{bmatrix}2x - y + z \\ x - y - z\end{bmatrix}$.
options:
- id: a
  content: |-
    $\begin{bmatrix}0 & 0 \\ 0 & 0 \\ 2 & -5 \\ 1 & 1\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}2 & -1 & 1 \\ 1 & -1 & 1 \\ 0 & 0 & 0\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}2 & 1 & 0 & 0 \\ -5 & 1 & 0 & 0\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}2 & -1 & 1 \\ 1 & -1 & -1\end{bmatrix}$
  correct: true
- id: e
  content: |-
    $\begin{bmatrix}2 & 1 \\ 1 & 1 \\ 1 & 0 \\ 0 & 1\end{bmatrix}$
```

---

<a id="determining-the-components-of-a-linear-map"></a>
## Determining the Components of a Linear Map

**Example:** $\begin{bmatrix}-8 & -5 \\ -3 & -6 \\ -4 & 5\end{bmatrix}$
Consider the standard matrix $T$ of the linear transformation $\mathbf{T}:\mathbb{R}^2 \to \mathbb{R}^3$ shown above. What is the value of $a-b$?

**Explanation**

Given the vector $\begin{bmatrix}x \\ y\end{bmatrix}$ we have

$$
\begin{aligned}
\begin{bmatrix}-8 & -5 \\ -3 & -6 \\ -4 & 5\end{bmatrix} \cdot \begin{bmatrix}x \\ y\end{bmatrix} &= \begin{bmatrix}-8x - 5y \\ -3x - 6y \\ -4x + 5y\end{bmatrix} \\
&= \begin{bmatrix}-5y - 8x \\ -3x - 6y \\ 5y - 4x\end{bmatrix}
\end{aligned}
$$

Therefore, the linear transformation $\mathbf{T}$ is given by
$\begin{bmatrix}x \\ y\end{bmatrix}$

Finally,

$$
a-b=(-5)-(-4)=-1
$$

---

**Question 3**

```quiz
type: radio
id: ma-38271
content: |-
  $T = \begin{bmatrix}2 & -3 & -2 \\ -3 & -1 & 4\end{bmatrix}, T: \begin{bmatrix}x \\ y \\ z\end{bmatrix}⟶\begin{bmatrix}ay + 2x - 2z \\ bz - 3x + cy\end{bmatrix}$

  Consider the standard matrix $T$ of the linear transformation $T: R^{3} → R^{2}$ shown above. What is the value of $a + b + c$?
options:
- id: a
  content: |-
    $0$
  correct: true
- id: b
  content: |-
    $2$
- id: c
  content: |-
    $-3$
- id: d
  content: |-
    $-1$
- id: e
  content: |-
    $5$
```

---

**Question 4:**

```quiz
type: radio
id: ma-38272
content: |-
  $T = \begin{bmatrix}-3 & 1 \\ -2 & -8 \\ -9 & 7\end{bmatrix}, T: \begin{bmatrix}x \\ y\end{bmatrix}⟶\begin{bmatrix}-3x + y \\ ax - 8y \\ -9x + by\end{bmatrix}$

  Consider the standard matrix $T$ of the linear transformation $T: R^{2} → R^{3}$ shown above. What is the value of $a + b$?
options:
- id: a
  content: |-
    $3$
- id: b
  content: |-
    $-2$
- id: c
  content: |-
    $-1$
- id: d
  content: |-
    $0$
- id: e
  content: |-
    $5$
  correct: true
```

---

<a id="finding-the-image-of-a-vector-under-a-linear-map"></a>
## Finding the Image of a Vector Under a Linear Map

**Example:** Consider the matrix $T$ and the vector $\mathbf{v}$ given below. If $T$ is the standard matrix of the linear transformation $\mathbf{T}:\mathbb{R}^3 \to \mathbb{R}^2$, find $\mathbf{T}(\mathbf{v})$.

$\begin{bmatrix}1 & -1 & 2 \\ 3 & 0 & 1\end{bmatrix}$

**Explanation**

Using the fact that

$$
\,\mathbf{T}(\mathbf{v})=T\cdot \mathbf{v}
$$

we get

$$
\begin{aligned}
T(v) &= T \cdot v \\
&= \begin{bmatrix}1 & -1 & 2 \\ 3 & 0 & 1\end{bmatrix} \cdot \begin{bmatrix}2 \\ 3 \\ 1\end{bmatrix} \\
&= \begin{bmatrix}1 \\ 7\end{bmatrix}
\end{aligned}
$$

---

**Question 5**

```quiz
type: radio
id: ma-38270
content: |-
  Consider the matrix $T$ and the vector $v$ given below. If $T$ is the standard matrix of the linear transformation $T: R^{2} → R^{3}$, find $T(v)$.

  $T = \begin{bmatrix}1 & -3 \\ -3 & 1 \\ 4 & -2\end{bmatrix}, v = \begin{bmatrix}4 \\ -2\end{bmatrix}$
options:
- id: a
  content: |-
    $\begin{bmatrix}0 \\ -4 \\ 0\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}14 \\ -10 \\ 20\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}1 \\ -1 \\ 2\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}10 \\ -14 \\ 20\end{bmatrix}$
  correct: true
- id: e
  content: |-
    $\begin{bmatrix}20 \\ -14 \\ 10\end{bmatrix}$
```

---

**Question 6**

```quiz
type: radio
id: ma-38262
content: |-
  Consider the matrix $T$ and the vector $v$ given below. If $T$ is the standard matrix of the linear transformation $T: R^{3} → R^{2}$, find $T(v)$.

  $T = \begin{bmatrix}1 & 2 & -3 \\ 3 & -5 & 1\end{bmatrix}, v = \begin{bmatrix}4 \\ -3 \\ 2\end{bmatrix}$
options:
- id: a
  content: |-
    $\begin{bmatrix}-8 \\ 29\end{bmatrix}$
  correct: true
- id: b
  content: |-
    $\begin{bmatrix}-18 \\ 29\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}12 \\ -21\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}-9 \\ 28\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}28 \\ -19\end{bmatrix}$
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
