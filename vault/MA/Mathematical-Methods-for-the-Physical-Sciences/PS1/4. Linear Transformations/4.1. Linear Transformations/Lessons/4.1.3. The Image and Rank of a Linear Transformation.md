# The Image and Rank of a Linear Transformation

<!--
lesson-id: 1963
topic-code: PS1.4.1.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Determining a Component of a Vector in the Image of a Linear Transformation](#determining-a-component-of-a-vector-in-the-image-of-a-linear-transformation)
- [Determining If a Vector Lies in the Image](#determining-if-a-vector-lies-in-the-image)
- [Determining Whether a Vector Is in the Image of a Linear Transformation](#determining-whether-a-vector-is-in-the-image-of-a-linear-transformation)
- [The Rank of a Linear Transformation](#the-rank-of-a-linear-transformation)
- [Calculating the Rank of a Linear Transformation](#calculating-the-rank-of-a-linear-transformation)

## Prerequisites

- [The Rank of a Matrix](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.4. Dimension and Rank in N-Dimensional Space/Lessons/3.4.2. The Rank of a Matrix.md>)
- [The Standard Matrix of a Linear Transformation](<../../../../PCL/6. Matrices/6.5. Linear Transformations/Lessons/6.5.2. The Standard Matrix of a Linear Transformation.md>)

---

<a id="introduction"></a>
## Introduction

The **image** (or **range**) of a linear transformation $\mathbf{T}$ is the set of all vectors $\mathbf{b}$ that the transformation maps to. That is to say
$T(u) = b$

The diagram below gives a schematic representation of the image of a linear transformation $\mathbf{T}: V \rightarrow V$ for a vector space $V$.

![](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/4. Linear Transformations/4.1. Linear Transformations/Source/4.1.3. The Image and Rank of a Linear Transformation/Images/20ce81e90f38a9ccf23d9f9ae97025c0.png>)

Notice that $\mathbf{0}$ is always in the image of a linear transformation since

$$
\mathbf{T}(\mathbf{0})=\mathbf{0}
$$

As an example, let's consider the linear transformation
$\begin{bmatrix}x \\ y\end{bmatrix}$

Notice that, for example, the vector

$$
\mathbf b = [6,9]^T
$$

is in the image of the transformation $\mathbf{S}$ since there is another vector

$$
\mathbf{u} = [1,1]^T
$$

such that

$$
\mathbf{S}(\mathbf{u})=\mathbf{b}{:}
$$

$$
\begin{bmatrix}1 \\ 1\end{bmatrix}
$$

---

<a id="determining-a-component-of-a-vector-in-the-image-of-a-linear-transformation"></a>
## Determining a Component of a Vector in the Image of a Linear Transformation

**Example:** Let $\begin{bmatrix}x \\ y \\ z\end{bmatrix}$ be a linear transformation. Determine the value of $k$ for which the vector $\begin{bmatrix}4 \\ k \\ 8\end{bmatrix}$ belongs to $\textrm{Im}(\mathbf{T})$.

**Explanation**

The vector $\mathbf{v}$ belongs to $\textrm{Im}(\mathbf{T})$ if there exists a vector $\begin{bmatrix}x \\ y \\ z\end{bmatrix}$ such that

$$
\mathbf{T}(\mathbf{u}) = \mathbf{v}
$$

Therefore, we must have

$$
\begin{aligned}
\begin{bmatrix}x + y \\ -x + y \\ 2y\end{bmatrix} &= \begin{bmatrix}4 \\ k \\ 8\end{bmatrix}
\end{aligned}
$$

This gives the following system of equations:

$$
{x + y = 4; - x + y = k; 2y = 8
$$

From the third equation, we get $y=4$, which we substitute into the first equation to get

$$
\begin{aligned}
x + y &= 4 \\
x + 4 &= 4 \\
x &= 0
\end{aligned}
$$

Finally, we substitute $x=0$ and $y=4$ into the third equation and obtain

$$
\begin{aligned}
-x + y &= k \\
0 + 4 &= k \\
k &= 4
\end{aligned}
$$

Therefore, $\mathbf{v}\in\textrm{Im}(\mathbf{T})$ when $k=4$.

---

**Question 1:**

```quiz
type: radio
id: ma-38627
content: |-
  Let $T = \begin{bmatrix}3 & -8 & 0 \\ 2 & -4 & 0 \\ -2 & 10 & 0\end{bmatrix}$ be the standard matrix of the linear transformation $T$. Determine the value of $k$ for which the vector $v = \begin{bmatrix}6 \\ 8 \\ k\end{bmatrix}$ belongs to $Im(T)$.
options:
- id: a
  content: |-
    $1$
- id: b
  content: |-
    $4$
- id: c
  content: |-
    $-4$
- id: d
  content: |-
    $10$
  correct: true
- id: e
  content: |-
    $-5$
```

---

**Question 2:**

```quiz
type: radio
id: ma-38615
content: |-
  Let $T: \begin{bmatrix}x \\ y \\ z\end{bmatrix}⟶\begin{bmatrix}-2x + y \\ -x + 4y \\ -3x - 5y\end{bmatrix}$ be a linear transformation. Determine the value of $k$ for which the vector $v = \begin{bmatrix}6 \\ -4 \\ k\end{bmatrix}$ belongs to $Im(T)$.
options:
- id: a
  content: |-
    $22$
  correct: true
- id: b
  content: |-
    $-12$
- id: c
  content: |-
    $-2$
- id: d
  content: |-
    $20$
- id: e
  content: |-
    $2$
```

---

<a id="determining-if-a-vector-lies-in-the-image"></a>
## Determining If a Vector Lies in the Image

In general, the image of a linear transformation $\mathbf{T}$ is equivalent to the column space of its standard matrix $T$. That is,
$T$

So to find out if a vector $\mathbf{b}$ is in the image of the linear transformation $\mathbf{T}$, we just need to check if the matrix equation

$$
T\mathbf{x}=\mathbf{b}
$$

is consistent.

---

<a id="determining-whether-a-vector-is-in-the-image-of-a-linear-transformation"></a>
## Determining Whether a Vector Is in the Image of a Linear Transformation

**Example:** The standard matrix $T$ of a linear transformation $\mathbf{T}$ and the vector $\mathbf{b} \in \mathbb{R}^3$ are given below. Determine if there exists a vector $\mathbf{x} \in \mathbb{R}^3$ such that $\mathbf{T}(\mathbf{x})=\mathbf{b}$. In the affirmative case, find such a vector.

$\begin{bmatrix}2 & 4 & 2 \\ 6 & 4 & -10 \\ 0 & 4 & 8\end{bmatrix}$

**Explanation**

Since $T$ is the standard matix of $\mathbf{T}$, we have

$$
\textrm{Im}(\mathbf{T}) = \textrm{Col}(T)
$$

In order to determine whether $\mathbf{b}$ lies in $\textrm{Im}(\mathbf{T})$, we need to check if the equation

$$
T\mathbf{x}=\mathbf{b}
$$

is consistent.

So, we find the row echelon form of the augmented matrix $\begin{bmatrix}T \, & \, \mathbf{b}\end{bmatrix}$, as follows:

$$
\begin{aligned}
\begin{bmatrix}T & b\end{bmatrix} &= \begin{bmatrix}2 & 4 & 2 & 6 \\ 6 & 4 & -10 & 26 \\ 0 & 4 & 8 & -4\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} + (-3)R_{1} \\ ∼\begin{bmatrix}2 & 4 & 2 & 6 \\ 0 & -8 & -16 & 8 \\ 0 & 4 & 8 & -4\end{bmatrix}\end{vmatrix}R_{3}:=R_{3} + \frac{1}{2}R_{2} \\
&= ∼\begin{bmatrix}2 & 4 & 2 & 6 \\ 0 & -8 & -16 & 8 \\ 0 & 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

The reduced matrix above has a zero row, and so the system is consistent. We have two pivot columns (the $1$st and the $2$nd). So, $x_3$ is a free variable.
Now, from the second equation, we get

$$
-8x_2-16x_3 = 8 \qquad\Longrightarrow\qquad x_2=-1-2x_3
$$

Substituting this into the first equation gives

$$
2x_1+4(-1-2x_3)+2x_3 = 6 \qquad\Longrightarrow\qquad x_1=5+3x_3
$$

As a result, the general solution is given by
$\begin{bmatrix}5 + 3x_{3} \\ -1 - 2x_{3} \\ x_{3}\end{bmatrix}$

Setting $x_3=0$, we have
$\begin{bmatrix}5 \\ -1 \\ 0\end{bmatrix}$

---

**Question 3**

```quiz
type: radio
id: ma-38007
content: |-
  The linear transformation $T$ and the vector $b ∈ R^{4}$ are given below. Determine if there exists a vector $x ∈ R^{4}$ such that $T(x) = b$.
  In the affirmative case, find the value of $x_{1} - x_{3}$, where $x_{1}, x_{2}, x_{3}, x_{4}$ are the components of $x$.

  $T: \begin{bmatrix}x_{1} \\ x_{2} \\ x_{3} \\ x_{4}\end{bmatrix}⟶\begin{bmatrix}x_{1} + x_{2} - x_{4} \\ 2x_{2} - x_{1} + 3x_{3} + 4x_{4} \\ 2x_{1} + x_{2} - x_{3} - 3x_{4} \\ 0\end{bmatrix}, b = \begin{bmatrix}3 \\ 6 \\ -3 \\ 0\end{bmatrix}$
options:
- id: a
  content: |-
    $b ∉ Im(T)$
  correct: true
- id: b
  content: |-
    $5$
- id: c
  content: |-
    $\frac{1}{5}$
- id: d
  content: |-
    $-\frac{3}{4}$
- id: e
  content: |-
    $3$
```

---

**Question 4**

```quiz
type: radio
id: ma-38001
content: |-
  The standard matrix $T$ of a linear transformation $T$ and the vector $b ∈ R^{3}$ are given below. Determine if there exists a vector $x ∈ R^{3}$ such that $T(x) = b$.
  In the affirmative case, find the value of $x_{1} + x_{2}$, where $x_{1}, x_{2}, x_{3}$ are the components of $x$.

  $T = \begin{bmatrix}-2 & -5 & 3 \\ 4 & -1 & 5 \\ 0 & 3 & -3\end{bmatrix}, b = \begin{bmatrix}9 \\ -7 \\ -3\end{bmatrix}$
options:
- id: a
  content: |-
    $\frac{2}{3}$
- id: b
  content: |-
    $-3$
  correct: true
- id: c
  content: |-
    $b ∉ Im(T)$
- id: d
  content: |-
    $1$
- id: e
  content: |-
    $4$
```

---

<a id="the-rank-of-a-linear-transformation"></a>
## The Rank of a Linear Transformation

In general, the rank of a linear transformation $\mathbf{T}$ is equal to the dimension of its image:

$$
\text{rank}(\mathbf{T}) = \text{dim}(\text{Im}(\mathbf{T}))
$$

If the linear transformation $\mathbf{T}$ has the standard matrix $T$, then the dimension of the image is equal to the rank of $T{:}$

$$
\text{dim}(\text{Im}(\mathbf{T})) = \text{rank}(T)
$$

So, in general, we have

$$
\text{rank}(\mathbf{T}) = \text{rank}(T)
$$

---

<a id="calculating-the-rank-of-a-linear-transformation"></a>
## Calculating the Rank of a Linear Transformation

**Example:** If $\begin{bmatrix}2 & -3 & -1 & -2 \\ -6 & 5 & -1 & 2 \\ 4 & 1 & 5 & 3 \\ 0 & 0 & 0 & 0\end{bmatrix}$ is the standard matrix of the linear transformation $\mathbf{T}$, then what is $\text{rank}(\mathbf{T})$?

**Explanation**

In general,

$$
\text{rank}(\mathbf{T}) = \text{dim}(\text{Im}(\mathbf{T})) = \text{rank}(T)
$$

So, we need to find the rank of the matrix $T$. We reduce $T$ to row echelon form, as follows:

$$
\begin{aligned}
T &= \begin{bmatrix}2 & -3 & -1 & -2 \\ -6 & 5 & -1 & 2 \\ 4 & 1 & 5 & 3 \\ 0 & 0 & 0 & 0\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} + 3R_{1} \\ R_{3}:=R_{3} + (-2)R_{1} \\ ∼\begin{bmatrix}2 & -3 & -1 & -2 \\ 0 & -4 & -4 & -4 \\ 0 & 7 & 7 & 7 \\ 0 & 0 & 0 & 0\end{bmatrix}\end{vmatrix}R_{3}:=R_{3} + \frac{7}{4}R_{2} \\
&= ∼\begin{bmatrix}2 & -3 & -1 & -2 \\ 0 & -4 & -4 & -4 \\ 0 & 0 & 0 & 0 \\ 0 & 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

Therefore, since $T$ has $2$ pivot columns, we have that

$$
\textrm{rank}(\mathbf{T})=\textrm{rank}(T)=2
$$

---

**Question 5:**

```quiz
type: radio
id: ma-40210
content: |-
  Given the transformation $T: \begin{bmatrix}x \\ y \\ z\end{bmatrix}⟶\begin{bmatrix}x + 2y \\ y - x + 6z \\ 8z - x + 3y\end{bmatrix}$, find its rank.
options:
- id: a
  content: |-
    $2$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $3$
  correct: true
- id: d
  content: |-
    $4$
- id: e
  content: |-
    $1$
```

---

**Question 6:**

```quiz
type: radio
id: ma-38005
content: |-
  If $T = \begin{bmatrix}2 & -1 & 1 \\ -4 & 2 & -2 \\ 6 & -3 & 3\end{bmatrix}$ is the standard matrix of the linear transformation $T$, then $rank(T) =$
options:
- id: a
  content: |-
    $2$
- id: b
  content: |-
    $0$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $4$
- id: e
  content: |-
    $1$
  correct: true
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
