# Linear Dependence and Independence

<!--
lesson-id: 1861
topic-code: PS1.3.1.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Sets of Two Vectors Containing the Zero Vector](#sets-of-two-vectors-containing-the-zero-vector)
- [Finding a Vector That Makes a Set Linearly Dependent](#finding-a-vector-that-makes-a-set-linearly-dependent)
- [Linear Dependence and Independence for a General Set of Vectors](#linear-dependence-and-independence-for-a-general-set-of-vectors)
- [General Sets of Vectors Containing the Zero Vector](#general-sets-of-vectors-containing-the-zero-vector)
- [Identifying Linearly Independent Sets by Inspection](#identifying-linearly-independent-sets-by-inspection)
- [Identifying Linearly Independent and Dependent Sets Using Row Reduction](#identifying-linearly-independent-and-dependent-sets-using-row-reduction)

## Prerequisites

- [Linear Span of Vectors in N-Dimensional Euclidean Space](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.1. Vectors in N-Dimensional Space/Lessons/3.1.3. Linear Span of Vectors in N-Dimensional Euclidean Space.md>)

---

<a id="introduction"></a>
## Introduction

We say that two vectors $\mathbf{v}_1$ and $\mathbf{v}_2$ are **linearly independent** if the only solution to the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 = \mathbf{0}
$$

is

$$
x_1 = x_2 = 0
$$

Otherwise, if there exists any solution other than

$$
x_1 = x_2 = 0
$$

then we say that the two vectors $\mathbf{v}_1$ and $\mathbf{v}_2$ are **linearly dependent**.

To illustrate, let's consider two sets of vectors in $\mathbb{R}^2$:

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}
$$

- For the first set ${\color{blue}(\textrm{I})}$, we have
$x_{1}a_{1} + x_{2}a_{2}|= 0; x_{1}\begin{bmatrix}1 \\ 0\end{bmatrix} + x_{2}\begin{bmatrix}0 \\ 1\end{bmatrix}|= 0$.
The above equation is true only if $x_1=x_2=0$. So, the set $\color{blue}(\textrm{I})$ is *linearly independent*.
- On the other hand, for the second set ${\color{red}(\textrm{II})}$, we have
$x_{1}b_{1} + x_{2}b_{2}|= 0; x_{1}\begin{bmatrix}1 \\ 1\end{bmatrix} + x_{2}\begin{bmatrix}-2 \\ -2\end{bmatrix}|= 0$.
The above equation has solutions other than $x_1=x_2=0$. For example, $x_1=2$ and $x_2=1$ gives
$\begin{bmatrix}1 \\ 1\end{bmatrix}$
Therefore, the set $\color{red}(\textrm{II})$ is *not* linearly independent. Rather, it is *linearly dependent*.

**Note:** You may notice that the vectors in the set $\color{red}(\textrm{II})$ were parallel (i.e. multiples of one another), while the vectors in set $\color{blue}(\textrm{I})$ were not. This trend holds in general, for all sets of two vectors:

- a set of two vectors is *linearly dependent* if and only if the two vectors are parallel, while
- a set of two vectors is *linearly independent* if and only if the two vectors are not parallel.

---

<a id="sets-of-two-vectors-containing-the-zero-vector"></a>
## Sets of Two Vectors Containing the Zero Vector

Any set of two vectors that contains the zero vector $\mathbf{0}$ must be linearly dependent. To see this, consider the set

$$
\left\{\mathbf{v}, \mathbf{0} \right\}
$$

We can realize that this set is linearly dependent using either of the following two methods.

**Method 1**

Notice that

$$
0 \cdot \mathbf{v} + 1 \cdot \mathbf{0} = \mathbf{0}
$$

Therefore, the equation

$$
x_1 \mathbf{v} + x_2 \cdot \mathbf{0} = \mathbf{0}
$$

has a solution $x_1=0$ and $x_2=1$, which is different from $x_1=x_2=0$. So, the set is linearly dependent.

**Method 2**

Alternatively, recall that a set of two vectors is linearly dependent if and only if the two vectors are parallel. That is, the two vectors are multiples of each other.

The zero vector $\mathbf{0}$ is parallel to every other vector because it is a multiple of any other vector. For any vector $\mathbf{v}$, we have

$$
\mathbf{0} = 0 \cdot \mathbf{v} \qquad \Longrightarrow \qquad \mathbf{0} \parallel \mathbf{v}
$$

So, because $\mathbf{0}$ is parallel to $\mathbf{v}$, the set

$$
\left\{\mathbf{v}, \mathbf{0} \right\}
$$

is linearly dependent.

---

<a id="finding-a-vector-that-makes-a-set-linearly-dependent"></a>
## Finding a Vector That Makes a Set Linearly Dependent

**Example:** The set $\{\mathbf{a_1},\mathbf{a_2} \}$ is linearly dependent. Which of the following vectors could be $\mathbf{a}_2$ if
$\begin{bmatrix}12 \\ 3 \\ 21\end{bmatrix}$

$\begin{bmatrix}4 \\ 1 \\ 7\end{bmatrix}$

**Explanation**

We say that two vectors $\mathbf{v}_1$ and $\mathbf{v}_2$ are *linearly independent* if the only solution to the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 = \mathbf{0}
$$

is

$$
x_1 = x_2 = 0
$$

Otherwise, the vectors are *linearly dependent*.

A set containing *only two* vectors is linearly *dependent* if and only if the two vectors are parallel.

From the given options, the correct answer is
$\begin{bmatrix}4 \\ 1 \\ 7\end{bmatrix}$
To see why, notice that
$\begin{bmatrix}4 \\ 1 \\ 7\end{bmatrix}$
In other words, the vectors $\mathbf a_1$ and $\mathbf a_2$ are parallel.

To see why this means that $\mathbf a_1$ and $\mathbf a_2$ are linearly dependent, notice that

$$
\begin{aligned}
\frac{1}{3}a_{1} - a_{2} &= 0
\end{aligned}
$$

So, the equation

$$
x_1 \mathbf{a}_1 +x_2 \mathbf{a}_2 = \mathbf{0}
$$

has the solution

$$
x_1=\dfrac 1 3
$$

and $x_2=-1$, which is different from $x_1=x_2=0$. Therefore, the set is linearly dependent.

---

**Question 1:**

```quiz
type: radio
id: ma-34289
content: |-
  The set ${a_{1}, a_{2}}$ is linearly dependent. Which of the following vectors could be $a_{2}$ if

  $a_{1} = \begin{bmatrix}2 \\ -3\end{bmatrix}$?
options:
- id: a
  content: |-
    $\begin{bmatrix}-8 \\ 12\end{bmatrix}$
  correct: true
- id: b
  content: |-
    $\begin{bmatrix}-4 \\ -12\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}12 \\ -4\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}6 \\ 9\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}12 \\ -8\end{bmatrix}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-34291
content: |-
  The set ${a_{1}, a_{2}}$ is linearly dependent. Which of the following vectors could be $a_{2}$ if

  $a_{1} = \begin{bmatrix}1 \\ -4 \\ 2\end{bmatrix}$?
options:
- id: a
  content: |-
    $\begin{bmatrix}-18 \\ -10 \\ 8\end{bmatrix}$
- id: b
  content: |-
    $\begin{bmatrix}5 \\ -20 \\ 15\end{bmatrix}$
- id: c
  content: |-
    $\begin{bmatrix}5 \\ -10 \\ -20\end{bmatrix}$
- id: d
  content: |-
    $\begin{bmatrix}-20 \\ 5 \\ 10\end{bmatrix}$
- id: e
  content: |-
    $\begin{bmatrix}5 \\ -20 \\ 10\end{bmatrix}$
  correct: true
```

---

<a id="linear-dependence-and-independence-for-a-general-set-of-vectors"></a>
## Linear Dependence and Independence for a General Set of Vectors

So far, we've discussed linear dependence and linear independence for sets of two vectors. However, these concepts can be extended to sets of any number of vectors.

A set of $n$ vectors

$$
\left\{\mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_n \right\}
$$

is called **linearly independent** if the only solution to the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 + \ldots + x_n \mathbf{v}_n = \mathbf{0}
$$

is

$$
x_1=x_2=\ldots=x_n=0
$$

Otherwise, if there exists any solution other than

$$
x_1 = x_2 = \ldots = x_n = 0
$$

then we say that the set

$$
\left\{\mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_n \right\}
$$

is **linearly dependent**.

Similar to sets containing two vectors, a general set of $n$ vectors is dependent if *any* two vectors in the set are parallel.

**But be careful!** When working with sets containing more than two vectors, having a linearly dependent set is possible even if *none* of the vectors are parallel!

For example, consider the following set of vectors:

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}
$$

None of the vectors in this set are parallel. However, we have

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}
$$

So, the equation

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}
$$

has a solution $x_1 = 1$, $x_2 = 1$, and $x_3 = -1$, which is different from

$$
x_1 = x_2 = x_3 = 0
$$

Therefore, we conclude that the vectors are linearly dependent.

---

<a id="general-sets-of-vectors-containing-the-zero-vector"></a>
## General Sets of Vectors Containing the Zero Vector

As with sets containing only two vectors, any set of vectors that contains the zero vector $\mathbf{0}$ must be linearly dependent.

To see this, consider the set

$$
\left\{\mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_{n-1}, \mathbf{0} \right\}
$$

Notice that

$$
0 \cdot \mathbf{v}_1 + 0 \cdot \mathbf{v}_2 + \ldots + 0 \cdot \mathbf{v}_{n-1} + 1 \cdot \mathbf{0} = \mathbf{0}
$$

So, the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 + \ldots + x_{n-1} \mathbf{v}_{n-1} + x_n \cdot \mathbf{0} = \mathbf{0}
$$

has the solution

$$
x_1=x_2=\ldots = x_{n-1}=0
$$

and $x_n=1$, which is different from

$$
x_1=x_2=\ldots = x_n=0
$$

Therefore, the set is linearly dependent.

---

<a id="identifying-linearly-independent-sets-by-inspection"></a>
## Identifying Linearly Independent Sets by Inspection

**Example:** Consider the following vectors:

$\begin{bmatrix}4 \\ -1 \\ 0\end{bmatrix}$

Which of the following statements are true?

1. $\mathbf{a}_1 - \mathbf{a}_2 -2 \mathbf{a}_3 = \mathbf{0}$.
2. $x_1\mathbf{a}_1 + x_2\mathbf{a}_2 + x_3\mathbf{a}_3 = \mathbf{0}$ only if $x_1=x_2=x_3 = 0$.
3. The set $\{\mathbf{a}_1, \mathbf{a}_2, \mathbf{a}_3\}$ is linearly independent.

**Explanation**

We say that three vectors $\mathbf{v}_1, \mathbf{v}_2$ and $\mathbf{v}_3$ are *linearly independent* if the only solution to the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 + x_3\mathbf{v}_3= \mathbf{0}
$$

is

$$
x_1 = x_2 = x_3 = 0
$$

Otherwise, the vectors are *linearly dependent*.

With that in mind, let's look at each statement in turn.

- Statement I is false. Indeed:
$a_{1} - a_{2} - 2a_{3}|= \begin{bmatrix}4 \\ -1 \\ 0\end{bmatrix} - \begin{bmatrix}0 \\ 1 \\ 0\end{bmatrix} - 2\begin{bmatrix}2 \\ -1 \\ 7\end{bmatrix}; = \begin{bmatrix}4 \\ -1 \\ 0\end{bmatrix} + \begin{bmatrix}0 \\ -1 \\ 0\end{bmatrix} + \begin{bmatrix}-4 \\ 2 \\ -14\end{bmatrix}; = \begin{bmatrix}4 + 0 - 4 \\ -1 - 1 + 2 \\ 0 + 0 - 14\end{bmatrix}; = \begin{bmatrix}0 \\ 0 \\ -14\end{bmatrix}; \ne 0$
- Statement II is true. The equation $x_1\mathbf{a}_1 + x_2\mathbf{a}_2 + x_3\mathbf{a}_3 = \mathbf{0}$ corresponds to the system
${4x_{1} + 2x_{3} = 0; - x_{1} + x_{2} - x_{3} = 0; 7x_{3} = 0$.
Notice that we can reorder the equations in this system as follows:
${-x_{1} + x_{2} - x_{3} = 0; 4x_{1} + 2x_{3} = 0; 7x_{3} = 0$
Solving this system using back substitution, we find that the solution is $x_1 = x_2 = x_3 = 0$.

- Statement III is true. Since statement II is true, the set $\{\mathbf{a}_1, \mathbf{a}_2, \mathbf{a}_3 \}$ is linearly independent.

Therefore, the correct answer is "II and III only."

---

**Question 3**

```quiz
type: radio
id: ma-155967
content: |-
  Consider the following vectors:

  $a_{1} = \begin{bmatrix}2 \\ 0 \\ 3\end{bmatrix}, a_{2} = \begin{bmatrix}1 \\ 1 \\ 0\end{bmatrix}$

  Which of the following statements are true?

  1. $a_{1} - 2a_{2} = 0$.
  2. $x_{1}a_{1} + x_{2}a_{2} = 0$ only if $x_{1} = x_{2} = 0$.
  3. The set ${a_{1}, a_{2}}$ is linearly independent.
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    I only
- id: c
  content: |-
    II and III only
  correct: true
- id: d
  content: |-
    II only
- id: e
  content: |-
    I and III only
```

---

**Question 4**

```quiz
type: radio
id: ma-156591
content: |-
  Consider the following vectors:

  $a_{1} = \begin{bmatrix}1 \\ 2 \\ 0\end{bmatrix}, a_{2} = \begin{bmatrix}1 \\ 0 \\ 2\end{bmatrix}, a_{3} = \begin{bmatrix}1 \\ 1 \\ 1\end{bmatrix}$.

  Which of the following statements are true?

  1. $a_{1} + a_{2} - 2a_{3} = 0$.
  2. $x_{1}a_{1} + x_{2}a_{2} + x_{3}a_{3} = 0$ only if $x_{1} = x_{2} = x_{3} = 0$.
  3. The set ${a_{1}, a_{2}, a_{3}}$ is linearly independent.
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    I only
  correct: true
- id: c
  content: |-
    II only
- id: d
  content: |-
    II and III only
- id: e
  content: |-
    I and III only
```

---

**Question 5**

```quiz
type: radio
id: ma-155965
content: |-
  Consider the following vectors:

  $a_{1} = \begin{bmatrix}1 \\ -1 \\ 2\end{bmatrix}, a_{2} = \begin{bmatrix}3 \\ 0 \\ 0\end{bmatrix}, a_{3} = \begin{bmatrix}1 \\ 2 \\ 0\end{bmatrix}$.

  Which of the following statements are true?

  1. $2a_{1} - a_{2} + a_{3} = 0$.
  2. $x_{1}a_{1} + x_{2}a_{2} + x_{3}a_{3} = 0$ only if $x_{1} = x_{2} = x_{3} = 0$.
  3. The set ${a_{1}, a_{2}, a_{3}}$ is linearly independent.
options:
- id: a
  content: |-
    II and III only
  correct: true
- id: b
  content: |-
    I and III only
- id: c
  content: |-
    I only
- id: d
  content: |-
    II only
- id: e
  content: |-
    III only
```

---

<a id="identifying-linearly-independent-and-dependent-sets-using-row-reduction"></a>
## Identifying Linearly Independent and Dependent Sets Using Row Reduction

**Example:** Consider the following vectors:

$\begin{bmatrix}2 \\ 4 \\ 0\end{bmatrix}$

Assume that $x_1,x_2$, and $x_3$ are real numbers. Which of the following statements are true?

1. The equation $x_1\mathbf{a}_1+x_2\mathbf{a}_2+x_3\mathbf{a}_3=\mathbf{0}$ has an infinite number of nonzero solutions.
2. The vectors $\mathbf{a}_1,\mathbf{a}_2$, and $\mathbf{a}_3$ are linearly independent.
3. The vectors $\mathbf{a}_1,\mathbf{a}_2$, and $\mathbf{a}_3$ are linearly dependent.

**Explanation**

We say that three vectors $\mathbf{v}_1, \mathbf{v}_2$, and $\mathbf{v}_3$ are *linearly independent* if the only solution to the equation

$$
x_1 \mathbf{v}_1 + x_2 \mathbf{v}_2 + x_3 \mathbf{v}_3 = \mathbf{0}
$$

is

$$
x_1 = x_2 = x_3 = 0
$$

Otherwise, the vectors are *linearly dependent*.

With that in mind, let's examine our statements.

The equation

$$
x_1\mathbf{a}_1+x_2\mathbf{a}_2+x_3\mathbf{a}_3=\mathbf{0}
$$

corresponds to the system

$$
{2x_{1} - x_{2} + 2x_{3} = 0; 4x_{1} + 4x_{2} + 5x_{3} = 0; 6x_{2} + x_{3} = 0
$$

To solve the system, we create its augmented matrix and reduce it using Gaussian elimination:

$$
\begin{aligned}
M &= \begin{bmatrix}2 & -1 & 2 & 0 \\ 4 & 4 & 5 & 0 \\ 0 & 6 & 1 & 0\end{bmatrix}\begin{vmatrix}R_{2}:=R_{2} + (-2)R_{1} \\ ∼\begin{bmatrix}2 & -1 & 2 & 0 \\ 0 & 6 & 1 & 0 \\ 0 & 6 & 1 & 0\end{bmatrix}\end{vmatrix}R_{3}:=R_{3} + (-1)R_{2} \\
&= ∼\begin{bmatrix}2 & -1 & 2 & 0 \\ 0 & 6 & 1 & 0 \\ 0 & 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

From the matrix above, we conclude that the basic variables are $x_1$ and $x_2$, while $x_3$ is the free variable.

Since there is one free variable, the equation

$$
x_1\mathbf{a}_1+x_2\mathbf{a}_2+x_3\mathbf{a}_3=\mathbf{0}
$$

has an infinite number of nonzero solutions, which means that the vectors are linearly dependent.

Therefore, statements II is false, while statements I and III are true.

---

**Question 6**

```quiz
type: radio
id: ma-34273
content: |-
  Consider the following vectors:

  $a_{1} = \begin{bmatrix}1 \\ 2 \\ 0\end{bmatrix}, a_{2} = \begin{bmatrix}-2 \\ 3 \\ 7\end{bmatrix}, a_{3} = \begin{bmatrix}2 \\ 1 \\ -3\end{bmatrix}$.

  Assume that $x_{1}, x_{2}$, and $x_{3}$ are real numbers. Which of the following statements are true?

  1. The equation $x_{1}a_{1} + x_{2}a_{2} + x_{3}a_{3} = 0$ has an infinite number of nonzero solutions.
  2. The vectors $a_{1}, a_{2}$, and $a_{3}$ are linearly dependent.
  3. The vectors $a_{1}, a_{2}$, and $a_{3}$ are linearly independent.
options:
- id: a
  content: |-
    I and II only
- id: b
  content: |-
    None of the statements are true
- id: c
  content: |-
    I and III only
- id: d
  content: |-
    I only
- id: e
  content: |-
    III only
  correct: true
```

---

**Question 7**

```quiz
type: radio
id: ma-34153
content: |-
  Consider the following vectors:

  $a_{1} = \begin{bmatrix}1 \\ 1 \\ 0\end{bmatrix}, a_{2} = \begin{bmatrix}-2 \\ 1 \\ 3\end{bmatrix}, a_{3} = \begin{bmatrix}1 \\ 2 \\ 1\end{bmatrix}$.

  Assume that $x_{1}, x_{2}$, and $x_{3}$ are real numbers. Which of the following statements are true?

  1. The equation $x_{1}a_{1} + x_{2}a_{2} + x_{3}a_{3} = 0$ has only the trivial solution $x_{1} = x_{2} = x_{3} = 0$.
  2. The vectors $a_{1}, a_{2}$, and $a_{3}$ are linearly dependent.
  3. The vectors $a_{1}, a_{2}$, and $a_{3}$ are linearly independent.
options:
- id: a
  content: |-
    None of the statements are true
- id: b
  content: |-
    I and III only
  correct: true
- id: c
  content: |-
    III only
- id: d
  content: |-
    II only
- id: e
  content: |-
    II and III only
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
