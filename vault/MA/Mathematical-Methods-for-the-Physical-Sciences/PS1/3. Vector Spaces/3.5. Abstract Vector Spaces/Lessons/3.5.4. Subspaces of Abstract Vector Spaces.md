# Subspaces of Abstract Vector Spaces

<!--
lesson-id: 1907
topic-code: PS1.3.5.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Example of a Subset That Is Not a Subspace](#example-of-a-subset-that-is-not-a-subspace)
- [Identifying Subspaces of Polynomial Vector Spaces](#identifying-subspaces-of-polynomial-vector-spaces)
- [Identifying Subspaces of Matrix Vector Spaces](#identifying-subspaces-of-matrix-vector-spaces)
- [Identifying Subspaces of Function Vector Spaces](#identifying-subspaces-of-function-vector-spaces)

## Prerequisites

- [Continuity Over an Interval](<1.6.8. Continuity Over an Interval.md>)
- [Defining Abstract Vector Spaces](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.5. Abstract Vector Spaces/Lessons/3.5.2. Defining Abstract Vector Spaces.md>)
- [Subspaces of N-Dimensional Space: Geometric Interpretation](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.2. Subspaces of N-Dimensional Space/Lessons/3.2.2. Subspaces of N-Dimensional Space- Geometric Interpretation.md>)

---

<a id="introduction"></a>
## Introduction

Given a vector space $V$, any subset $H$ of $V$, which is itself a vector space, is called a **subspace** of $V$.

Fortunately, we do not need to check all vector space axioms to determine whether a subset of some vector space is a subspace. Instead, we only need to check the **subspace criterion**, which states the following:

*A subset $H$ of a vector space $V$ is a subspace of $V$ if and only if $H$ is closed under vector addition and scalar multiplication. That is to say, both of the following conditions hold:*

- *If $\mathbf{u}$ and $\mathbf{v}$ lie in $H$, then their sum $\mathbf{u}+\mathbf{v}$ also lies in $H$. Formally,*
$\mathbf{u},\mathbf{v} \in H \quad\Rightarrow\quad \mathbf{u}+\mathbf{v} \in H$.
- *If $\mathbf{u}$ lies in $H$, then $k \cdot \mathbf{u}$ also lies in $H$ for any scalar $k$. Formally,*
$k \in \mathbb{R}, \,\mathbf{u} \in H \quad\Rightarrow\quad k\cdot\mathbf{u} \in H$.

For example, consider the vector space $\mathbb{R}_3[t]$ containing all polynomials of degree at most $3$, along with the subset

$$
A = \{at^2 \: \mid \: a \in \mathbb{R} \}
$$

Is $A$ a subspace? Let's check the two subspace criterion conditions:

- Suppose $p_1(t)=a_1 t^2$ and $p_2(t)=a_2 t^2$ are two polynomials from $A$. Summing the two polynomials, we get
$p_{1}(t) + p_{2}(t)|= (a_{1}t^{2}) + (a_{2}t^{2}); = (a_{1} + a_{2})_(⏟)_(a ∈ R)t^{2}$.
Thus, $p_1(t)+p_2(t)$ is a polynomial of the form $at^2$, which belongs to $A$.
- If $p_1(t)=a_1 t^2 \in A$ and $k \in \mathbb{R}$, then
$kp_{1}(t)|= k(a_{1}t^{2}); = (ka_{1})_(⏟)_(a ∈ R)t^{2}$.
Thus, $kp_1(t)$ is a polynomial of the form $at^2$, which belongs to $A$.

Since both conditions hold, we conclude that $A$ is a subspace $\mathbb{R}_3[t]$.

---

<a id="example-of-a-subset-that-is-not-a-subspace"></a>
## Example of a Subset That Is Not a Subspace

Let's now look at an example of a subset of a vector space that is not a subspace.

Consider the vector space $\mathbb R_2[t]$ of all polynomials in the variable $t$ of degree at most $2$, and its subset

$$
B = \big\{at+b \: \mid \: a,b \in\mathbb{Z} \big\}
$$

Recall that a subset of a vector space is a subspace if it's closed under addition and scalar multiplication.

- $B$ is closed under polynomial addition. Indeed, suppose that $f(t)=a_1t+b_1$ and $g(t)=a_2t+b_2$ are two polynomials from $B$. Then,
$f(t) + g(t)|= (a_{1}t + b_{1}) + (a_{2}t + b_{2}); = (a_{1} + a_{2})t + (b_{1} + b_{2})$,
which is a polynomial from $B$ since $a_1+a_2$ and $b_1+b_2$ are in $\Bbb Z$.
- $B$ is *not* closed under scalar multiplication. As a counterexample, consider $f(t)=t$ and $k=0.5$, then
$kf(t)|= 0.5t$,
which is *not* a polynomial from $B$, because $0.5 \notin \Bbb Z$.

Therefore, $B$ is not a subspace of $\mathbb R_2[t]$.

---

<a id="identifying-subspaces-of-polynomial-vector-spaces"></a>
## Identifying Subspaces of Polynomial Vector Spaces

**Example:** Consider the vector space $\mathbb R_3[t]$ of all polynomials in the variable $t$ of degree at most $3$, and its subset $A = \big\{at^3-bt \: \mid \: a,b \in\mathbb{R} \big\}$.

Which of the following statements are true?

1. $A$ is closed under polynomial addition
2. $A$ is closed under scalar multiplication
3. $A$ is a subspace of $\mathbb R_3[t]$
4. $A$ is **not** a subspace of $\mathbb R_3[t]$

**Explanation**

Recall that a subset of a vector space is a subspace if it's closed under addition and scalar multiplication.

With that in mind, let's examine each statement in turn.

- Statement I is true. If $f(t)=a_1t^3-b_1t$ and $g(t)=a_2t^3-b_2t$ are two polynomials from $A$, then
$f(t) + g(t)|= (a_{1}t^{3} - b_{1}t) + (a_{2}t^{3} - b_{2}t); = (a_{1} + a_{2})t^{3} - (b_{1} + b_{2})t$,
which is a polynomial from $A$. So, $A$ is closed under addition.
- Statement II is true. If $f(t)=at^3-bt \in A$ and $k \in \mathbb{R}$, then
$kf(t)|= k(at^{3} - bt); = (ka)t^{3} - (kb)t$,
which is a polynomial from $A$. So, $A$ is closed under scalar multiplication.
- Statement III is true. $A$ is a subspace because it's closed under addition and scalar multiplication.

Therefore, the correct answer is "I, II, and III only."

---

**Question 1**

Consider the vector space $R_{3}[t]$ of all polynomials in the variable $t$ of degree at most $3$, and its subset $A$ containing all polynomials of the form $at^{3}$, where $a ∈ R$.

Which of the following statements are true?

1. $A$ is closed under polynomial addition
2. $A$ is closed under scalar multiplication
3. $A$ is a subspace of $R_{3}[t]$
4. $A$ is **not** a subspace of $R_{3}[t]$

- [ ] A. I and IV only
- [ ] B. II and IV only
- [ ] C. I only
- [ ] D. IV only
- [ ] E. I, II, and III only

---

**Question 2**

Consider the vector space $R_{2}[t]$ of all polynomials in the variable $t$ of degree at most $2$, and its subset $B = {t^{2} + at + b \mid a, b ∈ R}$.

Which of the following statements are true?

1. $B$ is closed under polynomial addition
2. $B$ is closed under scalar multiplication
3. $B$ is a subspace of $R_{2}[t]$
4. $B$ is a **not** a subspace of $R_{2}[t]$

- [ ] A. IV only
- [ ] B. II and IV only
- [ ] C. I and IV only
- [ ] D. I, II, and III only
- [ ] E. II only

---

<a id="identifying-subspaces-of-matrix-vector-spaces"></a>
## Identifying Subspaces of Matrix Vector Spaces

**Example:** Consider the vector space $\text{M}_2(\mathbb{R})$ of all $2\times 2$ matrices over $\mathbb{R}$, and its subset $N$ containing all $2\times 2$ matrices such that the sum of the elements on the main diagonal is greater or equal to $0$.

Which of the following statements are true?

1. $N$ is closed under matrix addition
2. $N$ is closed under scalar multiplication
3. $N$ is a subspace of $\text{M}_2(\mathbb{R})$
4. $N$ is **not** a subspace of $\text{M}_2(\mathbb{R})$

**Explanation**

Recall that a subset of a vector space is a subspace if it's closed under addition and scalar multiplication.

With that in mind, let's examine each statement in turn.

- Statement I is true. If $A_1$ and $A_2$ are two matrices from $N$, then the sum of the elements on the main diagonal for each matrix is greater or equal to $0$. Therefore, $A_1+A_2$ is a matrix where the sum of the elements on the main diagonal is greater or equal to $0$. This matrix belongs to $N$, and so $N$ is closed under addition.
- Statement II is false. If $A$ is an element of $N$, then the sum of the elements on the main diagonal is greater than or equal to $0$. If we let $k=-1 \in \mathbb{R}$, then $kA$ is a matrix where the sum of the elements on the main diagonal is less than or equal to $0$. This matrix does not belong to $N$, and so $N$ is *not* closed under scalar multiplication.
- Statement III is false, while statement IV is true. $N$ is a *not* subspace because it's not closed under scalar multiplication.

Therefore, the correct answer is "I and IV only."

---

**Question 3**

Consider the vector space $M_{2}(R)$ of all $2 \cdot 2$ matrices over $R$ and its subset $C = {\begin{bmatrix}a & 0 \\ 0 & 0\end{bmatrix} \mid a ∈ R}$.

Which of the following statements are true?

1. $C$ is closed under matrix addition
2. $C$ is closed under scalar multiplication
3. $C$ is a subspace of $M_{2}(R)$
4. $C$ is **not** a subspace of $M_{2}(R)$

- [ ] A. I and IV only
- [ ] B. IV only
- [ ] C. I only
- [ ] D. I, II, and III only
- [ ] E. II and IV only

---

**Question 4**

Consider the vector space $M_{2}(R)$ of all $2 \cdot 2$ matrices over $R$, and its subset $D$ containing all $2 \cdot 2$ matrices such that the sum of the elements on the main diagonal is equal to $1$.

Which of the following statements are true?

1. $D$ is closed under matrix addition
2. $D$ is closed under scalar multiplication
3. $D$ is a subspace of $M_{2}(R)$
4. $D$ is **not** a subspace of $M_{2}(R)$

- [ ] A. IV only
- [ ] B. I only
- [ ] C. I and IV only
- [ ] D. II and IV only
- [ ] E. I, II, and III only

---

<a id="identifying-subspaces-of-function-vector-spaces"></a>
## Identifying Subspaces of Function Vector Spaces

**Example:** Consider the vector space $\mathcal{C}[-1,3]$ of all continuous functions $f:[-1,3] \to \mathbb{R}$ and its subset $A = \big\{f \in \mathcal{C}[-1,3] \: \mid \: f(0)=-f(2) \big\}$.

Which of the following statements are true?

1. $A$ is closed under function addition
2. $A$ is closed under scalar multiplication
3. $A$ is a subspace of $\mathcal{C}[-1,3]$
4. $A$ is **not** a subspace of $\mathcal{C}[-1,3]$

**Explanation**

Recall that a subset of a vector space is a subspace if it's closed under addition and scalar multiplication.

With that in mind, let's examine each statement in turn.

- Statement I is true. If $f(x),g(x) \in A$, then
$(f + g)(0)|= f(0) + g(0); =-f(2) - g(2); = - (f(2) + g(2)); = - (f + g)(2)$,
which means $f+g \in A$. So, $A$ is closed under addition.
- Statement II is true. If $f(x) \in A$ and $k \in \mathbb{R}$, then
$(kf)(0)|= kf(0); = k \cdot (-f(2)); =-kf(2); = - (kf)(2)$,
which means $kf \in A$. So, $A$ is closed under scalar multiplication.
- Statement III is true. $A$ is a subspace because it's closed under addition and scalar multiplication.

Therefore, the correct answer is "I, II, and III only."

---

**Question 5**

Consider the vector space $C[0, 1]$ of all continuous functions $f: [0, 1] → R$ and its subset $K$ containing all constant functions.

Which of the following statements are true?

1. $K$ is closed under function addition
2. $K$ is closed under scalar multiplication
3. $K$ is a subspace of $C[0, 1]$
4. $K$ is **not** a subspace of $C[0, 1]$

*Hint: $f$ is a constant function if $f(x) = const$ for all $x ∈ [0, 1]$.*

- [ ] A. II and IV only
- [ ] B. II only
- [ ] C. I and IV only
- [ ] D. IV only
- [ ] E. I, II, and III only

---

**Question 6**

Consider the vector space $C[0, 1]$ of all continuous functions $f: [0, 1] → R$ and its subset $D = {f ∈ C[0, 1] \mid f(0) = 3}$.

Which of the following statements are true?

1. $D$ is closed under function addition
2. $D$ is closed under scalar multiplication
3. $D$ is a subspace of $C[0, 1]$
4. $D$ is **not** a subspace of $C[0, 1]$

- [ ] A. I, II, and III only
- [ ] B. IV only
- [ ] C. I and IV only
- [ ] D. II only
- [ ] E. II and IV only

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
