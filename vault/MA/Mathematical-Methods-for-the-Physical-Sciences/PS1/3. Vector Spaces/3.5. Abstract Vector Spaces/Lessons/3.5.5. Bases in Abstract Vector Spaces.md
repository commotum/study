# Bases in Abstract Vector Spaces

<!--
lesson-id: 1909
topic-code: PS1.3.5.5
-->

## Table of Contents

- [Introduction](#introduction)
- [Identifying True Statements About Sets of Vectors in a Polynomial Vector Space](#identifying-true-statements-about-sets-of-vectors-in-a-polynomial-vector-space)
- [Identifying True Statements About Sets of Vectors in a Matrix Vector Space](#identifying-true-statements-about-sets-of-vectors-in-a-matrix-vector-space)
- [An Alternative Definition](#an-alternative-definition)
- [Identifying True Statements About Bases of Abstract Vector Spaces](#identifying-true-statements-about-bases-of-abstract-vector-spaces)
- [Some Important Standard Bases](#some-important-standard-bases)

## Prerequisites

- [Finding a Basis of a Span](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.3. Bases of N-Dimensional Space/Lessons/3.3.1. Finding a Basis of a Span.md>)
- [Linear Independence in Abstract Vector Spaces](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.5. Abstract Vector Spaces/Lessons/3.5.3. Linear Independence in Abstract Vector Spaces.md>)

---

<a id="introduction"></a>
## Introduction

A set of vectors

$$
\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2, \ldots, \mathbf{b}_n \}
$$

is called a **basis** of an abstract vector space $V$ if the following two conditions hold:

- $\mathcal{B}$ is linearly independent, and
- $\mathcal{B}$ spans $V$.

For example, let's consider the vector space $\mathbb{R}_2[t]$ of all polynomials in the variable $t$ over $\mathbb R$ with degree at most $2$, and its subset

$$
\mathcal{B} = \left\{{\color{blue}{1}}, {\color{blue}{t}}, {\color{blue}{t^2}} \right\}
$$

Is $\mathcal{B}$ a basis for $\mathbb{R}_2[t]$? Let's check our two conditions.

- First, we check for linear independence. Notice that the equation
$x_1 \cdot {\color{blue}1} + x_2 \cdot {\color{blue}t} + x_3 \cdot {\color{blue}t^2} = 0$
is true for all $t$ if and only if
$x_1=x_2=x_3=0$.
Therefore, the set $\mathcal{B}$ is linearly independent in $\mathbb{R}_2[t]$.
- Next, we check that $\mathcal B$ spans $\mathbb{R}_2[t]$. Notice that we can write any polynomial $at^2+bt+c\in\mathbb{R}_2[t]$ as a linear combination of vectors from $\mathcal{B}$:
$at^2+bt+c=c\cdot{\color{blue}1} + b\cdot{\color{blue}t} + a\cdot{\color{blue}t^2}$
Therefore, $\mathcal B$ spans $\mathbb{R}_2[t]$.

Since both conditions hold, we conclude that

$$
\mathcal{B} = \{{1}, {t}, {t^2} \}
$$

is a basis of $\mathbb{R}_2[t]$.

---

<a id="identifying-true-statements-about-sets-of-vectors-in-a-polynomial-vector-space"></a>
## Identifying True Statements About Sets of Vectors in a Polynomial Vector Space

**Example:** Consider the vector space $\mathbb{R}_2[t]$ of all polynomials in the variable $t$ over $\mathbb R$ with degrees at most $2$.

Which of the following statements are true?

1. $\big\{0,t^2 \big\}$ is a linearly independent set in $\mathbb{R}_2[t]$
2. $-5t^2$ can be expressed as a linear combination of $\big\{0, t^2 \big\}$
3. $\big\{0, t^2 \big\}$ is a basis of $\mathbb{R}_2[t]$

**Explanation**

Let's examine each of the statements in turn.

- Statement I is false. Notice that
$1 \cdot {\color{blue}0} + 0 \cdot {\color{blue}t^2} = 0$,
where the first coefficient is nonzero, so the set is linearly dependent. In general, any set that contains a zero-vector is linearly dependent.
- Statement II is true. Indeed,
$-5t^2 = 0 \cdot {\color{blue}0} + (-5) \cdot {\color{blue}t^2}$.
- Statement III is false. Since the set is not linearly independent, it can't form a basis.

Therefore, the correct answer is "II only."

---

**Question 1**

```quiz
type: radio
id: ma-94851
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector space $R_{3}[t]$ of all polynomials in the variable $t$ over $R$ with degrees at most $3$.

  Which of the following statements are true?

  1. ${1, t, t^{3}}$ is a linearly independent set in $R_{3}[t]$
  2. $-5t^{3} + t^{2} + t$ can be expressed as a linear combination of ${1, t, t^{3}}$
  3. ${1, t, t^{3}}$ is a basis of $R_{3}[t]$
options:
- id: a
  content: |-
    I and II only
- id: b
  content: |-
    I only
- id: c
  content: |-
    I and III only
- id: d
  content: |-
    II only
- id: e
  content: |-
    III only
```

---

**Question 2**

```quiz
type: radio
id: ma-36308
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector space $R_{2}[t]$ of all polynomials in the variable $t$ over $R$ with degrees at most $2$.

  Which of the following statements are true?

  1. ${0, t, t^{2}}$ is a linearly independent set in $R_{2}[t]$
  2. $t^{2} - 2t$ can be expressed as a linear combination of ${0, t, t^{2}}$
  3. ${0, t, t^{2}}$ is a basis of $R_{2}[t]$
options:
- id: a
  content: |-
    II and III only
- id: b
  content: |-
    None of the statements are true
- id: c
  content: |-
    II only
- id: d
  content: |-
    I and III only
- id: e
  content: |-
    III only
```

---

<a id="identifying-true-statements-about-sets-of-vectors-in-a-matrix-vector-space"></a>
## Identifying True Statements About Sets of Vectors in a Matrix Vector Space

**Example:** Consider the vector space $\text{M}_2(\mathbb{R})$ of all $2 \times 2$ matrices over $\mathbb{R}$.

Which of the following statements are true?

1. $\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$ is a linearly independent set in $\text{M}_2(\mathbb{R})$
2. $\begin{bmatrix}0 & -2 \\ 0 & 4\end{bmatrix}$ can be expressed as a linear combination of $\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$
3. $\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$ is a basis of $\text{M}_2(\mathbb{R})$

**Explanation**

Let's examine each of the statements in turn.

- Statement I is true. Indeed, the equation
$\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$
is true if and only if $x_1=x_2=0$. So, the set is linearly independent.
- Statement II is true. Indeed,
$\begin{bmatrix}0 & -2 \\ 0 & 4\end{bmatrix}$
- Statement III is false. For example, the matrix $\begin{bmatrix}1 & -1 \\ 0 & 5\end{bmatrix}$ can't be written as a linear combination of
$\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$
since all these linear combinations result in matrices with the entry $a_{11} = 0$:
$\begin{bmatrix}0 & -1 \\ 0 & 0\end{bmatrix}$

Therefore, the correct answer is "I and II only."

---

**Question 3**

```quiz
type: radio
id: ma-94866
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector space $M_{2}(R)$ of all $2 \cdot 2$ matrices over $R$.

  Which of the following statements are true?

  1. ${\begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}1 & 4 \\ 0 & -1\end{bmatrix}}$ is a linearly independent set in $M_{2}(R)$
  2. $\begin{bmatrix}3 & 12 \\ 0 & -3\end{bmatrix}$ can be expressed as a linear combination of ${\begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}1 & 4 \\ 0 & -1\end{bmatrix}}$
  3. ${\begin{bmatrix}0 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}1 & 4 \\ 0 & -1\end{bmatrix}}$ is a basis of $M_{2}(R)$
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    II and III only
- id: c
  content: |-
    II only
- id: d
  content: |-
    I only
- id: e
  content: |-
    I and II only
```

---

**Question 4**

```quiz
type: radio
id: ma-36571
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vector space $M_{2}(R)$ of all $2 \cdot 2$ matrices over $R$.

  Which of the following statements are true?

  1. ${\begin{bmatrix}1 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}0 & 0 \\ 0 & 1\end{bmatrix}}$ is a linearly independent set in $M_{2}(R)$
  2. $\begin{bmatrix}-2 & 0 \\ 0 & 5\end{bmatrix}$ can be expressed as a linear combination of ${\begin{bmatrix}1 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}0 & 0 \\ 0 & 1\end{bmatrix}}$
  3. ${\begin{bmatrix}1 & 0 \\ 0 & 0\end{bmatrix}, \begin{bmatrix}0 & 0 \\ 0 & 1\end{bmatrix}}$ is a basis of $M_{2}(R)$
options:
- id: a
  content: |-
    I and III only
- id: b
  content: |-
    II and III only
- id: c
  content: |-
    I, II, and III
- id: d
  content: |-
    I and II only
- id: e
  content: |-
    II only
```

---

<a id="an-alternative-definition"></a>
## An Alternative Definition

A set of vectors

$$
\mathcal{B}=\{\mathbf{b}_1,\mathbf{b}_2, \ldots, \mathbf{b}_n \}
$$

is a basis of a vector space $V$ if $\mathcal{B}$ is a **maximally linearly independent set in $V$**. In other words, $\mathcal{B}$ is linearly independent and for any vector ${\color{blue}\mathbf{w}} \in V$, the set

$$
\mathcal{B} \cup \{{\color{blue}\mathbf{w}} \} = \{\mathbf{b}_1,\mathbf{b}_2, \ldots, \mathbf{b}_n, {\color{blue}\mathbf{w}} \}
$$

is linearly dependent.

---

<a id="identifying-true-statements-about-bases-of-abstract-vector-spaces"></a>
## Identifying True Statements About Bases of Abstract Vector Spaces

**Example:** Let $\mathcal{B}= \left\{\mathbf{b_1}, \mathbf{b_2}, \mathbf{b_3} \right\}$ be a basis of a vector space $V$.

Which of the following statements are true?

1. $\left\{\mathbf{b_2},-\mathbf{b_1}, \mathbf{b_3}+\mathbf{b_1} \right\}$ is a basis of $V$
2. $\left\{\mathbf{b_2}, \mathbf{b_3}, \mathbf{0} \right\}$ spans $V$
3. $\left\{-\mathbf{b_2}, 3\mathbf{b_1}\right\}$ is linearly dependent

**Explanation**

Since $\mathcal{B}$ is a basis, the following properties must hold:

- $\mathcal{B}$ is a maximally linearly independent set of vectors in $V$, and
- $\mathcal{B}$ spans $V$.

With that in mind, let's examine our statements.

- Statement I is true. Indeed, the equation
$x_{1}b_{2} + x_{2}(-b_{1}) + x_{3}(b_{3} + b_{1})|= 0$
is equivalent to
$x_{1}b_{2} + (x_{3} - x_{2})b_{1} + x_{3}b_{3}|= 0$,
which, since $\left\{\mathbf{b_1}, \mathbf{b_2}, \mathbf{b_3} \right\}$ is linearly independent, gives
${x_{1} = 0; x_{3} - x_{2} = 0; x_{3} = 0$
Also, $\left\{\mathbf{b_2},-\mathbf{b_1}, \mathbf{b_3}+\mathbf{b_1} \right\}$ spans $V$ since any linear combination of vectors from $\mathcal{B}$ can be expressed as a linear combination of $\left\{\mathbf{b_2},-\mathbf{b_1}, \mathbf{b_3}+\mathbf{b_1} \right\}$:
$x_1\mathbf{b}_1+x_2\mathbf{b}_2+x_3\mathbf{b}_3 = x_2\mathbf{b}_2+(x_3-x_1)(-\mathbf{b}_1)+x_3 (\mathbf{b_3}+\mathbf{b_1})$
- Statement II is false. In order for $\left\{\mathbf{b_2}, \mathbf{b_3}, \mathbf{0} \right\}$ to span $V$, every element of $\mathcal{B}$ must be able to be expressed as a linear combination of elements of $\left\{\mathbf{b_2}, \mathbf{b_3}, \mathbf{0} \right\}$. But the vector $\mathbf{b}_1 \in \mathcal{B}$ can't be written as a linear combination of the elements of $\left\{\mathbf{b_2}, \mathbf{b_3}, \mathbf{0} \right\}$.
- Statement III is false. If $\left\{-\mathbf{b_2}, 3\mathbf{b_1}\right\}$ is linearly dependent then there exist $x_1$ and $x_2$ not both zero such that
$x_1(-\mathbf{b_2}) + x_2(3\mathbf{b_1}) = \mathbf{0}$.
Then, we would have
$(3x_2) \cdot \mathbf{b_1} + (-x_1) \cdot \mathbf{b_2} + 0 \cdot \mathbf{b}_3= \mathbf{0}$.
But this would mean that $\mathcal{B} = \left\{\mathbf{b_1}, \mathbf{b_2}, \mathbf{b_3} \right\}$ is linearly dependent, which contradicts the fact that $\mathcal{B}$ is a basis of $V$.

Therefore, the correct answer is "I only."

---

**Question 5**

```quiz
type: radio
id: ma-36309
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $B = {b_{1}, b_{2}, b_{3}}$ be a basis of a vector space $V$.

  Which of the following statements are true?

  1. ${b_{1}, b_{2}}$ is linearly independent
  2. ${b_{1}, b_{2}, b_{3}, w}$ is linearly independent
  3. ${2b_{1}, b_{2}, b_{3}}$ is a basis of $V$
options:
- id: a
  content: |-
    II only
- id: b
  content: |-
    II and III only
- id: c
  content: |-
    I only
- id: d
  content: |-
    I and III only
- id: e
  content: |-
    None of the statements are true
```

---

**Question 6**

```quiz
type: radio
id: ma-34638
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Let $B = {b_{1}, b_{2}}$ and $C = {c_{1}, c_{2}, c_{3}}$ be bases of $R^{2}$ and $R^{3}$, respectively.

  Which of the following statements are true?

  1. ${b_{1}, b_{2}, x}$ is a basis of $R^{2}$
  2. ${b_{1}, b_{2}}$ is a basis of $R^{3}$
  3. ${c_{3}, c_{2}, c_{1}}$ is a basis of $R^{3}$
options:
- id: a
  content: |-
    III only
- id: b
  content: |-
    II only
- id: c
  content: |-
    I, II, and III
- id: d
  content: |-
    I and III only
- id: e
  content: |-
    None of the statements are true
```

---

<a id="some-important-standard-bases"></a>
## Some Important Standard Bases

Let $\mathbb{R}_n[t]$ denote the vector space of all polynomials in the variable $t$ over $\mathbb{R}$ that have degree at most $n$. Also, let $\text{M}_2(\mathbb{R})$ be the vector space of all $2 \times 2$ matrices over $\mathbb{R}$. Then, we have the following important standard bases.

- The standard basis of $\mathbb{R}_1[t]$ is $\{1, \: t \}$.
- The standard basis of $\mathbb{R}_2[t]$ is $\{1, \: t, \: t^2 \}$.
- The standard basis of $\mathbb{R}_n[t]$ is $\{1, \: t, \: \ldots, \: t^n \}$.
- The standard basis of $\text{M}_2(\mathbb{R})$ is
$\begin{bmatrix}1 & 0 \\ 0 & 0\end{bmatrix}$

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
