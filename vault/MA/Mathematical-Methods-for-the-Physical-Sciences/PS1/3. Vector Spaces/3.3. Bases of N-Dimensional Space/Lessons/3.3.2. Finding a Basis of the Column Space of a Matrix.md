# Finding a Basis of the Column Space of a Matrix

<!--
lesson-id: 2623
topic-code: PS1.3.3.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Basis of the Column Space of a Matrix](#finding-a-basis-of-the-column-space-of-a-matrix)
- [Finding a Particular Linear Combination of Vectors](#finding-a-particular-linear-combination-of-vectors)
- [Determining Whether a Given Set Is a Basis of the Column Space of a Matrix With Two Rows](#determining-whether-a-given-set-is-a-basis-of-the-column-space-of-a-matrix-with-two-rows)
- [Determining Whether a Given Set Is a Basis of the Column Space of a Matrix For a Larger Matrix](#determining-whether-a-given-set-is-a-basis-of-the-column-space-of-a-matrix-for-a-larger-matrix)

## Prerequisites

- [The Column Space of a Matrix](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.2. Subspaces of N-Dimensional Space/Lessons/3.2.3. The Column Space of a Matrix.md>)
- [Finding a Basis of a Span](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.3. Bases of N-Dimensional Space/Lessons/3.3.1. Finding a Basis of a Span.md>)

---

<a id="introduction"></a>
## Introduction

Remember that the column space of a matrix is the span of its columns. For example, for the matrix

$$
\begin{bmatrix}1 & 2 & -1 \\ 3 & 2 & 5\end{bmatrix}
$$

the column space is

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

So, to find a basis of $\textrm{Col}(A)$, we can apply the usual procedure of converting the matrix $A$ to row echelon form and then picking the pivot columns from the original matrix $A$.

---

<a id="finding-a-basis-of-the-column-space-of-a-matrix"></a>
## Finding a Basis of the Column Space of a Matrix

**Example:** Consider the matrix $\begin{bmatrix}1 & 2 & -1 \\ 3 & 2 & 5\end{bmatrix}$ Find a basis $\mathcal B$ of $\textrm{Col}(A)$.

**Explanation**

In order to find a basis of $\textrm{Col}(A)$, we need reduce the matrix $A$ to row echelon form:

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 2 & -1 \\ 3 & 2 & 5\end{bmatrix} \mid R_{2} \mid:=R_{2} + (-3)R_{1} \\
&= ∼\begin{bmatrix}1 & 2 & -1 \\ 0 & -4 & 8\end{bmatrix}
\end{aligned}
$$

The pivot columns correspond to linearly independent vectors, while the non-pivot columns correspond to linearly dependent vectors that can be removed. Here, the $1$st and $2$nd columns are pivot columns, while the $3$rd column is a non-pivot column.

Remember that we need to pick the pivot columns from the *original matrix*, not from the reduced one.

The $1$st and $2$nd columns from the original matrix are $\mathbf{a}_1$ and $\mathbf{a}_2$. So, a basis of $\textrm{Col}(A)$ is $\{\mathbf{a}_1, \mathbf{a}_2 \}$, that is to say,

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-34524
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the matrix

  $A = \begin{bmatrix}2 & -6 & 0 \\ 1 & -1 & 1 \\ 3 & 1 & 5\end{bmatrix}$. Find a basis $B$ of $Col(A)$.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}2 \\ 1 \\ 3\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}0 \\ 1 \\ 5\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}-6 \\ -1 \\ 1\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}2 \\ 1 \\ 3\end{bmatrix}, \begin{bmatrix}-6 \\ -1 \\ 1\end{bmatrix}, \begin{bmatrix}0 \\ 1 \\ 5\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}2 \\ 1 \\ 3\end{bmatrix}, \begin{bmatrix}-6 \\ -1 \\ 1\end{bmatrix}}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-35676
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Given the matrix

  $A = \begin{bmatrix}0 & 0 & 0 \\ 0 & 0 & 3 \\ 1 & 0 & 2\end{bmatrix}$. Find a basis $B$ of $Col(A)$.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}2 \\ 3 \\ 0\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}0 \\ 0 \\ 1\end{bmatrix}, \begin{bmatrix}0 \\ 3 \\ 2\end{bmatrix}, \begin{bmatrix}0 \\ 0 \\ 0\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}0 \\ 0 \\ 1\end{bmatrix}, \begin{bmatrix}0 \\ 0 \\ 0\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}0 \\ 0 \\ 1\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}0 \\ 0 \\ 1\end{bmatrix}, \begin{bmatrix}0 \\ 3 \\ 2\end{bmatrix}}$
```

---

<a id="finding-a-particular-linear-combination-of-vectors"></a>
## Finding a Particular Linear Combination of Vectors

In the previous example, we computed a basis of $\textrm{Col}(A)$, where

$$
\begin{bmatrix}1 & 2 & -1 \\ 3 & 2 & 5\end{bmatrix}
$$

The basis that we found previously was

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

Therefore, we can write

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

The vector $\mathbf{a}_3$ was not included in the basis because the $3$rd column of $A$ was not a pivot column. This means that $\mathbf{a}_3$ can be written as a linear combination of $\mathbf{a}_1$ and $\mathbf{a}_2$.

Now, what if we want to find the exact linear combination of $\mathbf{a}_1$ and $\mathbf{a}_2$ that yields $\mathbf{a}_3$? That is to say, we want to find $x_1, x_2 \in \mathbb{R}$ such that

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

This is equivalent to a system with the augmented matrix
$\begin{bmatrix}1 & 2 & -1 \\ 3 & 2 & 5\end{bmatrix}$

If we reduced this matrix to the *reduced* row echelon form, we get

$$
\begin{bmatrix}1 & 0 & 3 \\ 0 & 1 & -2\end{bmatrix}
$$

Finally, the third column of the *reduced* matrix tells us that $x_1=3$ and $x_2=-2$, meaning that
$\begin{bmatrix}-1 \\ 5\end{bmatrix}$

In general, if the $n$th column of the reduced matrix is a non-pivot column, then the entries in that column tell us the coefficients needed to express $\mathbf{a}_n$ as a linear combination of all the other columns in the original matrix.

**Watch out!** The matrix must be written in *reduced* row echelon form (with $1$'s for pivots and $0$'s above and below the pivots), not just in a row echelon form.

---

<a id="determining-whether-a-given-set-is-a-basis-of-the-column-space-of-a-matrix-with-two-rows"></a>
## Determining Whether a Given Set Is a Basis of the Column Space of a Matrix With Two Rows

**Example:** The matrix $A$ is given below. Let $\mathbf{a}_1$, $\mathbf{a}_2$, and $\mathbf{a}_3$ be the respective columns of $A$. Determine if $a_{1}, a_{2}$ is a basis of $\textrm{Col}(A)$.
If that is the case, find $x_1 \cdot x_2$, where $\mathbf{a}_3=x_1\mathbf{a}_1+x_2\mathbf{a}_2$.

$\begin{bmatrix}1 & 2 & 9 \\ 3 & 1 & 17\end{bmatrix}$

**Explanation**

First, we reduce the matrix $A$ to reduced row echelon form (RREF):

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 2 & 9 \\ 3 & 1 & 17\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + (-3)R_{1} \\ ∼\begin{bmatrix}1 & 2 & 9 \\ 0 & -5 & -10\end{bmatrix} & R_{2}\end{vmatrix}:=(-\frac{1}{5})R_{2} \\
&= ∼\begin{bmatrix}1 & 2 & 9 \\ 0 & 1 & 2\end{bmatrix} \mid R_{1} \mid:=R_{1} + (-2)R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & 5 \\ 0 & 1 & 2\end{bmatrix}
\end{aligned}
$$

From the matrix above, we see that the pivot columns are the $1$st and $2$nd columns. So, a basis of $\textrm{Col}(A)$ is $\{\mathbf{a}_1,\mathbf{a}_2 \}$, that is to say,

$$
\begin{bmatrix}1 \\ 3\end{bmatrix}
$$

Now, since the $3$rd column of the reduced matrix is a non-pivot column, then the entries in that column tell us the coefficients needed to express $\mathbf{a}_3$ as a linear combination of all the other columns in the original matrix. So, we have

$$
\begin{bmatrix}9 \\ 17\end{bmatrix}
$$

This gives us

$$
x_1 \cdot x_2 = 5 \cdot 2=10
$$

---

**Question 3**

```quiz
type: radio
id: ma-34510
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The matrix $A$ is given below. Let $a_{1}$, $a_{2}$, and $a_{3}$ are the respective columns of $A$. Determine if $B = {a_{1}, a_{3}}$ is a basis of $Col(A)$.
  If that is the case, find $x_{1} \cdot x_{3}$, where $a_{2} = x_{1}a_{1} + x_{3}a_{3}$.

  $A = \begin{bmatrix}1 & 3 & 4 \\ -2 & -6 & 5\end{bmatrix}$
options:
- id: a
  content: |-
    $\frac{5}{4}$
- id: b
  content: |-
    ${a_{1}, a_{3}}$ is not a basis of $Col(A)$
- id: c
  content: |-
    $-2$
- id: d
  content: |-
    $0$
- id: e
  content: |-
    $9$
```

---

**Question 4**

```quiz
type: radio
id: ma-34462
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The matrix $A$ is given below. Let $a_{1}$, $a_{2}$, and $a_{3}$ be the respective columns of $A$. Determine if $B = {a_{1}, a_{2}}$ is a basis of $Col(A)$. If that is the case, find $x_{1} \cdot x_{2}$, where $a_{3} = x_{1}a_{1} + x_{2}a_{2}$.

  $A = \begin{bmatrix}1 & 0 & -1 \\ 0 & -2 & -6\end{bmatrix}$
options:
- id: a
  content: |-
    $\frac{3}{5}$
- id: b
  content: |-
    $-3$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $1$
- id: e
  content: |-
    ${a_{1}, a_{2}}$ is not a basis of $Col(A)$
```

---

<a id="determining-whether-a-given-set-is-a-basis-of-the-column-space-of-a-matrix-for-a-larger-matrix"></a>
## Determining Whether a Given Set Is a Basis of the Column Space of a Matrix For a Larger Matrix

**Example:** The matrix $A$ is given below. Let $\mathbf{a}_1$, $\mathbf{a}_2$, $\mathbf{a}_3$, and $\mathbf{a}_4$ be the respective columns of $A$. Determine if $a_{1}, a_{2}$ is a basis of $\textrm{Col}(A)$. If that is the case, find $x_1 \cdot x_2$, where $\mathbf{a}_4=x_1\mathbf{a}_1+x_2\mathbf{a}_2$.

$\begin{bmatrix}2 & 0 & -4 & 4 \\ 0 & 1 & -6 & -3 \\ 1 & 2 & -14 & -4\end{bmatrix}$

**Explanation**

First, we reduce the matrix $A$ to reduced row echelon form (RREF):

$$
\begin{aligned}
A &= \begin{bmatrix}2 & 0 & -4 & 4 \\ 0 & 1 & -6 & -3 \\ 1 & 2 & -14 & -4\end{bmatrix}\begin{vmatrix}R_{1} &:=\frac{1}{2}R_{1} \\ ∼\begin{bmatrix}1 & 0 & -2 & 2 \\ 0 & 1 & -6 & -3 \\ 1 & 2 & -14 & -4\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + (-1)R_{1} \\
&= ∼\begin{bmatrix}1 & 0 & -2 & 2 \\ 0 & 1 & -6 & -3 \\ 0 & 2 & -12 & -6\end{bmatrix} \mid R_{3} \mid:=R_{3} + (-2)R_{2} \\
&= ∼\begin{bmatrix}1 & 0 & -2 & 2 \\ 0 & 1 & -6 & -3 \\ 0 & 0 & 0 & 0\end{bmatrix}
\end{aligned}
$$

From the matrix above, we see that the pivot columns are the $1$st and $2$nd columns. So, a basis of $\textrm{Col}(A)$ is $\{\mathbf{a}_1,\mathbf{a}_2 \}$, that is to say,
$\begin{bmatrix}2 \\ 0 \\ 1\end{bmatrix}$

Now, since the $4$th column of the reduced matrix is a non-pivot column, then the entries in that column tell us the coefficients needed to express $\mathbf{a}_4$ as a linear combination of all the other columns in the original matrix. So, we have

$$
\begin{bmatrix}4 \\ -3 \\ -4\end{bmatrix}
$$

This gives us

$$
x_1 \cdot x_2 = 2 \cdot (-3) = -6
$$

---

**Question 5**

```quiz
type: radio
id: ma-75603
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The matrix $A$ is given below. Let $a_{1}$, $a_{2}$, $a_{3}$ and $a_{4}$ be the respective columns of $A$.
  Determine if $B = {a_{1}, a_{2}}$ is a basis of $Col(A)$.
  If that is the case, find $x_{1} \cdot x_{2}$, where $a_{4} = x_{1}a_{1} + x_{2}a_{2}$.

  $A = \begin{bmatrix}1 & 0 & 2 & 1 \\ 0 & 5 & 10 & 5 \\ 1 & 0 & 2 & 2\end{bmatrix}$
options:
- id: a
  content: |-
    $-8$
- id: b
  content: |-
    ${a_{1}, a_{2}}$ is not a basis of $Col(A)$
- id: c
  content: |-
    $15$
- id: d
  content: |-
    $4$
- id: e
  content: |-
    $-6$
```

---

**Question 6**

```quiz
type: radio
id: ma-34530
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The matrix $A$ is given below. Let $a_{1}$, $a_{2}$, $a_{3}$ and $a_{4}$ be the respective columns of $A$.
  Determine if $B = {a_{1}, a_{2}}$ is a basis of $Col(A)$.
  If that is the case, find $x_{1} \cdot x_{2}$, where $a_{4} = x_{1}a_{1} + x_{2}a_{2}$.

  $A = \begin{bmatrix}1 & 2 & -1 & 0 \\ 0 & 4 & -4 & 4 \\ 3 & 5 & -2 & -1\end{bmatrix}$
options:
- id: a
  content: |-
    ${a_{1}, a_{2}}$ is not a basis of $Col(A)$
- id: b
  content: |-
    $-13$
- id: c
  content: |-
    $11$
- id: d
  content: |-
    $-11$
- id: e
  content: |-
    $-2$
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
