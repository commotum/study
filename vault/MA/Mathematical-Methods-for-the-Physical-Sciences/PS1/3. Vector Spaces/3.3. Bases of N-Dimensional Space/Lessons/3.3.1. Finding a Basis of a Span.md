# Finding a Basis of a Span

<!--
lesson-id: 1855
topic-code: PS1.3.3.1
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Basis of the Space Spanned by a Set of Vectors Using Inspection](#finding-a-basis-of-the-space-spanned-by-a-set-of-vectors-using-inspection)
- [Finding a Basis by Identifying Pivot Columns](#finding-a-basis-by-identifying-pivot-columns)
- [Finding a Basis of the Space Spanned by a Set of Vectors by Identifying Pivot Columns](#finding-a-basis-of-the-space-spanned-by-a-set-of-vectors-by-identifying-pivot-columns)
- [Finding a Particular Linear Combination of Vectors](#finding-a-particular-linear-combination-of-vectors)
- [Determining Whether a Given Set Is a Basis of a Span](#determining-whether-a-given-set-is-a-basis-of-a-span)

## Prerequisites

- [Linear Dependence and Independence](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.1. Vectors in N-Dimensional Space/Lessons/3.1.4. Linear Dependence and Independence.md>)
- [Subspaces of N-Dimensional Space: Geometric Interpretation](<MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/3. Vector Spaces/3.2. Subspaces of N-Dimensional Space/Lessons/3.2.2. Subspaces of N-Dimensional Space- Geometric Interpretation.md>)

---

<a id="introduction"></a>
## Introduction

A **basis** of a subspace $U$ (in $\mathbb{R}^n$) is a set of vectors in $\mathbb{R}^n$ that is linearly independent and spans $U$.

For example, consider the following subspace consisting of the span of three vectors:
$\begin{bmatrix}1 \\ 0\end{bmatrix}$

To find a basis of $U$, we need to find a set of linearly independent vectors that spans $U$.

**Watch out!** The set
$\begin{bmatrix}1 \\ 0\end{bmatrix}$
is *not* a basis of $U$. It's true that this set *spans* $U$, but the vectors in this set are *not* linearly independent. In particular, the vector $\color{red}\mathbf{a}_3$ can be written as a linear combination of $\color{blue}\mathbf{a}_1$ and $\color{blue}\mathbf{a}_2$ as follows:

$$
\begin{bmatrix}-1 \\ 3\end{bmatrix}
$$

To find a basis of

$$
U = \textrm{Span} \{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2}, {\color{red}\mathbf{a}_3} \}
$$

we can start with $\{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2}, {\color{red}\mathbf{a}_3} \}$, but we need to remove any "redundant" vectors that can be written as a linear combination of other vectors in the set. In this case, we remove ${\color{red}\mathbf{a}_3}$ because it can be written as a linear combination of $\color{blue}\mathbf{a}_1$ and ${\color{blue}\mathbf{a}_2}$.

We are left with the set

$$
\begin{bmatrix}1 \\ 0\end{bmatrix}
$$

These two vectors are linearly independent (since they are not parallel), and removing a redundant vector does not change the span, so these vectors still span $U$. That is to say,

$$
U = \textrm{Span} \{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2}, {\color{red}\mathbf{a}_3} \} = \textrm{Span} \{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2} \}
$$

Therefore, the set

$$
\mathcal{B} = \{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2} \}
$$

is a basis of $U$.

**Note:** To see that the vectors $\{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2} \}$ still span $U$, first note that any vector $\mathbf{b} \in U$ can be written as a linear combination of the three original vectors:

$$
\mathbf{b} = x_1 {\color{blue}\mathbf{a}_1} + x_2 {\color{blue}\mathbf{a}_2} + x_3 {\color{red}\mathbf{a}_3}
$$

Since

$$
{\color{red}\mathbf{a}_3} = -{\color{blue}\mathbf{a}_1}+3{\color{blue}\mathbf{a}_2}
$$

then we must have

$$
\begin{aligned}
b &= x_{1}a_{1} + x_{2}a_{2} + x_{3}(-a_{1} + 3a_{2}) \\
&= (x_{1} - x_{3})a_{1} + (x_{2} + 3x_{3})a_{2}
\end{aligned}
$$

which shows that $\mathbf{b}$ can actually be written as a linear combination of only two vectors, ${\color{blue}\mathbf{a}_1}$ and ${\color{blue}\mathbf{a}_2}$.

Therefore, we can conclude that the set $\{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2} \}$ spans the subspace

$$
U = \textrm{Span} \{{\color{blue}\mathbf{a}_1}, {\color{blue}\mathbf{a}_2}, {\color{red}\mathbf{a}_3} \}
$$

---

<a id="finding-a-basis-of-the-space-spanned-by-a-set-of-vectors-using-inspection"></a>
## Finding a Basis of the Space Spanned by a Set of Vectors Using Inspection

**Example:** Consider the vectors $\begin{bmatrix}2 \\ 3\end{bmatrix}$ and $\begin{bmatrix}1 \\ 1\end{bmatrix}$ Find a basis of $S=\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3 \}$ by inspection.

**Explanation**

Remember that a basis of $S$ is a set of linearly independent vectors that spans $S$.

From the given vectors, we can see that $\mathbf{a}_2$ is a linear combination of vectors $\mathbf{a}_1$ and $\mathbf{a}_3$:

$$
\mathbf{a}_2=1\cdot \mathbf{a}_1+1\cdot \mathbf{a}_3
$$

Consequently, the vector $\mathbf{a}_2$ can be removed from $\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3 \}$ without changing the span. So

$$
S = \textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3 \}
$$

is spanned by just $\{\mathbf{a}_1,\mathbf{a}_3\}$.

Lastly, note that $\mathbf{a}_1$ and $\mathbf{a}_3$ are linearly independent since they are not parallel. So the set $\{\mathbf{a}_1,\mathbf{a}_3\}$ is a linearly independent set that spans $S$.

Therefore, a basis of $S$ is $\{\mathbf{a}_1,\mathbf{a}_3\}$, that is to say,

$$
\begin{bmatrix}2 \\ 3\end{bmatrix}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-74723
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $a_{1} = \begin{bmatrix}3 \\ 1\end{bmatrix}, a_{2} = \begin{bmatrix}2 \\ 2\end{bmatrix}$, and $a_{3} = \begin{bmatrix}1 \\ -1\end{bmatrix}$. Find a basis of $S = Span{a_{1}, a_{2}, a_{3}}$ by inspection.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}2 \\ 2\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}2 \\ 2\end{bmatrix}, \begin{bmatrix}1 \\ -1\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}1 \\ -1\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}3 \\ 1\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}3 \\ 1\end{bmatrix}, \begin{bmatrix}2 \\ 2\end{bmatrix}, \begin{bmatrix}1 \\ -1\end{bmatrix}}$
```

---

**Question 2:**

```quiz
type: radio
id: ma-74724
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $a_{1} = \begin{bmatrix}2 \\ 0\end{bmatrix}, a_{2} = \begin{bmatrix}1 \\ 3\end{bmatrix}$, and $a_{3} = \begin{bmatrix}3 \\ 3\end{bmatrix}$. Find a basis of $S = Span{a_{1}, a_{2}, a_{3}}$ by inspection.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}2 \\ 0\end{bmatrix}, \begin{bmatrix}1 \\ 3\end{bmatrix}, \begin{bmatrix}3 \\ 3\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}2 \\ 0\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}2 \\ 0\end{bmatrix}, \begin{bmatrix}1 \\ 3\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}3 \\ 3\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}1 \\ 3\end{bmatrix}}$
```

---

<a id="finding-a-basis-by-identifying-pivot-columns"></a>
## Finding a Basis by Identifying Pivot Columns

In the previous example, it was easy to see that one vector was a linear combination of the others. But what can we do when it's not so easy to tell? For example, consider the subspace

$$
\begin{bmatrix}1 \\ -2\end{bmatrix}
$$

To find a subset of linearly independent vectors, we can put the vectors in a matrix and compute the reduced row echelon form:

$$
=\begin{bmatrix}1 & -1 & -1 \\ -2 & 3 & 7\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + 2R_{1} \\ ∼\begin{bmatrix}1 & -1 & -1 \\ 0 & 1 & 5\end{bmatrix} & R_{1}\end{vmatrix}:=R_{1} + R_{2}; ∼\begin{bmatrix}1 & 0 & 4 \\ 0 & 1 & 5\end{bmatrix}
$$

The pivot columns correspond to linearly independent vectors, while the non-pivot columns correspond to linearly dependent (or redundant) vectors that can be removed. Here, the $1$st and $2$nd columns are pivot columns, while the $3$rd column is a non-pivot column.

**But watch out!** We need to pick the pivot columns from the *original matrix*, not from the reduced one.

The $1$st and $2$nd columns from the original matrix are $\mathbf{a}_1$ and $\mathbf{a}_2$. So, a basis of $U$ is $\{\mathbf{a}_1, \mathbf{a}_2 \}$, that is to say,

$$
\begin{bmatrix}1 \\ -2\end{bmatrix}
$$

---

<a id="finding-a-basis-of-the-space-spanned-by-a-set-of-vectors-by-identifying-pivot-columns"></a>
## Finding a Basis of the Space Spanned by a Set of Vectors by Identifying Pivot Columns

**Example:** Consider the vectors $\begin{bmatrix}5 \\ 2\end{bmatrix}$ $\begin{bmatrix}10 \\ 4\end{bmatrix}$ and $\begin{bmatrix}15 \\ 5\end{bmatrix}$ Find a basis of $S=\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3 \}$.

**Explanation**

We need to find a subset of linearly independent vectors. So, we start by creating a matrix whose columns are made up of our vectors, and then we reduce the matrix to row echelon form:

$$
\begin{aligned}
M &= \begin{bmatrix}5 & 10 & 15 \\ 2 & 4 & 5\end{bmatrix}\begin{vmatrix}R_{1} &:=\frac{1}{5}R_{1} \\ ∼\begin{bmatrix}1 & 2 & 3 \\ 2 & 4 & 5\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + (-2)R_{1} \\
&= ∼\begin{bmatrix}1 & 2 & 3 \\ 0 & 0 & -1\end{bmatrix}
\end{aligned}
$$

The pivot columns correspond to linearly independent vectors, while the non-pivot columns correspond to linearly dependent vectors that can be removed. Here, the $1$st and $3$rd columns are pivot columns, while the $2$nd column is a non-pivot column.

Remember that we need to pick the pivot columns from the *original matrix*, not from the reduced one.

The $1$st and $3$rd columns from the original matrix are $\mathbf{a}_1$ and $\mathbf{a}_3$. So, a basis of $S$ is $\{\mathbf{a}_1, \mathbf{a}_3 \}$, that is to say,

$$
\begin{bmatrix}5 \\ 2\end{bmatrix}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-35694
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $a_{1} = \begin{bmatrix}-2 \\ 3\end{bmatrix}$, $a_{2} = \begin{bmatrix}4 \\ -6\end{bmatrix}$, and $a_{3} = \begin{bmatrix}-2 \\ -3\end{bmatrix}$. Find a basis $B$ of $Span{a_{1}, a_{2}, a_{3}}$.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}-2 \\ 3\end{bmatrix}, \begin{bmatrix}-2 \\ -3\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}-2 \\ 3\end{bmatrix}, \begin{bmatrix}-4 \\ 6\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}-2 \\ 3\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}-4 \\ -6\end{bmatrix}, \begin{bmatrix}-2 \\ -3\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}-4 \\ 6\end{bmatrix}}$
```

---

**Question 4:**

```quiz
type: radio
id: ma-34464
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  Consider the vectors $a_{1} = \begin{bmatrix}5 \\ 0 \\ 0\end{bmatrix}$, $a_{2} = \begin{bmatrix}-1 \\ 7 \\ 0\end{bmatrix}$, and $a_{3} = \begin{bmatrix}2 \\ -14 \\ 0\end{bmatrix}$. Find a basis $B$ of $Span{a_{1}, a_{2}, a_{3}}$.
options:
- id: a
  content: |-
    $B = {\begin{bmatrix}5 \\ 0 \\ 0\end{bmatrix}, \begin{bmatrix}-1 \\ 7 \\ 0\end{bmatrix}}$
- id: b
  content: |-
    $B = {\begin{bmatrix}2 \\ -14 \\ 0\end{bmatrix}}$
- id: c
  content: |-
    $B = {\begin{bmatrix}-1 \\ 7 \\ 0\end{bmatrix}, \begin{bmatrix}2 \\ -14 \\ 0\end{bmatrix}}$
- id: d
  content: |-
    $B = {\begin{bmatrix}5 \\ 0 \\ 0\end{bmatrix}}$
- id: e
  content: |-
    $B = {\begin{bmatrix}-1 \\ 7 \\ 0\end{bmatrix}}$
```

---

<a id="finding-a-particular-linear-combination-of-vectors"></a>
## Finding a Particular Linear Combination of Vectors

Earlier, we computed a basis of

$$
\begin{bmatrix}1 \\ -2\end{bmatrix}
$$

The basis that we found was

$$
\begin{bmatrix}1 \\ -2\end{bmatrix}
$$

The vector $\mathbf{a}_3$ was not included in the basis. This means that $\mathbf{a}_3$ can be written as a linear combination of $\mathbf{a}_1$ and $\mathbf{a}_2$.

Now, what if we want to find the exact linear combination of $\mathbf{a}_1$ and $\mathbf{a}_2$ that yields $\mathbf{a}_3$? That is to say, we want to find $x_1, x_2 \in \mathbb{R}$ such that

$$
\begin{bmatrix}1 \\ -2\end{bmatrix}
$$

This is equivalent to a system with the augmented matrix
$\begin{bmatrix}1 & -1 & -1 \\ -2 & 3 & 7\end{bmatrix}$

If we reduced this matrix to the *reduced* row echelon form, we get

$$
\begin{bmatrix}1 & 0 & 4 \\ 0 & 1 & 5\end{bmatrix}
$$

Finally, the third column of the *reduced* matrix tells us that $x_1=4$ and $x_2=5$, meaning that
$\begin{bmatrix}-1 \\ 7\end{bmatrix}$

**Watch out!** The matrix must be written in *reduced* row echelon form (with $1$'s for pivots and $0$'s above and below the pivots), not just row echelon form.

---

<a id="determining-whether-a-given-set-is-a-basis-of-a-span"></a>
## Determining Whether a Given Set Is a Basis of a Span

**Example:** The vectors $\mathbf{a}_1$, $\mathbf{a}_2$, $\mathbf{a}_3$, and $\mathbf{a}_4$ are given below. Determine whether $\mathcal{B} = \{\mathbf{a}_1, \mathbf{a}_2, \mathbf{a}_3 \}$ is a basis of $\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3, \mathbf{a}_4 \}$. If that is the case, find $x_1 + x_2 + x_3$, where $\mathbf{a}_4=x_1\mathbf{a}_1+x_2\mathbf{a}_2+x_3\mathbf{a}_3$.
$\begin{bmatrix}1 \\ -1 \\ -3\end{bmatrix}$

**Explanation**

We want to determine if $\mathcal{B}$ is indeed a basis of $\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3,\mathbf{a}_4 \}$. So, we want to find $x_1, x_2, x_3 \in \mathbb{R}$ such that
$\begin{bmatrix}1 \\ -1 \\ -3\end{bmatrix}$

This equation is equivalent to the system

$$
{x_{1} + x_{3} = 2; - x_{1} + x_{2} - x_{3} =-2; - 3x_{1} - 2x_{3} = 1
$$

Now, we reduce the augmented matrix of the system to reduced row echelon form (RREF):

$$
\begin{aligned}
A &= \begin{bmatrix}1 & 0 & 1 & 2 \\ -1 & 1 & -1 & -2 \\ -3 & 0 & -2 & 1\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + R_{1} \\ ∼\begin{bmatrix}1 & 0 & 1 & 2 \\ 0 & 1 & 0 & 0 \\ -3 & 0 & -2 & 1\end{bmatrix} & R_{3}\end{vmatrix}:=R_{3} + 3R_{1} \\
&= ∼\begin{bmatrix}1 & 0 & 1 & 2 \\ 0 & 1 & 0 & 0 \\ 0 & 0 & 1 & 7\end{bmatrix} \mid R_{1} \mid:=R_{1} + (-1)R_{3} \\
&= ∼\begin{bmatrix}1 & 0 & 0 & -5 \\ 0 & 1 & 0 & 0 \\ 0 & 0 & 1 & 7\end{bmatrix}
\end{aligned}
$$

From the reduced matrix above, we see that the pivot columns are the $1$st, $2$nd, and $3$rd columns. So, $\mathcal{B}$ is indeed a basis of $\textrm{Span}\{\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3,\mathbf{a}_4 \}$.
Also, the matrix tells us that $x_1=-5$, $x_2=0$, and $x_3=7$, meaning that
$\begin{bmatrix}1 \\ -1 \\ -3\end{bmatrix}$

Therefore,

$$
x_1 + x_2 + x_3 = (-5) + 0 + 7 = 2
$$

---

**Question 5:**

```quiz
type: radio
id: ma-75588
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The vectors $a_{1}$, $a_{2}$, $a_{3}$, and $a_{4}$ are given below. Determine whether $B = {a_{1}, a_{2}, a_{3}}$ is a basis of $Span{a_{1}, a_{2}, a_{3}, a_{4}}$. If that is the case, find $x_{1} + x_{2} + x_{3}$, where $a_{4} = x_{1}a_{1} + x_{2}a_{2} + x_{3}a_{3}$.

  $a_{1} = \begin{bmatrix}1 \\ -2 \\ 5\end{bmatrix}, a_{2} = \begin{bmatrix}-1 \\ 3 \\ -5\end{bmatrix}, a_{3} = \begin{bmatrix}1 \\ -2 \\ 6\end{bmatrix}, a_{4} = \begin{bmatrix}-1 \\ 2 \\ 2\end{bmatrix}$
options:
- id: a
  content: |-
    ${a_{1}, a_{2}, a_{3}}$ is not a basis of $Span{a_{1}, a_{2}, a_{3}, a_{4}}$
- id: b
  content: |-
    $-1$
- id: c
  content: |-
    $12$
- id: d
  content: |-
    $4$
- id: e
  content: |-
    $-5$
```

---

**Question 6:**

```quiz
type: radio
id: ma-75580
# MA_ANSWER_MISSING: set exactly one options[].correct=true for radio, or all correct options for checkbox
content: |-
  The vectors $a_{1}$, $a_{2}$, and $a_{3}$ are given below. Determine whether $B = {a_{1}, a_{2}}$ is a basis of $Span{a_{1}, a_{2}, a_{3}}$. If that is the case, find $x_{1} \cdot x_{2}$, where $a_{3} = x_{1}a_{1} + x_{2}a_{2}$.

  $a_{1} = \begin{bmatrix}1 \\ -2\end{bmatrix}, a_{2} = \begin{bmatrix}-1 \\ 4\end{bmatrix}, a_{3} = \begin{bmatrix}2 \\ -2\end{bmatrix}$
options:
- id: a
  content: |-
    $-6$
- id: b
  content: |-
    ${a_{1}, a_{2}}$ is not a basis of $Span{a_{1}, a_{2}, a_{3}}$
- id: c
  content: |-
    $3$
- id: d
  content: |-
    $6$
- id: e
  content: |-
    $-3$
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
