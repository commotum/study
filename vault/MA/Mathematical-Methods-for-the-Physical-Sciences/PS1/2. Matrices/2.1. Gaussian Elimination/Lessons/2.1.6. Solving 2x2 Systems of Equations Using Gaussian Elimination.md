# Solving 2x2 Systems of Equations Using Gaussian Elimination

<!--
lesson-id: 151
topic-code: PS1.2.1.6
-->

## Table of Contents

- [Introduction](#introduction)
- [The Uniqueness of the Row Echelon Form Matrix](#the-uniqueness-of-the-row-echelon-form-matrix)
- [Reducing a 2x2 Augmented Matrix to REF Using Gaussian Elimination](#reducing-a-2x2-augmented-matrix-to-ref-using-gaussian-elimination)
- [Solving a 2x2 System of Equations Using Gaussian Elimination](#solving-a-2x2-system-of-equations-using-gaussian-elimination)

## Prerequisites

- [Solving Systems of Equations Using Back Substitution](<2.1.3. Solving Systems of Equations Using Back Substitution.md>)
- [Creating Rows or Columns Containing Zeros Using Gaussian Elimination](<2.1.5. Creating Rows or Columns Containing Zeros Using Gaussian Elimination.md>)

---

<a id="introduction"></a>
## Introduction

Consider the following system of linear equations and its respective **augmented matrix**:

$$
2x + 6y =-2; 3x - y = 2
$$

If we can convert the augmented matrix $M$ to row echelon form, then we can solve the system using back substitution.

The procedure for reducing $M$ to row echelon form using elementary row operations is called **Gaussian elimination**. To perform Gaussian elimination on a $2 \times 2$ matrix, we use the following procedure.

**Step 1:** Consider the entry $a_{11}=2$ of the coefficient matrix. This entry is called a **pivot element**. We want to make this entry equal to $1$. So, the first elementary row operation will be

$$
R_1:= \dfrac{1}{2}R_1
$$

and we have

$$
M \mid ∼\begin{bmatrix}2 & 6 & -2 \\ 3 & -1 & 2\end{bmatrix} \mid R_{1}:=\frac{1}{2}R_{1}; ∼\begin{bmatrix}1 & 3 & -1 \\ 3 & -1 & 2\end{bmatrix}
$$

**Step 2:** Consider the entry $a_{21}=3$ below the first pivot element. We want to make this entry equal to $0$. So, the second elementary row operation will be

$$
R_2:=R_2+(-3)R_1
$$

and we have

$$
M \mid ∼\begin{bmatrix}1 & 3 & -1 \\ 3 & -1 & 2\end{bmatrix} \mid R_{2}:=R_{2} + (-3)R_{1}; ∼\begin{bmatrix}1 & 3 & -1 \\ 0 & -10 & 5\end{bmatrix}
$$

**Step 3:** Finally, consider the entry $a_{22}=-10$. This entry is the second pivot element, and we would like to make it equal to $1$. So, the third elementary row operation will be

$$
R_2:=\dfrac{1}{-10}R_2
$$

and we get $M$ in row echelon form:

$$
M \mid ∼\begin{bmatrix}1 & 3 & -1 \\ 0 & -10 & 5\end{bmatrix} \mid R_{2}:=\frac{1}{-10}R_{2}; ∼\begin{bmatrix}1 & 3 & -1 \\ 0 & 1 & -\frac{1}{2}\end{bmatrix}
$$

The system is now

$$
x + 3y =-1; y =-\frac{1}{2}
$$

and we can apply back substitution. The final equation

$$
y=-\dfrac{1}{2}
$$

tells us the value of $y$. Substituting this value into the first equation, we get

$$
x+3 \left(-\dfrac{1}{2} \right) = -1 \qquad\Rightarrow\qquad x=\dfrac{1}{2}
$$

Therefore, the solution is

$$
x=\dfrac{1}{2}
$$

and

$$
y=-\dfrac{1}{2}
$$

Or, in vector form,

$$
\left< \dfrac{1}{2}, -\dfrac{1}{2} \right>
$$

---

<a id="the-uniqueness-of-the-row-echelon-form-matrix"></a>
## The Uniqueness of the Row Echelon Form Matrix

The row echelon form of a matrix is *not* unique. In the previous example, after the first row operation we arrived at

$$
\begin{bmatrix}1 & 3 & -1 \\ 0 & -10 & 5\end{bmatrix}
$$

This augmented matrix is already in row echelon form! However, it is always useful to do an extra step and obtain $1$'s along the main diagonal, like we did previously:

$$
\begin{bmatrix}1 & 3 & -1 \\ 0 & 1 & -\frac{1}{2}\end{bmatrix}
$$

Having $1$'s along the main diagonal makes the final back substitution process a little easier.

---

<a id="reducing-a-2x2-augmented-matrix-to-ref-using-gaussian-elimination"></a>
## Reducing a 2x2 Augmented Matrix to REF Using Gaussian Elimination

**Example:** Consider the augmented matrix $M$ given by
$\begin{bmatrix}1 & 2 & 0 \\ 3 & -2 & -8\end{bmatrix}$

Use the standard method of Gaussian elimination to reduce $M$ to row echelon form, and then solve the linear system represented by the matrix.

**Explanation**

Reducing $M$ to row echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}1 & 2 & 0 \\ 3 & -2 & -8\end{bmatrix} & R_{2}\end{vmatrix}:=R_{2} + (-3)R_{1}; ∼\begin{bmatrix}1 & 2 & 0 \\ 0 & -8 & -8\end{bmatrix} \mid R_{2} \mid:=-\frac{1}{8}R_{2}; ∼\begin{bmatrix}1 & 2 & 0 \\ 0 & 1 & 1\end{bmatrix}
$$

The system is now

$$
x + 2y = 0; y = 1
$$

and we can apply back substitution. The final equation $y=1$ tells us the value of $y$. Substituting this value into the first equation, we get

$$
x+2(1)=0 \quad\Rightarrow\quad x=-2
$$

Therefore, the solution is $x=-2$ and $y=1$. Or, in vector form,

$$
\left< -2,1 \right>
$$

---

**Question 1:** Consider the augmented matrix $M$ given by
$M = \begin{bmatrix}3 & -9 & -21 \\ 1 & 1 & 5\end{bmatrix}$.
Use the standard method of Gaussian elimination to reduce $M$ to row echelon form.

- [ ] A. $M∼\begin{bmatrix}1 & -9 & -1 \\ 0 & 1 & 1\end{bmatrix}$
- [ ] B. $M∼\begin{bmatrix}1 & -3 & -7 \\ 0 & 1 & 1\end{bmatrix}$
- [ ] C. $M∼\begin{bmatrix}1 & -3 & -7 \\ 0 & 1 & 3\end{bmatrix}$
- [ ] D. $M∼\begin{bmatrix}1 & -9 & -5 \\ 0 & 1 & 3\end{bmatrix}$
- [ ] E. $M∼\begin{bmatrix}1 & -3 & -7 \\ 0 & 1 & 2\end{bmatrix}$

---

**Question 2:** Consider the augmented matrix $M$ given by
$M = \begin{bmatrix}1 & 4 & 3 \\ 2 & 9 & 5\end{bmatrix}$.
Use the standard method of Gaussian elimination to reduce $M$ to row echelon form, and then solve the linear system represented by the matrix.

- [ ] A. $M∼\begin{bmatrix}1 & 4 & 1 \\ 0 & 1 & 1\end{bmatrix}$,
$x =-3$ and $y = 1$
- [ ] B. $M∼\begin{bmatrix}1 & 4 & 2 \\ 0 & 1 & -1\end{bmatrix}$,
$x = 1$ and $y =-1$
- [ ] C. $M∼\begin{bmatrix}1 & 4 & 3 \\ 0 & 1 & -1\end{bmatrix}$,
$x = 7$ and $y =-1$
- [ ] D. $M∼\begin{bmatrix}1 & 4 & 3 \\ 0 & 1 & 1\end{bmatrix}$,
$x = 7$ and $y =-1$
- [ ] E. $M∼\begin{bmatrix}1 & 4 & 1 \\ 0 & 1 & -1\end{bmatrix}$,
$x = 1$ and $y =-1$

---

<a id="solving-a-2x2-system-of-equations-using-gaussian-elimination"></a>
## Solving a 2x2 System of Equations Using Gaussian Elimination

**Example:** Consider the system of linear equations
$-x + 5y = 5; x - 6y = 5$.
Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.

**Explanation**

The augmented matrix for this system is

$$
\begin{bmatrix}-1 & 5 & 5 \\ 1 & -6 & 5\end{bmatrix}
$$

Reducing $M$ to row echelon form, we have

$$
M\begin{vmatrix}∼\begin{bmatrix}-1 & 5 & 5 \\ 1 & -6 & 5\end{bmatrix} & R_{1}\end{vmatrix}:=-R_{1}; ∼\begin{bmatrix}1 & -5 & -5 \\ 1 & -6 & 5\end{bmatrix}\begin{vmatrix}R_{2} &:=R_{2} + (-1)R_{1} \\ ∼\begin{bmatrix}1 & -5 & -5 \\ 0 & -1 & 10\end{bmatrix} & R_{2}\end{vmatrix}:=-R_{2}; ∼\begin{bmatrix}1 & -5 & -5 \\ 0 & 1 & -10\end{bmatrix}
$$

The system is now

$$
x - 5y =-5; y =-10
$$

and we can apply back substitution. The final equation $y=-10$ tells us the value of $y$. Substituting this value into the first equation, we get

$$
x-5(-10)=-5 \quad\Rightarrow\quad x=-55
$$

Therefore, the solution is $x=-55$ and $y=-10$. Or, in vector form, $\langle -55,-10 \rangle$.

---

**Question 3:** Consider the system of linear equations
${2x + 7y =-5; 7x + 11y =-4$.
Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form.

- [ ] A. $M∼\begin{bmatrix}1 & 7 & -5 \\ 0 & 1 & 0\end{bmatrix}$
- [ ] B. $M∼\begin{bmatrix}1 & \frac{7}{2} & -\frac{5}{2} \\ 0 & 1 & -1\end{bmatrix}$
- [ ] C. $M∼\begin{bmatrix}1 & \frac{7}{2} & -\frac{5}{2} \\ 0 & 1 & -3\end{bmatrix}$
- [ ] D. $M∼\begin{bmatrix}1 & \frac{7}{2} & -\frac{5}{2} \\ 0 & 1 & 1\end{bmatrix}$
- [ ] E. $M∼\begin{bmatrix}1 & 7 & -5 \\ 0 & 1 & -2\end{bmatrix}$

---

**Question 4:** Consider the system of linear equations
${x - 3y =-8; 2x + 4y = 4$.
Use the standard method of Gaussian elimination to reduce the augmented matrix $M$ of the system to row echelon form, and then solve the linear system.

- [ ] A. $M∼\begin{bmatrix}1 & -3 & -8 \\ 0 & 1 & -2\end{bmatrix}$,
$x =-4$ and $y = 1$
- [ ] B. $M∼\begin{bmatrix}1 & -3 & -8 \\ 0 & 1 & 1\end{bmatrix}$,
$x =-4$ and $y = 1$
- [ ] C. $M∼\begin{bmatrix}1 & -3 & -8 \\ 0 & 1 & 2\end{bmatrix}$,
$x =-2$ and $y = 2$
- [ ] D. $M∼\begin{bmatrix}1 & -3 & -8 \\ 0 & 1 & 5\end{bmatrix}$,
$x =-2$ and $y = 2$
- [ ] E. $M∼\begin{bmatrix}1 & 0 & -8 \\ 0 & 1 & 2\end{bmatrix}$,
$x =-8$ and $y = 2$

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
