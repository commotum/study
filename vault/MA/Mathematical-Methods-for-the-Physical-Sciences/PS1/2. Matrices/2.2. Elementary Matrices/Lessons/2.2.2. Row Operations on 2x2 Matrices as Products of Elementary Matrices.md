# Row Operations on 2x2 Matrices as Products of Elementary Matrices

<!--
lesson-id: 1727
topic-code: PS1.2.2.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Row Operation That's Equivalent To Left Multiplication by an Elementary Matrix](#finding-a-row-operation-thats-equivalent-to-left-multiplication-by-an-elementary-matrix)
- [Finding a Matrix Product That's Equivalent To a Given Row Operation](#finding-a-matrix-product-thats-equivalent-to-a-given-row-operation)
- [Finding a Matrix Product That's Equivalent To a Sequence of Row Operations](#finding-a-matrix-product-thats-equivalent-to-a-sequence-of-row-operations)

## Prerequisites

- [Elementary 2x2 Matrices](<2.2.1. Elementary 2x2 Matrices.md>)

---

<a id="introduction"></a>
## Introduction

To compute the product of an elementary matrix and another matrix, one option is to perform the matrix multiplication as usual. However, there's an easier, quicker way.

Remember that every elementary matrix is the result of applying an elementary row operation to the identity matrix. If we multiply another matrix by an elementary matrix, we're effectively applying the elementary row operation to the other matrix.

For example, suppose we are asked to find the product $EA$ where
$\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}$

The elementary matrix $E$ comes from swapping the two rows of the identity matrix (

$$
R_1 \leftrightarrow R_2
$$

). So, to compute the product $EA$, all we have to do is swap the two rows of $A$:
$\begin{bmatrix}8 & 6 \\ 3 & 4\end{bmatrix}$

This may seem almost too easy. But it's true! And we can verify it by computing the product the long way, using matrix multiplication. Indeed, we get the same result:

$$
\begin{aligned}
EA &= \begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}\begin{bmatrix}3 & 4 \\ 8 & 6\end{bmatrix} \\
&= \begin{bmatrix}8 & 6 \\ 3 & 4\end{bmatrix}
\end{aligned}
$$

So, we say that $E \cdot A$ is equivalent to applying to $A$ the elementary row operation

$$
R_1 \leftrightarrow R_2
$$

**Watch out!** It's important to multiply $A$ from the *left*. If we multiply $A$ by an elementary matrix from the *right*, we will perform a *column* operation on $A$ (not a row operation). In this case, multiplying $A$ by $E$ from the right will have the effect of switching the *columns* of $A$, not the rows.

$$
\begin{aligned}
AE &= \begin{bmatrix}3 & 4 \\ 8 & 6\end{bmatrix}\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix} \\
&= \begin{bmatrix}4 & 3 \\ 6 & 8\end{bmatrix}
\end{aligned}
$$

---

<a id="finding-a-row-operation-thats-equivalent-to-left-multiplication-by-an-elementary-matrix"></a>
## Finding a Row Operation That's Equivalent To Left Multiplication by an Elementary Matrix

**Example:** Let $\begin{bmatrix}1 & 0 \\ 5 & 1\end{bmatrix}$ Find the row operation that, when applied to a $2\times 2$ matrix $A$, gives the result of the product $EA$.

**Explanation**

The matrix $E$ is obtained by taking the identity matrix and replacing the second row with the result of adding $5$ times the first row to the second row:

$$
\begin{aligned}
I &= \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \mid R_{2} \mid:=5R_{1} + R_{2} \\
&= ∼\begin{bmatrix}1 & 0 \\ 5 & 1\end{bmatrix} \\
&= E
\end{aligned}
$$

Therefore, $EA$ is equivalent to applying the row operation

$$
R_2:=5R_1+ R_2
$$

to $A$.

---

**Question 1:** Let $E = \begin{bmatrix}1 & -2 \\ 0 & 1\end{bmatrix}$. Find the row operation that, when applied to a $2 \cdot 2$ matrix $A$, gives the result of the product $EA$.

- [ ] A. $R_{2}:=R_{2} + (-2)R_{1}$
- [ ] B. $R_{2}:=-2R_{2}$
- [ ] C. $R_{1}:=R_{1} + (-2)R_{2}$
- [ ] D. $R_{2}:=R_{2} + 2R_{1}$
- [ ] E. $R_{1}:=-2R_{1}$

---

**Question 2:** Let $E = \begin{bmatrix}1 & 0 \\ 7 & 1\end{bmatrix}$. Find the row operation that, when applied to a $2 \cdot 2$ matrix $A$, gives the result of the product $EA$.

- [ ] A. $R_{2}:=7R_{2}$
- [ ] B. $R_{1}:=7R_{1}$
- [ ] C. $R_{2}:=R_{2} + 7R_{1}$
- [ ] D. $R_{2}:=R_{2} + (-7)R_{1}$
- [ ] E. $R_{1}:=R_{1} + 7R_{2}$

---

<a id="finding-a-matrix-product-thats-equivalent-to-a-given-row-operation"></a>
## Finding a Matrix Product That's Equivalent To a Given Row Operation

**Example:** Suppose we are applying the elementary row operation $R_1:=R_1 - R_2$ to a $2\times 2$ matrix $A$. Find the equivalent notation in terms of matrix multiplication.

**Explanation**

We need to find an elementary matrix $E$ such that $EA$ is equivalent to applying the elementary row operation

$$
R_1:=R_1 - R_2
$$

to $A$.

To find $E$, we apply the given row operation to the identity matrix as follows:

$$
\begin{aligned}
I &= \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \mid R_{1} \mid:=R_{1} - R_{2} \\
&= ∼\begin{bmatrix}1 & -1 \\ 0 & 1\end{bmatrix} \\
&= E
\end{aligned}
$$

Therefore, the equivalent notation in terms of matrix multiplication is $EA$:
$\begin{bmatrix}1 & -1 \\ 0 & 1\end{bmatrix}$

---

**Question 3:** Suppose we are applying the elementary row operation $R_{1}:=-\frac{1}{4}R_{1}$ to a $2 \cdot 2$ matrix $A$. Find the equivalent notation in terms of matrix multiplication.

- [ ] A. $\begin{bmatrix}-\frac{1}{4} & 0 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] B. $A \cdot \begin{bmatrix}-\frac{1}{4} & 0 \\ 0 & 1\end{bmatrix}$
- [ ] C. $A \cdot \begin{bmatrix}-1 & 0 \\ 4 & 1\end{bmatrix}$
- [ ] D. $\begin{bmatrix}1 & 0 \\ -\frac{1}{4} & 1\end{bmatrix} \cdot A$
- [ ] E. $A \cdot \begin{bmatrix}1 & 0 \\ 0 & -\frac{1}{4}\end{bmatrix}$

---

**Question 4:** Suppose we are applying the elementary row operation $R_{1}:=R_{1} + 4R_{2}$ to a $2 \cdot 2$ matrix $A$. Find the equivalent notation in terms of matrix multiplication.

- [ ] A. $\begin{bmatrix}1 & 4 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] B. $\begin{bmatrix}4 & 1 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] C. $A \cdot \begin{bmatrix}1 & 4 \\ 0 & 1\end{bmatrix}$
- [ ] D. $A \cdot \begin{bmatrix}4 & 0 \\ 0 & 1\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 & 0 \\ 0 & 4\end{bmatrix} \cdot A$

---

<a id="finding-a-matrix-product-thats-equivalent-to-a-sequence-of-row-operations"></a>
## Finding a Matrix Product That's Equivalent To a Sequence of Row Operations

**Example:** Suppose that the following elementary row operations, in the given order, are to be applied to a $2\times 2$ matrix $A$:
$R_1 \leftrightarrow R_2, \qquad R_2:=R_2+3R_1$
Find the equivalent notation in terms of matrix multiplication.

**Explanation**

First, let's find the elementary matrices corresponding to the two given elementary row operations.

- The first elementary row operation is $R_1 \leftrightarrow R_2$. The corresponding elementary matrix can be found by applying this same elementary row operation to the identity matrix as follows:
$I|= \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \mid R_{1} \mid ↔R_{2}; ∼\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}; = E_{1}$
- The second elementary row operation is $R_2:=R_2+3R_1$. The corresponding elementary matrix can be found by applying this same elementary row operation to the identity matrix as follows:
$I|= \begin{bmatrix}1 & 0 \\ 0 & 1\end{bmatrix} \mid R_{2} \mid:=R_{2} + 3R_{1}; ∼\begin{bmatrix}1 & 0 \\ 3 & 1\end{bmatrix}; = E_{2}$

To apply the first elementary row operation to the matrix $A$, we multiply by $E_1$ on the left and get $E_1 A$:

$$
\begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix}
$$

Then, to apply the second elementary row operation to $E_1 A$, we multiply by $E_2$ on the left and get $E_2 E_1 A$:

$$
Missing \end{bmatrix}
$$

---

**Question 5**

Suppose that the following elementary row operations, in the given order, are to be applied to a $2 \cdot 2$ matrix $A$:

$R_{2}:=-2R_{2}, R_{1}↔R_{2}, R_{2}:=R_{2} + 3R_{1}$
Find the equivalent notation in terms of matrix multiplication.

- [ ] A. $\begin{bmatrix}1 & 0 \\ 3 & 1\end{bmatrix} \cdot \begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix} \cdot \begin{bmatrix}-2 & 0 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] B. $\begin{bmatrix}1 & 0 \\ 3 & 1\end{bmatrix} \cdot \begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ 0 & -2\end{bmatrix} \cdot A$
- [ ] C. $\begin{bmatrix}1 & 3 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & 1 \\ 1 & 0\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ 0 & -2\end{bmatrix} \cdot A$
- [ ] D. $\begin{bmatrix}1 & 3 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}0 & 1 \\ 1 & 0\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ 0 & -2\end{bmatrix} \cdot A$
- [ ] E. $\begin{bmatrix}1 & 3 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & 1 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ 0 & -2\end{bmatrix} \cdot A$

---

**Question 6:** Suppose that the following elementary row operations, in the given order, are to be applied to a $2 \cdot 2$ matrix $A$:
$R_{1}:=R_{1} - 5R_{2}, R_{1}:=3R_{1}$
Find the equivalent notation in terms of matrix multiplication.

- [ ] A. $\begin{bmatrix}3 & 0 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & -5 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] B. $\begin{bmatrix}1 & 0 \\ 0 & 3\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix} \cdot A$
- [ ] C. $\begin{bmatrix}1 & 0 \\ 0 & 3\end{bmatrix} \cdot \begin{bmatrix}1 & -5 \\ 0 & 1\end{bmatrix} \cdot A$
- [ ] D. $\begin{bmatrix}3 & 0 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ 0 & -5\end{bmatrix} \cdot A$
- [ ] E. $\begin{bmatrix}3 & 0 \\ 0 & 1\end{bmatrix} \cdot \begin{bmatrix}1 & 0 \\ -5 & 1\end{bmatrix} \cdot A$

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
