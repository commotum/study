# The Curvature of a Plane Curve

<!--
lesson-id: 1839
topic-code: PS1.7.2.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Curvature of a Plane Curve Given in Parametric Form](#finding-the-curvature-of-a-plane-curve-given-in-parametric-form)
- [Finding the Radius of Curvature of a Plane Curve Given in Parametric Form](#finding-the-radius-of-curvature-of-a-plane-curve-given-in-parametric-form)
- [The Curvature of a Plane Curve Given in Cartesian Form](#the-curvature-of-a-plane-curve-given-in-cartesian-form)
- [Finding the Curvature of a Plane Curve Given in Cartesian Form](#finding-the-curvature-of-a-plane-curve-given-in-cartesian-form)
- [Deriving the Formula for the Curvature of a Plane Parametric Curve](#deriving-the-formula-for-the-curvature-of-a-plane-parametric-curve)

## Prerequisites

- [Radius of Curvature](<7.2.3. Radius of Curvature.md>)
- [Relating Concavity to the Second Derivative](<5.2.9. Relating Concavity to the Second Derivative.md>)

---

<a id="introduction"></a>
## Introduction

For a plane curve $C$ defined by the vector-valued function

$$
\mathbf r(t) = \langle x(t), \: y(t) \rangle
$$

the *curvature* of $C$ at an arbitrary point can be found using the following formula:

$$
\kappa(t) = \dfrac{\mid x'(t)y''(t) - y'(t)x''(t) \mid}{(x'(t)^2 + y'(t)^2)^{3/2}}
$$

Additionally, the **signed curvature** $k$ is given by

$$
k(t) = \dfrac{x'(t)y''(t) - y'(t)x''(t)}{(x'(t)^2 + y'(t)^2)^{3/2}}
$$

For example, consider a curve defined by the parametric equations

$$
x(t) = 2t, \qquad y(t) = 1+t^2
$$

Let's find the curvature of this curve at the point where $t=1$.

First, we calculate the first and second derivatives of the components:

$$
\begin{bmatrix}x^{′}(t) = 2, & y^{′}(t) = 2t \\ x^{″}(t) = 0, & y^{″}(t) = 2\end{bmatrix}
$$

Substituting these expressions into our curvature formula, we get the following:

$$
\begin{aligned}
κ(t) &= (\mid 2 \cdot 2 - 2t \cdot 0\begin{vmatrix})/([2^{2} + (2t)^{2}]^{3/2}) \\ = \frac{\mid 4\end{vmatrix}}{2^{3}[1 + t^{2}]^{3/2}} \\
&= (1)/(2(t^{2} + 1)^{3/2})
\end{aligned}
$$

Therefore, the curvature at $t =1$ is

$$
\begin{aligned}
κ(1) &= (1)/(2(1^{2} + 1)^{3/2}) \\
&= \frac{1}{4\sqrt{2}} \\
&= \frac{\sqrt{2}}{8}
\end{aligned}
$$

---

<a id="finding-the-curvature-of-a-plane-curve-given-in-parametric-form"></a>
## Finding the Curvature of a Plane Curve Given in Parametric Form

**Example:** The signed curvature of the plane curve $\mathbf r(t) = \big\langle t, \: \sin t \big\rangle$ is given by
$k (t) = \dfrac{\bbox[2pt, border: 1pt solid black]{\phantom{AA}}}{\sqrt{(1+\cos^2 t)^3}}, \quad t\neq 0$.
What is the missing part of the expression?

**Explanation**

The signed curvature $k(t)$ of a plane curve

$$
\mathbf r(t) = \left \langle x(t), y(t) \right \rangle
$$

is given by

$$
k (t) = \dfrac {x'(t) y''(t) - y'(t) x''(t)} {\big([x'(t)]^2 + [y'(t)]^2\big)^{3/2}}
$$

Along our curve, we have

$$
x(t) = t, \qquad y(t) = \sin t
$$

First, we calculate the derivatives of the components:

$$
\begin{aligned}
\begin{bmatrix}x^{′}(t) &= 1, & y^{′}(t)|= \cos t \\ x^{″}(t)|= 0, & y^{″}(t)|=-\sin t\end{bmatrix}
\end{aligned}
$$

Substituting these into the formula for the signed curvature, we get:

$$
\begin{aligned}
k(t) &= (1 \cdot (-\sin t) - 0 \cdot \cos t)/((1^{2} + (\cos t)^{2})^{3/2}) \\
&= (-\sin t)/((1 + \cos^{2} t)^{3/2}) \\
&= (-sin ⁡ t)/(\sqrt{(1 + \cos^{2} t)^{3}})
\end{aligned}
$$

---

**Question 1:** The curvature of the plane curve $r(t) =-t^{2}i + 2t^{3}j$ is given by
$κ(t) = \frac{3}{2 \mid t \mid \cdot A A A A A A}, t \ne 0$.
What is the missing part of the expression?

- [ ] A. $\sqrt{1 + 9t^{2}}$
- [ ] B. $\sqrt{4 + t^{2}}$
- [ ] C. $(2 + t)^{3}$
- [ ] D. $\sqrt{(1 + 9t^{2})^{3}}$
- [ ] E. $\sqrt{(4 + t^{2})^{3}}$

---

**Question 2:** The signed curvature of the plane curve $r(t) = ⟨\frac{1}{t}, t⟩$ is given by
$κ(t) = (A A)/(\sqrt{(1 + t^{4})^{3}}), t \ne 0$.
What is the missing part of the expression?

- [ ] A. $-2t^{4}$
- [ ] B. $2t$
- [ ] C. $-2t$
- [ ] D. $2t^{2}$
- [ ] E. $-2t^{3}$

---

<a id="finding-the-radius-of-curvature-of-a-plane-curve-given-in-parametric-form"></a>
## Finding the Radius of Curvature of a Plane Curve Given in Parametric Form

**Example:** Find the radius of curvature for the curve $\mathbf r(t) = t \, \mathbf i + \sin t\, \mathbf j$ at $t = \dfrac \pi 2$.

**Explanation**

The curvature $\kappa(t)$ of a plane curve

$$
\mathbf r(t) = x(t) \, \mathbf i + y(t)\,\mathbf j
$$

is given by

$$
\kappa (t) = \dfrac {\mid x'(t) y''(t) - y'(t) x''(t) \mid} {\big([x'(t)]^2 + [y'(t)]^2\big)^{3/2}}
$$

The radius of curvature $R$ is defined as

$$
R(t) = \dfrac 1 {\kappa(t)}
$$

Along our curve, we have

$$
x(t) = t, \qquad y(t) = \sin t
$$

First, we calculate the derivatives of the components:

$$
\begin{aligned}
\begin{bmatrix}x^{′}(t) &= 1, & y^{′}(t)|= \cos t \\ x^{″}(t)|= 0, & y^{″}(t)|=-\sin t\end{bmatrix}
\end{aligned}
$$

Substituting these into the formula for the curvature, we get the following:

$$
\begin{aligned}
κ(t) &= (\mid 1 \cdot (-\sin t) - \cos t \cdot 0\begin{vmatrix})/((1^{2} + (\cos t)^{2})^{3/2}) \\ = (\mid \sin t\end{vmatrix})/((1 + \cos^{2} t)^{3/2})
\end{aligned}
$$

Therefore, the curvature at

$$
t = \dfrac{\pi}2
$$

is

$$
\begin{aligned}
κ(\frac{π}{2}) &= (\mid \sin (\frac{π}{2}) \mid)/((1 + \cos^{2} (\frac{π}{2}))^{3/2}) \\
&= (1)/((1 + 0^{2})^{3/2}) \\
&= 1
\end{aligned}
$$

Finally, the radius of curvature at

$$
t = \dfrac{\pi}2
$$

is

$$
\begin{aligned}
R(\frac{π}{2}) &= (1)/(κ(\frac{π}{2})) = 1
\end{aligned}
$$

---

**Question 3:** Find the radius of curvature for the curve $r(t) = (1 - t)i + e^{t}j$ at $t = 0$.

- [ ] A. $2\sqrt{2}$
- [ ] B. $((1 + e)^{3/2})/(e)$
- [ ] C. $\frac{\sqrt{2}}{4}$
- [ ] D. $\frac{1}{\sqrt[3]{4}}$
- [ ] E. $\sqrt[3]{4}$

---

**Question 4:** Find the radius of the curvature for the curve $r(t) = ⟨e^{t}, e^{-t}⟩$ at $t = 0$.

- [ ] A. $\sqrt{2}$
- [ ] B. $e$
- [ ] C. $\frac{1}{\sqrt{2}}$
- [ ] D. $\frac{1}{2}$
- [ ] E. $2$

---

<a id="the-curvature-of-a-plane-curve-given-in-cartesian-form"></a>
## The Curvature of a Plane Curve Given in Cartesian Form

Suppose we have a plane curve defined by the explicit equation

$$
y = y(x)
$$

In this case, we can parametrize this curve as follows:

$$
\mathbf r(t) = \langle t, \: y(t) \rangle
$$

Now, using the fact that $x=t$, we calculate the first and second derivatives of the components:

$$
\begin{aligned}
\begin{bmatrix}x^{′}(t) &= 1, & y^{′}(t)|= y^{′}(x) \\ x^{″}(t)|= 0, & y^{″}(t)|= y^{″}(x)\end{bmatrix}
\end{aligned}
$$

Substituting these into the formula for the (unsigned) curvature, we get

$$
\begin{aligned}
κ(x) &= (\mid x^{′}(t)y^{″}(t) - y^{′}(t)x^{″}(t)\begin{vmatrix})/(([x^{′}(t)]^{2} + [y^{′}(t)]^{2})^{3/2}) \\ = (\mid 1 \cdot y^{″}(x) - y^{′}(x) \cdot 0 \mid)/((1^{2} + [y^{′}(x)]^{2})^{3/2}) \\ = (\mid y^{″}(x)\end{vmatrix})/((1 + [y^{′}(x)]^{2})^{3/2})
\end{aligned}
$$

Therefore, the unsigned curvature for $y=y(x)$ is given by

$$
\kappa (x) = \dfrac{\mid y''(x) \mid}{(1 + [y'(x)]^2)^{3/2}}
$$

and the signed curvature is

$$
k(x) = \dfrac {y''(x)} {(1+[y'(x)]^2)^{3/2}}
$$

Note that the signed curvature $k(x)$ has the same sign as $y''(x)$. Therefore,

- if $k(x) > 0$, the curve is concave up, and
- if $k(x) < 0$, the curve is concave down.

---

<a id="finding-the-curvature-of-a-plane-curve-given-in-cartesian-form"></a>
## Finding the Curvature of a Plane Curve Given in Cartesian Form

**Example:** Find the curvature of the curve $y = x^3$ at $x=-1$.

**Explanation**

The curvature $\kappa(x)$ of a plane curve $y = y(x)$ is given by

$$
\kappa(x) = \dfrac {\mid y''(x) \mid} {(1+[y'(x)]^2)^{3/2}}
$$

First, we calculate $y'(x)$ and $y''(x){:}$

$$
\begin{aligned}
y^{′}(x) &= 3x^{2}, \mid y^{″}(x)|= 6x
\end{aligned}
$$

Substituting these into the formula for the curvature, we get the following:

$$
\begin{aligned}
κ(x) &= (\mid 6x\begin{vmatrix})/([1 + (3x^{2})^{2}]^{3/2}) \\ = (\mid 6x\end{vmatrix})/((1 + 9x^{4})^{3/2})
\end{aligned}
$$

Therefore, the curvature at $x=-1$ is

$$
\begin{aligned}
κ(-1) &= (\mid 6(-1)\begin{vmatrix})/((1 + 9(-1)^{4})^{3/2}) \\ = (\mid - 6\end{vmatrix})/((1 + 9)^{3/2}) \\
&= \frac{6}{10^{3/2}} \\
&= \frac{6}{10\sqrt{10}} \\
&= \frac{3\sqrt{10}}{50}
\end{aligned}
$$

---

**Question 5:** Find the curvature of the curve $y = 2\ln (x - 1)$ at $x = 2$.

- [ ] A. $\frac{2\sqrt{3}}{3}$
- [ ] B. $\frac{2\sqrt{5}}{25}$
- [ ] C. $\frac{2\sqrt{3}}{27}$
- [ ] D. $\frac{2\sqrt{3}}{9}$
- [ ] E. $\frac{2\sqrt{5}}{5}$

---

**Question 6:** Find the signed curvature of the curve $y = 2x^{3} - x^{2} + 1$ at $x = 0$.

- [ ] A. $\frac{2\sqrt{3}}{5}$
- [ ] B. $2$
- [ ] C. $-\frac{\sqrt{3}}{2}$
- [ ] D. $-2$
- [ ] E. $-6$

---

<a id="deriving-the-formula-for-the-curvature-of-a-plane-parametric-curve"></a>
## Deriving the Formula for the Curvature of a Plane Parametric Curve

We've been using the following formula for unsigned curvature:

$$
\kappa (t) = \dfrac{\mid x'(t)y''(t) - y'(t)x''(t) \mid}{(x'(t)^2 + y'(t)^2)^{3/2}}
$$

To see where this formula comes from, remember that for a curve

$$
\mathbf r(t) = \langle x(t), y(t), z(t)\rangle
$$

the unsigned curvature formula is

$$
\kappa(t) = \dfrac{\|\mathbf r'(t) \times \mathbf r''(t)\|}{\|\mathbf r'(t)\|^3}
$$

If a curve does not have a $z$-component, then we may consider $z(t) = 0$. Thus, any plane curve

$$
\mathbf r(t) = \langle x(t), \: y(t) \rangle
$$

can be represented as

$$
\mathbf r(t) = \langle x(t), \: y(t), \: 0\rangle
$$

First, we calculate $\mathbf r'(t)$ and $\mathbf r''(t)$:

$$
\mathbf r'(t) = \langle x'(t), y'(t),0 \rangle, \qquad \mathbf r''(t) = \langle x''(t), y''(y),0 \rangle
$$

Substituting these expressions into our cross-product formula, we find that the curvature $\kappa(t)$ is given by

$$
\begin{aligned}
κ(t) &= (‖r^{′}(t) \cdot r^{″}(t)‖)/(‖r^{′}(t)‖^{3}) \\
&= (‖⟨x^{′}(t), y^{′}(t), 0⟩ \cdot ⟨x^{″}(t), y^{″}(t), 0⟩‖)/(‖⟨x^{′}(t), y^{′}(t)⟩‖^{3}) \\
&= (‖⟨0, 0, x^{′}(t)y^{″}(t) - y^{′}(t)x^{″}(t)⟩‖)/((\sqrt{x^{′}(t)^{2} + y^{′}(t)^{2}})^{3}) \\
&= (\mid x^{′}(t)y^{″}(t) - y^{′}(t)x^{″}(t) \mid)/((x^{′}(t)^{2} + y^{′}(t)^{2})^{3/2})
\end{aligned}
$$

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
