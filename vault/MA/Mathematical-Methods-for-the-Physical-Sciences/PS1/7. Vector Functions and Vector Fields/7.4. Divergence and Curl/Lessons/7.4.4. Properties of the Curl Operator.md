# Properties of the Curl Operator

<!--
lesson-id: 3700
topic-code: PS1.7.4.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding the Curl of a Vector Field Using Linearity Properties](#finding-the-curl-of-a-vector-field-using-linearity-properties)
- [The Product Rule for Curl](#the-product-rule-for-curl)
- [Finding the Curl of a Vector Field Using the Product Rule](#finding-the-curl-of-a-vector-field-using-the-product-rule)

## Prerequisites

- [The Curl of a Vector Field](<7.4.3. The Curl of a Vector Field.md>)
- [Gradient Vector Fields](<7.3.3. Gradient Vector Fields.md>)

---

<a id="introduction"></a>
## Introduction

Recall that if

$$
\mathbf F = P\,\mathbf i + Q\,\mathbf j + R\,\mathbf k
$$

is a vector field on $\mathbb R^3$, then the curl of $\mathbf F$ is given by

$$
\begin{bmatrix}i & j & k \\ \frac{∂}{∂x} & \frac{∂}{∂y} & \frac{∂}{∂z} \\ P & Q & R\end{bmatrix}
$$

The curl is a quantity that's made up of derivatives, so we would expect it to have similar properties to derivatives.

For example, if $\mathbf F$ and $\mathbf H$ are vector fields and $k$ is a scalar constant, then we have the following linearity properties:

$$
∇ \cdot (kF) = k∇ \cdot F; ∇ \cdot (F + G) = ∇ \cdot F + ∇ \cdot G
$$

Applying these rules can often help us to save time when computing curl. Let's see some examples.

---

<a id="finding-the-curl-of-a-vector-field-using-linearity-properties"></a>
## Finding the Curl of a Vector Field Using Linearity Properties

**Example:** Calculate $\nabla\times(3\mathbf{F} + \mathbf{G})$ at the point $(-1,1,0)$ for the vector fields $\mathbf F$ and $\mathbf G$, where

$\mathbf{F}(x,y,z) = 3x\,\mathbf{i} + y^2z\,\mathbf{j} + 2\,\mathbf{k}, \quad (\nabla\times\mathbf{G})(x,y,z) = 3y^2\,\mathbf{i} + 4z\,\mathbf{j} + \mathbf{k}$.

**Explanation**

If $\mathbf F$ and $\mathbf G$ are vector fields and $k$ is a constant, then we have the following linearity properties for curl:

- $\nabla \times (k \mathbf F) = k \nabla \times \mathbf F$
- $\nabla \times (\mathbf F + \mathbf G) = \nabla \times \mathbf F + \nabla \times \mathbf G$

In our case, we have

$$
\begin{aligned}
∇ \cdot (3F + G) &= ∇ \cdot (3F) + ∇ \cdot G \\
&= 3∇ \cdot F + ∇ \cdot G
\end{aligned}
$$

First, we need to calculate $\nabla \times \mathbf{F}$. In doing so, we get

$$
\begin{aligned}
∇ \cdot F &= \begin{vmatrix}i & j & k \\ \frac{∂}{∂x} & \frac{∂}{∂y} & \frac{∂}{∂z} \\ 3x & y^{2}z & 2\end{vmatrix} \\
&= [\frac{∂}{∂y}(2) - \frac{∂}{∂z}(y^{2}z)]i - [\frac{∂}{∂x}(2) - \frac{∂}{∂z}(3x)]j + [\frac{∂}{∂x}(y^{2}z) - \frac{∂}{∂y}(3x)]k \\
&= (0 - y^{2})i - (0 - 0)j + (0 - 0)k \\
&=-y^{2}i
\end{aligned}
$$

Applying the linearity properties, we get

$$
\begin{aligned}
∇ \cdot (3F + G) &= 3∇ \cdot F + ∇ \cdot G \\
&= 3[- y^{2}i] + [3y^{2}i + 4zj + k] \\
&=-3y^{2}i + 3y^{2}i + 4zj + k \\
&= (3y^{2} - 3y^{2})i + 4zj + k \\
&= 4zj + k
\end{aligned}
$$

Finally, evaluating at the point $(-1,1,0)$, we get

$$
\begin{aligned}
(∇ \cdot (F - G))(-1, 1, 0) &= 4(0)j + k = k
\end{aligned}
$$

---

**Question 1:**

```quiz
type: radio
id: ma-105798
content: |-
  Calculate $∇ \cdot (F - G)$ at the point $(-2, 0, 3)$ for the vector fields $F$ and $G$, where

  $F(x, y, z) = ze^{y}i + 3j - xk, (∇ \cdot G)(x, y, z) = 2xe^{y}i - 2e^{y}j$.
options:
- id: a
  content: |-
    $5j - 2k$
- id: b
  content: |-
    $-k$
- id: c
  content: |-
    $2i + 3k$
- id: d
  content: |-
    $4i + 4j - 3k$
  correct: true
- id: e
  content: |-
    $-2i - 5j + k$
```

---

**Question 2**

```quiz
type: radio
id: ma-41898
content: |-
  Calculate $∇ \cdot (F + 2G)$ at the point $(1, 1, 1)$ for the vector fields $F$ and $G$, where

  $F(x, y, z) = x^{2}zi + x^{2}yk, (∇ \cdot G)(x, y, z) = x^{2}zi + (xy - 2xyz)j - xzk$.
options:
- id: a
  content: |-
    $3i - 3j - 2k$
  correct: true
- id: b
  content: |-
    $5i - 2j + k$
- id: c
  content: |-
    $2i - j + k$
- id: d
  content: |-
    $3i - 4j - 4k$
- id: e
  content: |-
    $2i - 3j - 2k$
```

---

<a id="the-product-rule-for-curl"></a>
## The Product Rule for Curl

Recall that for functions $f(x)$ and $g(x)$, the product rule for differentiation is given by

$$
\dfrac{\textrm d}{\textrm d x}(f\cdot g) = f\dfrac{\textrm d}{\textrm d x}(g) + g\dfrac{\textrm d}{\textrm d x}(f)
$$

There is an analogous rule for computing the curl of the product of a vector field and a scalar field.

Suppose that $\mathbf F$ is a vector field and $f$ is a scalar field. Then, we have the following identity:

$$
\nabla\times(f\,\mathbf F) = f \, \nabla \times \mathbf {F}+ \nabla \! f \times \mathbf F
$$

where $\nabla f$ is the gradient of $f$.

Notice that the del operator $\nabla$ acts on the terms in the same way as the

$$
\dfrac{\textrm d}{\textrm d x}
$$

operator in the product rule for differentiation!

There are a few other tips that we can use to remember this rule:

- The term $\nabla\times(f\,\mathbf F)$ on the left-hand side must be a vector field because it's a curl. So, the two terms on the right-hand side must be vector fields, too.
- The term $f \, \nabla \times \mathbf {F}$ on the right-hand side is a vector field because $f$ is a scalar field, $\nabla \times \mathbf F$ is a vector field, and the product of a scalar and a vector field is a vector field. Note that this "product" is just regular multiplication.
- The term $\nabla \! f \times \mathbf F$ on the right-hand side is a vector field because both $\nabla f$ and $\mathbf F$ are vector fields, and the *cross-product* of two vector fields is a vector field.

---

<a id="finding-the-curl-of-a-vector-field-using-the-product-rule"></a>
## Finding the Curl of a Vector Field Using the Product Rule

**Example:** Consider the vector field $\mathbf F(x,y,z) = \langle 0, y-z, x-y\rangle$ and the scalar field $f(x,y,z) = x+y$. Given that
$\nabla\times \mathbf F = \langle 0,\,-1,\,0\rangle, \quad \nabla f = \langle 1,\, 1,\, 0\rangle$,
calculate $\nabla\times(f\,\mathbf F)$ at the point $(-1,1,3)$.

**Explanation**

We shall use the following identity:

$$
\nabla\times(f\,\mathbf F) = f \nabla \times \mathbf {F}+ \nabla f \times \mathbf F
$$

First, we compute $\nabla f \times \mathbf F$. We get

$$
\begin{aligned}
∇f \cdot F &= \begin{vmatrix}i & j & k \\ 1 & 1 & 0 \\ 0 & y - z & x - y\end{vmatrix} \\
&= [1 \cdot (x - y) - 0 \cdot (y - z)]i - [1 \cdot (x - y) - 0 \cdot (0)]j + [1 \cdot (y - z) - 1 \cdot (0)]k \\
&= (x - y)i + (y - x)j + (y - z)k \\
&= ⟨x - y, y - x, y - z⟩
\end{aligned}
$$

Therefore, we have

$$
\begin{aligned}
∇ \cdot (fF) &= f∇ \cdot F + ∇f \cdot F \\
&= (x + y)⟨0,-1, 0⟩ + ⟨x - y, y - x, y - z⟩ \\
&= ⟨0, - (x + y), 0⟩ + ⟨x - y, y - x, y - z⟩ \\
&= ⟨x - y,-2x, y - z⟩
\end{aligned}
$$

Finally,

$$
\begin{aligned}
∇ \cdot (fF)(-1, 1, 3) &= ⟨(-1) - 1,-2(-1), 1 - 3⟩ \\
&= ⟨ - 2, 2,-2⟩
\end{aligned}
$$

---

**Question 3:**

```quiz
type: radio
id: ma-105957
content: |-
  Consider the vector field $F(x, y, z) = ⟨z, 0, \ln x⟩$ and the scalar field $f(x, y, z) = e^{2x}$. Given that

  $∇ \cdot F = ⟨0, 1 - \frac{1}{x}, 0⟩, ∇f = ⟨2e^{2x}, 0, 0⟩$,
  calculate $∇ \cdot (fF)$ at the point $(1, 1,-4)$.
options:
- id: a
  content: |-
    $⟨1, 2e^{2} - 1, 0⟩$
- id: b
  content: |-
    $⟨2e^{2}, 1, 1⟩$
- id: c
  content: |-
    $⟨0, 0, e^{2}⟩$
- id: d
  content: |-
    $⟨0, 0, 0⟩$
  correct: true
- id: e
  content: |-
    $⟨1,-e^{2}, 2e^{2}⟩$
```

---

**Question 4**

```quiz
type: radio
id: ma-105134
content: |-
  Consider the vector field $F(x, y, z) = ⟨x^{2}, xy, 0⟩$ and the scalar field $f(x, y, z) = xz$. Given that

  $∇ \cdot F = ⟨0, 0, y⟩, ∇f = ⟨z, 0, x⟩$,
  calculate $∇ \cdot (fF)$ at the point $(1, 1, 1)$.
options:
- id: a
  content: |-
    $⟨ - 1, 1, 2⟩$
  correct: true
- id: b
  content: |-
    $⟨2, 0,-1⟩$
- id: c
  content: |-
    $⟨2, 1,-1⟩$
- id: d
  content: |-
    $⟨2, 1, 1⟩$
- id: e
  content: |-
    $⟨1, 1, 1⟩$
```

```update-progress
```

[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/Home|Home]]
[[MA/Mathematical-Methods-for-the-Physical-Sciences/PS1/0. Table of Contents/TOC|Table of Contents]]
