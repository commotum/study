# Unbounded Intervals

<!--
lesson-id: 4028
topic-code: MF1.5.6.10
-->

## Table of Contents

- [Introduction](#introduction)
- [Converting Inequalities to Left-Bounded Intervals](#converting-inequalities-to-left-bounded-intervals)
- [Converting Left-Bounded Intervals to Inequalities](#converting-left-bounded-intervals-to-inequalities)
- [Right-Bounded Intervals](#right-bounded-intervals)
- [Converting Inequalities to Right-Bounded Intervals](#converting-inequalities-to-right-bounded-intervals)
- [Converting Right-Bounded Intervals to Inequalities](#converting-right-bounded-intervals-to-inequalities)

## Prerequisites

- [Interval Notation](<5.6.9. Interval Notation.md>)

---

<a id="introduction"></a>
## Introduction

Consider the following inequality:

$$
x > 0
$$

We can represent this inequality using a number line as follows:

![](<../Source/5.6.10. Unbounded Intervals/Images/54ef88735840139200de941c31faa476.png>)

How do we represent this inequality using interval notation?

Note the following:

- The left endpoint $x=0$ is *not* included. Therefore, we use a parenthesis $($ for this endpoint.
- The interval continues forever in the positive direction. To denote this, we must introduce the **infinity** symbol $\infty$. Infinity is not a number but a concept that means *larger than any number*.
- We use a right parenthesis $)$ in conjunction with the $\infty$ symbol.

Therefore, we can express the inequality $x > 0$ in interval notation as follows:

$$
x \in (0, \infty)
$$

**Watch Out!** Whenever $\infty$ appears in an interval, it is *always* associated with a right parenthesis. Infinity is *never* associated with a bracket, even if the other endpoint is.

---

<a id="converting-inequalities-to-left-bounded-intervals"></a>
## Converting Inequalities to Left-Bounded Intervals

**Example:** Express the inequality $x \geq 1$ in interval notation.

**Explanation**

The inequality

$$
x \geq 1
$$

corresponds to all numbers greater than or equal to $1$. A number line for this inequality is shown below:

![](<../Source/5.6.10. Unbounded Intervals/Images/abd3bd96b789edb126dcd95459f237a2.png>)

From the segment on the number line, we have the following:

- The left endpoint $1$ *is* included in the interval.
- There is no right endpoint - it continues forever. So, we write the interval with infinity as the right endpoint, not included.

Therefore, using interval notation, we represent

$$
x \geq 1
$$

as

$[MATH: x \in [1,\infty).]$

---

**Question 1:** The inequality $x > 3$ can be expressed in interval notation as

- [ ] A. $x ∈ (- ∞, 3)$
- [ ] B. $[MATH: x ∈ [3, ∞)]$
- [ ] C. $x ∈ (3, ∞)$
- [ ] D. $x ∈ (-3, ∞)$
- [ ] E. $[MATH: x ∈ [- 3, ∞)]$

---

**Question 2:** The inequality $t \ge - 1$ can be expressed in interval notation as

- [ ] A. $t ∈ (-1, ∞)$
- [ ] B. $[MATH: t ∈ [- 1, ∞)]$
- [ ] C. $t ∈ (- ∞,-1)$
- [ ] D. $[MATH: t ∈ [1, ∞)]$
- [ ] E. $t ∈ (1, ∞)$

---

<a id="converting-left-bounded-intervals-to-inequalities"></a>
## Converting Left-Bounded Intervals to Inequalities

**Example:** What inequality corresponds the interval $[MATH: x\in\left[5, \infty\right)?]$

**Explanation**

The statement $[MATH: x\in\left[5, \infty\right)]$ means that $x$ lies between $5$ and $\infty$, where

- the left endpoint $5$ *is* included, and
- the right endpoint $\infty$ is *not* included.

Therefore, the inequality represented by this interval is

$$
5 \leq x < \infty
$$

However, since all numbers are smaller than infinity, it suffices to write

$$
5 \leq x
$$

or, equivalently,

$$
x \geq 5
$$

---

**Question 3:** The interval $x ∈ (10, ∞)$ corresponds to the inequality

- [ ] A. $x > - 10$
- [ ] B. $x > 10$
- [ ] C. $x \ge 10$
- [ ] D. $x < 10$
- [ ] E. $x \le 10$

---

**Question 4**

The interval $[MATH: x ∈ [- 5, ∞)]$ corresponds to which inequality?

- [ ] A. $x > -5$
- [ ] B. $x \ge -5$
- [ ] C. $x < -5$
- [ ] D. $x \le -5$
- [ ] E. $x \ge 5$

---

<a id="right-bounded-intervals"></a>
## Right-Bounded Intervals

Now, consider the following inequality:

$$
x \lt 0
$$

We can represent this inequality using a number line as follows:

![](<../Source/5.6.10. Unbounded Intervals/Images/8f7d2e157bc7bcfcefc31716d49bf35a.png>)

To represent this inequality using interval notation, we note the following:

- The right endpoint $x=0$ is *not* included. Therefore, we use a parenthesis $)$ for this endpoint.
- The interval continues forever in the negative direction. To denote this, we use the **negative infinity** symbol $-\infty$. We use a left parenthesis $($ in conjunction with the negative infinity symbol.

Therefore, we can express the inequality $x < 0$ in interval notation as follows:

$$
x \in (-\infty, 0)
$$

---

<a id="converting-inequalities-to-right-bounded-intervals"></a>
## Converting Inequalities to Right-Bounded Intervals

**Example:** Express the inequality $x \le 1$ in interval notation.

**Explanation**

The inequality

$$
x \leq 1
$$

corresponds to all numbers less than or equal to $1$. A number line for this interval is shown below:

![](<../Source/5.6.10. Unbounded Intervals/Images/62d0e54f359f877529711c6dd978bd6e.png>)

From the segment on the number line, we have the following:

- There is no left endpoint - it continues forever. So, we write the interval with negative infinity as the left endpoint, not included.
- The right endpoint $1$ *is* included in the interval.

Therefore, using interval notation, we represent

$$
x \leq 1
$$

as

$[MATH: x\in(-\infty, 1].]$

---

**Question 5:** The inequality $x < - 7$ can be expressed in interval notation as

- [ ] A. $[MATH: x ∈ [- 7, ∞)]$
- [ ] B. $x ∈ (- ∞,-7)$
- [ ] C. $[MATH: x ∈ (- ∞,-7]]$
- [ ] D. $x ∈ (-7, ∞)$
- [ ] E. $[MATH: x ∈ [7, ∞)]$

---

**Question 6:** The inequality $y \le 0$ can be expressed in interval notation as

- [ ] A. $y ∈ (- ∞, 0)$
- [ ] B. $[MATH: y ∈ (- ∞, 0]]$
- [ ] C. $y ∈ [- ∞, 1]$
- [ ] D. $[MATH: y ∈ [0, ∞)]$
- [ ] E. $[MATH: y ∈ (- ∞, 1]]$

---

<a id="converting-right-bounded-intervals-to-inequalities"></a>
## Converting Right-Bounded Intervals to Inequalities

**Example:** What inequality corresponds to the interval $x\in\left(-\infty, 9\right)$?

**Explanation**

The statement

$$
x \in \left(-\infty, 9\right)
$$

means that $x$ lies between $-\infty$ and $9$, where

- the left endpoint $-\infty$ is *not* included, and
- the right endpoint $9$ is *not* included.

Therefore, the inequality represented by this interval is
$-\infty < x < 9$.

However, since all numbers are greater than negative infinity, it suffices to write
$x < 9$.

---

**Question 7**

The interval notation $x ∈ (- ∞,-1)$ corresponds to which inequality?

- [ ] A. $x \le -1$
- [ ] B. $x < -1$
- [ ] C. $x > -1$
- [ ] D. $x \ge -1$
- [ ] E. $x < 1$

---

**Question 8:** The interval notation $y ∈ (- ∞,-\frac{7}{4})$ corresponds to the inequality

- [ ] A. $y \le - \frac{7}{4}$
- [ ] B. $y < - \frac{7}{4}$
- [ ] C. $y \ge - \frac{7}{4}$
- [ ] D. $y > - \frac{7}{4}$
- [ ] E. $y < - \frac{3}{4}$

```update-progress
```

[[MA/MF1/Home|Home]]
[[MA/MF1/0. Table of Contents/TOC|Table of Contents]]
