# Properties of Expectation for Discrete Random Variables

<!--
lesson-id: 2836
topic-code: MML.10.4.2
-->

## Table of Contents

- [Introduction](#introduction)
- [Computing the Expected Value of a Constant](#computing-the-expected-value-of-a-constant)
- [The Expected Value of a Scaled Random Variable](#the-expected-value-of-a-scaled-random-variable)
- [Computing the Expected Value of a Scaled Random Variable](#computing-the-expected-value-of-a-scaled-random-variable)
- [The Expected Value of a Transformed Random Variable](#the-expected-value-of-a-transformed-random-variable)
- [Computing the Expected Value of a Transformed Random Variable](#computing-the-expected-value-of-a-transformed-random-variable)

## Prerequisites

- [Expected Values of Discrete Random Variables](<10.4.1. Expected Values of Discrete Random Variables.md>)
- [One-to-One Transformations of Discrete Random Variables](<../../10.3. Transformations of Random Variables/Lessons/10.3.1. One-to-One Transformations of Discrete Random Variables.md>)

---

<a id="introduction"></a>
## Introduction

There are several important properties that we can use to simplify expressions involving expected values.

The first property is that the expected value of a constant is just the constant itself. That is, for any constant $a$, we have

$$
\textrm E[a] = a
$$

For example,

$$
\textrm E[5] = 5,\qquad \textrm E[-99] = -99,\qquad \textrm E[\sqrt 2] = \sqrt 2
$$

The idea that the expected value of a constant is equal to the same constant makes intuitive sense. If $a$ is a constant, then it does not vary. Therefore, it is always sure to have the same value.

---

<a id="computing-the-expected-value-of-a-constant"></a>
## Computing the Expected Value of a Constant

**Example:** Compute $\textrm E[1.2]$.

**Explanation**

For any constant $a$, we have

$$
\textrm E[a] = a
$$

Therefore,

$$
\textrm E[1.2] = 1.2
$$

---

**Question 1:** Compute $E[2]$.

- [ ] A. $2$
- [ ] B. $1$
- [ ] C. $\frac{1}{2}$
- [ ] D. $0$
- [ ] E. $4$

---

**Question 2:** Compute $E[c^{2}]$, where $c$ is a constant.

- [ ] A. $2c$
- [ ] B. $c^{2}$
- [ ] C. $\frac{1}{c^{2}}$
- [ ] D. $c$
- [ ] E. $\frac{1}{c}$

---

<a id="the-expected-value-of-a-scaled-random-variable"></a>
## The Expected Value of a Scaled Random Variable

We can also factor constants outside of an expected value expression. So for any constant $a$ and any random variable $X$, we have

$$
\textrm E[aX] = a \textrm E[X]
$$

For example,

$$
\textrm E[5X] = 5 \textrm E[X],\qquad\textrm E[-9X] = -9 \textrm E[X], \qquad \textrm E[\pi X] = \pi \textrm E[X]
$$

We can prove this rule using the definition of the expected value, as follows:

$$
\begin{aligned}
E[aX] &= ∑_(x ∈ S)ax \cdot f(x) \\
&= a∑_(x ∈ S)x \cdot f(x) \\
&= aE[X]
\end{aligned}
$$

---

<a id="computing-the-expected-value-of-a-scaled-random-variable"></a>
## Computing the Expected Value of a Scaled Random Variable

**Example:** A random variable $X$ has the probability distribution given below.

| $x$ | $0$ | $1$ | $2$ | $3$ | $4$ |
| --- | ---: | ---: | ---: | ---: | ---: |
| $f(x)$ | $\dfrac{1}{5}$ | $\dfrac{1}{10}$ | $\dfrac{3}{10}$ | $\dfrac{3}{10}$ | $\dfrac{1}{10}$ |

Calculate $\textrm E\left[\dfrac{1}{4}X\right]$.

**Explanation**

First, let's find $\textrm E[X]$.

The expected value of a discrete random variable $X$ with probability mass function $f(x)$ and sample space $S$ is given by

$$
\textrm E[X] = \sum\limits_{x \in S} x \cdot f(x)
$$

Summing up the products of each value of $X$ and its associated probability, we get

$$
\begin{aligned}
E[X] &= ∑_(x ∈ S)x \cdot f(x) \\
&= 0 \cdot \frac{1}{5} + 1 \cdot \frac{1}{10} + 2 \cdot \frac{3}{10} + 3 \cdot \frac{3}{10} + 4 \cdot \frac{1}{10} \\
&= 2
\end{aligned}
$$

Therefore,

$$
\begin{aligned}
E[\frac{1}{4}X] &= E[\frac{1}{4}X] \\
&= \frac{1}{4}E[X] \\
&= \frac{1}{4} \cdot 2 \\
&= \frac{1}{2}
\end{aligned}
$$

---

**Question 3**

> A calculator is required to answer this question.

Compute $E[- 5X]$ if $E[X] =-1$.

- [ ] A. $-1$
- [ ] B. $-5$
- [ ] C. $1$
- [ ] D. $5$
- [ ] E. $0$

---

**Question 4**

> A calculator is required to answer this question.

A random variable $X$ has the probability distribution given below.

| $x$ | $0$ | $1$ | $2$ | $3$ | $4$ |
| --- | ---: | ---: | ---: | ---: | ---: |
| $f(x)$ | $0.1$ | $0.2$ | $0.25$ | $0.15$ | $0.3$ |

Calculate $E[3X]$.

- [ ] A. $7.05$
- [ ] B. $7.15$
- [ ] C. $6.45$
- [ ] D. $6.85$
- [ ] E. $6.95$

---

<a id="the-expected-value-of-a-transformed-random-variable"></a>
## The Expected Value of a Transformed Random Variable

Finally, we can combine the factor and constant rules into one simple rule.

Suppose that $X$ is a random variable and $a$ and $b$ are constants. By combining our two rules, we have

$$
\textrm E[aX + b] = a\textrm E[X] + b
$$

For example, if we know that

$$
\textrm{E}[X] = 4
$$

then

$$
\begin{aligned}
E[3X + 2] &= E[3X + 2] \\
&= 3E[X] + 2 \\
&= 3(4) + 2 \\
&= 12 + 2 \\
&= 14
\end{aligned}
$$

---

<a id="computing-the-expected-value-of-a-transformed-random-variable"></a>
## Computing the Expected Value of a Transformed Random Variable

**Example:** Compute $\textrm E[1-4X]$ if $\textrm E[X]=\dfrac{1}{2}$.

**Explanation**

For any constants $a$ and $b$ and any random variable $X$, we have

$$
\textrm E[aX+b] = a \textrm E[X]+b
$$

Therefore,

$$
\begin{aligned}
E[1 - 4X] &= E[- 4X + 1] \\
&=-4E[X] + 1 \\
&=-4(\frac{1}{2}) + 1 \\
&=-2 + 1 \\
&=-1
\end{aligned}
$$

---

**Question 5**

> A calculator is required to answer this question.

Compute $E[5 - 2X]$ if $E[X] = 3$.

- [ ] A. $2$
- [ ] B. $0$
- [ ] C. $1$
- [ ] D. $-1$
- [ ] E. $-2$

---

**Question 6**

> A calculator is required to answer this question.

A random variable $X$ has the probability distribution given below.

| $x$ | $0$ | $1$ | $2$ | $3$ | $4$ |
| --- | ---: | ---: | ---: | ---: | ---: |
| $f(x)$ | $0.1$ | $0.2$ | $0.25$ | $0.15$ | $0.3$ |

Calculate $E[5X + 4]$.

- [ ] A. $17.45$
- [ ] B. $18.15$
- [ ] C. $16.85$
- [ ] D. $15.75$
- [ ] E. $13.75$

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
