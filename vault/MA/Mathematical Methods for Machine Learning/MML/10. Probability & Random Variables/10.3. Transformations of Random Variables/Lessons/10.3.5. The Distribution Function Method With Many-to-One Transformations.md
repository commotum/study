# The Distribution Function Method With Many-to-One Transformations

<!--
lesson-id: 3637
topic-code: MML.10.3.5
-->

## Table of Contents

- [Introduction](#introduction)
- [A Worked Example](#a-worked-example)
- [Applying Two-to-One Transformations to Random Variables](#applying-two-to-one-transformations-to-random-variables)
- [One-to-One and Many-to-One Transformations](#one-to-one-and-many-to-one-transformations)
- [One-to-One and Many-to-One Transformations](#one-to-one-and-many-to-one-transformations)

## Prerequisites

- [The Distribution Function Method](<10.3.3. The Distribution Function Method.md>)
- [Further Rules of Absolute Value](<../../../../AG1/7. Absolute Value/7.1. Absolute Value Expressions, Equations & Inequalities/Lessons/7.1.3. Further Rules of Absolute Value.md>)

---

<a id="introduction"></a>
## Introduction

Let $X$ be a continuous random variable, and suppose that

$Y = u(X)$,
where $u(X)$ is a function of $X$.

We can use the distribution function method to compute the PDF of $Y$. Broadly speaking, the distribution function method involves the following two steps:

- First, we find an expression for $F_Y$ in terms of $F_X$, where $F_Y$ and $F_X$ are the cumulative distribution functions of $Y$ and $X$, respectively.
- Then, we differentiate $F_Y$ to get the probability density function $f_Y$ of $Y$.

We've already seen how we use the distribution function technique to find the PDF of $Y$ in cases where $u(X)$ is strictly monotonic (i.e., strictly increasing or decreasing). If $u(X)$ is monotonic, then there is a one-to-one correspondence between $Y$ and $X$, so computing the PDF of $Y$ is reasonably straightforward.

We can also use the distribution function method in cases where $u$ is not monotonic, although a little more care is needed. This is what we'll focus on in this lesson.

---

<a id="a-worked-example"></a>
## A Worked Example

Let $X$ be a continuous random variable with the following probability density function:

$$
{\frac{3}{16}x^{2},\begin{vmatrix}0 < & x\end{vmatrix}< 2; 0, \mid otherwise
$$

Suppose that we define a new random variable $Y$ as
$Y = X^2-1$.

First, notice that $Y = X^2-1$ is two-to-one over the interval $0 < \mid x \mid < 2$, as shown below.

![](<../Source/10.3.5. The Distribution Function Method With Many-to-One Transformations/Images/9849b4b47b6713b513adbcb29422b96d.png>)

Moreover, for $0 < \mid x \mid < 2$, we have that $-1 \lt y \lt 3$.

Let's use the distribution function method to calculate the probability density function of $Y$.

Consider the interval $-1 < y < 3$. We write down the definition of the cumulative distribution of $Y$, and then use our transformation to isolate $X$ in the parentheses. This will allow us to write $F_Y$ in terms of $F_X$:

$$
\begin{aligned}
F_{Y}(y) &= P(Y \le y) \\
&= P(X^{2} - 1 \le y) \\
&= P(X^{2} \le y + 1) \\
&= P(- (y + 1)^{1/2} \le X \le (y + 1)^{1/2}) \\
&= F_{X}((y + 1)^{1/2}) - F_{X}(- (y + 1)^{1/2})
\end{aligned}
$$

Now, to compute the PDF $f_Y(y)$ for $-1 \lt y < 3$, we differentiate $F_Y$ with respect to $y$ using the chain rule, as usual. This gives

$$
\begin{aligned}
f_{Y}(y) &= F_{Y}^{′}(y) \\
&= \frac{1}{2}(y + 1)^{-1/2} \cdot F_{X}^{′}((y + 1)^{1/2}) + \frac{1}{2}(y + 1)^{-1/2} \cdot F_{X}^{′}(- (y + 1)^{1/2}) \\
&= \frac{1}{2}(y + 1)^{-1/2} \cdot f_{X}((y + 1)^{1/2}) + \frac{1}{2}(y + 1)^{-1/2} \cdot f_{X}(- (y + 1)^{1/2}) \\
&= \frac{1}{2}(y + 1)^{-1/2}(f_{X}((y + 1)^{1/2}) + f_{X}(- (y + 1)^{1/2}))
\end{aligned}
$$

Applying the definition of $f_X$, we finally arrive at

$$
\begin{aligned}
f_{Y}(y) &= \frac{1}{2}(y + 1)^{-1/2}(f_{X}((y + 1)^{1/2}) + f_{X}(- (y + 1)^{1/2})) \\
&= \frac{1}{2}(y + 1)^{-1/2}(\frac{3}{16}((y + 1)^{1/2})^{2} + \frac{3}{16}(- (y + 1)^{1/2})^{2}) \\
&= \frac{1}{2}(y + 1)^{-1/2}(\frac{3}{16}(y + 1) + \frac{3}{16}(y + 1)) \\
&= \frac{3}{16}(y + 1)^{-1/2}(y + 1) \\
&= \frac{3}{16}(y + 1)^{1/2} \\
&= \frac{3}{16}\sqrt{y + 1}
\end{aligned}
$$

Therefore, the full expression for the PDF of $Y$ is

$$
{\frac{3}{16}\sqrt{y + 1},\begin{vmatrix}-1 < y < 3 \\ 0,\end{vmatrix}otherwise
$$

---

<a id="applying-two-to-one-transformations-to-random-variables"></a>
## Applying Two-to-One Transformations to Random Variables

**Example:** Let $X$ be a continuous random variable with the following probability density function:
${\frac{1}{24}(x + 1)^{2},\begin{vmatrix}-3 < x < 3 \\ 0,\end{vmatrix}otherwise$
If $Y = \mid X \mid$, then what is the probability density function of $Y$?

**Explanation**

First, notice that the transformation

$$
Y = \mid X \mid
$$

is two-to-one over the interval $-3\lt x \lt 3$.

![](<../Source/10.3.5. The Distribution Function Method With Many-to-One Transformations/Images/2e7a58ced6ad56907eefe111353fef40.png>)

Moreover, for $-3\lt x \lt 3$, we have that

$$
0 \leq y \lt 3
$$

Now, using the definition of the cumulative distribution of $Y$, for

$$
0 \leq y < 3
$$

we have

$$
\begin{aligned}
F_{Y}(y) &= P(Y \le y) \\
&= P(\mid X \mid \le y) \\
&= P(-y \le X \le y) \\
&= F_{X}(y) - F_{X}(-y)
\end{aligned}
$$

To compute the PDF $f_Y(y)$ for

$$
0 \leq y < 3
$$

we differentiate $F_Y$ with respect to $y$ using the chain rule. This gives

$$
\begin{aligned}
f_{Y}(y) &= F_{Y}^{′}(y) \\
&= F_{X}^{′}(y) - (-1)F_{X}^{′}(-y) \\
&= F_{X}^{′}(y) + F_{X}^{′}(-y) \\
&= f_{X}(y) + f_{X}(-y)
\end{aligned}
$$

Applying the definition of $f_X$, we finally arrive at

$$
\begin{aligned}
f_{Y}(y) &= f_{X}(y) + f_{X}(-y) \\
&= \frac{1}{24}(y + 1)^{2} + \frac{1}{24}(-y + 1)^{2} \\
&= \frac{1}{24}[(y^{2} + 2y + 1) + (y^{2} - 2y + 1)] \\
&= \frac{1}{24}(2y^{2} + 2) \\
&= \frac{1}{12}(y^{2} + 1)
\end{aligned}
$$

Therefore, the full expression for the PDF of $Y$ is

$$
{\frac{1}{12}(y^{2} + 1),\begin{vmatrix}0 \le y < 3 \\ 0,\end{vmatrix}otherwise
$$

---

**Question 1:** Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{\sin x}{2},\begin{vmatrix}0 < x < π \\ 0,\end{vmatrix}otherwise$
If $Y = \mid X - \frac{π}{2} \mid$, then what is the probability density function of $Y$?

- [ ] A. $f_{Y}(y) = {-\frac{y\cos y}{2},\begin{vmatrix}0 \le y < π \\ 0,\end{vmatrix}otherwise$.
- [ ] B. $f_{Y}(y) = {\sin y,\begin{vmatrix}0 \le y < \frac{π}{2} \\ 0,\end{vmatrix}otherwise$.
- [ ] C. $f_{Y}(y) = {\frac{y\sin y}{π},\begin{vmatrix}0 \le y < \frac{π}{2} \\ 0,\end{vmatrix}otherwise$.
- [ ] D. $f_{Y}(y) = {\cos y,\begin{vmatrix}0 \le y < \frac{π}{2} \\ 0,\end{vmatrix}otherwise$.
- [ ] E. $f_{Y}(y) = {\frac{\sin y + \cos y}{2},\begin{vmatrix}0 \le y < π \\ 0,\end{vmatrix}otherwise$.

---

**Question 2:** Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{1}{18}x^{2},\begin{vmatrix}-3 < x < 3 \\ 0,\end{vmatrix}otherwise$
If $Y = X^{2}$, then what is the probability density function of $Y$?

- [ ] A. $f_{Y}(y) = {\frac{1}{18}\sqrt{y},\begin{vmatrix}0 \le y < 9 \\ 0,\end{vmatrix}otherwise$
- [ ] B. $f_{Y}(y) = {\frac{2}{3}y^{3/2},\begin{vmatrix}0 \le y < \sqrt{3} \\ 0,\end{vmatrix}otherwise$
- [ ] C. $f_{Y}(y) = {\frac{1}{18}y^{2/3},\begin{vmatrix}0 \le y < 9 \\ 0,\end{vmatrix}otherwise$
- [ ] D. $f_{Y}(y) = {\frac{2}{9}y,\begin{vmatrix}0 \le y < 3 \\ 0,\end{vmatrix}otherwise$
- [ ] E. $f_{Y}(y) = {\frac{2}{81}y,\begin{vmatrix}0 \le y < 9 \\ 0,\end{vmatrix}otherwise$

---

<a id="one-to-one-and-many-to-one-transformations"></a>
## One-to-One and Many-to-One Transformations

We know how to compute the PDF of $Y = u(X)$ in cases where $u$ is either one-to-one or two-to-one over the entire support of $X$. Let's now expand this to cases where $u(X)$ is both one-to-one and many-to-one over different intervals.

As an example, let $X$ be a continuous random variable with the following probability density function:
${e^{- (x + 1)},\begin{vmatrix}x > - 1 \\ 0,\end{vmatrix}otherwise$
Now suppose that the random variable $Y$ is defined as

$$
Y = \mid X \mid
$$

To compute the PDF of $Y$, we separately apply the distribution function method on the regions where $Y$ is one-to-one and many-to-one.

First, let's visualize the transformation

$$
Y = \mid X \mid
$$

over the interval $x > - 1$.

![](<../Source/10.3.5. The Distribution Function Method With Many-to-One Transformations/Images/136d1ca5a0a01288917bb0feba2a2936.png>)

Notice the following:

- The transformation is one-to-one over the interval $x \geq 1$. On this interval, $y \geq 1$.
- The transformation is two-to-one over the interval $-1 \lt x \lt 1$. On this interval, $0 \leq y \lt 1$.

So, we need to consider these two cases separately.

- First, we consider the case $y \geq 1$. Using the definition of the cumulative distribution of $Y$, for $y \geq 1$, we have
$F_{Y}(y)|= P(Y \le y); = P(\mid X \mid \le y); = P(X \le y); = F_{X}(y)$.
Notice that we used the fact that $\mid X \mid = X$ for $X > 0$.To compute the PDF $f_Y(y)$ for $y \geq 1$, we differentiate $F_Y$ with respect to $y$. This gives
$f_{Y}(y)|= F_{Y}^{′}(y); = \frac{d}{dy}(F_{X}(y)); = F_{X}^{′}(y); = f_{X}(y)$.Applying the definition of $f_X$, we finally arrive at
$f_{Y}(y)|= f_{X}(y); = e^{- (y + 1)}$.
- Next, we consider the case $0 \leq y \lt 1$. Using the definition of the cumulative distribution of $Y$, for $0 \leq y \lt 1$, we have
$F_{Y}(y)|= P(Y \le y); = P(\mid X \mid \le y); = P(-y \le X \le y); = F_{X}(y) - F_{X}(-y)$.To compute the PDF $f_Y(y)$ for $0 \leq y \lt 1$, we differentiate $F_Y$ with respect to $y$ using the chain rule. This gives
$f_{Y}(y)|= F_{Y}^{′}(y); = \frac{d}{dy}(F_{X}(y) - F_{X}(-y)); = F_{X}^{′}(y) + F_{X}^{′}(-y); = f_{X}(y) + f_{X}(-y)$.Applying the definition of $f_X$, we finally arrive at
$f_{Y}(y)|= f_{X}(y) + f_{X}(-y); = e^{- (y + 1)} + e^{- (-y + 1)}; = e^{- (y + 1)} + e^{y - 1}$

Therefore, the full expression for the PDF of $y$ is

$$
{e^{- (y + 1)} + e^{y - 1},\begin{vmatrix}0 \le y < 1 \\ e^{- (y + 1)},\end{vmatrix}y \ge 1; 0, \mid otherwise
$$

---

<a id="one-to-one-and-many-to-one-transformations"></a>
## One-to-One and Many-to-One Transformations

**Example:** Let $X$ be a continuous random variable with the following probability density function:
${\frac{5}{33}x^{4},\begin{vmatrix}-1 < x < 2 \\ 0,\end{vmatrix}otherwise$
If the probability density function of $Y = X^2$ is given by
${g(y),\begin{vmatrix}0 < y < 1 \\ h(y),\end{vmatrix}1 \le y < 4; 0, \mid otherwise$
find the function $h(y)$.

**Explanation**

First, let's visualize the transformation $Y = X^2$ over the interval $-1 < x < 2$.

![](<../Source/10.3.5. The Distribution Function Method With Many-to-One Transformations/Images/b6a51451e1ceb6a93abbcdad999d12e1.png>)

Notice the following:

- The transformation is two-to-one over the interval $-1 \lt x < 1$. On this interval, $Y$ is nonzero on $0 \lt y \lt 1$.
- The transformation is one-to-one over the interval $1 \leq x \lt 2$. On this interval, $Y$ is nonzero on $1 \leq y \lt 4$.

In this question, we're asked to consider only the interval

$$
1 \leq y \lt 4
$$

Using the definition of the cumulative distribution of $Y$, for

$$
1 \leq y \lt 4
$$

we have

$$
\begin{aligned}
F_{Y}(y) &= P(Y \le y) \\
&= P(X^{2} \le y) \\
&= P(\sqrt{X^{2}} \le \sqrt{y}) \\
&= P(\mid X \mid \le \sqrt{y}) \\
&= P(X \le \sqrt{y}) \\
&= F_{X}(\sqrt{y})
\end{aligned}
$$

Notice that we used

$$
\mid X \mid = X
$$

for $X > 0$.

To compute the PDF $f_Y(y)$ for

$$
1 \leq y \lt 4
$$

we differentiate $F_Y$ with respect to $y$ using the chain rule. This gives

$$
\begin{aligned}
f_{Y}(y) &= F_{Y}^{′}(y) \\
&= \frac{1}{2\sqrt{y}} \cdot F_{X}^{′}(\sqrt{y}) \\
&= \frac{1}{2\sqrt{y}} \cdot f_{X}(\sqrt{y})
\end{aligned}
$$

Applying the definition of $f_X$, we finally arrive at

$$
\begin{aligned}
f_{Y}(y) &= \frac{1}{2\sqrt{y}} \cdot f_{X}(\sqrt{y}) \\
&= \frac{1}{2\sqrt{y}} \cdot \frac{5}{33}(\sqrt{y})^{4} \\
&= \frac{1}{2\sqrt{y}} \cdot \frac{5}{33}y^{2} \\
&= \frac{5}{66}y^{3/2}
\end{aligned}
$$

Consequently,

$$
h(y) = \dfrac{5}{66}\,y^{3/2}
$$

---

**Question 3:** Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{1}{3}x^{2},\begin{vmatrix}-1 < x < 2 \\ 0,\end{vmatrix}otherwise$
If the probability density function of $Y = X^{2}$ is given by
$f_{Y}(y) = {g(y),\begin{vmatrix}0 < y < 1 \\ h(y),\end{vmatrix}1 \le y < 4; 0, \mid otherwise$
find the function $h(y)$.

- [ ] A. $\frac{1}{3}y$
- [ ] B. $\frac{1}{6}y^{1/2}$
- [ ] C. $\frac{2}{3}y^{1/2}$
- [ ] D. $\frac{1}{6}y$
- [ ] E. $\frac{1}{3}y^{1/2}$

---

**Question 4:** Let $X$ be a continuous random variable with the following probability density function:
$f_{X}(x) = {\frac{1}{3}x^{2},\begin{vmatrix}-1 < x < 2 \\ 0,\end{vmatrix}otherwise$
If the probability density function of $Y = X^{2}$ is given by
$f_{Y}(y) = {g(y),\begin{vmatrix}0 < y < 1 \\ h(y),\end{vmatrix}1 \le y < 4; 0, \mid otherwise$
find the function $g(y)$.

- [ ] A. $\frac{1}{3}y^{1/2}$
- [ ] B. $\frac{1}{6}y^{1/2}$
- [ ] C. $\frac{1}{3}y$
- [ ] D. $\frac{2}{3}y^{1/2}$
- [ ] E. $\frac{2}{3}y$

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
