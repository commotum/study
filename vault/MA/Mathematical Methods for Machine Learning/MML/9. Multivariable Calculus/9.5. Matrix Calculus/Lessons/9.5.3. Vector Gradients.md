# Vector Gradients

<!--
lesson-id: 5536
topic-code: MML.9.5.3
-->

## Table of Contents

- [Introduction](#introduction)
- [Gradient of a Linear Form With Respect to a Vector](#gradient-of-a-linear-form-with-respect-to-a-vector)
- [Computing Gradients of Linear Forms](#computing-gradients-of-linear-forms)
- [The Gradient of a Squared Norm](#the-gradient-of-a-squared-norm)
- [Computing Gradients Including Squared Norms](#computing-gradients-including-squared-norms)
- [The Gradient of the L1 Norm](#the-gradient-of-the-l1-norm)
- [Chain Rule to Vector Gradients](#chain-rule-to-vector-gradients)
- [Computing Gradients of Functions Containing L1 Norms](#computing-gradients-of-functions-containing-l1-norms)

## Prerequisites

- [Euclidean, Manhattan, and Minkowski Distance](<../../../6. Orthogonality & Projections/6.1. Inner Products/Lessons/6.1.3. Euclidean, Manhattan, and Minkowski Distance.md>)
- [Gradients With Respect to a Variable Subset](<../../9.3. Vector-Valued Functions/Lessons/9.3.7. Gradients With Respect to a Variable Subset.md>)
- [The Chain Rule for Total Derivatives](<9.5.2. The Chain Rule for Total Derivatives.md>)

---

<a id="introduction"></a>
## Introduction

In this lesson, we'll extend our knowledge of gradients of scalar-valued functions.

Recall that, for a function $f: \mathbb{R}^n \to \mathbb{R}$ that maps a vector $\mathbf{x} \in \mathbb{R}^n$ to a scalar value $f(\mathbf{x})$, the *gradient* of $f$ with respect to $\mathbf{x}$ is defined as the column vector of partial derivatives:

$$
\begin{bmatrix}\frac{∂f}{∂x_{1}} \\ \frac{∂f}{∂x_{2}} \\ ⋮ \\ \frac{∂f}{∂x_{n}}\end{bmatrix}
$$

**Note:** Some resources use

$$
\dfrac{\partial f}{\partial \mathbf x}
$$

to denote the gradient. We shall avoid this notation.

Recall that the *total derivative* of a scalar function $f: \mathbb{R}^n \to \mathbb{R}$ with respect to a vector $\mathbf{x} \in \mathbb{R}^n$ is the row vector

$$
\begin{bmatrix}\frac{∂f}{∂x_{1}} & \frac{∂f}{∂x_{2}} & ⋯ & \frac{∂f}{∂x_{n}}\end{bmatrix}
$$

Hence, the gradient is simply the transpose of the total derivative:

$$
\nabla f(\mathbf{x}) = \left(\frac{\textrm{d} f}{\textrm{d} \mathbf{x}} \right)^T
$$

We'll now discuss some important results related to gradients widely used in machine learning applications.

Let's see an example.

---

<a id="gradient-of-a-linear-form-with-respect-to-a-vector"></a>
## Gradient of a Linear Form With Respect to a Vector

Let’s derive a general rule for the gradient of a linear function.

Suppose $\mathbf{x} \in \mathbb{R}^n$, and the scalar function $f$ is given by

$$
f(\mathbf{x}) = \mathbf{b}^T \mathbf{x}
$$

where $\mathbf{b} \in \mathbb{R}^n$ is a fixed vector.

We wish to find an expression for the gradient of $f$.

$$
\nabla_{\mathbf{x}} f = \nabla_{\mathbf{x}} (\mathbf{b}^T \mathbf{x})
$$

To do this, let's write our vectors $\mathbf b$ and $\mathbf x$ explicitly:

$$
\begin{bmatrix}b_{1} \\ b_{2} \\ ⋮ \\ b_{n}\end{bmatrix}
$$

Then, $\mathbf b^T \mathbf x$ is the scalar function

$$
\begin{aligned}
b^{T}x &= \begin{bmatrix}b_{1} & b_{2} & … & b_{n}\end{bmatrix}\begin{bmatrix}x_{1} \\ x_{2} \\ ⋮ \\ x_{n}\end{bmatrix}
\end{aligned}
$$

which simplifies as

$$
\mathbf b^T \mathbf x =b_1x_1+ b_2x_2 + \cdots + b_nx_n
$$

The partial derivative of $\mathbf b^T \mathbf x$ with respect to $x_i$ is $b_i$, since all the other terms will vanish.

$$
\dfrac{\partial}{\partial x_i}\left(\mathbf b^T\mathbf x\right) = \dfrac{\partial}{\partial x_i}\left(b_1x_1+ b_2x_2 + \cdots + b_i x_i + \cdots + b_nx_n\right) = b_i
$$

Since this is true for all

$$
1\leq i\leq n
$$

we have

$$
\nabla_{\mathbf x} \left(\mathbf b^T \mathbf x\right) = \mathbf b
$$

as required.

This is the multivariable analog of the familiar rule from single-variable calculus:

$$
\frac{\textrm d}{\textrm dx}(a x) = a
$$

---

<a id="computing-gradients-of-linear-forms"></a>
## Computing Gradients of Linear Forms

**Example:** Compute the gradient of the function $f: \mathbb{R}^4 \rightarrow \mathbb{R}$ defined by $\begin{bmatrix}0 & 1 & 0 & -1\end{bmatrix}$ with respect to $\mathbf{x}$ at $\begin{bmatrix}1 \\ 0 \\ 1 \\ 0\end{bmatrix}$

**Explanation**

Writing $\begin{bmatrix}0 \\ 1 \\ 0 \\ -1\end{bmatrix}$, we have

$$
f(\mathbf{x})= \mathbf{b}^T\mathbf{x}
$$

The gradient of a function

$$
f(x) = \mathbf{b}^T\mathbf{x}
$$

with respect to $\mathbf{x} \in \mathbb{R}^n$ is

$$
\nabla_{\mathbf{x}} f(\mathbf{x}) = \mathbf{b}
$$

Therefore, we get
$\begin{bmatrix}0 \\ 1 \\ 0 \\ -1\end{bmatrix}$

---

**Question 1:** Compute the gradient of the function $f: R^{3} → R$ defined by $f(x) = \begin{bmatrix}1 & 2 & 0\end{bmatrix} \cdot x$ with respect to $x$ at $x = \begin{bmatrix}-2 \\ 0 \\ 1\end{bmatrix}$.

- [ ] A. $-2$
- [ ] B. $\begin{bmatrix}-2 \\ 0 \\ 1\end{bmatrix}$
- [ ] C. $0$
- [ ] D. $\begin{bmatrix}-2 \\ 0 \\ 0\end{bmatrix}$
- [ ] E. $\begin{bmatrix}1 \\ 2 \\ 0\end{bmatrix}$

---

**Question 2:** Given the function
$f(x, w) =-2w^{T}x + 8$
that maps two vectors to a scalar, find $∇_{w}f$.

- [ ] A. $w$
- [ ] B. $x$
- [ ] C. $-2w$
- [ ] D. $-2x + 8$
- [ ] E. $-2x$

---

<a id="the-gradient-of-a-squared-norm"></a>
## The Gradient of a Squared Norm

We have the following result:

$$
\nabla_{\mathbf w} \left(\mathbf w^T \mathbf w\right) = 2\mathbf w
$$

This result is analogous to the following result from single-variable calculus:

$$
\dfrac{\textrm d}{\textrm d w}(w^2) = 2w
$$

To see why this is true, let $\mathbf w \in\mathbb R^n$ be given by
$\begin{bmatrix}w_{1} \\ w_{2} \\ ⋮ \\ w_{n}\end{bmatrix}$

Then, $\mathbf w^T \mathbf w$ is the *scalar* function

$$
\begin{aligned}
w^{T}w &= \begin{bmatrix}w_{1} & w_{2} & … & w_{n}\end{bmatrix}\begin{bmatrix}w_{1} \\ w_{2} \\ ⋮ \\ w_{n}\end{bmatrix}
\end{aligned}
$$

which can be written as

$$
\mathbf w^T \mathbf w =w_1^2+ w_2^2 + \cdots + w_n^2
$$

The partial derivative of $\mathbf w^T \mathbf w$ with respect to $w_i$ is $2w_i$, since all the other terms will vanish. Since this is true for all

$$
1\leq i\leq n
$$

we have

$$
\nabla_{\mathbf w} \left(\mathbf w^T \mathbf w\right) = 2\mathbf w
$$

as required.

Finally, note that since

$$
\mathbf w^T \mathbf w = \mid \mid \mathbf w \mid \mid ^2
$$

we have the following equivalent result:

$$
\nabla_{\mathbf w}(\mid \mid \mathbf w \mid \mid ^2) = 2\mathbf w
$$

---

<a id="computing-gradients-including-squared-norms"></a>
## Computing Gradients Including Squared Norms

**Example:** Given the function
$f(\mathbf{x}, \mathbf{w}) = -7\mathbf{w}^T\mathbf{x} + 3\|\mathbf{w}\|^2 - 2$
that maps two vectors to a scalar, find $\nabla_{\mathbf w} f$.

**Explanation**

Using the results

$$
\nabla_{\mathbf w}(\mathbf{w}^T\mathbf{x}) = \mathbf{x}, \qquad \nabla_{\mathbf w} (\mathbf{w}^T\mathbf{w}) = 2\mathbf{w}
$$

we have

$$
\begin{aligned}
∇_{w}f &= ∇_{w}(-7w^{T}x + 3‖w‖^{2} - 2) \\
&=-7 \cdot ∇_{w}(w^{T}x) + 3 \cdot ∇_{w}(‖w‖^{2}) - ∇_{w}(2) \\
&=-7 \cdot ∇_{w}(w^{T}x) + 3 \cdot ∇_{w}(w^{T}w) - ∇_{w}(2) \\
&=-7 \cdot x + 3 \cdot 2w - 0 \\
&=-7x + 6w
\end{aligned}
$$

---

**Question 3:** Given the function
$f(w) = 10‖w‖^{2} + 4$
that maps a vector to a scalar, find $∇_{w}f$.

- [ ] A. $10w + 4$
- [ ] B. $10‖w‖$
- [ ] C. $20‖w‖$
- [ ] D. $20w$
- [ ] E. $10w$

---

**Question 4:** Given the function
$f(x, w) = w^{T}x + 5‖w‖^{2} + 4$
that maps two vectors to a scalar, find $∇_{w}f$.

- [ ] A. $‖x‖ + 5‖w‖$
- [ ] B. $x + 5w$
- [ ] C. $x + 10w$
- [ ] D. $‖x‖ + 5‖w‖ + 4$
- [ ] E. $‖x‖ + 10‖w‖$

---

<a id="the-gradient-of-the-l1-norm"></a>
## The Gradient of the L1 Norm

Consider the $\textrm{L}_1$ (Manhattan) norm of a vector $\mathbf{x} \in \mathbb{R}^n$, defined as

$$
\\begin{vmatrix}\mathbf{x}\|_1 = \sum_{i=1}^n & x_i\end{vmatrix}
$$

We want to compute the gradient of this function with respect to $\mathbf{x}$. To do that, let’s focus on finding the partial derivative with respect to a single component $x_k{:}$

$$
\begin{aligned}
\frac{∂‖x‖_{1}}{∂x_{k}} &= \frac{∂}{∂x_{k}}∑_(i = 1)^(n) \mid x_{i} \mid \\
&= \frac{∂}{∂x_{k}}(\begin{vmatrix}x_{1} & + & x_{2} & + ⋯ + & x_{k} & + ⋯ + & x_{n}\end{vmatrix}) \\
&= \frac{∂\begin{vmatrix}x_{1} &}{∂x_{k}} + \frac{∂\begin{vmatrix}x_{2} &}{∂x_{k}} + ⋯ + \frac{∂ \mid x_{k} \mid}{∂x_{k}} + ⋯ + \frac{∂\end{vmatrix}x_{n}\end{vmatrix}}{∂x_{k}} \\
&= \frac{∂ \mid x_{k} \mid}{∂x_{k}} \\
&= sign(x_{k})
\end{aligned}
$$

where $\text{sign}(x_k)$ is defined as

$$
{-1,\begin{vmatrix}x_{k} > 0, \\ -1,\end{vmatrix}x_{k} < 0
$$

So, the full gradient of the $\textrm{L}_1$ norm is simply

$$
\nabla_{\mathbf{x}} \|\mathbf{x}\|_1 = \text{sign}(\mathbf{x})
$$

which is a vector whose $k$th component is $\text{sign}(x_k)$.

The gradient of $\|\mathbf x\|_1$ is not defined at points where any component of $\mathbf x$ equals zero. This is because the absolute value function has a sharp corner at zero and is therefore not differentiable there. Various methods are available to overcome this difficulty when the derivative of the $\textrm L1$ norm is used in practice, and we'll discuss these in future lessons.

---

<a id="chain-rule-to-vector-gradients"></a>
## Chain Rule to Vector Gradients

In many machine learning models, a scalar function depends on a vector that itself depends on another vector.

Suppose $f:\mathbb{R}^m \to \mathbb{R}$ and $\mathbf g:\mathbb{R}^n \to \mathbb{R}^m$, and define

$$
h(\mathbf x)=f(\mathbf g(\mathbf x))
$$

Then the **chain rule for gradients** states:

$$
\nabla_{\mathbf x} h(\mathbf x) = J_{\mathbf g}(\mathbf x)^T \,\nabla_{\mathbf u} f(\mathbf u)\Big \mid _{\mathbf u=\mathbf g(\mathbf x)}
$$

where $J_{\mathbf g}(\mathbf x)$ is the Jacobian of $\mathbf g$.

In other words, we have

$$
\bigg(\substack{\Large \text{Gradient of} \[3pt] \Large\text{composition}}\bigg) = \bigg(\substack{\Large \text{Jacobian of} \[3pt] \Large\text{the inner function}}\bigg)^T \cdot \:\: \bigg(\substack{\Large \text{Gradient of} \[3pt] \Large\text{the outer function}}\bigg)
$$

Let’s see this in action.

---

<a id="computing-gradients-of-functions-containing-l1-norms"></a>
## Computing Gradients of Functions Containing L1 Norms

**Example:** Given the function
$f(\mathbf{x}, \mathbf{w}) = -3\left(\|\mathbf{w}\|_1\right)^3 - 5\mathbf x^T\mathbf w$
that maps a vector to a scalar, find $\nabla_{\mathbf w}f$. You may assume that $\mathbf w$ has no zero components.

**Explanation**

Using the results

$$
\nabla_{\mathbf w} (\|\mathbf{w}\|_1) = \text{sign}(\mathbf{w}), \qquad \nabla_{\mathbf w}\left(\mathbf x^T\mathbf w\right) = \mathbf x
$$

we have

$$
\begin{aligned}
∇_{w}f &= ∇_{w}(-3(‖w‖_{1})^{3} - 5x^{T}w) \\
&= ∇_{w}(-3(‖w‖_{1})^{3}) - ∇_{w}(5x^{T}w) \\
&=-3 \cdot ∇_{w}((‖w‖_{1})^{3}) - 5 \cdot ∇_{w}(x^{T}w) \\
&=-3 \cdot 3(‖w‖_{1})^{2} \cdot ∇_{w}(‖w‖_{1}) - 5 \cdot x \\
&=-3 \cdot 3(‖w‖_{1})^{2} \cdot sign(w) - 5 \cdot x \\
&=-9(‖w‖_{1})^{2} \cdot sign(w) - 5x
\end{aligned}
$$

---

**Question 5:** Given the function
$f(w) = 5(‖w‖_{1})^{2} + 4$
that maps a vector to a scalar, find $∇_{w}f$. You may assume that $w$ has all nonzero components.

- [ ] A. $10w$
- [ ] B. $10w \cdot sign(w)$
- [ ] C. $5 \cdot sign(w)$
- [ ] D. $5w$
- [ ] E. $10‖w‖_{1} \cdot sign(w)$

---

**Question 6:** Given the function
$f(x, w) = 4(‖w‖_{1})^{3} + x^{T}w$
that maps a vector to a scalar, find $∇_{w}f$. You may assume that $w$ has no zero components.

- [ ] A. $12(‖w‖_{1})^{2} \cdot sign(w) + x$
- [ ] B. $12sign(w) + 2x$
- [ ] C. $12(‖w‖_{1})^{2} \cdot sign(w) + w$
- [ ] D. $12sign(w) + w$
- [ ] E. $6sign(w) + x$

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
