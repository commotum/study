# The Gradient as a Normal Vector

<!--
lesson-id: 1939
topic-code: MML.9.3.4
-->

## Table of Contents

- [Introduction](#introduction)
- [Finding a Normal Vector to a Level Curve](#finding-a-normal-vector-to-a-level-curve)
- [Finding the Equation of the Normal Line to a Plane Curve Using the Gradient](#finding-the-equation-of-the-normal-line-to-a-plane-curve-using-the-gradient)
- [The Gradient as a Normal Vector For Functions of More than Two Variables](#the-gradient-as-a-normal-vector-for-functions-of-more-than-two-variables)
- [Finding a Normal Vector to a Level Surface](#finding-a-normal-vector-to-a-level-surface)
- [Finding the Equation of the Normal Line to a Surface Using the Gradient](#finding-the-equation-of-the-normal-line-to-a-surface-using-the-gradient)

## Prerequisites

- [The Gradient Vector](<9.3.3. The Gradient Vector.md>)
- [Level Surfaces](<../../9.2. Partial Derivatives/Lessons/9.2.3. Level Surfaces.md>)

---

<a id="introduction"></a>
## Introduction

Given a function $z=f(x,y)$, if both partial derivatives $f_x$, $f_y$ exist and are continuous in a neighborhood of a point $P(x_0,y_0)$ in the domain of the function, and if

$$
\nabla \! f(x_0,y_0) \neq \mathbf 0
$$

then we have the following result:

> *$\nabla \! f(x_0,y_0)$ is a normal vector to the level curve $C$ that passes through the point $P$.*

This property is one of the most important facts in multivariable calculus since it gives a fundamental link between
calculus and geometry!

Let's illustrate this property with a concrete example. Consider the paraboloid

$$
z = f (x, y)= x^2 + y^2
$$

and the level curve $\color{red}C$ of $f(x,y)$ corresponding to the point $P(1,-1)$. It is easy to see that $\color{red}C$ is a circle of radius $\sqrt{2}$ with equation

$$
x^2+y^2 = 2
$$

![](<../Source/9.3.4. The Gradient as a Normal Vector/Images/371abeab11360d5eace282806fd822c7.png>)

In this particular instance, the property says that

> *$\nabla \! f(1,-1)$ is a normal vector to the level curve $\color{red}C$ that passes through the point $P(1,-1)$.*

![](<../Source/9.3.4. The Gradient as a Normal Vector/Images/476a96b391c411c85c5987909374056d.png>)

Looking at the situation in the $xy$-plane, we see the following:

![](<../Source/9.3.4. The Gradient as a Normal Vector/Images/aab6a17b1d35037916eaa2b148cf5c04.png>)

Let's verify that $\nabla f(1,-1)$ is a normal line to the level curve $C$ at the point $P(1,-1)$.

- First, computing the gradient, we find that $\nabla f = \langle 2x, 2y \rangle$. Therefore,
$\nabla f(1,-1) = \langle 2, -2 \rangle$.
- Then, using elementary calculus, we can show that the equation of the tangent to $C$ at $P$ is $y=x-2$. Since this line has slope $m=1$, it follows that the vector $\langle 1,1 \rangle$ is parallel to the level curve.
- Finally, taking the dot product between the gradient and the tangent vector, we obtain
$\nabla f(1,-1)\cdot \langle 1,1 \rangle = \langle 2, -2\rangle \cdot \langle 1,1 \rangle = 0$.

This means that $\nabla f(1,-1)$ is *perpendicular* to the tangent line to $C$ at $(1,-1)$.

---

<a id="finding-a-normal-vector-to-a-level-curve"></a>
## Finding a Normal Vector to a Level Curve

**Example:** Find a unit normal vector to the level curve of the function $f(x,y) = x^2 + 2y^2 +x$ that passes through the point where $(x,y) = (1, 1)$.

**Explanation**

A normal vector to the level curve that passes through the point where

$$
(x,y) = (1,1)
$$

can be found by computing $\nabla f(1,1)$.

So, we first find the gradient and evaluate it at

$$
(x,y) = (1,1)
$$

$$
\begin{aligned}
∇f(x, y) &= f_{x}(x, y)i + f_{y}(x, y)j \\
&= \frac{∂}{∂x}(x^{2} + 2y^{2} + x)i + \frac{∂}{∂y}(x^{2} + 2y^{2} + x)j \\
&= (2x + 0 + 1)i + (0 + 4y + 0)j \\
&= (2x + 1)i + 4yj \\
∇f(1, 1) &= (2(1) + 1)i + 4(1)j \\
&= 3i + 4j
\end{aligned}
$$

Finally, since we want a unit normal vector, we normalize $\nabla f(1,1)$:

$$
\begin{aligned}
(∇f(1, 1))/(‖∇f(1, 1)‖) &= \frac{3i + 4j}{\sqrt{3^{2} + 4^{2}}} \\
&= \frac{1}{5}(3i + 4j)
\end{aligned}
$$

---

**Question 1:** Find a unit normal vector to the level curve of the function $f(x, y) = 2x^{2} + 2y^{2} - y$ that passes through the point where $(x, y) = (0, 1)$.

- [ ] A. $i$
- [ ] B. $j$
- [ ] C. $\frac{1}{\sqrt{2}}i + \frac{1}{\sqrt{2}}j$
- [ ] D. $-i - j$
- [ ] E. $-k$

---

**Question 2:** Find a unit normal vector to the level curve of the function $f(x, y) = 2x + 4y + 5xy$ that passes through the point where $(x, y) = (2, 1)$.

- [ ] A. $\frac{\sqrt{5}}{5}(i + 2j)$
- [ ] B. $i - 4j$
- [ ] C. $7i + 7j$
- [ ] D. $\frac{\sqrt{5}}{5}(2i + j)$
- [ ] E. $7i - 12j$

---

<a id="finding-the-equation-of-the-normal-line-to-a-plane-curve-using-the-gradient"></a>
## Finding the Equation of the Normal Line to a Plane Curve Using the Gradient

**Example:** Consider the plane curve $\dfrac{x^2}9 + \dfrac {y^2}{16}=1$. Find an equation of the normal line to the curve at the point $(0, 4)$.

**Explanation**

To find the equation of the normal line, we first find the equation of a normal vector, and then write the equation of the line that is parallel to the normal vector.

To obtain a normal vector, we write our curve as a level curve and then compute the gradient. Bringing all the terms of our equation to one side, we get

$$
\begin{aligned}
\frac{x^{2}}{9} + \frac{y^{2}}{16} &= 1 \\
\frac{x^{2}}{9} + \frac{y^{2}}{16} - 1 &= 0
\end{aligned}
$$

If we set

$$
f(x,y) = \dfrac{x^2}9 + \dfrac {y^2}{16} - 1
$$

our curve can be viewed as the level curve

$$
f(x,y)=0
$$

A normal vector to the level curve that passes through the point $(0,4)$ can be found by computing $\nabla f(0,4)$.

$$
\begin{aligned}
∇f(x, y) &= f_{x}(x, y)i + f_{y}(x, y)j \\
&= \frac{∂}{∂x}(\frac{x^{2}}{9} + \frac{y^{2}}{16} - 1)i + \frac{∂}{∂y}(\frac{x^{2}}{9} + \frac{y^{2}}{16} - 1)j \\
&= \frac{2x}{9}i + \frac{y}{8}j \\
∇f(0, 4) &= (2(0))/(9)i + ((4))/(8)j \\
&= \frac{1}{2}j \\
&= ⟨0, \frac{1}{2}⟩
\end{aligned}
$$

Therefore,

$$
\nabla f(0,4)= \left\langle 0, \:\dfrac12 \right\rangle
$$

is a normal vector to the curve at $(0,4)$.

Now, remember that the Cartesian equation of the line that is parallel to

$$
\mathbf{v} = \langle v_x, v_y \rangle
$$

and passes through the point $(x_0,y_0)$ can be written as

$$
\frac{y - y_{0}}{x - x_{0}} = \frac{v_{y}}{v_{x}}
$$

or, equivalently,

$$
\begin{aligned}
v_{y}(x - x_{0}) &= v_{x}(y - y_{0})
\end{aligned}
$$

So, writing the Cartesian equation of the line that is parallel to

$$
\nabla f(0,4)= \left\langle 0, \:\dfrac12 \right\rangle
$$

we get

$$
\begin{aligned}
\frac{1}{2}(x - 0) &= 0(y - 4) \\
\frac{1}{2}x &= 0 \\
x &= 0
\end{aligned}
$$

Finally, we conclude that the equation of the normal line to the ellipse

$$
\dfrac{x^2}9 + \dfrac {y^2}{16}=1
$$

at the point $(0,4)$ is $x=0$.

---

**Question 3:** Consider the plane curve $6xy = 5x^{2} + y^{2}$. Find the equation of the normal line to the curve at the point $(1, 1)$.

- [ ] A. $x - y + 2 = 0$
- [ ] B. $x + y = 0$
- [ ] C. $x + y - 1 = 0$
- [ ] D. $x + y - 2 = 0$
- [ ] E. $2x + y + 2 = 0$

---

**Question 4:** Consider the plane curve $xy = x^{2} + y^{2} - 7$. Find the equation of the normal line to the curve at the point $(2,-1)$.

- [ ] A. $3x - 5y + 1 = 0$
- [ ] B. $-2x - 2y + 3 = 0$
- [ ] C. $4x + 5y - 3 = 0$
- [ ] D. $-5x - 4y + 1 = 0$
- [ ] E. $-x - 3y + 5 = 0$

---

<a id="the-gradient-as-a-normal-vector-for-functions-of-more-than-two-variables"></a>
## The Gradient as a Normal Vector For Functions of More than Two Variables

The same property holds true in the case of three or more variables. In general, if $f$ is a differentiable real-valued function of $n$ variables, $\mathbf y$ is a point in the domain of $f$, and if the gradient vector is nonzero, i.e.,

$$
\nabla f (\mathbf y)= \left \langle f_{x_1}(\mathbf y), f_{x_2}(\mathbf y), \cdots, f_{x_n}(\mathbf y)\right \rangle \neq \mathbf 0
$$

then we have the following result:

> *$\nabla f (\mathbf y)$ is a normal vector to the level surface $f(x_1,x_2, \cdots, x_n) =f(\mathbf y)$ at $\mathbf y$.*

To illustrate, let's consider the function

$$
w = f (x, y, z)= \dfrac 1 2\left(x^2 + y^2 + z^2-1\right)
$$

The level surface corresponding to $w=0$ is the sphere $\mathcal S$ with equation

$$
x^2 + y^2 +z^2 =1
$$

The point $P(0,0,1)$ lies on $\mathcal S$. Computing $\nabla f(0,0,1)$, we get

$$
\begin{aligned} \nabla f(x,y,z) &= \langle f_x, \: f_y, \:f_z\rangle \\[5pt] &= \dfrac12\langle2x, \: 2y, \: 2z\rangle \\[5pt] &= \langle x, \: y, \: z\rangle \\[10pt] \nabla f(0,0,1) &= \langle 0,\: 0,\: 1 \rangle. \end{aligned}
$$

Now clearly, $\nabla f (0,0, 1)$ is perpendicular to $\mathcal S$ at the point $(0,0,1)$.

![](<../Source/9.3.4. The Gradient as a Normal Vector/Images/06a0808110c48d624a9edb777213d05b.png>)

---

<a id="finding-a-normal-vector-to-a-level-surface"></a>
## Finding a Normal Vector to a Level Surface

**Example:** For the function $f(x,y,z) = 2x^2 - xy - z^2$, find a vector that's perpendicular to the level surface that passes through the point where $(x,y,z) = (2, 3, \sqrt 2)$.

**Explanation**

A normal vector to the level surface that passes through the point where

$$
(x,y,z) = (2, 3, \sqrt 2)
$$

can be found by computing
$\nabla f(2, 3, \sqrt 2)$.

So, we first find the gradient and evaluate it at

$$
(x,y,z) = (2, 3, \sqrt 2)
$$

$$
\begin{aligned}
∇f(x, y, z) &= \frac{∂}{∂x}(2x^{2} - xy - z^{2})i + \frac{∂}{∂y}(2x^{2} - xy - z^{2})j + \frac{∂}{∂z}(2x^{2} - xy - z^{2})k \\
&= (4x - y)i - xj - 2zk \\
∇f(2, 3, \sqrt{2}) &= (4 \cdot 2 - 3)i - 2j - 2\sqrt{2}k \\
&= 5i - 2j - 2\sqrt{2}k
\end{aligned}
$$

Therefore, $5 \mathbf i - 2 \mathbf j - 2 \sqrt 2\ \mathbf k$ is perpendicular to the level surface that passes through the point where

$$
(x,y,z) = (2, 3, \sqrt 2)
$$

---

**Question 5:** For the function $f(x, y, z) = xy + 5x + z^{2}$, find a vector that's perpendicular to the level surface that passes through the point where $(x, y, z) = (1,-1, 2)$.

- [ ] A. $4j + k$
- [ ] B. $4i - 2j + 4k$
- [ ] C. $i + 2j + 4k$
- [ ] D. $4i + j + 4k$
- [ ] E. $-4i - 2k$

---

**Question 6:** For the function $f(x, y, z) = xy^{2} - 2yz + z$, find a vector that's perpendicular to the level surface that passes through the point where $(x, y, z) = (1,-1, 1)$.

- [ ] A. $2i - j + 3k$
- [ ] B. $12i + 4j + 3k$
- [ ] C. $i - 4j + 3k$
- [ ] D. $-3i + 4j + 2k$
- [ ] E. $3i - 2j + 6k$

---

<a id="finding-the-equation-of-the-normal-line-to-a-surface-using-the-gradient"></a>
## Finding the Equation of the Normal Line to a Surface Using the Gradient

**Example:** Find the equation of the normal line to the surface $z = \ln (x^2 - xy)$ at the point $\left(2, 1, \ln 2 \right)$.

**Explanation**

To find the equation of the normal line, we first find the equation of a normal vector, and then write the equation of the line that is parallel to the normal vector.

To obtain a normal vector, we write our curve as a level curve and then compute the gradient. Bringing all the terms of our equation to one side, we get

$$
\begin{aligned}
z &= \ln (x^{2} - xy) \\
0 &= \ln (x^{2} - xy) - z
\end{aligned}
$$

If we set

$$
f(x,y,z) = \ln (x^2 - xy) - z
$$

our surface can be viewed as the level surface

$$
f(x,y,z)=0
$$

A normal vector to the level surface that passes through the point

$$
\left(2, 1, \ln 2 \right)
$$

can be found by computing $\nabla f(2,1,\ln 2){:}$

$$
\begin{aligned}
∇f(x, y, z) &= f_{x}(x, y, z)i + f_{y}(x, y, z)j + f_{z}(x, y, z)k \\
&= \frac{∂}{∂x}(\ln (x^{2} - xy) - z)i + \frac{∂}{∂y}(\ln (x^{2} - xy) - z)j + \frac{∂}{∂z}(\ln (x^{2} - xy) - z)k \\
&= (\frac{2x - y}{x^{2} - xy})i + (\frac{1}{y - x})j - k \\
∇f(2, 1, \ln 2) &= (\frac{2 \cdot 2 - 1}{2^{2} - 2 \cdot 1})i + (\frac{1}{1 - 2})j - k \\
&= \frac{3}{2}i - j - k \\
&= ⟨\frac{3}{2},-1,-1⟩
\end{aligned}
$$

Now, remember that the Cartesian equation of the line that is parallel to

$$
\mathbf v = \langle v_x, v_y, v_z \rangle
$$

and passes through the point $(x_0,y_0,z_0)$ can be written as

$$
\frac{x - x_{0}}{v_{x}} = \frac{y - y_{0}}{v_{y}} = \frac{z - z_{0}}{v_{z}}
$$

So, writing the Cartesian equation of the line that is parallel to

$$
\nabla f(2,1,\ln 2)= \left\langle \dfrac {3} {2}, -1, -1\right \rangle
$$

we get

$$
(x - 2)/((\frac{3}{2})) = \frac{y - 1}{-1} = \frac{z - \ln 2}{-1}; \frac{x - 2}{3} = \frac{y - 1}{-2} = \frac{z - \ln 2}{-2}
$$

---

**Question 7:** Find the equation of the normal line to the surface $z = \sin (xy) + y^{2}$ at the point $(0,-1, 1)$.

- [ ] A. $x =-y - 1 = \frac{z - 1}{2}$
- [ ] B. $x = y + 1 = z - 1$
- [ ] C. $-\frac{x}{2} = \frac{y + 1}{2} = z$
- [ ] D. $x =-\frac{y + 1}{2} = \frac{z - 1}{2}$
- [ ] E. $x = \frac{y + 1}{2} = z - 1$

---

**Question 8:** Find a vector equation of the normal line to the surface $\frac{x^{2}}{4} - y^{2} = z^{2}$ at the point $(2, 0, 1)$.

- [ ] A. $r = ⟨2, 0, 1⟩ + t⟨2, 0, 1⟩, t ∈ (- ∞, ∞)$
- [ ] B. $r = ⟨2, 0, 1⟩ + t⟨1, 0,-2⟩, t ∈ (- ∞, ∞)$
- [ ] C. $r = ⟨1, 0,-2⟩ + t⟨1, 0,-2⟩, t ∈ (- ∞, ∞)$
- [ ] D. $r = ⟨1, 0,-2⟩ + t⟨2, 0, 1⟩, t ∈ (- ∞, ∞)$
- [ ] E. $r = ⟨2, 0, 1⟩ + t⟨2, 1,-1⟩, t ∈ (- ∞, ∞)$

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
