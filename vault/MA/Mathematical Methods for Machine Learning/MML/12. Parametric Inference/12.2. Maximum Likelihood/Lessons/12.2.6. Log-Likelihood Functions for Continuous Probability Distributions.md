# Log-Likelihood Functions for Continuous Probability Distributions

<!--
lesson-id: 4125
topic-code: MML.12.2.6
-->

## Table of Contents

- [Introduction](#introduction)
- [Log-Likelihood Functions for Continuous Random Variables](#log-likelihood-functions-for-continuous-random-variables)
- [Log-Likelihood Functions for Exponential Random Variables](#log-likelihood-functions-for-exponential-random-variables)
- [Log-Likelihood Functions for Normal Random Variables](#log-likelihood-functions-for-normal-random-variables)

## Prerequisites

- [Likelihood Functions for Continuous Probability Distributions](<12.2.5. Likelihood Functions for Continuous Probability Distributions.md>)
- [Log-Likelihood Functions for Discrete Probability Distributions](<12.2.4. Log-Likelihood Functions for Discrete Probability Distributions.md>)

---

<a id="introduction"></a>
## Introduction

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed *continuous* random variables with probability density function $f(x; \theta)$, then the likelihood function $L(\theta)$ of a random sample $x_1, x_2, \ldots, x_n$ is given by

$$
L(\theta) = \displaystyle\prod_{i=1}^n f(x_i;\,\theta)
$$

where $f(x;\theta)$ is the probability density function of $X_i$ for

$$
i=1,2,\ldots, n
$$

and $\theta$ is an unknown parameter.

Similar to the case of discrete random variables, the **log-likelihood function** is given by

$$
\begin{aligned}
l(θ) &= \ln (L(θ)) \\
&= \ln (∏_(i = 1)^(n)f(x_{i} \\
&= θ)) \\
&= \ln (f(x_{1} \\
&= θ) \cdot f(x_{2} \\
&= θ)⋯f(x_{n} \\
&= θ)) \\
&= \ln (f(x_{1} \\
&= θ)) + \ln (f(x_{2} \\
&= θ)) + ⋯ + \ln (f(x_{n} \\
&= θ)) \\
&= ∑_(i = 1)^(n)\ln (f(x_{i} \\
&= θ))
\end{aligned}
$$

Let's get some practice at calculating log-likelihood functions for some continuous random variables.

---

<a id="log-likelihood-functions-for-continuous-random-variables"></a>
## Log-Likelihood Functions for Continuous Random Variables

**Example:** Let $X_1, X_2, X_3,X_4$ be I.I.D. random variables with probability density function
$f(x) = \theta x^{-\theta-1}, \qquad x \geq 1$,
where $\theta$ is an unknown parameter. Find the log-likelihood function of the sample $1.3, \, 2.4, \, 1.0,\, 1.7$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the log-likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
\begin{aligned}
l(θ) &= \ln L(θ) \\
&= \ln (∏_(i = 1)^(n)f(x_{i} \\
&= θ)) \\
&= ∑_(i = 1)^(n)\ln (f(x_{i} \\
&= θ))
\end{aligned}
$$

In our case, we are given a sample of $n=4$ independent random variables with unknown parameter $\theta$, for which the probability density function is

$$
f(x) = \theta x^{-\theta-1}, \qquad x \geq 1
$$

First, let's write down the log-density function:

$$
\begin{aligned}
\ln f(x \\
θ) &= \ln (θx^{- θ - 1}) \\
&= \ln θ + \ln (x^{- (θ + 1)}) \\
&= \ln θ - (θ + 1)\ln x
\end{aligned}
$$

Now, we can calculate the log-likelihood function as follows:

$$
\begin{aligned}
l(θ) &= ∑_(i = 1)^(n)(\ln θ - (θ + 1)\ln x_{i}) \\
&= ∑_(i = 1)^(n)\ln θ - ∑_(i = 1)^(n)(θ + 1)\ln x_{i} \\
&= \ln θ∑_(i = 1)^(n)1 - (θ + 1)∑_(i = 1)^(n)\ln x_{i} \\
&= n\ln θ - (θ + 1)∑_(i = 1)^(n)\ln x_{i}
\end{aligned}
$$

For our sample, we have that $n=4$, and

$$
\begin{aligned}
∑_(i = 1)^(n)\ln x_{i} &= \ln 1.3 + \ln 2.4 + \ln 1.0 + \ln 1.7 \approx 1.67
\end{aligned}
$$

Therefore,

$$
l(θ) \mid \approx 4\ln θ - (θ + 1) \cdot 1.67; = 4\ln θ - 1.67(θ + 1)
$$

---

**Question 1**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, …, X_{12}$ be I.I.D. random variables with probability density function
$f(x) = θx^{- θ - 1}, x \ge 1$,
where $θ$ is an unknown parameter. For a particular sample $x_{1}, x_{2}, …, x_{12}$, you're given that
$∑_(i = 1)^(12)\ln x_{i} \approx 13.45$.
The log-likelihood function of the sample is
$l(θ) = a\ln θ - b(θ + 1)$.
What is the value of $a + b$, rounded to two decimal places?

- [ ] A. $27.45$
- [ ] B. $23.45$
- [ ] C. $24.45$
- [ ] D. $26.45$
- [ ] E. $25.45$

---

**Question 2**

> A scientific calculator is required to answer this question.

Let $X_{1}, X_{2}, X_{3}$ be I.I.D. random variables with probability density function
$f(x) = θx^{- θ - 1}, x \ge 1$,
where $θ$ is an unknown parameter. The log-likelihood function of the sample $2.0, 3.2, 5.5$ is
$l(θ) = a\ln θ - b(θ + 1)$.
What is the value of $a + b$, rounded to two decimal places?

- [ ] A. $7.34$
- [ ] B. $6.56$
- [ ] C. $8.56$
- [ ] D. $6.24$
- [ ] E. $5.24$

---

<a id="log-likelihood-functions-for-exponential-random-variables"></a>
## Log-Likelihood Functions for Exponential Random Variables

**Example:** Suppose $X_1, X_2, X_3, X_4, X_5$ is an I.I.D random sample with $X_i \sim \text{Exp}(\theta)$ and unknown rate parameter $\theta$. Find the log-likelihood function of the sample $2, \, 4, \, 1, \, 3, \, 7$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the log-likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
\begin{aligned}
l(θ) &= \ln L(θ) \\
&= \ln (∏_(i = 1)^(n)f(x_{i} \\
&= θ)) \\
&= ∑_(i = 1)^(n)\ln (f(x_{i} \\
&= θ))
\end{aligned}
$$

In our case, we are given a sample of $n = 5$ independent exponential random variables with unknown parameter $\theta$, for which the probability density function is

$$
f(x, \theta) = \theta e^{-\theta x}, \qquad x \geq 0
$$

First, let's write down the log-density function:

$$
\begin{aligned}
\ln f(x, θ) &= \ln (θe^{- θx}) \\
&= \ln θ + \ln (e^{- θx}) \\
&= \ln θ - θx
\end{aligned}
$$

Now, we can calculate the log-likelihood function as follows:

$$
\begin{aligned}
l(θ) &= ∑_(i = 1)^(n)(\ln θ - θx_{i}) \\
&= ∑_(i = 1)^(n)\ln θ - θ∑_(i = 1)^(n)x_{i} \\
&= n\ln θ - θ∑_(i = 1)^(n)x_{i}
\end{aligned}
$$

For our sample, we have that

$$
\begin{aligned}
∑_(i = 1)^(n)x_{i} &= 2 + 4 + 1 + 3 + 7 = 17
\end{aligned}
$$

and $n = 5$. Therefore,

$$
\begin{aligned}
l(θ) &= 5\ln θ - θ \cdot 17 \\
&= 5\ln θ - 17θ
\end{aligned}
$$

---

**Question 3**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, …, X_{8}$ is an I.I.D random sample with $X_{i}∼Exp(θ)$ and unknown rate parameter $θ$. For a particular sample $x_{1}, x_{2}, …, x_{8}$, you're given that
$∑_(i = 1)^(8)x_{i} = 28.4$.
Given that the log-likelihood function of the sample is
$l(θ) = a\ln θ + bθ$,
what is the value of $a + b$?

*Hint: The probability density function for an exponential random variable is given by*
$f(x) = θe^{- θx}, x \ge 0$.

- [ ] A. $26.2$
- [ ] B. $-20.4$
- [ ] C. $-22.2$
- [ ] D. $-28.4$
- [ ] E. $28.4$

---

**Question 4**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, X_{3}$ is an I.I.D random sample with $X_{i}∼Exp(θ)$ and unknown rate parameter $θ$. The log-likelihood function of the sample $5.1, 4.2, 6.3$ is
$l(θ) = a\ln θ + bθ$.
What is the value of $a + b$?

- [ ] A. $15.6$
- [ ] B. $10.4$
- [ ] C. $-12.6$
- [ ] D. $-14.2$
- [ ] E. $-10.8$

---

<a id="log-likelihood-functions-for-normal-random-variables"></a>
## Log-Likelihood Functions for Normal Random Variables

**Example:** Suppose $X_1,X_2,\ldots,X_{8}$ is an I.I.D random sample with $X_i \sim N(\theta, 1^2)$, where $\theta$ is an unknown mean. For a particular sample $x_1, x_2, \ldots, x_{8}$, you're given that
$\sum_{i=1}^{8} x_i = 11.8, \qquad \sum_{i=1}^{8} x_i^2 = 25.2$.
Find the log-likelihood function of the sample.

*Hint: The probability density function for a normal random variable with mean $\theta$ and standard deviation $1$ is given by*
$f(x) = \dfrac{1}{\sqrt{2\pi}} e^{-(x-\theta)^2/2}, \qquad x \in (-\infty,\infty)$.

**Explanation**

Recall that if $X_1, X_2, \ldots, X_{n}$ are independent and identically distributed continuous random variables with probability density function $f(x; \theta)$, where $\theta$ is an unknown parameter, then the log-likelihood function of a random sample $x_1, x_2, \ldots, x_n$ is

$$
\begin{aligned}
l(θ) &= \ln L(θ) \\
&= \ln (∏_(i = 1)^(n)f(x_{i} \\
&= θ)) \\
&= ∑_(i = 1)^(n)\ln (f(x_{i} \\
&= θ))
\end{aligned}
$$

In our case, we are given a sample of $n=8$ independent normal random variables with unknown mean $\theta$ and variance

$$
\sigma^2 = 1
$$

for which the probability density function is

$$
f(x, \theta) = \dfrac{1}{\sqrt{2\pi}} e^{-(x-\theta)^2/2}, \qquad x \in (-\infty,\infty)
$$

First, let's write down the log-density function:

$$
\begin{aligned}
\ln f(x, θ) &= \ln (\frac{1}{\sqrt{2π}}e^{- (x - θ)^{2}/2}) \\
&= \ln (\frac{1}{\sqrt{2π}}) + \ln (e^{- (x - θ)^{2}/2}) \\
&=-\frac{1}{2}\ln (2π) - ((x - θ)^{2})/(2)
\end{aligned}
$$

Now, we can calculate the log-likelihood function as follows:

$$
\begin{aligned}
l(θ) &= ∑_(i = 1)^(n)(-\frac{1}{2}\ln (2π) - ((x_{i} - θ)^{2})/(2)) \\
&=-\frac{n}{2}\ln (2π) - ∑_(i = 1)^(n)((x_{i} - θ)^{2})/(2) \\
&=-\frac{n}{2}\ln (2π) - \frac{1}{2}(∑_(i = 1)^(n)x_{i}^{2} - 2θ∑_(i = 1)^(n)x_{i} + nθ^{2})
\end{aligned}
$$

We are told that

$$
\displaystyle\sum_{i=1}^{8} x_i = 11.8
$$

$$
\displaystyle\sum_{i=1}^{8} x_i^2 = 25.2
$$

and $n=8$. Therefore,

$$
\begin{aligned}
l(θ) &= -\frac{8}{2}\ln (2π) - \frac{1}{2}(25.2 - 2θ \cdot 11.8 + 8θ^{2}) \\
&=-4\ln (2π) - 12.6 + 11.8θ - 4θ^{2}
\end{aligned}
$$

---

**Question 5**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, …, X_{12}$ is an I.I.D random sample with $X_{i}∼N(θ, 1^{2})$, where $θ$ is an unknown mean. For a particular sample $x_{1}, x_{2}, …, x_{12}$, you're given that
$∑_(i = 1)^(12)x_{i} = 30.2, ∑_(i = 1)^(12)x_{i}^{2} = 90.6$.
Given that the log-likelihood function of the sample is
$l(θ) =-6\ln (2π) + a + bθ + cθ^{2}$,
what is the value of $a + b + c$?

*Hint: The probability density function for a normal random variable with mean $θ$ and standard deviation $1$ is given by*
$f(x) = \frac{1}{\sqrt{2π}}e^{- (x - θ)^{2}/2}, x ∈ (- ∞, ∞)$.

- [ ] A. $-21.1$
- [ ] B. $-26.3$
- [ ] C. $-24.6$
- [ ] D. $24.2$
- [ ] E. $27.2$

---

**Question 6**

> A scientific calculator is required to answer this question.

Suppose $X_{1}, X_{2}, X_{3}, X_{4}$ is an I.I.D random sample with $X_{i}∼N(θ, 1^{2})$, where $θ$ is an unknown mean. The log-likelihood function of the sample $1.2, 0.4, 0.2,-2.0$ is
$l(θ) =-2\ln (2π) + a + bθ + cθ^{2}$
What is the value of $a + b + c$?

- [ ] A. $-5.16$
- [ ] B. $5.86$
- [ ] C. $-5.24$
- [ ] D. $5.14$
- [ ] E. $-5.02$

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
