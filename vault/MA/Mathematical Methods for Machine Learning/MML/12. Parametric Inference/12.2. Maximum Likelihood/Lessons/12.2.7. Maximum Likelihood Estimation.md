# Maximum Likelihood Estimation

<!--
lesson-id: 3077
topic-code: MML.12.2.7
-->

## Table of Contents

- [Introduction](#introduction)
- [Maximum Likelihood Estimators for Discrete Probability Distributions](#maximum-likelihood-estimators-for-discrete-probability-distributions)
- [Maximum Likelihood Estimators for Continuous Probability Distributions](#maximum-likelihood-estimators-for-continuous-probability-distributions)
- [Deriving a Maximum Likelihood Estimator for Bernoulli Distributions](#deriving-a-maximum-likelihood-estimator-for-bernoulli-distributions)
- [A List of Important Maximum Likelihood Estimators](#a-list-of-important-maximum-likelihood-estimators)

## Prerequisites

- [The Second Derivative Test](<../../../../CAB/5. Analytical Applications of Differentiation/5.2. Analytical Applications of Differentiation/Lessons/5.2.11. The Second Derivative Test.md>)
- [Log-Likelihood Functions for Continuous Probability Distributions](<12.2.6. Log-Likelihood Functions for Continuous Probability Distributions.md>)

---

<a id="introduction"></a>
## Introduction

Suppose we have a random sample $X_1, X_2, X_3$ drawn from a population where $X_i\sim B(5,\theta)$, where $\theta$ is an unknown probability of success. Let's assume we conducted our sample and got the following data:

$$
x_1 = 4, \qquad x_2 = 1, \qquad x_3 = 1
$$

It can be shown that the log-likelihood function for this sample is

$$
l(\theta) = \ln 125 + 6 \ln \theta +9\ln(1-\theta)
$$

Our goal is to form an estimate for the unknown parameter $\theta$. To do this, we find the value of $\theta$ that maximizes the probability of getting the observed sample data. In other words, we need to maximize the likelihood function $L(\theta)$. And, since the natural logarithm is an increasing function, a value of $\theta$ that maximizes the likelihood function $L(\theta)$ will also maximize the log-likelihood function $l(\theta)$.

Estimates of an unknown parameter $\theta$ obtained by maximizing the likelihood function (or, equivalently, the log-likelihood function) are called **maximum likelihood estimates**. We'll use the usual $\hat{\theta}$ notation to denote a maximum likelihood estimate of $\theta$.

![](<../Source/12.2.7. Maximum Likelihood Estimation/Images/51cce9569b855082b4f0ed9a6a36ee44.png>)

To find the maximum likelihood estimate of $\theta$, we calculate the derivative of the log-likelihood function, set the derivative equal to zero, and solve for $\theta$.

First, we take the derivative of $l(\theta){:}$

$$
\begin{aligned}
\frac{dl}{dθ} &= \frac{d}{dθ}(\ln 125 + 6\ln θ + 9\ln (1 - θ)) \\
&= \frac{6}{θ} + \frac{9}{1 - θ} \cdot (-1) \\
&= \frac{6}{θ} + \frac{9}{θ - 1}
\end{aligned}
$$

Then, we set the derivative equal to $0$ and solve for $\theta{:}$

$$
\begin{aligned}
\frac{6}{θ} + \frac{9}{θ - 1} &= 0 \\
(6(θ - 1) + 9θ)/(θ(θ - 1)) &= 0 \\
6(θ - 1) + 9θ &= 0 \\
15θ - 6 &= 0 \\
15θ &= 6 \\
θ &= \frac{6}{15}
\end{aligned}
$$

Therefore, for this sample, the maximum likelihood estimate of $\theta$ is

$$
\widehat{\,\theta\,} = \dfrac{6}{15}
$$

**Note**: It's easy to check that this is a maximum of $l(\theta)$ by calculating the second derivative. We'll omit this step.

---

<a id="maximum-likelihood-estimators-for-discrete-probability-distributions"></a>
## Maximum Likelihood Estimators for Discrete Probability Distributions

**Example:** Given that the log-likelihood function for a particular sample of independent Bernoulli random variables is
$l(\theta) = 3\ln\theta + 4\ln(1-\theta)$,
what is the maximum likelihood estimate $\widehat{\,\theta\,}$ from this sample?

**Explanation**

In order to find the maximum likelihood estimate of $\theta$, we calculate the derivative of the log-likelihood function, set the derivative equal to zero, and solve for $\theta$.

First, we take the derivative of $l(\theta){:}$

$$
\begin{aligned}
\frac{dl}{dθ} &= \frac{d}{dθ}(3\ln θ + 4\ln (1 - θ)) \\
&= \frac{3}{θ} + \frac{4}{1 - θ} \cdot (-1) \\
&= \frac{3}{θ} + \frac{4}{θ - 1}
\end{aligned}
$$

Then, we set the derivative equal to $0$ and solve for $\theta{:}$

$$
\begin{aligned}
\frac{3}{θ} + \frac{4}{θ - 1} &= 0 \\
(3(θ - 1) + 4θ)/(θ(θ - 1)) &= 0 \\
3(θ - 1) + 4θ &= 0 \\
7θ - 3 &= 0 \\
7θ &= 3 \\
θ &= \frac{3}{7}
\end{aligned}
$$

Therefore, for this sample, the maximum likelihood estimate of $\theta$ is

$$
\widehat{\,\theta\,} = \dfrac{3}{7}
$$

---

**Question 1:** Given that the log-likelihood function for a particular sample of independent Bernoulli random variables is
$l(θ) = 5\ln θ + \ln (1 - θ)$,
what is the maximum likelihood estimate $θ^(^)$ from this sample?

- [ ] A. $\frac{2}{3}$
- [ ] B. $\frac{1}{2}$
- [ ] C. $\frac{1}{6}$
- [ ] D. $\frac{1}{5}$
- [ ] E. $\frac{5}{6}$

---

**Question 2:** Given that the log-likelihood function for a particular sample of independent binomial random variables is
$l(θ) = \ln 27 + 5\ln θ + 7\ln (1 - θ)$,
what is the maximum likelihood estimate $θ^(^)$ from this sample?

- [ ] A. $\frac{12}{27}$
- [ ] B. $\frac{5}{7}$
- [ ] C. $\frac{5}{12}$
- [ ] D. $\frac{7}{12}$
- [ ] E. $\frac{7}{5}$

---

<a id="maximum-likelihood-estimators-for-continuous-probability-distributions"></a>
## Maximum Likelihood Estimators for Continuous Probability Distributions

**Example:** Given that the log-likelihood function for a particular sample of independent normal random variables $X_i\sim N(\theta,1)$ is
$l(\theta) = -6\ln(2\pi)-8.4 + 12.1\, \! \theta - 6\ \! \theta^2$,
what is the maximum likelihood estimate $\widehat{\,\theta\,}$ for this sample? Round your answer to $3$ decimal places.

**Explanation**

In order to find the maximum likelihood estimate of $\theta$, we calculate the derivative of the log-likelihood function, set the derivative equal to zero, and solve for $\theta$.

First, we take the derivative of $l(\theta){:}$

$$
\begin{aligned}
\frac{dl}{dθ} &= \frac{d}{dθ}(-6\ln (2π) - 8.4 + 12.1θ - 6θ^{2}) \\
&= 12.1 - 12θ
\end{aligned}
$$

Then, we set the derivative equal to $0$ and solve for $\theta{:}$

$$
\begin{aligned}
12.1 - 12θ &= 0 \\
12θ &= 12.1 \\
&= θ \mid \approx 1.008
\end{aligned}
$$

Therefore, for this sample, the maximum likelihood estimate of $\theta$ is

$$
\widehat{\,\theta\,} \approx 1.008
$$

rounded to $3$ decimal places.

---

**Question 3**

> A scientific calculator is required to answer this question.

Given that the log-likelihood function for a particular sample of independent exponential random variables is
$l(θ) = 8\ln θ - 28.4θ$,
what is the maximum likelihood estimate $θ^(^)$ from this sample? Round your answer to $3$ decimal places.

- [ ] A. $0.405$
- [ ] B. $0.198$
- [ ] C. $0.215$
- [ ] D. $0.282$
- [ ] E. $0.318$

---

**Question 4**

> A scientific calculator is required to answer this question.

Given that the log-likelihood function for a particular sample of independent normal random variables is
$l(θ) =-6\ln (2π) - 45.3 + 30.2θ - 6θ^{2}$,
what is the maximum likelihood estimate $θ^(^)$ from this sample? Round your answer to $3$ decimal places.

- [ ] A. $3.141$
- [ ] B. $2.517$
- [ ] C. $1.928$
- [ ] D. $2.284$
- [ ] E. $2.905$

---

<a id="deriving-a-maximum-likelihood-estimator-for-bernoulli-distributions"></a>
## Deriving a Maximum Likelihood Estimator for Bernoulli Distributions

Let's derive a general expression for the maximum likelihood estimator for the unknown parameter $\theta$ for a set of $n$ independent and identically distributed Bernoulli random variables.

Suppose $X_1,X_2,\ldots,X_{n}$ is an I.I.D random sample, where $X_i\sim \text{Bernoulli}(\theta)$ with unknown probability of success $\theta$. For a particular sample $x_1, x_2, \ldots, x_{n}$, the log-likelihood function is given by

$$
l(\theta) = S\ln\theta + (n-S)\ln(1-\theta)
$$

where

$$
\displaystyle S = \sum_ {i=1}^n x_i
$$

is the number of successes in the sample.

To compute a general expression for the maximum likelihood estimate, we differentiate $l(\theta)$, set the derivative equal to zero, and solve for $\theta$.

First, we take the derivative of $l(\theta){:}$

$$
\begin{aligned}
\frac{dl}{dθ} &= \frac{d}{dθ}(S\ln θ + (n - S)\ln (1 - θ)) \\
&= \frac{S}{θ} + \frac{n - S}{1 - θ} \cdot (-1) \\
&= \frac{S}{θ} + \frac{n - S}{θ - 1}
\end{aligned}
$$

Then, we set the derivative equal to $0$ and solve for $\theta{:}$

$$
\begin{aligned}
\frac{S}{θ} + \frac{n - S}{θ - 1} &= 0 \\
(S(θ - 1) + (n - S)θ)/(θ(θ - 1)) &= 0 \\
S(θ - 1) + (n - S)θ &= 0 \\
Sθ - S + nθ - Sθ &= 0 \\
nθ - S &= 0 \\
nθ &= S \\
θ &= \frac{S}{n} \\
θ &= \frac{1}{n}∑_(i = 1)^(n)x_{i}
\end{aligned}
$$

Therefore, the maximum likelihood estimate of $\theta$ is

$$
\widehat{\,\theta\,} = \dfrac{1}{n} \sum_ {i=1}^n x_i
$$

which is simply the *mean of the sample*.

The maximum likelihood estimate for other common distributions can be derived similarly. Let's summarize the most important cases.

---

<a id="a-list-of-important-maximum-likelihood-estimators"></a>
## A List of Important Maximum Likelihood Estimators

The table below summarizes the maximum likelihood estimates for several common probability distributions. These results can be derived by maximizing the corresponding log-likelihood function.

| **Distribution** | **Probability Mass Function, $f(x; \theta)$** | **Parameter, $\theta$** | **Maximum Likelihood Estimate, $\widehat{\,\theta\,}$** |
| --- | ---: | ---: | ---: |
| Bernoulli | $\displaystyle \theta^x(1-\theta)^{1-x}$ | The probability of success | $\displaystyle \dfrac{1}{n}\sum_{i=1}^n x_i$<br>The mean of the sample |
| Binomial | $\displaystyle {N \choose x} \theta^x (1-\theta)^{N-x}$ | The probability of success | $\displaystyle \dfrac{1}{n}\sum_{i=1}^n x_i$<br>The mean of the sample |
| Poisson | $\displaystyle \dfrac{\theta^{x} e^{-\theta}}{x!}$ | The rate parameter | $\displaystyle \dfrac{1}{n}\sum_{i=1}^n x_i$<br>The mean of the sample |
| Geometric | $\displaystyle (1-\theta)^{x-1} \theta$ | The probability of success | $\displaystyle \bigg(\dfrac{1}{n}\sum_{i=1}^n x_i \bigg)^{-1}$<br>The reciprocal of the sample mean |
| Exponential | $\displaystyle \theta e^{-\theta x}$ | The rate parameter | $\displaystyle \bigg(\dfrac{1}{n}\sum_{i=1}^n x_i \bigg)^{-1}$<br>The reciprocal of the sample mean |
| Normal | $\displaystyle \dfrac{1}{\theta_2 \sqrt{2\pi}} e^{-\frac{1}{2} \left(\frac{x-\theta_1}{\theta_2} \right)^2}$ | The mean $(\theta_1)$<br><br>The standard deviation $(\theta_2)$ | $\displaystyle \widehat{\,\theta\,}_1 = \dfrac{1}{n}\sum_{i=1}^n x_i$<br>The mean of the sample<br><br>$\displaystyle \widehat{\,\theta\,}_2 = \sqrt{\dfrac{1}{n} \sum_{i=1}^n (x_i-\overline{x})^2}$<br>The standard deviation of the sample |

```update-progress
```

[[MA/MML/Home|Home]]
[[MA/MML/0. Table of Contents/TOC|Table of Contents]]
