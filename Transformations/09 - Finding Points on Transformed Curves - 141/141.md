# Finding Points on Transformed Curves

## Table of Contents

- [Introduction](#introduction)
- [Finding a Point That Lies on a Transformed Curve](#finding-a-point-that-lies-on-a-transformed-curve)
- [Finding a Point That Lies on a Transformed Curve Given Another Transformed Curve](#finding-a-point-that-lies-on-a-transformed-curve-given-another-transformed-curve)
- [Mapping Maxima and Minima](#mapping-maxima-and-minima)
- [Cases With Vertical Reflection](#cases-with-vertical-reflection)
- [A Summary](#a-summary)
- [Identifying Extrema on Transformed Curves](#identifying-extrema-on-transformed-curves)

## Prerequisites

- [Local Extrema of Functions](../2707/2707.md)
- [Combining Reflections With Other Graph Transformations](../2841/2841.md)

---

<a id="introduction"></a>
## Introduction

Suppose we know that the point $(-1, 2)$ lies on the graph of $y = f (x)$. What point *must* lie on the graph of

$$
y =-f (x) + 3
$$

This problem can be solved using one of two methods.

**Method 1 - Transform the Point Using Graph Transformations**

To plot

$$
y = -f(x) + 3
$$

we apply the following steps:

- First, we take the graph $y=f(x)$.
- Then, we reflect the graph across the $x$-axis and get the graph of $y=-f(x)$. After this transformation, the point $(-1,2)$ will be mapped to the point
$(-1,-(2)) = (-1,-2)$.
- Finally, we shift the result by $3$ units up and obtain the graph of $y = -f(x) + 3$. After this transformation, the point $(-1,-2)$ will be mapped to the point
$(-1,-2+3)={\color{blue}(-1,1)}$.

**Method 2 - Evaluate the Transformed Function**

Since the point $(-1, 2)$ lies on the graph of $y = f(x)$, we have that

$$
f(-1) = 2
$$

Now, consider the graph of

$$
y=-f(x)+3
$$

Substituting

$$
x = {\color{blue}-1}
$$

, we get

$$
\begin{aligned}
y &= -f(-1) + 3 \\
&= - (2) + 3 \\
&=-2 + 3 \\
&= 1
\end{aligned}
$$

Therefore, the point $\color{blue}(-1, 1)$ must lie on

$$
y = -f(x) + 3
$$

So, both methods give the same result.

---

<a id="finding-a-point-that-lies-on-a-transformed-curve"></a>
## Finding a Point That Lies on a Transformed Curve

**Example:** The point $(a,b)$ lies on the curve $y=x^4$. Which of the following points **must** lie on $y = (x-2)^4-6$?

1. $(a-2, b-6)$
2. $(a+2,b+ 6)$
3. $(a+2, b-6)$
4. $(a-2,b+ 6)$

**Explanation**

**Method 1**

To plot

$$
y = (x-2)^4-6
$$

we apply the following steps:

- First, we take the graph $y=x^4$.
- Then, we shift the graph horizontally by $2$ units to the right and get the graph of $y=(x-2)^4$. After this transformation, the point $(a,b)$ will be mapped to the point
$(a+2,b)$.
- Finally, we shift the result vertically by $6$ units down and obtain the graph of $y = (x-2)^4-6$. After this transformation, the point $(a+2,b)$ will be mapped to the point
$(a+2,b-6)$.

**Method 2**

We are told that the point $(a, b)$ lies on the graph of $y = x^4$, which means:
$b=a^4$

Now, we want to find which point must lie on the graph of:

$$
y = (x-2)^4-6
$$

Let’s test what happens when we substitute

$$
x = a + 2
$$

into this new equation:

$$
\begin{aligned}
y &= (a + 2 - 2)^{4} - 6 \\
&= a^{4} - 6 \\
&= b - 6
\end{aligned}
$$

So, when

$$
x = a + 2
$$

, we get $y = b - 6$.

Therefore, the correct answer is "III only."

---

**Question 1:**

```quiz
type: radio
id: ma-121220
content: |-
  The point $(a, b)$ lies on the curve $y = x^{2}$. Which of the following points **must** lie on $y = (x - 1)^{2} + 2$?
options:
- id: a
  content: |-
    $(a, b + 2)$
- id: b
  content: |-
    $(a - 1, b + 2)$
- id: c
  content: |-
    $(a + 2, b + 1)$
- id: d
  correct: true
  content: |-
    $(a + 1, b + 2)$
- id: e
  content: |-
    $(a, b + 3)$
```

---

**Question 2:**

```quiz
type: radio
id: ma-26398
content: |-
  The point $(1, 4)$ lies on the curve $y = f(x)$. Which of the following points **must** lie on $y = f(x - 1) + 1$?
options:
- id: a
  content: |-
    $(0, 3)$
- id: b
  content: |-
    $(2, 3)$
- id: c
  content: |-
    $(0, 5)$
- id: d
  content: |-
    $(1, 5)$
- id: e
  correct: true
  content: |-
    $(2, 5)$
```

---

<a id="finding-a-point-that-lies-on-a-transformed-curve-given-another-transformed-curve"></a>
## Finding a Point That Lies on a Transformed Curve Given Another Transformed Curve

**Example:** The point $(3, 0)$ lies on the curve $y = f(x) - 1$. What point **must** lie on $y = -f(x) + 3$?

**Explanation**

**Method 1**

To plot

$$
y = -f(x) + 3
$$

we apply the following steps:

- First, take the graph $y = f(x) - 1$.
- Then, we shift the graph by $1$ unit up and get the graph of $y=f(x)$. After this transformation, the point $(3,0)$ will be mapped to the point
$(3,0+1)=(3,1)$.
- Next, we reflect the result across the $x$-axis and get the graph of $y=-f(x)$. After this transformation, the point $(3,1)$ will be mapped to the point
$(3,-(1)) = (3,-1)$.
- Finally, shift the result by $3$ units up and obtain the graph of $y = -f(x) + 3$. After this transformation, the point $(3,-1)$ will be mapped to the point
$(3,-1+3)=(3,2)$.

**Method 2**

Since the point $(3, 0)$ lies on the graph of $y = f(x)-1$, we have that

$$
f(3)-1 = 0 \qquad\Longrightarrow\qquad f(3)=1
$$

Now consider the graph of

$$
y = -f(x) + 3
$$

Substituting $x = 3$, we get

$$
\begin{aligned}
y &= -f(3) + 3 \\
&=-1 + 3 \\
&= 2
\end{aligned}
$$

Therefore, the point $(3, 2)$ must lie on

$$
y = -f(x) + 3
$$

---

**Question 3:**

```quiz
type: radio
id: ma-26595
content: |-
  The point $(-1, 1)$ lies on the curve $y = f(x) + 1$. Which of the following points **must** lie on $y =-f(x) - 1$?
options:
- id: a
  content: |-
    $(-1, 1)$
- id: b
  correct: true
  content: |-
    $(-1,-1)$
- id: c
  content: |-
    $(1, 1)$
- id: d
  content: |-
    $(-1,-3)$
- id: e
  content: |-
    $(-1, 0)$
```

---

**Question 4:**

```quiz
type: radio
id: ma-121405
content: |-
  The point $(-2, 4)$ lies on the curve $y = 2f(x)$. Which of the following points **must** lie on $y =-f(x) + 2$?
options:
- id: a
  content: |-
    $(-3,-1)$
- id: b
  correct: true
  content: |-
    $(-2, 0)$
- id: c
  content: |-
    $(2, 3)$
- id: d
  content: |-
    $(-2, 2)$
- id: e
  content: |-
    $(2,-1)$
```

---

<a id="mapping-maxima-and-minima"></a>
## Mapping Maxima and Minima

Consider the function $y=f(x)$ shown below.

![](Source/Images/b53c8586926d67b540d201fd62ca8985.png)

This function has a local minimum point, and a local maximum point, as shown.

Notice that if we perform the graph transformation $y=f(2x)-1$, then

- the local *maximum* point on the original graph is mapped to a local *maximum* point on the transformed curve, and
- the local *minimum* point on the original graph is mapped to a local *minimum* point on the transformed curve.

![](Source/Images/bce935fca56867d04a01bc4bd9064284.png)

So, this transformation maps maximums (local and global) to maximums and minimums (local and global) to minimums.

Is this always the case? In other words, does *any* graph transformation map maximums to maximums and minimums to minimums?

To answer this, let's consider another example.

---

<a id="cases-with-vertical-reflection"></a>
## Cases With Vertical Reflection

Consider the transformation

$$
y=-f(x)+2
$$

which contains a reflection about the $x$-axis.

![](Source/Images/818f2f99d6cd5ba26f6092f0af8b81a2.png)

Notice that

- the local *maximum* point on the original graph is mapped to a local ***minimum*** point on the transformed curve, and
- the local *minimum* point on the original graph is mapped to a local ***maximum*** point on the transformed curve.

So, due to the presence of vertical reflection, this transformation maps maximums (local and global) to minimums and minimums (local and global) to maximums.

---

<a id="a-summary"></a>
## A Summary

We can summarize our findings by the following theorem:

> *If a transformation does **not** contain a reflection across the $x$-axis, then the transformation maps local (or global) maximums to local (or global) maximums, and maps local (or global) minimums to local (or global) minimums.*

In the case where we do have a vertical reflection, we have the following theorem.

> *If a transformation contains a reflection across the $x$-axis, then the transformation maps local (or global) maximums to local (or global) minimums, and maps local (or global) minimums to local (or global) maximums.*

---

<a id="identifying-extrema-on-transformed-curves"></a>
## Identifying Extrema on Transformed Curves

**Example:** The diagram above shows the local minimum and maximum of the function $y=f(x)$. What is the local maximum of $y=f(2x)-1$?

![](Source/Images/f409140a46a8034453948c18db9672e9.png)

**Explanation**

Notice that the graph of $y=f(2x)-1$ can be obtained from the graph of $y=f(x)$ by

- stretching it by a scale factor of $\dfrac{1}{2}$ parallel to the $x$-axis, and then
- translating it by $1$ unit down.

Since these transformations do not flip the graph across the $x$-axis, the combined transformation maps the local maximum of $y=f(x)$ to the local maximum of $y=f(2x)-1$. So, we need to find the image of $(2,6)$, the local maximum of $y=f(x)$, under the transformation.

To plot

$$
y = f(2x)-1
$$

we apply the following steps:

- First, we take the graph $y=f(x)$ that passes through $(2,6)$.
- Then, we stretch the result along the $x$-axis by a scale factor of $\dfrac{1}{2}$ and get the graph of $y=f(2x)$. After this transformation, the point $(2,6)$ will be mapped to the point
$\left(\dfrac{1}{2} \cdot 2, 6 \right) = (1,6)$.
- Finally, we shift the result vertically by $1$ unit downward and get the graph of $y = f(2x)-1$. After this transformation, the point $(1,6)$ will be mapped to the point
$(1,6-1) = (1,5)$.

Therefore, the local maximum of $y=f(2x)-1$ is $(1,5)$.

---

**Question 5:**

```quiz
type: radio
id: ma-121221
content: |-
  ![](Source/Images/q-121221.png)

  The diagram above shows the local minimum and maximum of the function $y = f(x)$. What is the local maximum of $y = f(x - 2) + 1$?
options:
- id: a
  correct: true
  content: |-
    $(4, 4)$
- id: b
  content: |-
    $(0, 4)$
- id: c
  content: |-
    $(3, 5)$
- id: d
  content: |-
    $(5, 3)$
- id: e
  content: |-
    $(3, 2)$
```

---

**Question 6:**

```quiz
type: radio
id: ma-186866
content: |-
  ![](Source/Images/q-186866.png)

  The diagram above shows the global minimum and maximum of the function $y = f(x)$. What is the global minimum of $y =-2f(x + 1)$?
options:
- id: a
  content: |-
    $(2, 8)$.
- id: b
  correct: true
  content: |-
    $(-2,-8)$
- id: c
  content: |-
    $(3,-4)$
- id: d
  content: |-
    $(-2, 8)$.
- id: e
  content: |-
    $(0,-4)$
```

